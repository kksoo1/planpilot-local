# PlanPilot Local 수동 테스트 체크리스트

이 문서는 자동 테스트가 아니라, 리팩터링 또는 기능 변경 후 사람이 실제 화면에서 하나씩 확인하는 수동 테스트 체크리스트다.

테스트 전 확인:

- [ ] 테스트 전 `npm run build`가 성공했다.
- [ ] 테스트 대상 변경에 `src/App.css`, DB schema, `package-lock.json` 변경이 포함되지 않았는지 확인했다.
- [ ] 테스트 중 서버 API, 로그인, 클라우드 동기화, 알림 권한 요청, Capacitor 기능을 새로 사용하지 않는다.

## 1. 앱 시작/초기 데이터

- [ ] 앱을 처음 열었을 때 하단 탭에 `오늘`, `업무`, `프로젝트`, `설정`이 보인다.
- [ ] IndexedDB 초기 상태에서 기본 프로젝트가 생성된다.
- [ ] 새로고침 후 기본 프로젝트가 유지된다.
- [ ] 화면 이동 시 앱이 흰 화면이나 오류 화면으로 멈추지 않는다.

## 2. 오늘 화면

- [ ] `오늘` 탭으로 이동할 수 있다.
- [ ] 전체 업무 수가 현재 저장된 전체 업무 개수와 일치한다.
- [ ] 완료 업무 수가 `done` 상태 업무 개수와 일치한다.
- [ ] 오늘 이전 마감일을 가진 미완료 업무가 지난 마감 업무에 표시된다.
- [ ] 완료된 지난 마감 업무는 지난 마감 업무에 표시되지 않는다.
- [ ] 오늘부터 7일 이내 마감일을 가진 미완료 업무가 7일 이내 마감 업무에 표시된다.
- [ ] 완료된 예정 업무는 7일 이내 마감 업무에 표시되지 않는다.
- [ ] 추천 업무가 우선순위와 마감일 기준으로 표시된다.
- [ ] 추천 업무가 없을 때 빈 상태 메시지가 어색하지 않게 표시된다.
- [ ] 오늘 화면의 업무 카드에서 프로젝트 이름이 올바르게 표시된다.

## 3. 전체 업무 화면

- [ ] `업무` 탭으로 이동할 수 있다.
- [ ] `새 업무 추가` 버튼으로 업무 추가 form을 열고 닫을 수 있다.
- [ ] 제목만 입력해 업무를 추가할 수 있다.
- [ ] 제목, 메모, 마감일, 중요도, 프로젝트를 입력해 업무를 추가할 수 있다.
- [ ] 빈 제목으로 업무가 추가되지 않는다.
- [ ] 추가한 업무가 전체 업무 목록에 표시된다.
- [ ] 업무 수정 버튼으로 수정 form이 표시된다.
- [ ] 업무 제목, 메모, 마감일, 중요도, 프로젝트를 수정할 수 있다.
- [ ] 수정 취소 버튼이 수정 전 상태로 돌아간다.
- [ ] 업무 삭제 버튼 클릭 시 확인 창이 표시된다.
- [ ] 삭제를 확인하면 업무가 목록에서 사라진다.
- [ ] 삭제된 업무가 오늘 화면과 프로젝트 통계에도 남지 않는다.
- [ ] 완료 버튼으로 업무를 완료 처리할 수 있다.
- [ ] 완료된 업무를 다시 미완료로 되돌릴 수 있다.
- [ ] 업무 검색은 제목 기준으로 동작한다.
- [ ] 검색어를 지우면 전체 조건에 맞는 업무가 다시 표시된다.
- [ ] `완료 업무 표시`를 끄면 완료 업무가 숨겨진다.
- [ ] `완료 업무 표시`를 켜면 완료 업무가 다시 표시된다.
- [ ] 프로젝트 필터에서 특정 프로젝트를 선택하면 해당 프로젝트 업무만 보인다.
- [ ] 프로젝트 필터를 `전체 프로젝트`로 되돌리면 전체 업무가 보인다.
- [ ] `마감일 빠른 순` 정렬에서 마감일이 있는 업무가 날짜순으로 표시된다.
- [ ] 마감일이 없는 업무는 마감일 빠른 순 정렬에서 뒤쪽에 표시된다.

## 4. 프로젝트 화면

- [ ] `프로젝트` 탭으로 이동할 수 있다.
- [ ] 프로젝트 이름만 입력해 프로젝트를 추가할 수 있다.
- [ ] 프로젝트 이름과 설명을 입력해 프로젝트를 추가할 수 있다.
- [ ] 빈 이름으로 프로젝트가 추가되지 않는다.
- [ ] 프로젝트 수정 버튼으로 이름과 설명을 수정할 수 있다.
- [ ] 프로젝트 수정 취소 버튼이 수정 전 상태로 돌아간다.
- [ ] 기본 프로젝트는 삭제되지 않는다.
- [ ] 기본 프로젝트 카드에는 삭제 불가 상태가 이해 가능하게 표시된다.
- [ ] 업무가 연결된 프로젝트는 삭제되지 않는다.
- [ ] 업무가 있는 프로젝트 카드에는 삭제 불가 상태가 이해 가능하게 표시된다.
- [ ] 업무가 없는 사용자 프로젝트는 확인 후 삭제된다.
- [ ] 프로젝트별 전체 업무 수가 실제 연결 업무 수와 일치한다.
- [ ] 프로젝트별 완료 업무 수가 실제 완료 업무 수와 일치한다.
- [ ] 프로젝트별 미완료 업무 수가 실제 미완료 업무 수와 일치한다.

## 5. 설정 화면

- [ ] `설정` 탭으로 이동할 수 있다.
- [ ] 테마 값이 표시된다.
- [ ] 언어 값이 표시된다.
- [ ] AI Provider 값이 표시된다.
- [ ] 알림 사용 여부가 표시된다.
- [ ] 첫 실행 완료 상태가 표시된다.
- [ ] 설정 화면에서 알림 권한 요청 팝업이 뜨지 않는다.
- [ ] 설정 화면에서 서버 또는 계정 로그인을 요구하지 않는다.

## 6. 데이터 유지

- [ ] 업무를 추가한 뒤 새로고침해도 업무가 유지된다.
- [ ] 프로젝트를 추가한 뒤 새로고침해도 프로젝트가 유지된다.
- [ ] 업무 수정 내용이 새로고침 후에도 유지된다.
- [ ] 프로젝트 수정 내용이 새로고침 후에도 유지된다.
- [ ] 완료/미완료 상태가 새로고침 후에도 유지된다.
- [ ] 브라우저를 종료하고 다시 열어도 IndexedDB에 저장된 업무와 프로젝트가 유지된다.
- [ ] 설정 값이 새로고침 후에도 유지된다.

## 7. 리팩터링 회귀 테스트

- [ ] `TodayView` 분리 후 오늘 화면의 통계, 추천, 지난 마감, 예정 업무 표시가 유지된다.
- [ ] `TasksView` 분리 후 업무 추가, 수정, 삭제, 완료 토글, 검색, 필터, 정렬이 유지된다.
- [ ] `ProjectsView` 분리 후 프로젝트 추가, 수정, 삭제 방지, 통계 표시가 유지된다.
- [ ] `SettingsView` 분리 후 설정 값 표시가 유지된다.
- [ ] `TaskCard` 분리 후 업무 카드의 라벨, 버튼, 프로젝트 이름 표시가 유지된다.
- [ ] `ProjectCard` 분리 후 프로젝트 카드의 설명 fallback, 통계, 버튼 상태가 유지된다.
- [ ] `TaskForm` 분리 후 업무 추가 form과 수정 form의 입력/저장/취소 동작이 유지된다.
- [ ] `ProjectForm` 분리 후 프로젝트 추가 form과 수정 form의 입력/저장/취소 동작이 유지된다.
- [ ] `taskFilters` 분리 후 검색, 완료 업무 표시, 프로젝트 필터, 마감일 정렬 결과가 이전과 같다.
- [ ] `taskDates` 분리 후 지난 마감 업무와 7일 이내 마감 업무 계산이 이전과 같다.

## 8. 날짜 처리 정책 테스트

- [ ] 마감일이 비어 있는 미완료 업무는 지난 마감 업무에 표시되지 않는다.
- [ ] 마감일이 비어 있는 미완료 업무는 7일 이내 마감 업무에 표시되지 않는다.
- [ ] invalid `dueDate`가 있는 업무는 날짜 기반 목록에서 제외된다.
- [ ] 오늘 이전 마감일의 미완료 업무는 지난 마감 업무에 표시된다.
- [ ] 오늘 마감 업무는 지난 마감 업무에 표시되지 않는다.
- [ ] 오늘 마감 업무는 7일 이내 마감 업무에 표시된다.
- [ ] 오늘부터 7일 이내 마감일의 미완료 업무는 7일 이내 마감 업무에 표시된다.
- [ ] 7일 범위를 벗어난 미래 마감 업무는 7일 이내 마감 업무에 표시되지 않는다.
- [ ] 완료된 지난 마감 업무는 지난 마감 업무에 표시되지 않는다.
- [ ] 완료된 예정 업무는 7일 이내 마감 업무에 표시되지 않는다.
- [ ] 추천 업무는 단순 날짜 목록과 별개로 우선순위와 마감일 기준 정렬을 유지한다.
- [ ] 날짜 유틸 통합 후에도 추천 업무의 기존 표시 개수와 정렬 의도가 유지된다.

## 9. 추천 업무 로직 회귀 테스트

- [ ] `done` 상태 업무는 추천 업무 목록에 표시되지 않는다.
- [ ] `high` 우선순위 업무가 동일 조건의 `medium`, `low` 업무보다 먼저 추천된다.
- [ ] `medium` 우선순위 업무가 동일 조건의 `low` 업무보다 먼저 추천된다.
- [ ] 오늘보다 이전 마감일을 가진 미완료 업무가 높은 추천 점수를 받아 상단에 표시된다.
- [ ] 오늘 마감인 미완료 업무가 마감일 점수를 받아 추천 후보에 반영된다.
- [ ] 미래 마감일 업무는 마감일 추가 점수 없이 우선순위 점수 중심으로 정렬된다.
- [ ] 마감일이 없는 업무도 미완료 상태이면 추천 후보가 될 수 있다.
- [ ] invalid `dueDate`가 있는 업무는 마감일 점수 없이 추천 후보로만 처리된다.
- [ ] 추천 점수가 같은 업무 중 둘 다 유효한 마감일이 있으면 마감일이 빠른 업무가 먼저 표시된다.
- [ ] 추천 점수가 같은 업무 중 한쪽에만 유효한 마감일이 있으면 마감일이 있는 업무가 먼저 표시된다.
- [ ] 추천 업무 기본 표시 개수는 최대 3개로 유지된다.
- [ ] 추천 점수 helper 분리 전후 추천 업무의 개수와 순서가 바뀌지 않는다.
- [ ] 추천 로직 리팩터링 후에도 지난 마감 업무 목록과 7일 이내 마감 업무 목록의 표시 기준은 바뀌지 않는다.
- [ ] comparator 분리 전후 `getTaskScore`가 높은 업무가 낮은 업무보다 먼저 표시된다.
- [ ] comparator 분리 전후 점수가 같은 업무 중 dueDate가 빠른 업무가 먼저 표시된다.
- [ ] comparator 분리 전후 점수가 같은 업무 중 dueDate가 있는 업무가 dueDate 없는 업무보다 먼저 표시된다.
- [ ] comparator 분리 전후 두 업무 모두 dueDate가 없거나 invalid이면 추천 순서가 불필요하게 뒤섞이지 않는다.
- [ ] comparator 분리 전후 invalid `dueDate` 업무는 추천 후보에서 제외되지 않고 dueDate 없는 업무처럼 정렬된다.
- [ ] comparator 분리 전후 추천 업무 최대 표시 개수 3개가 유지된다.

## 10. 금지 기능 확인

- [ ] 네트워크 탭 또는 코드 검토 기준으로 서버 API 호출이 없다.
- [ ] `localStorage`를 사용하지 않는다.
- [ ] 로그인 또는 계정 생성 UI가 없다.
- [ ] 클라우드 동기화 UI 또는 동작이 없다.
- [ ] 브라우저 알림 권한 요청이 발생하지 않는다.
- [ ] Android notification 권한 요청이 없다.
- [ ] Capacitor 관련 UI, 설정, 패키징 동작이 없다.
- [ ] 개인정보가 외부 서버로 전송되는 동작이 없다.

## 11. 테스트 완료 기록

- [ ] 테스트 날짜:
- [ ] 테스트한 브랜치:
- [ ] 테스트한 commit:
- [ ] 발견한 문제:
- [ ] 배포 또는 다음 리팩터링 진행 가능 여부:

## 12. 설정 읽기 전용 상태 확인 및 향후 편집 도입 테스트

설정 화면은 현재 MVP에서 읽기 전용 상태 확인 화면으로 동작한다.

- [ ] 설정 화면에 현재 설정 상태 섹션이 표시된다.
- [ ] 설정 화면에서 theme 값이 표시된다.
- [ ] 설정 화면에서 theme 값은 읽기 전용으로 표시된다.
- [ ] 설정 화면에서 theme 선택 컨트롤은 표시되지 않는다.
- [ ] 설정 화면에서 language 값이 표시된다.
- [ ] 설정 화면에서 AI Provider 값이 표시된다.
- [ ] 설정 화면에서 알림 사용 여부가 표시된다.
- [ ] 설정 화면에서 첫 실행 완료 상태가 표시된다.
- [ ] 설정 화면을 열기만 해도 `updatedAt`이 변경되지 않는다.
- [ ] 설정 화면에서 theme, language, AI Provider, 알림, 첫 실행 완료 값을 직접 편집할 수 없다.
- [ ] 설정 화면 표시만으로 `createdAt`은 유지된다.
- [ ] MVP 범위에서 theme, language, aiProvider, enableNotifications 선택지가 타입에 없는 값으로 확장되지 않는다.
- [ ] 알림 설정을 통해 브라우저 알림 권한 요청이 발생하지 않는다.
- [ ] 향후 설정 편집 기능을 구현하더라도 서버 API, `localStorage`, 로그인, cloud sync, Capacitor가 추가되지 않는다.

## 13. 향후 다중 테마 도입 전후 테스트

다중 테마는 현재 구현 범위가 아니며, `theme` 타입이 `light` 단일 값인 상태를 기준으로 아래 항목을 먼저 확인한다.

- [ ] 현재 `theme` 허용 값은 `light` 하나뿐이다.
- [ ] 현재 theme 값은 읽기 전용 표시용이며 실제 앱 색상 전환을 수행하지 않는다.
- [ ] `dark` 같은 신규 값을 추가하기 전 `AppSettings` 타입 확장 범위가 문서화되어 있다.
- [ ] 신규 theme 값을 추가할 경우 기본 설정 생성 값과 기존 IndexedDB 데이터 처리 기준이 정해져 있다.
- [ ] 신규 theme 값을 추가할 경우 `SettingsView` 선택지, 실제 스타일 적용 방식, `App.css` 수정 범위가 먼저 확정되어 있다.
- [ ] `App.css` 대규모 변경이나 기존 색상 체계 재설계가 필요하면 구현을 중단하고 별도 디자인 작업으로 분리한다.
- [ ] 다중 테마 구현 후 새로고침해도 선택한 theme 값이 IndexedDB에 유지된다.
- [ ] 다중 테마 구현 후 `updatedAt`은 실제 theme 변경 저장 시에만 갱신되고 `createdAt`은 유지된다.
- [ ] 다중 테마 구현 후에도 서버 API, `localStorage`, 로그인, cloud sync, 알림 권한 요청, Capacitor가 추가되지 않는다.

## 14. 향후 다국어 도입 전후 테스트

다국어 전환은 현재 구현 범위가 아니며, `language` 타입이 `ko` 단일 값인 상태를 기준으로 아래 항목을 먼저 확인한다.

- [ ] 현재 `language` 허용 값은 `ko` 하나뿐이다.
- [ ] 현재 language 표시는 실제 화면 문구 전환이 아니라 설정 값 표시용이다.
- [ ] `en` 같은 신규 값을 추가하기 전 `AppSettings` 타입 확장 범위가 문서화되어 있다.
- [ ] 신규 language 값을 추가할 경우 기본 설정 생성 값과 기존 IndexedDB 데이터 처리 기준이 정해져 있다.
- [ ] 신규 language 값을 추가할 경우 `SettingsView` 선택지와 문자열 리소스 구조가 먼저 확정되어 있다.
- [ ] 사용자-facing 문자열이 `src/i18n` 또는 `src/locales` 같은 리소스 구조로 이동될 범위가 정해져 있다.
- [ ] 영어 문자열 길이 때문에 버튼, 카드, 하단 탭, form label이 깨지지 않는지 모바일 폭에서 확인한다.
- [ ] 다국어 구현 후 새로고침해도 선택한 language 값이 IndexedDB에 유지된다.
- [ ] 다국어 구현 후 `updatedAt`은 실제 language 변경 저장 시에만 갱신되고 `createdAt`은 유지된다.
- [ ] 다국어 구현 후에도 서버 API, 외부 번역 API, `localStorage`, 로그인, cloud sync, 알림 권한 요청, Capacitor가 추가되지 않는다.

## 15. 향후 AI Provider 확장 전후 테스트

AI Provider 확장은 현재 구현 범위가 아니며, `aiProvider` 타입이 `rule_based` 단일 값인 상태를 기준으로 아래 항목을 먼저 확인한다.

- [ ] 현재 `aiProvider` 허용 값은 `rule_based` 하나뿐이다.
- [ ] `RuleBasedAIProvider`는 서버 호출 없이 로컬 업무 데이터만 사용한다.
- [ ] 외부 AI provider 추가 전 API Key 입력/저장 정책이 문서화되어 있다.
- [ ] 외부 AI provider 추가 전 개인정보 전송 범위와 사용자 고지 방식이 정해져 있다.
- [ ] API Key를 IndexedDB에 저장할 경우 저장 위험, 삭제 기능, backup/export 포함 여부가 정해져 있다.
- [ ] `localStorage`에 API Key나 endpoint를 저장하지 않는다.
- [ ] local LLM provider 추가 전 endpoint 입력 방식, CORS, 연결 실패 fallback 기준이 정해져 있다.
- [ ] provider 전환 후에도 `rule_based` fallback이 동작한다.
- [ ] AI Provider 확장 후에도 로그인, cloud sync, 알림 권한 요청, Capacitor가 추가되지 않는다.
- [ ] 네트워크 호출이 생기는 경우 사용자가 명시적으로 허용한 범위에서만 동작한다.

## 16. 향후 알림 기능 도입 전후 테스트

알림 기능은 현재 구현 범위가 아니며, `enableNotifications` 타입이 `false` 단일 값인 상태를 기준으로 아래 항목을 먼저 확인한다.

- [ ] 현재 알림 설정 필드명은 `enableNotifications`다.
- [ ] 현재 `enableNotifications` 허용 값은 `false` 하나뿐이다.
- [ ] 설정 화면은 알림 상태를 표시하지만 브라우저 알림 권한을 요청하지 않는다.
- [ ] 브라우저 `Notification` API가 호출되지 않는다.
- [ ] Android notification 권한 요청이 발생하지 않는다.
- [ ] Capacitor 알림 기능, 패키지, 설정이 추가되지 않는다.
- [ ] 값 토글만 구현할 경우 실제 알림이 울리지 않는다는 사용자-facing 문구가 준비되어 있다.
- [ ] 알림 권한 요청 시점과 사용자 고지 문구가 문서화되어 있다.
- [ ] 로컬 알림, Android 로컬 알림, 서버 push 알림의 범위가 구분되어 있다.
- [ ] 알림 설정 변경 후 새로고침해도 값이 IndexedDB에 유지된다.
- [ ] 알림 설정 변경 후 `updatedAt`은 실제 값 변경 저장 시에만 갱신되고 `createdAt`은 유지된다.
- [ ] 알림 기능 구현 후에도 서버 API, 로그인, cloud sync, `localStorage`가 추가되지 않는다.

## 17. firstLaunchCompleted UX 정책 테스트

`firstLaunchCompleted`는 내부 onboarding 상태에 가까우므로 현재 설정 화면에서는 읽기 전용으로만 확인한다.

- [ ] 현재 `firstLaunchCompleted` 기본값은 `false`다.
- [ ] 설정 화면에서 첫 실행 완료 상태가 읽기 전용으로 표시된다.
- [ ] 설정 화면에서 첫 실행 완료 상태를 직접 토글할 수 없다.
- [ ] 설정 화면에서 첫 실행 완료 상태 표시만으로 `updatedAt`이 변경되지 않는다.
- [ ] 설정 화면을 열기만 해도 `updatedAt`이 변경되지 않는다.
- [ ] 향후 onboarding 흐름이 생기면 값 변경은 해당 흐름이나 내부 로직에서만 발생한다.
- [ ] 이 값을 완전히 숨길 경우 SettingsView 제거 계획과 수동 테스트 항목 변경이 먼저 문서화되어 있다.
- [ ] onboarding 흐름이 생기기 전까지 이 값이 실제 앱 동작에 미치는 효과를 과장하지 않는다.

## 18. App form state / CRUD handler hook 분리 회귀 테스트

이 항목은 `App.tsx`에 남아 있는 업무/프로젝트 form state와 CRUD handler를 custom hook으로 분리할 때마다 확인한다. 자동 테스트가 아니라 사람이 화면에서 직접 확인하는 수동 테스트 기준이다.

- [ ] 업무 추가 form을 열고 닫을 수 있다.
- [ ] 업무 추가 form에 제목만 입력해 저장하면 업무가 추가된다.
- [ ] 업무 추가 form에 제목, 메모, 마감일, 중요도, 프로젝트를 입력해 저장하면 입력값이 그대로 반영된다.
- [ ] 빈 제목으로 업무를 추가해도 새 업무가 생성되지 않는다.
- [ ] 업무 추가 저장 후 추가 form 입력값이 초기값으로 reset된다.
- [ ] 업무 수정 버튼을 누르면 기존 제목, 메모, 마감일, 중요도, 프로젝트가 수정 form에 채워진다.
- [ ] 업무 수정 취소 시 기존 목록 표시로 돌아가고 임시 수정값이 다음 수정 form에 남지 않는다.
- [ ] 업무 수정 저장 후 변경된 값만 반영되고 다른 업무는 바뀌지 않는다.
- [ ] 업무 완료/미완료 토글이 hook 분리 전과 동일하게 동작한다.
- [ ] 업무 삭제 확인창에서 취소하면 업무가 유지된다.
- [ ] 업무 삭제 확인창에서 확인하면 업무가 목록, 오늘 화면 통계, 프로젝트 통계에서 제거된다.
- [ ] 프로젝트 추가 form에 이름만 입력해 저장하면 프로젝트가 추가된다.
- [ ] 프로젝트 추가 form에 이름과 설명을 입력해 저장하면 입력값이 그대로 반영된다.
- [ ] 빈 이름으로 프로젝트를 추가해도 새 프로젝트가 생성되지 않는다.
- [ ] 프로젝트 추가 저장 후 추가 form 입력값이 초기값으로 reset된다.
- [ ] 프로젝트 수정 버튼을 누르면 기존 이름과 설명이 수정 form에 채워진다.
- [ ] 프로젝트 수정 취소 시 기존 목록 표시로 돌아가고 임시 수정값이 다음 수정 form에 남지 않는다.
- [ ] 프로젝트 수정 저장 후 프로젝트 이름과 설명이 유지되며 연결된 업무 수 통계가 유지된다.
- [ ] 기본 프로젝트는 삭제할 수 없다.
- [ ] 업무가 연결된 프로젝트는 삭제할 수 없다.
- [ ] 업무가 없는 사용자 프로젝트는 확인 후 삭제할 수 있다.
- [ ] form state hook 분리 후에도 검색, 완료 업무 표시/숨김, 프로젝트 필터, 마감일 빠른 순 정렬이 유지된다.
- [ ] CRUD handler hook 분리 후에도 서버 API, `localStorage`, 로그인, cloud sync, 알림 권한 요청, Capacitor가 추가되지 않는다.
- [ ] 분리 작업에 `src/App.css`, DB schema, `types.ts`, `store.ts` 구조 변경이 포함되지 않는다.

## 19. useProjectActions 분리 전후 회귀 테스트

이 항목은 프로젝트 추가/수정/삭제 handler를 `useProjectActions` 같은 hook으로 옮기기 전후에 확인한다. 현재 단계에서는 코드 변경 없이 기준만 고정한다.

- [ ] 프로젝트 추가 submit이 `preventDefault()`를 유지해 화면 새로고침을 일으키지 않는다.
- [ ] 프로젝트 추가 submit handler를 hook으로 분리한 뒤에도 저장 성공 후 추가 form 입력값이 reset된다.
- [ ] 프로젝트 이름만 입력해 저장하면 프로젝트가 추가된다.
- [ ] 프로젝트 이름과 설명을 입력해 저장하면 두 값이 모두 반영된다.
- [ ] 빈 이름으로 저장하면 프로젝트가 추가되지 않는다.
- [ ] 프로젝트 추가 성공 후 추가 form 입력값이 reset된다.
- [ ] 프로젝트 수정 저장 시 이름과 설명이 반영된다.
- [ ] 프로젝트 수정 저장 후 수정 form이 닫히고 임시 입력값이 reset된다.
- [ ] 프로젝트 수정 submit handler를 hook으로 분리한 뒤에도 빈 이름 저장 방지와 저장 후 reset 시점이 유지된다.
- [ ] 프로젝트 수정 취소 시 기존 값이 유지되고 임시 입력값이 다음 수정 form에 남지 않는다.
- [ ] 기본 프로젝트 삭제 버튼은 비활성 상태로 유지된다.
- [ ] 기본 프로젝트 id가 handler로 전달되어도 삭제되지 않는다.
- [ ] 업무가 연결된 프로젝트 삭제 버튼은 비활성 상태로 유지된다.
- [ ] 업무가 연결된 프로젝트 id가 handler로 전달되어도 삭제되지 않는다.
- [ ] 업무가 없는 사용자 프로젝트는 확인창에서 확인하면 삭제된다.
- [ ] 삭제 확인창에서 취소하면 프로젝트가 유지된다.
- [ ] 프로젝트 삭제 후 프로젝트 목록, 프로젝트 통계, 업무 카드의 프로젝트 이름 표시가 깨지지 않는다.
- [ ] 삭제 가능 여부 helper를 분리한다면 `ProjectCard` disabled 기준과 handler 삭제 방지 기준이 동일하다.
- [ ] 프로젝트 삭제 가능 여부 helper 분리 후 기본 프로젝트는 버튼 비활성과 handler 방어가 같은 기준을 사용한다.
- [ ] 프로젝트 삭제 가능 여부 helper 분리 후 업무가 있는 프로젝트는 버튼 비활성과 handler 방어가 같은 기준을 사용한다.
- [ ] hook이 `addProject`, `updateProject`, `deleteProject` store action을 호출만 하고 IndexedDB 저장 로직을 중복 구현하지 않는다.
- [ ] 프로젝트 handler 분리 중 `ProjectsView`, `ProjectForm`, `ProjectCard` JSX 구조를 대규모로 재작성하지 않는다.
- [ ] 프로젝트 handler 분리 중 업무 form state나 업무 CRUD handler를 함께 수정하지 않는다.
- [ ] 분리 후에도 `src/App.css`, DB schema, `types.ts`, `store.ts` 구조 변경이 없다.

## 20. 프로젝트 hook/helper 현재 구조 회귀 테스트

이 항목은 `useProjectFormState`, `useProjectActions`, `projectDeletion` 분리 후 프로젝트 화면 동작이 유지되는지 사람이 직접 확인하기 위한 체크리스트다.

- [ ] 프로젝트 추가 form에서 이름만 입력해 저장하면 프로젝트가 생성된다.
- [ ] 프로젝트 추가 form에서 이름과 설명을 입력해 저장하면 두 값이 모두 반영된다.
- [ ] 프로젝트 추가 저장 성공 후 추가 form 입력값이 reset된다.
- [ ] 빈 이름 또는 공백 이름으로 저장하면 프로젝트가 생성되지 않는다.
- [ ] 프로젝트 수정 시작 시 기존 이름과 설명이 수정 form에 표시된다.
- [ ] 프로젝트 수정 저장 후 수정 form이 닫히고 입력값이 reset된다.
- [ ] 프로젝트 수정 취소 후 기존 프로젝트 값이 유지되고 다음 수정 form에 이전 입력값이 남지 않는다.
- [ ] 기본 프로젝트 삭제 버튼은 비활성 상태로 표시된다.
- [ ] 기본 프로젝트 id가 삭제 handler로 전달되어도 삭제되지 않는다.
- [ ] 업무가 연결된 프로젝트 삭제 버튼은 비활성 상태로 표시된다.
- [ ] 업무가 연결된 프로젝트 id가 삭제 handler로 전달되어도 삭제되지 않는다.
- [ ] 업무가 없는 사용자 프로젝트는 삭제 확인창에서 취소하면 유지된다.
- [ ] 업무가 없는 사용자 프로젝트는 삭제 확인창에서 확인하면 삭제된다.
- [ ] 삭제 가능 여부 기준은 `ProjectCard` disabled 표시와 `useProjectActions` handler 방어에서 동일하게 적용된다.
- [ ] 프로젝트 추가/수정 submit 분리 후에도 `ProjectsView`, `ProjectForm`, `ProjectCard`의 화면 구조와 문구가 유지된다.
- [ ] 프로젝트 리팩터링 후 업무 form state와 업무 CRUD handler 동작이 바뀌지 않는다.
- [ ] 리팩터링 후 `src/App.css`, DB schema, `types.ts`, `store.ts` 구조 변경이 없다.

## 21. useTaskActions 분리 전후 회귀 테스트

이 항목은 `useTaskFormState` 적용 이후, 업무 추가/수정 submit handler와 이후 확장된 업무 action handler가 `useTaskActions`에서 기존 동작을 유지하는지 확인할 수동 테스트 기준이다.

- [ ] 업무 추가 form을 열고 닫을 수 있다.
- [ ] 업무 추가 form에 제목만 입력해 저장하면 업무가 생성된다.
- [ ] 업무 추가 form에 제목, 메모, 마감일, 우선순위, 프로젝트를 입력해 저장하면 값이 모두 반영된다.
- [ ] 빈 제목 또는 공백 제목으로 저장하면 업무가 생성되지 않는다.
- [ ] 잘못된 길이의 마감일 값은 저장되지 않는다.
- [ ] 업무 추가 저장 성공 후 추가 form이 닫히고 입력값이 초기화된다.
- [ ] 업무 수정 시작 시 기존 제목, 메모, 마감일, 우선순위, 프로젝트가 수정 form에 표시된다.
- [ ] 업무 수정 취소 시 목록 표시로 돌아가고 다음 수정 form에 이전 입력값이 남지 않는다.
- [ ] 업무 수정 저장 후 변경값만 반영되고 수정 form이 닫힌다.
- [ ] 업무 수정 저장 시 빈 제목은 저장되지 않는다.
- [ ] 업무 수정 저장 시 잘못된 길이의 마감일 값은 저장되지 않는다.
- [ ] 업무 삭제 handler는 현재 `useTaskActions`가 담당한다.
- [ ] 완료/미완료 토글 handler는 현재 `useTaskActions`가 담당한다.
- [ ] 업무 삭제 확인창에서 취소하면 업무가 유지된다.
- [ ] 업무 삭제 확인창에서 확인하면 업무가 삭제되고 Today/Projects 통계가 갱신된다.
- [ ] 완료/미완료 토글 후 Today 완료 업무 수와 Projects 완료/미완료 통계가 갱신된다.
- [ ] 업무 추가/수정 submit 분리 후 검색, 완료 업무 표시/숨김, 프로젝트 필터, 마감일 빠른 순 정렬이 유지된다.
- [ ] `TasksView`, `TaskForm`, `TaskCard` JSX 구조를 대규모로 재작성하지 않는다.
- [ ] 프로젝트 hook/helper 파일은 업무 submit handler 분리 작업에서 수정하지 않는다.
- [ ] 분리 후에도 `src/App.css`, DB schema, `types.ts`, `store.ts` 구조 변경이 없다.

## 22. 업무 hook 현재 구조 확인

이 항목은 `useTaskFormState`와 `useTaskActions`가 추가된 현재 구조가 화면 동작과 맞는지 확인하기 위한 체크리스트다.

- [ ] 업무 추가 form state는 `useTaskFormState`가 담당하고, 업무 추가 submit은 `useTaskActions`가 담당한다.
- [ ] 업무 수정 form state는 `useTaskFormState`가 담당하고, 업무 수정 submit은 `useTaskActions`가 담당한다.
- [ ] 업무 추가 submit 후 추가 form이 닫히고 입력값이 reset된다.
- [ ] 업무 수정 submit 후 수정 form이 닫히고 입력값이 reset된다.
- [ ] 빈 제목과 공백 제목은 추가/수정 submit에서 저장되지 않는다.
- [ ] memo, dueDate, priority, projectId 값은 추가/수정 submit 후 기존과 동일하게 반영된다.
- [ ] 업무 삭제 handler는 `useTaskActions`가 담당한다.
- [ ] 완료/미완료 토글 handler는 `useTaskActions`가 담당한다.
- [ ] 업무 삭제 확인창에서 취소하면 업무가 유지된다.
- [ ] 업무 삭제 확인창에서 확인하면 업무가 삭제되고 Today/Projects 통계가 갱신된다.
- [ ] 완료/미완료 토글 후 Today 완료 업무 수와 Projects 완료/미완료 통계가 갱신된다.
- [ ] 업무 hook 구조 동기화 후에도 `TasksView`, `TaskForm`, `TaskCard` JSX 구조 변경이 없다.
- [ ] 문서 동기화 작업에서 `src` 코드, `App.css`, DB schema, `types.ts`, `store.ts` 변경이 없다.

## 23. 업무 삭제 handler 분리 전후 회귀 테스트

이 항목은 `handleDeleteTask`를 hook으로 옮긴 이후에도 동일하게 확인한다. 삭제 확인창 문구와 삭제/취소 흐름은 기존 동작을 유지해야 한다.

- [ ] 업무 삭제 버튼을 누르면 삭제 확인창이 표시된다.
- [ ] 삭제 확인창에서 취소하면 업무가 목록에 그대로 남아 있다.
- [ ] 삭제 확인창에서 확인하면 해당 업무가 전체 업무 목록에서 제거된다.
- [ ] 검색어가 입력된 상태에서 업무를 삭제해도 검색 결과 목록이 깨지지 않는다.
- [ ] 프로젝트 필터가 적용된 상태에서 업무를 삭제해도 필터 상태와 목록이 유지된다.
- [ ] 완료 업무 표시/숨김 상태에서 업무를 삭제해도 표시 상태가 유지된다.
- [ ] 마감일 빠른 순 정렬 상태에서 업무를 삭제해도 남은 업무 정렬이 유지된다.
- [ ] 오늘 화면의 전체 업무 수가 삭제 결과를 반영한다.
- [ ] 완료 업무를 삭제하면 오늘 화면의 완료 업무 수가 삭제 결과를 반영한다.
- [ ] 지난 마감 업무를 삭제하면 오늘 화면의 지난 마감 업무 목록과 수가 갱신된다.
- [ ] 7일 이내 마감 업무를 삭제하면 오늘 화면의 7일 이내 마감 업무 목록과 수가 갱신된다.
- [ ] 추천 업무에 포함된 업무를 삭제하면 추천 업무 목록에 삭제된 업무가 남지 않는다.
- [ ] 프로젝트 화면의 프로젝트별 전체/완료/미완료 업무 수가 삭제 결과를 반영한다.
- [ ] 업무 삭제 handler와 완료/미완료 토글 handler는 모두 `useTaskActions`가 담당한다.
- [ ] 삭제 handler hook 이동 후에도 `src/App.css`, DB schema, `types.ts`, `store.ts` 구조 변경이 없다.

## 24. 업무 완료/미완료 토글 handler 분리 전후 회귀 테스트

이 항목은 `handleToggleTaskDone`을 hook으로 옮긴 이후에도 동일하게 확인한다. 토글은 `todo`와 `done` 상태 전환만 수행해야 하며, 삭제 handler도 현재 `useTaskActions`가 담당한다.

- [ ] 미완료 업무의 완료 토글을 누르면 상태가 완료로 바뀐다.
- [ ] 완료 업무의 토글을 다시 누르면 상태가 미완료로 바뀐다.
- [ ] 완료 업무 표시가 켜진 상태에서는 완료/미완료 업무가 모두 표시된다.
- [ ] 완료 업무 표시가 꺼진 상태에서는 완료로 바꾼 업무가 목록에서 숨겨진다.
- [ ] 완료 업무 표시가 꺼진 상태에서 완료 업무를 미완료로 되돌리면 업무가 다시 표시된다.
- [ ] 검색어가 입력된 상태에서 토글해도 검색 결과 목록이 깨지지 않는다.
- [ ] 프로젝트 필터가 적용된 상태에서 토글해도 필터 상태와 목록이 유지된다.
- [ ] 마감일 빠른 순 정렬 상태에서 토글해도 남은 업무 정렬이 유지된다.
- [ ] 오늘 화면의 완료 업무 수가 완료/미완료 토글 결과를 반영한다.
- [ ] 지난 마감 업무를 완료로 바꾸면 지난 마감 업무 목록과 수에서 제외된다.
- [ ] 완료된 지난 마감 업무를 미완료로 되돌리면 지난 마감 업무 목록과 수에 다시 반영된다.
- [ ] 7일 이내 마감 업무를 완료로 바꾸면 7일 이내 마감 업무 목록과 수에서 제외된다.
- [ ] 완료된 7일 이내 마감 업무를 미완료로 되돌리면 7일 이내 마감 업무 목록과 수에 다시 반영된다.
- [ ] 완료로 바꾼 업무는 추천 업무 목록에서 제외된다.
- [ ] 미완료로 되돌린 업무는 추천 점수 기준에 따라 추천 업무 후보가 될 수 있다.
- [ ] 프로젝트 화면의 프로젝트별 전체/완료/미완료 업무 수가 토글 결과를 반영한다.
- [ ] 완료/미완료 토글 handler와 업무 삭제 handler는 모두 `useTaskActions`가 담당한다.
- [ ] 토글 handler hook 이동 후에도 `src/App.css`, DB schema, `types.ts`, `store.ts` 구조 변경이 없다.

## 25. MVP 완료 판정 전 최종 확인

이 항목은 기능 고도화에 들어가기 전 현재 MVP를 완료로 볼 수 있는지 사람이 직접 확인하기 위한 최종 체크리스트다.

수동 확인 결과는 `docs/manual-regression-test-result.md`에 기록한다. 실제 확인 전까지 전체 결과는 `미수행`으로 둔다.

- [ ] `npm run build`가 성공한다.
- [ ] Today 화면에서 전체 업무 수, 완료 업무 수, 지난 마감 업무, 7일 이내 마감 업무, 추천 업무가 표시된다.
- [ ] 전체 업무 화면에서 업무 추가, 수정, 삭제, 완료/미완료 토글이 동작한다.
- [ ] 전체 업무 화면에서 검색, 프로젝트 필터, 완료 업무 표시/숨김, 마감일 빠른 순 정렬이 동작한다.
- [ ] 프로젝트 화면에서 프로젝트 추가, 수정, 삭제가 동작한다.
- [ ] 기본 프로젝트는 삭제되지 않는다.
- [ ] 업무가 연결된 프로젝트는 삭제되지 않는다.
- [ ] 설정 화면은 theme, language, AI Provider, 알림, 첫 실행 완료 상태를 읽기 전용으로 표시한다.
- [ ] 새로고침 후 업무, 프로젝트, 설정 데이터가 IndexedDB 기준으로 유지된다.
- [ ] 서버 API, `localStorage`, 로그인, 클라우드 동기화, 알림 권한 요청, Capacitor, 외부 AI API가 추가되지 않았다.
- [ ] README, ROADMAP, memory-bank가 현재 hook/utils/view 구조와 충돌하지 않는다.

## 26. 향후 데이터 백업/내보내기 테스트

데이터 백업/내보내기는 MVP 이후 기능 고도화 후보이며, 현재 단계에서는 정책만 문서화되어 있다.

- [ ] 내보낸 JSON에 `format`, `schemaVersion`, `exportedAt`이 포함된다.
- [ ] 내보낸 JSON에 `tasks`, `projects`, `appSettings`가 포함된다.
- [ ] 내보낸 업무 수가 전체 업무 화면의 업무 수와 일치한다.
- [ ] 내보낸 프로젝트 수가 프로젝트 화면의 프로젝트 수와 일치한다.
- [ ] 내보낸 설정 값이 설정 화면의 읽기 전용 값과 일치한다.
- [ ] 내보내기 후 기존 IndexedDB 데이터가 변경되지 않는다.
- [ ] 내보내기 후 새로고침해도 기존 데이터가 유지된다.
- [ ] 잘못된 JSON 파일 가져오기 시 기존 데이터가 유지된다.
- [ ] 지원하지 않는 `format` 또는 `schemaVersion` 파일은 가져오기를 중단한다.
- [ ] 필수 필드가 빠진 파일은 가져오기를 중단한다.
- [ ] 가져오기 취소 시 기존 데이터가 유지된다.
- [ ] 가져오기 실패 시 기존 데이터가 유지된다.
- [ ] 가져오기 성공 후 새로고침해도 데이터가 유지된다.
- [ ] 백업/내보내기 기능 구현 후에도 서버 API, `localStorage`, 로그인, 클라우드 동기화, 알림 권한 요청, Capacitor가 추가되지 않는다.

### 26.1 JSON export 전용 피드백 테스트

- [x] 설정 화면 또는 백업 섹션에 JSON export 버튼이 표시된다.
- [x] export 버튼 클릭 시 JSON 파일 다운로드가 시작된다.
- [x] 다운로드 파일명이 앱 이름과 export 날짜를 식별할 수 있는 `planpilot-backup-YYYY-MM-DD.json` 형식이다.
- [x] JSON 파일에 `format`, `schemaVersion`, `exportedAt`, `tasks`, `projects`, `appSettings`가 포함된다.
- [x] export 성공 후 버튼 근처에 성공 메시지가 표시된다.
- [x] export 성공 후 기존 IndexedDB 데이터가 변경되지 않는다.
- [ ] export 성공 메시지가 서버 전송이나 클라우드 백업으로 오해되지 않는다. (확인 필요: 이번 기록에는 포함되지 않음)
- [ ] export 실패 상황은 일반 수동 재현이 어려우면 보류로 기록한다. (보류: 실패 상황을 수동 재현하지 못함)
- [ ] export 실패 시 기존 IndexedDB 데이터가 변경되지 않는다. (확인 필요: 실패 상황 미재현)
- [ ] export 기능 구현 후에도 JSON import 기능, 서버 API, `localStorage`, 로그인, 클라우드 동기화가 추가되지 않는다. (확인 필요: 이번 기록에는 포함되지 않음)

### 26.2 JSON export 실패 경로 확인 기준

일반 브라우저 수동 테스트에서는 JSON export 실패 상황을 안정적으로 재현하기 어렵다. 실패 경로는 다음 기준으로 보류 또는 별도 테스트 대상으로 기록한다.

- [ ] 데이터 직렬화 실패를 재현할 수 있는 테스트 절차가 있다. (현재 보류)
- [ ] Blob 생성 실패를 재현할 수 있는 테스트 절차가 있다. (현재 보류)
- [ ] object URL 생성 실패를 재현할 수 있는 테스트 절차가 있다. (현재 보류)
- [ ] 다운로드 링크 클릭 실패 또는 브라우저 다운로드 차단 상황을 재현할 수 있는 테스트 절차가 있다. (현재 보류)
- [ ] 알 수 없는 예외가 발생해도 SettingsView에 실패 메시지가 표시된다. (현재 코드 경로 확인 필요, 수동 재현 보류)
- [ ] 실패 상황에서도 기존 IndexedDB 데이터가 변경되지 않는다. (현재 실패 재현 보류)
- [ ] 실패 경로 검증을 위해 테스트 hook, download helper 분리, 단위 테스트 도입이 필요한지 별도 정책으로 결정한다.
- [ ] 실패 경로 검증 중에도 JSON import/복원 기능은 추가하지 않는다.

## 27. 향후 오류/로딩/빈 상태 테스트

이 항목은 오류/로딩/빈 상태 보강 기능을 실제 구현한 뒤 사람이 화면에서 확인할 수동 테스트 기준이다. 현재 단계에서는 정책 문서화만 완료되어 있으며, 구현 전까지는 미수행으로 둔다.

- [ ] 앱 초기 데이터 로딩 중 화면이 깨지지 않는다.
- [ ] IndexedDB 초기화 후 기본 프로젝트가 유지된다.
- [ ] 업무가 하나도 없을 때 빈 상태가 오류처럼 보이지 않는다.
- [ ] 검색 결과가 없을 때 검색 결과 없음 상태가 표시된다.
- [ ] 프로젝트 필터 결과가 없을 때 필터 결과 없음 상태가 표시된다.
- [ ] 완료 업무 표시/숨김 조건 때문에 표시할 업무가 없을 때 빈 상태가 표시된다.
- [ ] 추천 업무가 없을 때 추천 업무 없음 상태가 표시된다.
- [ ] 지난 마감 업무가 없을 때 빈 상태가 표시된다.
- [ ] 7일 이내 마감 업무가 없을 때 빈 상태가 표시된다.
- [ ] 추천 업무 생성 실패가 발생해도 업무/프로젝트 CRUD 화면이 막히지 않는다.
- [ ] IndexedDB 읽기/쓰기 오류는 일반 수동 재현이 어려우므로 별도 개발용 재현 절차가 준비될 때까지 보류로 기록한다.
- [ ] JSON 내보내기 실패 상태는 JSON 내보내기 기능 구현 후 확인한다.
- [ ] JSON 가져오기 파일 파싱 실패 상태는 JSON 가져오기 기능 구현 후 확인한다.
- [ ] 오류/로딩/빈 상태 보강 후에도 서버 API, `localStorage`, 로그인, 클라우드 동기화, 알림 권한 요청, Capacitor가 추가되지 않는다.

### 27.1 최소 empty 상태 보강 후 우선 확인

다음 코드 작업에서 전체 오류/로딩 체계를 구현하지 않고 빈 상태만 보강할 경우 아래 항목을 우선 확인한다.

- [ ] 업무가 없을 때 전체 업무 화면이 정상 빈 상태로 보인다.
- [ ] 검색 결과가 없을 때 검색 결과 없음 상태가 업무 없음 상태와 구분된다.
- [ ] 프로젝트 필터 결과가 없을 때 필터 결과 없음 상태가 표시된다.
- [ ] 추천 업무가 없을 때 추천 업무 없음 상태가 표시된다.
- [ ] 지난 마감 업무가 없을 때 빈 상태가 표시된다.
- [ ] 7일 이내 마감 업무가 없을 때 빈 상태가 표시된다.
- [ ] 최소 empty 상태 보강 후에도 IndexedDB 오류 UI, CRUD 실패 error boundary, 전역 loading overlay가 새로 추가되지 않는다.
- [ ] JSON export 구현 후에는 export 성공/실패 메시지를 별도 항목으로 확인한다.
