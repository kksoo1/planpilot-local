# AI Dev Diff

## Generated At

2026-06-09 23:47:39

## Git Status

```text
 M .ai-dev/codex-result.md
 M .ai-dev/codex-review-result.md
 M .ai-dev/current-task-prompt.md
 M .ai-dev/diff.md
 M .ai-dev/goal.md
 M .ai-dev/queue.json
 M .ai-dev/review-prompt.md
 M .ai-dev/review-response.json
 M .ai-dev/review.md
 M .ai-dev/state.json
 M .ai-dev/test-result.md
 M src/views/TasksView.tsx
```

## Unstaged Diff Stat

```text
 .ai-dev/codex-result.md        |  6051 +-----
 .ai-dev/codex-review-result.md | 40442 +++++++--------------------------------
 .ai-dev/goal.md                |    88 +-
 .ai-dev/queue.json             |   180 +-
 .ai-dev/state.json             |    16 +-
 src/views/TasksView.tsx        |     2 +-
 6 files changed, 7456 insertions(+), 39323 deletions(-)
```

## Unstaged Diff

```text
diff --git a/.ai-dev/codex-result.md b/.ai-dev/codex-result.md
index 5566d61..a74bac9 100644
--- a/.ai-dev/codex-result.md
+++ b/.ai-dev/codex-result.md
@@ -2,8 +2,8 @@
 
 ## Run
 
-- Started at: 2026-06-09 22:57:17
-- Ended at: 2026-06-09 23:00:32
+- Started at: 2026-06-09 23:46:33
+- Ended at: 2026-06-09 23:47:26
 - Exit code: 0
 - Prompt: .ai-dev/current-task-prompt.md
 - Command: codex exec <short wrapper pointing to .ai-dev/current-task-prompt.md>
@@ -26,17 +26,17 @@ approval: never
 sandbox: workspace-write [workdir, /tmp, $TMPDIR]
 reasoning effort: medium
 reasoning summaries: none
-session id: 019eacac-5c99-7953-8b94-55b60b2b4afb
+session id: 019eacd9-78e5-7a03-8cad-9754f329cfa3
 --------
 user
 Read and follow the full task prompt at this absolute file path: D:\ai-apps\planpilot-local\.ai-dev
 \current-task-prompt.md
 codex
-지정된 작업 프롬프트를 먼저 읽고, 그 안의 범위와 제약을 확인한 뒤 필요한 파일만 추가로 살펴보겠습니다.
+작업 지시 파일을 먼저 읽고, 그 안의 범위와 제약을 확인한 뒤 필요한 파일만 추가로 보겠습니다.
 exec
 "C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
  'D:\\ai-apps\\planpilot-local\\.ai-dev\\current-task-prompt.md'" in D:\ai-apps\planpilot-local
- succeeded in 1677ms:
+ succeeded in 1140ms:
 # Current Task Prompt
 
 ## Role
@@ -45,2799 +45,42 @@ exec
 
 ## Goal
 
-# Goal
+# 목표
+업무 검색창 placeholder 문구를 더 명확한 한국어 문구로 개선한다.
 
-AI Dev Loop Codex CLI 완전 자동화 도입
+## 배경
+현재 업무 검색창의 안내 문구가 사용자가 무엇을 검색할 수 있는지 충분히 분명하지 않을 수 있다.
 
-## Background
+## 성공 기준
+- 업무 검색창 placeholder가 자연스럽고 명확한 한국어 문구로 변경된다.
+- 변경 범위는 placeholder 문구 수정에 한정된다.
+- 기존 검색 동작과 상태 관리 방식은 유지된다.
 
-Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완료 처리를 가능한 한 end-to-end로 자동화한다.
+## 제약사항
+- 기존 코드 스타일과 컴포넌트 구조를 따른다.
+- 사용자-facing UI 문자열은 한국어를 사용한다.
+- 불필요한 리팩터링이나 구조 변경은 하지 않는다.
+- 한 번에 하나의 작은 변경만 수행한다.
 
-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생
-성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
-
-초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commit 
-금지 정책을 적용한다.
-
-## Success Criteria
-
-- Codex CLI 실행 정책이 문서화된다.
-- `current-task-prompt.md`를 Codex CLI에 전달하는 `ai-dev-run-codex.ps1`이 추가된다.
-- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-review-codex.ps1`이 추가된다.
-- Codex 구현, build/check, diff, Codex 리뷰, save-review 흐름이 연결된다.
-- pass 리뷰에서만 자동 커밋과 complete-task를 수행하는 full cycle 초안이 추가된다.
-- git status가 dirty이면 Codex 실행을 중단한다.
-- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-- build 실패 시 자동 커밋하지 않는다.
-- git reset, git clean, npm install은 자동 실행하지 않는다.
-- PowerShell 문법 검증이 통과한다.
-- 작은 앱 기능 task로 MaxTasks 1 end-to-end 검증이 가능하다.
-
-## Constraints
-
-- Codex CLI는 구현자와 리뷰어로 사용한다.
-- GPT API 키 없이 진행한다.
-- Cline은 삭제되어 사용하지 않는다.
-- Copilot CLI와 gh는 현재 사용하지 않는다.
-- 자동화는 명시적 Allow 옵션 없이는 위험한 단계를 실행하지 않는다.
-- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
-- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-- build/check 실패 시 자동 커밋하지 않는다.
-- git reset, git clean, npm install은 자동 실행하지 않는다.
-- src 코드는 각 구현 task에서만 수정한다.
-
-## Out of Scope
-
-- GPT API 직접 호출
-- Cline 사용
-- Copilot CLI 사용
-- gh 또는 GitHub PR 자동 연동
-- git push 자동화
-- git reset/git clean 같은 destructive git 명령
-- npm install 자동 실행
-- package 대량 교체
-- DB 삭제, 초기화, 복원, 마이그레이션 자동화
-
-## Manual Verification
-
-- Codex CLI 실행 정책이 문서화되어 있다.
-- Codex 구현 실행 스크립트가 DryRun과 dirty worktree 중단을 지원한다.
-- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
-- full auto-cycle DryRun이 전체 단계를 보여준다.
-- full auto-cycle은 MaxTasks 1 제한과 Allow 옵션을 가진다.
-- pass 리뷰가 아니면 자동 커밋하지 않는다.
-- build 실패 시 자동 커밋하지 않는다.
-- package 파일 변경 감지 시 자동 커밋하지 않는다.
-- PowerShell 문법 검증이 통과한다.
-- 작은 task로 MaxTasks 1 end-to-end 검증이 가능하다.
+## 범위 제외
+- 검색 기능의 동작 변경은 제외한다.
+- 새 화면, 새 상태, 새 저장 구조 추가는 제외한다.
+- 디자인 전반 수정은 제외한다.
 
+## 수동 검증
+- 업무 화면의 검색창에 더 명확한 placeholder 문구가 표시되는지 확인한다.
+- 검색어 입력과 기존 필터링 동작이 그대로 유지되는지 확인한다.
 
 ## Current Task
 
-- Task ID: T007
-- Title: 목표 입력 기반 자동 goal 실행 스크립트 추가
-- Description: 사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/queue/state를 생성하고, current
--task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다.
+- Task ID: T001
+- Title: 업무 검색창 placeholder 문구 수정
+- Description: 업무 검색창 입력 요소를 찾아 placeholder 문구만 더 명확한 한국어 표현으로 변경한다.
 - Type: implementation
-- Status: in_progress
-- Priority: P0
-- Depends on:
-- T005
-
-## Task Scope
-
-- 현재 task만 수행한다.
-- 현재 task 범위를 벗어나는 리팩터링을 하지 않는다.
-- 다음 task를 미리 구현하지 않는다.
-- 현재 task의 완료 기준과 검증 방법을 먼저 확인한다.
-
-## Likely Files
-
-- scripts/ai-dev-auto-goal.ps1
-- .ai-dev/README.md
-
-## Verification
-
-- DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인
-- Json 출력이 가능한지 확인
-- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-- auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인
-- GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인
-- PowerShell 문법 검증
-
-- 필요한 경우 `npm run build`는 사람이 별도로 실행한다.
-- 이 프롬프트는 자동으로 build, test, lint를 실행하라고 지시하지 않는다.
-
-## Required Output
-
-- 변경한 파일 목록
-- 구현 또는 분석 내용 요약
-- 검증 방법
-- 남은 위험
-- 다음 task로 넘어가도 되는지 여부
-- `.ai-dev/loop-log.md`에 기록할 작업 요약
-
-## Hard Rules
-
-- `src` 외 파일을 수정해야 하는 경우 이유를 기록한다.
-- `package.json`과 `package-lock.json`은 수정하지 않는다. 꼭 필요하면 중단하고 이유만 기록한다.
-- 실제 DB 삭제 또는 초기화를 하지 않는다.
-- 사용자 데이터 복원 또는 덮어쓰기를 하지 않는다.
-- 현재 task와 무관한 UI 전면 개편을 하지 않는다.
-- 대규모 리팩터링을 하지 않는다.
-- git commit을 실행하지 않는다.
-- git reset, git checkout, git clean을 실행하지 않는다.
-- npm install을 실행하지 않는다.
-
-## Stop Conditions
-
-- 요구사항이 충돌하는 경우
-- 현재 task 범위 밖 수정이 필요한 경우
-- 패키지 추가가 필요한 경우
-- 데이터 삭제 또는 마이그레이션이 필요한 경우
-- 같은 오류가 반복되는 경우
-
-codex
-작업 범위는 `scripts/ai-dev-auto-goal.ps1` 추가와 `.ai-dev/README.md` 갱신입니다. 기존 자동화 스크립트의 옵션 이름과 JSON 파일 형식
-을 맞춰야 하므로 관련 스크립트와 문서만 확인하겠습니다.
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
- 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md'" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -LiteralPa
-th 'D:\\ai-apps\\planpilot-local\\scripts' | Select-Object -ExpandProperty Name" in D:\ai-apps\plan
-pilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -LiteralPa
-th 'D:\\ai-apps\\planpilot-local\\.ai-dev' | Select-Object -ExpandProperty Name" in D:\ai-apps\plan
-pilot-local
- succeeded in 2705ms:
-ai-dev-auto-cycle-full.ps1
-ai-dev-auto-cycle.ps1
-ai-dev-auto-goal.ps1
-ai-dev-auto-step.ps1
-ai-dev-check.ps1
-ai-dev-commit.ps1
-ai-dev-complete-task.ps1
-ai-dev-copy-review-prompt.ps1
-ai-dev-current-task.ps1
-ai-dev-env.ps1
-ai-dev-init.ps1
-ai-dev-make-prompt.ps1
-ai-dev-make-review-prompt.ps1
-ai-dev-make-revise-prompt.ps1
-ai-dev-manual-cycle.ps1
-ai-dev-next.ps1
-ai-dev-run-codex.ps1
-ai-dev-run-review-codex.ps1
-ai-dev-save-diff.ps1
-ai-dev-save-review.ps1
-ai-dev-status.ps1
-monitor-and-dev-loop.ps1
-
- succeeded in 2803ms:
-# PlanPilot Local AI 개발 입력 폴더
-
-이 폴더는 PlanPilot Local의 Level 5 AI 자동 개발 루프에서 목표와 후속 작업 후보를 관리하기 위한 기본 공간이다.
-
-현재 단계에서는 사용자 입력 템플릿과 수동 AI Dev 루프 보조 스크립트를 제공한다. Codex CLI 완전 자동화 실행기는 다음 목표에서 단계적으로 도입하며, 각 task 전
-환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-
-## 사람이 직접 관리하는 파일
-
-### `goal.md`
-
-현재 자동 개발 루프가 수행할 하나의 목표를 작성한다.
-
-- 목표는 결과 중심으로 작성한다.
-- 완료 기준, 제약 조건, 범위 밖 항목, 수동 검증 방법을 함께 적는다.
-- 한 번에 하나의 명확한 목표만 유지한다.
-- 저장소의 `AGENTS.md`와 `docs/ai-dev-loop-policy.md`를 위반하는 목표는 실행하지 않는다.
-
-### `backlog.md`
-
-추후 검토할 목표 후보와 아이디어를 우선순위별로 기록한다.
-
-- backlog 항목은 자동 실행이 확정된 task가 아니다.
-- 현재 단계에서는 AI가 backlog에서 다음 목표를 자동 선택하지 않는다.
-- 실행할 항목은 사용자가 검토한 뒤 `goal.md`로 옮긴다.
-
-## AI가 추후 생성하거나 수정할 파일
-
-실제 자동 개발 루프를 구현하는 단계에서는 AI가 다음 파일을 생성하거나 갱신할 수 있다.
-
-- `plan.md`
-- `queue.json`
-- `state.json`
-- `test-result.md`
-- `review.md`
-- `loop-log.md`
-
-이 파일들은 목표 분석, task queue, 실행 상태, 검증 결과, 리뷰 결과, 실행 이력을 기록하기 위한 파일이다.
-
-현재 단계에서는 위 파일을 만들지 않는다.
-
-## 구조 예시 파일
-
-- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시다.
-- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시다.
-
-예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복사 원본으로 사용한다.
-
-## UTF-8 콘솔 설정
-
-AI Dev PowerShell 스크립트의 UTF-8 콘솔 설정은 `scripts/ai-dev-env.ps1`에서 자동 처리한다. 사용자가 매번 `chcp 65001`을 직접 실
-행할 필요는 없다.
-
-기존에 한글이 깨진 `.ai-dev/*.md` 상태 파일은 필요한 경우 다음 명령으로 재초기화할 수 있다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-init.ps1 -Force
-```
-
-## 실행 파일 초기화
-
-프로젝트 루트에서 다음 명령을 실행하면 실제 실행용 상태 파일과 기본 Markdown 템플릿을 생성한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1
-```
-
-이미 생성된 파일을 덮어쓰려면 `-Force` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1 -Force
-```
-
-초기화 스크립트는 Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
-
-## 현재 task 확인
-
-`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1
-```
-
-현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1 -Json
-```
-
-현재 task 확인 스크립트는 상태 파일을 수정하지 않으며, Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
-
-## 현재 task 프롬프트 생성
-
-`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai-dev/current-task-prompt.md` 파일을 
-생성한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
-```
-
-프롬프트 생성 스크립트는 기존 `current-task-prompt.md`를 현재 task 기준으로 덮어쓴다. Codex 실행, GPT 리뷰, build/test 실행, git 
-commit은 수행하지 않는다.
-
-기본 추가 지시 파일인 `.ai-dev/extra-instructions.md`가 있으면 해당 내용을 프롬프트 끝의 `Extra Instructions` 섹션에 포함한다. 이 파
-일이 없으면 기존과 동일하게 goal, queue, state만 사용해 프롬프트를 생성한다.
-
-다른 추가 지시 파일을 사용하려면 `-ExtraInstructionsPath`를 지정한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1 -ExtraInstructionsPath .ai-
-dev/extra-instructions.example.md
-```
-
-추가 지시는 goal, queue, state 내용을 덮어쓰지 않는다. `current-task-prompt.md`를 직접 수정하지 말고, 반복해서 필요한 보조 지시는 `.ai-
-dev/extra-instructions.md` 또는 별도 파일에 작성한다.
-
-## task 완료 처리
-
-현재 task를 완료 처리하고 다음 pending task로 이동하려면 다음 명령을 실행한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary "T001 분석 완
-료"
-```
-
-특정 task를 완료 처리하려면 `-TaskId`를 지정한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -TaskId T001 -ResultSumma
-ry "분석 완료"
-```
-
-다음 task로 자동 이동하지 않으려면 `-NoNext` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -NoNext
-```
-
-task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행, 
-git commit은 수행하지 않는다.
-
-## 프로젝트 검증 실행
-
-`package.json`에 정의된 build, test, lint script를 확인하고 실행 결과를 `test-result.md`와 `state.json`에 기록한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1
-```
-
-build만 실행하려면 `-BuildOnly` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -BuildOnly
-```
-
-필요한 검증을 개별적으로 건너뛰려면 `-SkipBuild`, `-SkipTest`, `-SkipLint` 옵션을 사용한다.
-
-검증 실행 스크립트는 Codex 실행, GPT 리뷰, git commit을 수행하지 않는다.
-
-T006 같은 최종 검증 task에서 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 결
-과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build/test/lint를 실행하지 않는다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -ManualSummaryOnly -ManualSummary
- "PowerShell 문법 검증 통과. auto-step DryRun 통과. auto-cycle DryRun 통과. save-review 실패/성공 흐름 확인."
-```
-
-수동 요약 모드는 실제 명령 실행 결과를 조작하는 용도가 아니다. 실행하지 않은 검증은 통과로 적지 말고, 수행한 명령과 결과만 요약한다.
-
-이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다. 실제 검증 통과/실패/미수행 판단은 `test-result
-.md`의 요약 본문에 명확히 적는다.
-
-## git diff 저장
-
-현재 git 변경사항을 `diff.md`에 저장해 GPT 리뷰 또는 사람 검토에 사용할 수 있게 한다. 기본 동작은 untracked 파일명을 git status에 표시하지만 파
-일 내용은 포함하지 않는다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1
-```
-
-추적되지 않은 작은 텍스트 파일의 내용도 포함하려면 `-IncludeUntrackedContent` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1 -IncludeUntrackedContent
-```
-
-git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행, git commit은 수행하지 
-않는다.
-
-다음 생성 산출물은 자기 중첩과 과도한 크기 증가를 줄이기 위해 Unstaged/Staged Diff와 Diff Stat 대상에서 기본 제외한다. tracked 변경 파일은 `S
-kipped Generated AI Dev Artifact Diffs` 섹션에 파일명만 기록한다. `-IncludeUntrackedContent`를 사용한 경우 untracked
- 생성 산출물도 내용 포함에서 제외하고 `Skipped Generated AI Dev Artifacts` 섹션에 파일명만 기록한다.
-
-- `.ai-dev/diff.md`
-- `.ai-dev/review-prompt.md`
-- `.ai-dev/current-task-prompt.md`
-- `.ai-dev/revise-prompt.md`
-- `.ai-dev/test-result.md`
-- `.ai-dev/review.md`
-- `.ai-dev/review-response.json`
-
-git diff/stat 명령의 stderr 경고는 diff 본문과 분리한다. git 명령이 실패하면 기존처럼 `state.json`에 실패 결과를 기록하고 종료한다.
-
-`Git Status` 섹션에는 생성 산출물을 포함한 전체 변경 파일을 그대로 표시한다. `.ai-dev/README.md`, `scripts/ai-dev-save-diff.ps
-1`처럼 실제 작업 대상 파일은 위 제외 목록에 없으므로 diff 본문에 포함된다.
-
-## GPT 리뷰 프롬프트 생성
-
-현재 goal, task, 검증 결과, git diff를 포함한 GPT 코드 리뷰용 `review-prompt.md`를 생성한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-review-prompt.ps1
-```
-
-더 엄격한 판정 기준을 포함하려면 `-Strict` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-review-prompt.ps1 -Strict
-```
-
-리뷰 프롬프트 생성 스크립트는 GPT API 호출, Codex 실행, build/test 실행, git commit을 수행하지 않는다.
-
-## 수동 GPT 리뷰 브리지
-
-GPT API 키가 없는 환경에서는 ChatGPT 웹 화면을 사용해 리뷰를 수동으로 진행한다. AI Dev Loop는 prompt 생성과 저장 흐름만 돕고, ChatGPT 웹 화
-면에 자동 접속하거나 GPT API를 직접 호출하지 않는다.
-
-기본 흐름:
-
-1. `ai-dev-make-review-prompt.ps1 -Strict`로 `.ai-dev/review-prompt.md`를 생성한다.
-2. `review-prompt.md` 내용을 클립보드에 복사한다.
-3. 사용자가 ChatGPT 웹 화면에 직접 붙여넣는다.
-4. ChatGPT가 JSON 리뷰만 출력하도록 한다.
-5. 사용자가 JSON 리뷰를 클립보드에 복사한다.
-6. `ai-dev-save-review.ps1 -FromClipboard`로 리뷰를 저장한다.
-7. 저장된 decision에 따라 `pass`, `revise`, `blocked` 흐름을 계속 진행한다.
-
-현재 task의 다음 단계에서는 `review-prompt.md` 클립보드 복사를 돕는 helper를 추가할 예정이다. 그 전까지는 파일 내용을 직접 복사하거나 운영 환경에 맞는
- 안전한 방법으로 클립보드에 옮긴다.
-
-`review-prompt.md`가 이미 있으면 다음 명령으로 내용을 클립보드에 복사한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
-```
-
-`review-prompt.md`가 없을 때 생성 후 복사하려면 `-GenerateIfMissing`을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -GenerateIfMissing
-```
-
-엄격한 리뷰 프롬프트를 생성한 뒤 복사하려면 `-Strict`를 함께 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -GenerateIfMissing -
-Strict
-```
-
-자동화에서 결과만 읽고 싶으면 `-Json` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -Json
-```
-
-복사 후에는 ChatGPT 웹 화면에 붙여넣고, JSON 리뷰만 출력하도록 요청한다. 리뷰 JSON을 받은 뒤에는 아래 명령으로 저장한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
-```
-
-주의:
-
-- `review-prompt.md`에는 diff와 테스트 결과가 포함될 수 있다.
-- API Key, 개인정보, 백업 데이터, 민감한 로컬 경로가 포함되어 있으면 ChatGPT에 붙여넣지 않는다.
-- prompt가 너무 길거나 민감 정보가 의심되면 diff 범위를 줄인 뒤 다시 생성한다.
-- `ai-dev-auto-step.ps1`는 `ask_gpt_review` 상태에서 GPT API를 호출하지 않고 수동 브리지 안내만 해야 한다.
-
-## GPT 리뷰 결과 저장
-
-GPT Chat 또는 외부 리뷰어가 반환한 리뷰 JSON을 `review.md`에 저장하고, decision과 severity를 `state.json`에 반영한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-
-response.json
-```
-
-JSON 문자열이나 클립보드 내용을 직접 사용할 수도 있다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewJson '{"decision":"p
-ass","severity":"none","summary":"OK","required_changes":[],"optional_suggestions":[],"next_step":"
-complete_task"}'
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
-```
-
-ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboard`를 실행한다. 순수 JSON 객체, `json` 코드블
-록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-
-저장 실패 시 확인할 항목:
-
-- 클립보드가 비어 있지 않은지 확인한다.
-- ChatGPT 응답에서 JSON 객체 전체를 다시 복사한다.
-- Markdown 코드블록을 포함해도 되지만, 가능하면 JSON만 복사한다.
-- 오류 메시지의 입력 preview 앞부분을 보고 다른 설명 문장이나 잘린 JSON이 섞였는지 확인한다.
-
-파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수정하지 않는다. review-prompt나 명령어를 실수로 
-클립보드에 둔 상태에서 `-FromClipboard`를 실행해도 기존 리뷰 상태를 blocked/critical로 덮어쓰지 않기 위한 안전 동작이다.
-
-실패 자체를 리뷰 결과로 기록해야 하는 특별한 경우에만 `-WriteFailureReview`를 함께 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard -WriteFailur
-eReview
-```
-
-잘못 실행했다면 ChatGPT가 출력한 JSON 리뷰만 다시 클립보드에 복사한 뒤 `-FromClipboard`를 재실행한다.
-
-리뷰 결과 저장 스크립트는 `pass + complete_task`, `revise + revise_with_codex`, `blocked` 또는 `stop_for_user` 결
-과에 맞는 다음 행동을 안내만 한다. GPT API 호출, Codex 실행, build/test 실행, git commit은 수행하지 않는다.
-
-## 리뷰 반영 재수정 프롬프트 생성
-
-`review.md`의 required changes를 반영하도록 Codex 또는 Cline에 전달할 `revise-prompt.md`를 생성한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1
-```
-
-optional suggestions도 현재 task 범위 안에서 반영할 수 있게 하려면 `-AllowOptionalSuggestions` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1 -AllowOptionalSugges
-tions
-```
-
-재수정 프롬프트 생성 스크립트는 Codex 실행, GPT API 호출, build/test 실행, git commit을 수행하지 않는다.
-
-## task 단위 자동 커밋
-
-검증과 리뷰를 통과한 현재 변경사항을 task 단위로 커밋한다. 먼저 `-DryRun`으로 커밋 대상과 메시지를 확인하는 것을 권장한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -DryRun
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1
-```
-
-지정한 파일만 커밋 대상으로 확인하거나 실제로 커밋하려면 `-Files` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.ps1
-,.ai-dev/README.md -DryRun
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.ps1
-,.ai-dev/README.md
-```
-
-커밋 메시지를 직접 지정하거나 검증·리뷰 게이트를 명시적으로 우회할 수도 있다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Message "chore(ai-dev): add aut
-omation helpers"
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -AllowWithoutPassedCheck -AllowW
-ithoutPassedReview
-```
-
-`-Files`를 지정하지 않으면 자동 커밋 스크립트는 기존처럼 `git add -A`로 현재 작업 트리의 모든 변경사항을 stage한다. `-Files`를 지정하면 각 파일이 
-존재하고 변경사항이 있는지 확인한 뒤 `git add -- <files>`로 지정 파일만 stage한다. 선택 파일 외에 이미 staged 된 파일이 있으면 안전을 위해 중단한다
-.
-
-실행 전 예상하지 못한 사용자 변경이 없는지 반드시 확인한다. Codex 실행, GPT API 호출, build/test 실행은 수행하지 않는다.
-
-## 실행 산출물 커밋 기준
-
-AI Dev Loop의 실제 기능 변경과 `.ai-dev` 실행 상태는 가능한 한 별도 커밋으로 관리한다.
-
-- 기능 커밋에는 현재 task의 코드와 직접 관련 문서만 포함한다.
-- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `test-result.md`, `review.md` 등을 포함할 
-수 있다.
-- `diff.md`와 `review-prompt.md`는 커질 수 있으므로 항상 커밋하지 않는다.
-- `review-response.json`은 민감 정보나 장문 내용이 포함될 수 있으므로 내용을 확인한 뒤 커밋 여부를 결정한다.
-- untracked 텍스트 파일 내용은 리뷰에 필요할 수 있지만, 리뷰 포함 여부와 커밋 대상 여부는 별도로 판단한다.
-- 대형 또는 민감 산출물은 정리하거나 별도 정책에 따라 제외한다.
-
-커밋 전에는 `git status --short`와 `ai-dev-commit.ps1 -DryRun`으로 대상을 먼저 확인한다. 선택 파일 커밋 기능이 준비되면 전체 stage보
-다 선택 파일 방식을 우선한다.
-
-세부 기준은 `docs/ai-dev-loop-policy.md`의 `.ai-dev` 실행 산출물 커밋/무시 정책을 따른다. 실제 `.gitignore` 변경은 별도 task에서 
-검토한다.
-
-## 자동 개발 루프 상태 요약
-
-현재 queue, state, git 변경사항, 테스트 결과, 리뷰 결과를 한 번에 확인한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-status.ps1
-```
-
-상태 요약을 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-status.ps1 -Json
-```
-
-상태 요약 스크립트는 파일을 수정하지 않으며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하지 않는다.
-
-## 다음 행동 안내
-
-현재 queue, state, review, test, git 상태를 확인해 다음에 실행할 추천 명령을 출력한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-next.ps1
-```
-
-다음 행동 안내를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-next.ps1 -Json
-```
-
-다음 행동 안내 스크립트는 추천 명령만 출력하며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하지 않는다.
-
-## 수동 사이클 안내
-
-상태 요약과 다음 행동 안내를 한 번에 확인하고, 관련 프롬프트·결과 파일의 존재 여부를 표시한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
-```
-
-상태와 다음 행동을 하나의 JSON 객체로 출력하려면 `-Json` 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1 -Json
-```
-
-수동 사이클 안내 스크립트는 추천 명령과 파일 경로만 보여주며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하지 
-않는다.
-
-`manual-cycle`에서 다음 action이 `ask_gpt_review`로 표시되면 수동 리뷰 브리지를 사용한다. GPT API 키가 없어도 아래 흐름으로 리뷰를 진행할 
-수 있다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
-```
-
-1. `manual-cycle`에서 현재 task와 다음 action이 `ask_gpt_review`인지 확인한다.
-2. `ai-dev-copy-review-prompt.ps1`로 `review-prompt.md`를 클립보드에 복사한다.
-3. ChatGPT 웹 화면에 붙여넣고 JSON 리뷰만 출력하도록 요청한다.
-4. ChatGPT가 반환한 JSON 리뷰를 클립보드에 복사한다.
-5. 아래 명령으로 리뷰 결과를 저장한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
-```
-
-리뷰 저장 후 decision별 다음 단계:
-
-- `pass`: `ai-dev-complete-task.ps1` 실행 또는 커밋 조건을 검토한다.
-- `revise`: `ai-dev-make-revise-prompt.ps1`로 재수정 프롬프트를 만든 뒤 Codex/Cline에 수동 전달한다.
-- `blocked`: 사용자 판단이 필요하므로 루프를 중단하고 요구사항이나 범위를 조정한다.
-
-`review-prompt.md`에는 diff, 테스트 결과, 로컬 경로, 민감 정보가 포함될 수 있다. ChatGPT에 붙여넣기 전에 사용자가 공유 범위를 확인해야 한다.
-
-## 자동 실행 단계 정책
-
-`ai-dev-auto-step.ps1`는 현재 상태를 보고 안전한 다음 한 단계만 실행하는 보조 스크립트로 도입할 예정이다. `ai-dev-next.ps1`처럼 안내만 하는 명
-령과 달리, 허용된 로컬 스크립트는 직접 실행할 수 있다.
-
-명령 선택 기준:
-
-| 상황 | 사용할 명령 | 이유 |
-| --- | --- | --- |
-| 현재 상태와 다음 행동을 한 화면에서 보고 싶을 때 | `scripts/ai-dev-manual-cycle.ps1` | 상태 요약, 다음 action, 관련 파일 존재 여부를
- 함께 보여주는 수동 운영 대시보드다. |
-| 다음 action과 추천 명령만 확인하고 싶을 때 | `scripts/ai-dev-next.ps1` | 읽기/안내용 스크립트이며 하위 명령을 자동 실행하지 않는다. |
-| 안전한 다음 한 단계만 자동으로 진행하고 싶을 때 | `scripts/ai-dev-auto-step.ps1` | `next` 판단 결과를 기반으로 허용된 로컬 스크립트만 한 
-번 실행한다. |
-| 여러 안전 단계를 제한 횟수 안에서 이어가고 싶을 때 | `scripts/ai-dev-auto-cycle.ps1` | auto-step 반복용이며 사용자 개입 필요 상태에서 
-중단한다. |
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -DryRun
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -Json
-```
-
-검증, diff 저장, 리뷰 프롬프트 생성처럼 기본 자동 실행보다 한 단계 위험한 로컬 작업은 명시적 허용 옵션을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -AllowCheck
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -AllowSaveDiff
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -AllowReviewPrompt
-```
-
-초기 auto-step 자동 실행 허용 후보:
-
-- `current-task-prompt.md`가 없을 때 `scripts/ai-dev-make-prompt.ps1`
-- 명시적으로 안전한 옵션이 선택된 경우 `scripts/ai-dev-check.ps1`
-- diff 저장이 필요한 경우 `scripts/ai-dev-save-diff.ps1`
-- review prompt 생성이 필요한 경우 `scripts/ai-dev-make-review-prompt.ps1`
-
-auto-step이 직접 실행하지 않고 중단 또는 안내해야 하는 단계:
-
-- Codex 또는 Cline 작업
-- GPT 리뷰
-- blocked 상태
-- revise 반영
-- git commit
-- 사용자 데이터 삭제, 복원, 마이그레이션 위험이 있는 작업
-
-`ask_gpt_review` 상태에서 auto-step은 GPT API를 호출하지 않고 수동 리뷰 브리지 명령을 안내한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -Json
-```
-
-안내에 포함되는 수동 리뷰 브리지 명령:
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
-```
-
-첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. ChatGPT가 JSON 리뷰만 출력하면 그 JSON을 다시 클립
-보드에 복사하고 두 번째 명령으로 저장한다.
-
-`ai-dev-auto-cycle.ps1`는 제한 횟수 안에서 auto-step을 반복하는 초기 자동화 스크립트다. 사용자 개입이 필요한 action, 실패, 반복 위험, com
-mit 필요 상태에서는 중단한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -DryRun
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -MaxSteps 3
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -Json
-```
-
-auto-cycle에서 check, diff 저장, review prompt 생성을 허용하려면 auto-step과 같은 허용 옵션을 전달한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -AllowCheck
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -AllowSaveDiff
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -AllowReviewPrompt
-```
-
-auto-cycle은 Codex/Cline 작업, GPT 리뷰, revise, commit, blocked, 사용자 개입 필요 action에서 멈춘다. 무한 루프를 피하기 위해 
-`-MaxSteps` 제한과 같은 action 반복 감지를 사용한다.
-
-auto-cycle이 `ask_gpt_review`에서 멈추면 `stoppedReason`은 `user_intervention_required:ask_gpt_review`로 표시
-된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
-```
-
-첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 리뷰를 받은 뒤 두 번째 명령으로 클립보드의 리뷰 JSON을
- 저장한다. auto-cycle은 이 단계에서 GPT API를 호출하지 않고 중단한다.
-
-명령 역할 구분:
-
-- `ai-dev-next.ps1`: 현재 상태를 읽고 다음 추천 action과 명령만 출력한다. 자동 실행하지 않는다.
-- `ai-dev-manual-cycle.ps1`: `status`와 `next`를 함께 보여주는 수동 운영 대시보드다. 자동 실행하지 않는다.
-- `ai-dev-auto-step.ps1`: `next` 판단 결과를 기반으로 안전한 action 한 단계만 자동 실행한다. 위험 action은 실행하지 않고 안내 후 중단한다
-.
-- `ai-dev-auto-cycle.ps1`: 제한 횟수 안에서 auto-step을 반복한다. 사용자 개입 필요 action에서 중단한다.
-
-auto-step과 auto-cycle은 Codex/GPT API 호출, git commit, git push를 자동 실행하지 않는다.
-
-## 수동 자동화 UX 판단 순서
-
-GPT API 없이 수동 자동화 흐름을 운영할 때는 먼저 상태를 읽고, 그 다음 안전한 DryRun으로 다음 행동을 확인한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-status.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-next.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -DryRun
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -DryRun -MaxSteps 3
-```
-
-확인 기준:
-
-- `goal_completed`: 더 이상 실행할 task가 없으므로 새 goal 초기화가 필요하다.
-- `ask_gpt_review`: `ai-dev-copy-review-prompt.ps1`와 `ai-dev-save-review.ps1 -FromClipboard`를 사용해 수
-동 리뷰 브리지로 진행한다.
-- `revise`: `ai-dev-make-revise-prompt.ps1`로 재수정 프롬프트를 만든다.
-- `blocked`: 사용자 판단이 필요하므로 자동 진행을 멈춘다.
-- `commit`: 자동 실행하지 않고 커밋 조건과 대상 파일을 사람이 확인한다.
-
-`goal_completed` 상태에서 `auto-step`은 추가 작업을 실행하지 않고 현재 goal이 완료되었으며 더 실행할 task가 없다고 안내한다. 추천 명령에는 상태 
-확인용 `ai-dev-status.ps1`만 포함한다.
-
-`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고, JSON 출력에서는 `completed: true`, `
-stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-
-dev/state.json`을 새 목표 기준으로 초기화한다.
-
-`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하고 JSON 리뷰만 다시 복사한다. 완료된 goal이나 기존
- 정상 상태를 불필요하게 오염시키지 않도록 실패 기록 방식은 별도 정책에 따라 개선한다.
-
-최종 검증 task에서는 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 결과를 `tes
-t-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-
-## Codex CLI 완전 자동화 준비
-
-다음 자동화 단계에서는 Codex CLI를 구현자와 리뷰어로 사용한다. GPT API Key, Cline, Copilot CLI, `gh` 없이 Codex CLI만 사용한다.
-
-현재 전제:
-
-- `codex-cli 0.133.0` 사용 가능
-- `codex exec` 동작 확인 완료
-- approval: `never`
-- sandbox: `workspace-write`
-- workdir: `D:\ai-apps\planpilot-local`
-- Cline은 삭제되어 사용하지 않음
-- Copilot CLI와 `gh`는 현재 사용하지 않음
-
-도입 예정 스크립트:
-
-- `scripts/ai-dev-run-codex.ps1`: `.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 수행한다
-.
-- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 저
-장 흐름에 연결한다.
-- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 저장, Codex 리뷰, save-review,
- pass 시 commit/complete-task를 연결한다.
-- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력하면 goal/queue/state 생성, cu
-rrent-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
-
-초기 안전 기준:
-
-- full cycle은 처음에 `MaxTasks 1` 중심으로 제한한다.
-- Codex 구현 실행은 `AllowCodex`가 있을 때만 수행한다.
-- Codex 리뷰 실행은 `AllowReviewCodex`가 있을 때만 수행한다.
-- git commit은 `AllowCommit`이 있을 때만 수행한다.
-- auto-goal에서 실제 full cycle 실행은 `AllowRun` 또는 명시적 실행 옵션이 있을 때만 수행한다.
-- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
-- build/check 실패, package 파일 변경, 리뷰 decision이 `pass`가 아닌 경우 자동 커밋하지 않는다.
-- `git reset`, `git clean`, `npm install`, `git push`는 자동 실행하지 않는다.
-
-자동화 로그와 결과 파일:
-
-- `.ai-dev/codex-result.md`: Codex 구현 실행 결과
-- `.ai-dev/review-response.json`: Codex 리뷰 JSON 원문
-- `.ai-dev/review.md`: 저장된 리뷰 요약
-- `.ai-dev/test-result.md`: check/build 또는 수동 검증 요약
-- `.ai-dev/loop-log.md`: 루프 진행 기록
-- `.ai-dev/state.json`: 현재 상태와 마지막 실패 요약
-
-### Codex 구현 실행
-
-`.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 실행한다. 실행 전 `git status --short`를 확인하고,
- 작업 트리가 dirty이면 기본 중단한다. 단, dirty 파일이 `.ai-dev/current-task-prompt.md`뿐이면 프롬프트 생성 산출물로 보고 진행할 수 있다.
-
-실행 예정 정보만 확인하려면 `-DryRun`을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -DryRun
-```
-
-기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-result.md`에 저장한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1
-```
-
-`current-task-prompt.md`가 없을 때 자동 생성까지 허용하려면 `-GeneratePromptIfMissing`을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -GeneratePromptIfMissing
-```
-
-자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -DryRun -Json
-```
-
-dirty worktree에서도 실행해야 하는 특별한 경우에만 `-AllowDirty`를 명시한다. 이 옵션은 사용자 변경과 Codex 변경이 섞일 수 있으므로 full cycl
-e에서는 초기 기본값으로 사용하지 않는다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -AllowDirty
-```
-
-이 스크립트는 Codex에 전달하는 프롬프트 끝에 추가 안전 규칙을 붙인다. git commit, git reset, git clean, npm install, package 파
-일 수정, 현재 task 범위 밖 작업을 금지한다. 스크립트 자체도 build/test/lint, git add, git commit을 실행하지 않는다.
-
-### Codex 리뷰 실행
-
-`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/review-response.json`에 저장한다. 실행 전
- `git status --short`를 확인하고, 작업 트리가 dirty이면 기본 중단한다. 리뷰 대상 변경사항이 남아 있는 상태에서 실행해야 하는 경우에만 `-AllowDir
-ty`를 명시한다.
-
-실행 예정 정보만 확인하려면 `-DryRun`을 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -DryRun
-```
-
-기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-result.md`에 저장한다. Codex 출력에서 첫 번째 유효
- JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1
-```
-
-`review-prompt.md`가 없을 때 자동 생성까지 허용하려면 `-GenerateReviewPromptIfMissing`를 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -GenerateReviewPromptI
-fMissing
-```
-
-리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-response.json` 흐름까지 이어가려면 `-SaveRev
-iew`를 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveRevie
-w
-```
-
-자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -DryRun -Json
-```
-
-주의:
-
-- Codex 리뷰 실행 프롬프트에는 파일 수정 금지, git 명령 금지, build/test/lint 실행 금지, JSON 객체만 출력하라는 안전 규칙을 추가한다.
-- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`, `optional_suggestions`, `next_st
-ep` 필드가 필요하다.
-- JSON 추출에 실패하면 출력 preview를 보여주고 종료 코드 1로 종료한다.
-- `-AllowDirty`는 구현 변경사항을 리뷰해야 할 때만 사용한다. 사용자 변경과 자동화 산출물이 섞여 있으면 리뷰 결과와 후속 저장 상태가 혼동될 수 있다.
-- 이 스크립트는 git add, git commit, build/test/lint, GPT API 호출을 수행하지 않는다.
-
-### full auto-cycle 초안
-
-`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict review
- prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-
-T005 버전의 실행 단계:
-
-1. `ai-dev-make-prompt.ps1`
-2. `ai-dev-run-codex.ps1`
-3. `ai-dev-check.ps1 -BuildOnly`
-4. `ai-dev-save-diff.ps1`
-5. `ai-dev-make-review-prompt.ps1 -Strict`
-6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
-7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
-8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
-9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다. `-CommitFiles`가 있으면 선택 파일만 커밋 대상으로 전달한다.
-10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
-11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-12. `MaxTasks`가 1보다 크면 complete-task 성공 후 다음 task를 다시 읽고 같은 흐름을 반복한다.
-
-먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun
-```
-
-자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-```
-
-기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이다. `MaxTasks`가 1보다 크면 각
- task가 커밋과 complete-task까지 성공한 경우에만 다음 task로 반복한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -Ma
-xSteps 20
-```
-
-여러 task 반복 구조를 확인하려면 `MaxTasks`를 늘릴 수 있다. 실제 반복은 각 task의 commit/complete-task가 성공해야 다음 task로 넘어간다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 3 -Ma
-xSteps 20
-```
-
-`-AllowCodex` 없이 실제 실행하면 prompt 생성 이후 Codex 구현 실행 직전에서 중단하고 추천 명령을 출력한다. Codex 구현까지 허용하려면 명시적으로 `-A
-llowCodex`를 지정한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex
-```
-
-Codex 구현과 Codex 리뷰까지 이어가려면 `-AllowCodex`와 `-AllowReviewCodex`를 함께 지정한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevie
-wCodex -MaxTasks 1
-```
-
-리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 함께 지정한다. 커밋 메시지는 기본적으로 현재 t
-ask의 `commitMessage`를 사용한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevie
-wCodex -AllowCommit -MaxTasks 1
-```
-
-선택 파일만 커밋하려면 `-CommitFiles`를 지정한다. 이 값은 그대로 `ai-dev-commit.ps1 -Files`에 전달된다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevie
-wCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 1
-```
-
-최대 3개 task까지 연속 실행하려면 `MaxTasks 3`을 지정한다. complete-task가 queue/state/loop-log를 갱신하므로, 다음 task 구현 실행
- 시 dirty worktree 정책을 별도로 판단해야 한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevie
-wCodex -AllowCommit -AllowDirty -MaxTasks 3
-```
-
-dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-cod
-ex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevie
-wCodex -AllowDirty -MaxTasks 1
-```
-
-리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 complete
--task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package-lo
-ck.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 `com
-mit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-
-### 목표 입력 기반 자동 실행
-
-최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 하는 것이다.
-
-먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트 
-추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-```
-
-자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 파일 작성 전에 생성될 goal/task 계획
-을 검토할 수 있다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트 
-추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-```
-
-실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이 모드는 `ai-dev-auto-cycle-f
-ull.ps1`을 호출하지 않는다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트 
-추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-```
-
-생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommit
-` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다. 실제 구
-현까지 허용하려면 `-AllowCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하게
- `-AllowCodex -AllowReviewCodex -AllowCommit`을 함께 지정한다.
-
-```powershell
-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트 
-추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -Allow
-Commit -MaxTasks 1
-```
-
-동작:
-
-1. `GoalTitle`과 `GoalDescription`을 검증한다.
-2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
-3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다.
-5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
-6. `.ai-dev/current-task-prompt.md`를 생성한다.
-7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-task-prompt.md`, 결과 파일 때문
-에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트는 aut
-o-goal 시작 전에 별도로 유지한다.
-
-안전 기준:
-
-- `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
-- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
-- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 않는다.
-- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommit` 중 하나가 있을 때만 수행한다.
-- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCodex`, `-AllowReviewCodex`,
- `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
-- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커밋하지 않는다.
-- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
-
-## 운영 원칙
-
-- 저장소의 `AGENTS.md`, 사용자 지시, 보안 정책이 자동 개발 루프보다 우선한다.
-- 한 번에 하나의 task만 수행한다.
-- task 단위로 구현, 검증, 리뷰, 커밋한다.
-- 현재 task 범위 밖 파일을 임의로 수정하지 않는다.
-- 검증하지 않은 결과를 통과로 기록하지 않는다.
-- 실제 DB 삭제, 초기화, 복원, 대규모 리팩터링은 초기 실험 범위에서 제외한다.
-
-## 다음 단계
-
-실제 자동 개발 루프 실행기와 상태 갱신 로직은 별도 정책 검토 후 추가한다.
-
-세부 기준은 `docs/ai-dev-loop-policy.md`를 따른다.
-
- succeeded in 2823ms:
-backlog.md
-codex-result.md
-codex-review-result.md
-current-task-prompt.md
-diff.md
-extra-instructions.example.md
-goal.md
-loop-log.md
-plan.md
-queue.example.json
-queue.json
-README.md
-review-prompt.md
-review-response.json
-review.md
-state.example.json
-state.json
-test-result.md
-
-codex
-`ai-dev-auto-goal.ps1` 파일이 이미 존재합니다. 새로 만들기보다 현재 구현이 T007 완료 기준을 충족하는지 확인하고, 부족한 부분만 좁게 수정하겠습니다.
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1'" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-make-prompt.ps1'" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-cycle-full.ps1'" in D:\ai-apps\planpilot-local
- succeeded in 3092ms:
-param(
-    [int]$MaxTasks = 1,
-    [int]$MaxSteps = 20,
-    [switch]$DryRun,
-    [switch]$Json,
-    [switch]$AllowCodex,
-    [switch]$AllowReviewCodex,
-    [switch]$AllowCommit,
-    [switch]$AllowDirty,
-    [string[]]$CommitFiles
-)
-
-. $PSScriptRoot\ai-dev-env.ps1
-
-$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-$queueRelativePath = ".ai-dev/queue.json"
-$stateRelativePath = ".ai-dev/state.json"
-$reviewResponseRelativePath = ".ai-dev/review-response.json"
-$queuePath = Join-Path $repoRoot $queueRelativePath
-$statePath = Join-Path $repoRoot $stateRelativePath
-$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-
-function Test-HasValue {
-    param(
-        [object]$Value
-    )
-
-    if ($null -eq $Value) {
-        return $false
-    }
-
-    if ($Value -is [string]) {
-        return -not [string]::IsNullOrWhiteSpace($Value)
-    }
-
-    return $true
-}
-
-function Read-JsonFile {
-    param(
-        [string]$Path,
-        [string]$RelativePath
-    )
-
-    try {
-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
-    } catch {
-        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
-    }
-}
-
-function Get-CurrentTask {
-    param(
-        [object]$Queue,
-        [object]$State
-    )
-
-    $tasks = @($Queue.tasks)
-    $currentTaskId = $null
-
-    if (Test-HasValue $State.currentTaskId) {
-        $currentTaskId = [string]$State.currentTaskId
-    } elseif (Test-HasValue $Queue.currentTaskId) {
-        $currentTaskId = [string]$Queue.currentTaskId
-    } else {
-        $inProgressTask = $tasks | Where-Object { $_.status -eq "in_progress" } | Select-Object -Fi
-rst 1
-
-        if ($null -ne $inProgressTask) {
-            $currentTaskId = [string]$inProgressTask.id
-        } else {
-            $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -First
- 1
-
-            if ($null -ne $pendingTask) {
-                $currentTaskId = [string]$pendingTask.id
-            }
-        }
-    }
-
-    if (-not (Test-HasValue $currentTaskId)) {
-        return $null
-    }
-
-    return $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
-}
-
-function New-StepResult {
-    param(
-        [int]$Step,
-        [string]$Name,
-        [string]$Command,
-        [bool]$Executed,
-        [bool]$Skipped,
-        [int]$ExitCode,
-        [string]$Message
-    )
-
-    return [PSCustomObject][ordered]@{
-        step = $Step
-        name = $Name
-        command = $Command
-        executed = $Executed
-        skipped = $Skipped
-        exitCode = $ExitCode
-        message = $Message
-    }
-}
-
-function New-CycleResult {
-    param(
-        [object[]]$Steps,
-        [string]$StoppedReason,
-        [bool]$Completed,
-        [int]$ExitCode
-    )
-
-    return [PSCustomObject][ordered]@{
-        steps = @($Steps)
-        stoppedReason = $StoppedReason
-        completed = $Completed
-        exitCode = $ExitCode
-    }
-}
-
-function Write-CycleResult {
-    param(
-        [object]$Result
-    )
-
-    if ($Json) {
-        $Result | ConvertTo-Json -Depth 20
-        return
-    }
-
-    foreach ($step in @($Result.steps)) {
-        Write-Host "Step $($step.step): $($step.name)"
-        Write-Host "  Command: $($step.command)"
-        Write-Host "  Executed: $($step.executed)"
-        Write-Host "  Skipped: $($step.skipped)"
-        Write-Host "  Exit code: $($step.exitCode)"
-        Write-Host "  Message: $($step.message)"
-    }
-
-    Write-Host "Stopped reason: $($Result.stoppedReason)"
-    Write-Host "Completed: $($Result.completed)"
-    Write-Host "Exit code: $($Result.exitCode)"
-
-}
-
-function Stop-Cycle {
-    param(
-        [object[]]$Steps,
-        [string]$StoppedReason,
-        [bool]$Completed,
-        [int]$ExitCode
-    )
-
-    $result = New-CycleResult $Steps $StoppedReason $Completed $ExitCode
-    Write-CycleResult $result
-    exit $ExitCode
-}
-
-function Invoke-CycleCommand {
-    param(
-        [int]$StepNumber,
-        [string]$Name,
-        [string]$Command,
-        [string]$ScriptPath,
-        [string[]]$Arguments
-    )
-
-    if ($DryRun) {
-        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: 하위 스크립트를
- 실행하지 않았습니다."
-        return
-    }
-
-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
-    $exitCode = $LASTEXITCODE
-    $message = $output.Trim()
-
-    if ([string]::IsNullOrWhiteSpace($message)) {
-        $message = "완료"
-    }
-
-    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-
-    if ($exitCode -ne 0) {
-        Stop-Cycle $script:steps "$Name`_failed" $false 1
-    }
-}
-
-function Get-ScriptPath {
-    param(
-        [string]$Name
-    )
-
-    $scriptPath = Join-Path $PSScriptRoot $Name
-
-    if (-not (Test-Path -LiteralPath $scriptPath -PathType Leaf)) {
-        throw "필수 스크립트를 찾을 수 없습니다: scripts/$Name"
-    }
-
-    return $scriptPath
-}
-
-function Invoke-GitCapture {
-    param(
-        [string[]]$Arguments,
-        [string]$DisplayName
-    )
-
-    $output = & git @Arguments 2>&1 | Out-String
-    $exitCode = $LASTEXITCODE
-
-    if ($exitCode -ne 0) {
-        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-    }
-
-    return $output.TrimEnd()
-}
-
-function Test-PackageFileChanged {
-    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.json
-") "git status --porcelain -- package.json package-lock.json"
-    return -not [string]::IsNullOrWhiteSpace($status)
-}
-
-function Get-ReviewGate {
-    $state = Read-JsonFile $statePath $stateRelativePath
-    $nextStep = $null
-
-    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-
-        if (Test-HasValue $reviewResponse.next_step) {
-            $nextStep = [string]$reviewResponse.next_step
-        }
-    }
-
-    return [PSCustomObject][ordered]@{
-        lastCommandStatus = [string]$state.lastCommandStatus
-        decision = [string]$state.lastReviewDecision
-        nextStep = $nextStep
-    }
-}
-
-function Get-CommitArguments {
-    $arguments = @()
-
-    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -not 
-[string]::IsNullOrWhiteSpace($_) })
-
-        if ($normalizedFiles.Count -gt 0) {
-            $arguments += "-Files"
-            $arguments += ($normalizedFiles -join ",")
-        }
-    }
-
-    return $arguments
-}
-
-function Get-CommitGate {
-    $state = Read-JsonFile $statePath $stateRelativePath
-
-    return [PSCustomObject][ordered]@{
-        lastCommand = [string]$state.lastCommand
-        lastCommandStatus = [string]$state.lastCommandStatus
-        lastCommitHash = [string]$state.lastCommitHash
-    }
-}
-
-Set-Location $repoRoot
-
-$script:steps = @()
-
-if ($MaxTasks -lt 1) {
-    Stop-Cycle $script:steps "max_tasks_must_be_at_least_1" $false 1
-}
-
-if ($MaxSteps -lt 1) {
-    Stop-Cycle $script:steps "max_steps_must_be_at_least_1" $false 1
-}
-
-try {
-    foreach ($requiredPath in @($queuePath, $statePath)) {
-        if (-not (Test-Path -LiteralPath $requiredPath -PathType Leaf)) {
-            throw "필수 상태 파일이 없습니다: $requiredPath"
-        }
-    }
-
-    $queue = Read-JsonFile $queuePath $queueRelativePath
-    $state = Read-JsonFile $statePath $stateRelativePath
-
-    if ($state.goalStatus -eq "completed") {
-        Stop-Cycle $script:steps "goal_completed" $true 0
-    }
-
-    if (-not ($queue.PSObject.Properties.Name -contains "tasks") -or $null -eq $queue.tasks) {
-        Stop-Cycle $script:steps "no_task" $false 0
-    }
-
-    $currentTask = Get-CurrentTask $queue $state
-
-    if ($null -eq $currentTask) {
-        Stop-Cycle $script:steps "no_task" $false 0
-    }
-
-    if ($currentTask.status -eq "done") {
-        Stop-Cycle $script:steps "current_task_done" $true 0
-    }
-
-    $scriptPaths = @{
-        makePrompt = Get-ScriptPath "ai-dev-make-prompt.ps1"
-        runCodex = Get-ScriptPath "ai-dev-run-codex.ps1"
-        check = Get-ScriptPath "ai-dev-check.ps1"
-        saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-        makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-        runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-        commit = Get-ScriptPath "ai-dev-commit.ps1"
-        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-    }
-} catch {
-    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Messa
-ge
-    Stop-Cycle $script:steps "prepare_failed" $false 1
-}
-
-$plannedSteps = @(
-    "make-prompt",
-    "run-codex",
-    "check",
-    "save-diff",
-    "make-review-prompt",
-    "run-review-codex",
-    "review-gate",
-    "package-change-gate",
-    "commit",
-    "commit-result-gate",
-    "complete-task"
-)
-
-if ($plannedSteps.Count -gt $MaxSteps) {
-    Stop-Cycle $script:steps "max_steps_too_small_for_full_cycle" $false 1
-}
-
-$stepNumber = 1
-$completedTaskCount = 0
-
-while ($completedTaskCount -lt $MaxTasks) {
-    try {
-        $queue = Read-JsonFile $queuePath $queueRelativePath
-        $state = Read-JsonFile $statePath $stateRelativePath
-        $currentTask = Get-CurrentTask $queue $state
-    } catch {
-        $script:steps += New-StepResult $stepNumber "load-task" "state/queue 확인" $false $false 1 $_
-.Exception.Message
-        Stop-Cycle $script:steps "load_task_failed" $false 1
-    }
-
-    if ($state.goalStatus -eq "completed") {
-        Stop-Cycle $script:steps "goal_completed" $true 0
-    }
-
-    if ($null -eq $currentTask) {
-        Stop-Cycle $script:steps "no_task" $false 0
-    }
-
-    if ($currentTask.status -eq "done") {
-        Stop-Cycle $script:steps "current_task_done" $true 0
-    }
-
-    $taskLabel = "$($currentTask.id) $($currentTask.title)"
-    $script:steps += New-StepResult $stepNumber "task-start" "MaxTasks=$MaxTasks" $false $false 0 "
-현재 task 실행 시작: $taskLabel"
-    $stepNumber++
-
-    Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts
-/ai-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-    $stepNumber++
-
-    if (-not $AllowCodex -and -not $DryRun) {
-        $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Al
-lowCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-        if ($AllowDirty) {
-            $command = "$command -AllowDirty"
-        }
-
-        if ($AllowCommit) {
-            $command = "$command -AllowCommit"
-        }
-
-        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-            $command = "$command -CommitFiles $($CommitFiles -join ',')"
-        }
-
-        $script:steps += New-StepResult $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass
- -File scripts/ai-dev-run-codex.ps1" $false $true 1 "Codex 구현 실행에는 -AllowCodex가 필요합니다. 추천 명령: $comm
-and"
-        Stop-Cycle $script:steps "allow_codex_required" $false 1
-    }
-
-    $runCodexArguments = @()
-    if ($AllowDirty) {
-        $runCodexArguments += "-AllowDirty"
-    }
-
-    Invoke-CycleCommand $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass -File scripts/a
-i-dev-run-codex.ps1" $scriptPaths.runCodex $runCodexArguments
-    $stepNumber++
-
-    Invoke-CycleCommand $stepNumber "check" "powershell -ExecutionPolicy Bypass -File scripts/ai-de
-v-check.ps1 -BuildOnly" $scriptPaths.check @("-BuildOnly")
-    $stepNumber++
-
-    Invoke-CycleCommand $stepNumber "save-diff" "powershell -ExecutionPolicy Bypass -File scripts/a
-i-dev-save-diff.ps1" $scriptPaths.saveDiff @()
-    $stepNumber++
-
-    Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File 
-scripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-    $stepNumber++
-
-    if (-not $AllowReviewCodex -and -not $DryRun) {
-        $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Al
-lowCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-        if ($AllowDirty) {
-            $command = "$command -AllowDirty"
-        }
-
-        if ($AllowCommit) {
-            $command = "$command -AllowCommit"
-        }
-
-        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-            $command = "$command -CommitFiles $($CommitFiles -join ',')"
-        }
-
-        $script:steps += New-StepResult $stepNumber "run-review-codex" "powershell -ExecutionPolicy
- Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $false $true 1 "Codex 리뷰
- 실행에는 -AllowReviewCodex가 필요합니다. 추천 명령: $command"
-        Stop-Cycle $script:steps "allow_review_codex_required" $false 1
-    }
-
-    Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File sc
-ripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-ty", "-SaveReview")
-    $stepNumber++
-
-    if ($DryRun) {
-        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fa
-lse $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-        $stepNumber++
-        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -
-- package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-        $stepNumber++
-        $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -F
-ile scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-        $stepNumber++
-        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCom
-mitHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
-        $stepNumber++
-        $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy By
-pass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"자동 완료: Codex 구현, build/check, Codex 리뷰
- pass, 자동 커밋 완료`"" $false $true 0 "DryRun: task 완료 처리를 실행하지 않았습니다."
-        Stop-Cycle $script:steps "dry_run" $false 0
-    }
-
-    try {
-        $reviewGate = Get-ReviewGate
-    } catch {
-        $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false
- $false 1 $_.Exception.Message
-        Stop-Cycle $script:steps "review_gate_failed" $false 1
-    }
-
-    if ($reviewGate.lastCommandStatus -ne "passed") {
-        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fal
-se $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-        Stop-Cycle $script:steps "review_save_not_passed" $false 1
-    }
-
-    if ($reviewGate.decision -ne "pass") {
-        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fa
-lse $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-        Stop-Cycle $script:steps "review_not_pass" $false 0
-    }
-
-    if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-        $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next
-_step 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-iewGate.nextStep)"
-        Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-    }
-
-    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false 
-$false 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-    $stepNumber++
-
-    try {
-        if (Test-PackageFileChanged) {
-            $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcela
-in -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 커밋하지 않습니다."
-            Stop-Cycle $script:steps "package_files_changed" $false 1
-        }
-    } catch {
-        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -
-- package.json package-lock.json" $false $false 1 $_.Exception.Message
-        Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-    }
-
-    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pa
-ckage.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-    $stepNumber++
-
-    if (-not $AllowCommit) {
-        $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.p
-s1 -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-        if ($AllowDirty) {
-            $commitCommand = "$commitCommand -AllowDirty"
-        }
-
-        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-            $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-        }
-
-        $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -F
-ile scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-        Stop-Cycle $script:steps "allow_commit_required" $false 1
-    }
-
-    $commitArguments = Get-CommitArguments
-    $commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-    if ($commitArguments.Count -gt 0) {
-        $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-    }
-
-    Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArgument
-s
-    $stepNumber++
-
-    try {
-        $commitGate = Get-CommitGate
-    } catch {
-        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCom
-mitHash 확인" $false $false 1 $_.Exception.Message
-        Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-    }
-
-    if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -no
-t (Test-HasValue $commitGate.lastCommitHash)) {
-        $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastComma
-nd), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHas
-h)"
-        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCom
-mitHash 확인" $false $true 1 $message
-        Stop-Cycle $script:steps "commit_not_confirmed" $false 1
-    }
-
-    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitH
-ash 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
-    $stepNumber++
-
-    $resultSummary = "자동 완료: Codex 구현, build/check, Codex 리뷰 pass, 자동 커밋 완료"
-    Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scrip
-ts/ai-dev-complete-task.ps1 -ResultSummary `"$resultSummary`"" $scriptPaths.completeTask @("-Result
-Summary", $resultSummary)
-    $stepNumber++
-    $completedTaskCount++
-}
-
-Stop-Cycle $script:steps "max_tasks_reached" $true 0
-
- succeeded in 3115ms:
-param(
-    [AllowEmptyString()]
-    [string]$GoalTitle,
-    [AllowEmptyString()]
-    [string]$GoalDescription,
-    [int]$MaxTasks = 1,
-    [int]$MaxSteps = 20,
-    [switch]$DryRun,
-    [switch]$Json,
-    [switch]$AllowRun,
-    [switch]$AllowCodex,
-    [switch]$AllowReviewCodex,
-    [switch]$AllowCommit,
-    [switch]$AllowDirty,
-    [string[]]$CommitFiles,
-    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
-)
-
-. "$PSScriptRoot\ai-dev-env.ps1"
-
-$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-$codeFence = '```'
-$goalRelativePath = ".ai-dev/goal.md"
-$queueRelativePath = ".ai-dev/queue.json"
-$stateRelativePath = ".ai-dev/state.json"
-$promptRelativePath = ".ai-dev/current-task-prompt.md"
-
-function Resolve-RepoPath {
-    param([string]$Path)
-
-    if ([System.IO.Path]::IsPathRooted($Path)) {
-        return $Path
-    }
-
-    return Join-Path $repoRoot $Path
-}
-
-function ConvertTo-RepoRelativePath {
-    param([string]$Path)
-
-    $fullPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $Path))
-    $rootWithSeparator = $repoRoot.TrimEnd("\") + "\"
-
-    if ($fullPath.StartsWith($rootWithSeparator, [System.StringComparison]::OrdinalIgnoreCase)) {
-        return $fullPath.Substring($rootWithSeparator.Length).Replace("\", "/")
-    }
-
-    return $fullPath.Replace("\", "/")
-}
-
-function Test-HasValue {
-    param([object]$Value)
-
-    if ($null -eq $Value) {
-        return $false
-    }
-
-    if ($Value -is [string]) {
-        return -not [string]::IsNullOrWhiteSpace($Value)
-    }
-
-    return $true
-}
-
-function New-StepResult {
-    param(
-        [int]$Step,
-        [string]$Name,
-        [string]$Command,
-        [bool]$Executed,
-        [bool]$Skipped,
-        [int]$ExitCode,
-        [string]$Message
-    )
-
-    return [PSCustomObject][ordered]@{
-        step = $Step
-        name = $Name
-        command = $Command
-        executed = $Executed
-        skipped = $Skipped
-        exitCode = $ExitCode
-        message = $Message
-    }
-}
-
-function New-AutoGoalResult {
-    param(
-        [object[]]$Steps,
-        [string]$StoppedReason,
-        [bool]$Completed,
-        [int]$ExitCode
-    )
-
-    return [PSCustomObject][ordered]@{
-        steps = @($Steps)
-        stoppedReason = $StoppedReason
-        completed = $Completed
-        exitCode = $ExitCode
-        plan = $script:autoGoalPlanPreview
-        goalPath = $goalRelativePath
-        queuePath = $queueRelativePath
-        statePath = $stateRelativePath
-        promptPath = $promptRelativePath
-        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-    }
-}
-
-function Write-AutoGoalResult {
-    param([object]$Result)
-
-    if ($Json) {
-        $Result | ConvertTo-Json -Depth 30
-        return
-    }
-
-    foreach ($step in @($Result.steps)) {
-        Write-Host "Step $($step.step): $($step.name)"
-        Write-Host "  Command: $($step.command)"
-        Write-Host "  Executed: $($step.executed)"
-        Write-Host "  Skipped: $($step.skipped)"
-        Write-Host "  Exit code: $($step.exitCode)"
-        Write-Host "  Message: $($step.message)"
-    }
-
-    if ($null -ne $Result.plan) {
-        Write-Host "Plan preview:"
-        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)"
-        Write-Host "  Current task id: $($Result.plan.queue.currentTaskId)"
-
-        foreach ($task in @($Result.plan.queue.tasks)) {
-            Write-Host "  Task $($task.id): $($task.title)"
-            Write-Host "    Type: $($task.type)"
-            Write-Host "    Status: $($task.status)"
-            Write-Host "    Priority: $($task.priority)"
-            Write-Host "    Likely files: $((@($task.filesLikelyToChange) -join ', '))"
-            Write-Host "    Verification: $((@($task.verification) -join ' / '))"
-        }
-    }
-
-    Write-Host "Stopped reason: $($Result.stoppedReason)"
-    Write-Host "Completed: $($Result.completed)"
-    Write-Host "Exit code: $($Result.exitCode)"
-}
-
-function Stop-AutoGoal {
-    param(
-        [object[]]$Steps,
-        [string]$StoppedReason,
-        [bool]$Completed,
-        [int]$ExitCode
-    )
-
-    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReason $Completed $ExitCode)
-    exit $ExitCode
-}
-
-function Get-InputPreview {
-    param([string]$RawInput)
-
-    if ($null -eq $RawInput) {
-        return "<null>"
-    }
-
-    $normalized = $RawInput.Replace("`r", " ").Replace("`n", " ").Trim()
-
-    if ($normalized.Length -eq 0) {
-        return "<empty>"
-    }
-
-    if ($normalized.Length -le 500) {
-        return $normalized
-    }
-
-    return $normalized.Substring(0, 500)
-}
-
-function Get-GitStatusLines {
-    $statusOutput = & git status --short 2>&1
-
-    if ($LASTEXITCODE -ne 0) {
-        $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 
-1 "git status failed: $($statusOutput -join "`n")"
-        Stop-AutoGoal $script:steps "git_status_failed" $false 1
-    }
-
-    return @($statusOutput | Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
-}
-
-function Get-JsonObjectCandidates {
-    param([string]$RawInput)
-
-    $candidates = @()
-
-    for ($start = 0; $start -lt $RawInput.Length; $start++) {
-        if ($RawInput[$start] -ne "{") {
-            continue
-        }
-
-        $depth = 0
-        $inString = $false
-        $escaped = $false
-
-        for ($index = $start; $index -lt $RawInput.Length; $index++) {
-            $char = $RawInput[$index]
-
-            if ($escaped) {
-                $escaped = $false
-                continue
-            }
-
-            if ($char -eq "\") {
-                $escaped = $true
-                continue
-            }
-
-            if ($char -eq '"') {
-                $inString = -not $inString
-                continue
-            }
-
-            if ($inString) {
-                continue
-            }
-
-            if ($char -eq "{") {
-                $depth++
-                continue
-            }
-
-            if ($char -eq "}") {
-                $depth--
-
-                if ($depth -eq 0) {
-                    $candidates += $RawInput.Substring($start, $index - $start + 1).Trim()
-                    break
-                }
-            }
-        }
-    }
-
-    return @($candidates | Select-Object -Unique)
-}
-
-function Get-MissingFields {
-    param(
-        [object]$InputObject,
-        [string[]]$RequiredFields
-    )
-
-    $missingFields = @()
-
-    foreach ($field in $RequiredFields) {
-        if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
-            $missingFields += $field
-        }
-    }
-
-    return $missingFields
-}
-
-function Get-QueueValidationErrors {
-    param([object]$Queue)
-
-    $errors = @()
-    $requiredFields = @("goalTitle", "goalSource", "createdAt", "updatedAt", "currentTaskId", "task
-s")
-
-    foreach ($field in @(Get-MissingFields $Queue $requiredFields)) {
-        $errors += "queue missing field: $field"
-    }
-
-    if ($errors.Count -gt 0) {
-        return $errors
-    }
-
-    if (-not (Test-HasValue $Queue.goalTitle)) {
-        $errors += "queue.goalTitle must not be empty."
-    }
-
-    if ($Queue.goalSource -ne $goalRelativePath) {
-        $errors += "queue.goalSource must be $goalRelativePath."
-    }
-
-    if ($Queue.tasks -isnot [System.Collections.IEnumerable] -or $Queue.tasks -is [string]) {
-        $errors += "queue.tasks must be an array."
-        return $errors
-    }
-
-    $tasks = @($Queue.tasks)
-
-    if ($tasks.Count -lt 1) {
-        $errors += "queue.tasks must contain at least one task."
-        return $errors
-    }
-
-    if ($tasks.Count -gt 3) {
-        $errors += "queue.tasks must contain no more than three tasks."
-    }
-
-    $taskIds = @()
-
-    foreach ($task in $tasks) {
-        $taskRequiredFields = @("id", "title", "description", "type", "status", "priority", "depend
-sOn", "filesLikelyToChange", "verification", "commitMessage")
-        $taskMissingFields = @(Get-MissingFields $task $taskRequiredFields)
-
-        foreach ($field in $taskMissingFields) {
-            $errors += "task missing field: $field"
-        }
-
-        if ($taskMissingFields.Count -gt 0) {
-            continue
-        }
-
-        if (-not (Test-HasValue $task.id)) {
-            $errors += "task.id must not be empty."
-        } else {
-            $taskIds += [string]$task.id
-        }
-
-        if (-not (Test-HasValue $task.title)) {
-            $errors += "task.title must not be empty."
-        }
-
-        if (-not (Test-HasValue $task.description)) {
-            $errors += "task.description must not be empty."
-        }
-
-        if (@("analysis", "implementation", "documentation", "verification") -notcontains $task.typ
-e) {
-            $errors += "invalid task.type: $($task.type)"
-        }
-
-        if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
-            $errors += "invalid task.status: $($task.status)"
-        }
-
-        if (@("P0", "P1", "P2") -notcontains $task.priority) {
-            $errors += "invalid task.priority: $($task.priority)"
-        }
-
-        if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is [string
-]) {
-            $errors += "task.dependsOn must be an array: $($task.id)"
-        }
-
-        if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.filesLikely
-ToChange -is [string]) {
-            $errors += "task.filesLikelyToChange must be an array: $($task.id)"
-        }
-
-        if ($task.verification -isnot [System.Collections.IEnumerable] -or $task.verification -is [
-string]) {
-            $errors += "task.verification must be an array: $($task.id)"
-        }
-    }
-
-    if ((@($tasks | Where-Object { $_.status -eq "in_progress" })).Count -ne 1) {
-        $errors += "queue.tasks must contain exactly one in_progress task."
-    }
-
-    if (-not (Test-HasValue $Queue.currentTaskId)) {
-        $errors += "queue.currentTaskId must not be empty."
-    } elseif ($taskIds -notcontains [string]$Queue.currentTaskId) {
-        $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-    }
-
-    return $errors
-}
-
-function Get-StateValidationErrors {
-    param(
-        [object]$State,
-        [object]$Queue
-    )
-
-    $errors = @()
-    $requiredFields = @("goalStatus", "currentTaskId", "currentLoop", "maxLoopsPerTask", "repeatedF
-ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDecision", "lastRe
-viewSeverity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
-
-    foreach ($field in @(Get-MissingFields $State $requiredFields)) {
-        $errors += "state missing field: $field"
-    }
-
-    if ($errors.Count -gt 0) {
-        return $errors
-    }
-
-    if (@("idle", "in_progress", "completed", "blocked") -notcontains $State.goalStatus) {
-        $errors += "invalid state.goalStatus: $($State.goalStatus)"
-    }
-
-    if ($State.currentTaskId -ne $Queue.currentTaskId) {
-        $errors += "state.currentTaskId must match queue.currentTaskId."
-    }
-
-    if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-        $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-    }
-
-    if (@("not_started", "pass", "revise", "blocked") -notcontains $State.lastReviewDecision) {
-        $errors += "invalid state.lastReviewDecision: $($State.lastReviewDecision)"
-    }
-
-    return $errors
-}
-
-function Get-AutoGoalValidationErrors {
-    param([object]$Plan)
-
-    $errors = @()
-
-    foreach ($field in @(Get-MissingFields $Plan @("goalMarkdown", "queue", "state"))) {
-        $errors += "plan missing field: $field"
-    }
-
-    if ($errors.Count -gt 0) {
-        return $errors
-    }
-
-    if (-not (Test-HasValue $Plan.goalMarkdown)) {
-        $errors += "goalMarkdown must not be empty."
-    }
-
-    $errors += @(Get-QueueValidationErrors $Plan.queue)
-    $errors += @(Get-StateValidationErrors $Plan.state $Plan.queue)
-
-    return $errors
-}
-
-function New-DryRunAutoGoalPlan {
-    param(
-        [string]$Title,
-        [string]$Description
-    )
-
-    $now = (Get-Date).ToUniversalTime().ToString("o")
-    $safeCommitTitle = $Title.Trim()
-
-    $plan = [PSCustomObject][ordered]@{
-        goalMarkdown = @"
-# 紐⑺몴
-
-$safeCommitTitle
-
-## 諛곌꼍
-
-$($Description.Trim())
-
-## ?깃났 湲곗?
-
-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-
-## ?쒖빟?ы빆
-
-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-
-## ?쒖쇅 踰붿쐞
-
-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-
-## ?섎룞 ?뺤씤
-
-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-"@
-        queue = [PSCustomObject][ordered]@{
-            goalTitle = $safeCommitTitle
-            goalSource = $goalRelativePath
-            createdAt = $now
-            updatedAt = $now
-            currentTaskId = "T001"
-            tasks = @(
-                [PSCustomObject][ordered]@{
-                    id = "T001"
-                    title = $safeCommitTitle
-                    description = $Description.Trim()
-                    type = "implementation"
-                    status = "in_progress"
-                    priority = "P0"
-                    dependsOn = @()
-                    filesLikelyToChange = @()
-                    verification = @(
-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-                    )
-                    commitMessage = $null
-                }
-            )
-        }
-        state = [PSCustomObject][ordered]@{
-            goalStatus = "in_progress"
-            currentTaskId = "T001"
-            currentLoop = 0
-            maxLoopsPerTask = 2
-            repeatedFailureCount = 0
-            lastCommand = $null
-            lastCommandStatus = "not_started"
-            lastErrorSummary = $null
-            lastReviewDecision = "not_started"
-            lastReviewSeverity = $null
-            lastCommitHash = $null
-            startedAt = $now
-            updatedAt = $now
-            stopReason = $null
-        }
-    }
-
-    $plan.goalMarkdown = @"
-# 紐⑺몴
-
-$safeCommitTitle
-
-## 諛곌꼍
-
-$($Description.Trim())
-
-## ?깃났 湲곗?
-
-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-
-## ?쒖빟?ы빆
-
-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-
-## ?쒖쇅 踰붿쐞
-
-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-
-## ?섎룞 ?뺤씤
-
-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-"@
-
-    $dryRunTask = @($plan.queue.tasks)[0]
-    $dryRunTask.verification = @(
-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-    )
-
-    return $plan
-}
-
-function ConvertFrom-CodexAutoGoalOutput {
-    param([string]$RawInput)
-
-    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-        try {
-            $parsed = $candidate | ConvertFrom-Json
-
-            if ($null -eq $parsed -or $parsed -is [System.Array]) {
-                continue
-            }
-
-            if (@(Get-AutoGoalValidationErrors $parsed).Count -gt 0) {
-                continue
-            }
-
-            return [PSCustomObject]@{
-                Parsed = $parsed
-                JsonText = ($parsed | ConvertTo-Json -Depth 30)
-            }
-        } catch {
-        }
-    }
-
-    throw "No valid auto-goal JSON object was found in Codex output."
-}
-
-function New-CodexAutoGoalPrompt {
-    param(
-        [string]$Title,
-        [string]$Description
-    )
-
-    return @"
-You are preparing local AI Dev Loop state files for the repository at $repoRoot.
-
-Return exactly one JSON object and no markdown fences or commentary.
-
-The user supplied this new goal:
-Title: $Title
-Description: $Description
-
-Create a small, safe goal plan for this repository. The response object must have these fields:
-- goalMarkdown: .ai-dev/goal.md에 작성할 마크다운 문자열입니다. "# 목표", "배경", "성공 기준", "제약사항", "범위 제외", "수동 검증" 섹
-션을 한국어로 포함해야 합니다.
-- queue: an object for .ai-dev/queue.json.
-- state: an object for .ai-dev/state.json.
-
-queue rules:
-- goalTitle must equal the supplied title.
-- goalSource must be ".ai-dev/goal.md".
-- currentTaskId must be "T001".
-- tasks must contain one to three tasks.
-- T001 must be status "in_progress"; later tasks, if any, must be "pending".
-- each task must include id, title, description, type, status, priority, dependsOn, filesLikelyToCh
-ange, verification, commitMessage.
-- type must be one of analysis, implementation, documentation, verification.
-- priority must be P0, P1, or P2.
-- dependsOn, filesLikelyToChange, and verification must be arrays.
-- commitMessage must be null or a short English commit message.
-
-state rules:
-- goalStatus must be "in_progress".
-- currentTaskId must be "T001".
-- currentLoop must be 0.
-- maxLoopsPerTask must be 2.
-- repeatedFailureCount must be 0.
-- lastCommand must be null.
-- lastCommandStatus must be "not_started".
-- lastErrorSummary must be null.
-- lastReviewDecision must be "not_started".
-- lastReviewSeverity must be null.
-- lastCommitHash must be null.
-- startedAt and updatedAt must be ISO 8601 strings.
-- stopReason must be null.
-
-Safety rules:
-- Do not mention server APIs, login, cloud sync, npm install, git reset, git clean, git push, DB de
-letion, or broad rewrites as implementation steps.
-- Prefer one small implementation task when the goal is small.
-- Use Korean for user-facing task titles, descriptions, verification, and goalMarkdown.
-"@
-}
-
-function Invoke-CycleCommand {
-    param(
-        [int]$StepNumber,
-        [string]$Name,
-        [string]$Command,
-        [string]$ScriptPath,
-        [string[]]$Arguments
-    )
-
-    if ($DryRun) {
-        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: child sc
-ript was not executed."
-        return
-    }
-
-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
-    $exitCode = $LASTEXITCODE
-    $message = $output.Trim()
-
-    if ([string]::IsNullOrWhiteSpace($message)) {
-        $message = "completed"
-    }
-
-    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-
-    if ($exitCode -ne 0) {
-        Stop-AutoGoal $script:steps "$Name`_failed" $false 1
-    }
-}
-
-function Get-FullCycleArguments {
-    $arguments = @("-MaxTasks", ([string]$MaxTasks), "-MaxSteps", ([string]$MaxSteps))
-
-    if ($AllowCodex) {
-        $arguments += "-AllowCodex"
-    }
-
-    if ($AllowReviewCodex) {
-        $arguments += "-AllowReviewCodex"
-    }
-
-    if ($AllowCommit) {
-        $arguments += "-AllowCommit"
-    }
-
-    # auto-goal writes goal/queue/state/current prompt/result files after the initial dirty gate.
-    # The downstream full cycle must tolerate those intended artifacts.
-    $arguments += "-AllowDirty"
-
-    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -not 
-[string]::IsNullOrWhiteSpace($_) })
-
-        if ($normalizedFiles.Count -gt 0) {
-            $arguments += "-CommitFiles"
-            $arguments += ($normalizedFiles -join ",")
-        }
-    }
-
-    return $arguments
-}
-
-Set-Location $repoRoot
-
-$script:steps = @()
-$script:autoGoalPlanPreview = $null
-$resolvedGoalPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $goalRelativePath))
-$resolvedQueuePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $queueRelativePath))
-$resolvedStatePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $stateRelativePath))
-$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $ResultPath))
-
-if (-not (Test-HasValue $GoalTitle)) {
-    $script:steps += New-StepResult 0 "validate-input" "check GoalTitle" $false $false 1 "GoalTitle
- must not be empty."
-    Stop-AutoGoal $script:steps "invalid_goal_title" $false 1
-}
-
-if (-not (Test-HasValue $GoalDescription)) {
-    $script:steps += New-StepResult 0 "validate-input" "check GoalDescription" $false $false 1 "Goa
-lDescription must not be empty."
-    Stop-AutoGoal $script:steps "invalid_goal_description" $false 1
-}
-
-if ($MaxTasks -lt 1) {
-    $script:steps += New-StepResult 0 "validate-input" "check MaxTasks" $false $false 1 "MaxTasks m
-ust be at least 1."
-    Stop-AutoGoal $script:steps "max_tasks_must_be_at_least_1" $false 1
-}
-
-if ($MaxSteps -lt 1) {
-    $script:steps += New-StepResult 0 "validate-input" "check MaxSteps" $false $false 1 "MaxSteps m
-ust be at least 1."
-    Stop-AutoGoal $script:steps "max_steps_must_be_at_least_1" $false 1
-}
-
-$makePromptPath = Join-Path $PSScriptRoot "ai-dev-make-prompt.ps1"
-$autoCycleFullPath = Join-Path $PSScriptRoot "ai-dev-auto-cycle-full.ps1"
-
-foreach ($requiredScript in @($makePromptPath, $autoCycleFullPath)) {
-    if (-not (Test-Path -LiteralPath $requiredScript -PathType Leaf)) {
-        $script:steps += New-StepResult 0 "prepare" "check required scripts" $false $false 1 "Missi
-ng required script: $requiredScript"
-        Stop-AutoGoal $script:steps "prepare_failed" $false 1
-    }
-}
-
-$shouldRunFullCycle = [bool]($AllowRun -or $AllowCodex -or $AllowReviewCodex -or $AllowCommit)
-$fullCycleArguments = @(Get-FullCycleArguments)
-$fullCycleCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1 $($fullCycleArguments -join ' ')".Trim()
-
-$script:steps += New-StepResult 1 "validate-input" "check GoalTitle/GoalDescription" $false $false 
-0 "Input validation completed: $GoalTitle"
-
-if ($DryRun) {
-    $script:autoGoalPlanPreview = New-DryRunAutoGoalPlan $GoalTitle.Trim() $GoalDescription.Trim()
-    $previewValidationErrors = @(Get-AutoGoalValidationErrors $script:autoGoalPlanPreview)
-
-    if ($previewValidationErrors.Count -gt 0) {
-        $script:steps += New-StepResult 2 "preview-plan" "local dry-run plan preview" $false $false
- 1 "DryRun preview plan validation failed: $($previewValidationErrors -join '; ')"
-        Stop-AutoGoal $script:steps "dry_run_preview_invalid" $false 1
-    }
-
-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 0 "Dr
-yRun: dirty worktree gate was not executed."
-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false $
-true 0 "DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, task: $GoalTitle
-"
-    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" 
-$false $true 0 "DryRun: preview goal/queue/state plan passed local schema validation."
-    $script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, $
-stateRelativePath" $false $true 0 "DryRun: state files were not written."
-    $script:steps += New-StepResult 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scrip
-ts/ai-dev-make-prompt.ps1" $false $true 0 "DryRun: current-task-prompt.md was not generated."
-
-    if ($shouldRunFullCycle) {
-        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "D
-ryRun: explicit run option is present, but full cycle was not executed. -AllowRun only invokes the 
-full-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-    } else {
-        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "D
-ryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the full
--cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-    }
-
-    Stop-AutoGoal $script:steps "dry_run" $false 0
-}
-
-$statusLines = @(Get-GitStatusLines)
-
-if ($statusLines.Count -gt 0 -and -not $AllowDirty) {
-    $dirtyText = ($statusLines -join "`n")
-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 1 "Wo
-rktree is dirty. Use -AllowDirty only when this is intentional.`n$dirtyText"
-    Stop-AutoGoal $script:steps "dirty_worktree" $false 1
-}
-
-if ($statusLines.Count -gt 0) {
-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 "A
-llowDirty is set. DirtyCount: $($statusLines.Count)"
-} else {
-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 "W
-orktree is clean."
-}
-
-$codexCommand = Get-Command codex -ErrorAction SilentlyContinue
-
-if ($null -eq $codexCommand) {
-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false $
-false 1 "Codex CLI was not found."
-    Stop-AutoGoal $script:steps "codex_not_found" $false 1
-}
-
-$plannerPrompt = New-CodexAutoGoalPrompt $GoalTitle.Trim() $GoalDescription.Trim()
-$startedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-$codexExitCode = $LASTEXITCODE
-$endedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-
-$resultContent = @"
-# Codex Auto Goal Result
-
-## Run
-
-- Started at: $startedAt
-- Ended at: $endedAt
-- Exit code: $codexExitCode
-- Goal title: $($GoalTitle.Trim())
-- Command: codex exec <auto-goal planning prompt>
-
-## Output
-
-${codeFence}text
-$codexOutput
-$codeFence
-"@
-
-[System.IO.File]::WriteAllText($resolvedResultPath, $resultContent, $utf8WithBom)
-
-if ($codexExitCode -ne 0) {
-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $f
-alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
-    Stop-AutoGoal $script:steps "codex_plan_failed" $false 1
-}
-
-$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $false
- 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
-
-try {
-    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-} catch {
-    $preview = Get-InputPreview $codexOutput
-    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" 
-$false $false 1 "Codex plan JSON extraction failed: $($_.Exception.Message) Output preview: $previe
-w"
-    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
-}
-
-$script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" $fal
-se $false 0 "goalMarkdown, queue, and state JSON validation completed. currentTaskId: $($autoGoalPl
-an.Parsed.queue.currentTaskId)"
-$script:autoGoalPlanPreview = $autoGoalPlan.Parsed
-
-$goalMarkdown = [string]$autoGoalPlan.Parsed.goalMarkdown
-$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 30
-$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 30
-
-[System.IO.File]::WriteAllText($resolvedGoalPath, $goalMarkdown, $utf8WithBom)
-[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $utf8WithBom)
-[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $utf8WithBom)
-
-$script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, $stat
-eRelativePath" $true $false 0 "New goal, queue, and state files were written."
-
-Invoke-CycleCommand 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-p
-rompt.ps1" $makePromptPath @()
-
-if (-not $shouldRunFullCycle) {
-    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "Full 
-cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the full-cycle wrapp
-er; implementation and review still require -AllowCodex and -AllowReviewCodex."
-    Stop-AutoGoal $script:steps "prepared_without_full_cycle" $true 0
-}
-
-Invoke-CycleCommand 7 "auto-cycle-full" $fullCycleCommandText $autoCycleFullPath $fullCycleArgument
-s
-
-Stop-AutoGoal $script:steps "completed" $true 0
-
-
- succeeded in 3198ms:
-param(
-    [string]$ExtraInstructionsPath = ".ai-dev/extra-instructions.md"
-)
-
-. "$PSScriptRoot\ai-dev-env.ps1"
-
-$projectRoot = (Get-Location).Path
-$goalRelativePath = ".ai-dev/goal.md"
-$queueRelativePath = ".ai-dev/queue.json"
-$stateRelativePath = ".ai-dev/state.json"
-$promptRelativePath = ".ai-dev/current-task-prompt.md"
-
-$goalPath = Join-Path $projectRoot $goalRelativePath
-$queuePath = Join-Path $projectRoot $queueRelativePath
-$statePath = Join-Path $projectRoot $stateRelativePath
-$promptPath = Join-Path $projectRoot $promptRelativePath
-
-function Stop-WithError {
-    param(
-        [string]$Message
-    )
-
-    Write-Error $Message
-    exit 1
-}
-
-function Read-JsonFile {
-    param(
-        [string]$Path,
-        [string]$RelativePath
-    )
-
-    try {
-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
-    } catch {
-        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
-    }
-}
-
-function Get-MissingFields {
-    param(
-        [object]$InputObject,
-        [string[]]$RequiredFields
-    )
-
-    $missingFields = @()
-
-    foreach ($field in $RequiredFields) {
-        if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
-            $missingFields += $field
-        }
-    }
-
-    return $missingFields
-}
-
-function Stop-ForMissingFields {
-    param(
-        [string]$SourceName,
-        [string[]]$MissingFields
-    )
-
-    if ($MissingFields.Count -eq 0) {
-        return
-    }
-
-    Write-Host "$SourceName 누락 필드:"
-    foreach ($field in $MissingFields) {
-        Write-Host "  - $field"
-    }
-
-    Stop-WithError "$SourceName 필수 필드가 누락되었습니다."
-}
-
-function Test-HasValue {
-    param(
-        [object]$Value
-    )
-
-    if ($null -eq $Value) {
-        return $false
-    }
-
-    if ($Value -is [string]) {
-        return -not [string]::IsNullOrWhiteSpace($Value)
-    }
-
-    return $true
-}
-
-function Convert-ToMarkdownList {
-    param(
-        [object]$Value
-    )
-
-    if ($null -eq $Value -or @($Value).Count -eq 0) {
-        return "- 없음"
-    }
-
-    return (@($Value) | ForEach-Object { "- $_" }) -join "`r`n"
-}
-
-foreach ($requiredPath in @($goalRelativePath, $queueRelativePath, $stateRelativePath)) {
-    $fullPath = Join-Path $projectRoot $requiredPath
-
-    if (-not (Test-Path -LiteralPath $fullPath -PathType Leaf)) {
-        Stop-WithError "필수 파일이 없습니다: $requiredPath"
-    }
-}
-
-$goalContent = Get-Content -Raw -Encoding UTF8 -LiteralPath $goalPath
-$queue = Read-JsonFile $queuePath $queueRelativePath
-$state = Read-JsonFile $statePath $stateRelativePath
-
-$queueRequiredFields = @(
-    "goalTitle",
-    "currentTaskId",
-    "tasks"
-)
-$stateRequiredFields = @(
-    "goalStatus",
-    "currentTaskId"
-)
-
-Stop-ForMissingFields $queueRelativePath (Get-MissingFields $queue $queueRequiredFields)
-Stop-ForMissingFields $stateRelativePath (Get-MissingFields $state $stateRequiredFields)
-
-if ($null -eq $queue.tasks -or -not ($queue.tasks -is [System.Collections.IEnumerable])) {
-    Stop-WithError "$queueRelativePath tasks 필드는 배열이어야 합니다."
-}
-
-$tasks = @($queue.tasks)
-$currentTaskId = $null
-
-if (Test-HasValue $state.currentTaskId) {
-    $currentTaskId = [string]$state.currentTaskId
-} elseif (Test-HasValue $queue.currentTaskId) {
-    $currentTaskId = [string]$queue.currentTaskId
-} else {
-    $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -First 1
-
-    if ($null -ne $pendingTask) {
-        $currentTaskId = [string]$pendingTask.id
-    }
-}
-
-if (-not (Test-HasValue $currentTaskId)) {
-    Stop-WithError "수행할 task가 없습니다."
-}
-
-$currentTask = $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
-
-if ($null -eq $currentTask) {
-    Stop-WithError "currentTaskId에 해당하는 task를 찾을 수 없습니다: $currentTaskId"
-}
-
-$taskRequiredFields = @(
-    "id",
-    "title",
-    "description",
-    "type",
-    "status",
-    "priority",
-    "dependsOn",
-    "filesLikelyToChange",
-    "verification"
-)
-
-Stop-ForMissingFields "task $currentTaskId" (Get-MissingFields $currentTask $taskRequiredFields)
-
-$dependsOnText = Convert-ToMarkdownList $currentTask.dependsOn
-$likelyFilesText = Convert-ToMarkdownList $currentTask.filesLikelyToChange
-$verificationText = Convert-ToMarkdownList $currentTask.verification
-$extraInstructionsContent = $null
-
-if (-not [string]::IsNullOrWhiteSpace($ExtraInstructionsPath)) {
-    $resolvedExtraInstructionsPath = if ([System.IO.Path]::IsPathRooted($ExtraInstructionsPath)) {
-        $ExtraInstructionsPath
-    } else {
-        Join-Path $projectRoot $ExtraInstructionsPath
-    }
-
-    if (Test-Path -LiteralPath $resolvedExtraInstructionsPath -PathType Leaf) {
-        $extraInstructionsContent = Get-Content -Raw -Encoding UTF8 -LiteralPath $resolvedExtraInst
-ructionsPath
-    }
-}
-
-$promptContent = @"
-# Current Task Prompt
-
-## Role
-
-너는 이 저장소의 자동 개발 에이전트다.
-
-## Goal
-
-$goalContent
-
-## Current Task
-
-- Task ID: $($currentTask.id)
-- Title: $($currentTask.title)
-- Description: $($currentTask.description)
-- Type: $($currentTask.type)
-- Status: $($currentTask.status)
-- Priority: $($currentTask.priority)
+- Status: in_progress
+- Priority: P1
 - Depends on:
-$dependsOnText
+- 없음
 
 ## Task Scope
 
@@ -2848,13 +91,14 @@ $dependsOnText
 
 ## Likely Files
 
-$likelyFilesText
+- src/App.tsx
 
 ## Verification
 
-$verificationText
+- 업무 검색창 placeholder 문구가 변경되었는지 확인한다.
+- 검색 입력 동작과 기존 필터링 동작이 유지되는지 확인한다.
 
-- 필요한 경우 ``npm run build``는 사람이 별도로 실행한다.
+- 필요한 경우 `npm run build`는 사람이 별도로 실행한다.
 - 이 프롬프트는 자동으로 build, test, lint를 실행하라고 지시하지 않는다.
 
 ## Required Output
@@ -2864,12 +108,12 @@ $verificationText
 - 검증 방법
 - 남은 위험
 - 다음 task로 넘어가도 되는지 여부
-- ``.ai-dev/loop-log.md``에 기록할 작업 요약
+- `.ai-dev/loop-log.md`에 기록할 작업 요약
 
 ## Hard Rules
 
-- ``src`` 외 파일을 수정해야 하는 경우 이유를 기록한다.
-- ``package.json``과 ``package-lock.json``은 수정하지 않는다. 꼭 필요하면 중단하고 이유만 기록한다.
+- `src` 외 파일을 수정해야 하는 경우 이유를 기록한다.
+- `package.json`과 `package-lock.json`은 수정하지 않는다. 꼭 필요하면 중단하고 이유만 기록한다.
 - 실제 DB 삭제 또는 초기화를 하지 않는다.
 - 사용자 데이터 복원 또는 덮어쓰기를 하지 않는다.
 - 현재 task와 무관한 UI 전면 개편을 하지 않는다.
@@ -2885,2673 +129,620 @@ $verificationText
 - 패키지 추가가 필요한 경우
 - 데이터 삭제 또는 마이그레이션이 필요한 경우
 - 같은 오류가 반복되는 경우
-"@
-
-if (-not [string]::IsNullOrWhiteSpace($extraInstructionsContent)) {
-    $promptContent += @"
-
-
-## Extra Instructions
-
-$extraInstructionsContent
-"@
-}
-
-$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-[System.IO.File]::WriteAllText($promptPath, $promptContent, $utf8WithBom)
-
-Write-Host "생성된 프롬프트 파일: $promptRelativePath"
-Write-Host "Current task id: $($currentTask.id)"
-Write-Host "Current task title: $($currentTask.title)"
 
 codex
-현재 스크립트는 이미 주요 흐름을 갖고 있지만, DryRun 미리보기의 한국어 문자열이 깨져 있고 생성 JSON 검증이 프롬프트 규칙 일부를 충분히 강제하지 않습니다. 파일 작성
- 전에 이 두 부분을 좁게 보강하겠습니다.
+지시 범위는 placeholder 문구 하나만 바꾸는 작은 구현입니다. 검색 입력 위치를 확인하기 위해 `src` 안에서 placeholder 관련 문자열만 찾겠습니다.
 exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPa
-th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern 'New-DryRunAutoGoalPlan',
-'Get-QueueValidationErrors','currentTaskId must be' -Context 0,80" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPa
-th 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md' -Pattern '목표 입력 기반 자동 실행' -Context 0,120" in 
-D:\ai-apps\planpilot-local
- succeeded in 2293ms:
-
-> scripts\ai-dev-auto-goal.ps1:262:function Get-QueueValidationErrors {
-  scripts\ai-dev-auto-goal.ps1:263:    param([object]$Queue)
-  scripts\ai-dev-auto-goal.ps1:264:
-  scripts\ai-dev-auto-goal.ps1:265:    $errors = @()
-  scripts\ai-dev-auto-goal.ps1:266:    $requiredFields = @("goalTitle", "goalSource", "createdAt", 
-"updatedAt", "curren
-tTaskId", "tasks")
-  scripts\ai-dev-auto-goal.ps1:267:
-  scripts\ai-dev-auto-goal.ps1:268:    foreach ($field in @(Get-MissingFields $Queue $requiredField
-s)) {
-  scripts\ai-dev-auto-goal.ps1:269:        $errors += "queue missing field: $field"
-  scripts\ai-dev-auto-goal.ps1:270:    }
-  scripts\ai-dev-auto-goal.ps1:271:
-  scripts\ai-dev-auto-goal.ps1:272:    if ($errors.Count -gt 0) {
-  scripts\ai-dev-auto-goal.ps1:273:        return $errors
-  scripts\ai-dev-auto-goal.ps1:274:    }
-  scripts\ai-dev-auto-goal.ps1:275:
-  scripts\ai-dev-auto-goal.ps1:276:    if (-not (Test-HasValue $Queue.goalTitle)) {
-  scripts\ai-dev-auto-goal.ps1:277:        $errors += "queue.goalTitle must not be empty."
-  scripts\ai-dev-auto-goal.ps1:278:    }
-  scripts\ai-dev-auto-goal.ps1:279:
-  scripts\ai-dev-auto-goal.ps1:280:    if ($Queue.goalSource -ne $goalRelativePath) {
-  scripts\ai-dev-auto-goal.ps1:281:        $errors += "queue.goalSource must be $goalRelativePath."
-  scripts\ai-dev-auto-goal.ps1:282:    }
-  scripts\ai-dev-auto-goal.ps1:283:
-  scripts\ai-dev-auto-goal.ps1:284:    if ($Queue.tasks -isnot [System.Collections.IEnumerable] -or
- $Queue.tasks -is [s
-tring]) {
-  scripts\ai-dev-auto-goal.ps1:285:        $errors += "queue.tasks must be an array."
-  scripts\ai-dev-auto-goal.ps1:286:        return $errors
-  scripts\ai-dev-auto-goal.ps1:287:    }
-  scripts\ai-dev-auto-goal.ps1:288:
-  scripts\ai-dev-auto-goal.ps1:289:    $tasks = @($Queue.tasks)
-  scripts\ai-dev-auto-goal.ps1:290:
-  scripts\ai-dev-auto-goal.ps1:291:    if ($tasks.Count -lt 1) {
-  scripts\ai-dev-auto-goal.ps1:292:        $errors += "queue.tasks must contain at least one task."
-  scripts\ai-dev-auto-goal.ps1:293:        return $errors
-  scripts\ai-dev-auto-goal.ps1:294:    }
-  scripts\ai-dev-auto-goal.ps1:295:
-  scripts\ai-dev-auto-goal.ps1:296:    if ($tasks.Count -gt 3) {
-  scripts\ai-dev-auto-goal.ps1:297:        $errors += "queue.tasks must contain no more than three 
-tasks."
-  scripts\ai-dev-auto-goal.ps1:298:    }
-  scripts\ai-dev-auto-goal.ps1:299:
-  scripts\ai-dev-auto-goal.ps1:300:    $taskIds = @()
-  scripts\ai-dev-auto-goal.ps1:301:
-  scripts\ai-dev-auto-goal.ps1:302:    foreach ($task in $tasks) {
-  scripts\ai-dev-auto-goal.ps1:303:        $taskRequiredFields = @("id", "title", "description", "t
-ype", "status", "pri
-ority", "dependsOn", "filesLikelyToChange", "verification", "commitMessage")
-  scripts\ai-dev-auto-goal.ps1:304:        $taskMissingFields = @(Get-MissingFields $task $taskRequ
-iredFields)
-  scripts\ai-dev-auto-goal.ps1:305:
-  scripts\ai-dev-auto-goal.ps1:306:        foreach ($field in $taskMissingFields) {
-  scripts\ai-dev-auto-goal.ps1:307:            $errors += "task missing field: $field"
-  scripts\ai-dev-auto-goal.ps1:308:        }
-  scripts\ai-dev-auto-goal.ps1:309:
-  scripts\ai-dev-auto-goal.ps1:310:        if ($taskMissingFields.Count -gt 0) {
-  scripts\ai-dev-auto-goal.ps1:311:            continue
-  scripts\ai-dev-auto-goal.ps1:312:        }
-  scripts\ai-dev-auto-goal.ps1:313:
-  scripts\ai-dev-auto-goal.ps1:314:        if (-not (Test-HasValue $task.id)) {
-  scripts\ai-dev-auto-goal.ps1:315:            $errors += "task.id must not be empty."
-  scripts\ai-dev-auto-goal.ps1:316:        } else {
-  scripts\ai-dev-auto-goal.ps1:317:            $taskIds += [string]$task.id
-  scripts\ai-dev-auto-goal.ps1:318:        }
-  scripts\ai-dev-auto-goal.ps1:319:
-  scripts\ai-dev-auto-goal.ps1:320:        if (-not (Test-HasValue $task.title)) {
-  scripts\ai-dev-auto-goal.ps1:321:            $errors += "task.title must not be empty."
-  scripts\ai-dev-auto-goal.ps1:322:        }
-  scripts\ai-dev-auto-goal.ps1:323:
-  scripts\ai-dev-auto-goal.ps1:324:        if (-not (Test-HasValue $task.description)) {
-  scripts\ai-dev-auto-goal.ps1:325:            $errors += "task.description must not be empty."
-  scripts\ai-dev-auto-goal.ps1:326:        }
-  scripts\ai-dev-auto-goal.ps1:327:
-  scripts\ai-dev-auto-goal.ps1:328:        if (@("analysis", "implementation", "documentation", "ve
-rification") -notcon
-tains $task.type) {
-  scripts\ai-dev-auto-goal.ps1:329:            $errors += "invalid task.type: $($task.type)"
-  scripts\ai-dev-auto-goal.ps1:330:        }
-  scripts\ai-dev-auto-goal.ps1:331:
-  scripts\ai-dev-auto-goal.ps1:332:        if (@("pending", "in_progress", "done", "blocked") -notc
-ontains $task.status
-) {
-  scripts\ai-dev-auto-goal.ps1:333:            $errors += "invalid task.status: $($task.status)"
-  scripts\ai-dev-auto-goal.ps1:334:        }
-  scripts\ai-dev-auto-goal.ps1:335:
-  scripts\ai-dev-auto-goal.ps1:336:        if (@("P0", "P1", "P2") -notcontains $task.priority) {
-  scripts\ai-dev-auto-goal.ps1:337:            $errors += "invalid task.priority: $($task.priority)
-"
-  scripts\ai-dev-auto-goal.ps1:338:        }
-  scripts\ai-dev-auto-goal.ps1:339:
-  scripts\ai-dev-auto-goal.ps1:340:        if ($task.dependsOn -isnot [System.Collections.IEnumerab
-le] -or $task.depend
-sOn -is [string]) {
-  scripts\ai-dev-auto-goal.ps1:341:            $errors += "task.dependsOn must be an array: $($task
-.id)"
-  scripts\ai-dev-auto-goal.ps1:342:        }
-> scripts\ai-dev-auto-goal.ps1:419:    $errors += @(Get-QueueValidationErrors $Plan.queue)
-  scripts\ai-dev-auto-goal.ps1:420:    $errors += @(Get-StateValidationErrors $Plan.state $Plan.que
-ue)
-  scripts\ai-dev-auto-goal.ps1:421:
-  scripts\ai-dev-auto-goal.ps1:422:    return $errors
-  scripts\ai-dev-auto-goal.ps1:423:}
-  scripts\ai-dev-auto-goal.ps1:424:
-> scripts\ai-dev-auto-goal.ps1:425:function New-DryRunAutoGoalPlan {
-  scripts\ai-dev-auto-goal.ps1:426:    param(
-  scripts\ai-dev-auto-goal.ps1:427:        [string]$Title,
-  scripts\ai-dev-auto-goal.ps1:428:        [string]$Description
-  scripts\ai-dev-auto-goal.ps1:429:    )
-  scripts\ai-dev-auto-goal.ps1:430:
-  scripts\ai-dev-auto-goal.ps1:431:    $now = (Get-Date).ToUniversalTime().ToString("o")
-  scripts\ai-dev-auto-goal.ps1:432:    $safeCommitTitle = $Title.Trim()
-  scripts\ai-dev-auto-goal.ps1:433:
-  scripts\ai-dev-auto-goal.ps1:434:    $plan = [PSCustomObject][ordered]@{
-  scripts\ai-dev-auto-goal.ps1:435:        goalMarkdown = @"
-  scripts\ai-dev-auto-goal.ps1:436:# 紐⑺몴
-  scripts\ai-dev-auto-goal.ps1:437:
-  scripts\ai-dev-auto-goal.ps1:438:$safeCommitTitle
-  scripts\ai-dev-auto-goal.ps1:439:
-  scripts\ai-dev-auto-goal.ps1:440:## 諛곌꼍
-  scripts\ai-dev-auto-goal.ps1:441:
-  scripts\ai-dev-auto-goal.ps1:442:$($Description.Trim())
-  scripts\ai-dev-auto-goal.ps1:443:
-  scripts\ai-dev-auto-goal.ps1:444:## ?깃났 湲곗?
-  scripts\ai-dev-auto-goal.ps1:445:
-  scripts\ai-dev-auto-goal.ps1:446:- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-  scripts\ai-dev-auto-goal.ps1:447:- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-  scripts\ai-dev-auto-goal.ps1:448:- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-  scripts\ai-dev-auto-goal.ps1:449:
-  scripts\ai-dev-auto-goal.ps1:450:## ?쒖빟?ы빆
-  scripts\ai-dev-auto-goal.ps1:451:
-  scripts\ai-dev-auto-goal.ps1:452:- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?
-섏? ?딅뒗??
-  scripts\ai-dev-auto-goal.ps1:453:- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-  scripts\ai-dev-auto-goal.ps1:454:
-  scripts\ai-dev-auto-goal.ps1:455:## ?쒖쇅 踰붿쐞
-  scripts\ai-dev-auto-goal.ps1:456:
-  scripts\ai-dev-auto-goal.ps1:457:- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-  scripts\ai-dev-auto-goal.ps1:458:
-  scripts\ai-dev-auto-goal.ps1:459:## ?섎룞 ?뺤씤
-  scripts\ai-dev-auto-goal.ps1:460:
-  scripts\ai-dev-auto-goal.ps1:461:- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-  scripts\ai-dev-auto-goal.ps1:462:- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-  scripts\ai-dev-auto-goal.ps1:463:- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-  scripts\ai-dev-auto-goal.ps1:464:"@
-  scripts\ai-dev-auto-goal.ps1:465:        queue = [PSCustomObject][ordered]@{
-  scripts\ai-dev-auto-goal.ps1:466:            goalTitle = $safeCommitTitle
-  scripts\ai-dev-auto-goal.ps1:467:            goalSource = $goalRelativePath
-  scripts\ai-dev-auto-goal.ps1:468:            createdAt = $now
-  scripts\ai-dev-auto-goal.ps1:469:            updatedAt = $now
-  scripts\ai-dev-auto-goal.ps1:470:            currentTaskId = "T001"
-  scripts\ai-dev-auto-goal.ps1:471:            tasks = @(
-  scripts\ai-dev-auto-goal.ps1:472:                [PSCustomObject][ordered]@{
-  scripts\ai-dev-auto-goal.ps1:473:                    id = "T001"
-  scripts\ai-dev-auto-goal.ps1:474:                    title = $safeCommitTitle
-  scripts\ai-dev-auto-goal.ps1:475:                    description = $Description.Trim()
-  scripts\ai-dev-auto-goal.ps1:476:                    type = "implementation"
-  scripts\ai-dev-auto-goal.ps1:477:                    status = "in_progress"
-  scripts\ai-dev-auto-goal.ps1:478:                    priority = "P0"
-  scripts\ai-dev-auto-goal.ps1:479:                    dependsOn = @()
-  scripts\ai-dev-auto-goal.ps1:480:                    filesLikelyToChange = @()
-  scripts\ai-dev-auto-goal.ps1:481:                    verification = @(
-  scripts\ai-dev-auto-goal.ps1:482:                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-  scripts\ai-dev-auto-goal.ps1:483:                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-  scripts\ai-dev-auto-goal.ps1:484:                    )
-  scripts\ai-dev-auto-goal.ps1:485:                    commitMessage = $null
-  scripts\ai-dev-auto-goal.ps1:486:                }
-  scripts\ai-dev-auto-goal.ps1:487:            )
-  scripts\ai-dev-auto-goal.ps1:488:        }
-  scripts\ai-dev-auto-goal.ps1:489:        state = [PSCustomObject][ordered]@{
-  scripts\ai-dev-auto-goal.ps1:490:            goalStatus = "in_progress"
-  scripts\ai-dev-auto-goal.ps1:491:            currentTaskId = "T001"
-  scripts\ai-dev-auto-goal.ps1:492:            currentLoop = 0
-  scripts\ai-dev-auto-goal.ps1:493:            maxLoopsPerTask = 2
-  scripts\ai-dev-auto-goal.ps1:494:            repeatedFailureCount = 0
-  scripts\ai-dev-auto-goal.ps1:495:            lastCommand = $null
-  scripts\ai-dev-auto-goal.ps1:496:            lastCommandStatus = "not_started"
-  scripts\ai-dev-auto-goal.ps1:497:            lastErrorSummary = $null
-  scripts\ai-dev-auto-goal.ps1:498:            lastReviewDecision = "not_started"
-  scripts\ai-dev-auto-goal.ps1:499:            lastReviewSeverity = $null
-  scripts\ai-dev-auto-goal.ps1:500:            lastCommitHash = $null
-  scripts\ai-dev-auto-goal.ps1:501:            startedAt = $now
-  scripts\ai-dev-auto-goal.ps1:502:            updatedAt = $now
-  scripts\ai-dev-auto-goal.ps1:503:            stopReason = $null
-  scripts\ai-dev-auto-goal.ps1:504:        }
-  scripts\ai-dev-auto-goal.ps1:505:    }
-> scripts\ai-dev-auto-goal.ps1:596:- currentTaskId must be "T001".
-  scripts\ai-dev-auto-goal.ps1:597:- tasks must contain one to three tasks.
-  scripts\ai-dev-auto-goal.ps1:598:- T001 must be status "in_progress"; later tasks, if any, must b
-e "pending".
-  scripts\ai-dev-auto-goal.ps1:599:- each task must include id, title, description, type, status, p
-riority, dependsOn, 
-filesLikelyToChange, verification, commitMessage.
-  scripts\ai-dev-auto-goal.ps1:600:- type must be one of analysis, implementation, documentation, v
-erification.
-  scripts\ai-dev-auto-goal.ps1:601:- priority must be P0, P1, or P2.
-  scripts\ai-dev-auto-goal.ps1:602:- dependsOn, filesLikelyToChange, and verification must be array
-s.
-  scripts\ai-dev-auto-goal.ps1:603:- commitMessage must be null or a short English commit message.
-  scripts\ai-dev-auto-goal.ps1:604:
-  scripts\ai-dev-auto-goal.ps1:605:state rules:
-  scripts\ai-dev-auto-goal.ps1:606:- goalStatus must be "in_progress".
-> scripts\ai-dev-auto-goal.ps1:607:- currentTaskId must be "T001".
-  scripts\ai-dev-auto-goal.ps1:608:- currentLoop must be 0.
-  scripts\ai-dev-auto-goal.ps1:609:- maxLoopsPerTask must be 2.
-  scripts\ai-dev-auto-goal.ps1:610:- repeatedFailureCount must be 0.
-  scripts\ai-dev-auto-goal.ps1:611:- lastCommand must be null.
-  scripts\ai-dev-auto-goal.ps1:612:- lastCommandStatus must be "not_started".
-  scripts\ai-dev-auto-goal.ps1:613:- lastErrorSummary must be null.
-  scripts\ai-dev-auto-goal.ps1:614:- lastReviewDecision must be "not_started".
-  scripts\ai-dev-auto-goal.ps1:615:- lastReviewSeverity must be null.
-  scripts\ai-dev-auto-goal.ps1:616:- lastCommitHash must be null.
-  scripts\ai-dev-auto-goal.ps1:617:- startedAt and updatedAt must be ISO 8601 strings.
-  scripts\ai-dev-auto-goal.ps1:618:- stopReason must be null.
-  scripts\ai-dev-auto-goal.ps1:619:
-  scripts\ai-dev-auto-goal.ps1:620:Safety rules:
-  scripts\ai-dev-auto-goal.ps1:621:- Do not mention server APIs, login, cloud sync, npm install, gi
-t reset, git clean, 
-git push, DB deletion, or broad rewrites as implementation steps.
-  scripts\ai-dev-auto-goal.ps1:622:- Prefer one small implementation task when the goal is small.
-  scripts\ai-dev-auto-goal.ps1:623:- Use Korean for user-facing task titles, descriptions, verifica
-tion, and goalMarkdo
-wn.
-  scripts\ai-dev-auto-goal.ps1:624:"@
-  scripts\ai-dev-auto-goal.ps1:625:}
-  scripts\ai-dev-auto-goal.ps1:626:
-  scripts\ai-dev-auto-goal.ps1:627:function Invoke-CycleCommand {
-  scripts\ai-dev-auto-goal.ps1:628:    param(
-  scripts\ai-dev-auto-goal.ps1:629:        [int]$StepNumber,
-  scripts\ai-dev-auto-goal.ps1:630:        [string]$Name,
-  scripts\ai-dev-auto-goal.ps1:631:        [string]$Command,
-  scripts\ai-dev-auto-goal.ps1:632:        [string]$ScriptPath,
-  scripts\ai-dev-auto-goal.ps1:633:        [string[]]$Arguments
-  scripts\ai-dev-auto-goal.ps1:634:    )
-  scripts\ai-dev-auto-goal.ps1:635:
-  scripts\ai-dev-auto-goal.ps1:636:    if ($DryRun) {
-  scripts\ai-dev-auto-goal.ps1:637:        $script:steps += New-StepResult $StepNumber $Name $Comma
-nd $false $true 0 "D
-ryRun: child script was not executed."
-  scripts\ai-dev-auto-goal.ps1:638:        return
-  scripts\ai-dev-auto-goal.ps1:639:    }
-  scripts\ai-dev-auto-goal.ps1:640:
-  scripts\ai-dev-auto-goal.ps1:641:    $output = & powershell -ExecutionPolicy Bypass -File $Script
-Path @Arguments 2>&1
- | Out-String
-  scripts\ai-dev-auto-goal.ps1:642:    $exitCode = $LASTEXITCODE
-  scripts\ai-dev-auto-goal.ps1:643:    $message = $output.Trim()
-  scripts\ai-dev-auto-goal.ps1:644:
-  scripts\ai-dev-auto-goal.ps1:645:    if ([string]::IsNullOrWhiteSpace($message)) {
-  scripts\ai-dev-auto-goal.ps1:646:        $message = "completed"
-  scripts\ai-dev-auto-goal.ps1:647:    }
-  scripts\ai-dev-auto-goal.ps1:648:
-  scripts\ai-dev-auto-goal.ps1:649:    $script:steps += New-StepResult $StepNumber $Name $Command $
-true $false $exitCod
-e $message
-  scripts\ai-dev-auto-goal.ps1:650:
-  scripts\ai-dev-auto-goal.ps1:651:    if ($exitCode -ne 0) {
-  scripts\ai-dev-auto-goal.ps1:652:        Stop-AutoGoal $script:steps "$Name`_failed" $false 1
-  scripts\ai-dev-auto-goal.ps1:653:    }
-  scripts\ai-dev-auto-goal.ps1:654:}
-  scripts\ai-dev-auto-goal.ps1:655:
-  scripts\ai-dev-auto-goal.ps1:656:function Get-FullCycleArguments {
-  scripts\ai-dev-auto-goal.ps1:657:    $arguments = @("-MaxTasks", ([string]$MaxTasks), "-MaxSteps"
-, ([string]$MaxSteps
-))
-  scripts\ai-dev-auto-goal.ps1:658:
-  scripts\ai-dev-auto-goal.ps1:659:    if ($AllowCodex) {
-  scripts\ai-dev-auto-goal.ps1:660:        $arguments += "-AllowCodex"
-  scripts\ai-dev-auto-goal.ps1:661:    }
-  scripts\ai-dev-auto-goal.ps1:662:
-  scripts\ai-dev-auto-goal.ps1:663:    if ($AllowReviewCodex) {
-  scripts\ai-dev-auto-goal.ps1:664:        $arguments += "-AllowReviewCodex"
-  scripts\ai-dev-auto-goal.ps1:665:    }
-  scripts\ai-dev-auto-goal.ps1:666:
-  scripts\ai-dev-auto-goal.ps1:667:    if ($AllowCommit) {
-  scripts\ai-dev-auto-goal.ps1:668:        $arguments += "-AllowCommit"
-  scripts\ai-dev-auto-goal.ps1:669:    }
-  scripts\ai-dev-auto-goal.ps1:670:
-  scripts\ai-dev-auto-goal.ps1:671:    # auto-goal writes goal/queue/state/current prompt/result fi
-les after the initia
-l dirty gate.
-  scripts\ai-dev-auto-goal.ps1:672:    # The downstream full cycle must tolerate those intended art
-ifacts.
-  scripts\ai-dev-auto-goal.ps1:673:    $arguments += "-AllowDirty"
-  scripts\ai-dev-auto-goal.ps1:674:
-  scripts\ai-dev-auto-goal.ps1:675:    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-  scripts\ai-dev-auto-goal.ps1:676:        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ 
--split "," } | Where
--Object { -not [string]::IsNullOrWhiteSpace($_) })
-  scripts\ai-dev-auto-goal.ps1:677:
-  scripts\ai-dev-auto-goal.ps1:678:        if ($normalizedFiles.Count -gt 0) {
-  scripts\ai-dev-auto-goal.ps1:679:            $arguments += "-CommitFiles"
-  scripts\ai-dev-auto-goal.ps1:680:            $arguments += ($normalizedFiles -join ",")
-  scripts\ai-dev-auto-goal.ps1:681:        }
-  scripts\ai-dev-auto-goal.ps1:682:    }
-  scripts\ai-dev-auto-goal.ps1:683:
-  scripts\ai-dev-auto-goal.ps1:684:    return $arguments
-  scripts\ai-dev-auto-goal.ps1:685:}
-  scripts\ai-dev-auto-goal.ps1:686:
-  scripts\ai-dev-auto-goal.ps1:687:Set-Location $repoRoot
-> scripts\ai-dev-auto-goal.ps1:733:    $script:autoGoalPlanPreview = New-DryRunAutoGoalPlan $GoalTi
-tle.Trim() $GoalDesc
-ription.Trim()
-  scripts\ai-dev-auto-goal.ps1:734:    $previewValidationErrors = @(Get-AutoGoalValidationErrors $s
-cript:autoGoalPlanPr
-eview)
-  scripts\ai-dev-auto-goal.ps1:735:
-  scripts\ai-dev-auto-goal.ps1:736:    if ($previewValidationErrors.Count -gt 0) {
-  scripts\ai-dev-auto-goal.ps1:737:        $script:steps += New-StepResult 2 "preview-plan" "local 
-dry-run plan preview
-" $false $false 1 "DryRun preview plan validation failed: $($previewValidationErrors -join '; ')"
-  scripts\ai-dev-auto-goal.ps1:738:        Stop-AutoGoal $script:steps "dry_run_preview_invalid" $f
-alse 1
-  scripts\ai-dev-auto-goal.ps1:739:    }
-  scripts\ai-dev-auto-goal.ps1:740:
-  scripts\ai-dev-auto-goal.ps1:741:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git
- status --short" $fa
-lse $true 0 "DryRun: dirty worktree gate was not executed."
-  scripts\ai-dev-auto-goal.ps1:742:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <a
-uto-goal planning pr
-ompt>" $false $true 0 "DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, t
-ask: $GoalTitle"
-  scripts\ai-dev-auto-goal.ps1:743:    $script:steps += New-StepResult 4 "validate-generated-json" 
-"goal/queue/state JS
-ON validation" $false $true 0 "DryRun: preview goal/queue/state plan passed local schema validation
-."
-  scripts\ai-dev-auto-goal.ps1:744:    $script:steps += New-StepResult 5 "write-state-files" "$goal
-RelativePath, $queue
-RelativePath, $stateRelativePath" $false $true 0 "DryRun: state files were not written."
-  scripts\ai-dev-auto-goal.ps1:745:    $script:steps += New-StepResult 6 "make-prompt" "powershell 
--ExecutionPolicy Byp
-ass -File scripts/ai-dev-make-prompt.ps1" $false $true 0 "DryRun: current-task-prompt.md was not ge
-nerated."
-  scripts\ai-dev-auto-goal.ps1:746:
-  scripts\ai-dev-auto-goal.ps1:747:    if ($shouldRunFullCycle) {
-  scripts\ai-dev-auto-goal.ps1:748:        $script:steps += New-StepResult 7 "auto-cycle-full" $ful
-lCycleCommandText $f
-alse $true 0 "DryRun: explicit run option is present, but full cycle was not executed. -AllowRun on
-ly invokes the full-
-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-  scripts\ai-dev-auto-goal.ps1:749:    } else {
-  scripts\ai-dev-auto-goal.ps1:750:        $script:steps += New-StepResult 7 "auto-cycle-full" $ful
-lCycleCommandText $f
-alse $true 0 "DryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only i
-nvokes the full-cycl
-e wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-  scripts\ai-dev-auto-goal.ps1:751:    }
-  scripts\ai-dev-auto-goal.ps1:752:
-  scripts\ai-dev-auto-goal.ps1:753:    Stop-AutoGoal $script:steps "dry_run" $false 0
-  scripts\ai-dev-auto-goal.ps1:754:}
-  scripts\ai-dev-auto-goal.ps1:755:
-  scripts\ai-dev-auto-goal.ps1:756:$statusLines = @(Get-GitStatusLines)
-  scripts\ai-dev-auto-goal.ps1:757:
-  scripts\ai-dev-auto-goal.ps1:758:if ($statusLines.Count -gt 0 -and -not $AllowDirty) {
-  scripts\ai-dev-auto-goal.ps1:759:    $dirtyText = ($statusLines -join "`n")
-  scripts\ai-dev-auto-goal.ps1:760:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git
- status --short" $fa
-lse $true 1 "Worktree is dirty. Use -AllowDirty only when this is intentional.`n$dirtyText"
-  scripts\ai-dev-auto-goal.ps1:761:    Stop-AutoGoal $script:steps "dirty_worktree" $false 1
-  scripts\ai-dev-auto-goal.ps1:762:}
-  scripts\ai-dev-auto-goal.ps1:763:
-  scripts\ai-dev-auto-goal.ps1:764:if ($statusLines.Count -gt 0) {
-  scripts\ai-dev-auto-goal.ps1:765:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git
- status --short" $fa
-lse $false 0 "AllowDirty is set. DirtyCount: $($statusLines.Count)"
-  scripts\ai-dev-auto-goal.ps1:766:} else {
-  scripts\ai-dev-auto-goal.ps1:767:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git
- status --short" $fa
-lse $false 0 "Worktree is clean."
-  scripts\ai-dev-auto-goal.ps1:768:}
-  scripts\ai-dev-auto-goal.ps1:769:
-  scripts\ai-dev-auto-goal.ps1:770:$codexCommand = Get-Command codex -ErrorAction SilentlyContinue
-  scripts\ai-dev-auto-goal.ps1:771:
-  scripts\ai-dev-auto-goal.ps1:772:if ($null -eq $codexCommand) {
-  scripts\ai-dev-auto-goal.ps1:773:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <a
-uto-goal planning pr
-ompt>" $false $false 1 "Codex CLI was not found."
-  scripts\ai-dev-auto-goal.ps1:774:    Stop-AutoGoal $script:steps "codex_not_found" $false 1
-  scripts\ai-dev-auto-goal.ps1:775:}
-  scripts\ai-dev-auto-goal.ps1:776:
-  scripts\ai-dev-auto-goal.ps1:777:$plannerPrompt = New-CodexAutoGoalPrompt $GoalTitle.Trim() $Goal
-Description.Trim()
-  scripts\ai-dev-auto-goal.ps1:778:$startedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-  scripts\ai-dev-auto-goal.ps1:779:$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-  scripts\ai-dev-auto-goal.ps1:780:$codexExitCode = $LASTEXITCODE
-  scripts\ai-dev-auto-goal.ps1:781:$endedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-  scripts\ai-dev-auto-goal.ps1:782:
-  scripts\ai-dev-auto-goal.ps1:783:$resultContent = @"
-  scripts\ai-dev-auto-goal.ps1:784:# Codex Auto Goal Result
-  scripts\ai-dev-auto-goal.ps1:785:
-  scripts\ai-dev-auto-goal.ps1:786:## Run
-  scripts\ai-dev-auto-goal.ps1:787:
-  scripts\ai-dev-auto-goal.ps1:788:- Started at: $startedAt
-  scripts\ai-dev-auto-goal.ps1:789:- Ended at: $endedAt
-  scripts\ai-dev-auto-goal.ps1:790:- Exit code: $codexExitCode
-  scripts\ai-dev-auto-goal.ps1:791:- Goal title: $($GoalTitle.Trim())
-  scripts\ai-dev-auto-goal.ps1:792:- Command: codex exec <auto-goal planning prompt>
-  scripts\ai-dev-auto-goal.ps1:793:
-  scripts\ai-dev-auto-goal.ps1:794:## Output
-  scripts\ai-dev-auto-goal.ps1:795:
-  scripts\ai-dev-auto-goal.ps1:796:${codeFence}text
-  scripts\ai-dev-auto-goal.ps1:797:$codexOutput
-  scripts\ai-dev-auto-goal.ps1:798:$codeFence
-  scripts\ai-dev-auto-goal.ps1:799:"@
-  scripts\ai-dev-auto-goal.ps1:800:
-  scripts\ai-dev-auto-goal.ps1:801:[System.IO.File]::WriteAllText($resolvedResultPath, $resultConte
-nt, $utf8WithBom)
-  scripts\ai-dev-auto-goal.ps1:802:
-  scripts\ai-dev-auto-goal.ps1:803:if ($codexExitCode -ne 0) {
-  scripts\ai-dev-auto-goal.ps1:804:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <a
-uto-goal planning pr
-ompt>" $true $false 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativePath $ResultPath
-)"
-  scripts\ai-dev-auto-goal.ps1:805:    Stop-AutoGoal $script:steps "codex_plan_failed" $false 1
-  scripts\ai-dev-auto-goal.ps1:806:}
-  scripts\ai-dev-auto-goal.ps1:807:
-  scripts\ai-dev-auto-goal.ps1:808:$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-
-goal planning prompt
->" $true $false 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativePath $ResultPath)
-"
-  scripts\ai-dev-auto-goal.ps1:809:
-  scripts\ai-dev-auto-goal.ps1:810:try {
-  scripts\ai-dev-auto-goal.ps1:811:    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-  scripts\ai-dev-auto-goal.ps1:812:} catch {
-  scripts\ai-dev-auto-goal.ps1:813:    $preview = Get-InputPreview $codexOutput
-
-
-
- succeeded in 2311ms:
-
-> .ai-dev\README.md:750:### 목표 입력 기반 자동 실행
-  .ai-dev\README.md:751:
-  .ai-dev\README.md:752:최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 
-하는 것이다.
-  .ai-dev\README.md:753:
-  .ai-dev\README.md:754:먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-  .ai-dev\README.md:755:
-  .ai-dev\README.md:756:```powershell
-  .ai-dev\README.md:757:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -Go
-alTitle "업무 검색 결과 하이
-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-  .ai-dev\README.md:758:```
-  .ai-dev\README.md:759:
-  .ai-dev\README.md:760:자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-  .ai-dev\README.md:761:DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 파
-일 작성 전에 생성될 goal/tas
-k 계획을 검토할 수 있다.
-  .ai-dev\README.md:762:
-  .ai-dev\README.md:763:```powershell
-  .ai-dev\README.md:764:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -Go
-alTitle "업무 검색 결과 하이
-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-  .ai-dev\README.md:765:```
-  .ai-dev\README.md:766:
-  .ai-dev\README.md:767:실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이 
-모드는 `ai-dev-auto-cyc
-le-full.ps1`을 호출하지 않는다.
-  .ai-dev\README.md:768:
-  .ai-dev\README.md:769:```powershell
-  .ai-dev\README.md:770:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -Go
-alTitle "업무 검색 결과 하이
-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-  .ai-dev\README.md:771:```
-  .ai-dev\README.md:772:
-  .ai-dev\README.md:773:생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowRev
-iewCodex`, `-AllowCo
-mmit` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다. 
-실제 구현까지 허용하려면 `-Allo
-wCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하게 `-AllowCodex -A
-llowReviewCodex -All
-owCommit`을 함께 지정한다.
-  .ai-dev\README.md:774:
-  .ai-dev\README.md:775:```powershell
-  .ai-dev\README.md:776:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -Go
-alTitle "업무 검색 결과 하이
-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -A
-llowCommit -MaxTasks
- 1
-  .ai-dev\README.md:777:```
-  .ai-dev\README.md:778:
-  .ai-dev\README.md:779:동작:
-  .ai-dev\README.md:780:
-  .ai-dev\README.md:781:1. `GoalTitle`과 `GoalDescription`을 검증한다.
-  .ai-dev\README.md:782:2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
-  .ai-dev\README.md:783:3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-  .ai-dev\README.md:784:4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다.
-  .ai-dev\README.md:785:5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
-  .ai-dev\README.md:786:6. `.ai-dev/current-task-prompt.md`를 생성한다.
-  .ai-dev\README.md:787:7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-  .ai-dev\README.md:788:8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-t
-ask-prompt.md`, 결과 파
-일 때문에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트는
- auto-goal 시작 전에 별도로
- 유지한다.
-  .ai-dev\README.md:789:
-  .ai-dev\README.md:790:안전 기준:
-  .ai-dev\README.md:791:
-  .ai-dev\README.md:792:- `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
-  .ai-dev\README.md:793:- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
-  .ai-dev\README.md:794:- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-  .ai-dev\README.md:795:- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 않
-는다.
-  .ai-dev\README.md:796:- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowCo
-mmit` 중 하나가 있을 때만 수행
-한다.
-  .ai-dev\README.md:797:- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCode
-x`, `-AllowReviewCod
-ex`, `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
-  .ai-dev\README.md:798:- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커밋
-하지 않는다.
-  .ai-dev\README.md:799:- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
-  .ai-dev\README.md:800:
-  .ai-dev\README.md:801:## 운영 원칙
-  .ai-dev\README.md:802:
-  .ai-dev\README.md:803:- 저장소의 `AGENTS.md`, 사용자 지시, 보안 정책이 자동 개발 루프보다 우선한다.
-  .ai-dev\README.md:804:- 한 번에 하나의 task만 수행한다.
-  .ai-dev\README.md:805:- task 단위로 구현, 검증, 리뷰, 커밋한다.
-  .ai-dev\README.md:806:- 현재 task 범위 밖 파일을 임의로 수정하지 않는다.
-  .ai-dev\README.md:807:- 검증하지 않은 결과를 통과로 기록하지 않는다.
-  .ai-dev\README.md:808:- 실제 DB 삭제, 초기화, 복원, 대규모 리팩터링은 초기 실험 범위에서 제외한다.
-  .ai-dev\README.md:809:
-  .ai-dev\README.md:810:## 다음 단계
-  .ai-dev\README.md:811:
-  .ai-dev\README.md:812:실제 자동 개발 루프 실행기와 상태 갱신 로직은 별도 정책 검토 후 추가한다.
-  .ai-dev\README.md:813:
-  .ai-dev\README.md:814:세부 기준은 `docs/ai-dev-loop-policy.md`를 따른다.
-
-
+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "placeholder|검색|Se
+arch" src' in D:\ai-apps\planpilot-local
+ succeeded in 1768ms:
+src\App.tsx:28:  const [taskSearchQuery, setTaskSearchQuery] = useState("");
+src\App.tsx:115:    taskSearchQuery,
+src\App.tsx:159:            taskSearchQuery={taskSearchQuery}
+src\App.tsx:173:            onTaskSearchQueryChange={setTaskSearchQuery}
+src\components\ProjectForm.tsx:38:          placeholder="예: 프로젝트 이름"
+src\components\ProjectForm.tsx:48:          placeholder="예: 프로젝트 설명"
+src\components\TaskForm.tsx:53:          placeholder="예: 사업계획서 초안 작성"
+src\components\TaskForm.tsx:63:          placeholder="예: 참고할 내용"
+src\views\TasksView.tsx:14:  taskSearchQuery: string;
+src\views\TasksView.tsx:28:  onTaskSearchQueryChange: (value: string) => void;
+src\views\TasksView.tsx:58:  taskSearchQuery,
+src\views\TasksView.tsx:72:  onTaskSearchQueryChange,
+src\views\TasksView.tsx:94:  const hasSearchQuery = taskSearchQuery.trim().length > 0;
+src\views\TasksView.tsx:98:    hasSearchQuery
+src\views\TasksView.tsx:99:      ? "검색 결과가 없습니다."
+src\views\TasksView.tsx:110:        업무 검색
+src\views\TasksView.tsx:113:          value={taskSearchQuery}
+src\views\TasksView.tsx:114:          onChange={(event) => onTaskSearchQueryChange(event.target.val
+ue)}
+src\views\TasksView.tsx:115:          placeholder="업무 제목이나 메모로 검색"
+src\views\TasksView.tsx:186:          {hasSearchQuery && <p>다른 검색어를 입력해보세요.</p>}
+src\utils\taskFilters.ts:8:  taskSearchQuery: string;
+src\utils\taskFilters.ts:17:    taskSearchQuery,
+src\utils\taskFilters.ts:29:    const searchText = taskSearchQuery.trim().toLowerCase();
+src\utils\taskFilters.ts:35:    const matchesSearch =
+src\utils\taskFilters.ts:40:    return matchesProject && matchesSearch && matchesStatus;
 
 codex
-수정 대상은 자동화 스크립트 하나입니다. README에는 이미 목표 입력 기반 실행 문서가 있어, 스크립트 동작 보강 후 문서와 실제 동작이 어긋나는 부분이 있는지만 다시 확인하
-겠습니다.
+검색창은 `src/views/TasksView.tsx`에 분리되어 있습니다. 동작 코드는 건드리지 않고 placeholder 문자열만 바꾸겠습니다.
 exec
 "C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' | Select-Object -Skip 330 -First 230
-" in D:\ai-apps\planpilot-local
- succeeded in 1784ms:
-
-        if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
-            $errors += "invalid task.status: $($task.status)"
-        }
-
-        if (@("P0", "P1", "P2") -notcontains $task.priority) {
-            $errors += "invalid task.priority: $($task.priority)"
-        }
-
-        if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is [string
-]) {
-            $errors += "task.dependsOn must be an array: $($task.id)"
-        }
-
-        if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.filesLikely
-ToChange -is [string]) {
-            $errors += "task.filesLikelyToChange must be an array: $($task.id)"
-        }
-
-        if ($task.verification -isnot [System.Collections.IEnumerable] -or $task.verification -is [
-string]) {
-            $errors += "task.verification must be an array: $($task.id)"
-        }
-    }
-
-    if ((@($tasks | Where-Object { $_.status -eq "in_progress" })).Count -ne 1) {
-        $errors += "queue.tasks must contain exactly one in_progress task."
-    }
-
-    if (-not (Test-HasValue $Queue.currentTaskId)) {
-        $errors += "queue.currentTaskId must not be empty."
-    } elseif ($taskIds -notcontains [string]$Queue.currentTaskId) {
-        $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-    }
-
-    return $errors
-}
-
-function Get-StateValidationErrors {
-    param(
-        [object]$State,
-        [object]$Queue
-    )
-
-    $errors = @()
-    $requiredFields = @("goalStatus", "currentTaskId", "currentLoop", "maxLoopsPerTask", "repeatedF
-ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDecision", "lastRe
-viewSeverity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
-
-    foreach ($field in @(Get-MissingFields $State $requiredFields)) {
-        $errors += "state missing field: $field"
-    }
-
-    if ($errors.Count -gt 0) {
-        return $errors
-    }
-
-    if (@("idle", "in_progress", "completed", "blocked") -notcontains $State.goalStatus) {
-        $errors += "invalid state.goalStatus: $($State.goalStatus)"
-    }
-
-    if ($State.currentTaskId -ne $Queue.currentTaskId) {
-        $errors += "state.currentTaskId must match queue.currentTaskId."
-    }
-
-    if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-        $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-    }
-
-    if (@("not_started", "pass", "revise", "blocked") -notcontains $State.lastReviewDecision) {
-        $errors += "invalid state.lastReviewDecision: $($State.lastReviewDecision)"
-    }
-
-    return $errors
-}
-
-function Get-AutoGoalValidationErrors {
-    param([object]$Plan)
-
-    $errors = @()
-
-    foreach ($field in @(Get-MissingFields $Plan @("goalMarkdown", "queue", "state"))) {
-        $errors += "plan missing field: $field"
-    }
-
-    if ($errors.Count -gt 0) {
-        return $errors
-    }
-
-    if (-not (Test-HasValue $Plan.goalMarkdown)) {
-        $errors += "goalMarkdown must not be empty."
-    }
-
-    $errors += @(Get-QueueValidationErrors $Plan.queue)
-    $errors += @(Get-StateValidationErrors $Plan.state $Plan.queue)
-
-    return $errors
-}
-
-function New-DryRunAutoGoalPlan {
-    param(
-        [string]$Title,
-        [string]$Description
-    )
-
-    $now = (Get-Date).ToUniversalTime().ToString("o")
-    $safeCommitTitle = $Title.Trim()
-
-    $plan = [PSCustomObject][ordered]@{
-        goalMarkdown = @"
-# 紐⑺몴
-
-$safeCommitTitle
-
-## 諛곌꼍
-
-$($Description.Trim())
-
-## ?깃났 湲곗?
-
-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-
-## ?쒖빟?ы빆
-
-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-
-## ?쒖쇅 踰붿쐞
-
-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-
-## ?섎룞 ?뺤씤
-
-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-"@
-        queue = [PSCustomObject][ordered]@{
-            goalTitle = $safeCommitTitle
-            goalSource = $goalRelativePath
-            createdAt = $now
-            updatedAt = $now
-            currentTaskId = "T001"
-            tasks = @(
-                [PSCustomObject][ordered]@{
-                    id = "T001"
-                    title = $safeCommitTitle
-                    description = $Description.Trim()
-                    type = "implementation"
-                    status = "in_progress"
-                    priority = "P0"
-                    dependsOn = @()
-                    filesLikelyToChange = @()
-                    verification = @(
-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-                    )
-                    commitMessage = $null
-                }
-            )
-        }
-        state = [PSCustomObject][ordered]@{
-            goalStatus = "in_progress"
-            currentTaskId = "T001"
-            currentLoop = 0
-            maxLoopsPerTask = 2
-            repeatedFailureCount = 0
-            lastCommand = $null
-            lastCommandStatus = "not_started"
-            lastErrorSummary = $null
-            lastReviewDecision = "not_started"
-            lastReviewSeverity = $null
-            lastCommitHash = $null
-            startedAt = $now
-            updatedAt = $now
-            stopReason = $null
-        }
-    }
-
-    $plan.goalMarkdown = @"
-# 紐⑺몴
-
-$safeCommitTitle
-
-## 諛곌꼍
-
-$($Description.Trim())
-
-## ?깃났 湲곗?
-
-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-
-## ?쒖빟?ы빆
-
-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-
-## ?쒖쇅 踰붿쐞
-
-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-
-## ?섎룞 ?뺤씤
-
-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-"@
-
-    $dryRunTask = @($plan.queue.tasks)[0]
-    $dryRunTask.verification = @(
-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-    )
-
-    return $plan
-}
-
-function ConvertFrom-CodexAutoGoalOutput {
-    param([string]$RawInput)
-
-    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-        try {
-            $parsed = $candidate | ConvertFrom-Json
-
-            if ($null -eq $parsed -or $parsed -is [System.Array]) {
-                continue
-            }
-
-            if (@(Get-AutoGoalValidationErrors $parsed).Count -gt 0) {
-                continue
+ 'D:\\ai-apps\\planpilot-local\\src\\views\\TasksView.tsx'" in D:\ai-apps\planpilot-local
+ succeeded in 1191ms:
+import type { FormEvent } from "react";
+import { TaskCard } from "../components/TaskCard";
+import { TaskForm } from "../components/TaskForm";
+import type { Project, Task } from "../types";
+import type { TaskSortOrder } from "../utils/taskFilters";
+
+type TasksViewProps = {
+  projects: Project[];
+  filteredTasks: Task[];
+  sortedTasks: Task[];
+  selectedProjectFilter: string;
+  showCompletedTasks: boolean;
+  taskSortOrder: TaskSortOrder;
+  taskSearchQuery: string;
+  isTaskFormOpen: boolean;
+  newTaskTitle: string;
+  newTaskMemo: string;
+  newTaskDueDate: string;
+  newTaskPriority: Task["priority"];
+  newTaskProjectId: string;
+  editingTaskId: string | null;
+  editTaskTitle: string;
+  editTaskMemo: string;
+  editTaskDueDate: string;
+  editTaskPriority: Task["priority"];
+  editTaskProjectId: string;
+  getProjectName: (projectId: string) => string;
+  onTaskSearchQueryChange: (value: string) => void;
+  onShowCompletedTasksChange: (value: boolean) => void;
+  onTaskSortOrderChange: (value: TaskSortOrder) => void;
+  onSelectedProjectFilterChange: (value: string) => void;
+  onTaskFormOpenChange: (updater: (current: boolean) => boolean) => void;
+  onNewTaskTitleChange: (value: string) => void;
+  onNewTaskMemoChange: (value: string) => void;
+  onNewTaskDueDateChange: (value: string) => void;
+  onNewTaskPriorityChange: (value: Task["priority"]) => void;
+  onNewTaskProjectIdChange: (value: string) => void;
+  onEditTaskTitleChange: (value: string) => void;
+  onEditTaskMemoChange: (value: string) => void;
+  onEditTaskDueDateChange: (value: string) => void;
+  onEditTaskPriorityChange: (value: Task["priority"]) => void;
+  onEditTaskProjectIdChange: (value: string) => void;
+  onAddTask: (event: FormEvent<HTMLFormElement>) => void;
+  onSaveEditTask: (task: Task) => void;
+  onCancelEditTask: () => void;
+  onToggleTaskDone: (task: Task) => void;
+  onDeleteTask: (task: Task) => void;
+  onStartEditTask: (task: Task) => void;
+};
+
+export function TasksView({
+  projects,
+  filteredTasks,
+  sortedTasks,
+  selectedProjectFilter,
+  showCompletedTasks,
+  taskSortOrder,
+  taskSearchQuery,
+  isTaskFormOpen,
+  newTaskTitle,
+  newTaskMemo,
+  newTaskDueDate,
+  newTaskPriority,
+  newTaskProjectId,
+  editingTaskId,
+  editTaskTitle,
+  editTaskMemo,
+  editTaskDueDate,
+  editTaskPriority,
+  editTaskProjectId,
+  getProjectName,
+  onTaskSearchQueryChange,
+  onShowCompletedTasksChange,
+  onTaskSortOrderChange,
+  onSelectedProjectFilterChange,
+  onTaskFormOpenChange,
+  onNewTaskTitleChange,
+  onNewTaskMemoChange,
+  onNewTaskDueDateChange,
+  onNewTaskPriorityChange,
+  onNewTaskProjectIdChange,
+  onEditTaskTitleChange,
+  onEditTaskMemoChange,
+  onEditTaskDueDateChange,
+  onEditTaskPriorityChange,
+  onEditTaskProjectIdChange,
+  onAddTask,
+  onSaveEditTask,
+  onCancelEditTask,
+  onToggleTaskDone,
+  onDeleteTask,
+  onStartEditTask,
+}: TasksViewProps) {
+  const hasSearchQuery = taskSearchQuery.trim().length > 0;
+  const hasProjectFilter = selectedProjectFilter !== "all";
+  const hasVisibilityFilter = !showCompletedTasks;
+  const emptyMessage =
+    hasSearchQuery
+      ? "寃??寃곌낵媛 ?놁뒿?덈떎."
+      : hasProjectFilter || hasVisibilityFilter
+        ? "?꾩옱 ?꾪꽣 議곌굔??留욌뒗 ?낅Т媛 ?놁뒿?덈떎."
+        : "?깅줉???낅Т媛 ?놁뒿?덈떎.";
+
+  return (
+    <section className="screen-card">
+      <h2>?꾩껜 ?낅Т</h2>
+      <p className="summary">珥?{filteredTasks.length}媛?/p>
+
+      <label>
+        ?낅Т 寃??        <input
+          type="text"
+          value={taskSearchQuery}
+          onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+          placeholder="?낅Т ?쒕ぉ?대굹 硫붾え濡?寃??
+        />
+      </label>
+
+      <label>
+        <input
+          type="checkbox"
+          checked={showCompletedTasks}
+          onChange={(event) => onShowCompletedTasksChange(event.target.checked)}
+        />
+        ?꾨즺 ?낅Т ?쒖떆
+      </label>
+
+      <label>
+        ?뺣젹
+        <select
+          value={taskSortOrder}
+          onChange={(event) =>
+            onTaskSortOrderChange(event.target.value as TaskSortOrder)
+          }
+        >
+          <option value="none">湲곕낯 ?쒖꽌</option>
+          <option value="dueDateAsc">留덇컧??鍮좊Ⅸ ??/option>
+        </select>
+      </label>
+
+      <label>
+        ?꾨줈?앺듃 ?꾪꽣
+        <select
+          value={selectedProjectFilter}
+          onChange={(event) => onSelectedProjectFilterChange(event.target.value)}
+        >
+          <option value="all">?꾩껜 ?꾨줈?앺듃</option>
+          {projects.map((project) => (
+            <option key={project.id} value={project.id}>
+              {project.name}
+            </option>
+          ))}
+        </select>
+      </label>
+
+      <button
+        type="button"
+        onClick={() => onTaskFormOpenChange((current) => !current)}
+      >
+        {isTaskFormOpen ? "???낅Т 異붽? ?リ린" : "???낅Т 異붽?"}
+      </button>
+
+      {isTaskFormOpen && (
+        <TaskForm
+          title="???낅Т 異붽?"
+          ariaLabel="?낅Т 異붽?"
+          taskTitle={newTaskTitle}
+          memo={newTaskMemo}
+          dueDate={newTaskDueDate}
+          priority={newTaskPriority}
+          projectId={newTaskProjectId}
+          projects={projects}
+          submitLabel="?낅Т 異붽?"
+          onTitleChange={onNewTaskTitleChange}
+          onMemoChange={onNewTaskMemoChange}
+          onDueDateChange={onNewTaskDueDateChange}
+          onPriorityChange={onNewTaskPriorityChange}
+          onProjectIdChange={onNewTaskProjectIdChange}
+          onSubmit={onAddTask}
+        />
+      )}
+
+      {filteredTasks.length === 0 ? (
+        <div className="empty">
+          <p>{emptyMessage}</p>
+          {hasSearchQuery && <p>?ㅻⅨ 寃?됱뼱瑜??낅젰?대낫?몄슂.</p>}
+        </div>
+      ) : (
+        <ul className="task-list">
+          {sortedTasks.map((task) => {
+            if (editingTaskId === task.id) {
+              return (
+                <li key={task.id} className="task-card">
+                  <TaskForm
+                    title="?낅Т ?섏젙"
+                    ariaLabel="?낅Т ?섏젙"
+                    taskTitle={editTaskTitle}
+                    memo={editTaskMemo}
+                    dueDate={editTaskDueDate}
+                    priority={editTaskPriority}
+                    projectId={editTaskProjectId}
+                    projects={projects}
+                    submitLabel="???
+                    onTitleChange={onEditTaskTitleChange}
+                    onMemoChange={onEditTaskMemoChange}
+                    onDueDateChange={onEditTaskDueDateChange}
+                    onPriorityChange={onEditTaskPriorityChange}
+                    onProjectIdChange={onEditTaskProjectIdChange}
+                    onSubmit={(event) => {
+                      event.preventDefault();
+                      onSaveEditTask(task);
+                    }}
+                    onCancel={onCancelEditTask}
+                    className=""
+                  />
+                </li>
+              );
             }
 
-codex
-이제 `ai-dev-auto-goal.ps1`만 수정합니다. DryRun 미리보기 텍스트를 정상 한국어로 복구하고, Codex가 만든 queue/state가 `T001` 시작 규
-칙을 벗어나면 파일을 쓰지 않도록 검증을 강화합니다.
-apply patch
-patch: completed
-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1
-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..40e87615fb5a517df15ebc3cce7963afb27cf9cd
---- a/scripts/ai-dev-auto-goal.ps1
-+++ b/scripts/ai-dev-auto-goal.ps1
-@@ -297,6 +297,10 @@
-         $errors += "queue.tasks must contain no more than three tasks."
-     }
- 
-+    if ($Queue.currentTaskId -ne "T001") {
-+        $errors += "queue.currentTaskId must be T001."
-+    }
-+
-     $taskIds = @()
- 
-     foreach ($task in $tasks) {
-@@ -360,6 +364,19 @@
-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-     }
- 
-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-+    if ($null -eq $firstTask) {
-+        $errors += "queue.tasks must include T001."
-+    } elseif ($firstTask.status -ne "in_progress") {
-+        $errors += "T001 must be in_progress."
-+    }
-+
-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-+        if ($laterTask.status -ne "pending") {
-+            $errors += "later tasks must be pending: $($laterTask.id)"
-+        }
-+    }
-+
-     return $errors
- }
- 
-@@ -388,6 +405,10 @@
-         $errors += "state.currentTaskId must match queue.currentTaskId."
-     }
- 
-+    if ($State.currentTaskId -ne "T001") {
-+        $errors += "state.currentTaskId must be T001."
-+    }
-+
-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-     }
-
-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..40e87615fb5a517df15ebc3cce7963afb27cf9cd
---- a/scripts/ai-dev-auto-goal.ps1
-+++ b/scripts/ai-dev-auto-goal.ps1
-@@ -297,6 +297,10 @@
-         $errors += "queue.tasks must contain no more than three tasks."
-     }
- 
-+    if ($Queue.currentTaskId -ne "T001") {
-+        $errors += "queue.currentTaskId must be T001."
-+    }
-+
-     $taskIds = @()
- 
-     foreach ($task in $tasks) {
-@@ -360,6 +364,19 @@
-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-     }
- 
-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-+    if ($null -eq $firstTask) {
-+        $errors += "queue.tasks must include T001."
-+    } elseif ($firstTask.status -ne "in_progress") {
-+        $errors += "T001 must be in_progress."
-+    }
-+
-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-+        if ($laterTask.status -ne "pending") {
-+            $errors += "later tasks must be pending: $($laterTask.id)"
-+        }
-+    }
-+
-     return $errors
- }
- 
-@@ -388,6 +405,10 @@
-         $errors += "state.currentTaskId must match queue.currentTaskId."
-     }
- 
-+    if ($State.currentTaskId -ne "T001") {
-+        $errors += "state.currentTaskId must be T001."
-+    }
-+
-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-     }
-
-codex
-검증 규칙 보강은 적용했습니다. 이어서 DryRun 계획 생성 함수의 깨진 문자열을 정상 한국어 미리보기로 교체합니다.
-apply patch
-patch: completed
-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1
-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
---- a/scripts/ai-dev-auto-goal.ps1
-+++ b/scripts/ai-dev-auto-goal.ps1
-@@ -297,6 +297,10 @@
-         $errors += "queue.tasks must contain no more than three tasks."
-     }
- 
-+    if ($Queue.currentTaskId -ne "T001") {
-+        $errors += "queue.currentTaskId must be T001."
-+    }
-+
-     $taskIds = @()
- 
-     foreach ($task in $tasks) {
-@@ -360,6 +364,19 @@
-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-     }
- 
-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-+    if ($null -eq $firstTask) {
-+        $errors += "queue.tasks must include T001."
-+    } elseif ($firstTask.status -ne "in_progress") {
-+        $errors += "T001 must be in_progress."
-+    }
-+
-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-+        if ($laterTask.status -ne "pending") {
-+            $errors += "later tasks must be pending: $($laterTask.id)"
-+        }
-+    }
-+
-     return $errors
- }
- 
-@@ -388,6 +405,10 @@
-         $errors += "state.currentTaskId must match queue.currentTaskId."
-     }
- 
-+    if ($State.currentTaskId -ne "T001") {
-+        $errors += "state.currentTaskId must be T001."
-+    }
-+
-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-     }
-@@ -429,41 +450,44 @@
-     )
- 
-     $now = (Get-Date).ToUniversalTime().ToString("o")
--    $safeCommitTitle = $Title.Trim()
-+    $safeTitle = $Title.Trim()
-+    $safeDescription = $Description.Trim()
- 
-     $plan = [PSCustomObject][ordered]@{
-         goalMarkdown = @"
--# 紐⑺몴
-+# 목표
- 
--$safeCommitTitle
-+$safeTitle
- 
--## 諛곌꼍
-+## 배경
- 
--$($Description.Trim())
-+$safeDescription
- 
--## ?깃났 湲곗?
-+## 성공 기준
- 
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+- 입력한 목표에서 현재 작업을 준비한다.
-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
- 
--## ?쒖빟?ы빆
-+## 제약사항
- 
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-+- 저장소 정책과 현재 작업 범위를 따른다.
- 
--## ?쒖쇅 踰붿쐞
-+## 범위 제외
- 
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+- 패키지 변경
-+- DB 삭제 또는 초기화
-+- 광범위한 리팩터링
- 
--## ?섎룞 ?뺤씤
-+## 수동 검증
- 
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+- DryRun 계획과 JSON 출력을 검토한다.
-+- 현재 작업 프롬프트 생성 여부를 확인한다.
-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
- "@
-         queue = [PSCustomObject][ordered]@{
--            goalTitle = $safeCommitTitle
-+            goalTitle = $safeTitle
-             goalSource = $goalRelativePath
-             createdAt = $now
-             updatedAt = $now
-@@ -471,16 +495,16 @@
-             tasks = @(
-                 [PSCustomObject][ordered]@{
-                     id = "T001"
--                    title = $safeCommitTitle
--                    description = $Description.Trim()
-+                    title = $safeTitle
-+                    description = $safeDescription
-                     type = "implementation"
-                     status = "in_progress"
-                     priority = "P0"
-                     dependsOn = @()
-                     filesLikelyToChange = @()
-                     verification = @(
--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-+                        "필요한 경우 npm run build를 별도로 실행한다."
-                     )
-                     commitMessage = $null
-                 }
-@@ -503,43 +527,6 @@
-             stopReason = $null
-         }
-     }
--
--    $plan.goalMarkdown = @"
--# 紐⑺몴
--
--$safeCommitTitle
--
--## 諛곌꼍
--
--$($Description.Trim())
--
--## ?깃났 湲곗?
--
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
--
--## ?쒖빟?ы빆
--
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
--
--## ?쒖쇅 踰붿쐞
--
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
--
--## ?섎룞 ?뺤씤
--
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
--"@
--
--    $dryRunTask = @($plan.queue.tasks)[0]
--    $dryRunTask.verification = @(
--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
--    )
- 
-     return $plan
- }
-
-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
---- a/scripts/ai-dev-auto-goal.ps1
-+++ b/scripts/ai-dev-auto-goal.ps1
-@@ -297,6 +297,10 @@
-         $errors += "queue.tasks must contain no more than three tasks."
-     }
- 
-+    if ($Queue.currentTaskId -ne "T001") {
-+        $errors += "queue.currentTaskId must be T001."
-+    }
-+
-     $taskIds = @()
- 
-     foreach ($task in $tasks) {
-@@ -360,6 +364,19 @@
-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-     }
- 
-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-+    if ($null -eq $firstTask) {
-+        $errors += "queue.tasks must include T001."
-+    } elseif ($firstTask.status -ne "in_progress") {
-+        $errors += "T001 must be in_progress."
-+    }
-+
-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-+        if ($laterTask.status -ne "pending") {
-+            $errors += "later tasks must be pending: $($laterTask.id)"
-+        }
-+    }
-+
-     return $errors
- }
- 
-@@ -388,6 +405,10 @@
-         $errors += "state.currentTaskId must match queue.currentTaskId."
-     }
- 
-+    if ($State.currentTaskId -ne "T001") {
-+        $errors += "state.currentTaskId must be T001."
-+    }
-+
-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-     }
-@@ -429,41 +450,44 @@
-     )
- 
-     $now = (Get-Date).ToUniversalTime().ToString("o")
--    $safeCommitTitle = $Title.Trim()
-+    $safeTitle = $Title.Trim()
-+    $safeDescription = $Description.Trim()
- 
-     $plan = [PSCustomObject][ordered]@{
-         goalMarkdown = @"
--# 紐⑺몴
-+# 목표
- 
--$safeCommitTitle
-+$safeTitle
- 
--## 諛곌꼍
-+## 배경
- 
--$($Description.Trim())
-+$safeDescription
- 
--## ?깃났 湲곗?
-+## 성공 기준
- 
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+- 입력한 목표에서 현재 작업을 준비한다.
-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
- 
--## ?쒖빟?ы빆
-+## 제약사항
- 
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-+- 저장소 정책과 현재 작업 범위를 따른다.
- 
--## ?쒖쇅 踰붿쐞
-+## 범위 제외
- 
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+- 패키지 변경
-+- DB 삭제 또는 초기화
-+- 광범위한 리팩터링
- 
--## ?섎룞 ?뺤씤
-+## 수동 검증
- 
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+- DryRun 계획과 JSON 출력을 검토한다.
-+- 현재 작업 프롬프트 생성 여부를 확인한다.
-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
- "@
-         queue = [PSCustomObject][ordered]@{
--            goalTitle = $safeCommitTitle
-+            goalTitle = $safeTitle
-             goalSource = $goalRelativePath
-             createdAt = $now
-             updatedAt = $now
-@@ -471,16 +495,16 @@
-             tasks = @(
-                 [PSCustomObject][ordered]@{
-                     id = "T001"
--                    title = $safeCommitTitle
--                    description = $Description.Trim()
-+                    title = $safeTitle
-+                    description = $safeDescription
-                     type = "implementation"
-                     status = "in_progress"
-                     priority = "P0"
-                     dependsOn = @()
-                     filesLikelyToChange = @()
-                     verification = @(
--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-+                        "필요한 경우 npm run build를 별도로 실행한다."
-                     )
-                     commitMessage = $null
-                 }
-@@ -503,43 +527,6 @@
-             stopReason = $null
-         }
-     }
--
--    $plan.goalMarkdown = @"
--# 紐⑺몴
--
--$safeCommitTitle
--
--## 諛곌꼍
--
--$($Description.Trim())
--
--## ?깃났 湲곗?
--
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
--
--## ?쒖빟?ы빆
--
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
--
--## ?쒖쇅 踰붿쐞
--
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
--
--## ?섎룞 ?뺤씤
--
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
--"@
--
--    $dryRunTask = @($plan.queue.tasks)[0]
--    $dryRunTask.verification = @(
--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
--    )
- 
-     return $plan
- }
-
-codex
-수정은 완료되었습니다. 이제 PowerShell 파서로 문법을 확인하고, DryRun/JSON/빈 입력 오류 경로가 요구사항대로 동작하는지 실행 수준에서 검증하겠습니다.
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription '
-검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription '
-검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun -Json" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '' -GoalDescription '설명' -DryRun -Json
-" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$tokens=$null; $errors=$
-null; [System.Management.Automation.Language.Parser]::ParseFile('"'D:\\ai-apps\\planpilot-local\\sc
-ripts\\ai-dev-auto-goal.ps1',[ref]"'$tokens,[ref]$errors) > $null; if ($errors.Count -gt 0) { $erro
-rs | ForEach-Object { $_.Message }; exit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-local
- succeeded in 3046ms:
-syntax ok
-
- exited 1 in 4136ms:
-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1 : Missing an argument for parameter 'GoalTi
-tle'. Specify a para
-meter of type 'System.String' and try again.
-    + CategoryInfo          : InvalidArgument: (:) [ai-dev-auto-goal.ps1], ParentContainsErrorRecor
-dException
-    + FullyQualifiedErrorId : MissingArgument,ai-dev-auto-goal.ps1
- 
-
-2026-06-09T13:59:38.985059Z ERROR codex_core::tools::router: error=Exit code: 1
-Wall time: 4.1 seconds
-Output:
-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1 : Missing an argument for parameter 'GoalTi
-tle'. Specify a para
-meter of type 'System.String' and try again.
-    + CategoryInfo          : InvalidArgument: (:) [ai-dev-auto-goal.ps1], ParentContainsErrorRecor
-dException
-    + FullyQualifiedErrorId : MissingArgument,ai-dev-auto-goal.ps1
- 
-
- succeeded in 4962ms:
-{
-    "steps":  [
-                  {
-                      "step":  1,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle/GoalDescription",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  0,
-                      "message":  "Input validation completed: 업무 검색 결과 하이라이트 추가"
-                  },
-                  {
-                      "step":  2,
-                      "name":  "dirty-worktree-gate",
-                      "command":  "git status --short",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: dirty worktree gate was not executed."
-                  },
-                  {
-                      "step":  3,
-                      "name":  "plan-goal",
-                      "command":  "codex exec \u003cauto-goal planning prompt\u003e",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: Codex goal planning was not executed. Preview currentTas
-kId: T001, task: 업무 검색 결과 하이라이트 추가"
-                  },
-                  {
-                      "step":  4,
-                      "name":  "validate-generated-json",
-                      "command":  "goal/queue/state JSON validation",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: preview goal/queue/state plan passed local schema valida
-tion."
-                  },
-                  {
-                      "step":  5,
-                      "name":  "write-state-files",
-                      "command":  ".ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: state files were not written."
-                  },
-                  {
-                      "step":  6,
-                      "name":  "make-prompt",
-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pro
-mpt.ps1",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: current-task-prompt.md was not generated."
-                  },
-                  {
-                      "step":  7,
-                      "name":  "auto-cycle-full",
-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cyc
-le-full.ps1 -MaxTasks 1 -MaxSteps 20 -AllowDirty",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: full cycle requires -AllowRun or explicit execution opti
-ons. -AllowRun only invokes the full-cycle wrapper; implementation and review still require -AllowC
-odex and -AllowReviewCodex."
-                  }
-              ],
-    "stoppedReason":  "dry_run",
-    "completed":  false,
-    "exitCode":  0,
-    "plan":  {
-                 "goalMarkdown":  "# 목표\n\n업무 검색 결과 하이라이트 추가\n\n## 배경\n\n검색어와 일치하는 업무 제목/메모/프로젝트명을 
-화면에서 강조 표시한다.\n\n## 성공 기준\n\n- 입력한 목표에서 현재 작업을 준비한다.\n- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.\n- 
-full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.\n\n## 제약사항\n\n- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 gi
-t 명령은 사용하지 않는다.\n- 저장소 정책과 현재 작업 범위를 따른다.\n\n## 범위 제외\n\n- 패키지 변경\n- DB 삭제 또는 초기화\n- 광범위한 리팩터링\n\n#
-# 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재 작업 프롬프트 생성 여부를 확인한다.\n- 필요한 경우 빌드와 검증은 별도로 실행한다.",
-                 "queue":  {
-                               "goalTitle":  "업무 검색 결과 하이라이트 추가",
-                               "goalSource":  ".ai-dev/goal.md",
-                               "createdAt":  "2026-06-09T13:59:38.3307470Z",
-                               "updatedAt":  "2026-06-09T13:59:38.3307470Z",
-                               "currentTaskId":  "T001",
-                               "tasks":  [
-                                             {
-                                                 "id":  "T001",
-                                                 "title":  "업무 검색 결과 하이라이트 추가",
-                                                 "description":  "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조
- 표시한다.",
-                                                 "type":  "implementation",
-                                                 "status":  "in_progress",
-                                                 "priority":  "P0",
-                                                 "dependsOn":  [
-
-                                                               ],
-                                                 "filesLikelyToChange":  [
-
-                                                                         ],
-                                                 "verification":  [
-                                                                      "변경 범위가 현재 목표와 일치하는지 확인한다.",
-                                                                      "필요한 경우 npm run build를 별도로 실행
-한다."
-                                                                  ],
-                                                 "commitMessage":  null
-                                             }
-                                         ]
-                           },
-                 "state":  {
-                               "goalStatus":  "in_progress",
-                               "currentTaskId":  "T001",
-                               "currentLoop":  0,
-                               "maxLoopsPerTask":  2,
-                               "repeatedFailureCount":  0,
-                               "lastCommand":  null,
-                               "lastCommandStatus":  "not_started",
-                               "lastErrorSummary":  null,
-                               "lastReviewDecision":  "not_started",
-                               "lastReviewSeverity":  null,
-                               "lastCommitHash":  null,
-                               "startedAt":  "2026-06-09T13:59:38.3307470Z",
-                               "updatedAt":  "2026-06-09T13:59:38.3307470Z",
-                               "stopReason":  null
-                           }
-             },
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+            return (
+              <TaskCard
+                key={task.id}
+                task={task}
+                projectName={getProjectName(task.projectId)}
+                onToggleDone={onToggleTaskDone}
+                onDelete={onDeleteTask}
+                onStartEdit={onStartEditTask}
+              />
+            );
+          })}
+        </ul>
+      )}
+    </section>
+  );
 }
 
- succeeded in 5085ms:
-Step 1: validate-input
-  Command: check GoalTitle/GoalDescription
-  Executed: False
-  Skipped: False
-  Exit code: 0
-  Message: Input validation completed: 업무 검색 결과 하이라이트 추가
-Step 2: dirty-worktree-gate
-  Command: git status --short
-  Executed: False
-  Skipped: True
-  Exit code: 0
-  Message: DryRun: dirty worktree gate was not executed.
-Step 3: plan-goal
-  Command: codex exec <auto-goal planning prompt>
-  Executed: False
-  Skipped: True
-  Exit code: 0
-  Message: DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, task: 업무 검색 결
-과 하이라이트 추가
-Step 4: validate-generated-json
-  Command: goal/queue/state JSON validation
-  Executed: False
-  Skipped: True
-  Exit code: 0
-  Message: DryRun: preview goal/queue/state plan passed local schema validation.
-Step 5: write-state-files
-  Command: .ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json
-  Executed: False
-  Skipped: True
-  Exit code: 0
-  Message: DryRun: state files were not written.
-Step 6: make-prompt
-  Command: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
-  Executed: False
-  Skipped: True
-  Exit code: 0
-  Message: DryRun: current-task-prompt.md was not generated.
-Step 7: auto-cycle-full
-  Command: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -MaxTasks 1 
--MaxSteps 20 -AllowDirty
-  Executed: False
-  Skipped: True
-  Exit code: 0
-  Message: DryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only invo
-kes the full-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCod
-ex.
-Plan preview:
-  Goal title: 업무 검색 결과 하이라이트 추가
-  Current task id: T001
-  Task T001: 업무 검색 결과 하이라이트 추가
-    Type: implementation
-    Status: in_progress
-    Priority: P0
-    Likely files: 
-    Verification: 변경 범위가 현재 목표와 일치하는지 확인한다. / 필요한 경우 npm run build를 별도로 실행한다.
-Stopped reason: dry_run
-Completed: False
-Exit code: 0
-
-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
---- a/scripts/ai-dev-auto-goal.ps1
-+++ b/scripts/ai-dev-auto-goal.ps1
-@@ -297,6 +297,10 @@
-         $errors += "queue.tasks must contain no more than three tasks."
-     }
- 
-+    if ($Queue.currentTaskId -ne "T001") {
-+        $errors += "queue.currentTaskId must be T001."
-+    }
-+
-     $taskIds = @()
- 
-     foreach ($task in $tasks) {
-@@ -360,6 +364,19 @@
-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-     }
- 
-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-+    if ($null -eq $firstTask) {
-+        $errors += "queue.tasks must include T001."
-+    } elseif ($firstTask.status -ne "in_progress") {
-+        $errors += "T001 must be in_progress."
-+    }
-+
-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-+        if ($laterTask.status -ne "pending") {
-+            $errors += "later tasks must be pending: $($laterTask.id)"
-+        }
-+    }
-+
-     return $errors
- }
- 
-@@ -388,6 +405,10 @@
-         $errors += "state.currentTaskId must match queue.currentTaskId."
-     }
- 
-+    if ($State.currentTaskId -ne "T001") {
-+        $errors += "state.currentTaskId must be T001."
-+    }
-+
-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-     }
-@@ -429,41 +450,44 @@
-     )
- 
-     $now = (Get-Date).ToUniversalTime().ToString("o")
--    $safeCommitTitle = $Title.Trim()
-+    $safeTitle = $Title.Trim()
-+    $safeDescription = $Description.Trim()
- 
-     $plan = [PSCustomObject][ordered]@{
-         goalMarkdown = @"
--# 紐⑺몴
-+# 목표
- 
--$safeCommitTitle
-+$safeTitle
- 
--## 諛곌꼍
-+## 배경
- 
--$($Description.Trim())
-+$safeDescription
- 
--## ?깃났 湲곗?
-+## 성공 기준
- 
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+- 입력한 목표에서 현재 작업을 준비한다.
-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
- 
--## ?쒖빟?ы빆
-+## 제약사항
- 
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-+- 저장소 정책과 현재 작업 범위를 따른다.
- 
--## ?쒖쇅 踰붿쐞
-+## 범위 제외
- 
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+- 패키지 변경
-+- DB 삭제 또는 초기화
-+- 광범위한 리팩터링
- 
--## ?섎룞 ?뺤씤
-+## 수동 검증
- 
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+- DryRun 계획과 JSON 출력을 검토한다.
-+- 현재 작업 프롬프트 생성 여부를 확인한다.
-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
- "@
-         queue = [PSCustomObject][ordered]@{
--            goalTitle = $safeCommitTitle
-+            goalTitle = $safeTitle
-             goalSource = $goalRelativePath
-             createdAt = $now
-             updatedAt = $now
-@@ -471,16 +495,16 @@
-             tasks = @(
-                 [PSCustomObject][ordered]@{
-                     id = "T001"
--                    title = $safeCommitTitle
--                    description = $Description.Trim()
-+                    title = $safeTitle
-+                    description = $safeDescription
-                     type = "implementation"
-                     status = "in_progress"
-                     priority = "P0"
-                     dependsOn = @()
-                     filesLikelyToChange = @()
-                     verification = @(
--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-+                        "필요한 경우 npm run build를 별도로 실행한다."
-                     )
-                     commitMessage = $null
-                 }
-@@ -503,43 +527,6 @@
-             stopReason = $null
-         }
-     }
--
--    $plan.goalMarkdown = @"
--# 紐⑺몴
--
--$safeCommitTitle
--
--## 諛곌꼍
--
--$($Description.Trim())
--
--## ?깃났 湲곗?
--
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
--
--## ?쒖빟?ы빆
--
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
--
--## ?쒖쇅 踰붿쐞
--
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
--
--## ?섎룞 ?뺤씤
--
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
--"@
--
--    $dryRunTask = @($plan.queue.tasks)[0]
--    $dryRunTask.verification = @(
--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
--    )
- 
-     return $plan
- }
-
 codex
-DryRun과 JSON 출력은 정상입니다. 빈 문자열 테스트는 PowerShell 인수 파서가 `''`를 값 없는 파라미터로 처리해 스크립트 내부 검증까지 들어가지 않았고, 공백
- 값과 생략 값 경로로 입력 검증을 다시 확인하겠습니다.
+방금 출력은 PowerShell 표시 인코딩 때문에 한글이 깨져 보였습니다. 파일 자체를 UTF-8로 다시 확인한 뒤 정확한 문자열만 패치하겠습니다.
 exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle ' ' -GoalDescription '설명' -DryRun -Jso
-n" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription '
-검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun -AllowRun -Json" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalDescription '설명' -DryRun -Json" in D:\ai-app
-s\planpilot-local
- exited 1 in 4098ms:
-{
-    "steps":  [
-                  {
-                      "step":  0,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  1,
-                      "message":  "GoalTitle must not be empty."
-                  }
-              ],
-    "stoppedReason":  "invalid_goal_title",
-    "completed":  false,
-    "exitCode":  1,
-    "plan":  null,
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-}
-
-2026-06-09T13:59:56.754354Z ERROR codex_core::tools::router: error=Exit code: 1
-Wall time: 4.1 seconds
-Output:
-{
-    "steps":  [
-                  {
-                      "step":  0,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  1,
-                      "message":  "GoalTitle must not be empty."
-                  }
-              ],
-    "stoppedReason":  "invalid_goal_title",
-    "completed":  false,
-    "exitCode":  1,
-    "plan":  null,
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-}
-
- exited 1 in 4252ms:
-{
-    "steps":  [
-                  {
-                      "step":  0,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  1,
-                      "message":  "GoalTitle must not be empty."
-                  }
-              ],
-    "stoppedReason":  "invalid_goal_title",
-    "completed":  false,
-    "exitCode":  1,
-    "plan":  null,
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-}
-
- succeeded in 4254ms:
-{
-    "steps":  [
-                  {
-                      "step":  1,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle/GoalDescription",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  0,
-                      "message":  "Input validation completed: 업무 검색 결과 하이라이트 추가"
-                  },
-                  {
-                      "step":  2,
-                      "name":  "dirty-worktree-gate",
-                      "command":  "git status --short",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: dirty worktree gate was not executed."
-                  },
-                  {
-                      "step":  3,
-                      "name":  "plan-goal",
-                      "command":  "codex exec \u003cauto-goal planning prompt\u003e",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: Codex goal planning was not executed. Preview currentTas
-kId: T001, task: 업무 검색 결과 하이라이트 추가"
-                  },
-                  {
-                      "step":  4,
-                      "name":  "validate-generated-json",
-                      "command":  "goal/queue/state JSON validation",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: preview goal/queue/state plan passed local schema valida
-tion."
-                  },
-                  {
-                      "step":  5,
-                      "name":  "write-state-files",
-                      "command":  ".ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: state files were not written."
-                  },
-                  {
-                      "step":  6,
-                      "name":  "make-prompt",
-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pro
-mpt.ps1",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: current-task-prompt.md was not generated."
-                  },
-                  {
-                      "step":  7,
-                      "name":  "auto-cycle-full",
-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cyc
-le-full.ps1 -MaxTasks 1 -MaxSteps 20 -AllowDirty",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: explicit run option is present, but full cycle was not e
-xecuted. -AllowRun only invokes the full-cycle wrapper; implementation and review still require -Al
-lowCodex and -AllowReviewCodex."
-                  }
-              ],
-    "stoppedReason":  "dry_run",
-    "completed":  false,
-    "exitCode":  0,
-    "plan":  {
-                 "goalMarkdown":  "# 목표\n\n업무 검색 결과 하이라이트 추가\n\n## 배경\n\n검색어와 일치하는 업무 제목/메모/프로젝트명을 
-화면에서 강조 표시한다.\n\n## 성공 기준\n\n- 입력한 목표에서 현재 작업을 준비한다.\n- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.\n- 
-full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.\n\n## 제약사항\n\n- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 gi
-t 명령은 사용하지 않는다.\n- 저장소 정책과 현재 작업 범위를 따른다.\n\n## 범위 제외\n\n- 패키지 변경\n- DB 삭제 또는 초기화\n- 광범위한 리팩터링\n\n#
-# 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재 작업 프롬프트 생성 여부를 확인한다.\n- 필요한 경우 빌드와 검증은 별도로 실행한다.",
-                 "queue":  {
-                               "goalTitle":  "업무 검색 결과 하이라이트 추가",
-                               "goalSource":  ".ai-dev/goal.md",
-                               "createdAt":  "2026-06-09T13:59:55.7497650Z",
-                               "updatedAt":  "2026-06-09T13:59:55.7497650Z",
-                               "currentTaskId":  "T001",
-                               "tasks":  [
-                                             {
-                                                 "id":  "T001",
-                                                 "title":  "업무 검색 결과 하이라이트 추가",
-                                                 "description":  "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조
- 표시한다.",
-                                                 "type":  "implementation",
-                                                 "status":  "in_progress",
-                                                 "priority":  "P0",
-                                                 "dependsOn":  [
-
-                                                               ],
-                                                 "filesLikelyToChange":  [
-
-                                                                         ],
-                                                 "verification":  [
-                                                                      "변경 범위가 현재 목표와 일치하는지 확인한다.",
-                                                                      "필요한 경우 npm run build를 별도로 실행
-한다."
-                                                                  ],
-                                                 "commitMessage":  null
-                                             }
-                                         ]
-                           },
-                 "state":  {
-                               "goalStatus":  "in_progress",
-                               "currentTaskId":  "T001",
-                               "currentLoop":  0,
-                               "maxLoopsPerTask":  2,
-                               "repeatedFailureCount":  0,
-                               "lastCommand":  null,
-                               "lastCommandStatus":  "not_started",
-                               "lastErrorSummary":  null,
-                               "lastReviewDecision":  "not_started",
-                               "lastReviewSeverity":  null,
-                               "lastCommitHash":  null,
-                               "startedAt":  "2026-06-09T13:59:55.7497650Z",
-                               "updatedAt":  "2026-06-09T13:59:55.7497650Z",
-                               "stopReason":  null
-                           }
-             },
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-}
+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -Encoding UT
+F8 -LiteralPath 'D:\\ai-apps\\planpilot-local\\src\\views\\TasksView.tsx'" in D:\ai-apps\planpilot-
+local
+ succeeded in 1185ms:
+import type { FormEvent } from "react";
+import { TaskCard } from "../components/TaskCard";
+import { TaskForm } from "../components/TaskForm";
+import type { Project, Task } from "../types";
+import type { TaskSortOrder } from "../utils/taskFilters";
+
+type TasksViewProps = {
+  projects: Project[];
+  filteredTasks: Task[];
+  sortedTasks: Task[];
+  selectedProjectFilter: string;
+  showCompletedTasks: boolean;
+  taskSortOrder: TaskSortOrder;
+  taskSearchQuery: string;
+  isTaskFormOpen: boolean;
+  newTaskTitle: string;
+  newTaskMemo: string;
+  newTaskDueDate: string;
+  newTaskPriority: Task["priority"];
+  newTaskProjectId: string;
+  editingTaskId: string | null;
+  editTaskTitle: string;
+  editTaskMemo: string;
+  editTaskDueDate: string;
+  editTaskPriority: Task["priority"];
+  editTaskProjectId: string;
+  getProjectName: (projectId: string) => string;
+  onTaskSearchQueryChange: (value: string) => void;
+  onShowCompletedTasksChange: (value: boolean) => void;
+  onTaskSortOrderChange: (value: TaskSortOrder) => void;
+  onSelectedProjectFilterChange: (value: string) => void;
+  onTaskFormOpenChange: (updater: (current: boolean) => boolean) => void;
+  onNewTaskTitleChange: (value: string) => void;
+  onNewTaskMemoChange: (value: string) => void;
+  onNewTaskDueDateChange: (value: string) => void;
+  onNewTaskPriorityChange: (value: Task["priority"]) => void;
+  onNewTaskProjectIdChange: (value: string) => void;
+  onEditTaskTitleChange: (value: string) => void;
+  onEditTaskMemoChange: (value: string) => void;
+  onEditTaskDueDateChange: (value: string) => void;
+  onEditTaskPriorityChange: (value: Task["priority"]) => void;
+  onEditTaskProjectIdChange: (value: string) => void;
+  onAddTask: (event: FormEvent<HTMLFormElement>) => void;
+  onSaveEditTask: (task: Task) => void;
+  onCancelEditTask: () => void;
+  onToggleTaskDone: (task: Task) => void;
+  onDeleteTask: (task: Task) => void;
+  onStartEditTask: (task: Task) => void;
+};
+
+export function TasksView({
+  projects,
+  filteredTasks,
+  sortedTasks,
+  selectedProjectFilter,
+  showCompletedTasks,
+  taskSortOrder,
+  taskSearchQuery,
+  isTaskFormOpen,
+  newTaskTitle,
+  newTaskMemo,
+  newTaskDueDate,
+  newTaskPriority,
+  newTaskProjectId,
+  editingTaskId,
+  editTaskTitle,
+  editTaskMemo,
+  editTaskDueDate,
+  editTaskPriority,
+  editTaskProjectId,
+  getProjectName,
+  onTaskSearchQueryChange,
+  onShowCompletedTasksChange,
+  onTaskSortOrderChange,
+  onSelectedProjectFilterChange,
+  onTaskFormOpenChange,
+  onNewTaskTitleChange,
+  onNewTaskMemoChange,
+  onNewTaskDueDateChange,
+  onNewTaskPriorityChange,
+  onNewTaskProjectIdChange,
+  onEditTaskTitleChange,
+  onEditTaskMemoChange,
+  onEditTaskDueDateChange,
+  onEditTaskPriorityChange,
+  onEditTaskProjectIdChange,
+  onAddTask,
+  onSaveEditTask,
+  onCancelEditTask,
+  onToggleTaskDone,
+  onDeleteTask,
+  onStartEditTask,
+}: TasksViewProps) {
+  const hasSearchQuery = taskSearchQuery.trim().length > 0;
+  const hasProjectFilter = selectedProjectFilter !== "all";
+  const hasVisibilityFilter = !showCompletedTasks;
+  const emptyMessage =
+    hasSearchQuery
+      ? "검색 결과가 없습니다."
+      : hasProjectFilter || hasVisibilityFilter
+        ? "현재 필터 조건에 맞는 업무가 없습니다."
+        : "등록된 업무가 없습니다.";
+
+  return (
+    <section className="screen-card">
+      <h2>전체 업무</h2>
+      <p className="summary">총 {filteredTasks.length}개</p>
+
+      <label>
+        업무 검색
+        <input
+          type="text"
+          value={taskSearchQuery}
+          onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+          placeholder="업무 제목이나 메모로 검색"
+        />
+      </label>
+
+      <label>
+        <input
+          type="checkbox"
+          checked={showCompletedTasks}
+          onChange={(event) => onShowCompletedTasksChange(event.target.checked)}
+        />
+        완료 업무 표시
+      </label>
+
+      <label>
+        정렬
+        <select
+          value={taskSortOrder}
+          onChange={(event) =>
+            onTaskSortOrderChange(event.target.value as TaskSortOrder)
+          }
+        >
+          <option value="none">기본 순서</option>
+          <option value="dueDateAsc">마감일 빠른 순</option>
+        </select>
+      </label>
+
+      <label>
+        프로젝트 필터
+        <select
+          value={selectedProjectFilter}
+          onChange={(event) => onSelectedProjectFilterChange(event.target.value)}
+        >
+          <option value="all">전체 프로젝트</option>
+          {projects.map((project) => (
+            <option key={project.id} value={project.id}>
+              {project.name}
+            </option>
+          ))}
+        </select>
+      </label>
+
+      <button
+        type="button"
+        onClick={() => onTaskFormOpenChange((current) => !current)}
+      >
+        {isTaskFormOpen ? "새 업무 추가 닫기" : "새 업무 추가"}
+      </button>
+
+      {isTaskFormOpen && (
+        <TaskForm
+          title="새 업무 추가"
+          ariaLabel="업무 추가"
+          taskTitle={newTaskTitle}
+          memo={newTaskMemo}
+          dueDate={newTaskDueDate}
+          priority={newTaskPriority}
+          projectId={newTaskProjectId}
+          projects={projects}
+          submitLabel="업무 추가"
+          onTitleChange={onNewTaskTitleChange}
+          onMemoChange={onNewTaskMemoChange}
+          onDueDateChange={onNewTaskDueDateChange}
+          onPriorityChange={onNewTaskPriorityChange}
+          onProjectIdChange={onNewTaskProjectIdChange}
+          onSubmit={onAddTask}
+        />
+      )}
+
+      {filteredTasks.length === 0 ? (
+        <div className="empty">
+          <p>{emptyMessage}</p>
+          {hasSearchQuery && <p>다른 검색어를 입력해보세요.</p>}
+        </div>
+      ) : (
+        <ul className="task-list">
+          {sortedTasks.map((task) => {
+            if (editingTaskId === task.id) {
+              return (
+                <li key={task.id} className="task-card">
+                  <TaskForm
+                    title="업무 수정"
+                    ariaLabel="업무 수정"
+                    taskTitle={editTaskTitle}
+                    memo={editTaskMemo}
+                    dueDate={editTaskDueDate}
+                    priority={editTaskPriority}
+                    projectId={editTaskProjectId}
+                    projects={projects}
+                    submitLabel="저장"
+                    onTitleChange={onEditTaskTitleChange}
+                    onMemoChange={onEditTaskMemoChange}
+                    onDueDateChange={onEditTaskDueDateChange}
+                    onPriorityChange={onEditTaskPriorityChange}
+                    onProjectIdChange={onEditTaskProjectIdChange}
+                    onSubmit={(event) => {
+                      event.preventDefault();
+                      onSaveEditTask(task);
+                    }}
+                    onCancel={onCancelEditTask}
+                    className=""
+                  />
+                </li>
+              );
+            }
 
-2026-06-09T13:59:56.936113Z ERROR codex_core::tools::router: error=Exit code: 1
-Wall time: 4.3 seconds
-Output:
-{
-    "steps":  [
-                  {
-                      "step":  0,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  1,
-                      "message":  "GoalTitle must not be empty."
-                  }
-              ],
-    "stoppedReason":  "invalid_goal_title",
-    "completed":  false,
-    "exitCode":  1,
-    "plan":  null,
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+            return (
+              <TaskCard
+                key={task.id}
+                task={task}
+                projectName={getProjectName(task.projectId)}
+                onToggleDone={onToggleTaskDone}
+                onDelete={onDeleteTask}
+                onStartEdit={onStartEditTask}
+              />
+            );
+          })}
+        </ul>
+      )}
+    </section>
+  );
 }
 
-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
---- a/scripts/ai-dev-auto-goal.ps1
-+++ b/scripts/ai-dev-auto-goal.ps1
-@@ -297,6 +297,10 @@
-         $errors += "queue.tasks must contain no more than three tasks."
-     }
- 
-+    if ($Queue.currentTaskId -ne "T001") {
-+        $errors += "queue.currentTaskId must be T001."
-+    }
-+
-     $taskIds = @()
- 
-     foreach ($task in $tasks) {
-@@ -360,6 +364,19 @@
-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-     }
- 
-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-+    if ($null -eq $firstTask) {
-+        $errors += "queue.tasks must include T001."
-+    } elseif ($firstTask.status -ne "in_progress") {
-+        $errors += "T001 must be in_progress."
-+    }
-+
-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-+        if ($laterTask.status -ne "pending") {
-+            $errors += "later tasks must be pending: $($laterTask.id)"
-+        }
-+    }
-+
-     return $errors
- }
- 
-@@ -388,6 +405,10 @@
-         $errors += "state.currentTaskId must match queue.currentTaskId."
-     }
- 
-+    if ($State.currentTaskId -ne "T001") {
-+        $errors += "state.currentTaskId must be T001."
-+    }
-+
-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-     }
-@@ -429,41 +450,44 @@
-     )
- 
-     $now = (Get-Date).ToUniversalTime().ToString("o")
--    $safeCommitTitle = $Title.Trim()
-+    $safeTitle = $Title.Trim()
-+    $safeDescription = $Description.Trim()
- 
-     $plan = [PSCustomObject][ordered]@{
-         goalMarkdown = @"
--# 紐⑺몴
-+# 목표
- 
--$safeCommitTitle
-+$safeTitle
- 
--## 諛곌꼍
-+## 배경
- 
--$($Description.Trim())
-+$safeDescription
- 
--## ?깃났 湲곗?
-+## 성공 기준
- 
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+- 입력한 목표에서 현재 작업을 준비한다.
-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
- 
--## ?쒖빟?ы빆
-+## 제약사항
- 
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-+- 저장소 정책과 현재 작업 범위를 따른다.
- 
--## ?쒖쇅 踰붿쐞
-+## 범위 제외
- 
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+- 패키지 변경
-+- DB 삭제 또는 초기화
-+- 광범위한 리팩터링
- 
--## ?섎룞 ?뺤씤
-+## 수동 검증
- 
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+- DryRun 계획과 JSON 출력을 검토한다.
-+- 현재 작업 프롬프트 생성 여부를 확인한다.
-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
- "@
-         queue = [PSCustomObject][ordered]@{
--            goalTitle = $safeCommitTitle
-+            goalTitle = $safeTitle
-             goalSource = $goalRelativePath
-             createdAt = $now
-             updatedAt = $now
-@@ -471,16 +495,16 @@
-             tasks = @(
-                 [PSCustomObject][ordered]@{
-                     id = "T001"
--                    title = $safeCommitTitle
--                    description = $Description.Trim()
-+                    title = $safeTitle
-+                    description = $safeDescription
-                     type = "implementation"
-                     status = "in_progress"
-                     priority = "P0"
-                     dependsOn = @()
-                     filesLikelyToChange = @()
-                     verification = @(
--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-+                        "필요한 경우 npm run build를 별도로 실행한다."
-                     )
-                     commitMessage = $null
-                 }
-@@ -503,43 +527,6 @@
-             stopReason = $null
-         }
-     }
--
--    $plan.goalMarkdown = @"
--# 紐⑺몴
--
--$safeCommitTitle
--
--## 諛곌꼍
--
--$($Description.Trim())
--
--## ?깃났 湲곗?
--
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
--
--## ?쒖빟?ы빆
--
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
--
--## ?쒖쇅 踰붿쐞
--
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
--
--## ?섎룞 ?뺤씤
--
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
--"@
--
--    $dryRunTask = @($plan.queue.tasks)[0]
--    $dryRunTask.verification = @(
--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
--    )
+codex
+변경 대상은 단일 input의 `placeholder`뿐입니다. 검색 상태, 필터링 로직, JSX 구조는 그대로 둡니다.
+apply patch
+patch: completed
+D:\ai-apps\planpilot-local\src\views\TasksView.tsx
+diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
+index 7167cf7befdbe0130902ab6d86eb19a9eab5b300..d887bf99d720d17cb48f7bbbb3126e5de8490399
+--- a/src/views/TasksView.tsx
++++ b/src/views/TasksView.tsx
+@@ -112,7 +112,7 @@
+           type="text"
+           value={taskSearchQuery}
+           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+-          placeholder="업무 제목이나 메모로 검색"
++          placeholder="업무 제목 또는 메모 내용을 검색하세요"
+         />
+       </label>
+ 
+
+diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
+index 7167cf7befdbe0130902ab6d86eb19a9eab5b300..d887bf99d720d17cb48f7bbbb3126e5de8490399
+--- a/src/views/TasksView.tsx
++++ b/src/views/TasksView.tsx
+@@ -112,7 +112,7 @@
+           type="text"
+           value={taskSearchQuery}
+           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+-          placeholder="업무 제목이나 메모로 검색"
++          placeholder="업무 제목 또는 메모 내용을 검색하세요"
+         />
+       </label>
  
-     return $plan
- }
 
 codex
-검증 결과 DryRun은 파일을 쓰지 않고 계획과 단계만 출력했고, JSON 모드는 텍스트와 섞이지 않았습니다. 마지막으로 수정된 함수 주변을 다시 읽어 의도치 않은 중복이나 깨
-진 문자열이 남아 있는지 확인하겠습니다.
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPa
-th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern 'queue.currentTaskId must
- be T001','T001 must be in_progress','function New-DryRunAutoGoalPlan' -Context 0,35" in D:\ai-apps
-\planpilot-local
+문구 수정은 적용됐습니다. 빌드나 lint는 지시상 자동 실행 대상이 아니므로, 파일 내 변경 위치만 확인하겠습니다.
 exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPa
-th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern '紐|諛|쒖|꾩|蹂|湲|젣|뺤|⑦'" in D
-:\ai-apps\planpilot-local
- succeeded in 2011ms:
- succeeded in 2149ms:
-
-> scripts\ai-dev-auto-goal.ps1:301:        $errors += "queue.currentTaskId must be T001."
-  scripts\ai-dev-auto-goal.ps1:302:    }
-  scripts\ai-dev-auto-goal.ps1:303:
-  scripts\ai-dev-auto-goal.ps1:304:    $taskIds = @()
-  scripts\ai-dev-auto-goal.ps1:305:
-  scripts\ai-dev-auto-goal.ps1:306:    foreach ($task in $tasks) {
-  scripts\ai-dev-auto-goal.ps1:307:        $taskRequiredFields = @("id", "title", "description", "t
-ype", "status", "pri
-ority", "dependsOn", "filesLikelyToChange", "verification", "commitMessage")
-  scripts\ai-dev-auto-goal.ps1:308:        $taskMissingFields = @(Get-MissingFields $task $taskRequ
-iredFields)
-  scripts\ai-dev-auto-goal.ps1:309:
-  scripts\ai-dev-auto-goal.ps1:310:        foreach ($field in $taskMissingFields) {
-  scripts\ai-dev-auto-goal.ps1:311:            $errors += "task missing field: $field"
-  scripts\ai-dev-auto-goal.ps1:312:        }
-  scripts\ai-dev-auto-goal.ps1:313:
-  scripts\ai-dev-auto-goal.ps1:314:        if ($taskMissingFields.Count -gt 0) {
-  scripts\ai-dev-auto-goal.ps1:315:            continue
-  scripts\ai-dev-auto-goal.ps1:316:        }
-  scripts\ai-dev-auto-goal.ps1:317:
-  scripts\ai-dev-auto-goal.ps1:318:        if (-not (Test-HasValue $task.id)) {
-  scripts\ai-dev-auto-goal.ps1:319:            $errors += "task.id must not be empty."
-  scripts\ai-dev-auto-goal.ps1:320:        } else {
-  scripts\ai-dev-auto-goal.ps1:321:            $taskIds += [string]$task.id
-  scripts\ai-dev-auto-goal.ps1:322:        }
-  scripts\ai-dev-auto-goal.ps1:323:
-  scripts\ai-dev-auto-goal.ps1:324:        if (-not (Test-HasValue $task.title)) {
-  scripts\ai-dev-auto-goal.ps1:325:            $errors += "task.title must not be empty."
-  scripts\ai-dev-auto-goal.ps1:326:        }
-  scripts\ai-dev-auto-goal.ps1:327:
-  scripts\ai-dev-auto-goal.ps1:328:        if (-not (Test-HasValue $task.description)) {
-  scripts\ai-dev-auto-goal.ps1:329:            $errors += "task.description must not be empty."
-  scripts\ai-dev-auto-goal.ps1:330:        }
-  scripts\ai-dev-auto-goal.ps1:331:
-  scripts\ai-dev-auto-goal.ps1:332:        if (@("analysis", "implementation", "documentation", "ve
-rification") -notcon
-tains $task.type) {
-  scripts\ai-dev-auto-goal.ps1:333:            $errors += "invalid task.type: $($task.type)"
-  scripts\ai-dev-auto-goal.ps1:334:        }
-  scripts\ai-dev-auto-goal.ps1:335:
-  scripts\ai-dev-auto-goal.ps1:336:        if (@("pending", "in_progress", "done", "blocked") -notc
-ontains $task.status
-) {
-> scripts\ai-dev-auto-goal.ps1:371:        $errors += "T001 must be in_progress."
-  scripts\ai-dev-auto-goal.ps1:372:    }
-  scripts\ai-dev-auto-goal.ps1:373:
-  scripts\ai-dev-auto-goal.ps1:374:    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "
-T001" })) {
-  scripts\ai-dev-auto-goal.ps1:375:        if ($laterTask.status -ne "pending") {
-  scripts\ai-dev-auto-goal.ps1:376:            $errors += "later tasks must be pending: $($laterTas
-k.id)"
-  scripts\ai-dev-auto-goal.ps1:377:        }
-  scripts\ai-dev-auto-goal.ps1:378:    }
-  scripts\ai-dev-auto-goal.ps1:379:
-  scripts\ai-dev-auto-goal.ps1:380:    return $errors
-  scripts\ai-dev-auto-goal.ps1:381:}
-  scripts\ai-dev-auto-goal.ps1:382:
-  scripts\ai-dev-auto-goal.ps1:383:function Get-StateValidationErrors {
-  scripts\ai-dev-auto-goal.ps1:384:    param(
-  scripts\ai-dev-auto-goal.ps1:385:        [object]$State,
-  scripts\ai-dev-auto-goal.ps1:386:        [object]$Queue
-  scripts\ai-dev-auto-goal.ps1:387:    )
-  scripts\ai-dev-auto-goal.ps1:388:
-  scripts\ai-dev-auto-goal.ps1:389:    $errors = @()
-  scripts\ai-dev-auto-goal.ps1:390:    $requiredFields = @("goalStatus", "currentTaskId", "currentL
-oop", "maxLoopsPerTa
-sk", "repeatedFailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDec
-ision", "lastReviewS
-everity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
-  scripts\ai-dev-auto-goal.ps1:391:
-  scripts\ai-dev-auto-goal.ps1:392:    foreach ($field in @(Get-MissingFields $State $requiredField
-s)) {
-  scripts\ai-dev-auto-goal.ps1:393:        $errors += "state missing field: $field"
-  scripts\ai-dev-auto-goal.ps1:394:    }
-  scripts\ai-dev-auto-goal.ps1:395:
-  scripts\ai-dev-auto-goal.ps1:396:    if ($errors.Count -gt 0) {
-  scripts\ai-dev-auto-goal.ps1:397:        return $errors
-  scripts\ai-dev-auto-goal.ps1:398:    }
-  scripts\ai-dev-auto-goal.ps1:399:
-  scripts\ai-dev-auto-goal.ps1:400:    if (@("idle", "in_progress", "completed", "blocked") -notcon
-tains $State.goalSta
-tus) {
-  scripts\ai-dev-auto-goal.ps1:401:        $errors += "invalid state.goalStatus: $($State.goalStatu
-s)"
-  scripts\ai-dev-auto-goal.ps1:402:    }
-  scripts\ai-dev-auto-goal.ps1:403:
-  scripts\ai-dev-auto-goal.ps1:404:    if ($State.currentTaskId -ne $Queue.currentTaskId) {
-  scripts\ai-dev-auto-goal.ps1:405:        $errors += "state.currentTaskId must match queue.current
-TaskId."
-  scripts\ai-dev-auto-goal.ps1:406:    }
-> scripts\ai-dev-auto-goal.ps1:446:function New-DryRunAutoGoalPlan {
-  scripts\ai-dev-auto-goal.ps1:447:    param(
-  scripts\ai-dev-auto-goal.ps1:448:        [string]$Title,
-  scripts\ai-dev-auto-goal.ps1:449:        [string]$Description
-  scripts\ai-dev-auto-goal.ps1:450:    )
-  scripts\ai-dev-auto-goal.ps1:451:
-  scripts\ai-dev-auto-goal.ps1:452:    $now = (Get-Date).ToUniversalTime().ToString("o")
-  scripts\ai-dev-auto-goal.ps1:453:    $safeTitle = $Title.Trim()
-  scripts\ai-dev-auto-goal.ps1:454:    $safeDescription = $Description.Trim()
-  scripts\ai-dev-auto-goal.ps1:455:
-  scripts\ai-dev-auto-goal.ps1:456:    $plan = [PSCustomObject][ordered]@{
-  scripts\ai-dev-auto-goal.ps1:457:        goalMarkdown = @"
-  scripts\ai-dev-auto-goal.ps1:458:# 목표
-  scripts\ai-dev-auto-goal.ps1:459:
-  scripts\ai-dev-auto-goal.ps1:460:$safeTitle
-  scripts\ai-dev-auto-goal.ps1:461:
-  scripts\ai-dev-auto-goal.ps1:462:## 배경
-  scripts\ai-dev-auto-goal.ps1:463:
-  scripts\ai-dev-auto-goal.ps1:464:$safeDescription
-  scripts\ai-dev-auto-goal.ps1:465:
-  scripts\ai-dev-auto-goal.ps1:466:## 성공 기준
-  scripts\ai-dev-auto-goal.ps1:467:
-  scripts\ai-dev-auto-goal.ps1:468:- 입력한 목표에서 현재 작업을 준비한다.
-  scripts\ai-dev-auto-goal.ps1:469:- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-  scripts\ai-dev-auto-goal.ps1:470:- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-  scripts\ai-dev-auto-goal.ps1:471:
-  scripts\ai-dev-auto-goal.ps1:472:## 제약사항
-  scripts\ai-dev-auto-goal.ps1:473:
-  scripts\ai-dev-auto-goal.ps1:474:- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-  scripts\ai-dev-auto-goal.ps1:475:- 저장소 정책과 현재 작업 범위를 따른다.
-  scripts\ai-dev-auto-goal.ps1:476:
-  scripts\ai-dev-auto-goal.ps1:477:## 범위 제외
-  scripts\ai-dev-auto-goal.ps1:478:
-  scripts\ai-dev-auto-goal.ps1:479:- 패키지 변경
-  scripts\ai-dev-auto-goal.ps1:480:- DB 삭제 또는 초기화
-  scripts\ai-dev-auto-goal.ps1:481:- 광범위한 리팩터링
-
-
-
-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
---- a/scripts/ai-dev-auto-goal.ps1
-+++ b/scripts/ai-dev-auto-goal.ps1
-@@ -297,6 +297,10 @@
-         $errors += "queue.tasks must contain no more than three tasks."
-     }
- 
-+    if ($Queue.currentTaskId -ne "T001") {
-+        $errors += "queue.currentTaskId must be T001."
-+    }
-+
-     $taskIds = @()
- 
-     foreach ($task in $tasks) {
-@@ -360,6 +364,19 @@
-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-     }
- 
-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-+    if ($null -eq $firstTask) {
-+        $errors += "queue.tasks must include T001."
-+    } elseif ($firstTask.status -ne "in_progress") {
-+        $errors += "T001 must be in_progress."
-+    }
-+
-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-+        if ($laterTask.status -ne "pending") {
-+            $errors += "later tasks must be pending: $($laterTask.id)"
-+        }
-+    }
-+
-     return $errors
- }
- 
-@@ -388,6 +405,10 @@
-         $errors += "state.currentTaskId must match queue.currentTaskId."
-     }
- 
-+    if ($State.currentTaskId -ne "T001") {
-+        $errors += "state.currentTaskId must be T001."
-+    }
-+
-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-     }
-@@ -429,41 +450,44 @@
-     )
- 
-     $now = (Get-Date).ToUniversalTime().ToString("o")
--    $safeCommitTitle = $Title.Trim()
-+    $safeTitle = $Title.Trim()
-+    $safeDescription = $Description.Trim()
- 
-     $plan = [PSCustomObject][ordered]@{
-         goalMarkdown = @"
--# 紐⑺몴
-+# 목표
- 
--$safeCommitTitle
-+$safeTitle
- 
--## 諛곌꼍
-+## 배경
- 
--$($Description.Trim())
-+$safeDescription
- 
--## ?깃났 湲곗?
-+## 성공 기준
- 
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+- 입력한 목표에서 현재 작업을 준비한다.
-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
- 
--## ?쒖빟?ы빆
-+## 제약사항
- 
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-+- 저장소 정책과 현재 작업 범위를 따른다.
+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "rg -n \"placeholder\" sr
+c\\views\\TasksView.tsx" in D:\ai-apps\planpilot-local
+ succeeded in 2216ms:
+115:          placeholder="업무 제목 또는 메모 내용을 검색하세요"
+
+diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
+index 7167cf7befdbe0130902ab6d86eb19a9eab5b300..d887bf99d720d17cb48f7bbbb3126e5de8490399
+--- a/src/views/TasksView.tsx
++++ b/src/views/TasksView.tsx
+@@ -112,7 +112,7 @@
+           type="text"
+           value={taskSearchQuery}
+           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+-          placeholder="업무 제목이나 메모로 검색"
++          placeholder="업무 제목 또는 메모 내용을 검색하세요"
+         />
+       </label>
  
--## ?쒖쇅 踰붿쐞
-+## 범위 제외
- 
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+- 패키지 변경
-+- DB 삭제 또는 초기화
-+- 광범위한 리팩터링
- 
--## ?섎룞 ?뺤씤
-+## 수동 검증
- 
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+- DryRun 계획과 JSON 출력을 검토한다.
-+- 현재 작업 프롬프트 생성 여부를 확인한다.
-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
- "@
-         queue = [PSCustomObject][ordered]@{
--            goalTitle = $safeCommitTitle
-+            goalTitle = $safeTitle
-             goalSource = $goalRelativePath
-             createdAt = $now
-             updatedAt = $now
-@@ -471,16 +495,16 @@
-             tasks = @(
-                 [PSCustomObject][ordered]@{
-                     id = "T001"
--                    title = $safeCommitTitle
--                    description = $Description.Trim()
-+                    title = $safeTitle
-+                    description = $safeDescription
-                     type = "implementation"
-                     status = "in_progress"
-                     priority = "P0"
-                     dependsOn = @()
-                     filesLikelyToChange = @()
-                     verification = @(
--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-+                        "필요한 경우 npm run build를 별도로 실행한다."
-                     )
-                     commitMessage = $null
-                 }
-@@ -503,43 +527,6 @@
-             stopReason = $null
-         }
-     }
--
--    $plan.goalMarkdown = @"
--# 紐⑺몴
--
--$safeCommitTitle
--
--## 諛곌꼍
--
--$($Description.Trim())
--
--## ?깃났 湲곗?
--
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
--
--## ?쒖빟?ы빆
--
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
--
--## ?쒖쇅 踰붿쐞
--
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
--
--## ?섎룞 ?뺤씤
--
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
--"@
--
--    $dryRunTask = @($plan.queue.tasks)[0]
--    $dryRunTask.verification = @(
--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
--    )
- 
-     return $plan
- }
 
 codex
-완료했습니다.
-
-변경한 파일:
-- [scripts/ai-dev-auto-goal.ps1](D:/ai-apps/planpilot-local/scripts/ai-dev-auto-goal.ps1:301)
-
-구현 요약:
-- DryRun goal/task 미리보기의 깨진 한국어 문자열을 정상 한국어로 복구했습니다.
-- Codex가 생성한 `queue/state` 검증을 강화했습니다.
-  - `queue.currentTaskId`는 `T001`이어야 함
-  - `state.currentTaskId`는 `T001`이어야 함
-  - `T001` task가 존재하고 `in_progress`여야 함
-  - 이후 task는 `pending`이어야 함
-- `.ai-dev/README.md`에는 이미 `ai-dev-auto-goal.ps1` 사용법과 안전 기준이 문서화되어 있어 수정하지 않았습니다.
-
-검증:
-- PowerShell 문법 검증 통과
-- `-DryRun` 텍스트 출력 확인
-- `-DryRun -Json` 순수 JSON 출력 확인
-- 공백/누락 `GoalTitle` 오류 종료 확인
-- `-DryRun -AllowRun -Json`에서 full-cycle 호출이 실제 실행되지 않고 단계만 표시되는 것 확인
-
-실행하지 않은 검증:
-- `npm run build`, `npm run lint`, `npm run test`는 지시 범위상 실행하지 않았습니다.
-- 실제 Codex CLI 실행 및 실제 full cycle 실행은 파일 작성/외부 실행을 수반하므로 실행하지 않았습니다.
+변경 완료했습니다.
 
-남은 위험:
-- 실제 `codex exec` 출력 품질과 실제 dirty worktree 게이트는 실사용 환경에서 별도 확인이 필요합니다.
-
-다음 task로 넘어가도 됩니다.
+- 변경 파일: [src/views/TasksView.tsx](D:/ai-apps/planpilot-local/src/views/TasksView.tsx:115)
+- 변경 내용: 업무 검색창 placeholder를 `업무 제목 또는 메모 내용을 검색하세요`로 수정했습니다.
+- 유지한 내용: 검색 입력 상태, 필터링 동작, 컴포넌트 구조는 변경하지 않았습니다.
+- 검증: `rg -n "placeholder" src\views\TasksView.tsx`로 변경된 문구를 확인했습니다.
+- 미실행 검증: 지시상 자동 build/test/lint 대상이 아니므로 실행하지 않았습니다.
+- 남은 위험: 화면 수동 확인은 아직 필요합니다.
+- 다음 task 진행 여부: 현재 task 기준으로 다음 task로 넘어가도 됩니다.
 
 `.ai-dev/loop-log.md` 기록용 요약:
-`T007: ai-dev-auto-goal.ps1 DryRun 미리보기 문자열을 복구하고 goal/queue/state 생성 검증을 T001 시작 규칙에 맞게 강화했다. Powe
-rShell 문법, DryRun, Json 출력, 빈 GoalTitle 오류 경로를 확인했다.`
-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
---- a/scripts/ai-dev-auto-goal.ps1
-+++ b/scripts/ai-dev-auto-goal.ps1
-@@ -297,6 +297,10 @@
-         $errors += "queue.tasks must contain no more than three tasks."
-     }
- 
-+    if ($Queue.currentTaskId -ne "T001") {
-+        $errors += "queue.currentTaskId must be T001."
-+    }
-+
-     $taskIds = @()
- 
-     foreach ($task in $tasks) {
-@@ -360,6 +364,19 @@
-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-     }
- 
-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-+    if ($null -eq $firstTask) {
-+        $errors += "queue.tasks must include T001."
-+    } elseif ($firstTask.status -ne "in_progress") {
-+        $errors += "T001 must be in_progress."
-+    }
-+
-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-+        if ($laterTask.status -ne "pending") {
-+            $errors += "later tasks must be pending: $($laterTask.id)"
-+        }
-+    }
-+
-     return $errors
- }
- 
-@@ -388,6 +405,10 @@
-         $errors += "state.currentTaskId must match queue.currentTaskId."
-     }
- 
-+    if ($State.currentTaskId -ne "T001") {
-+        $errors += "state.currentTaskId must be T001."
-+    }
-+
-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-     }
-@@ -429,41 +450,44 @@
-     )
- 
-     $now = (Get-Date).ToUniversalTime().ToString("o")
--    $safeCommitTitle = $Title.Trim()
-+    $safeTitle = $Title.Trim()
-+    $safeDescription = $Description.Trim()
- 
-     $plan = [PSCustomObject][ordered]@{
-         goalMarkdown = @"
--# 紐⑺몴
-+# 목표
+`T001 업무 검색창 placeholder 문구 수정: src/views/TasksView.tsx의 업무 검색 input placeholder를 "업무 제목 또는 메모 내용을 
+검색하세요"로 변경했다. 검색 동작과 상태 관리는 변경하지 않았고, 파일 내 placeholder 문자열 확인만 수행했다.`
+diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
+index 7167cf7befdbe0130902ab6d86eb19a9eab5b300..d887bf99d720d17cb48f7bbbb3126e5de8490399
+--- a/src/views/TasksView.tsx
++++ b/src/views/TasksView.tsx
+@@ -112,7 +112,7 @@
+           type="text"
+           value={taskSearchQuery}
+           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+-          placeholder="업무 제목이나 메모로 검색"
++          placeholder="업무 제목 또는 메모 내용을 검색하세요"
+         />
+       </label>
  
--$safeCommitTitle
-+$safeTitle
- 
--## 諛곌꼍
-+## 배경
- 
--$($Description.Trim())
-+$safeDescription
- 
--## ?깃났 湲곗?
-+## 성공 기준
- 
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+- 입력한 목표에서 현재 작업을 준비한다.
-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
- 
--## ?쒖빟?ы빆
-+## 제약사항
- 
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-+- 저장소 정책과 현재 작업 범위를 따른다.
- 
--## ?쒖쇅 踰붿쐞
-+## 범위 제외
- 
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+- 패키지 변경
-+- DB 삭제 또는 초기화
-+- 광범위한 리팩터링
- 
--## ?섎룞 ?뺤씤
-+## 수동 검증
- 
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+- DryRun 계획과 JSON 출력을 검토한다.
-+- 현재 작업 프롬프트 생성 여부를 확인한다.
-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
- "@
-         queue = [PSCustomObject][ordered]@{
--            goalTitle = $safeCommitTitle
-+            goalTitle = $safeTitle
-             goalSource = $goalRelativePath
-             createdAt = $now
-             updatedAt = $now
-@@ -471,16 +495,16 @@
-             tasks = @(
-                 [PSCustomObject][ordered]@{
-                     id = "T001"
--                    title = $safeCommitTitle
--                    description = $Description.Trim()
-+                    title = $safeTitle
-+                    description = $safeDescription
-                     type = "implementation"
-                     status = "in_progress"
-                     priority = "P0"
-                     dependsOn = @()
-                     filesLikelyToChange = @()
-                     verification = @(
--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-+                        "필요한 경우 npm run build를 별도로 실행한다."
-                     )
-                     commitMessage = $null
-                 }
-@@ -503,43 +527,6 @@
-             stopReason = $null
-         }
-     }
--
--    $plan.goalMarkdown = @"
--# 紐⑺몴
--
--$safeCommitTitle
--
--## 諛곌꼍
--
--$($Description.Trim())
--
--## ?깃났 湲곗?
--
--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
--
--## ?쒖빟?ы빆
--
--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
--
--## ?쒖쇅 踰붿쐞
--
--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
--
--## ?섎룞 ?뺤씤
--
--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
--"@
--
--    $dryRunTask = @($plan.queue.tasks)[0]
--    $dryRunTask.verification = @(
--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
--    )
- 
-     return $plan
- }
 
 tokens used
-완료했습니다.
-
-변경한 파일:
-- [scripts/ai-dev-auto-goal.ps1](D:/ai-apps/planpilot-local/scripts/ai-dev-auto-goal.ps1:301)
-
-구현 요약:
-- DryRun goal/task 미리보기의 깨진 한국어 문자열을 정상 한국어로 복구했습니다.
-- Codex가 생성한 `queue/state` 검증을 강화했습니다.
-  - `queue.currentTaskId`는 `T001`이어야 함
-  - `state.currentTaskId`는 `T001`이어야 함
-  - `T001` task가 존재하고 `in_progress`여야 함
-  - 이후 task는 `pending`이어야 함
-- `.ai-dev/README.md`에는 이미 `ai-dev-auto-goal.ps1` 사용법과 안전 기준이 문서화되어 있어 수정하지 않았습니다.
-
-검증:
-- PowerShell 문법 검증 통과
-- `-DryRun` 텍스트 출력 확인
-- `-DryRun -Json` 순수 JSON 출력 확인
-- 공백/누락 `GoalTitle` 오류 종료 확인
-- `-DryRun -AllowRun -Json`에서 full-cycle 호출이 실제 실행되지 않고 단계만 표시되는 것 확인
-
-실행하지 않은 검증:
-- `npm run build`, `npm run lint`, `npm run test`는 지시 범위상 실행하지 않았습니다.
-- 실제 Codex CLI 실행 및 실제 full cycle 실행은 파일 작성/외부 실행을 수반하므로 실행하지 않았습니다.
-
-남은 위험:
-- 실제 `codex exec` 출력 품질과 실제 dirty worktree 게이트는 실사용 환경에서 별도 확인이 필요합니다.
+12,353
+변경 완료했습니다.
 
-다음 task로 넘어가도 됩니다.
+- 변경 파일: [src/views/TasksView.tsx](D:/ai-apps/planpilot-local/src/views/TasksView.tsx:115)
+- 변경 내용: 업무 검색창 placeholder를 `업무 제목 또는 메모 내용을 검색하세요`로 수정했습니다.
+- 유지한 내용: 검색 입력 상태, 필터링 동작, 컴포넌트 구조는 변경하지 않았습니다.
+- 검증: `rg -n "placeholder" src\views\TasksView.tsx`로 변경된 문구를 확인했습니다.
+- 미실행 검증: 지시상 자동 build/test/lint 대상이 아니므로 실행하지 않았습니다.
+- 남은 위험: 화면 수동 확인은 아직 필요합니다.
+- 다음 task 진행 여부: 현재 task 기준으로 다음 task로 넘어가도 됩니다.
 
 `.ai-dev/loop-log.md` 기록용 요약:
-`T007: ai-dev-auto-goal.ps1 DryRun 미리보기 문자열을 복구하고 goal/queue/state 생성 검증을 T001 시작 규칙에 맞게 강화했다. PowerShell 문법, DryRun, Json 출력, 빈 GoalTitle 오류 경로를 확인했다.`
-71,363
+`T001 업무 검색창 placeholder 문구 수정: src/views/TasksView.tsx의 업무 검색 input placeholder를 "업무 제목 또는 메모 내용을 검색하세요"로 변경했다. 검색 동작과 상태 관리는 변경하지 않았고, 파일 내 placeholder 문자열 확인만 수행했다.`
 
 ```
\ No newline at end of file
diff --git a/.ai-dev/codex-review-result.md b/.ai-dev/codex-review-result.md
index 74a2166..7ce6718 100644
--- a/.ai-dev/codex-review-result.md
+++ b/.ai-dev/codex-review-result.md
@@ -2,8 +2,8 @@
 
 ## Run
 
-- Started at: 2026-06-09 23:00:55
-- Ended at: 2026-06-09 23:02:38
+- Started at: 2026-06-09 23:44:02
+- Ended at: 2026-06-09 23:45:07
 - Exit code: 0
 - Review prompt: .ai-dev/review-prompt.md
 - Review response: .ai-dev/review-response.json
@@ -27,17 +27,17 @@ approval: never
 sandbox: workspace-write [workdir, /tmp, $TMPDIR]
 reasoning effort: medium
 reasoning summaries: none
-session id: 019eacaf-afd2-7b70-9fcd-8f3b72249eb0
+session id: 019eacd7-2a54-7d02-b548-4ce71a7914a6
 --------
 user
 Read and follow the full review prompt at this absolute file path: D:\ai-apps\planpilot-local\.ai-d
 ev\review-prompt.md
 codex
-`.ai-dev\review-prompt.md`의 지시를 먼저 읽고, 그 범위에 맞춰 리뷰를 진행하겠습니다. 읽기만 수행합니다.
+지정된 리뷰 프롬프트를 먼저 읽고, 그 지시 범위 안에서만 필요한 파일을 확인하겠습니다.
 exec
 "C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
  'D:\\ai-apps\\planpilot-local\\.ai-dev\\review-prompt.md'" in D:\ai-apps\planpilot-local
- succeeded in 2887ms:
+ succeeded in 1341ms:
 # AI Dev Review Prompt
 
 ## Role
@@ -52,100 +52,54 @@ exec
 
 ## Project Goal
 
-# Goal
-
-AI Dev Loop Codex CLI 완전 자동화 도입
-
-## Background
-
-Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완료 처리를 가능한 한 end-to-end로 자동화한다.
-
-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생
-성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
-
-초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commit 
-금지 정책을 적용한다.
-
-## Success Criteria
-
-- Codex CLI 실행 정책이 문서화된다.
-- `current-task-prompt.md`를 Codex CLI에 전달하는 `ai-dev-run-codex.ps1`이 추가된다.
-- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-review-codex.ps1`이 추가된다.
-- Codex 구현, build/check, diff, Codex 리뷰, save-review 흐름이 연결된다.
-- pass 리뷰에서만 자동 커밋과 complete-task를 수행하는 full cycle 초안이 추가된다.
-- git status가 dirty이면 Codex 실행을 중단한다.
-- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-- build 실패 시 자동 커밋하지 않는다.
-- git reset, git clean, npm install은 자동 실행하지 않는다.
-- PowerShell 문법 검증이 통과한다.
-- 작은 앱 기능 task로 MaxTasks 1 end-to-end 검증이 가능하다.
-
-## Constraints
-
-- Codex CLI는 구현자와 리뷰어로 사용한다.
-- GPT API 키 없이 진행한다.
-- Cline은 삭제되어 사용하지 않는다.
-- Copilot CLI와 gh는 현재 사용하지 않는다.
-- 자동화는 명시적 Allow 옵션 없이는 위험한 단계를 실행하지 않는다.
-- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
-- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-- build/check 실패 시 자동 커밋하지 않는다.
-- git reset, git clean, npm install은 자동 실행하지 않는다.
-- src 코드는 각 구현 task에서만 수정한다.
+# 목표
+업무 검색창 placeholder 문구를 더 명확한 한국어 문구로 개선한다.
 
-## Out of Scope
+## 배경
+현재 업무 검색창의 안내 문구가 사용자가 무엇을 검색할 수 있는지 충분히 분명하지 않을 수 있다.
 
-- GPT API 직접 호출
-- Cline 사용
-- Copilot CLI 사용
-- gh 또는 GitHub PR 자동 연동
-- git push 자동화
-- git reset/git clean 같은 destructive git 명령
-- npm install 자동 실행
-- package 대량 교체
-- DB 삭제, 초기화, 복원, 마이그레이션 자동화
+## 성공 기준
+- 업무 검색창 placeholder가 자연스럽고 명확한 한국어 문구로 변경된다.
+- 변경 범위는 placeholder 문구 수정에 한정된다.
+- 기존 검색 동작과 상태 관리 방식은 유지된다.
 
-## Manual Verification
+## 제약사항
+- 기존 코드 스타일과 컴포넌트 구조를 따른다.
+- 사용자-facing UI 문자열은 한국어를 사용한다.
+- 불필요한 리팩터링이나 구조 변경은 하지 않는다.
+- 한 번에 하나의 작은 변경만 수행한다.
 
-- Codex CLI 실행 정책이 문서화되어 있다.
-- Codex 구현 실행 스크립트가 DryRun과 dirty worktree 중단을 지원한다.
-- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
-- full auto-cycle DryRun이 전체 단계를 보여준다.
-- full auto-cycle은 MaxTasks 1 제한과 Allow 옵션을 가진다.
-- pass 리뷰가 아니면 자동 커밋하지 않는다.
-- build 실패 시 자동 커밋하지 않는다.
-- package 파일 변경 감지 시 자동 커밋하지 않는다.
-- PowerShell 문법 검증이 통과한다.
-- 작은 task로 MaxTasks 1 end-to-end 검증이 가능하다.
+## 범위 제외
+- 검색 기능의 동작 변경은 제외한다.
+- 새 화면, 새 상태, 새 저장 구조 추가는 제외한다.
+- 디자인 전반 수정은 제외한다.
 
+## 수동 검증
+- 업무 화면의 검색창에 더 명확한 placeholder 문구가 표시되는지 확인한다.
+- 검색어 입력과 기존 필터링 동작이 그대로 유지되는지 확인한다.
 
 ## Current Task
 
-- Task ID: T007
-- Title: 목표 입력 기반 자동 goal 실행 스크립트 추가
-- Description: 사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/queue/state를 생성하고, current
--task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다.
+- Task ID: T001
+- Title: 업무 검색창 placeholder 문구 수정
+- Description: 업무 검색창 입력 요소를 찾아 placeholder 문구만 더 명확한 한국어 표현으로 변경한다.
 - Type: implementation
 - Status: in_progress
-- Priority: P0
+- Priority: P1
 - Depends on:
-- T005
+- 없음
 - Verification:
-- DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인
-- Json 출력이 가능한지 확인
-- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-- auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인
-- GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인
-- PowerShell 문법 검증
+- 업무 검색창 placeholder 문구가 변경되었는지 확인한다.
+- 검색 입력 동작과 기존 필터링 동작이 유지되는지 확인한다.
 
 ## Test Result
 
 # AI Dev Test Result
 
-## 2026-06-09 23:00:40
+## 2026-06-09 23:43:52
 
 - Overall result: passed
-- Current task: T007
+- Current task: T001
 - Commands:
   - npm run build: passed
   - npm run test: skipped
@@ -166,11 +120,11 @@ Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완
 transforming...✓ 48 modules transformed.
 rendering chunks...
 computing gzip size...
-dist/index.html                   0.46 kB │ gzip:  0.29 kB
+dist/index.html                   0.46 kB │ gzip:  0.30 kB
 dist/assets/index-DvjxWt30.css    5.69 kB │ gzip:  1.93 kB
-dist/assets/index-C8DmBEYa.js   315.70 kB │ gzip: 99.56 kB
+dist/assets/index-B7f1yjPK.js   315.72 kB │ gzip: 99.57 kB
 
-[32m✓ built in 264ms[39m
+[32m✓ built in 253ms[39m
 ```
 ### npm run test
 
@@ -195,34145 +149,7325 @@ dist/assets/index-C8DmBEYa.js   315.70 kB │ gzip: 99.56 kB
 
 ## Generated At
 
-2026-06-09 23:00:51
+2026-06-09 23:43:59
 
 ## Git Status
 
 ```text
- M .ai-dev/README.md
  M .ai-dev/codex-result.md
- M .ai-dev/codex-review-result.md
  M .ai-dev/current-task-prompt.md
- M .ai-dev/diff.md
- M .ai-dev/review-prompt.md
- M .ai-dev/review-response.json
- M .ai-dev/review.md
+ M .ai-dev/goal.md
+ M .ai-dev/queue.json
  M .ai-dev/state.json
  M .ai-dev/test-result.md
-?? scripts/ai-dev-auto-goal.ps1
+ M src/views/TasksView.tsx
+?? .ai-dev/auto-goal-codex-result.md
+?? .ai-dev/auto-goal-planning-prompt.md
 ```
 
 ## Unstaged Diff Stat
 
 ```text
- .ai-dev/README.md              |    40 +-
- .ai-dev/codex-result.md        | 13475 +++++++-------------
- .ai-dev/codex-review-result.md | 25749 ++++++++++++++++++++++++++++++++++++++-
- .ai-dev/state.json             |     8 +-
- 4 files changed, 30208 insertions(+), 9064 deletions(-)
+ .ai-dev/codex-result.md | 5955 +++++------------------------------------------
+ .ai-dev/goal.md         |   88 +-
+ .ai-dev/queue.json      |  180 +-
+ .ai-dev/state.json      |   16 +-
+ src/views/TasksView.tsx |    2 +-
+ 5 files changed, 584 insertions(+), 5657 deletions(-)
 ```
 
 ## Unstaged Diff
 
 ```text
-diff --git a/.ai-dev/README.md b/.ai-dev/README.md
-index 6f70632..8bf4b1a 100644
---- a/.ai-dev/README.md
-+++ b/.ai-dev/README.md
-@@ -751,28 +751,52 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -
-All
- 
- 최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 하는 것이다.
- 
--예상 최종 명령:
-+먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
- 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-+```
-+
-+자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-+DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 파일 작성 전에 생성될 goal/task 계
-획을 검토할 수 있다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
- 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-+```
-+
-+실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이 모드는 `ai-dev-auto-cycle-
-full.ps1`을 호출하지 않는다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
- 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-+```
-+
-+생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommi
-t` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다. 실제 
-구현까지 허용하려면 `-AllowCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하
-게 `-AllowCodex -AllowReviewCodex -AllowCommit`을 함께 지정한다.
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
- 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -Allo
-wCommit -MaxTasks 1
- ```
- 
--예정 동작:
-+동작:
- 
- 1. `GoalTitle`과 `GoalDescription`을 검증한다.
- 2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
--3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
--4. `.ai-dev/current-task-prompt.md`를 생성한다.
--5. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-+3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-+4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다.
-+5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
-+6. `.ai-dev/current-task-prompt.md`를 생성한다.
-+7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-+8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-task-prompt.md`, 결과 파일 때
-문에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트는 au
-to-goal 시작 전에 별도로 유지한다.
- 
- 안전 기준:
- 
- - `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
--- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시한다.
-+- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
- - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
--- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
--- full cycle 호출은 `-AllowRun` 또는 그에 준하는 명시적 실행 옵션이 있을 때만 수행한다.
-+- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 않는다.
-+- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommit` 중 하나가 있을 때만 수행한다.
-+- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCodex`, `-AllowReviewCodex`
-, `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
- - package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커밋하지 않는다.
-+- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
- 
- ## 운영 원칙
- 
 diff --git a/.ai-dev/codex-result.md b/.ai-dev/codex-result.md
-index 22a164a..5566d61 100644
+index 5566d61..bbed0c8 100644
 --- a/.ai-dev/codex-result.md
 +++ b/.ai-dev/codex-result.md
-@@ -2,11 +2,11 @@
+@@ -2,8 +2,8 @@
  
  ## Run
  
--- Started at: 2026-06-07 22:58:54
--- Ended at: 2026-06-07 23:04:11
-+- Started at: 2026-06-09 22:57:17
-+- Ended at: 2026-06-09 23:00:32
+-- Started at: 2026-06-09 22:57:17
+-- Ended at: 2026-06-09 23:00:32
++- Started at: 2026-06-09 23:42:36
++- Ended at: 2026-06-09 23:43:47
  - Exit code: 0
  - Prompt: .ai-dev/current-task-prompt.md
--- Command: codex exec <content from .ai-dev/current-task-prompt.md plus safety rules>
-+- Command: codex exec <short wrapper pointing to .ai-dev/current-task-prompt.md>
- 
- ## Output
- 
-@@ -26,9 +26,17 @@ approval: never
+ - Command: codex exec <short wrapper pointing to .ai-dev/current-task-prompt.md>
+@@ -26,17 +26,17 @@ approval: never
  sandbox: workspace-write [workdir, /tmp, $TMPDIR]
  reasoning effort: medium
  reasoning summaries: none
--session id: 019ea261-2436-7163-86a0-ea5663975656
-+session id: 019eacac-5c99-7953-8b94-55b60b2b4afb
+-session id: 019eacac-5c99-7953-8b94-55b60b2b4afb
++session id: 019eacd5-daa5-7070-b65b-9c3391932b86
  --------
  user
-+Read and follow the full task prompt at this absolute file path: D:\ai-apps\planpilot-local\.ai-de
+ Read and follow the full task prompt at this absolute file path: D:\ai-apps\planpilot-local\.ai-de
 v
-+\current-task-prompt.md
-+codex
-+지정된 작업 프롬프트를 먼저 읽고, 그 안의 범위와 제약을 확인한 뒤 필요한 파일만 추가로 살펴보겠습니다.
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
+ \current-task-prompt.md
+ codex
+-지정된 작업 프롬프트를 먼저 읽고, 그 안의 범위와 제약을 확인한 뒤 필요한 파일만 추가로 살펴보겠습니다.
++작업 지시 파일을 먼저 읽어서 범위와 제약을 확인하겠습니다. 지정된 파일만 읽고, 그 내용에 맞춰 다음 단계를 진행하겠습니다.
+ exec
+ "C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
 h
-+ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\current-task-prompt.md'" in D:\ai-apps\planpilot-local
-+ succeeded in 1677ms:
+  'D:\\ai-apps\\planpilot-local\\.ai-dev\\current-task-prompt.md'" in D:\ai-apps\planpilot-local
+- succeeded in 1677ms:
++ succeeded in 1161ms:
  # Current Task Prompt
  
  ## Role
-@@ -45,11 +53,11 @@ AI Dev Loop Codex CLI 완전 자동화 도입
+@@ -45,2799 +45,42 @@ exec
  
- Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완료 처리를 가능한 한 end-to-end로 자동화한다.
+ ## Goal
  
--`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 
--생성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
-+`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 
-생
-+성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
+-# Goal
++# 목표
++업무 검색창 placeholder 문구를 더 명확한 한국어 문구로 개선한다.
  
--초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commit
-- 금지 정책을 적용한다.
-+초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commit
+-AI Dev Loop Codex CLI 완전 자동화 도입
++## 배경
++현재 업무 검색창의 안내 문구가 사용자가 무엇을 검색할 수 있는지 충분히 분명하지 않을 수 있다.
  
-+금지 정책을 적용한다.
+-## Background
++## 성공 기준
++- 업무 검색창 placeholder가 자연스럽고 명확한 한국어 문구로 변경된다.
++- 변경 범위는 placeholder 문구 수정에 한정된다.
++- 기존 검색 동작과 상태 관리 방식은 유지된다.
  
- ## Success Criteria
+-Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완료 처리를 가능한 한 end-to-end로 자동화한다.
++## 제약사항
++- 기존 코드 스타일과 컴포넌트 구조를 따른다.
++- 사용자-facing UI 문자열은 한국어를 사용한다.
++- 불필요한 리팩터링이나 구조 변경은 하지 않는다.
++- 한 번에 하나의 작은 변경만 수행한다.
  
-@@ -106,14 +114,15 @@ Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완
+-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 
+생
+-성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
++## 범위 제외
++- 검색 기능의 동작 변경은 제외한다.
++- 새 화면, 새 상태, 새 저장 구조 추가는 제외한다.
++- 디자인 전반 수정은 제외한다.
  
- ## Current Task
+-초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commit
  
--- Task ID: T005
--- Title: 자동 커밋과 task 완료 연결
--- Description: 리뷰 결과가 pass일 때만 선택 파일 커밋과 complete-task를 수행하도록 full auto-cycle에 연결한다.
-+- Task ID: T007
-+- Title: 목표 입력 기반 자동 goal 실행 스크립트 추가
-+- Description: 사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/queue/state를 생성하고, curren
+-금지 정책을 적용한다.
+-
+-## Success Criteria
+-
+-- Codex CLI 실행 정책이 문서화된다.
+-- `current-task-prompt.md`를 Codex CLI에 전달하는 `ai-dev-run-codex.ps1`이 추가된다.
+-- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-review-codex.ps1`이 추가된다.
+-- Codex 구현, build/check, diff, Codex 리뷰, save-review 흐름이 연결된다.
+-- pass 리뷰에서만 자동 커밋과 complete-task를 수행하는 full cycle 초안이 추가된다.
+-- git status가 dirty이면 Codex 실행을 중단한다.
+-- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
+-- build 실패 시 자동 커밋하지 않는다.
+-- git reset, git clean, npm install은 자동 실행하지 않는다.
+-- PowerShell 문법 검증이 통과한다.
+-- 작은 앱 기능 task로 MaxTasks 1 end-to-end 검증이 가능하다.
+-
+-## Constraints
+-
+-- Codex CLI는 구현자와 리뷰어로 사용한다.
+-- GPT API 키 없이 진행한다.
+-- Cline은 삭제되어 사용하지 않는다.
+-- Copilot CLI와 gh는 현재 사용하지 않는다.
+-- 자동화는 명시적 Allow 옵션 없이는 위험한 단계를 실행하지 않는다.
+-- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
+-- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
+-- build/check 실패 시 자동 커밋하지 않는다.
+-- git reset, git clean, npm install은 자동 실행하지 않는다.
+-- src 코드는 각 구현 task에서만 수정한다.
+-
+-## Out of Scope
+-
+-- GPT API 직접 호출
+-- Cline 사용
+-- Copilot CLI 사용
+-- gh 또는 GitHub PR 자동 연동
+-- git push 자동화
+-- git reset/git clean 같은 destructive git 명령
+-- npm install 자동 실행
+-- package 대량 교체
+-- DB 삭제, 초기화, 복원, 마이그레이션 자동화
+-
+-## Manual Verification
+-
+-- Codex CLI 실행 정책이 문서화되어 있다.
+-- Codex 구현 실행 스크립트가 DryRun과 dirty worktree 중단을 지원한다.
+-- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
+-- full auto-cycle DryRun이 전체 단계를 보여준다.
+-- full auto-cycle은 MaxTasks 1 제한과 Allow 옵션을 가진다.
+-- pass 리뷰가 아니면 자동 커밋하지 않는다.
+-- build 실패 시 자동 커밋하지 않는다.
+-- package 파일 변경 감지 시 자동 커밋하지 않는다.
+-- PowerShell 문법 검증이 통과한다.
+-- 작은 task로 MaxTasks 1 end-to-end 검증이 가능하다.
+-
+-
+-## Current Task
+-
+-- Task ID: T007
+-- Title: 목표 입력 기반 자동 goal 실행 스크립트 추가
+-- Description: 사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/queue/state를 생성하고, curren
 t
-+-task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다.
- - Type: implementation
- - Status: in_progress
- - Priority: P0
- - Depends on:
--- T004
-+- T005
- 
- ## Task Scope
- 
-@@ -124,15 +133,16 @@ Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완
- 
- ## Likely Files
- 
--- scripts/ai-dev-auto-cycle-full.ps1
-+- scripts/ai-dev-auto-goal.ps1
- - .ai-dev/README.md
- 
- ## Verification
- 
--- pass가 아니면 commit하지 않는지 확인
--- build 실패 시 commit하지 않는지 확인
--- package 파일 변경 감지 시 중단하는지 확인
--- complete-task가 pass 이후에만 실행되는지 확인
-+- DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인
-+- Json 출력이 가능한지 확인
-+- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-+- auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인
-+- GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인
- - PowerShell 문법 검증
- 
- - 필요한 경우 `npm run build`는 사람이 별도로 실행한다.
-@@ -167,557 +177,175 @@ Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완
- - 데이터 삭제 또는 마이그레이션이 필요한 경우
- - 같은 오류가 반복되는 경우
- 
--## Codex CLI Additional Safety Rules
--
+--task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다.
+-- Type: implementation
+-- Status: in_progress
+-- Priority: P0
+-- Depends on:
+-- T005
+-
+-## Task Scope
+-
+-- 현재 task만 수행한다.
+-- 현재 task 범위를 벗어나는 리팩터링을 하지 않는다.
+-- 다음 task를 미리 구현하지 않는다.
+-- 현재 task의 완료 기준과 검증 방법을 먼저 확인한다.
+-
+-## Likely Files
+-
+-- scripts/ai-dev-auto-goal.ps1
+-- .ai-dev/README.md
+-
+-## Verification
+-
+-- DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인
+-- Json 출력이 가능한지 확인
+-- goal/queue/state JSON 파싱 검증을 수행하는지 확인
+-- auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인
+-- GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인
+-- PowerShell 문법 검증
+-
+-- 필요한 경우 `npm run build`는 사람이 별도로 실행한다.
+-- 이 프롬프트는 자동으로 build, test, lint를 실행하라고 지시하지 않는다.
+-
+-## Required Output
+-
+-- 변경한 파일 목록
+-- 구현 또는 분석 내용 요약
+-- 검증 방법
+-- 남은 위험
+-- 다음 task로 넘어가도 되는지 여부
+-- `.ai-dev/loop-log.md`에 기록할 작업 요약
+-
+-## Hard Rules
+-
+-- `src` 외 파일을 수정해야 하는 경우 이유를 기록한다.
+-- `package.json`과 `package-lock.json`은 수정하지 않는다. 꼭 필요하면 중단하고 이유만 기록한다.
+-- 실제 DB 삭제 또는 초기화를 하지 않는다.
+-- 사용자 데이터 복원 또는 덮어쓰기를 하지 않는다.
+-- 현재 task와 무관한 UI 전면 개편을 하지 않는다.
+-- 대규모 리팩터링을 하지 않는다.
 -- git commit을 실행하지 않는다.
 -- git reset, git checkout, git clean을 실행하지 않는다.
 -- npm install을 실행하지 않는다.
--- package.json 또는 package-lock.json을 수정하지 않는다. 꼭 필요하면 작업을 중단하고 이유만 기록한다.
--- 현재 task 범위 밖 작업을 하지 않는다.
--- build, test, lint는 이 프롬프트가 명시적으로 요청하지 않는 한 실행하지 않는다.
- codex
--작업 범위가 `scripts/ai-dev-auto-cycle-full.ps1`와 `.ai-dev/README.md`로 보입니다. 먼저 현재 스크립트와 문서의 구조를 읽고, T0
--05 완료 조건에 이미 맞춰진 부분과 빠진 연결 지점만 확인하겠습니다.
-+작업 범위는 `scripts/ai-dev-auto-goal.ps1` 추가와 `.ai-dev/README.md` 갱신입니다. 기존 자동화 스크립트의 옵션 이름과 JSON 파일 형
+-
+-## Stop Conditions
+-
+-- 요구사항이 충돌하는 경우
+-- 현재 task 범위 밖 수정이 필요한 경우
+-- 패키지 추가가 필요한 경우
+-- 데이터 삭제 또는 마이그레이션이 필요한 경우
+-- 같은 오류가 반복되는 경우
+-
+-codex
+-작업 범위는 `scripts/ai-dev-auto-goal.ps1` 추가와 `.ai-dev/README.md` 갱신입니다. 기존 자동화 스크립트의 옵션 이름과 JSON 파일 형
 식
-+을 맞춰야 하므로 관련 스크립트와 문서만 확인하겠습니다.
- exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path scrip
--ts/ai-dev-auto-cycle-full.ps1' in D:\ai-apps\planpilot-local
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
+-을 맞춰야 하므로 관련 스크립트와 문서만 확인하겠습니다.
+-exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
 h
-+ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md'" in D:\ai-apps\planpilot-local
- exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path .ai-d
--ev/README.md' in D:\ai-apps\planpilot-local
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -LiteralP
+- 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md'" in D:\ai-apps\planpilot-local
+-exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -LiteralP
 a
-+th 'D:\\ai-apps\\planpilot-local\\scripts' | Select-Object -ExpandProperty Name" in D:\ai-apps\pla
+-th 'D:\\ai-apps\\planpilot-local\\scripts' | Select-Object -ExpandProperty Name" in D:\ai-apps\pla
 n
-+pilot-local
- exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Path scr
--ipts -Filter *.ps1' in D:\ai-apps\planpilot-local
-- succeeded in 3593ms:
--param(
--    [int]$MaxTasks = 1,
--    [int]$MaxSteps = 20,
--    [switch]$DryRun,
--    [switch]$Json,
--    [switch]$AllowCodex,
--    [switch]$AllowReviewCodex,
--    [switch]$AllowCommit,
--    [switch]$AllowDirty
--)
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -LiteralP
+-pilot-local
+-exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -LiteralP
 a
-+th 'D:\\ai-apps\\planpilot-local\\.ai-dev' | Select-Object -ExpandProperty Name" in D:\ai-apps\pla
+-th 'D:\\ai-apps\\planpilot-local\\.ai-dev' | Select-Object -ExpandProperty Name" in D:\ai-apps\pla
 n
-+pilot-local
-+ succeeded in 2705ms:
-+ai-dev-auto-cycle-full.ps1
-+ai-dev-auto-cycle.ps1
-+ai-dev-auto-goal.ps1
-+ai-dev-auto-step.ps1
-+ai-dev-check.ps1
-+ai-dev-commit.ps1
-+ai-dev-complete-task.ps1
-+ai-dev-copy-review-prompt.ps1
-+ai-dev-current-task.ps1
-+ai-dev-env.ps1
-+ai-dev-init.ps1
-+ai-dev-make-prompt.ps1
-+ai-dev-make-review-prompt.ps1
-+ai-dev-make-revise-prompt.ps1
-+ai-dev-manual-cycle.ps1
-+ai-dev-next.ps1
-+ai-dev-run-codex.ps1
-+ai-dev-run-review-codex.ps1
-+ai-dev-save-diff.ps1
-+ai-dev-save-review.ps1
-+ai-dev-status.ps1
-+monitor-and-dev-loop.ps1
-+
-+ succeeded in 2803ms:
-+# PlanPilot Local AI 개발 입력 폴더
- 
--. $PSScriptRoot\ai-dev-env.ps1
-+이 폴더는 PlanPilot Local의 Level 5 AI 자동 개발 루프에서 목표와 후속 작업 후보를 관리하기 위한 기본 공간이다.
- 
--$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
--$queueRelativePath = ".ai-dev/queue.json"
--$stateRelativePath = ".ai-dev/state.json"
--$queuePath = Join-Path $repoRoot $queueRelativePath
--$statePath = Join-Path $repoRoot $stateRelativePath
-+현재 단계에서는 사용자 입력 템플릿과 수동 AI Dev 루프 보조 스크립트를 제공한다. Codex CLI 완전 자동화 실행기는 다음 목표에서 단계적으로 도입하며, 각 task 
+-pilot-local
+- succeeded in 2705ms:
+-ai-dev-auto-cycle-full.ps1
+-ai-dev-auto-cycle.ps1
+-ai-dev-auto-goal.ps1
+-ai-dev-auto-step.ps1
+-ai-dev-check.ps1
+-ai-dev-commit.ps1
+-ai-dev-complete-task.ps1
+-ai-dev-copy-review-prompt.ps1
+-ai-dev-current-task.ps1
+-ai-dev-env.ps1
+-ai-dev-init.ps1
+-ai-dev-make-prompt.ps1
+-ai-dev-make-review-prompt.ps1
+-ai-dev-make-revise-prompt.ps1
+-ai-dev-manual-cycle.ps1
+-ai-dev-next.ps1
+-ai-dev-run-codex.ps1
+-ai-dev-run-review-codex.ps1
+-ai-dev-save-diff.ps1
+-ai-dev-save-review.ps1
+-ai-dev-status.ps1
+-monitor-and-dev-loop.ps1
+-
+- succeeded in 2803ms:
+-# PlanPilot Local AI 개발 입력 폴더
+-
+-이 폴더는 PlanPilot Local의 Level 5 AI 자동 개발 루프에서 목표와 후속 작업 후보를 관리하기 위한 기본 공간이다.
+-
+-현재 단계에서는 사용자 입력 템플릿과 수동 AI Dev 루프 보조 스크립트를 제공한다. Codex CLI 완전 자동화 실행기는 다음 목표에서 단계적으로 도입하며, 각 task 
 전
-+환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
- 
--function Test-HasValue {
--    param(
--        [object]$Value
--    )
-+## 사람이 직접 관리하는 파일
- 
--    if ($null -eq $Value) {
--        return $false
--    }
-+### `goal.md`
- 
--    if ($Value -is [string]) {
--        return -not [string]::IsNullOrWhiteSpace($Value)
--    }
-+현재 자동 개발 루프가 수행할 하나의 목표를 작성한다.
- 
--    return $true
--}
-+- 목표는 결과 중심으로 작성한다.
-+- 완료 기준, 제약 조건, 범위 밖 항목, 수동 검증 방법을 함께 적는다.
-+- 한 번에 하나의 명확한 목표만 유지한다.
-+- 저장소의 `AGENTS.md`와 `docs/ai-dev-loop-policy.md`를 위반하는 목표는 실행하지 않는다.
- 
--function Read-JsonFile {
--    param(
--        [string]$Path,
--        [string]$RelativePath
--    )
-+### `backlog.md`
+-환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
+-
+-## 사람이 직접 관리하는 파일
+-
+-### `goal.md`
+-
+-현재 자동 개발 루프가 수행할 하나의 목표를 작성한다.
+-
+-- 목표는 결과 중심으로 작성한다.
+-- 완료 기준, 제약 조건, 범위 밖 항목, 수동 검증 방법을 함께 적는다.
+-- 한 번에 하나의 명확한 목표만 유지한다.
+-- 저장소의 `AGENTS.md`와 `docs/ai-dev-loop-policy.md`를 위반하는 목표는 실행하지 않는다.
+-
+-### `backlog.md`
+-
+-추후 검토할 목표 후보와 아이디어를 우선순위별로 기록한다.
+-
+-- backlog 항목은 자동 실행이 확정된 task가 아니다.
+-- 현재 단계에서는 AI가 backlog에서 다음 목표를 자동 선택하지 않는다.
+-- 실행할 항목은 사용자가 검토한 뒤 `goal.md`로 옮긴다.
+-
+-## AI가 추후 생성하거나 수정할 파일
+-
+-실제 자동 개발 루프를 구현하는 단계에서는 AI가 다음 파일을 생성하거나 갱신할 수 있다.
+-
+-- `plan.md`
+-- `queue.json`
+-- `state.json`
+-- `test-result.md`
+-- `review.md`
+-- `loop-log.md`
+-
+-이 파일들은 목표 분석, task queue, 실행 상태, 검증 결과, 리뷰 결과, 실행 이력을 기록하기 위한 파일이다.
+-
+-현재 단계에서는 위 파일을 만들지 않는다.
+-
+-## 구조 예시 파일
+-
+-- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시다.
+-- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시다.
+-
+-예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복사 원본으로 사용한다.
+-
+-## UTF-8 콘솔 설정
+-
+-AI Dev PowerShell 스크립트의 UTF-8 콘솔 설정은 `scripts/ai-dev-env.ps1`에서 자동 처리한다. 사용자가 매번 `chcp 65001`을 직접 
+실
+-행할 필요는 없다.
+-
+-기존에 한글이 깨진 `.ai-dev/*.md` 상태 파일은 필요한 경우 다음 명령으로 재초기화할 수 있다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-init.ps1 -Force
+-```
+-
+-## 실행 파일 초기화
+-
+-프로젝트 루트에서 다음 명령을 실행하면 실제 실행용 상태 파일과 기본 Markdown 템플릿을 생성한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1
+-```
+-
+-이미 생성된 파일을 덮어쓰려면 `-Force` 옵션을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1 -Force
+-```
+-
+-초기화 스크립트는 Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
+-
+-## 현재 task 확인
+-
+-`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1
+-```
+-
+-현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1 -Json
+-```
+-
+-현재 task 확인 스크립트는 상태 파일을 수정하지 않으며, Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
+-
+-## 현재 task 프롬프트 생성
+-
+-`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai-dev/current-task-prompt.md` 파일을
  
--    try {
--        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
--    } catch {
--        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
--    }
--}
-+추후 검토할 목표 후보와 아이디어를 우선순위별로 기록한다.
+-생성한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
+-```
+-
+-프롬프트 생성 스크립트는 기존 `current-task-prompt.md`를 현재 task 기준으로 덮어쓴다. Codex 실행, GPT 리뷰, build/test 실행, git
  
--function Get-CurrentTask {
--    param(
--        [object]$Queue,
--        [object]$State
--    )
-+- backlog 항목은 자동 실행이 확정된 task가 아니다.
-+- 현재 단계에서는 AI가 backlog에서 다음 목표를 자동 선택하지 않는다.
-+- 실행할 항목은 사용자가 검토한 뒤 `goal.md`로 옮긴다.
+-commit은 수행하지 않는다.
+-
+-기본 추가 지시 파일인 `.ai-dev/extra-instructions.md`가 있으면 해당 내용을 프롬프트 끝의 `Extra Instructions` 섹션에 포함한다. 이 
+파
+-일이 없으면 기존과 동일하게 goal, queue, state만 사용해 프롬프트를 생성한다.
+-
+-다른 추가 지시 파일을 사용하려면 `-ExtraInstructionsPath`를 지정한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1 -ExtraInstructionsPath .ai
+-
+-dev/extra-instructions.example.md
+-```
+-
+-추가 지시는 goal, queue, state 내용을 덮어쓰지 않는다. `current-task-prompt.md`를 직접 수정하지 말고, 반복해서 필요한 보조 지시는 `.ai
+-
+-dev/extra-instructions.md` 또는 별도 파일에 작성한다.
+-
+-## task 완료 처리
+-
+-현재 task를 완료 처리하고 다음 pending task로 이동하려면 다음 명령을 실행한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary "T001 분석 
+완
+-료"
+-```
+-
+-특정 task를 완료 처리하려면 `-TaskId`를 지정한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -TaskId T001 -ResultSumm
+a
+-ry "분석 완료"
+-```
+-
+-다음 task로 자동 이동하지 않으려면 `-NoNext` 옵션을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -NoNext
+-```
+-
+-task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행,
  
--    $tasks = @($Queue.tasks)
--    $currentTaskId = $null
-+## AI가 추후 생성하거나 수정할 파일
- 
--    if (Test-HasValue $State.currentTaskId) {
--        $currentTaskId = [string]$State.currentTaskId
--    } elseif (Test-HasValue $Queue.currentTaskId) {
--        $currentTaskId = [string]$Queue.currentTaskId
--    } else {
--        $inProgressTask = $tasks | Where-Object { $_.status -eq "in_progress" } | Select-Object -F
--irst 1
-+실제 자동 개발 루프를 구현하는 단계에서는 AI가 다음 파일을 생성하거나 갱신할 수 있다.
- 
--        if ($null -ne $inProgressTask) {
--            $currentTaskId = [string]$inProgressTask.id
--        } else {
--            $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -Firs
--t 1
-+- `plan.md`
-+- `queue.json`
-+- `state.json`
-+- `test-result.md`
-+- `review.md`
-+- `loop-log.md`
- 
--            if ($null -ne $pendingTask) {
--                $currentTaskId = [string]$pendingTask.id
--            }
--        }
--    }
-+이 파일들은 목표 분석, task queue, 실행 상태, 검증 결과, 리뷰 결과, 실행 이력을 기록하기 위한 파일이다.
- 
--    if (-not (Test-HasValue $currentTaskId)) {
--        return $null
--    }
-+현재 단계에서는 위 파일을 만들지 않는다.
- 
--    return $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
--}
-+## 구조 예시 파일
- 
--function New-StepResult {
--    param(
--        [int]$Step,
--        [string]$Name,
--        [string]$Command,
--        [bool]$Executed,
--        [bool]$Skipped,
--        [int]$ExitCode,
--        [string]$Message
--    )
-+- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시다.
-+- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시다.
- 
--    return [PSCustomObject][ordered]@{
--        step = $Step
--        name = $Name
--        command = $Command
--        executed = $Executed
--        skipped = $Skipped
--        exitCode = $ExitCode
--        message = $Message
--    }
--}
-+예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복사 원본으로 사용한다.
- 
--function New-CycleResult {
--    param(
--        [object[]]$Steps,
--        [string]$StoppedReason,
--        [bool]$Completed,
--        [int]$ExitCode
--    )
-+## UTF-8 콘솔 설정
- 
--    return [PSCustomObject][ordered]@{
--        steps = @($Steps)
--        stoppedReason = $StoppedReason
--        completed = $Completed
--        exitCode = $ExitCode
--    }
--}
-+AI Dev PowerShell 스크립트의 UTF-8 콘솔 설정은 `scripts/ai-dev-env.ps1`에서 자동 처리한다. 사용자가 매번 `chcp 65001`을 직접 
-실
-+행할 필요는 없다.
- 
--function Write-CycleResult {
--    param(
--        [object]$Result
--    )
-+기존에 한글이 깨진 `.ai-dev/*.md` 상태 파일은 필요한 경우 다음 명령으로 재초기화할 수 있다.
- 
--    if ($Json) {
--        $Result | ConvertTo-Json -Depth 20
--        return
--    }
-+```powershell
-+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-init.ps1 -Force
-+```
- 
--    foreach ($step in @($Result.steps)) {
--        Write-Host "Step $($step.step): $($step.name)"
--        Write-Host "  Command: $($step.command)"
--        Write-Host "  Executed: $($step.executed)"
--        Write-Host "  Skipped: $($step.skipped)"
--        Write-Host "  Exit code: $($step.exitCode)"
--        Write-Host "  Message: $($step.message)"
--    }
-+## 실행 파일 초기화
- 
--    Write-Host "Stopped reason: $($Result.stoppedReason)"
--    Write-Host "Completed: $($Result.completed)"
--    Write-Host "Exit code: $($Result.exitCode)"
-+프로젝트 루트에서 다음 명령을 실행하면 실제 실행용 상태 파일과 기본 Markdown 템플릿을 생성한다.
- 
--    if ($AllowCommit) {
--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
--    }
--}
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1
-+```
- 
--function Stop-Cycle {
--    param(
--        [object[]]$Steps,
--        [string]$StoppedReason,
--        [bool]$Completed,
--        [int]$ExitCode
--    )
-+이미 생성된 파일을 덮어쓰려면 `-Force` 옵션을 사용한다.
- 
--    $result = New-CycleResult $Steps $StoppedReason $Completed $ExitCode
--    Write-CycleResult $result
--    exit $ExitCode
--}
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1 -Force
-+```
- 
--function Invoke-CycleCommand {
--    param(
--        [int]$StepNumber,
--        [string]$Name,
--        [string]$Command,
--        [string]$ScriptPath,
--        [string[]]$Arguments
--    )
-+초기화 스크립트는 Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
- 
--    if ($DryRun) {
--        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: 하위 스크립트
--를 실행하지 않았습니다."
--        return
--    }
-+## 현재 task 확인
- 
--    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
--    $exitCode = $LASTEXITCODE
--    $message = $output.Trim()
-+`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
- 
--    if ([string]::IsNullOrWhiteSpace($message)) {
--        $message = "완료"
--    }
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1
-+```
- 
--    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-+현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
- 
--    if ($exitCode -ne 0) {
--        Stop-Cycle $script:steps "$Name`_failed" $false 1
--    }
--}
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1 -Json
-+```
- 
--function Get-ScriptPath {
--    param(
--        [string]$Name
--    )
-+현재 task 확인 스크립트는 상태 파일을 수정하지 않으며, Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
- 
--    $scriptPath = Join-Path $PSScriptRoot $Name
-+## 현재 task 프롬프트 생성
- 
--    if (-not (Test-Path -LiteralPath $scriptPath -PathType Leaf)) {
--        throw "필수 스크립트를 찾을 수 없습니다: scripts/$Name"
--    }
-+`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai-dev/current-task-prompt.md` 파일을
- 
-+생성한다.
- 
--    return $scriptPath
--}
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
-+```
- 
--Set-Location $repoRoot
-+프롬프트 생성 스크립트는 기존 `current-task-prompt.md`를 현재 task 기준으로 덮어쓴다. Codex 실행, GPT 리뷰, build/test 실행, git
- 
-+commit은 수행하지 않는다.
- 
--$script:steps = @()
-+기본 추가 지시 파일인 `.ai-dev/extra-instructions.md`가 있으면 해당 내용을 프롬프트 끝의 `Extra Instructions` 섹션에 포함한다. 이 
-파
-+일이 없으면 기존과 동일하게 goal, queue, state만 사용해 프롬프트를 생성한다.
- 
--if ($MaxTasks -lt 1) {
--    Stop-Cycle $script:steps "max_tasks_must_be_at_least_1" $false 1
--}
-+다른 추가 지시 파일을 사용하려면 `-ExtraInstructionsPath`를 지정한다.
- 
--if ($MaxSteps -lt 1) {
--    Stop-Cycle $script:steps "max_steps_must_be_at_least_1" $false 1
--}
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1 -ExtraInstructionsPath .ai
+-git commit은 수행하지 않는다.
 -
-+dev/extra-instructions.example.md
-+```
- 
--try {
--    foreach ($requiredPath in @($queuePath, $statePath)) {
--        if (-not (Test-Path -LiteralPath $requiredPath -PathType Leaf)) {
--            throw "필수 상태 파일이 없습니다: $requiredPath"
--        }
--    }
-+추가 지시는 goal, queue, state 내용을 덮어쓰지 않는다. `current-task-prompt.md`를 직접 수정하지 말고, 반복해서 필요한 보조 지시는 `.ai
+-## 프로젝트 검증 실행
 -
-+dev/extra-instructions.md` 또는 별도 파일에 작성한다.
- 
--    $queue = Read-JsonFile $queuePath $queueRelativePath
--    $state = Read-JsonFile $statePath $stateRelativePath
-+## task 완료 처리
- 
--    if ($state.goalStatus -eq "completed") {
--        Stop-Cycle $script:steps "goal_completed" $true 0
--    }
-+현재 task를 완료 처리하고 다음 pending task로 이동하려면 다음 명령을 실행한다.
- 
--    if (-not ($queue.PSObject.Properties.Name -contains "tasks") -or $null -eq $queue.tasks) {
--        Stop-Cycle $script:steps "no_task" $false 0
--    }
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary "T001 분석 
-완
-+료"
-+```
- 
--    $currentTask = Get-CurrentTask $queue $state
+-`package.json`에 정의된 build, test, lint script를 확인하고 실행 결과를 `test-result.md`와 `state.json`에 기록한다.
 -
--    if ($null -eq $currentTask) {
--        Stop-Cycle $script:steps "no_task" $false 0
--    }
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1
+-```
 -
--    if ($currentTask.status -eq "done") {
--        Stop-Cycle $script:steps "current_task_done" $true 0
--    }
+-build만 실행하려면 `-BuildOnly` 옵션을 사용한다.
 -
--    $scriptPaths = @{
--        makePrompt = Get-ScriptPath "ai-dev-make-prompt.ps1"
--        runCodex = Get-ScriptPath "ai-dev-run-codex.ps1"
--        check = Get-ScriptPath "ai-dev-check.ps1"
--        saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--        makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--        runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--    }
--} catch {
--    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mess
--age
--    Stop-Cycle $script:steps "prepare_failed" $false 1
--}
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -BuildOnly
+-```
 -
--$plannedSteps = @(
--    "make-prompt",
--    "run-codex",
--    "check",
--    "save-diff",
--    "make-review-prompt",
--    "run-review-codex"
--)
+-필요한 검증을 개별적으로 건너뛰려면 `-SkipBuild`, `-SkipTest`, `-SkipLint` 옵션을 사용한다.
 -
--if ($plannedSteps.Count -gt $MaxSteps) {
--    Stop-Cycle $script:steps "max_steps_too_small_for_full_cycle" $false 1
--}
+-검증 실행 스크립트는 Codex 실행, GPT 리뷰, git commit을 수행하지 않는다.
 -
--if ($MaxTasks -gt 1) {
--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 ta
--sk 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--}
+-T006 같은 최종 검증 task에서 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 
+결
+-과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build/test/lint를 실행하지 않는다.
 -
--$stepNumber = 1
--Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/ai
---dev-make-prompt.ps1" $scriptPaths.makePrompt @()
--$stepNumber++
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -ManualSummaryOnly -ManualSummar
+y
+- "PowerShell 문법 검증 통과. auto-step DryRun 통과. auto-cycle DryRun 통과. save-review 실패/성공 흐름 확인."
+-```
 -
--if (-not $AllowCodex) {
--    $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allow
--Codex -AllowReviewCodex -MaxTasks $MaxTasks"
--    if ($AllowDirty) {
--        $command = "$command -AllowDirty"
--    }
+-수동 요약 모드는 실제 명령 실행 결과를 조작하는 용도가 아니다. 실행하지 않은 검증은 통과로 적지 말고, 수행한 명령과 결과만 요약한다.
 -
--    $script:steps += New-StepResult $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass -F
--ile scripts/ai-dev-run-codex.ps1" $false $true 1 "Codex 구현 실행에는 -AllowCodex가 필요합니다. 추천 명령: $comman
--d"
--    Stop-Cycle $script:steps "allow_codex_required" $false 1
--}
+-이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다. 실제 검증 통과/실패/미수행 판단은 `test-resul
+t
+-.md`의 요약 본문에 명확히 적는다.
 -
--$runCodexArguments = @()
--if ($AllowDirty) {
--    $runCodexArguments += "-AllowDirty"
--}
+-## git diff 저장
 -
--Invoke-CycleCommand $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass -File scripts/ai-d
--ev-run-codex.ps1" $scriptPaths.runCodex $runCodexArguments
--$stepNumber++
+-현재 git 변경사항을 `diff.md`에 저장해 GPT 리뷰 또는 사람 검토에 사용할 수 있게 한다. 기본 동작은 untracked 파일명을 git status에 표시하지만 
+파
+-일 내용은 포함하지 않는다.
 -
--Invoke-CycleCommand $stepNumber "check" "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-c
--heck.ps1 -BuildOnly" $scriptPaths.check @("-BuildOnly")
--$stepNumber++
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1
+-```
 -
--Invoke-CycleCommand $stepNumber "save-diff" "powershell -ExecutionPolicy Bypass -File scripts/ai-d
--ev-save-diff.ps1" $scriptPaths.saveDiff @()
--$stepNumber++
+-추적되지 않은 작은 텍스트 파일의 내용도 포함하려면 `-IncludeUntrackedContent` 옵션을 사용한다.
 -
--Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File scr
--ipts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
--$stepNumber++
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1 -IncludeUntrackedContent
+-```
 -
--if (-not $AllowReviewCodex) {
--    $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allow
--Codex -AllowReviewCodex -MaxTasks $MaxTasks"
--    if ($AllowDirty) {
--        $command = "$command -AllowDirty"
--    }
+-git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행, git commit은 수행하지
+ 
+-않는다.
 -
--    $script:steps += New-StepResult $stepNumber "run-review-codex" "powershell -ExecutionPolicy By
--pass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $false $true 1 "Codex 리뷰 실
--행에는 -AllowReviewCodex가 필요합니다. 추천 명령: $command"
--    Stop-Cycle $script:steps "allow_review_codex_required" $false 1
--}
+-다음 생성 산출물은 자기 중첩과 과도한 크기 증가를 줄이기 위해 Unstaged/Staged Diff와 Diff Stat 대상에서 기본 제외한다. tracked 변경 파일은 `
+S
+-kipped Generated AI Dev Artifact Diffs` 섹션에 파일명만 기록한다. `-IncludeUntrackedContent`를 사용한 경우 untracke
+d
+- 생성 산출물도 내용 포함에서 제외하고 `Skipped Generated AI Dev Artifacts` 섹션에 파일명만 기록한다.
 -
--Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scrip
--ts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirty
--", "-SaveReview")
+-- `.ai-dev/diff.md`
+-- `.ai-dev/review-prompt.md`
+-- `.ai-dev/current-task-prompt.md`
+-- `.ai-dev/revise-prompt.md`
+-- `.ai-dev/test-result.md`
+-- `.ai-dev/review.md`
+-- `.ai-dev/review-response.json`
 -
--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
--if ($AllowCommit) {
--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--}
+-git diff/stat 명령의 stderr 경고는 diff 본문과 분리한다. git 명령이 실패하면 기존처럼 `state.json`에 실패 결과를 기록하고 종료한다.
 -
--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-com
--mit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--
-- succeeded in 3595ms:
--
--
--    디렉터리: D:\ai-apps\planpilot-local\scripts
--
--
--Mode                 LastWriteTime         Length Name                                            
--                     
------                 -------------         ------ ----                                            
--                     
---a----        2026-06-07  오후 10:30           9919 ai-dev-auto-cycle-full.ps1                      
--                     
---a----        2026-06-07   오후 3:14           5451 ai-dev-auto-cycle.ps1                           
--                     
---a----        2026-06-07   오후 3:03           8471 ai-dev-auto-step.ps1                            
--                     
---a----        2026-06-07   오후 4:07           7762 ai-dev-check.ps1                                
--                     
---a----        2026-06-03  오후 11:54          10635 ai-dev-commit.ps1                               
--                     
---a----        2026-06-03  오후 10:14           5440 ai-dev-complete-task.ps1                        
--                     
---a----        2026-06-04   오후 4:24           3805 ai-dev-copy-review-prompt.ps1                   
--                     
---a----        2026-06-03  오후 10:14           5446 ai-dev-current-task.ps1                         
--                     
---a----        2026-06-03  오후 10:14            309 ai-dev-env.ps1                                  
--                     
---a----        2026-06-03  오후 10:14           3085 ai-dev-init.ps1                                 
--                     
---a----        2026-06-04  오전 12:01           7243 ai-dev-make-prompt.ps1                          
--                     
---a----        2026-06-03  오후 10:14           8050 ai-dev-make-review-prompt.ps1                   
--                     
---a----        2026-06-03  오후 10:14          11227 ai-dev-make-revise-prompt.ps1                   
--                     
---a----        2026-06-03  오후 10:14           2692 ai-dev-manual-cycle.ps1                         
--                     
---a----        2026-06-03  오후 10:14          12771 ai-dev-next.ps1                                 
--                     
---a----        2026-06-07   오후 9:55           6510 ai-dev-run-codex.ps1                            
--                     
---a----        2026-06-07  오후 10:19          12076 ai-dev-run-review-codex.ps1                     
--                     
---a----        2026-06-03  오후 11:44          10302 ai-dev-save-diff.ps1                            
--                     
---a----        2026-06-07   오후 3:57          13077 ai-dev-save-review.ps1                          
--                     
---a----        2026-06-03  오후 10:14          10013 ai-dev-status.ps1                               
--                     
---a----        2026-05-24   오후 1:50           9682 monitor-and-dev-loop.ps1                        
--                     
--
--
--
-- succeeded in 3698ms:
--# PlanPilot Local AI 개발 입력 폴더
+-`Git Status` 섹션에는 생성 산출물을 포함한 전체 변경 파일을 그대로 표시한다. `.ai-dev/README.md`, `scripts/ai-dev-save-diff.p
+s
+-1`처럼 실제 작업 대상 파일은 위 제외 목록에 없으므로 diff 본문에 포함된다.
 -
--이 폴더는 PlanPilot Local의 Level 5 AI 자동 개발 루프에서 목표와 후속 작업 후보를 관리하기 위한 기본 공간이다.
+-## GPT 리뷰 프롬프트 생성
 -
--현재 단계에서는 사용자 입력 템플릿과 수동 AI Dev 루프 보조 스크립트를 제공한다. Codex CLI 완전 자동화 실행기는 다음 목표에서 단계적으로 도입하며, 각 task 
--전환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
+-현재 goal, task, 검증 결과, git diff를 포함한 GPT 코드 리뷰용 `review-prompt.md`를 생성한다.
 -
--## 사람이 직접 관리하는 파일
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-review-prompt.ps1
+-```
 -
--### `goal.md`
+-더 엄격한 판정 기준을 포함하려면 `-Strict` 옵션을 사용한다.
 -
--현재 자동 개발 루프가 수행할 하나의 목표를 작성한다.
--
--- 목표는 결과 중심으로 작성한다.
--- 완료 기준, 제약 조건, 범위 밖 항목, 수동 검증 방법을 함께 적는다.
--- 한 번에 하나의 명확한 목표만 유지한다.
--- 저장소의 `AGENTS.md`와 `docs/ai-dev-loop-policy.md`를 위반하는 목표는 실행하지 않는다.
--
--### `backlog.md`
--
--추후 검토할 목표 후보와 아이디어를 우선순위별로 기록한다.
--
--- backlog 항목은 자동 실행이 확정된 task가 아니다.
--- 현재 단계에서는 AI가 backlog에서 다음 목표를 자동 선택하지 않는다.
--- 실행할 항목은 사용자가 검토한 뒤 `goal.md`로 옮긴다.
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-review-prompt.ps1 -Strict
+-```
 -
--## AI가 추후 생성하거나 수정할 파일
+-리뷰 프롬프트 생성 스크립트는 GPT API 호출, Codex 실행, build/test 실행, git commit을 수행하지 않는다.
 -
--실제 자동 개발 루프를 구현하는 단계에서는 AI가 다음 파일을 생성하거나 갱신할 수 있다.
+-## 수동 GPT 리뷰 브리지
 -
--- `plan.md`
--- `queue.json`
--- `state.json`
--- `test-result.md`
--- `review.md`
--- `loop-log.md`
+-GPT API 키가 없는 환경에서는 ChatGPT 웹 화면을 사용해 리뷰를 수동으로 진행한다. AI Dev Loop는 prompt 생성과 저장 흐름만 돕고, ChatGPT 웹 
+화
+-면에 자동 접속하거나 GPT API를 직접 호출하지 않는다.
 -
--이 파일들은 목표 분석, task queue, 실행 상태, 검증 결과, 리뷰 결과, 실행 이력을 기록하기 위한 파일이다.
+-기본 흐름:
 -
--현재 단계에서는 위 파일을 만들지 않는다.
+-1. `ai-dev-make-review-prompt.ps1 -Strict`로 `.ai-dev/review-prompt.md`를 생성한다.
+-2. `review-prompt.md` 내용을 클립보드에 복사한다.
+-3. 사용자가 ChatGPT 웹 화면에 직접 붙여넣는다.
+-4. ChatGPT가 JSON 리뷰만 출력하도록 한다.
+-5. 사용자가 JSON 리뷰를 클립보드에 복사한다.
+-6. `ai-dev-save-review.ps1 -FromClipboard`로 리뷰를 저장한다.
+-7. 저장된 decision에 따라 `pass`, `revise`, `blocked` 흐름을 계속 진행한다.
 -
--## 구조 예시 파일
+-현재 task의 다음 단계에서는 `review-prompt.md` 클립보드 복사를 돕는 helper를 추가할 예정이다. 그 전까지는 파일 내용을 직접 복사하거나 운영 환경에 맞
+는
+- 안전한 방법으로 클립보드에 옮긴다.
 -
--- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시다.
--- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시다.
+-`review-prompt.md`가 이미 있으면 다음 명령으로 내용을 클립보드에 복사한다.
 -
--예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복사 원본으로 사용한다.
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
+-```
 -
--## UTF-8 콘솔 설정
+-`review-prompt.md`가 없을 때 생성 후 복사하려면 `-GenerateIfMissing`을 사용한다.
 -
--AI Dev PowerShell 스크립트의 UTF-8 콘솔 설정은 `scripts/ai-dev-env.ps1`에서 자동 처리한다. 사용자가 매번 `chcp 65001`을 직접 
--실행할 필요는 없다.
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -GenerateIfMissing
+-```
 -
--기존에 한글이 깨진 `.ai-dev/*.md` 상태 파일은 필요한 경우 다음 명령으로 재초기화할 수 있다.
+-엄격한 리뷰 프롬프트를 생성한 뒤 복사하려면 `-Strict`를 함께 사용한다.
 -
 -```powershell
--powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-init.ps1 -Force
--```
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -GenerateIfMissing 
 -
--## 실행 파일 초기화
+-Strict
+-```
 -
--프로젝트 루트에서 다음 명령을 실행하면 실제 실행용 상태 파일과 기본 Markdown 템플릿을 생성한다.
+-자동화에서 결과만 읽고 싶으면 `-Json` 옵션을 사용한다.
 -
 -```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -Json
 -```
 -
--이미 생성된 파일을 덮어쓰려면 `-Force` 옵션을 사용한다.
+-복사 후에는 ChatGPT 웹 화면에 붙여넣고, JSON 리뷰만 출력하도록 요청한다. 리뷰 JSON을 받은 뒤에는 아래 명령으로 저장한다.
 -
 -```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1 -Force
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
 -```
 -
--초기화 스크립트는 Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
+-주의:
 -
--## 현재 task 확인
+-- `review-prompt.md`에는 diff와 테스트 결과가 포함될 수 있다.
+-- API Key, 개인정보, 백업 데이터, 민감한 로컬 경로가 포함되어 있으면 ChatGPT에 붙여넣지 않는다.
+-- prompt가 너무 길거나 민감 정보가 의심되면 diff 범위를 줄인 뒤 다시 생성한다.
+-- `ai-dev-auto-step.ps1`는 `ask_gpt_review` 상태에서 GPT API를 호출하지 않고 수동 브리지 안내만 해야 한다.
 -
--`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
+-## GPT 리뷰 결과 저장
+-
+-GPT Chat 또는 외부 리뷰어가 반환한 리뷰 JSON을 `review.md`에 저장하고, decision과 severity를 `state.json`에 반영한다.
 -
 -```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewFile .ai-dev/review
+-
+-response.json
 -```
 -
--현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
+-JSON 문자열이나 클립보드 내용을 직접 사용할 수도 있다.
 -
 -```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1 -Json
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewJson '{"decision":"
+p
+-ass","severity":"none","summary":"OK","required_changes":[],"optional_suggestions":[],"next_step":
+"
+-complete_task"}'
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
 -```
 -
--현재 task 확인 스크립트는 상태 파일을 수정하지 않으며, Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
+-ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboard`를 실행한다. 순수 JSON 객체, `json` 코드
+블
+-록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
 -
--## 현재 task 프롬프트 생성
+-저장 실패 시 확인할 항목:
 -
--`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai-dev/current-task-prompt.md` 파일을
-- 생성한다.
+-- 클립보드가 비어 있지 않은지 확인한다.
+-- ChatGPT 응답에서 JSON 객체 전체를 다시 복사한다.
+-- Markdown 코드블록을 포함해도 되지만, 가능하면 JSON만 복사한다.
+-- 오류 메시지의 입력 preview 앞부분을 보고 다른 설명 문장이나 잘린 JSON이 섞였는지 확인한다.
+-
+-파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수정하지 않는다. review-prompt나 명령어를 실수로
+ 
+-클립보드에 둔 상태에서 `-FromClipboard`를 실행해도 기존 리뷰 상태를 blocked/critical로 덮어쓰지 않기 위한 안전 동작이다.
+-
+-실패 자체를 리뷰 결과로 기록해야 하는 특별한 경우에만 `-WriteFailureReview`를 함께 사용한다.
 -
 -```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard -WriteFailu
+r
+-eReview
 -```
 -
--프롬프트 생성 스크립트는 기존 `current-task-prompt.md`를 현재 task 기준으로 덮어쓴다. Codex 실행, GPT 리뷰, build/test 실행, git
-- commit은 수행하지 않는다.
+-잘못 실행했다면 ChatGPT가 출력한 JSON 리뷰만 다시 클립보드에 복사한 뒤 `-FromClipboard`를 재실행한다.
 -
--기본 추가 지시 파일인 `.ai-dev/extra-instructions.md`가 있으면 해당 내용을 프롬프트 끝의 `Extra Instructions` 섹션에 포함한다. 이 
--파일이 없으면 기존과 동일하게 goal, queue, state만 사용해 프롬프트를 생성한다.
+-리뷰 결과 저장 스크립트는 `pass + complete_task`, `revise + revise_with_codex`, `blocked` 또는 `stop_for_user` 
+결
+-과에 맞는 다음 행동을 안내만 한다. GPT API 호출, Codex 실행, build/test 실행, git commit은 수행하지 않는다.
 -
--다른 추가 지시 파일을 사용하려면 `-ExtraInstructionsPath`를 지정한다.
+-## 리뷰 반영 재수정 프롬프트 생성
+-
+-`review.md`의 required changes를 반영하도록 Codex 또는 Cline에 전달할 `revise-prompt.md`를 생성한다.
 -
 -```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1 -ExtraInstructionsPath .ai
---dev/extra-instructions.example.md
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1
 -```
 -
--추가 지시는 goal, queue, state 내용을 덮어쓰지 않는다. `current-task-prompt.md`를 직접 수정하지 말고, 반복해서 필요한 보조 지시는 `.ai
---dev/extra-instructions.md` 또는 별도 파일에 작성한다.
--
--## task 완료 처리
--
--현재 task를 완료 처리하고 다음 pending task로 이동하려면 다음 명령을 실행한다.
+-optional suggestions도 현재 task 범위 안에서 반영할 수 있게 하려면 `-AllowOptionalSuggestions` 옵션을 사용한다.
 -
 -```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary "T001 분석 
--완료"
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1 -AllowOptionalSugge
+s
+-tions
 -```
 -
--특정 task를 완료 처리하려면 `-TaskId`를 지정한다.
-+특정 task를 완료 처리하려면 `-TaskId`를 지정한다.
- 
- ```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -TaskId T001 -ResultSumm
--ary "분석 완료"
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -TaskId T001 -ResultSumm
-a
-+ry "분석 완료"
- ```
- 
- 다음 task로 자동 이동하지 않으려면 `-NoNext` 옵션을 사용한다.
-@@ -726,8 +354,8 @@ ary "분석 완료"
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -NoNext
- ```
- 
--task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행,
-- git commit은 수행하지 않는다.
-+task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행,
- 
-+git commit은 수행하지 않는다.
- 
- ## 프로젝트 검증 실행
- 
-@@ -747,23 +375,23 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -BuildOnly
- 
- 검증 실행 스크립트는 Codex 실행, GPT 리뷰, git commit을 수행하지 않는다.
- 
--T006 같은 최종 검증 task에서 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 
--결과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build/test/lint를 실행하지 않는다.
-+T006 같은 최종 검증 task에서 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 
-결
-+과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build/test/lint를 실행하지 않는다.
- 
- ```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -ManualSummaryOnly -ManualSummar
--y "PowerShell 문법 검증 통과. auto-step DryRun 통과. auto-cycle DryRun 통과. save-review 실패/성공 흐름 확인."
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -ManualSummaryOnly -ManualSummar
-y
-+ "PowerShell 문법 검증 통과. auto-step DryRun 통과. auto-cycle DryRun 통과. save-review 실패/성공 흐름 확인."
- ```
- 
- 수동 요약 모드는 실제 명령 실행 결과를 조작하는 용도가 아니다. 실행하지 않은 검증은 통과로 적지 말고, 수행한 명령과 결과만 요약한다.
- 
--이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다. 실제 검증 통과/실패/미수행 판단은 `test-resul
--t.md`의 요약 본문에 명확히 적는다.
-+이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다. 실제 검증 통과/실패/미수행 판단은 `test-resul
-t
-+.md`의 요약 본문에 명확히 적는다.
- 
- ## git diff 저장
- 
--현재 git 변경사항을 `diff.md`에 저장해 GPT 리뷰 또는 사람 검토에 사용할 수 있게 한다. 기본 동작은 untracked 파일명을 git status에 표시하지만 
--파일 내용은 포함하지 않는다.
-+현재 git 변경사항을 `diff.md`에 저장해 GPT 리뷰 또는 사람 검토에 사용할 수 있게 한다. 기본 동작은 untracked 파일명을 git status에 표시하지만 
-파
-+일 내용은 포함하지 않는다.
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1
-@@ -775,12 +403,12 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1 -IncludeUntrackedContent
- ```
- 
--git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행, git commit은 수행하지
-- 않는다.
-+git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행, git commit은 수행하지
- 
-+않는다.
- 
--다음 생성 산출물은 자기 중첩과 과도한 크기 증가를 줄이기 위해 Unstaged/Staged Diff와 Diff Stat 대상에서 기본 제외한다. tracked 변경 파일은 `
--Skipped Generated AI Dev Artifact Diffs` 섹션에 파일명만 기록한다. `-IncludeUntrackedContent`를 사용한 경우 untrack
--ed 생성 산출물도 내용 포함에서 제외하고 `Skipped Generated AI Dev Artifacts` 섹션에 파일명만 기록한다.
-+다음 생성 산출물은 자기 중첩과 과도한 크기 증가를 줄이기 위해 Unstaged/Staged Diff와 Diff Stat 대상에서 기본 제외한다. tracked 변경 파일은 `
-S
-+kipped Generated AI Dev Artifact Diffs` 섹션에 파일명만 기록한다. `-IncludeUntrackedContent`를 사용한 경우 untracke
-d
-+ 생성 산출물도 내용 포함에서 제외하고 `Skipped Generated AI Dev Artifacts` 섹션에 파일명만 기록한다.
- 
- - `.ai-dev/diff.md`
- - `.ai-dev/review-prompt.md`
-@@ -792,8 +420,8 @@ ed 생성 산출물도 내용 포함에서 제외하고 `Skipped Generated AI De
- 
- git diff/stat 명령의 stderr 경고는 diff 본문과 분리한다. git 명령이 실패하면 기존처럼 `state.json`에 실패 결과를 기록하고 종료한다.
- 
--`Git Status` 섹션에는 생성 산출물을 포함한 전체 변경 파일을 그대로 표시한다. `.ai-dev/README.md`, `scripts/ai-dev-save-diff.p
--s1`처럼 실제 작업 대상 파일은 위 제외 목록에 없으므로 diff 본문에 포함된다.
-+`Git Status` 섹션에는 생성 산출물을 포함한 전체 변경 파일을 그대로 표시한다. `.ai-dev/README.md`, `scripts/ai-dev-save-diff.p
-s
-+1`처럼 실제 작업 대상 파일은 위 제외 목록에 없으므로 diff 본문에 포함된다.
- 
- ## GPT 리뷰 프롬프트 생성
- 
-@@ -813,8 +441,8 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-review-prompt.ps1 
+-재수정 프롬프트 생성 스크립트는 Codex 실행, GPT API 호출, build/test 실행, git commit을 수행하지 않는다.
 -
- 
- ## 수동 GPT 리뷰 브리지
- 
--GPT API 키가 없는 환경에서는 ChatGPT 웹 화면을 사용해 리뷰를 수동으로 진행한다. AI Dev Loop는 prompt 생성과 저장 흐름만 돕고, ChatGPT 웹 
--화면에 자동 접속하거나 GPT API를 직접 호출하지 않는다.
-+GPT API 키가 없는 환경에서는 ChatGPT 웹 화면을 사용해 리뷰를 수동으로 진행한다. AI Dev Loop는 prompt 생성과 저장 흐름만 돕고, ChatGPT 웹 
-화
-+면에 자동 접속하거나 GPT API를 직접 호출하지 않는다.
- 
- 기본 흐름:
- 
-@@ -826,8 +454,8 @@ GPT API 키가 없는 환경에서는 ChatGPT 웹 화면을 사용해 리뷰를
- 6. `ai-dev-save-review.ps1 -FromClipboard`로 리뷰를 저장한다.
- 7. 저장된 decision에 따라 `pass`, `revise`, `blocked` 흐름을 계속 진행한다.
- 
--현재 task의 다음 단계에서는 `review-prompt.md` 클립보드 복사를 돕는 helper를 추가할 예정이다. 그 전까지는 파일 내용을 직접 복사하거나 운영 환경에 맞
--는 안전한 방법으로 클립보드에 옮긴다.
-+현재 task의 다음 단계에서는 `review-prompt.md` 클립보드 복사를 돕는 helper를 추가할 예정이다. 그 전까지는 파일 내용을 직접 복사하거나 운영 환경에 맞
-는
-+ 안전한 방법으로 클립보드에 옮긴다.
- 
- `review-prompt.md`가 이미 있으면 다음 명령으로 내용을 클립보드에 복사한다.
- 
-@@ -844,8 +472,8 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 
+-## task 단위 자동 커밋
 -
- 엄격한 리뷰 프롬프트를 생성한 뒤 복사하려면 `-Strict`를 함께 사용한다.
- 
- ```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -GenerateIfMissing 
---Strict
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -GenerateIfMissing 
+-검증과 리뷰를 통과한 현재 변경사항을 task 단위로 커밋한다. 먼저 `-DryRun`으로 커밋 대상과 메시지를 확인하는 것을 권장한다.
 -
-+Strict
- ```
- 
- 자동화에서 결과만 읽고 싶으면 `-Json` 옵션을 사용한다.
-@@ -872,21 +500,21 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -From
-Cli
- GPT Chat 또는 외부 리뷰어가 반환한 리뷰 JSON을 `review.md`에 저장하고, decision과 severity를 `state.json`에 반영한다.
- 
- ```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewFile .ai-dev/review
---response.json
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewFile .ai-dev/review
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -DryRun
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1
+-```
 -
-+response.json
- ```
- 
- JSON 문자열이나 클립보드 내용을 직접 사용할 수도 있다.
- 
- ```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewJson '{"decision":"
--pass","severity":"none","summary":"OK","required_changes":[],"optional_suggestions":[],"next_step"
--:"complete_task"}'
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewJson '{"decision":"
-p
-+ass","severity":"none","summary":"OK","required_changes":[],"optional_suggestions":[],"next_step":
-"
-+complete_task"}'
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
- ```
- 
--ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboard`를 실행한다. 순수 JSON 객체, `json` 코드
--블록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-+ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboard`를 실행한다. 순수 JSON 객체, `json` 코드
-블
-+록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
- 
- 저장 실패 시 확인할 항목:
- 
-@@ -895,20 +523,20 @@ ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를
- - Markdown 코드블록을 포함해도 되지만, 가능하면 JSON만 복사한다.
- - 오류 메시지의 입력 preview 앞부분을 보고 다른 설명 문장이나 잘린 JSON이 섞였는지 확인한다.
- 
--파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수정하지 않는다. review-prompt나 명령어를 실수로
-- 클립보드에 둔 상태에서 `-FromClipboard`를 실행해도 기존 리뷰 상태를 blocked/critical로 덮어쓰지 않기 위한 안전 동작이다.
-+파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수정하지 않는다. review-prompt나 명령어를 실수로
- 
-+클립보드에 둔 상태에서 `-FromClipboard`를 실행해도 기존 리뷰 상태를 blocked/critical로 덮어쓰지 않기 위한 안전 동작이다.
- 
- 실패 자체를 리뷰 결과로 기록해야 하는 특별한 경우에만 `-WriteFailureReview`를 함께 사용한다.
- 
- ```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard -WriteFailu
--reReview
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard -WriteFailu
-r
-+eReview
- ```
- 
- 잘못 실행했다면 ChatGPT가 출력한 JSON 리뷰만 다시 클립보드에 복사한 뒤 `-FromClipboard`를 재실행한다.
- 
--리뷰 결과 저장 스크립트는 `pass + complete_task`, `revise + revise_with_codex`, `blocked` 또는 `stop_for_user` 
--결과에 맞는 다음 행동을 안내만 한다. GPT API 호출, Codex 실행, build/test 실행, git commit은 수행하지 않는다.
-+리뷰 결과 저장 스크립트는 `pass + complete_task`, `revise + revise_with_codex`, `blocked` 또는 `stop_for_user` 
-결
-+과에 맞는 다음 행동을 안내만 한다. GPT API 호출, Codex 실행, build/test 실행, git commit은 수행하지 않는다.
- 
- ## 리뷰 반영 재수정 프롬프트 생성
- 
-@@ -921,8 +549,8 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1
- optional suggestions도 현재 task 범위 안에서 반영할 수 있게 하려면 `-AllowOptionalSuggestions` 옵션을 사용한다.
- 
- ```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1 -AllowOptionalSugge
--stions
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1 -AllowOptionalSugge
-s
-+tions
- ```
- 
- 재수정 프롬프트 생성 스크립트는 Codex 실행, GPT API 호출, build/test 실행, git commit을 수행하지 않는다.
-@@ -939,24 +567,24 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1
- 지정한 파일만 커밋 대상으로 확인하거나 실제로 커밋하려면 `-Files` 옵션을 사용한다.
- 
- ```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.ps
--1,.ai-dev/README.md -DryRun
+-지정한 파일만 커밋 대상으로 확인하거나 실제로 커밋하려면 `-Files` 옵션을 사용한다.
+-
+-```powershell
 -powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.ps
--1,.ai-dev/README.md
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.ps
 1
-+,.ai-dev/README.md -DryRun
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.ps
+-,.ai-dev/README.md -DryRun
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.ps
 1
-+,.ai-dev/README.md
- ```
- 
- 커밋 메시지를 직접 지정하거나 검증·리뷰 게이트를 명시적으로 우회할 수도 있다.
- 
- ```powershell
+-,.ai-dev/README.md
+-```
+-
+-커밋 메시지를 직접 지정하거나 검증·리뷰 게이트를 명시적으로 우회할 수도 있다.
+-
+-```powershell
 -powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Message "chore(ai-dev): add au
--tomation helpers"
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -AllowWithoutPassedCheck -Allow
--WithoutPassedReview
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Message "chore(ai-dev): add au
 t
-+omation helpers"
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -AllowWithoutPassedCheck -Allow
+-omation helpers"
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -AllowWithoutPassedCheck -Allow
 W
-+ithoutPassedReview
- ```
- 
+-ithoutPassedReview
+-```
+-
 -`-Files`를 지정하지 않으면 자동 커밋 스크립트는 기존처럼 `git add -A`로 현재 작업 트리의 모든 변경사항을 stage한다. `-Files`를 지정하면 각 파일이
-- 존재하고 변경사항이 있는지 확인한 뒤 `git add -- <files>`로 지정 파일만 stage한다. 선택 파일 외에 이미 staged 된 파일이 있으면 안전을 위해 중단
--한다.
-+`-Files`를 지정하지 않으면 자동 커밋 스크립트는 기존처럼 `git add -A`로 현재 작업 트리의 모든 변경사항을 stage한다. `-Files`를 지정하면 각 파일이
  
-+존재하고 변경사항이 있는지 확인한 뒤 `git add -- <files>`로 지정 파일만 stage한다. 선택 파일 외에 이미 staged 된 파일이 있으면 안전을 위해 중단한
+-존재하고 변경사항이 있는지 확인한 뒤 `git add -- <files>`로 지정 파일만 stage한다. 선택 파일 외에 이미 staged 된 파일이 있으면 안전을 위해 중단한
 다
-+.
- 
- 실행 전 예상하지 못한 사용자 변경이 없는지 반드시 확인한다. Codex 실행, GPT API 호출, build/test 실행은 수행하지 않는다.
- 
-@@ -965,18 +593,18 @@ WithoutPassedReview
- AI Dev Loop의 실제 기능 변경과 `.ai-dev` 실행 상태는 가능한 한 별도 커밋으로 관리한다.
- 
- - 기능 커밋에는 현재 task의 코드와 직접 관련 문서만 포함한다.
+-.
+-
+-실행 전 예상하지 못한 사용자 변경이 없는지 반드시 확인한다. Codex 실행, GPT API 호출, build/test 실행은 수행하지 않는다.
+-
+-## 실행 산출물 커밋 기준
+-
+-AI Dev Loop의 실제 기능 변경과 `.ai-dev` 실행 상태는 가능한 한 별도 커밋으로 관리한다.
+-
+-- 기능 커밋에는 현재 task의 코드와 직접 관련 문서만 포함한다.
 -- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `test-result.md`, `review.md` 등을 포함할
-- 수 있다.
-+- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `test-result.md`, `review.md` 등을 포함할
- 
-+수 있다.
- - `diff.md`와 `review-prompt.md`는 커질 수 있으므로 항상 커밋하지 않는다.
- - `review-response.json`은 민감 정보나 장문 내용이 포함될 수 있으므로 내용을 확인한 뒤 커밋 여부를 결정한다.
- - untracked 텍스트 파일 내용은 리뷰에 필요할 수 있지만, 리뷰 포함 여부와 커밋 대상 여부는 별도로 판단한다.
- - 대형 또는 민감 산출물은 정리하거나 별도 정책에 따라 제외한다.
  
+-수 있다.
+-- `diff.md`와 `review-prompt.md`는 커질 수 있으므로 항상 커밋하지 않는다.
+-- `review-response.json`은 민감 정보나 장문 내용이 포함될 수 있으므로 내용을 확인한 뒤 커밋 여부를 결정한다.
+-- untracked 텍스트 파일 내용은 리뷰에 필요할 수 있지만, 리뷰 포함 여부와 커밋 대상 여부는 별도로 판단한다.
+-- 대형 또는 민감 산출물은 정리하거나 별도 정책에 따라 제외한다.
+-
 -커밋 전에는 `git status --short`와 `ai-dev-commit.ps1 -DryRun`으로 대상을 먼저 확인한다. 선택 파일 커밋 기능이 준비되면 전체 stage
--보다 선택 파일 방식을 우선한다.
-+커밋 전에는 `git status --short`와 `ai-dev-commit.ps1 -DryRun`으로 대상을 먼저 확인한다. 선택 파일 커밋 기능이 준비되면 전체 stage
 보
-+다 선택 파일 방식을 우선한다.
- 
+-다 선택 파일 방식을 우선한다.
+-
 -세부 기준은 `docs/ai-dev-loop-policy.md`의 `.ai-dev` 실행 산출물 커밋/무시 정책을 따른다. 실제 `.gitignore` 변경은 별도 task에서
-- 검토한다.
-+세부 기준은 `docs/ai-dev-loop-policy.md`의 `.ai-dev` 실행 산출물 커밋/무시 정책을 따른다. 실제 `.gitignore` 변경은 별도 task에서
- 
-+검토한다.
- 
- ## 자동 개발 루프 상태 요약
- 
-@@ -1024,11 +652,11 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1 -Json
- ```
  
+-검토한다.
+-
+-## 자동 개발 루프 상태 요약
+-
+-현재 queue, state, git 변경사항, 테스트 결과, 리뷰 결과를 한 번에 확인한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-status.ps1
+-```
+-
+-상태 요약을 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-status.ps1 -Json
+-```
+-
+-상태 요약 스크립트는 파일을 수정하지 않으며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하지 않는다.
+-
+-## 다음 행동 안내
+-
+-현재 queue, state, review, test, git 상태를 확인해 다음에 실행할 추천 명령을 출력한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-next.ps1
+-```
+-
+-다음 행동 안내를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-next.ps1 -Json
+-```
+-
+-다음 행동 안내 스크립트는 추천 명령만 출력하며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하지 않는다.
+-
+-## 수동 사이클 안내
+-
+-상태 요약과 다음 행동 안내를 한 번에 확인하고, 관련 프롬프트·결과 파일의 존재 여부를 표시한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
+-```
+-
+-상태와 다음 행동을 하나의 JSON 객체로 출력하려면 `-Json` 옵션을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1 -Json
+-```
+-
 -수동 사이클 안내 스크립트는 추천 명령과 파일 경로만 보여주며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하지
-- 않는다.
-+수동 사이클 안내 스크립트는 추천 명령과 파일 경로만 보여주며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하지
- 
-+않는다.
  
+-않는다.
+-
 -`manual-cycle`에서 다음 action이 `ask_gpt_review`로 표시되면 수동 리뷰 브리지를 사용한다. GPT API 키가 없어도 아래 흐름으로 리뷰를 진행할
-- 수 있다.
-+`manual-cycle`에서 다음 action이 `ask_gpt_review`로 표시되면 수동 리뷰 브리지를 사용한다. GPT API 키가 없어도 아래 흐름으로 리뷰를 진행할
- 
-+수 있다.
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
-@@ -1055,20 +683,20 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -Fro
-mCli
- 
- ## 자동 실행 단계 정책
- 
--`ai-dev-auto-step.ps1`는 현재 상태를 보고 안전한 다음 한 단계만 실행하는 보조 스크립트로 도입할 예정이다. `ai-dev-next.ps1`처럼 안내만 하는 
--명령과 달리, 허용된 로컬 스크립트는 직접 실행할 수 있다.
-+`ai-dev-auto-step.ps1`는 현재 상태를 보고 안전한 다음 한 단계만 실행하는 보조 스크립트로 도입할 예정이다. `ai-dev-next.ps1`처럼 안내만 하는 
-명
-+령과 달리, 허용된 로컬 스크립트는 직접 실행할 수 있다.
- 
- 명령 선택 기준:
  
- | 상황 | 사용할 명령 | 이유 |
- | --- | --- | --- |
+-수 있다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
+-```
+-
+-1. `manual-cycle`에서 현재 task와 다음 action이 `ask_gpt_review`인지 확인한다.
+-2. `ai-dev-copy-review-prompt.ps1`로 `review-prompt.md`를 클립보드에 복사한다.
+-3. ChatGPT 웹 화면에 붙여넣고 JSON 리뷰만 출력하도록 요청한다.
+-4. ChatGPT가 반환한 JSON 리뷰를 클립보드에 복사한다.
+-5. 아래 명령으로 리뷰 결과를 저장한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
+-```
+-
+-리뷰 저장 후 decision별 다음 단계:
+-
+-- `pass`: `ai-dev-complete-task.ps1` 실행 또는 커밋 조건을 검토한다.
+-- `revise`: `ai-dev-make-revise-prompt.ps1`로 재수정 프롬프트를 만든 뒤 Codex/Cline에 수동 전달한다.
+-- `blocked`: 사용자 판단이 필요하므로 루프를 중단하고 요구사항이나 범위를 조정한다.
+-
+-`review-prompt.md`에는 diff, 테스트 결과, 로컬 경로, 민감 정보가 포함될 수 있다. ChatGPT에 붙여넣기 전에 사용자가 공유 범위를 확인해야 한다.
+-
+-## 자동 실행 단계 정책
+-
+-`ai-dev-auto-step.ps1`는 현재 상태를 보고 안전한 다음 한 단계만 실행하는 보조 스크립트로 도입할 예정이다. `ai-dev-next.ps1`처럼 안내만 하는 
+명
+-령과 달리, 허용된 로컬 스크립트는 직접 실행할 수 있다.
+-
+-명령 선택 기준:
+-
+-| 상황 | 사용할 명령 | 이유 |
+-| --- | --- | --- |
 -| 현재 상태와 다음 행동을 한 화면에서 보고 싶을 때 | `scripts/ai-dev-manual-cycle.ps1` | 상태 요약, 다음 action, 관련 파일 존재 여부
--를 함께 보여주는 수동 운영 대시보드다. |
-+| 현재 상태와 다음 행동을 한 화면에서 보고 싶을 때 | `scripts/ai-dev-manual-cycle.ps1` | 상태 요약, 다음 action, 관련 파일 존재 여부
 를
-+ 함께 보여주는 수동 운영 대시보드다. |
- | 다음 action과 추천 명령만 확인하고 싶을 때 | `scripts/ai-dev-next.ps1` | 읽기/안내용 스크립트이며 하위 명령을 자동 실행하지 않는다. |
+- 함께 보여주는 수동 운영 대시보드다. |
+-| 다음 action과 추천 명령만 확인하고 싶을 때 | `scripts/ai-dev-next.ps1` | 읽기/안내용 스크립트이며 하위 명령을 자동 실행하지 않는다. |
 -| 안전한 다음 한 단계만 자동으로 진행하고 싶을 때 | `scripts/ai-dev-auto-step.ps1` | `next` 판단 결과를 기반으로 허용된 로컬 스크립트만 한
-- 번 실행한다. |
--| 여러 안전 단계를 제한 횟수 안에서 이어가고 싶을 때 | `scripts/ai-dev-auto-cycle.ps1` | auto-step 반복용이며 사용자 개입 필요 상태에서
-- 중단한다. |
-+| 안전한 다음 한 단계만 자동으로 진행하고 싶을 때 | `scripts/ai-dev-auto-step.ps1` | `next` 판단 결과를 기반으로 허용된 로컬 스크립트만 한
- 
-+번 실행한다. |
-+| 여러 안전 단계를 제한 횟수 안에서 이어가고 싶을 때 | `scripts/ai-dev-auto-cycle.ps1` | auto-step 반복용이며 사용자 개입 필요 상태에서
  
-+중단한다. |
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1
-@@ -1114,11 +742,11 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.p
-s1
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
- ```
+-번 실행한다. |
+-| 여러 안전 단계를 제한 횟수 안에서 이어가고 싶을 때 | `scripts/ai-dev-auto-cycle.ps1` | auto-step 반복용이며 사용자 개입 필요 상태에서
  
+-중단한다. |
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -DryRun
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -Json
+-```
+-
+-검증, diff 저장, 리뷰 프롬프트 생성처럼 기본 자동 실행보다 한 단계 위험한 로컬 작업은 명시적 허용 옵션을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -AllowCheck
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -AllowSaveDiff
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -AllowReviewPrompt
+-```
+-
+-초기 auto-step 자동 실행 허용 후보:
+-
+-- `current-task-prompt.md`가 없을 때 `scripts/ai-dev-make-prompt.ps1`
+-- 명시적으로 안전한 옵션이 선택된 경우 `scripts/ai-dev-check.ps1`
+-- diff 저장이 필요한 경우 `scripts/ai-dev-save-diff.ps1`
+-- review prompt 생성이 필요한 경우 `scripts/ai-dev-make-review-prompt.ps1`
+-
+-auto-step이 직접 실행하지 않고 중단 또는 안내해야 하는 단계:
+-
+-- Codex 또는 Cline 작업
+-- GPT 리뷰
+-- blocked 상태
+-- revise 반영
+-- git commit
+-- 사용자 데이터 삭제, 복원, 마이그레이션 위험이 있는 작업
+-
+-`ask_gpt_review` 상태에서 auto-step은 GPT API를 호출하지 않고 수동 리뷰 브리지 명령을 안내한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -Json
+-```
+-
+-안내에 포함되는 수동 리뷰 브리지 명령:
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
+-```
+-
 -첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. ChatGPT가 JSON 리뷰만 출력하면 그 JSON을 다시 클
--립보드에 복사하고 두 번째 명령으로 저장한다.
-+첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. ChatGPT가 JSON 리뷰만 출력하면 그 JSON을 다시 클
 립
-+보드에 복사하고 두 번째 명령으로 저장한다.
- 
+-보드에 복사하고 두 번째 명령으로 저장한다.
+-
 -`ai-dev-auto-cycle.ps1`는 제한 횟수 안에서 auto-step을 반복하는 초기 자동화 스크립트다. 사용자 개입이 필요한 action, 실패, 반복 위험, co
--mmit 필요 상태에서는 중단한다.
-+`ai-dev-auto-cycle.ps1`는 제한 횟수 안에서 auto-step을 반복하는 초기 자동화 스크립트다. 사용자 개입이 필요한 action, 실패, 반복 위험, co
 m
-+mit 필요 상태에서는 중단한다.
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1
-@@ -1135,26 +763,26 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -Allo
-wSav
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -AllowReviewPrompt
- ```
- 
+-mit 필요 상태에서는 중단한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -DryRun
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -MaxSteps 3
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -Json
+-```
+-
+-auto-cycle에서 check, diff 저장, review prompt 생성을 허용하려면 auto-step과 같은 허용 옵션을 전달한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -AllowCheck
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -AllowSaveDiff
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -AllowReviewPrompt
+-```
+-
 -auto-cycle은 Codex/Cline 작업, GPT 리뷰, revise, commit, blocked, 사용자 개입 필요 action에서 멈춘다. 무한 루프를 피하기 위해
-- `-MaxSteps` 제한과 같은 action 반복 감지를 사용한다.
-+auto-cycle은 Codex/Cline 작업, GPT 리뷰, revise, commit, blocked, 사용자 개입 필요 action에서 멈춘다. 무한 루프를 피하기 위해
- 
-+`-MaxSteps` 제한과 같은 action 반복 감지를 사용한다.
  
+-`-MaxSteps` 제한과 같은 action 반복 감지를 사용한다.
+-
 -auto-cycle이 `ask_gpt_review`에서 멈추면 `stoppedReason`은 `user_intervention_required:ask_gpt_review`로 표
--시된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-+auto-cycle이 `ask_gpt_review`에서 멈추면 `stoppedReason`은 `user_intervention_required:ask_gpt_review`로 표
 시
-+된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
- ```
- 
+-된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
+-```
+-
 -첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 리뷰를 받은 뒤 두 번째 명령으로 클립보드의 리뷰 JSON
--을 저장한다. auto-cycle은 이 단계에서 GPT API를 호출하지 않고 중단한다.
-+첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 리뷰를 받은 뒤 두 번째 명령으로 클립보드의 리뷰 JSON
 을
-+ 저장한다. auto-cycle은 이 단계에서 GPT API를 호출하지 않고 중단한다.
- 
- 명령 역할 구분:
- 
- - `ai-dev-next.ps1`: 현재 상태를 읽고 다음 추천 action과 명령만 출력한다. 자동 실행하지 않는다.
- - `ai-dev-manual-cycle.ps1`: `status`와 `next`를 함께 보여주는 수동 운영 대시보드다. 자동 실행하지 않는다.
+- 저장한다. auto-cycle은 이 단계에서 GPT API를 호출하지 않고 중단한다.
+-
+-명령 역할 구분:
+-
+-- `ai-dev-next.ps1`: 현재 상태를 읽고 다음 추천 action과 명령만 출력한다. 자동 실행하지 않는다.
+-- `ai-dev-manual-cycle.ps1`: `status`와 `next`를 함께 보여주는 수동 운영 대시보드다. 자동 실행하지 않는다.
 -- `ai-dev-auto-step.ps1`: `next` 판단 결과를 기반으로 안전한 action 한 단계만 자동 실행한다. 위험 action은 실행하지 않고 안내 후 중단한
--다.
-+- `ai-dev-auto-step.ps1`: `next` 판단 결과를 기반으로 안전한 action 한 단계만 자동 실행한다. 위험 action은 실행하지 않고 안내 후 중단한
 다
-+.
- - `ai-dev-auto-cycle.ps1`: 제한 횟수 안에서 auto-step을 반복한다. 사용자 개입 필요 action에서 중단한다.
- 
- auto-step과 auto-cycle은 Codex/GPT API 호출, git commit, git push를 자동 실행하지 않는다.
-@@ -1174,24 +802,24 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -DryR
-un -
- 확인 기준:
- 
- - `goal_completed`: 더 이상 실행할 task가 없으므로 새 goal 초기화가 필요하다.
+-.
+-- `ai-dev-auto-cycle.ps1`: 제한 횟수 안에서 auto-step을 반복한다. 사용자 개입 필요 action에서 중단한다.
+-
+-auto-step과 auto-cycle은 Codex/GPT API 호출, git commit, git push를 자동 실행하지 않는다.
+-
+-## 수동 자동화 UX 판단 순서
+-
+-GPT API 없이 수동 자동화 흐름을 운영할 때는 먼저 상태를 읽고, 그 다음 안전한 DryRun으로 다음 행동을 확인한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-status.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-next.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1 -DryRun
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -DryRun -MaxSteps 3
+-```
+-
+-확인 기준:
+-
+-- `goal_completed`: 더 이상 실행할 task가 없으므로 새 goal 초기화가 필요하다.
 -- `ask_gpt_review`: `ai-dev-copy-review-prompt.ps1`와 `ai-dev-save-review.ps1 -FromClipboard`를 사용해 
--수동 리뷰 브리지로 진행한다.
-+- `ask_gpt_review`: `ai-dev-copy-review-prompt.ps1`와 `ai-dev-save-review.ps1 -FromClipboard`를 사용해 
 수
-+동 리뷰 브리지로 진행한다.
- - `revise`: `ai-dev-make-revise-prompt.ps1`로 재수정 프롬프트를 만든다.
- - `blocked`: 사용자 판단이 필요하므로 자동 진행을 멈춘다.
- - `commit`: 자동 실행하지 않고 커밋 조건과 대상 파일을 사람이 확인한다.
- 
+-동 리뷰 브리지로 진행한다.
+-- `revise`: `ai-dev-make-revise-prompt.ps1`로 재수정 프롬프트를 만든다.
+-- `blocked`: 사용자 판단이 필요하므로 자동 진행을 멈춘다.
+-- `commit`: 자동 실행하지 않고 커밋 조건과 대상 파일을 사람이 확인한다.
+-
 -`goal_completed` 상태에서 `auto-step`은 추가 작업을 실행하지 않고 현재 goal이 완료되었으며 더 실행할 task가 없다고 안내한다. 추천 명령에는 상태
-- 확인용 `ai-dev-status.ps1`만 포함한다.
-+`goal_completed` 상태에서 `auto-step`은 추가 작업을 실행하지 않고 현재 goal이 완료되었으며 더 실행할 task가 없다고 안내한다. 추천 명령에는 상태
- 
-+확인용 `ai-dev-status.ps1`만 포함한다.
  
+-확인용 `ai-dev-status.ps1`만 포함한다.
+-
 -`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고, JSON 출력에서는 `completed: true`, 
--`stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.a
--i-dev/state.json`을 새 목표 기준으로 초기화한다.
-+`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고, JSON 출력에서는 `completed: true`, 
 `
-+stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai
+-stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai
+-
+-dev/state.json`을 새 목표 기준으로 초기화한다.
 -
-+dev/state.json`을 새 목표 기준으로 초기화한다.
- 
 -`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하고 JSON 리뷰만 다시 복사한다. 완료된 goal이나 기
--존 정상 상태를 불필요하게 오염시키지 않도록 실패 기록 방식은 별도 정책에 따라 개선한다.
-+`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하고 JSON 리뷰만 다시 복사한다. 완료된 goal이나 기
 존
-+ 정상 상태를 불필요하게 오염시키지 않도록 실패 기록 방식은 별도 정책에 따라 개선한다.
- 
+- 정상 상태를 불필요하게 오염시키지 않도록 실패 기록 방식은 별도 정책에 따라 개선한다.
+-
 -최종 검증 task에서는 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 결과를 `te
--st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-+최종 검증 task에서는 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 결과를 `te
 s
-+t-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
- 
- ## Codex CLI 완전 자동화 준비
- 
-@@ -1209,14 +837,14 @@ st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검
- 
- 도입 예정 스크립트:
- 
+-t-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
+-
+-## Codex CLI 완전 자동화 준비
+-
+-다음 자동화 단계에서는 Codex CLI를 구현자와 리뷰어로 사용한다. GPT API Key, Cline, Copilot CLI, `gh` 없이 Codex CLI만 사용한다.
+-
+-현재 전제:
+-
+-- `codex-cli 0.133.0` 사용 가능
+-- `codex exec` 동작 확인 완료
+-- approval: `never`
+-- sandbox: `workspace-write`
+-- workdir: `D:\ai-apps\planpilot-local`
+-- Cline은 삭제되어 사용하지 않음
+-- Copilot CLI와 `gh`는 현재 사용하지 않음
+-
+-도입 예정 스크립트:
+-
 -- `scripts/ai-dev-run-codex.ps1`: `.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 수행한
--다.
--- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 
--저장 흐름에 연결한다.
--- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 저장, Codex 리뷰, save-review
--, pass 시 commit/complete-task를 연결한다.
--- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력하면 goal/queue/state 생성, c
--urrent-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
-+- `scripts/ai-dev-run-codex.ps1`: `.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 수행한
 다
-+.
-+- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 
+-.
+-- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 
 저
-+장 흐름에 연결한다.
-+- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 저장, Codex 리뷰, save-review
+-장 흐름에 연결한다.
+-- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 저장, Codex 리뷰, save-review
 ,
-+ pass 시 commit/complete-task를 연결한다.
-+- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력하면 goal/queue/state 생성, c
+- pass 시 commit/complete-task를 연결한다.
+-- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력하면 goal/queue/state 생성, c
 u
-+rrent-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
- 
- 초기 안전 기준:
- 
-@@ -1240,9 +868,8 @@ urrent-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
- 
- ### Codex 구현 실행
- 
+-rrent-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
+-
+-초기 안전 기준:
+-
+-- full cycle은 처음에 `MaxTasks 1` 중심으로 제한한다.
+-- Codex 구현 실행은 `AllowCodex`가 있을 때만 수행한다.
+-- Codex 리뷰 실행은 `AllowReviewCodex`가 있을 때만 수행한다.
+-- git commit은 `AllowCommit`이 있을 때만 수행한다.
+-- auto-goal에서 실제 full cycle 실행은 `AllowRun` 또는 명시적 실행 옵션이 있을 때만 수행한다.
+-- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
+-- build/check 실패, package 파일 변경, 리뷰 decision이 `pass`가 아닌 경우 자동 커밋하지 않는다.
+-- `git reset`, `git clean`, `npm install`, `git push`는 자동 실행하지 않는다.
+-
+-자동화 로그와 결과 파일:
+-
+-- `.ai-dev/codex-result.md`: Codex 구현 실행 결과
+-- `.ai-dev/review-response.json`: Codex 리뷰 JSON 원문
+-- `.ai-dev/review.md`: 저장된 리뷰 요약
+-- `.ai-dev/test-result.md`: check/build 또는 수동 검증 요약
+-- `.ai-dev/loop-log.md`: 루프 진행 기록
+-- `.ai-dev/state.json`: 현재 상태와 마지막 실패 요약
+-
+-### Codex 구현 실행
+-
 -`.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 실행한다. 실행 전 `git status --short`를 확인하고
--, 작업 트리가 dirty이면 기본 중단한다. 단, dirty 파일이 `.ai-dev/current-task-prompt.md`뿐이면 프롬프트 생성 산출물로 보고 진행할 수 있
--다.
-+`.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 실행한다. 실행 전 `git status --short`를 확인하고
 ,
-+ 작업 트리가 dirty이면 기본 중단한다. 단, dirty 파일이 `.ai-dev/current-task-prompt.md`뿐이면 프롬프트 생성 산출물로 보고 진행할 수 있다
+- 작업 트리가 dirty이면 기본 중단한다. 단, dirty 파일이 `.ai-dev/current-task-prompt.md`뿐이면 프롬프트 생성 산출물로 보고 진행할 수 있다
 .
- 
- 실행 예정 정보만 확인하려면 `-DryRun`을 사용한다.
- 
-@@ -1268,21 +895,21 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -Gener
-ateP
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -DryRun -Json
- ```
- 
+-
+-실행 예정 정보만 확인하려면 `-DryRun`을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -DryRun
+-```
+-
+-기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-result.md`에 저장한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1
+-```
+-
+-`current-task-prompt.md`가 없을 때 자동 생성까지 허용하려면 `-GeneratePromptIfMissing`을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -GeneratePromptIfMissing
+-```
+-
+-자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -DryRun -Json
+-```
+-
 -dirty worktree에서도 실행해야 하는 특별한 경우에만 `-AllowDirty`를 명시한다. 이 옵션은 사용자 변경과 Codex 변경이 섞일 수 있으므로 full cyc
--le에서는 초기 기본값으로 사용하지 않는다.
-+dirty worktree에서도 실행해야 하는 특별한 경우에만 `-AllowDirty`를 명시한다. 이 옵션은 사용자 변경과 Codex 변경이 섞일 수 있으므로 full cyc
 l
-+e에서는 초기 기본값으로 사용하지 않는다.
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -AllowDirty
- ```
- 
+-e에서는 초기 기본값으로 사용하지 않는다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -AllowDirty
+-```
+-
 -이 스크립트는 Codex에 전달하는 프롬프트 끝에 추가 안전 규칙을 붙인다. git commit, git reset, git clean, npm install, package 
--파일 수정, 현재 task 범위 밖 작업을 금지한다. 스크립트 자체도 build/test/lint, git add, git commit을 실행하지 않는다.
-+이 스크립트는 Codex에 전달하는 프롬프트 끝에 추가 안전 규칙을 붙인다. git commit, git reset, git clean, npm install, package 
 파
-+일 수정, 현재 task 범위 밖 작업을 금지한다. 스크립트 자체도 build/test/lint, git add, git commit을 실행하지 않는다.
- 
- ### Codex 리뷰 실행
- 
+-일 수정, 현재 task 범위 밖 작업을 금지한다. 스크립트 자체도 build/test/lint, git add, git commit을 실행하지 않는다.
+-
+-### Codex 리뷰 실행
+-
 -`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/review-response.json`에 저장한다. 실행 
--전 `git status --short`를 확인하고, 작업 트리가 dirty이면 기본 중단한다. 리뷰 대상 변경사항이 남아 있는 상태에서 실행해야 하는 경우에만 `-AllowD
--irty`를 명시한다.
-+`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/review-response.json`에 저장한다. 실행 
 전
-+ `git status --short`를 확인하고, 작업 트리가 dirty이면 기본 중단한다. 리뷰 대상 변경사항이 남아 있는 상태에서 실행해야 하는 경우에만 `-AllowDi
+- `git status --short`를 확인하고, 작업 트리가 dirty이면 기본 중단한다. 리뷰 대상 변경사항이 남아 있는 상태에서 실행해야 하는 경우에만 `-AllowDi
 r
-+ty`를 명시한다.
- 
- 실행 예정 정보만 확인하려면 `-DryRun`을 사용한다.
- 
-@@ -1290,8 +917,8 @@ irty`를 명시한다.
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -DryRun
- ```
- 
+-ty`를 명시한다.
+-
+-실행 예정 정보만 확인하려면 `-DryRun`을 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -DryRun
+-```
+-
 -기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-result.md`에 저장한다. Codex 출력에서 첫 번째 유
--효 JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-+기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-result.md`에 저장한다. Codex 출력에서 첫 번째 유
 효
-+ JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1
-@@ -1300,16 +927,16 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1
- `review-prompt.md`가 없을 때 자동 생성까지 허용하려면 `-GenerateReviewPromptIfMissing`를 사용한다.
- 
- ```powershell
+- JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1
+-```
+-
+-`review-prompt.md`가 없을 때 자동 생성까지 허용하려면 `-GenerateReviewPromptIfMissing`를 사용한다.
+-
+-```powershell
 -powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -GenerateReviewPrompt
--IfMissing
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -GenerateReviewPrompt
 I
-+fMissing
- ```
- 
+-fMissing
+-```
+-
 -리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-response.json` 흐름까지 이어가려면 `-SaveRe
--view`를 사용한다.
-+리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-response.json` 흐름까지 이어가려면 `-SaveRe
 v
-+iew`를 사용한다.
- 
- ```powershell
+-iew`를 사용한다.
+-
+-```powershell
 -powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveRevi
--ew
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveRevi
 e
-+w
- ```
- 
- 자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
-@@ -1321,18 +948,18 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1
- -Dr
- 주의:
- 
- - Codex 리뷰 실행 프롬프트에는 파일 수정 금지, git 명령 금지, build/test/lint 실행 금지, JSON 객체만 출력하라는 안전 규칙을 추가한다.
+-w
+-```
+-
+-자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -DryRun -Json
+-```
+-
+-주의:
+-
+-- Codex 리뷰 실행 프롬프트에는 파일 수정 금지, git 명령 금지, build/test/lint 실행 금지, JSON 객체만 출력하라는 안전 규칙을 추가한다.
 -- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`, `optional_suggestions`, `next_s
--tep` 필드가 필요하다.
-+- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`, `optional_suggestions`, `next_s
 t
-+ep` 필드가 필요하다.
- - JSON 추출에 실패하면 출력 preview를 보여주고 종료 코드 1로 종료한다.
- - `-AllowDirty`는 구현 변경사항을 리뷰해야 할 때만 사용한다. 사용자 변경과 자동화 산출물이 섞여 있으면 리뷰 결과와 후속 저장 상태가 혼동될 수 있다.
- - 이 스크립트는 git add, git commit, build/test/lint, GPT API 호출을 수행하지 않는다.
- 
- ### full auto-cycle 초안
- 
+-ep` 필드가 필요하다.
+-- JSON 추출에 실패하면 출력 preview를 보여주고 종료 코드 1로 종료한다.
+-- `-AllowDirty`는 구현 변경사항을 리뷰해야 할 때만 사용한다. 사용자 변경과 자동화 산출물이 섞여 있으면 리뷰 결과와 후속 저장 상태가 혼동될 수 있다.
+-- 이 스크립트는 git add, git commit, build/test/lint, GPT API 호출을 수행하지 않는다.
+-
+-### full auto-cycle 초안
+-
 -`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revie
--w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revie
 w
-+ prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
- 
--T004 버전의 실행 단계:
-+T005 버전의 실행 단계:
- 
- 1. `ai-dev-make-prompt.ps1`
- 2. `ai-dev-run-codex.ps1`
-@@ -1340,6 +967,12 @@ T004 버전의 실행 단계:
- 4. `ai-dev-save-diff.ps1`
- 5. `ai-dev-make-review-prompt.ps1 -Strict`
- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
-+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다. `-CommitFiles`가 있으면 선택 파일만 커밋 대상으로 전달한다.
-+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
-+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-+12. `MaxTasks`가 1보다 크면 complete-task 성공 후 다음 task를 다시 읽고 같은 흐름을 반복한다.
- 
- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
- 
-@@ -1353,16 +986,23 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 
--Dry
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
- ```
- 
--기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처리
--를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
-+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이다. `MaxTasks`가 1보다 크면 
+- prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
+-
+-T005 버전의 실행 단계:
+-
+-1. `ai-dev-make-prompt.ps1`
+-2. `ai-dev-run-codex.ps1`
+-3. `ai-dev-check.ps1 -BuildOnly`
+-4. `ai-dev-save-diff.ps1`
+-5. `ai-dev-make-review-prompt.ps1 -Strict`
+-6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
+-7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
+-8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
+-9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다. `-CommitFiles`가 있으면 선택 파일만 커밋 대상으로 전달한다.
+-10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
+-11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
+-12. `MaxTasks`가 1보다 크면 complete-task 성공 후 다음 task를 다시 읽고 같은 흐름을 반복한다.
+-
+-먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun
+-```
+-
+-자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
+-```
+-
+-기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이다. `MaxTasks`가 1보다 크면 
 각
-+ task가 커밋과 complete-task까지 성공한 경우에만 다음 task로 반복한다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -M
-a
-+xSteps 20
-+```
-+
-+여러 task 반복 구조를 확인하려면 `MaxTasks`를 늘릴 수 있다. 실제 반복은 각 task의 commit/complete-task가 성공해야 다음 task로 넘어간다.
- 
- ```powershell
+- task가 커밋과 complete-task까지 성공한 경우에만 다음 task로 반복한다.
+-
+-```powershell
 -powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -M
--axSteps 20
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 3 -M
 a
-+xSteps 20
- ```
- 
+-xSteps 20
+-```
+-
+-여러 task 반복 구조를 확인하려면 `MaxTasks`를 늘릴 수 있다. 실제 반복은 각 task의 commit/complete-task가 성공해야 다음 task로 넘어간다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 3 -M
+a
+-xSteps 20
+-```
+-
 -`-AllowCodex` 없이 실제 실행하면 prompt 생성 이후 Codex 구현 실행 직전에서 중단하고 추천 명령을 출력한다. Codex 구현까지 허용하려면 명시적으로 `-
--AllowCodex`를 지정한다.
-+`-AllowCodex` 없이 실제 실행하면 prompt 생성 이후 Codex 구현 실행 직전에서 중단하고 추천 명령을 출력한다. Codex 구현까지 허용하려면 명시적으로 `-
 A
-+llowCodex`를 지정한다.
- 
- ```powershell
- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex
-@@ -1371,49 +1011,109 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1 -All
- Codex 구현과 Codex 리뷰까지 이어가려면 `-AllowCodex`와 `-AllowReviewCodex`를 함께 지정한다.
- 
- ```powershell
+-llowCodex`를 지정한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex
+-```
+-
+-Codex 구현과 Codex 리뷰까지 이어가려면 `-AllowCodex`와 `-AllowReviewCodex`를 함께 지정한다.
+-
+-```powershell
 -powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
--ewCodex -MaxTasks 1
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
 e
-+wCodex -MaxTasks 1
-+```
-+
-+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 함께 지정한다. 커밋 메시지는 기본적으로 현재 
+-wCodex -MaxTasks 1
+-```
+-
+-리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 함께 지정한다. 커밋 메시지는 기본적으로 현재 
 t
-+ask의 `commitMessage`를 사용한다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
+-ask의 `commitMessage`를 사용한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
 e
-+wCodex -AllowCommit -MaxTasks 1
-+```
-+
-+선택 파일만 커밋하려면 `-CommitFiles`를 지정한다. 이 값은 그대로 `ai-dev-commit.ps1 -Files`에 전달된다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
+-wCodex -AllowCommit -MaxTasks 1
+-```
+-
+-선택 파일만 커밋하려면 `-CommitFiles`를 지정한다. 이 값은 그대로 `ai-dev-commit.ps1 -Files`에 전달된다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
 e
-+wCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 1
-+```
-+
-+최대 3개 task까지 연속 실행하려면 `MaxTasks 3`을 지정한다. complete-task가 queue/state/loop-log를 갱신하므로, 다음 task 구현 실
+-wCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 1
+-```
+-
+-최대 3개 task까지 연속 실행하려면 `MaxTasks 3`을 지정한다. complete-task가 queue/state/loop-log를 갱신하므로, 다음 task 구현 실
 행
-+ 시 dirty worktree 정책을 별도로 판단해야 한다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
-e
-+wCodex -AllowCommit -AllowDirty -MaxTasks 3
- ```
- 
+- 시 dirty worktree 정책을 별도로 판단해야 한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
+e
+-wCodex -AllowCommit -AllowDirty -MaxTasks 3
+-```
+-
 -dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-co
--dex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-+dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-co
 d
-+ex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
- 
- ```powershell
+-ex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
+-
+-```powershell
 -powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
--ewCodex -AllowDirty -MaxTasks 1
-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
 e
-+wCodex -AllowDirty -MaxTasks 1
- ```
- 
--`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연결
--은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 complet
+-wCodex -AllowDirty -MaxTasks 1
+-```
+-
+-리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 complet
 e
-+-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package-l
+--task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package-l
 o
-+ck.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 `co
+-ck.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 `co
 m
-+mit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
- 
- ### 목표 입력 기반 자동 실행
- 
- 최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 하는 것이다.
- 
--예상 최종 명령:
-+먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
+-mit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
+-
+-### 목표 입력 기반 자동 실행
+-
+-최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 하는 것이다.
+-
+-먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
  
-+추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-+```
-+
-+자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-+DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 파일 작성 전에 생성될 goal/task 계
+-추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
+-```
+-
+-자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
+-DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 파일 작성 전에 생성될 goal/task 계
 획
-+을 검토할 수 있다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
+-을 검토할 수 있다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
  
-+추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-+```
-+
-+실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이 모드는 `ai-dev-auto-cycle-
+-추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
+-```
+-
+-실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이 모드는 `ai-dev-auto-cycle-
 f
-+ull.ps1`을 호출하지 않는다.
-+
-+```powershell
-+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
+-ull.ps1`을 호출하지 않는다.
+-
+-```powershell
+-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
  
-+추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-+```
-+
-+생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommi
+-추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
+-```
+-
+-생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommi
 t
-+` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다. 실제 
+-` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다. 실제 
 구
-+현까지 허용하려면 `-AllowCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하
+-현까지 허용하려면 `-AllowCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하
 게
-+ `-AllowCodex -AllowReviewCodex -AllowCommit`을 함께 지정한다.
- 
- ```powershell
+- `-AllowCodex -AllowReviewCodex -AllowCommit`을 함께 지정한다.
+-
+-```powershell
 -powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
-- 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -All
--owCommit -MaxTasks 1
-+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이트
  
-+추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -Allo
+-추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -Allo
 w
-+Commit -MaxTasks 1
- ```
- 
--예정 동작:
-+동작:
- 
- 1. `GoalTitle`과 `GoalDescription`을 검증한다.
- 2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
--3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
--4. `.ai-dev/current-task-prompt.md`를 생성한다.
--5. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-+3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-+4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다.
-+5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
-+6. `.ai-dev/current-task-prompt.md`를 생성한다.
-+7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-+8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-task-prompt.md`, 결과 파일 때
+-Commit -MaxTasks 1
+-```
+-
+-동작:
+-
+-1. `GoalTitle`과 `GoalDescription`을 검증한다.
+-2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
+-3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
+-4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다.
+-5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
+-6. `.ai-dev/current-task-prompt.md`를 생성한다.
+-7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
+-8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-task-prompt.md`, 결과 파일 때
 문
-+에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트는 au
+-에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트는 au
 t
-+o-goal 시작 전에 별도로 유지한다.
- 
- 안전 기준:
- 
- - `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
--- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시한다.
-+- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
- - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
--- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
--- full cycle 호출은 `-AllowRun` 또는 그에 준하는 명시적 실행 옵션이 있을 때만 수행한다.
-+- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 않는다.
-+- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommit` 중 하나가 있을 때만 수행한다.
-+- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCodex`, `-AllowReviewCodex`
+-o-goal 시작 전에 별도로 유지한다.
+-
+-안전 기준:
+-
+-- `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
+-- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
+-- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
+-- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 않는다.
+-- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommit` 중 하나가 있을 때만 수행한다.
+-- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCodex`, `-AllowReviewCodex`
 ,
-+ `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
- - package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커밋하지 않는다.
-+- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
- 
- ## 운영 원칙
- 
-@@ -1430,695 +1130,75 @@ owCommit -MaxTasks 1
- 
- 세부 기준은 `docs/ai-dev-loop-policy.md`를 따른다.
- 
+- `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
+-- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커밋하지 않는다.
+-- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
+-
+-## 운영 원칙
+-
+-- 저장소의 `AGENTS.md`, 사용자 지시, 보안 정책이 자동 개발 루프보다 우선한다.
+-- 한 번에 하나의 task만 수행한다.
+-- task 단위로 구현, 검증, 리뷰, 커밋한다.
+-- 현재 task 범위 밖 파일을 임의로 수정하지 않는다.
+-- 검증하지 않은 결과를 통과로 기록하지 않는다.
+-- 실제 DB 삭제, 초기화, 복원, 대규모 리팩터링은 초기 실험 범위에서 제외한다.
+-
+-## 다음 단계
+-
+-실제 자동 개발 루프 실행기와 상태 갱신 로직은 별도 정책 검토 후 추가한다.
+-
+-세부 기준은 `docs/ai-dev-loop-policy.md`를 따른다.
+-
+- succeeded in 2823ms:
+-backlog.md
+-codex-result.md
+-codex-review-result.md
+-current-task-prompt.md
+-diff.md
+-extra-instructions.example.md
+-goal.md
+-loop-log.md
+-plan.md
+-queue.example.json
+-queue.json
+-README.md
+-review-prompt.md
+-review-response.json
+-review.md
+-state.example.json
+-state.json
+-test-result.md
+-
 -codex
--현재 full cycle은 T004 자리표시자에서 멈추고, `-AllowCommit`도 실제 사용하지 않는 상태입니다. 커밋 스크립트와 task 완료 스크립트의 파라미터/게이트
--를 확인한 뒤 full cycle에 최소 연결만 추가하겠습니다.
--exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path scr
--ipts/ai-dev-complete-task.ps1 -Pattern "param|ResultSummary|TaskId|loop-log|queue|state" -Context 
--0,3' in D:\ai-apps\planpilot-local
+-`ai-dev-auto-goal.ps1` 파일이 이미 존재합니다. 새로 만들기보다 현재 구현이 T007 완료 기준을 충족하는지 확인하고, 부족한 부분만 좁게 수정하겠습니다.
 -exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path scr
--ipts/ai-dev-save-review.ps1 -Pattern "lastReview|decision|next_step|state" -Context 0,3' in D:\ai-
--apps\planpilot-local
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
+h
+- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1'" in D:\ai-apps\planpilot-local
 -exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path scr
--ipts/ai-dev-commit.ps1 -Pattern "param|Files|DryRun|package|lastCommandStatus|lastReview|AllowWith
--out" -Context 0,3' in D:\ai-apps\planpilot-local
-- succeeded in 2602ms:
--
--> scripts\ai-dev-complete-task.ps1:1:param(
--> scripts\ai-dev-complete-task.ps1:2:    [string]$TaskId,
--> scripts\ai-dev-complete-task.ps1:3:    [string]$ResultSummary,
--  scripts\ai-dev-complete-task.ps1:4:    [switch]$NoNext
--  scripts\ai-dev-complete-task.ps1:5:)
--  scripts\ai-dev-complete-task.ps1:6:
--> scripts\ai-dev-complete-task.ps1:10:$queueRelativePath = ".ai-dev/queue.json"
--> scripts\ai-dev-complete-task.ps1:11:$stateRelativePath = ".ai-dev/state.json"
--> scripts\ai-dev-complete-task.ps1:12:$loopLogRelativePath = ".ai-dev/loop-log.md"
--  scripts\ai-dev-complete-task.ps1:13:
--> scripts\ai-dev-complete-task.ps1:14:$queuePath = Join-Path $projectRoot $queueRelativePath
--> scripts\ai-dev-complete-task.ps1:15:$statePath = Join-Path $projectRoot $stateRelativePath
--  scripts\ai-dev-complete-task.ps1:16:$loopLogPath = Join-Path $projectRoot $loopLogRelativePath
--  scripts\ai-dev-complete-task.ps1:17:$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
--  scripts\ai-dev-complete-task.ps1:18:
--> scripts\ai-dev-complete-task.ps1:20:    param(
--  scripts\ai-dev-complete-task.ps1:21:        [string]$Message
--  scripts\ai-dev-complete-task.ps1:22:    )
--  scripts\ai-dev-complete-task.ps1:23:
--> scripts\ai-dev-complete-task.ps1:29:    param(
--  scripts\ai-dev-complete-task.ps1:30:        [string]$Path,
--  scripts\ai-dev-complete-task.ps1:31:        [string]$RelativePath
--  scripts\ai-dev-complete-task.ps1:32:    )
--> scripts\ai-dev-complete-task.ps1:42:    param(
--  scripts\ai-dev-complete-task.ps1:43:        [object]$Value
--  scripts\ai-dev-complete-task.ps1:44:    )
--  scripts\ai-dev-complete-task.ps1:45:
--> scripts\ai-dev-complete-task.ps1:58:    param(
--  scripts\ai-dev-complete-task.ps1:59:        [object]$InputObject,
--  scripts\ai-dev-complete-task.ps1:60:        [string]$Name,
--  scripts\ai-dev-complete-task.ps1:61:        [object]$Value
--> scripts\ai-dev-complete-task.ps1:72:    param(
--  scripts\ai-dev-complete-task.ps1:73:        [string]$Path,
--  scripts\ai-dev-complete-task.ps1:74:        [object]$Value
--  scripts\ai-dev-complete-task.ps1:75:    )
--> scripts\ai-dev-complete-task.ps1:81:foreach ($requiredPath in @($queueRelativePath, $stateRelati
--vePath, $loopLogRelat
--ivePath)) {
--  scripts\ai-dev-complete-task.ps1:82:    $fullPath = Join-Path $projectRoot $requiredPath
--  scripts\ai-dev-complete-task.ps1:83:
--  scripts\ai-dev-complete-task.ps1:84:    if (-not (Test-Path -LiteralPath $fullPath -PathType Lea
--f)) {
--> scripts\ai-dev-complete-task.ps1:89:$queue = Read-JsonFile $queuePath $queueRelativePath
--> scripts\ai-dev-complete-task.ps1:90:$state = Read-JsonFile $statePath $stateRelativePath
--  scripts\ai-dev-complete-task.ps1:91:
--> scripts\ai-dev-complete-task.ps1:92:if (-not ($queue.PSObject.Properties.Name -contains "tasks")
--) {
--> scripts\ai-dev-complete-task.ps1:93:    Stop-WithError "$queueRelativePath 필수 필드가 누락되었습니다: tasks
--"
--  scripts\ai-dev-complete-task.ps1:94:}
--  scripts\ai-dev-complete-task.ps1:95:
--> scripts\ai-dev-complete-task.ps1:96:if ($null -eq $queue.tasks -or -not ($queue.tasks -is [Syste
--m.Collections.IEnumer
--able])) {
--> scripts\ai-dev-complete-task.ps1:97:    Stop-WithError "$queueRelativePath tasks 필드는 배열이어야 합니다."
--  scripts\ai-dev-complete-task.ps1:98:}
--  scripts\ai-dev-complete-task.ps1:99:
--> scripts\ai-dev-complete-task.ps1:100:$tasks = @($queue.tasks)
--> scripts\ai-dev-complete-task.ps1:101:$targetTaskId = $null
--  scripts\ai-dev-complete-task.ps1:102:
--> scripts\ai-dev-complete-task.ps1:103:if (Test-HasValue $TaskId) {
--> scripts\ai-dev-complete-task.ps1:104:    $targetTaskId = $TaskId
--> scripts\ai-dev-complete-task.ps1:105:} elseif (Test-HasValue $state.currentTaskId) {
--> scripts\ai-dev-complete-task.ps1:106:    $targetTaskId = [string]$state.currentTaskId
--> scripts\ai-dev-complete-task.ps1:107:} elseif (Test-HasValue $queue.currentTaskId) {
--> scripts\ai-dev-complete-task.ps1:108:    $targetTaskId = [string]$queue.currentTaskId
--  scripts\ai-dev-complete-task.ps1:109:} else {
--  scripts\ai-dev-complete-task.ps1:110:    $fallbackTask = $tasks |
--  scripts\ai-dev-complete-task.ps1:111:        Where-Object { $_.status -eq "in_progress" -or $_.s
--tatus -eq "pending" }
-- |
--> scripts\ai-dev-complete-task.ps1:115:        $targetTaskId = [string]$fallbackTask.id
--  scripts\ai-dev-complete-task.ps1:116:    }
--  scripts\ai-dev-complete-task.ps1:117:}
--  scripts\ai-dev-complete-task.ps1:118:
--> scripts\ai-dev-complete-task.ps1:119:if (-not (Test-HasValue $targetTaskId)) {
--  scripts\ai-dev-complete-task.ps1:120:    Stop-WithError "완료 처리할 task가 없습니다."
--  scripts\ai-dev-complete-task.ps1:121:}
--  scripts\ai-dev-complete-task.ps1:122:
--> scripts\ai-dev-complete-task.ps1:123:$completedTask = $tasks | Where-Object { $_.id -eq $targetT
--askId } | Select-Obje
--ct -First 1
--  scripts\ai-dev-complete-task.ps1:124:
--  scripts\ai-dev-complete-task.ps1:125:if ($null -eq $completedTask) {
--> scripts\ai-dev-complete-task.ps1:126:    Stop-WithError "완료 처리할 task를 찾을 수 없습니다: $targetTaskId"
--  scripts\ai-dev-complete-task.ps1:127:}
--  scripts\ai-dev-complete-task.ps1:128:
--  scripts\ai-dev-complete-task.ps1:129:$now = [DateTimeOffset]::UtcNow.ToString("o")
--> scripts\ai-dev-complete-task.ps1:130:$summary = if (Test-HasValue $ResultSummary) { $ResultSumma
--ry } else { "완료 처리됨" 
--}
--  scripts\ai-dev-complete-task.ps1:131:
--  scripts\ai-dev-complete-task.ps1:132:Set-ObjectProperty $completedTask "status" "done"
--  scripts\ai-dev-complete-task.ps1:133:Set-ObjectProperty $completedTask "completedAt" $now
--> scripts\ai-dev-complete-task.ps1:135:if (Test-HasValue $ResultSummary) {
--> scripts\ai-dev-complete-task.ps1:136:    Set-ObjectProperty $completedTask "resultSummary" $Resu
--ltSummary
--  scripts\ai-dev-complete-task.ps1:137:}
--  scripts\ai-dev-complete-task.ps1:138:
--  scripts\ai-dev-complete-task.ps1:139:$nextTask = $null
--> scripts\ai-dev-complete-task.ps1:146:    Set-ObjectProperty $queue "currentTaskId" $null
--> scripts\ai-dev-complete-task.ps1:147:    Set-ObjectProperty $state "currentTaskId" $null
--> scripts\ai-dev-complete-task.ps1:148:    Set-ObjectProperty $state "goalStatus" "in_progress"
--> scripts\ai-dev-complete-task.ps1:149:    Set-ObjectProperty $state "stopReason" "manual_next_sel
--ection_required"
--  scripts\ai-dev-complete-task.ps1:150:} elseif ($null -ne $nextTask) {
--  scripts\ai-dev-complete-task.ps1:151:    Set-ObjectProperty $nextTask "status" "in_progress"
--> scripts\ai-dev-complete-task.ps1:152:    Set-ObjectProperty $queue "currentTaskId" $nextTask.id
--> scripts\ai-dev-complete-task.ps1:153:    Set-ObjectProperty $state "currentTaskId" $nextTask.id
--> scripts\ai-dev-complete-task.ps1:154:    Set-ObjectProperty $state "goalStatus" "in_progress"
--> scripts\ai-dev-complete-task.ps1:155:    Set-ObjectProperty $state "stopReason" $null
--  scripts\ai-dev-complete-task.ps1:156:} else {
--> scripts\ai-dev-complete-task.ps1:157:    Set-ObjectProperty $queue "currentTaskId" $null
--> scripts\ai-dev-complete-task.ps1:158:    Set-ObjectProperty $state "currentTaskId" $null
--> scripts\ai-dev-complete-task.ps1:159:    Set-ObjectProperty $state "goalStatus" "completed"
--> scripts\ai-dev-complete-task.ps1:160:    Set-ObjectProperty $state "stopReason" "all_tasks_compl
--eted"
--  scripts\ai-dev-complete-task.ps1:161:}
--  scripts\ai-dev-complete-task.ps1:162:
--> scripts\ai-dev-complete-task.ps1:163:Set-ObjectProperty $queue "updatedAt" $now
--> scripts\ai-dev-complete-task.ps1:164:Set-ObjectProperty $state "updatedAt" $now
--> scripts\ai-dev-complete-task.ps1:165:Set-ObjectProperty $state "lastCommand" "complete-task"
--> scripts\ai-dev-complete-task.ps1:166:Set-ObjectProperty $state "lastCommandStatus" "passed"
--> scripts\ai-dev-complete-task.ps1:167:Set-ObjectProperty $state "lastErrorSummary" ""
--  scripts\ai-dev-complete-task.ps1:168:
--> scripts\ai-dev-complete-task.ps1:169:Write-JsonFile $queuePath $queue
--> scripts\ai-dev-complete-task.ps1:170:Write-JsonFile $statePath $state
--  scripts\ai-dev-complete-task.ps1:171:
--  scripts\ai-dev-complete-task.ps1:172:$nextTaskText = if ($null -ne $nextTask) {
--  scripts\ai-dev-complete-task.ps1:173:    "$($nextTask.id) $($nextTask.title)"
--> scripts\ai-dev-complete-task.ps1:192:Write-Host "queue/state 저장 완료"
--
--
--
-- succeeded in 2771ms:
--
--> scripts\ai-dev-commit.ps1:1:param(
--  scripts\ai-dev-commit.ps1:2:    [string]$Message,
--> scripts\ai-dev-commit.ps1:3:    [string[]]$Files,
--> scripts\ai-dev-commit.ps1:4:    [switch]$AllowWithoutPassedCheck,
--> scripts\ai-dev-commit.ps1:5:    [switch]$AllowWithoutPassedReview,
--> scripts\ai-dev-commit.ps1:6:    [switch]$DryRun
--  scripts\ai-dev-commit.ps1:7:)
--  scripts\ai-dev-commit.ps1:8:
--  scripts\ai-dev-commit.ps1:9:. "$PSScriptRoot\ai-dev-env.ps1"
--> scripts\ai-dev-commit.ps1:22:    param(
--  scripts\ai-dev-commit.ps1:23:        [string]$Message
--  scripts\ai-dev-commit.ps1:24:    )
--  scripts\ai-dev-commit.ps1:25:
--> scripts\ai-dev-commit.ps1:31:    param(
--  scripts\ai-dev-commit.ps1:32:        [string]$Path,
--  scripts\ai-dev-commit.ps1:33:        [string]$RelativePath
--  scripts\ai-dev-commit.ps1:34:    )
--> scripts\ai-dev-commit.ps1:44:    param(
--  scripts\ai-dev-commit.ps1:45:        [object]$Value
--  scripts\ai-dev-commit.ps1:46:    )
--  scripts\ai-dev-commit.ps1:47:
--> scripts\ai-dev-commit.ps1:60:    param(
--  scripts\ai-dev-commit.ps1:61:        [object]$InputObject,
--  scripts\ai-dev-commit.ps1:62:        [string]$Name,
--  scripts\ai-dev-commit.ps1:63:        [object]$Value
--> scripts\ai-dev-commit.ps1:74:    param(
--  scripts\ai-dev-commit.ps1:75:        [object]$State,
--  scripts\ai-dev-commit.ps1:76:        [string]$Status,
--  scripts\ai-dev-commit.ps1:77:        [string]$ErrorSummary,
--> scripts\ai-dev-commit.ps1:82:    Set-ObjectProperty $State "lastCommandStatus" $Status
--  scripts\ai-dev-commit.ps1:83:    Set-ObjectProperty $State "lastErrorSummary" $ErrorSummary
--  scripts\ai-dev-commit.ps1:84:    Set-ObjectProperty $State "updatedAt" ([DateTimeOffset]::UtcNow
--.ToString("o"))
--  scripts\ai-dev-commit.ps1:85:
--> scripts\ai-dev-commit.ps1:95:    param(
--  scripts\ai-dev-commit.ps1:96:        [string[]]$Arguments,
--  scripts\ai-dev-commit.ps1:97:        [string]$DisplayName
--  scripts\ai-dev-commit.ps1:98:    )
--> scripts\ai-dev-commit.ps1:166:if (-not $AllowWithoutPassedCheck -and $state.lastCommandStatus -n
--e "passed") {
--> scripts\ai-dev-commit.ps1:167:    Stop-WithError "state.lastCommandStatus가 passed가 아닙니다. 검증 결과를 
--확인하거나 -AllowWithoutPa
--ssedCheck를 사용하세요."
--  scripts\ai-dev-commit.ps1:168:}
--  scripts\ai-dev-commit.ps1:169:
--> scripts\ai-dev-commit.ps1:170:if (-not $AllowWithoutPassedReview -and $state.lastReviewDecision 
---ne "pass") {
--> scripts\ai-dev-commit.ps1:171:    Stop-WithError "state.lastReviewDecision이 pass가 아닙니다. 리뷰 결과를 확
--인하거나 -AllowWithoutPas
--sedReview를 사용하세요."
--  scripts\ai-dev-commit.ps1:172:}
--  scripts\ai-dev-commit.ps1:173:
--  scripts\ai-dev-commit.ps1:174:try {
--> scripts\ai-dev-commit.ps1:187:$selectedFiles = @()
--  scripts\ai-dev-commit.ps1:188:$targetChangeLines = $changeLines
--  scripts\ai-dev-commit.ps1:189:
--> scripts\ai-dev-commit.ps1:190:if ($null -ne $Files -and $Files.Count -gt 0) {
--> scripts\ai-dev-commit.ps1:191:    $requestedFiles = @($Files | ForEach-Object { $_ -split "," } 
--| Where-Object { -not
-- [string]::IsNullOrWhiteSpace($_) })
--  scripts\ai-dev-commit.ps1:192:
--> scripts\ai-dev-commit.ps1:193:    if ($requestedFiles.Count -eq 0) {
--> scripts\ai-dev-commit.ps1:194:        Stop-WithError "-Files에는 비어 있지 않은 파일 경로만 지정할 수 있습니다."
--  scripts\ai-dev-commit.ps1:195:    }
--  scripts\ai-dev-commit.ps1:196:
--> scripts\ai-dev-commit.ps1:197:    foreach ($file in $requestedFiles) {
--  scripts\ai-dev-commit.ps1:198:        if ([string]::IsNullOrWhiteSpace($file)) {
--> scripts\ai-dev-commit.ps1:199:            Stop-WithError "-Files에는 비어 있지 않은 파일 경로만 지정할 수 있습니다."
--  scripts\ai-dev-commit.ps1:200:        }
--  scripts\ai-dev-commit.ps1:201:
--  scripts\ai-dev-commit.ps1:202:        $normalizedFile = $file.Replace('\', '/')
--> scripts\ai-dev-commit.ps1:215:            $fileStatus = Invoke-GitCapture -Arguments @("status",
-- "--porcelain", "--",
-- $normalizedFile) -DisplayName "git status --porcelain -- $normalizedFile"
--  scripts\ai-dev-commit.ps1:216:        } catch {
--  scripts\ai-dev-commit.ps1:217:            Stop-WithError $_.Exception.Message
--  scripts\ai-dev-commit.ps1:218:        }
--> scripts\ai-dev-commit.ps1:220:        if ([string]::IsNullOrWhiteSpace($fileStatus)) {
--  scripts\ai-dev-commit.ps1:221:            Stop-WithError "선택한 파일에 커밋할 변경사항이 없습니다: $file"
--  scripts\ai-dev-commit.ps1:222:        }
--  scripts\ai-dev-commit.ps1:223:
--> scripts\ai-dev-commit.ps1:224:        $selectedFiles += $normalizedFile
--  scripts\ai-dev-commit.ps1:225:    }
--  scripts\ai-dev-commit.ps1:226:
--> scripts\ai-dev-commit.ps1:227:    $selectedFiles = @($selectedFiles | Select-Object -Unique)
--> scripts\ai-dev-commit.ps1:228:    $targetChangeLines = @($selectedFiles)
--  scripts\ai-dev-commit.ps1:229:
--  scripts\ai-dev-commit.ps1:230:    try {
--> scripts\ai-dev-commit.ps1:231:        $stagedFiles = Invoke-GitCapture -Arguments @("diff", "--c
--ached", "--name-only"
--) -DisplayName "git diff --cached --name-only"
--  scripts\ai-dev-commit.ps1:232:    } catch {
--  scripts\ai-dev-commit.ps1:233:        Stop-WithError $_.Exception.Message
--  scripts\ai-dev-commit.ps1:234:    }
--> scripts\ai-dev-commit.ps1:236:    $stagedFileList = @($stagedFiles -split "`r?`n" | Where-Object
-- { -not [string]::IsN
--ullOrWhiteSpace($_) })
--> scripts\ai-dev-commit.ps1:237:    $unexpectedStagedFiles = @($stagedFileList | Where-Object { $s
--electedFiles -notcont
--ains $_ })
--  scripts\ai-dev-commit.ps1:238:
--> scripts\ai-dev-commit.ps1:239:    if ($unexpectedStagedFiles.Count -gt 0) {
--> scripts\ai-dev-commit.ps1:240:        Stop-WithError "선택 파일 외에 이미 staged 된 파일이 있습니다: $($unexpect
--edStagedFiles -join '
--, ')"
--  scripts\ai-dev-commit.ps1:241:    }
--  scripts\ai-dev-commit.ps1:242:}
--  scripts\ai-dev-commit.ps1:243:
--> scripts\ai-dev-commit.ps1:262:if ($DryRun) {
--  scripts\ai-dev-commit.ps1:263:    Write-Host "Dry run: git add/commit을 실행하지 않습니다."
--  scripts\ai-dev-commit.ps1:264:    Write-Host "커밋 메시지: $commitMessage"
--  scripts\ai-dev-commit.ps1:265:    Write-Host "현재 task: $taskText"
--> scripts\ai-dev-commit.ps1:275:    if ($selectedFiles.Count -gt 0) {
--> scripts\ai-dev-commit.ps1:276:        $addOutput = & git add -- @selectedFiles 2>&1 | Out-String
--> scripts\ai-dev-commit.ps1:277:        $addDisplayName = "git add -- <selected files>"
--  scripts\ai-dev-commit.ps1:278:    } else {
--  scripts\ai-dev-commit.ps1:279:        $addOutput = & git add -A 2>&1 | Out-String
--  scripts\ai-dev-commit.ps1:280:        $addDisplayName = "git add -A"
--> scripts\ai-dev-commit.ps1:287:    if ($selectedFiles.Count -gt 0) {
--> scripts\ai-dev-commit.ps1:288:        $stagedFilesAfterAdd = Invoke-GitCapture -Arguments @("dif
--f", "--cached", "--na
--me-only") -DisplayName "git diff --cached --name-only"
--> scripts\ai-dev-commit.ps1:289:        $stagedFileListAfterAdd = @($stagedFilesAfterAdd -split "`
--r?`n" | Where-Object 
--{ -not [string]::IsNullOrWhiteSpace($_) })
--> scripts\ai-dev-commit.ps1:290:        $unexpectedStagedFilesAfterAdd = @($stagedFileListAfterAdd
-- | Where-Object { $se
--lectedFiles -notcontains $_ })
--  scripts\ai-dev-commit.ps1:291:
--> scripts\ai-dev-commit.ps1:292:        if ($unexpectedStagedFilesAfterAdd.Count -gt 0) {
--> scripts\ai-dev-commit.ps1:293:            throw "선택 파일 외의 staged 파일이 감지되었습니다: $($unexpectedStage
--dFilesAfterAdd -join 
--', ')"
--  scripts\ai-dev-commit.ps1:294:        }
--  scripts\ai-dev-commit.ps1:295:    }
--  scripts\ai-dev-commit.ps1:296:
--
--
--
-- succeeded in 2836ms:
--
--> scripts\ai-dev-save-review.ps1:11:$stateRelativePath = ".ai-dev/state.json"
--  scripts\ai-dev-save-review.ps1:12:$reviewRelativePath = ".ai-dev/review.md"
--> scripts\ai-dev-save-review.ps1:13:$statePath = Join-Path $projectRoot $stateRelativePath
--  scripts\ai-dev-save-review.ps1:14:$reviewPath = Join-Path $projectRoot $reviewRelativePath
--  scripts\ai-dev-save-review.ps1:15:$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
--  scripts\ai-dev-save-review.ps1:16:$codeFence = '```'
--> scripts\ai-dev-save-review.ps1:70:function Save-StateReviewResult {
--  scripts\ai-dev-save-review.ps1:71:    param(
--> scripts\ai-dev-save-review.ps1:72:        [object]$State,
--> scripts\ai-dev-save-review.ps1:73:        [string]$Decision,
--  scripts\ai-dev-save-review.ps1:74:        [string]$Severity,
--  scripts\ai-dev-save-review.ps1:75:        [string]$CommandStatus,
--  scripts\ai-dev-save-review.ps1:76:        [string]$ErrorSummary
--> scripts\ai-dev-save-review.ps1:79:    Set-ObjectProperty $State "lastReviewDecision" $Decision
--> scripts\ai-dev-save-review.ps1:80:    Set-ObjectProperty $State "lastReviewSeverity" $Severity
--> scripts\ai-dev-save-review.ps1:81:    Set-ObjectProperty $State "lastCommand" "save-review"
--> scripts\ai-dev-save-review.ps1:82:    Set-ObjectProperty $State "lastCommandStatus" $CommandStat
--us
--> scripts\ai-dev-save-review.ps1:83:    Set-ObjectProperty $State "lastErrorSummary" $ErrorSummary
--> scripts\ai-dev-save-review.ps1:84:    Set-ObjectProperty $State "updatedAt" ([DateTimeOffset]::U
--tcNow.ToString("o"))
--  scripts\ai-dev-save-review.ps1:85:
--> scripts\ai-dev-save-review.ps1:86:    $stateJson = $State | ConvertTo-Json -Depth 20
--> scripts\ai-dev-save-review.ps1:87:    [System.IO.File]::WriteAllText($statePath, $stateJson, $ut
--f8WithBom)
--  scripts\ai-dev-save-review.ps1:88:}
--  scripts\ai-dev-save-review.ps1:89:
--  scripts\ai-dev-save-review.ps1:90:function Get-ReviewInput {
--> scripts\ai-dev-save-review.ps1:108:        Write-Host '  powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-save
---review.ps1 -ReviewJson ''{"decision":"pass","severity":"none","summary":"OK","required_changes":[
--],"optional_suggestio
--ns":[],"next_step":"complete_task"}'''
--  scripts\ai-dev-save-review.ps1:109:        Write-Host "  powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-save
---review.ps1 -FromClipboard"
--  scripts\ai-dev-save-review.ps1:110:        Stop-WithError "리뷰 입력을 지정해야 합니다."
--  scripts\ai-dev-save-review.ps1:111:    }
--> scripts\ai-dev-save-review.ps1:282:        "decision",
--  scripts\ai-dev-save-review.ps1:283:        "severity",
--  scripts\ai-dev-save-review.ps1:284:        "summary",
--  scripts\ai-dev-save-review.ps1:285:        "required_changes",
--> scripts\ai-dev-save-review.ps1:287:        "next_step"
--  scripts\ai-dev-save-review.ps1:288:    )
--  scripts\ai-dev-save-review.ps1:289:
--  scripts\ai-dev-save-review.ps1:290:    foreach ($field in $requiredFields) {
--> scripts\ai-dev-save-review.ps1:300:    if (@("pass", "revise", "blocked") -notcontains $Review.d
--ecision) {
--> scripts\ai-dev-save-review.ps1:301:        $errors += "허용되지 않은 decision: $($Review.decision)"
--  scripts\ai-dev-save-review.ps1:302:    }
--  scripts\ai-dev-save-review.ps1:303:
--  scripts\ai-dev-save-review.ps1:304:    if (@("none", "low", "medium", "high", "critical") -notco
--ntains $Review.severi
--ty) {
--> scripts\ai-dev-save-review.ps1:308:    if (@("complete_task", "revise_with_codex", "stop_for_use
--r") -notcontains $Rev
--iew.next_step) {
--> scripts\ai-dev-save-review.ps1:309:        $errors += "허용되지 않은 next_step: $($Review.next_step)"
--  scripts\ai-dev-save-review.ps1:310:    }
--  scripts\ai-dev-save-review.ps1:311:
--  scripts\ai-dev-save-review.ps1:312:    if ($Review.required_changes -isnot [System.Collections.I
--Enumerable] -or $Revi
--ew.required_changes -is [string]) {
--> scripts\ai-dev-save-review.ps1:362:        [object]$State,
--  scripts\ai-dev-save-review.ps1:363:        [string]$RawInput,
--  scripts\ai-dev-save-review.ps1:364:        [string]$ErrorSummary
--  scripts\ai-dev-save-review.ps1:365:    )
--> scripts\ai-dev-save-review.ps1:373:- Decision: blocked
--  scripts\ai-dev-save-review.ps1:374:- Severity: critical
--  scripts\ai-dev-save-review.ps1:375:- Next step: stop_for_user
--  scripts\ai-dev-save-review.ps1:376:- Summary: 리뷰 JSON을 파싱하거나 검증하지 못했습니다.
--> scripts\ai-dev-save-review.ps1:390:    Save-StateReviewResult $State "blocked" "critical" "faile
--d" $ErrorSummary
--  scripts\ai-dev-save-review.ps1:391:}
--  scripts\ai-dev-save-review.ps1:392:
--  scripts\ai-dev-save-review.ps1:393:function Stop-WithInvalidReviewInput {
--> scripts\ai-dev-save-review.ps1:395:        [object]$State,
--  scripts\ai-dev-save-review.ps1:396:        [string]$RawInput,
--  scripts\ai-dev-save-review.ps1:397:        [string]$ErrorSummary
--  scripts\ai-dev-save-review.ps1:398:    )
--> scripts\ai-dev-save-review.ps1:401:        Save-InvalidReview $State $RawInput $ErrorSummary
--  scripts\ai-dev-save-review.ps1:402:        Stop-WithError "리뷰 JSON 저장에 실패했습니다: $ErrorSummary"
--  scripts\ai-dev-save-review.ps1:403:    }
--  scripts\ai-dev-save-review.ps1:404:
--> scripts\ai-dev-save-review.ps1:405:    Write-Host "리뷰 입력 오류로 review.md와 state.json을 수정하지 않았습니다."
--  scripts\ai-dev-save-review.ps1:406:    Write-Host "실패 리뷰를 명시적으로 기록하려면 -WriteFailureReview 옵션을 사용
--하세요."
--  scripts\ai-dev-save-review.ps1:407:    Stop-WithError "리뷰 JSON 저장에 실패했습니다: $ErrorSummary"
--  scripts\ai-dev-save-review.ps1:408:}
--> scripts\ai-dev-save-review.ps1:410:if (-not (Test-Path -LiteralPath $statePath -PathType Leaf)) 
--{
--> scripts\ai-dev-save-review.ps1:411:    Stop-WithError "필수 파일이 없습니다: $stateRelativePath"
--  scripts\ai-dev-save-review.ps1:412:}
--  scripts\ai-dev-save-review.ps1:413:
--> scripts\ai-dev-save-review.ps1:414:$state = Read-JsonFile $statePath $stateRelativePath
--  scripts\ai-dev-save-review.ps1:415:$rawReview = Get-ReviewInput
--  scripts\ai-dev-save-review.ps1:416:$inputPreview = Get-InputPreview $rawReview
--  scripts\ai-dev-save-review.ps1:417:
--> scripts\ai-dev-save-review.ps1:420:    Stop-WithInvalidReviewInput $state $rawReview $errorSumma
--ry
--  scripts\ai-dev-save-review.ps1:421:}
--  scripts\ai-dev-save-review.ps1:422:
--  scripts\ai-dev-save-review.ps1:423:try {
--> scripts\ai-dev-save-review.ps1:433:    Stop-WithInvalidReviewInput $state $rawReview $errorSumma
--ry
--  scripts\ai-dev-save-review.ps1:434:}
--  scripts\ai-dev-save-review.ps1:435:
--  scripts\ai-dev-save-review.ps1:436:$prettyJson = $review | ConvertTo-Json -Depth 20
--> scripts\ai-dev-save-review.ps1:446:- Decision: $($review.decision)
--  scripts\ai-dev-save-review.ps1:447:- Severity: $($review.severity)
--> scripts\ai-dev-save-review.ps1:448:- Next step: $($review.next_step)
--  scripts\ai-dev-save-review.ps1:449:- Summary: $($review.summary)
--  scripts\ai-dev-save-review.ps1:450:
--  scripts\ai-dev-save-review.ps1:451:### Required Changes
--> scripts\ai-dev-save-review.ps1:467:Save-StateReviewResult $state $review.decision $review.severi
--ty "passed" ""
--  scripts\ai-dev-save-review.ps1:468:
--> scripts\ai-dev-save-review.ps1:469:$nextAction = if ($review.decision -eq "pass" -and $review.ne
--xt_step -eq "complete
--_task") {
--  scripts\ai-dev-save-review.ps1:470:    "pass + complete_task: 다음 명령을 실행해 현재 task 완료 처리를 검토하세요. p
--owershell -ExecutionP
--olicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 통과`""
--> scripts\ai-dev-save-review.ps1:471:} elseif ($review.decision -eq "revise" -and $review.next_ste
--p -eq "revise_with_co
--dex") {
--  scripts\ai-dev-save-review.ps1:472:    "revise + make_revise_prompt: 다음 명령으로 재수정 프롬프트를 생성한 뒤 Cod
--ex/Cline에 전달하세요. powe
--rshell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1"
--> scripts\ai-dev-save-review.ps1:473:} elseif ($review.decision -eq "blocked" -or $review.next_ste
--p -eq "stop_for_user"
--) {
--  scripts\ai-dev-save-review.ps1:474:    "blocked/stop_for_user: 사용자 판단이 필요합니다. 자동 진행을 중단하고 review
--.md의 Required Changes
--와 Summary를 확인하세요."
--  scripts\ai-dev-save-review.ps1:475:} else {
--> scripts\ai-dev-save-review.ps1:476:    "decision과 next_step 조합을 확인한 뒤 다음 행동을 결정하세요."
--  scripts\ai-dev-save-review.ps1:477:}
--  scripts\ai-dev-save-review.ps1:478:
--  scripts\ai-dev-save-review.ps1:479:Write-Host "리뷰 저장 완료: $reviewRelativePath"
--> scripts\ai-dev-save-review.ps1:480:Write-Host "Decision: $($review.decision)"
--  scripts\ai-dev-save-review.ps1:481:Write-Host "Severity: $($review.severity)"
--> scripts\ai-dev-save-review.ps1:482:Write-Host "Next step: $($review.next_step)"
--  scripts\ai-dev-save-review.ps1:483:Write-Host "안내: $nextAction"
--
--
-+ succeeded in 2823ms:
-+backlog.md
-+codex-result.md
-+codex-review-result.md
-+current-task-prompt.md
-+diff.md
-+extra-instructions.example.md
-+goal.md
-+loop-log.md
-+plan.md
-+queue.example.json
-+queue.json
-+README.md
-+review-prompt.md
-+review-response.json
-+review.md
-+state.example.json
-+state.json
-+test-result.md
- 
-+codex
-+`ai-dev-auto-goal.ps1` 파일이 이미 존재합니다. 새로 만들기보다 현재 구현이 T007 완료 기준을 충족하는지 확인하고, 부족한 부분만 좁게 수정하겠습니다.
- exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path .ai-d
--ev/queue.json' in D:\ai-apps\planpilot-local
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
 h
-+ 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1'" in D:\ai-apps\planpilot-local
- exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path scrip
--ts/ai-dev-commit.ps1 | Select-Object -First 340' in D:\ai-apps\planpilot-local
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
+- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-make-prompt.ps1'" in D:\ai-apps\planpilot-local
+-exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
 h
-+ 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-make-prompt.ps1'" in D:\ai-apps\planpilot-local
- exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path .ai-d
--ev/state.json' in D:\ai-apps\planpilot-local
-- succeeded in 4378ms:
--{
--    "goalTitle":  "AI Dev Loop Codex CLI 완전 자동화 도입",
--    "goalSource":  ".ai-dev/goal.md",
--    "createdAt":  "2026-06-07T19:59:48.1282711+09:00",
--    "updatedAt":  "2026-06-07T13:57:13.9217915+00:00",
--    "currentTaskId":  "T005",
--    "tasks":  [
--                  {
--                      "id":  "T001",
--                      "title":  "Codex CLI 완전 자동화 정책 문서화",
--                      "description":  "Codex CLI를 구현자와 리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전 중단 조건을 문서
--화한다.",
--                      "type":  "documentation",
--                      "status":  "done",
--                      "priority":  "P0",
--                      "dependsOn":  [
--
--                                    ],
--                      "filesLikelyToChange":  [
--                                                  "docs/ai-dev-loop-policy.md",
--                                                  ".ai-dev/README.md",
--                                                  ".ai-dev/plan.md"
--                                              ],
--                      "verification":  [
--                                           "Codex CLI 사용 방식과 안전 기준이 문서화되었는지 확인",
--                                           "Cline/Copilot/GPT API 없이 진행하는 구조가 명확한지 확인"
--                                       ],
--                      "commitMessage":  "Document Codex CLI automation policy",
--                      "completedAt":  "2026-06-07T12:46:13.9691878+00:00",
--                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전
-- 중단 조건 문서화"
--                  },
--                  {
--                      "id":  "T002",
--                      "title":  "Codex 구현 실행 스크립트 추가",
--                      "description":  "current-task-prompt.md를 Codex CLI에 전달해 현재 task 구현을 수행하는 ai-
--dev-run-codex.ps1을 추가한다.",
--                      "type":  "implementation",
--                      "status":  "done",
--                      "priority":  "P0",
--                      "dependsOn":  [
--                                        "T001"
--                                    ],
--                      "filesLikelyToChange":  [
--                                                  "scripts/ai-dev-run-codex.ps1",
--                                                  ".ai-dev/README.md"
--                                              ],
--                      "verification":  [
--                                           "DryRun에서 실행 예정 명령만 출력되는지 확인",
--                                           "current-task-prompt.md가 없으면 명확히 안내하는지 확인",
--                                           "git status dirty 상태에서 기본 중단하는지 확인",
--                                           "PowerShell 문법 검증"
--                                       ],
--                      "commitMessage":  "Add Codex task runner",
--                      "completedAt":  "2026-06-07T12:48:36.8786703+00:00",
--                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전
-- 중단 조건 문서화"
--                  },
--                  {
--                      "id":  "T003",
--                      "title":  "Codex 리뷰 실행 스크립트 추가",
--                      "description":  "review-prompt.md를 Codex CLI에 전달해 JSON 리뷰를 생성하고 review-respo
--nse.json 및 save-review 흐름에 연결하는 ai-dev-run-review-codex.ps1을 추가한다.",
--                      "type":  "implementation",
--                      "status":  "done",
--                      "priority":  "P0",
--                      "dependsOn":  [
--                                        "T002"
--                                    ],
--                      "filesLikelyToChange":  [
--                                                  "scripts/ai-dev-run-review-codex.ps1",
--                                                  ".ai-dev/README.md"
--                                              ],
--                      "verification":  [
--                                           "DryRun에서 실행 예정 명령만 출력되는지 확인",
--                                           "Codex 출력에서 JSON 객체를 추출해 저장하는지 확인",
--                                           "save-review와 연결되는지 확인",
--                                           "PowerShell 문법 검증"
--                                       ],
--                      "commitMessage":  "Add Codex review runner",
--                      "completedAt":  "2026-06-07T13:23:42.7240601+00:00",
--                      "resultSummary":  "T003 완료: review-prompt.md를 codex exec에 전달해 JSON 리뷰를 생성하는 
--ai-dev-run-review-codex.ps1 추가, 문법 검증과 DryRun/Json 확인"
--                  },
--                  {
--                      "id":  "T004",
--                      "title":  "full auto-cycle 초안 추가",
--                      "description":  "make-prompt, run-codex, check, save-diff, make-review-promp
--t, run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안을 추가한다.",
--                      "type":  "implementation",
--                      "status":  "done",
--                      "priority":  "P0",
--                      "dependsOn":  [
--                                        "T003"
--                                    ],
--                      "filesLikelyToChange":  [
--                                                  "scripts/ai-dev-auto-cycle-full.ps1",
--                                                  ".ai-dev/README.md"
--                                              ],
--                      "verification":  [
--                                           "DryRun에서 전체 단계가 표시되는지 확인",
--                                           "MaxTasks 제한이 있는지 확인",
--                                           "AllowCodex, AllowReviewCodex, AllowCommit 같은 명시적 옵션이 있
--는지 확인",
--                                           "PowerShell 문법 검증"
--                                       ],
--                      "commitMessage":  "Add full AI dev auto cycle draft",
--                      "completedAt":  "2026-06-07T13:57:13.9217915+00:00",
--                      "resultSummary":  "T004 완료: make-prompt/run-codex/check/save-diff/make-revie
--w-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가, DryRun/Json/AllowCodex 중단 확인"
--                  },
--                  {
--                      "id":  "T005",
--                      "title":  "자동 커밋과 task 완료 연결",
--                      "description":  "리뷰 결과가 pass일 때만 선택 파일 커밋과 complete-task를 수행하도록 full auto-cy
--cle에 연결한다.",
--                      "type":  "implementation",
--                      "status":  "in_progress",
--                      "priority":  "P0",
--                      "dependsOn":  [
--                                        "T004"
--                                    ],
--                      "filesLikelyToChange":  [
--                                                  "scripts/ai-dev-auto-cycle-full.ps1",
--                                                  ".ai-dev/README.md"
--                                              ],
--                      "verification":  [
--                                           "pass가 아니면 commit하지 않는지 확인",
--                                           "build 실패 시 commit하지 않는지 확인",
--                                           "package 파일 변경 감지 시 중단하는지 확인",
--                                           "complete-task가 pass 이후에만 실행되는지 확인",
--                                           "PowerShell 문법 검증"
--                                       ],
--                      "commitMessage":  "Connect pass review to auto commit and task completion"
--                  },
--                  {
--                      "id":  "T006",
--                      "title":  "작은 앱 task로 end-to-end 검증",
--                      "description":  "작은 문서 또는 UI 변경 task를 MaxTasks 1로 실행해 Codex 구현부터 리뷰, 커밋, 완료 
--처리까지 검증한다.",
--                      "type":  "verification",
--                      "status":  "pending",
--                      "priority":  "P0",
--                      "dependsOn":  [
--                                        "T005"
--                                    ],
--                      "filesLikelyToChange":  [
--                                                  ".ai-dev/plan.md",
--                                                  ".ai-dev/loop-log.md",
--                                                  ".ai-dev/test-result.md"
--                                              ],
--                      "verification":  [
--                                           "full auto-cycle DryRun 확인",
--                                           "MaxTasks 1 실제 실행 확인",
--                                           "Codex 구현 로그 확인",
--                                           "Codex 리뷰 JSON 저장 확인",
--                                           "pass 시 커밋/complete-task 확인",
--                                           "git status clean 확인"
--                                       ],
--                      "commitMessage":  null
--                  },
--                  {
--                      "id":  "T007",
--                      "title":  "목표 입력 기반 자동 goal 실행 스크립트 추가",
--                      "description":  "사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/q
--ueue/state를 생성하고, current-task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다
--.",
--                      "type":  "implementation",
--                      "status":  "pending",
--                      "priority":  "P0",
--                      "dependsOn":  [
--                                        "T005"
--                                    ],
--                      "filesLikelyToChange":  [
--                                                  "scripts/ai-dev-auto-goal.ps1",
--                                                  ".ai-dev/README.md"
--                                              ],
--                      "verification":  [
--                                           "DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인",
--                                           "Json 출력이 가능한지 확인",
--                                           "goal/queue/state JSON 파싱 검증을 수행하는지 확인",
--                                           "auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인
--",
--                                           "GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인",
--                                           "PowerShell 문법 검증"
--                                       ],
--                      "commitMessage":  "Add automatic goal runner"
--                  }
--              ]
--}
+- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-cycle-full.ps1'" in D:\ai-apps\planpilot-loca
+l
+- succeeded in 3092ms:
+-param(
+-    [int]$MaxTasks = 1,
+-    [int]$MaxSteps = 20,
+-    [switch]$DryRun,
+-    [switch]$Json,
+-    [switch]$AllowCodex,
+-    [switch]$AllowReviewCodex,
+-    [switch]$AllowCommit,
+-    [switch]$AllowDirty,
+-    [string[]]$CommitFiles
+-)
 -
-- succeeded in 4404ms:
--{
--    "goalStatus":  "in_progress",
--    "currentTaskId":  "T005",
--    "currentLoop":  0,
--    "maxLoopsPerTask":  2,
--    "repeatedFailureCount":  0,
--    "lastCommand":  "complete-task",
--    "lastCommandStatus":  "passed",
--    "lastErrorSummary":  "",
--    "lastReviewDecision":  "not_started",
--    "lastReviewSeverity":  "",
--    "lastCommitHash":  null,
--    "startedAt":  "2026-06-07T19:59:48.1282711+09:00",
--    "updatedAt":  "2026-06-07T13:57:13.9217915+00:00",
--    "stopReason":  null
--}
+-. $PSScriptRoot\ai-dev-env.ps1
 -
-- succeeded in 4567ms:
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
-h
-+ 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-cycle-full.ps1'" in D:\ai-apps\planpilot-loca
-l
-+ succeeded in 3092ms:
- param(
--    [string]$Message,
--    [string[]]$Files,
--    [switch]$AllowWithoutPassedCheck,
--    [switch]$AllowWithoutPassedReview,
--    [switch]$DryRun
-+    [int]$MaxTasks = 1,
-+    [int]$MaxSteps = 20,
-+    [switch]$DryRun,
-+    [switch]$Json,
-+    [switch]$AllowCodex,
-+    [switch]$AllowReviewCodex,
-+    [switch]$AllowCommit,
-+    [switch]$AllowDirty,
-+    [string[]]$CommitFiles
- )
- 
--. "$PSScriptRoot\ai-dev-env.ps1"
-+. $PSScriptRoot\ai-dev-env.ps1
- 
--$projectRoot = (Get-Location).Path
+-$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
+-$queueRelativePath = ".ai-dev/queue.json"
 -$stateRelativePath = ".ai-dev/state.json"
-+$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
- $queueRelativePath = ".ai-dev/queue.json"
--$loopLogRelativePath = ".ai-dev/loop-log.md"
+-$reviewResponseRelativePath = ".ai-dev/review-response.json"
+-$queuePath = Join-Path $repoRoot $queueRelativePath
+-$statePath = Join-Path $repoRoot $stateRelativePath
+-$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
 -
--$statePath = Join-Path $projectRoot $stateRelativePath
--$queuePath = Join-Path $projectRoot $queueRelativePath
--$loopLogPath = Join-Path $projectRoot $loopLogRelativePath
--$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-+$stateRelativePath = ".ai-dev/state.json"
-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+$queuePath = Join-Path $repoRoot $queueRelativePath
-+$statePath = Join-Path $repoRoot $stateRelativePath
-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
- 
--function Stop-WithError {
-+function Test-HasValue {
-     param(
--        [string]$Message
-+        [object]$Value
-     )
- 
--    Write-Error $Message
--    exit 1
--}
-+    if ($null -eq $Value) {
-+        return $false
-+    }
-+
-+    if ($Value -is [string]) {
-+        return -not [string]::IsNullOrWhiteSpace($Value)
-+    }
-+
-+    return $true
-+}
- 
- function Read-JsonFile {
-     param(
-@@ -2129,8038 +1209,4349 @@ function Read-JsonFile {
-     try {
-         return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
-     } catch {
--        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
-+        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
-     }
- }
- 
 -function Test-HasValue {
-+function Get-CurrentTask {
-     param(
+-    param(
 -        [object]$Value
-+        [object]$Queue,
-+        [object]$State
-     )
- 
+-    )
+-
 -    if ($null -eq $Value) {
 -        return $false
-+    $tasks = @($Queue.tasks)
-+    $currentTaskId = $null
-+
-+    if (Test-HasValue $State.currentTaskId) {
-+        $currentTaskId = [string]$State.currentTaskId
-+    } elseif (Test-HasValue $Queue.currentTaskId) {
-+        $currentTaskId = [string]$Queue.currentTaskId
-+    } else {
-+        $inProgressTask = $tasks | Where-Object { $_.status -eq "in_progress" } | Select-Object -F
-i
-+rst 1
-+
-+        if ($null -ne $inProgressTask) {
-+            $currentTaskId = [string]$inProgressTask.id
-+        } else {
-+            $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -Firs
-t
-+ 1
-+
-+            if ($null -ne $pendingTask) {
-+                $currentTaskId = [string]$pendingTask.id
-+            }
-+        }
-     }
- 
+-    }
+-
 -    if ($Value -is [string]) {
 -        return -not [string]::IsNullOrWhiteSpace($Value)
-+    if (-not (Test-HasValue $currentTaskId)) {
-+        return $null
-     }
- 
+-    }
+-
 -    return $true
-+    return $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
- }
- 
--function Set-ObjectProperty {
-+function New-StepResult {
-     param(
--        [object]$InputObject,
-+        [int]$Step,
-         [string]$Name,
--        [object]$Value
-+        [string]$Command,
-+        [bool]$Executed,
-+        [bool]$Skipped,
-+        [int]$ExitCode,
-+        [string]$Message
-     )
- 
--    if ($InputObject.PSObject.Properties.Name -contains $Name) {
--        $InputObject.$Name = $Value
+-}
+-
+-function Read-JsonFile {
+-    param(
+-        [string]$Path,
+-        [string]$RelativePath
+-    )
+-
+-    try {
+-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
+-    } catch {
+-        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
+-    }
+-}
+-
+-function Get-CurrentTask {
+-    param(
+-        [object]$Queue,
+-        [object]$State
+-    )
+-
+-    $tasks = @($Queue.tasks)
+-    $currentTaskId = $null
+-
+-    if (Test-HasValue $State.currentTaskId) {
+-        $currentTaskId = [string]$State.currentTaskId
+-    } elseif (Test-HasValue $Queue.currentTaskId) {
+-        $currentTaskId = [string]$Queue.currentTaskId
 -    } else {
--        $InputObject | Add-Member -NotePropertyName $Name -NotePropertyValue $Value
-+    return [PSCustomObject][ordered]@{
-+        step = $Step
-+        name = $Name
-+        command = $Command
-+        executed = $Executed
-+        skipped = $Skipped
-+        exitCode = $ExitCode
-+        message = $Message
-     }
- }
- 
--function Save-StateResult {
-+function New-CycleResult {
-     param(
--        [object]$State,
--        [string]$Status,
--        [string]$ErrorSummary,
--        [string]$CommitHash
-+        [object[]]$Steps,
-+        [string]$StoppedReason,
-+        [bool]$Completed,
-+        [int]$ExitCode
-     )
- 
--    Set-ObjectProperty $State "lastCommand" "commit"
--    Set-ObjectProperty $State "lastCommandStatus" $Status
--    Set-ObjectProperty $State "lastErrorSummary" $ErrorSummary
--    Set-ObjectProperty $State "updatedAt" ([DateTimeOffset]::UtcNow.ToString("o"))
--
--    if (Test-HasValue $CommitHash) {
--        Set-ObjectProperty $State "lastCommitHash" $CommitHash
-+    return [PSCustomObject][ordered]@{
-+        steps = @($Steps)
-+        stoppedReason = $StoppedReason
-+        completed = $Completed
-+        exitCode = $ExitCode
-     }
--
--    $stateJson = $State | ConvertTo-Json -Depth 20
--    [System.IO.File]::WriteAllText($statePath, $stateJson, $utf8WithBom)
- }
- 
+-        $inProgressTask = $tasks | Where-Object { $_.status -eq "in_progress" } | Select-Object -F
+i
+-rst 1
+-
+-        if ($null -ne $inProgressTask) {
+-            $currentTaskId = [string]$inProgressTask.id
+-        } else {
+-            $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -Firs
+t
+- 1
+-
+-            if ($null -ne $pendingTask) {
+-                $currentTaskId = [string]$pendingTask.id
+-            }
+-        }
+-    }
+-
+-    if (-not (Test-HasValue $currentTaskId)) {
+-        return $null
+-    }
+-
+-    return $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
+-}
+-
+-function New-StepResult {
+-    param(
+-        [int]$Step,
+-        [string]$Name,
+-        [string]$Command,
+-        [bool]$Executed,
+-        [bool]$Skipped,
+-        [int]$ExitCode,
+-        [string]$Message
+-    )
+-
+-    return [PSCustomObject][ordered]@{
+-        step = $Step
+-        name = $Name
+-        command = $Command
+-        executed = $Executed
+-        skipped = $Skipped
+-        exitCode = $ExitCode
+-        message = $Message
+-    }
+-}
+-
+-function New-CycleResult {
+-    param(
+-        [object[]]$Steps,
+-        [string]$StoppedReason,
+-        [bool]$Completed,
+-        [int]$ExitCode
+-    )
+-
+-    return [PSCustomObject][ordered]@{
+-        steps = @($Steps)
+-        stoppedReason = $StoppedReason
+-        completed = $Completed
+-        exitCode = $ExitCode
+-    }
+-}
+-
+-function Write-CycleResult {
+-    param(
+-        [object]$Result
+-    )
+-
+-    if ($Json) {
+-        $Result | ConvertTo-Json -Depth 20
+-        return
+-    }
+-
+-    foreach ($step in @($Result.steps)) {
+-        Write-Host "Step $($step.step): $($step.name)"
+-        Write-Host "  Command: $($step.command)"
+-        Write-Host "  Executed: $($step.executed)"
+-        Write-Host "  Skipped: $($step.skipped)"
+-        Write-Host "  Exit code: $($step.exitCode)"
+-        Write-Host "  Message: $($step.message)"
+-    }
+-
+-    Write-Host "Stopped reason: $($Result.stoppedReason)"
+-    Write-Host "Completed: $($Result.completed)"
+-    Write-Host "Exit code: $($Result.exitCode)"
+-
+-}
+-
+-function Stop-Cycle {
+-    param(
+-        [object[]]$Steps,
+-        [string]$StoppedReason,
+-        [bool]$Completed,
+-        [int]$ExitCode
+-    )
+-
+-    $result = New-CycleResult $Steps $StoppedReason $Completed $ExitCode
+-    Write-CycleResult $result
+-    exit $ExitCode
+-}
+-
+-function Invoke-CycleCommand {
+-    param(
+-        [int]$StepNumber,
+-        [string]$Name,
+-        [string]$Command,
+-        [string]$ScriptPath,
+-        [string[]]$Arguments
+-    )
+-
+-    if ($DryRun) {
+-        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: 하위 스크립트
+를
+- 실행하지 않았습니다."
+-        return
+-    }
+-
+-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
+-    $exitCode = $LASTEXITCODE
+-    $message = $output.Trim()
+-
+-    if ([string]::IsNullOrWhiteSpace($message)) {
+-        $message = "완료"
+-    }
+-
+-    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
+-
+-    if ($exitCode -ne 0) {
+-        Stop-Cycle $script:steps "$Name`_failed" $false 1
+-    }
+-}
+-
+-function Get-ScriptPath {
+-    param(
+-        [string]$Name
+-    )
+-
+-    $scriptPath = Join-Path $PSScriptRoot $Name
+-
+-    if (-not (Test-Path -LiteralPath $scriptPath -PathType Leaf)) {
+-        throw "필수 스크립트를 찾을 수 없습니다: scripts/$Name"
+-    }
+-
+-    return $scriptPath
+-}
+-
 -function Invoke-GitCapture {
-+function Write-CycleResult {
-     param(
+-    param(
 -        [string[]]$Arguments,
 -        [string]$DisplayName
-+        [object]$Result
-     )
- 
+-    )
+-
 -    $output = & git @Arguments 2>&1 | Out-String
 -    $exitCode = $LASTEXITCODE
 -
 -    if ($exitCode -ne 0) {
 -        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+    if ($Json) {
-+        $Result | ConvertTo-Json -Depth 20
-+        return
-     }
- 
+-    }
+-
 -    return $output.TrimEnd()
 -}
 -
--foreach ($requiredPath in @($stateRelativePath, $queueRelativePath, $loopLogRelativePath)) {
--    $fullPath = Join-Path $projectRoot $requiredPath
+-function Test-PackageFileChanged {
+-    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.jso
+n
+-") "git status --porcelain -- package.json package-lock.json"
+-    return -not [string]::IsNullOrWhiteSpace($status)
+-}
 -
--    if (-not (Test-Path -LiteralPath $fullPath -PathType Leaf)) {
--        Stop-WithError "필수 파일이 없습니다: $requiredPath"
-+    foreach ($step in @($Result.steps)) {
-+        Write-Host "Step $($step.step): $($step.name)"
-+        Write-Host "  Command: $($step.command)"
-+        Write-Host "  Executed: $($step.executed)"
-+        Write-Host "  Skipped: $($step.skipped)"
-+        Write-Host "  Exit code: $($step.exitCode)"
-+        Write-Host "  Message: $($step.message)"
-     }
+-function Get-ReviewGate {
+-    $state = Read-JsonFile $statePath $stateRelativePath
+-    $nextStep = $null
+-
+-    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
+-        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
+-
+-        if (Test-HasValue $reviewResponse.next_step) {
+-            $nextStep = [string]$reviewResponse.next_step
+-        }
+-    }
+-
+-    return [PSCustomObject][ordered]@{
+-        lastCommandStatus = [string]$state.lastCommandStatus
+-        decision = [string]$state.lastReviewDecision
+-        nextStep = $nextStep
+-    }
 -}
+-
+-function Get-CommitArguments {
+-    $arguments = @()
+-
+-    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
+-        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -not
  
--try {
--    $null = Invoke-GitCapture -Arguments @("rev-parse", "--show-toplevel") -DisplayName "git rev-p
--arse --show-toplevel"
--} catch {
--    Stop-WithError "현재 위치가 git repository가 아닙니다."
-+    Write-Host "Stopped reason: $($Result.stoppedReason)"
-+    Write-Host "Completed: $($Result.completed)"
-+    Write-Host "Exit code: $($Result.exitCode)"
-+
- }
- 
--$queue = Read-JsonFile $queuePath $queueRelativePath
--$state = Read-JsonFile $statePath $stateRelativePath
-+function Stop-Cycle {
-+    param(
-+        [object[]]$Steps,
-+        [string]$StoppedReason,
-+        [bool]$Completed,
-+        [int]$ExitCode
-+    )
- 
--if (-not ($queue.PSObject.Properties.Name -contains "tasks")) {
--    Stop-WithError "$queueRelativePath 필수 필드가 누락되었습니다: tasks"
-+    $result = New-CycleResult $Steps $StoppedReason $Completed $ExitCode
-+    Write-CycleResult $result
-+    exit $ExitCode
- }
- 
--if ($null -eq $queue.tasks -or -not ($queue.tasks -is [System.Collections.IEnumerable])) {
--    Stop-WithError "$queueRelativePath tasks 필드는 배열이어야 합니다."
--}
-+function Invoke-CycleCommand {
-+    param(
-+        [int]$StepNumber,
-+        [string]$Name,
-+        [string]$Command,
-+        [string]$ScriptPath,
-+        [string[]]$Arguments
-+    )
- 
--$tasks = @($queue.tasks)
--$currentTaskId = $null
-+    if ($DryRun) {
-+        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: 하위 스크립트
-를
-+ 실행하지 않았습니다."
-+        return
-+    }
- 
--if (Test-HasValue $state.currentTaskId) {
--    $currentTaskId = [string]$state.currentTaskId
--} elseif (Test-HasValue $queue.currentTaskId) {
--    $currentTaskId = [string]$queue.currentTaskId
--} else {
--    $inProgressTask = $tasks | Where-Object { $_.status -eq "in_progress" } | Select-Object -First
-- 1
-+    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
-+    $exitCode = $LASTEXITCODE
-+    $message = $output.Trim()
- 
--    if ($null -ne $inProgressTask) {
--        $currentTaskId = [string]$inProgressTask.id
--    } else {
--        $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -First 1
-+    if ([string]::IsNullOrWhiteSpace($message)) {
-+        $message = "완료"
-+    }
- 
--        if ($null -ne $pendingTask) {
--            $currentTaskId = [string]$pendingTask.id
+-[string]::IsNullOrWhiteSpace($_) })
+-
+-        if ($normalizedFiles.Count -gt 0) {
+-            $arguments += "-Files"
+-            $arguments += ($normalizedFiles -join ",")
 -        }
-+    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-+
-+    if ($exitCode -ne 0) {
-+        Stop-Cycle $script:steps "$Name`_failed" $false 1
-     }
- }
- 
--$currentTask = $null
-+function Get-ScriptPath {
-+    param(
-+        [string]$Name
-+    )
- 
--if (Test-HasValue $currentTaskId) {
--    $currentTask = $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
+-    }
+-
+-    return $arguments
 -}
-+    $scriptPath = Join-Path $PSScriptRoot $Name
- 
--if ($null -eq $currentTask) {
--    Write-Warning "현재 task를 찾지 못했습니다. 기본 커밋 메시지를 사용할 수 있습니다."
+-
+-function Get-CommitGate {
+-    $state = Read-JsonFile $statePath $stateRelativePath
+-
+-    return [PSCustomObject][ordered]@{
+-        lastCommand = [string]$state.lastCommand
+-        lastCommandStatus = [string]$state.lastCommandStatus
+-        lastCommitHash = [string]$state.lastCommitHash
+-    }
 -}
-+    if (-not (Test-Path -LiteralPath $scriptPath -PathType Leaf)) {
-+        throw "필수 스크립트를 찾을 수 없습니다: scripts/$Name"
-+    }
- 
--if (-not $AllowWithoutPassedCheck -and $state.lastCommandStatus -ne "passed") {
--    Stop-WithError "state.lastCommandStatus가 passed가 아닙니다. 검증 결과를 확인하거나 -AllowWithoutPassedCheck를 
--사용하세요."
-+    return $scriptPath
- }
- 
--if (-not $AllowWithoutPassedReview -and $state.lastReviewDecision -ne "pass") {
--    Stop-WithError "state.lastReviewDecision이 pass가 아닙니다. 리뷰 결과를 확인하거나 -AllowWithoutPassedReview를 
--사용하세요."
+-
+-Set-Location $repoRoot
+-
+-$script:steps = @()
+-
+-if ($MaxTasks -lt 1) {
+-    Stop-Cycle $script:steps "max_tasks_must_be_at_least_1" $false 1
 -}
-+function Invoke-GitCapture {
-+    param(
-+        [string[]]$Arguments,
-+        [string]$DisplayName
-+    )
- 
+-
+-if ($MaxSteps -lt 1) {
+-    Stop-Cycle $script:steps "max_steps_must_be_at_least_1" $false 1
+-}
+-
 -try {
--    $statusPorcelain = Invoke-GitCapture -Arguments @("status", "--porcelain") -DisplayName "git s
--tatus --porcelain"
+-    foreach ($requiredPath in @($queuePath, $statePath)) {
+-        if (-not (Test-Path -LiteralPath $requiredPath -PathType Leaf)) {
+-            throw "필수 상태 파일이 없습니다: $requiredPath"
+-        }
+-    }
+-
+-    $queue = Read-JsonFile $queuePath $queueRelativePath
+-    $state = Read-JsonFile $statePath $stateRelativePath
+-
+-    if ($state.goalStatus -eq "completed") {
+-        Stop-Cycle $script:steps "goal_completed" $true 0
+-    }
+-
+-    if (-not ($queue.PSObject.Properties.Name -contains "tasks") -or $null -eq $queue.tasks) {
+-        Stop-Cycle $script:steps "no_task" $false 0
+-    }
+-
+-    $currentTask = Get-CurrentTask $queue $state
+-
+-    if ($null -eq $currentTask) {
+-        Stop-Cycle $script:steps "no_task" $false 0
+-    }
+-
+-    if ($currentTask.status -eq "done") {
+-        Stop-Cycle $script:steps "current_task_done" $true 0
+-    }
+-
+-    $scriptPaths = @{
+-        makePrompt = Get-ScriptPath "ai-dev-make-prompt.ps1"
+-        runCodex = Get-ScriptPath "ai-dev-run-codex.ps1"
+-        check = Get-ScriptPath "ai-dev-check.ps1"
+-        saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
+-        makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
+-        runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
+-        commit = Get-ScriptPath "ai-dev-commit.ps1"
+-        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
+-    }
 -} catch {
--    Stop-WithError $_.Exception.Message
+-    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mess
+a
+-ge
+-    Stop-Cycle $script:steps "prepare_failed" $false 1
 -}
-+    $output = & git @Arguments 2>&1 | Out-String
-+    $exitCode = $LASTEXITCODE
- 
--$changeLines = @($statusPorcelain -split "`r?`n" | Where-Object { -not [string]::IsNullOrWhiteSpac
--e($_) })
-+    if ($exitCode -ne 0) {
-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+    }
- 
--if ($changeLines.Count -eq 0) {
--    Write-Host "커밋할 변경사항 없음"
--    exit 0
-+    return $output.TrimEnd()
- }
- 
--$selectedFiles = @()
--$targetChangeLines = $changeLines
-+function Test-PackageFileChanged {
-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.jso
-n
-+") "git status --porcelain -- package.json package-lock.json"
-+    return -not [string]::IsNullOrWhiteSpace($status)
-+}
- 
--if ($null -ne $Files -and $Files.Count -gt 0) {
--    $requestedFiles = @($Files | ForEach-Object { $_ -split "," } | Where-Object { -not [string]::
--IsNullOrWhiteSpace($_) })
-+function Get-ReviewGate {
-+    $state = Read-JsonFile $statePath $stateRelativePath
-+    $nextStep = $null
- 
--    if ($requestedFiles.Count -eq 0) {
--        Stop-WithError "-Files에는 비어 있지 않은 파일 경로만 지정할 수 있습니다."
+-
+-$plannedSteps = @(
+-    "make-prompt",
+-    "run-codex",
+-    "check",
+-    "save-diff",
+-    "make-review-prompt",
+-    "run-review-codex",
+-    "review-gate",
+-    "package-change-gate",
+-    "commit",
+-    "commit-result-gate",
+-    "complete-task"
+-)
+-
+-if ($plannedSteps.Count -gt $MaxSteps) {
+-    Stop-Cycle $script:steps "max_steps_too_small_for_full_cycle" $false 1
+-}
+-
+-$stepNumber = 1
+-$completedTaskCount = 0
+-
+-while ($completedTaskCount -lt $MaxTasks) {
+-    try {
+-        $queue = Read-JsonFile $queuePath $queueRelativePath
+-        $state = Read-JsonFile $statePath $stateRelativePath
+-        $currentTask = Get-CurrentTask $queue $state
+-    } catch {
+-        $script:steps += New-StepResult $stepNumber "load-task" "state/queue 확인" $false $false 1 $
+_
+-.Exception.Message
+-        Stop-Cycle $script:steps "load_task_failed" $false 1
 -    }
-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
- 
--    foreach ($file in $requestedFiles) {
--        if ([string]::IsNullOrWhiteSpace($file)) {
--            Stop-WithError "-Files에는 비어 있지 않은 파일 경로만 지정할 수 있습니다."
-+        if (Test-HasValue $reviewResponse.next_step) {
-+            $nextStep = [string]$reviewResponse.next_step
-         }
-+    }
- 
--        $normalizedFile = $file.Replace('\', '/')
--        $fullPath = [System.IO.Path]::GetFullPath((Join-Path $projectRoot $normalizedFile))
--        $projectRootPrefix = [System.IO.Path]::GetFullPath($projectRoot).TrimEnd('\', '/') + [Syst
--em.IO.Path]::DirectorySeparatorChar
--
--        if (-not $fullPath.StartsWith($projectRootPrefix, [System.StringComparison]::OrdinalIgnore
--Case)) {
--            Stop-WithError "저장소 밖 파일은 선택할 수 없습니다: $file"
+-
+-    if ($state.goalStatus -eq "completed") {
+-        Stop-Cycle $script:steps "goal_completed" $true 0
+-    }
+-
+-    if ($null -eq $currentTask) {
+-        Stop-Cycle $script:steps "no_task" $false 0
+-    }
+-
+-    if ($currentTask.status -eq "done") {
+-        Stop-Cycle $script:steps "current_task_done" $true 0
+-    }
+-
+-    $taskLabel = "$($currentTask.id) $($currentTask.title)"
+-    $script:steps += New-StepResult $stepNumber "task-start" "MaxTasks=$MaxTasks" $false $false 0 
+"
+-현재 task 실행 시작: $taskLabel"
+-    $stepNumber++
+-
+-    Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File script
+s
+-/ai-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
+-    $stepNumber++
+-
+-    if (-not $AllowCodex -and -not $DryRun) {
+-        $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -A
+l
+-lowCodex -AllowReviewCodex -MaxTasks $MaxTasks"
+-        if ($AllowDirty) {
+-            $command = "$command -AllowDirty"
 -        }
-+    return [PSCustomObject][ordered]@{
-+        lastCommandStatus = [string]$state.lastCommandStatus
-+        decision = [string]$state.lastReviewDecision
-+        nextStep = $nextStep
-+    }
-+}
- 
--        if (-not (Test-Path -LiteralPath $fullPath -PathType Leaf)) {
--            Stop-WithError "선택한 파일이 존재하지 않습니다: $file"
+-
+-        if ($AllowCommit) {
+-            $command = "$command -AllowCommit"
 -        }
-+function Get-CommitArguments {
-+    $arguments = @()
- 
--        try {
--            $fileStatus = Invoke-GitCapture -Arguments @("status", "--porcelain", "--", $normalize
--dFile) -DisplayName "git status --porcelain -- $normalizedFile"
--        } catch {
--            Stop-WithError $_.Exception.Message
+-
+-        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
+-            $command = "$command -CommitFiles $($CommitFiles -join ',')"
 -        }
-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -not
- 
-+[string]::IsNullOrWhiteSpace($_) })
- 
--        if ([string]::IsNullOrWhiteSpace($fileStatus)) {
--            Stop-WithError "선택한 파일에 커밋할 변경사항이 없습니다: $file"
-+        if ($normalizedFiles.Count -gt 0) {
-+            $arguments += "-Files"
-+            $arguments += ($normalizedFiles -join ",")
-         }
--
--        $selectedFiles += $normalizedFile
-     }
+-
+-        $script:steps += New-StepResult $stepNumber "run-codex" "powershell -ExecutionPolicy Bypas
+s
+- -File scripts/ai-dev-run-codex.ps1" $false $true 1 "Codex 구현 실행에는 -AllowCodex가 필요합니다. 추천 명령: $com
+m
+-and"
+-        Stop-Cycle $script:steps "allow_codex_required" $false 1
+-    }
+-
+-    $runCodexArguments = @()
+-    if ($AllowDirty) {
+-        $runCodexArguments += "-AllowDirty"
+-    }
+-
+-    Invoke-CycleCommand $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass -File scripts/
+a
+-i-dev-run-codex.ps1" $scriptPaths.runCodex $runCodexArguments
+-    $stepNumber++
+-
+-    Invoke-CycleCommand $stepNumber "check" "powershell -ExecutionPolicy Bypass -File scripts/ai-d
+e
+-v-check.ps1 -BuildOnly" $scriptPaths.check @("-BuildOnly")
+-    $stepNumber++
+-
+-    Invoke-CycleCommand $stepNumber "save-diff" "powershell -ExecutionPolicy Bypass -File scripts/
+a
+-i-dev-save-diff.ps1" $scriptPaths.saveDiff @()
+-    $stepNumber++
+-
+-    Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File
  
--    $selectedFiles = @($selectedFiles | Select-Object -Unique)
--    $targetChangeLines = @($selectedFiles)
+-scripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
+-    $stepNumber++
+-
+-    if (-not $AllowReviewCodex -and -not $DryRun) {
+-        $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -A
+l
+-lowCodex -AllowReviewCodex -MaxTasks $MaxTasks"
+-        if ($AllowDirty) {
+-            $command = "$command -AllowDirty"
+-        }
+-
+-        if ($AllowCommit) {
+-            $command = "$command -AllowCommit"
+-        }
+-
+-        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
+-            $command = "$command -CommitFiles $($CommitFiles -join ',')"
+-        }
+-
+-        $script:steps += New-StepResult $stepNumber "run-review-codex" "powershell -ExecutionPolic
+y
+- Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $false $true 1 "Codex 리
+뷰
+- 실행에는 -AllowReviewCodex가 필요합니다. 추천 명령: $command"
+-        Stop-Cycle $script:steps "allow_review_codex_required" $false 1
+-    }
+-
+-    Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File s
+c
+-ripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDi
+r
+-ty", "-SaveReview")
+-    $stepNumber++
+-
+-    if ($DryRun) {
+-        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $f
+a
+-lse $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
+-        $stepNumber++
+-        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain 
+-
+-- package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
+-        $stepNumber++
+-        $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -
+F
+-ile scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
+-        $stepNumber++
+-        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCo
+m
+-mitHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
+-        $stepNumber++
+-        $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy B
+y
+-pass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"자동 완료: Codex 구현, build/check, Codex 리
+뷰
+- pass, 자동 커밋 완료`"" $false $true 0 "DryRun: task 완료 처리를 실행하지 않았습니다."
+-        Stop-Cycle $script:steps "dry_run" $false 0
+-    }
 -
 -    try {
--        $stagedFiles = Invoke-GitCapture -Arguments @("diff", "--cached", "--name-only") -DisplayN
--ame "git diff --cached --name-only"
+-        $reviewGate = Get-ReviewGate
 -    } catch {
--        Stop-WithError $_.Exception.Message
+-        $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $fals
+e
+- $false 1 $_.Exception.Message
+-        Stop-Cycle $script:steps "review_gate_failed" $false 1
 -    }
-+    return $arguments
-+}
- 
--    $stagedFileList = @($stagedFiles -split "`r?`n" | Where-Object { -not [string]::IsNullOrWhiteS
--pace($_) })
--    $unexpectedStagedFiles = @($stagedFileList | Where-Object { $selectedFiles -notcontains $_ })
-+function Get-CommitGate {
-+    $state = Read-JsonFile $statePath $stateRelativePath
- 
--    if ($unexpectedStagedFiles.Count -gt 0) {
--        Stop-WithError "선택 파일 외에 이미 staged 된 파일이 있습니다: $($unexpectedStagedFiles -join ', ')"
-+    return [PSCustomObject][ordered]@{
-+        lastCommand = [string]$state.lastCommand
-+        lastCommandStatus = [string]$state.lastCommandStatus
-+        lastCommitHash = [string]$state.lastCommitHash
-     }
- }
- 
--$commitMessage = if (Test-HasValue $Message) {
--    $Message
--} elseif ($null -ne $currentTask -and (Test-HasValue $currentTask.commitMessage)) {
--    [string]$currentTask.commitMessage
--} elseif ($null -ne $currentTask) {
--    "chore(ai-dev): complete $($currentTask.id) $($currentTask.title)"
--} else {
--    "chore(ai-dev): automated task commit"
--}
-+Set-Location $repoRoot
- 
--$taskText = if ($null -ne $currentTask) {
--    "$($currentTask.id) $($currentTask.title)"
--} elseif (Test-HasValue $currentTaskId) {
--    "$currentTaskId unknown"
--} else {
--    "unknown"
-+$script:steps = @()
-+
-+if ($MaxTasks -lt 1) {
-+    Stop-Cycle $script:steps "max_tasks_must_be_at_least_1" $false 1
- }
- 
--if ($DryRun) {
--    Write-Host "Dry run: git add/commit을 실행하지 않습니다."
--    Write-Host "커밋 메시지: $commitMessage"
--    Write-Host "현재 task: $taskText"
--    Write-Host "커밋 대상 변경 파일:"
--    foreach ($line in $targetChangeLines) {
--        Write-Host "  - $line"
+-
+-    if ($reviewGate.lastCommandStatus -ne "passed") {
+-        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fa
+l
+-se $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
+-        Stop-Cycle $script:steps "review_save_not_passed" $false 1
 -    }
--    Write-Host "변경 파일 수: $($targetChangeLines.Count)"
--    exit 0
-+if ($MaxSteps -lt 1) {
-+    Stop-Cycle $script:steps "max_steps_must_be_at_least_1" $false 1
- }
- 
- try {
--    if ($selectedFiles.Count -gt 0) {
--        $addOutput = & git add -- @selectedFiles 2>&1 | Out-String
--        $addDisplayName = "git add -- <selected files>"
--    } else {
--        $addOutput = & git add -A 2>&1 | Out-String
--        $addDisplayName = "git add -A"
-+    foreach ($requiredPath in @($queuePath, $statePath)) {
-+        if (-not (Test-Path -LiteralPath $requiredPath -PathType Leaf)) {
-+            throw "필수 상태 파일이 없습니다: $requiredPath"
-+        }
-     }
- 
--    if ($LASTEXITCODE -ne 0) {
--        throw "$addDisplayName 실행에 실패했습니다. exit code: $LASTEXITCODE`n$addOutput"
+-
+-    if ($reviewGate.decision -ne "pass") {
+-        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $f
+a
+-lse $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
+-        Stop-Cycle $script:steps "review_not_pass" $false 0
 -    }
-+    $queue = Read-JsonFile $queuePath $queueRelativePath
-+    $state = Read-JsonFile $statePath $stateRelativePath
- 
--    if ($selectedFiles.Count -gt 0) {
--        $stagedFilesAfterAdd = Invoke-GitCapture -Arguments @("diff", "--cached", "--name-only") -
--DisplayName "git diff --cached --name-only"
--        $stagedFileListAfterAdd = @($stagedFilesAfterAdd -split "`r?`n" | Where-Object { -not [str
--ing]::IsNullOrWhiteSpace($_) })
--        $unexpectedStagedFilesAfterAdd = @($stagedFileListAfterAdd | Where-Object { $selectedFiles
-- -notcontains $_ })
-+    if ($state.goalStatus -eq "completed") {
-+        Stop-Cycle $script:steps "goal_completed" $true 0
-+    }
+-
+-    if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
+-        $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath nex
+t
+-_step 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($re
+v
+-iewGate.nextStep)"
+-        Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
+-    }
+-
+-    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false
  
--        if ($unexpectedStagedFilesAfterAdd.Count -gt 0) {
--            throw "선택 파일 외의 staged 파일이 감지되었습니다: $($unexpectedStagedFilesAfterAdd -join ', ')"
+-$false 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
+-    $stepNumber++
+-
+-    try {
+-        if (Test-PackageFileChanged) {
+-            $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcel
+a
+-in -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자
+동
+- 커밋하지 않습니다."
+-            Stop-Cycle $script:steps "package_files_changed" $false 1
 -        }
-+    if (-not ($queue.PSObject.Properties.Name -contains "tasks") -or $null -eq $queue.tasks) {
-+        Stop-Cycle $script:steps "no_task" $false 0
-     }
- 
--    $commitOutput = & git commit -m $commitMessage 2>&1 | Out-String
-+    $currentTask = Get-CurrentTask $queue $state
- 
--    if ($LASTEXITCODE -ne 0) {
--        throw "git commit 실행에 실패했습니다. exit code: $LASTEXITCODE`n$commitOutput"
-+    if ($null -eq $currentTask) {
-+        Stop-Cycle $script:steps "no_task" $false 0
-+    }
-+
-+    if ($currentTask.status -eq "done") {
-+        Stop-Cycle $script:steps "current_task_done" $true 0
-     }
- 
--    $commitHash = Invoke-GitCapture -Arguments @("rev-parse", "HEAD") -DisplayName "git rev-parse 
--HEAD"
-+    $scriptPaths = @{
-+        makePrompt = Get-ScriptPath "ai-dev-make-prompt.ps1"
-+        runCodex = Get-ScriptPath "ai-dev-run-codex.ps1"
-+        check = Get-ScriptPath "ai-dev-check.ps1"
-+        saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+        makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+        runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+    }
- } catch {
--    $errorSummary = $_.Exception.Message
--    Save-StateResult $state "failed" $errorSummary $null
--    Stop-WithError "자동 커밋에 실패했습니다: $errorSummary"
-+    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mess
+-    } catch {
+-        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain 
+-
+-- package.json package-lock.json" $false $false 1 $_.Exception.Message
+-        Stop-Cycle $script:steps "package_change_gate_failed" $false 1
+-    }
+-
+-    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- p
 a
-+ge
-+    Stop-Cycle $script:steps "prepare_failed" $false 1
- }
- 
--Save-StateResult $state "passed" "" $commitHash
-+$plannedSteps = @(
-+    "make-prompt",
-+    "run-codex",
-+    "check",
-+    "save-diff",
-+    "make-review-prompt",
-+    "run-review-codex",
-+    "review-gate",
-+    "package-change-gate",
-+    "commit",
-+    "commit-result-gate",
-+    "complete-task"
-+)
- 
--$logTimestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
--$logEntry = @"
-+if ($plannedSteps.Count -gt $MaxSteps) {
-+    Stop-Cycle $script:steps "max_steps_too_small_for_full_cycle" $false 1
-+}
- 
--## $logTimestamp - Commit created
-+$stepNumber = 1
-+$completedTaskCount = 0
- 
--- Task: $taskText
--- Commit: $commitHash
--- Message: $commitMessage
--"@
-+while ($completedTaskCount -lt $MaxTasks) {
-+    try {
-+        $queue = Read-JsonFile $queuePath $queueRelativePath
-+        $state = Read-JsonFile $statePath $stateRelativePath
-+        $currentTask = Get-CurrentTask $queue $state
-+    } catch {
-+        $script:steps += New-StepResult $stepNumber "load-task" "state/queue 확인" $false $false 1 $
-_
-+.Exception.Message
-+        Stop-Cycle $script:steps "load_task_failed" $false 1
-+    }
- 
--[System.IO.File]::AppendAllText($loopLogPath, $logEntry, $utf8WithBom)
-+    if ($state.goalStatus -eq "completed") {
-+        Stop-Cycle $script:steps "goal_completed" $true 0
-+    }
- 
--Write-Host "커밋 메시지: $commitMessage"
--Write-Host "커밋 해시: $commitHash"
--Write-Host "변경 파일 수: $($targetChangeLines.Count)"
--Write-Host "현재 task: $taskText"
-+    if ($null -eq $currentTask) {
-+        Stop-Cycle $script:steps "no_task" $false 0
-+    }
- 
--codex
--커밋 연결은 `ai-dev-commit.ps1`의 기존 검증 게이트를 재사용하되, full cycle 쪽에서 한 번 더 `review pass`, `next_step compl
--ete_task`, package 파일 변경 여부를 확인하도록 넣겠습니다. `complete-task`는 커밋 단계 성공 뒤에만 호출되게 순서를 고정합니다.
--apply patch
--patch: completed
--D:\ai-apps\planpilot-local\scripts\ai-dev-auto-cycle-full.ps1
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..7240437c74a8273e33925eeb7e651c6e85eac995
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,9 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,7 +328,7 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
--@@ -314,11 +373,68 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
--
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..7240437c74a8273e33925eeb7e651c6e85eac995
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,9 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,7 +328,7 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
--@@ -314,11 +373,68 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+    if ($currentTask.status -eq "done") {
-+        Stop-Cycle $script:steps "current_task_done" $true 0
-+    }
- 
--codex
--패치 후 한 가지 중요한 점은 DryRun과 실제 실행의 의미가 갈리지 않게 하는 것입니다. 이제 문법을 먼저 확인하고, 필요하면 DryRun에서 게이트 표시가 어색한 부분만 
--좁게 조정하겠습니다.
--exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$null = [System.Managem
--ent.Automation.Language.Parser]::ParseFile('"'scripts/ai-dev-auto-cycle-full.ps1', [ref]"'$null, [
--ref]$parseErrors); if ($parseErrors.Count -gt 0) { $parseErrors | ForEach-Object { $_.Message }; e
--xit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-local
--exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path scr
--ipts/ai-dev-auto-cycle-full.ps1 -Pattern "review-gate|package-change-gate|AllowCommit|complete-tas
--k|CommitFiles|plannedSteps" -Context 2,2' in D:\ai-apps\planpilot-local
-- succeeded in 2171ms:
--syntax ok
--존재하지 않는 변수에 [ref]을(를) 적용할 수 없습니다.
--위치 줄:2 문자:1
--+ $null = [System.Management.Automation.Language.Parser]::ParseFile('sc ...
--+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
--    + CategoryInfo          : InvalidOperation: (parseErrors:VariablePath) [], RuntimeException
--    + FullyQualifiedErrorId : NonExistingVariableReference
-- 
-+    $taskLabel = "$($currentTask.id) $($currentTask.title)"
-+    $script:steps += New-StepResult $stepNumber "task-start" "MaxTasks=$MaxTasks" $false $false 0 
-"
-+현재 task 실행 시작: $taskLabel"
-+    $stepNumber++
- 
-- succeeded in 2288ms:
--
--  scripts\ai-dev-auto-cycle-full.ps1:6:    [switch]$AllowCodex,
--  scripts\ai-dev-auto-cycle-full.ps1:7:    [switch]$AllowReviewCodex,
--> scripts\ai-dev-auto-cycle-full.ps1:8:    [switch]$AllowCommit,
--  scripts\ai-dev-auto-cycle-full.ps1:9:    [switch]$AllowDirty,
--> scripts\ai-dev-auto-cycle-full.ps1:10:    [string[]]$CommitFiles
--  scripts\ai-dev-auto-cycle-full.ps1:11:)
--  scripts\ai-dev-auto-cycle-full.ps1:12:
--  scripts\ai-dev-auto-cycle-full.ps1:246:    $arguments = @()
--  scripts\ai-dev-auto-cycle-full.ps1:247:
--> scripts\ai-dev-auto-cycle-full.ps1:248:    if ($null -ne $CommitFiles -and $CommitFiles.Count -g
--t 0) {
--> scripts\ai-dev-auto-cycle-full.ps1:249:        $normalizedFiles = @($CommitFiles | ForEach-Objec
--t { $_ -split "," } |
-- Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
--  scripts\ai-dev-auto-cycle-full.ps1:250:
--  scripts\ai-dev-auto-cycle-full.ps1:251:        if ($normalizedFiles.Count -gt 0) {
--  scripts\ai-dev-auto-cycle-full.ps1:306:        runReviewCodex = Get-ScriptPath "ai-dev-run-revie
--w-codex.ps1"
--  scripts\ai-dev-auto-cycle-full.ps1:307:        commit = Get-ScriptPath "ai-dev-commit.ps1"
--> scripts\ai-dev-auto-cycle-full.ps1:308:        completeTask = Get-ScriptPath "ai-dev-complete-ta
--sk.ps1"
--  scripts\ai-dev-auto-cycle-full.ps1:309:    }
--  scripts\ai-dev-auto-cycle-full.ps1:310:} catch {
--  scripts\ai-dev-auto-cycle-full.ps1:313:}
--  scripts\ai-dev-auto-cycle-full.ps1:314:
--> scripts\ai-dev-auto-cycle-full.ps1:315:$plannedSteps = @(
--  scripts\ai-dev-auto-cycle-full.ps1:316:    "make-prompt",
--  scripts\ai-dev-auto-cycle-full.ps1:317:    "run-codex",
--  scripts\ai-dev-auto-cycle-full.ps1:321:    "run-review-codex",
--  scripts\ai-dev-auto-cycle-full.ps1:322:    "commit",
--> scripts\ai-dev-auto-cycle-full.ps1:323:    "complete-task"
--  scripts\ai-dev-auto-cycle-full.ps1:324:)
--  scripts\ai-dev-auto-cycle-full.ps1:325:
--> scripts\ai-dev-auto-cycle-full.ps1:326:if ($plannedSteps.Count -gt $MaxSteps) {
--  scripts\ai-dev-auto-cycle-full.ps1:327:    Stop-Cycle $script:steps "max_steps_too_small_for_ful
--l_cycle" $false 1
--  scripts\ai-dev-auto-cycle-full.ps1:328:}
--  scripts\ai-dev-auto-cycle-full.ps1:379:    $reviewGate = Get-ReviewGate
--  scripts\ai-dev-auto-cycle-full.ps1:380:} catch {
--> scripts\ai-dev-auto-cycle-full.ps1:381:    $script:steps += New-StepResult $stepNumber "review-g
--ate" "state/review-re
--sponse 확인" $false $false 1 $_.Exception.Message
--  scripts\ai-dev-auto-cycle-full.ps1:382:    Stop-Cycle $script:steps "review_gate_failed" $false 
--1
--  scripts\ai-dev-auto-cycle-full.ps1:383:}
--  scripts\ai-dev-auto-cycle-full.ps1:384:
--  scripts\ai-dev-auto-cycle-full.ps1:385:if ($reviewGate.lastCommandStatus -ne "passed") {
--> scripts\ai-dev-auto-cycle-full.ps1:386:    $script:steps += New-StepResult $stepNumber "review-g
--ate" "state.lastComma
--ndStatus 확인" $false $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandS
--tatus)"
--  scripts\ai-dev-auto-cycle-full.ps1:387:    Stop-Cycle $script:steps "review_save_not_passed" $fa
--lse 1
--  scripts\ai-dev-auto-cycle-full.ps1:388:}
--  scripts\ai-dev-auto-cycle-full.ps1:389:
--  scripts\ai-dev-auto-cycle-full.ps1:390:if ($reviewGate.decision -ne "pass") {
--> scripts\ai-dev-auto-cycle-full.ps1:391:    $script:steps += New-StepResult $stepNumber "review-g
--ate" "state.lastRevie
--wDecision 확인" $false $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGa
--te.decision)"
--  scripts\ai-dev-auto-cycle-full.ps1:392:    Stop-Cycle $script:steps "review_not_pass" $false 0
--  scripts\ai-dev-auto-cycle-full.ps1:393:}
--  scripts\ai-dev-auto-cycle-full.ps1:394:
--  scripts\ai-dev-auto-cycle-full.ps1:395:if (Test-HasValue $reviewGate.nextStep -and $reviewGate.n
--extStep -ne "complete
--_task") {
--> scripts\ai-dev-auto-cycle-full.ps1:396:    $script:steps += New-StepResult $stepNumber "review-g
--ate" "$reviewResponse
--RelativePath next_step 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를
-- 실행하지 않습니다: $($review
--Gate.nextStep)"
--  scripts\ai-dev-auto-cycle-full.ps1:397:    Stop-Cycle $script:steps "review_next_step_not_comple
--te_task" $false 0
--  scripts\ai-dev-auto-cycle-full.ps1:398:}
--  scripts\ai-dev-auto-cycle-full.ps1:399:
--> scripts\ai-dev-auto-cycle-full.ps1:400:$script:steps += New-StepResult $stepNumber "review-gate"
-- "state.lastReviewDec
--ision 확인" $false $false 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--  scripts\ai-dev-auto-cycle-full.ps1:401:$stepNumber++
--  scripts\ai-dev-auto-cycle-full.ps1:402:
--  scripts\ai-dev-auto-cycle-full.ps1:403:try {
--  scripts\ai-dev-auto-cycle-full.ps1:404:    if (Test-PackageFileChanged) {
--> scripts\ai-dev-auto-cycle-full.ps1:405:        $script:steps += New-StepResult $stepNumber "pack
--age-change-gate" "git
-- status --porcelain -- package.json package-lock.json" $false $true 1 "package.json 또는 package-loc
--k.json 변경이 감지되어 자동 커밋
--하지 않습니다."
--  scripts\ai-dev-auto-cycle-full.ps1:406:        Stop-Cycle $script:steps "package_files_changed" 
--$false 1
--  scripts\ai-dev-auto-cycle-full.ps1:407:    }
--  scripts\ai-dev-auto-cycle-full.ps1:408:} catch {
--> scripts\ai-dev-auto-cycle-full.ps1:409:    $script:steps += New-StepResult $stepNumber "package-
--change-gate" "git sta
--tus --porcelain -- package.json package-lock.json" $false $false 1 $_.Exception.Message
--  scripts\ai-dev-auto-cycle-full.ps1:410:    Stop-Cycle $script:steps "package_change_gate_failed"
-- $false 1
--  scripts\ai-dev-auto-cycle-full.ps1:411:}
--  scripts\ai-dev-auto-cycle-full.ps1:412:
--> scripts\ai-dev-auto-cycle-full.ps1:413:$script:steps += New-StepResult $stepNumber "package-chan
--ge-gate" "git status 
----porcelain -- package.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다.
--"
--  scripts\ai-dev-auto-cycle-full.ps1:414:$stepNumber++
--  scripts\ai-dev-auto-cycle-full.ps1:415:
--> scripts\ai-dev-auto-cycle-full.ps1:416:if (-not $AllowCommit) {
--> scripts\ai-dev-auto-cycle-full.ps1:417:    $commitCommand = "powershell -ExecutionPolicy Bypass 
---File scripts/ai-dev-
--auto-cycle-full.ps1 -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--  scripts\ai-dev-auto-cycle-full.ps1:418:    if ($AllowDirty) {
--  scripts\ai-dev-auto-cycle-full.ps1:419:        $commitCommand = "$commitCommand -AllowDirty"
--  scripts\ai-dev-auto-cycle-full.ps1:420:    }
--  scripts\ai-dev-auto-cycle-full.ps1:421:
--> scripts\ai-dev-auto-cycle-full.ps1:422:    if ($null -ne $CommitFiles -and $CommitFiles.Count -g
--t 0) {
--> scripts\ai-dev-auto-cycle-full.ps1:423:        $commitCommand = "$commitCommand -CommitFiles $($
--CommitFiles -join ','
--)"
--  scripts\ai-dev-auto-cycle-full.ps1:424:    }
--  scripts\ai-dev-auto-cycle-full.ps1:425:
--> scripts\ai-dev-auto-cycle-full.ps1:426:    $script:steps += New-StepResult $stepNumber "commit" 
--"powershell -Executio
--nPolicy Bypass -File scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령
--: $commitCommand"
--  scripts\ai-dev-auto-cycle-full.ps1:427:    Stop-Cycle $script:steps "allow_commit_required" $fal
--se 1
--  scripts\ai-dev-auto-cycle-full.ps1:428:}
--  scripts\ai-dev-auto-cycle-full.ps1:437:$stepNumber++
--  scripts\ai-dev-auto-cycle-full.ps1:438:
--> scripts\ai-dev-auto-cycle-full.ps1:439:Invoke-CycleCommand $stepNumber "complete-task" "powershe
--ll -ExecutionPolicy B
--ypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.c
--ompleteTask @("-Resul
--tSummary", "리뷰 pass 후 자동 커밋 완료")
--  scripts\ai-dev-auto-cycle-full.ps1:440:Stop-Cycle $script:steps "completed" $true 0
--
--
--
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..7240437c74a8273e33925eeb7e651c6e85eac995
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,9 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,7 +328,7 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
--@@ -314,11 +373,68 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+    Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File script
+-ckage.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
+-    $stepNumber++
+-
+-    if (-not $AllowCommit) {
+-        $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.
+p
+-s1 -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
+-        if ($AllowDirty) {
+-            $commitCommand = "$commitCommand -AllowDirty"
+-        }
+-
+-        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
+-            $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
+-        }
+-
+-        $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -
+F
+-ile scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
+-        Stop-Cycle $script:steps "allow_commit_required" $false 1
+-    }
+-
+-    $commitArguments = Get-CommitArguments
+-    $commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
+-    if ($commitArguments.Count -gt 0) {
+-        $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
+-    }
+-
+-    Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArgumen
+t
+-s
+-    $stepNumber++
+-
+-    try {
+-        $commitGate = Get-CommitGate
+-    } catch {
+-        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCo
+m
+-mitHash 확인" $false $false 1 $_.Exception.Message
+-        Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
+-    }
+-
+-    if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -n
+o
+-t (Test-HasValue $commitGate.lastCommitHash)) {
+-        $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastComm
+a
+-nd), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHa
 s
-+/ai-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-+    $stepNumber++
- 
--codex
--문법 검증 명령에서 `[ref]` 대상 변수를 초기화하지 않아 PowerShell 경고가 섞였습니다. 스크립트 자체 문법은 통과했지만, 검증 명령을 바로잡아 다시 돌리기 전에 
--DryRun이 실제 상태를 읽지 않고 전체 단계를 보여주도록 보완하겠습니다.
--apply patch
--patch: completed
--D:\ai-apps\planpilot-local\scripts\ai-dev-auto-cycle-full.ps1
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,11 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +330,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +364,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +375,79 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
--
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,11 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +330,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +364,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +375,79 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+    if (-not $AllowCodex -and -not $DryRun) {
-+        $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -A
-l
-+lowCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+        if ($AllowDirty) {
-+            $command = "$command -AllowDirty"
-+        }
- 
--codex
--스크립트는 T005 흐름으로 바뀌었습니다. 이제 `.ai-dev/README.md`의 T004 자리표시자 설명을 T005 기준으로 갱신해 사용자가 Allow 옵션, pass 게
--이트, package 중단 조건을 확인할 수 있게 하겠습니다.
--exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path .ai
---dev/README.md -Pattern "full auto-cycle 초안|T004|AllowCommit|package|complete-task" -Context 2,4' 
--in D:\ai-apps\planpilot-local
-- succeeded in 3078ms:
--
--  .ai-dev\README.md:113:
--  .ai-dev\README.md:114:```powershell
--> .ai-dev\README.md:115:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 
---ResultSummary "T001 
--분석 완료"
--  .ai-dev\README.md:116:```
--  .ai-dev\README.md:117:
--  .ai-dev\README.md:118:특정 task를 완료 처리하려면 `-TaskId`를 지정한다.
--  .ai-dev\README.md:119:
--  .ai-dev\README.md:120:```powershell
--> .ai-dev\README.md:121:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 
---TaskId T001 -ResultS
--ummary "분석 완료"
--  .ai-dev\README.md:122:```
--  .ai-dev\README.md:123:
--  .ai-dev\README.md:124:다음 task로 자동 이동하지 않으려면 `-NoNext` 옵션을 사용한다.
--  .ai-dev\README.md:125:
--  .ai-dev\README.md:126:```powershell
--> .ai-dev\README.md:127:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 
---NoNext
--  .ai-dev\README.md:128:```
--  .ai-dev\README.md:129:
--  .ai-dev\README.md:130:task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Codex 실행
--, GPT 리뷰, build/test 
--실행, git commit은 수행하지 않는다.
--  .ai-dev\README.md:131:
--  .ai-dev\README.md:132:## 프로젝트 검증 실행
--  .ai-dev\README.md:133:
--> .ai-dev\README.md:134:`package.json`에 정의된 build, test, lint script를 확인하고 실행 결과를 `test-result.md`
--와 `state.json`에 기록한다.
--  .ai-dev\README.md:135:
--  .ai-dev\README.md:136:```powershell
--  .ai-dev\README.md:137:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1
--  .ai-dev\README.md:138:```
--  .ai-dev\README.md:418:리뷰 저장 후 decision별 다음 단계:
--  .ai-dev\README.md:419:
--> .ai-dev\README.md:420:- `pass`: `ai-dev-complete-task.ps1` 실행 또는 커밋 조건을 검토한다.
--  .ai-dev\README.md:421:- `revise`: `ai-dev-make-revise-prompt.ps1`로 재수정 프롬프트를 만든 뒤 Codex/Cline에 수
--동 전달한다.
--  .ai-dev\README.md:422:- `blocked`: 사용자 판단이 필요하므로 루프를 중단하고 요구사항이나 범위를 조정한다.
--  .ai-dev\README.md:423:
--  .ai-dev\README.md:424:`review-prompt.md`에는 diff, 테스트 결과, 로컬 경로, 민감 정보가 포함될 수 있다. ChatGPT에 붙여넣기 전
--에 사용자가 공유 범위를 확인해야 한다
--.
--  .ai-dev\README.md:566:- `scripts/ai-dev-run-codex.ps1`: `.ai-dev/current-task-prompt.md`를 Codex 
--CLI에 전달해 현재 task 구현을 
--수행한다.
--  .ai-dev\README.md:567:- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Codex
-- CLI에 전달해 JSON 리뷰를 생성
--하고 저장 흐름에 연결한다.
--> .ai-dev\README.md:568:- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 저
--장, Codex 리뷰, save-rev
--iew, pass 시 commit/complete-task를 연결한다.
--  .ai-dev\README.md:569:- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력하
--면 goal/queue/state 생성
--, current-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
--  .ai-dev\README.md:570:
--  .ai-dev\README.md:571:초기 안전 기준:
--  .ai-dev\README.md:572:
--  .ai-dev\README.md:574:- Codex 구현 실행은 `AllowCodex`가 있을 때만 수행한다.
--  .ai-dev\README.md:575:- Codex 리뷰 실행은 `AllowReviewCodex`가 있을 때만 수행한다.
--> .ai-dev\README.md:576:- git commit은 `AllowCommit`이 있을 때만 수행한다.
--  .ai-dev\README.md:577:- auto-goal에서 실제 full cycle 실행은 `AllowRun` 또는 명시적 실행 옵션이 있을 때만 수행한다.
--  .ai-dev\README.md:578:- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
--> .ai-dev\README.md:579:- build/check 실패, package 파일 변경, 리뷰 decision이 `pass`가 아닌 경우 자동 커밋하지 않는다.
--  .ai-dev\README.md:580:- `git reset`, `git clean`, `npm install`, `git push`는 자동 실행하지 않는다.
--  .ai-dev\README.md:581:
--  .ai-dev\README.md:582:자동화 로그와 결과 파일:
--  .ai-dev\README.md:583:
--  .ai-dev\README.md:623:```
--  .ai-dev\README.md:624:
--> .ai-dev\README.md:625:이 스크립트는 Codex에 전달하는 프롬프트 끝에 추가 안전 규칙을 붙인다. git commit, git reset, git clea
--n, npm install, packa
--ge 파일 수정, 현재 task 범위 밖 작업을 금지한다. 스크립트 자체도 build/test/lint, git add, git commit을 실행하지 않는다.
--  .ai-dev\README.md:626:
--  .ai-dev\README.md:627:### Codex 리뷰 실행
--  .ai-dev\README.md:628:
--  .ai-dev\README.md:629:`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/review-r
--esponse.json`에 저장한다. 
--실행 전 `git status --short`를 확인하고, 작업 트리가 dirty이면 기본 중단한다. 리뷰 대상 변경사항이 남아 있는 상태에서 실행해야 하는 경우에만 `-All
--owDirty`를 명시한다.
--  .ai-dev\README.md:667:- 이 스크립트는 git add, git commit, build/test/lint, GPT API 호출을 수행하지 않는다.
--  .ai-dev\README.md:668:
--> .ai-dev\README.md:669:### full auto-cycle 초안
--  .ai-dev\README.md:670:
--  .ai-dev\README.md:671:`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build chec
--k, diff 저장, strict re
--view prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--  .ai-dev\README.md:672:
--> .ai-dev\README.md:673:T004 버전의 실행 단계:
--  .ai-dev\README.md:674:
--  .ai-dev\README.md:675:1. `ai-dev-make-prompt.ps1`
--  .ai-dev\README.md:676:2. `ai-dev-run-codex.ps1`
--  .ai-dev\README.md:677:3. `ai-dev-check.ps1 -BuildOnly`
--  .ai-dev\README.md:692:```
--  .ai-dev\README.md:693:
--> .ai-dev\README.md:694:기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제
--한이며, T004 초안은 task 완료
-- 처리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--  .ai-dev\README.md:695:
--  .ai-dev\README.md:696:```powershell
--  .ai-dev\README.md:697:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
--1 -DryRun -MaxTasks 1
-- -MaxSteps 20
--  .ai-dev\README.md:698:```
--  .ai-dev\README.md:716:```
--  .ai-dev\README.md:717:
--> .ai-dev\README.md:718:`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-d
--ev-complete-task.ps1`
-- 연결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다
--.
--  .ai-dev\README.md:719:
--  .ai-dev\README.md:720:### 목표 입력 기반 자동 실행
--  .ai-dev\README.md:721:
--  .ai-dev\README.md:722:최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게
-- 하는 것이다.
--  .ai-dev\README.md:725:
--  .ai-dev\README.md:726:```powershell
--> .ai-dev\README.md:727:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
--oalTitle "업무 검색 결과 하이
--라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -
--AllowCommit -MaxTasks
-- 1
--  .ai-dev\README.md:728:```
--  .ai-dev\README.md:729:
--  .ai-dev\README.md:730:예정 동작:
--  .ai-dev\README.md:731:
--  .ai-dev\README.md:743:- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
--  .ai-dev\README.md:744:- full cycle 호출은 `-AllowRun` 또는 그에 준하는 명시적 실행 옵션이 있을 때만 수행한다.
--> .ai-dev\README.md:745:- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커
--밋하지 않는다.
--  .ai-dev\README.md:746:
--  .ai-dev\README.md:747:## 운영 원칙
--  .ai-dev\README.md:748:
--  .ai-dev\README.md:749:- 저장소의 `AGENTS.md`, 사용자 지시, 보안 정책이 자동 개발 루프보다 우선한다.
--
--
--
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,11 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +330,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +364,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +375,79 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+        if ($AllowCommit) {
-+            $command = "$command -AllowCommit"
-+        }
- 
--exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path .ai-d
--ev/README.md | Select-Object -Skip 668 -First 55' in D:\ai-apps\planpilot-local
-- succeeded in 1406ms:
--### full auto-cycle 초안
-+        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+            $command = "$command -CommitFiles $($CommitFiles -join ',')"
-+        }
- 
--`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revie
--w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-+        $script:steps += New-StepResult $stepNumber "run-codex" "powershell -ExecutionPolicy Bypas
-s
-+ -File scripts/ai-dev-run-codex.ps1" $false $true 1 "Codex 구현 실행에는 -AllowCodex가 필요합니다. 추천 명령: $com
+-h)"
+-        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCo
 m
-+and"
-+        Stop-Cycle $script:steps "allow_codex_required" $false 1
-+    }
- 
--T004 버전의 실행 단계:
-+    $runCodexArguments = @()
-+    if ($AllowDirty) {
-+        $runCodexArguments += "-AllowDirty"
-+    }
- 
--1. `ai-dev-make-prompt.ps1`
--2. `ai-dev-run-codex.ps1`
--3. `ai-dev-check.ps1 -BuildOnly`
--4. `ai-dev-save-diff.ps1`
--5. `ai-dev-make-review-prompt.ps1 -Strict`
--6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
-+    Invoke-CycleCommand $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass -File scripts/
-a
-+i-dev-run-codex.ps1" $scriptPaths.runCodex $runCodexArguments
-+    $stepNumber++
- 
--먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-+    Invoke-CycleCommand $stepNumber "check" "powershell -ExecutionPolicy Bypass -File scripts/ai-d
-e
-+v-check.ps1 -BuildOnly" $scriptPaths.check @("-BuildOnly")
-+    $stepNumber++
- 
--```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun
--```
-+    Invoke-CycleCommand $stepNumber "save-diff" "powershell -ExecutionPolicy Bypass -File scripts/
-a
-+i-dev-save-diff.ps1" $scriptPaths.saveDiff @()
-+    $stepNumber++
- 
--자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다.
-+    Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File
- 
-+scripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-+    $stepNumber++
- 
--```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
--```
-+    if (-not $AllowReviewCodex -and -not $DryRun) {
-+        $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -A
-l
-+lowCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+        if ($AllowDirty) {
-+            $command = "$command -AllowDirty"
-+        }
- 
--기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처리
--를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
-+        if ($AllowCommit) {
-+            $command = "$command -AllowCommit"
-+        }
- 
--```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -M
--axSteps 20
--```
-+        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+            $command = "$command -CommitFiles $($CommitFiles -join ',')"
-+        }
- 
--`-AllowCodex` 없이 실제 실행하면 prompt 생성 이후 Codex 구현 실행 직전에서 중단하고 추천 명령을 출력한다. Codex 구현까지 허용하려면 명시적으로 `-
--AllowCodex`를 지정한다.
-+        $script:steps += New-StepResult $stepNumber "run-review-codex" "powershell -ExecutionPolic
-y
-+ Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $false $true 1 "Codex 리
-뷰
-+ 실행에는 -AllowReviewCodex가 필요합니다. 추천 명령: $command"
-+        Stop-Cycle $script:steps "allow_review_codex_required" $false 1
-+    }
- 
--```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex
--```
-+    Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File s
-c
-+ripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDi
-r
-+ty", "-SaveReview")
-+    $stepNumber++
- 
--Codex 구현과 Codex 리뷰까지 이어가려면 `-AllowCodex`와 `-AllowReviewCodex`를 함께 지정한다.
-+    if ($DryRun) {
-+        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $f
-a
-+lse $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+        $stepNumber++
-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain 
+-mitHash 확인" $false $true 1 $message
+-        Stop-Cycle $script:steps "commit_not_confirmed" $false 1
+-    }
 -
-+- package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+        $stepNumber++
-+        $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -
-F
-+ile scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-+        $stepNumber++
-+        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCo
-m
-+mitHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
-+        $stepNumber++
-+        $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy B
-y
-+pass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"자동 완료: Codex 구현, build/check, Codex 리
-뷰
-+ pass, 자동 커밋 완료`"" $false $true 0 "DryRun: task 완료 처리를 실행하지 않았습니다."
-+        Stop-Cycle $script:steps "dry_run" $false 0
-+    }
- 
--```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
--ewCodex -MaxTasks 1
--```
-+    try {
-+        $reviewGate = Get-ReviewGate
-+    } catch {
-+        $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $fals
-e
-+ $false 1 $_.Exception.Message
-+        Stop-Cycle $script:steps "review_gate_failed" $false 1
-+    }
- 
--dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-co
--dex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-+    if ($reviewGate.lastCommandStatus -ne "passed") {
-+        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fa
-l
-+se $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+        Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+    }
- 
--```powershell
--powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRevi
--ewCodex -AllowDirty -MaxTasks 1
--```
-+    if ($reviewGate.decision -ne "pass") {
-+        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $f
-a
-+lse $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+        Stop-Cycle $script:steps "review_not_pass" $false 0
-+    }
- 
--`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연결
--은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
-+    if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+        $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath nex
-t
-+_step 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($re
-v
-+iewGate.nextStep)"
-+        Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+    }
- 
--### 목표 입력 기반 자동 실행
-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false
- 
-+$false 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+    $stepNumber++
- 
--최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 하는 것이다.
-+    try {
-+        if (Test-PackageFileChanged) {
-+            $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcel
-a
-+in -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자
-동
-+ 커밋하지 않습니다."
-+            Stop-Cycle $script:steps "package_files_changed" $false 1
-+        }
-+    } catch {
-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain 
--
-+- package.json package-lock.json" $false $false 1 $_.Exception.Message
-+        Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+    }
- 
-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- p
-a
-+ckage.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+    $stepNumber++
- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,11 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +330,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +364,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +375,79 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+    if (-not $AllowCommit) {
-+        $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.
+-    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommit
+H
+-ash 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
+-    $stepNumber++
+-
+-    $resultSummary = "자동 완료: Codex 구현, build/check, Codex 리뷰 pass, 자동 커밋 완료"
+-    Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scri
 p
-+s1 -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+        if ($AllowDirty) {
-+            $commitCommand = "$commitCommand -AllowDirty"
-+        }
- 
--codex
--README의 full cycle 설명은 아직 T004 기준이라 실제 동작과 어긋납니다. 해당 섹션만 갱신하고, 다른 정책/운영 문구는 그대로 두겠습니다.
--apply patch
--patch: completed
--D:\ai-apps\planpilot-local\.ai-dev\README.md
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..b50e832b1d99857130daa9949b9f52658b299bb3
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,10 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +695,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +713,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,11 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +330,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +364,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +375,79 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
--
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..b50e832b1d99857130daa9949b9f52658b299bb3
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,10 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +695,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +713,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,11 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +330,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +364,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +375,79 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+            $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+        }
- 
--codex
--문서까지 맞췄습니다. 이제 수정한 PowerShell 파일의 파서 문법 검증과 full cycle DryRun을 실행해 실제 하위 스크립트, build, git commit이 
--호출되지 않는 범위에서 흐름을 확인하겠습니다.
--exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$tokens = $null; $parse
--Errors = $null; $null = [System.Management.Automation.Language.Parser]::ParseFile('"'scripts/ai-de
--v-auto-cycle-full.ps1', [ref]"'$tokens, [ref]$parseErrors); if ($parseErrors.Count -gt 0) { $parse
--Errors | ForEach-Object { $_.Message }; exit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-l
--ocal
--exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'powershell -ExecutionPo
--licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -MaxSteps 20 -Json' in D:
--\ai-apps\planpilot-local
-- succeeded in 1811ms:
--syntax ok
-+        $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -
-F
-+ile scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+        Stop-Cycle $script:steps "allow_commit_required" $false 1
-+    }
- 
-- succeeded in 3087ms:
--{
--    "steps":  [
--                  {
--                      "step":  1,
--                      "name":  "make-prompt",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pr
--ompt.ps1",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
--                  },
--                  {
--                      "step":  2,
--                      "name":  "run-codex",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-cod
--ex.ps1",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
--                  },
--                  {
--                      "step":  3,
--                      "name":  "check",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.p
--s1 -BuildOnly",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
--                  },
--                  {
--                      "step":  4,
--                      "name":  "save-diff",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-di
--ff.ps1",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
--                  },
--                  {
--                      "step":  5,
--                      "name":  "make-review-prompt",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-re
--view-prompt.ps1 -Strict",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
--                  },
--                  {
--                      "step":  6,
--                      "name":  "run-review-codex",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-rev
--iew-codex.ps1 -AllowDirty -SaveReview",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
--                  },
--                  {
--                      "step":  7,
--                      "name":  "review-gate",
--                      "command":  "state.lastReviewDecision 확인",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--                  },
--                  {
--                      "step":  8,
--                      "name":  "package-change-gate",
--                      "command":  "git status --porcelain -- package.json package-lock.json",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--                  },
--                  {
--                      "step":  9,
--                      "name":  "commit",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.
--ps1",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: git add/commit을 실행하지 않았습니다."
--                  },
--                  {
--                      "step":  10,
--                      "name":  "complete-task",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complet
--e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
--                      "executed":  false,
--                      "skipped":  true,
--                      "exitCode":  0,
--                      "message":  "DryRun: task 완료 처리를 실행하지 않았습니다."
--                  }
--              ],
--    "stoppedReason":  "dry_run",
--    "completed":  false,
--    "exitCode":  0
-+    $commitArguments = Get-CommitArguments
-+    $commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+    if ($commitArguments.Count -gt 0) {
-+        $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+    }
-+
-+    Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArgumen
+-ts/ai-dev-complete-task.ps1 -ResultSummary `"$resultSummary`"" $scriptPaths.completeTask @("-Resul
 t
-+s
-+    $stepNumber++
-+
-+    try {
-+        $commitGate = Get-CommitGate
-+    } catch {
-+        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCo
-m
-+mitHash 확인" $false $false 1 $_.Exception.Message
-+        Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-+    }
-+
-+    if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -n
-o
-+t (Test-HasValue $commitGate.lastCommitHash)) {
-+        $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastComm
-a
-+nd), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHa
-s
-+h)"
-+        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCo
-m
-+mitHash 확인" $false $true 1 $message
-+        Stop-Cycle $script:steps "commit_not_confirmed" $false 1
-+    }
-+
-+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommit
-H
-+ash 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
-+    $stepNumber++
-+
-+    $resultSummary = "자동 완료: Codex 구현, build/check, Codex 리뷰 pass, 자동 커밋 완료"
-+    Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scri
-p
-+ts/ai-dev-complete-task.ps1 -ResultSummary `"$resultSummary`"" $scriptPaths.completeTask @("-Resul
-t
-+Summary", $resultSummary)
-+    $stepNumber++
-+    $completedTaskCount++
- }
- 
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..b50e832b1d99857130daa9949b9f52658b299bb3
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,10 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +695,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +713,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,61 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +304,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +318,11 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +330,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +364,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +375,79 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+Stop-Cycle $script:steps "max_tasks_reached" $true 0
+-Summary", $resultSummary)
+-    $stepNumber++
+-    $completedTaskCount++
+-}
+-
+-Stop-Cycle $script:steps "max_tasks_reached" $true 0
+-
+- succeeded in 3115ms:
+-param(
+-    [AllowEmptyString()]
+-    [string]$GoalTitle,
+-    [AllowEmptyString()]
+-    [string]$GoalDescription,
+-    [int]$MaxTasks = 1,
+-    [int]$MaxSteps = 20,
+-    [switch]$DryRun,
+-    [switch]$Json,
+-    [switch]$AllowRun,
+-    [switch]$AllowCodex,
+-    [switch]$AllowReviewCodex,
+-    [switch]$AllowCommit,
+-    [switch]$AllowDirty,
+-    [string[]]$CommitFiles,
+-    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
+-)
+-
+-. "$PSScriptRoot\ai-dev-env.ps1"
+-
+-$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
+-$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
+-$codeFence = '```'
+-$goalRelativePath = ".ai-dev/goal.md"
+-$queueRelativePath = ".ai-dev/queue.json"
+-$stateRelativePath = ".ai-dev/state.json"
+-$promptRelativePath = ".ai-dev/current-task-prompt.md"
+-
+-function Resolve-RepoPath {
+-    param([string]$Path)
+-
+-    if ([System.IO.Path]::IsPathRooted($Path)) {
+-        return $Path
+-    }
+-
+-    return Join-Path $repoRoot $Path
+-}
+-
+-function ConvertTo-RepoRelativePath {
+-    param([string]$Path)
+-
+-    $fullPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $Path))
+-    $rootWithSeparator = $repoRoot.TrimEnd("\") + "\"
+-
+-    if ($fullPath.StartsWith($rootWithSeparator, [System.StringComparison]::OrdinalIgnoreCase)) {
+-        return $fullPath.Substring($rootWithSeparator.Length).Replace("\", "/")
+-    }
+-
+-    return $fullPath.Replace("\", "/")
+-}
+-
+-function Test-HasValue {
+-    param([object]$Value)
+-
+-    if ($null -eq $Value) {
+-        return $false
+-    }
+-
+-    if ($Value -is [string]) {
+-        return -not [string]::IsNullOrWhiteSpace($Value)
+-    }
+-
+-    return $true
+-}
+-
+-function New-StepResult {
+-    param(
+-        [int]$Step,
+-        [string]$Name,
+-        [string]$Command,
+-        [bool]$Executed,
+-        [bool]$Skipped,
+-        [int]$ExitCode,
+-        [string]$Message
+-    )
+-
+-    return [PSCustomObject][ordered]@{
+-        step = $Step
+-        name = $Name
+-        command = $Command
+-        executed = $Executed
+-        skipped = $Skipped
+-        exitCode = $ExitCode
+-        message = $Message
+-    }
+-}
+-
+-function New-AutoGoalResult {
+-    param(
+-        [object[]]$Steps,
+-        [string]$StoppedReason,
+-        [bool]$Completed,
+-        [int]$ExitCode
+-    )
+-
+-    return [PSCustomObject][ordered]@{
+-        steps = @($Steps)
+-        stoppedReason = $StoppedReason
+-        completed = $Completed
+-        exitCode = $ExitCode
+-        plan = $script:autoGoalPlanPreview
+-        goalPath = $goalRelativePath
+-        queuePath = $queueRelativePath
+-        statePath = $stateRelativePath
+-        promptPath = $promptRelativePath
+-        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
+-    }
+-}
+-
+-function Write-AutoGoalResult {
+-    param([object]$Result)
+-
+-    if ($Json) {
+-        $Result | ConvertTo-Json -Depth 30
+-        return
+-    }
+-
+-    foreach ($step in @($Result.steps)) {
+-        Write-Host "Step $($step.step): $($step.name)"
+-        Write-Host "  Command: $($step.command)"
+-        Write-Host "  Executed: $($step.executed)"
+-        Write-Host "  Skipped: $($step.skipped)"
+-        Write-Host "  Exit code: $($step.exitCode)"
+-        Write-Host "  Message: $($step.message)"
+-    }
+-
+-    if ($null -ne $Result.plan) {
+-        Write-Host "Plan preview:"
+-        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)"
+-        Write-Host "  Current task id: $($Result.plan.queue.currentTaskId)"
+-
+-        foreach ($task in @($Result.plan.queue.tasks)) {
+-            Write-Host "  Task $($task.id): $($task.title)"
+-            Write-Host "    Type: $($task.type)"
+-            Write-Host "    Status: $($task.status)"
+-            Write-Host "    Priority: $($task.priority)"
+-            Write-Host "    Likely files: $((@($task.filesLikelyToChange) -join ', '))"
+-            Write-Host "    Verification: $((@($task.verification) -join ' / '))"
+-        }
+-    }
+-
+-    Write-Host "Stopped reason: $($Result.stoppedReason)"
+-    Write-Host "Completed: $($Result.completed)"
+-    Write-Host "Exit code: $($Result.exitCode)"
+-}
+-
+-function Stop-AutoGoal {
+-    param(
+-        [object[]]$Steps,
+-        [string]$StoppedReason,
+-        [bool]$Completed,
+-        [int]$ExitCode
+-    )
+-
+-    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReason $Completed $ExitCode)
+-    exit $ExitCode
+-}
+-
+-function Get-InputPreview {
+-    param([string]$RawInput)
+-
+-    if ($null -eq $RawInput) {
+-        return "<null>"
+-    }
+-
+-    $normalized = $RawInput.Replace("`r", " ").Replace("`n", " ").Trim()
+-
+-    if ($normalized.Length -eq 0) {
+-        return "<empty>"
+-    }
+-
+-    if ($normalized.Length -le 500) {
+-        return $normalized
+-    }
+-
+-    return $normalized.Substring(0, 500)
+-}
+-
+-function Get-GitStatusLines {
+-    $statusOutput = & git status --short 2>&1
+-
+-    if ($LASTEXITCODE -ne 0) {
+-        $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false
  
--codex
--DryRun은 10단계 전체를 보여주며 하위 실행이 없음을 확인했습니다. 추가로, `ai-dev-commit.ps1`은 변경사항이 없으면 exit 0으로 끝날 수 있어 full
-- cycle이 실제 커밋 없이 complete-task로 넘어갈 수 있습니다. 이 경우를 막기 위해 커밋 후 state에 `lastCommand=commit`, `passed`
--, `lastCommitHash`가 기록됐는지 확인하는 게이트를 하나 더 넣겠습니다.
--apply patch
--patch: completed
--D:\ai-apps\planpilot-local\scripts\ai-dev-auto-cycle-full.ps1
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..b50e832b1d99857130daa9949b9f52658b299bb3
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,10 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +695,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +713,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..84c3a6c8713a84a4304f6cc025a4949946302bb5
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,97 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
--
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..b50e832b1d99857130daa9949b9f52658b299bb3
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,10 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +695,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +713,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..84c3a6c8713a84a4304f6cc025a4949946302bb5
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,97 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+ succeeded in 3115ms:
-+param(
-+    [AllowEmptyString()]
-+    [string]$GoalTitle,
-+    [AllowEmptyString()]
-+    [string]$GoalDescription,
-+    [int]$MaxTasks = 1,
-+    [int]$MaxSteps = 20,
-+    [switch]$DryRun,
-+    [switch]$Json,
-+    [switch]$AllowRun,
-+    [switch]$AllowCodex,
-+    [switch]$AllowReviewCodex,
-+    [switch]$AllowCommit,
-+    [switch]$AllowDirty,
-+    [string[]]$CommitFiles,
-+    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
-+)
-+
-+. "$PSScriptRoot\ai-dev-env.ps1"
-+
-+$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-+$codeFence = '```'
-+$goalRelativePath = ".ai-dev/goal.md"
-+$queueRelativePath = ".ai-dev/queue.json"
-+$stateRelativePath = ".ai-dev/state.json"
-+$promptRelativePath = ".ai-dev/current-task-prompt.md"
-+
-+function Resolve-RepoPath {
-+    param([string]$Path)
-+
-+    if ([System.IO.Path]::IsPathRooted($Path)) {
-+        return $Path
-+    }
-+
-+    return Join-Path $repoRoot $Path
-+}
-+
-+function ConvertTo-RepoRelativePath {
-+    param([string]$Path)
-+
-+    $fullPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $Path))
-+    $rootWithSeparator = $repoRoot.TrimEnd("\") + "\"
-+
-+    if ($fullPath.StartsWith($rootWithSeparator, [System.StringComparison]::OrdinalIgnoreCase)) {
-+        return $fullPath.Substring($rootWithSeparator.Length).Replace("\", "/")
-+    }
-+
-+    return $fullPath.Replace("\", "/")
-+}
-+
-+function Test-HasValue {
-+    param([object]$Value)
-+
-+    if ($null -eq $Value) {
-+        return $false
-+    }
-+
-+    if ($Value -is [string]) {
-+        return -not [string]::IsNullOrWhiteSpace($Value)
-+    }
-+
-+    return $true
-+}
-+
-+function New-StepResult {
-+    param(
-+        [int]$Step,
-+        [string]$Name,
-+        [string]$Command,
-+        [bool]$Executed,
-+        [bool]$Skipped,
-+        [int]$ExitCode,
-+        [string]$Message
-+    )
-+
-+    return [PSCustomObject][ordered]@{
-+        step = $Step
-+        name = $Name
-+        command = $Command
-+        executed = $Executed
-+        skipped = $Skipped
-+        exitCode = $ExitCode
-+        message = $Message
-+    }
-+}
-+
-+function New-AutoGoalResult {
-+    param(
-+        [object[]]$Steps,
-+        [string]$StoppedReason,
-+        [bool]$Completed,
-+        [int]$ExitCode
-+    )
-+
-+    return [PSCustomObject][ordered]@{
-+        steps = @($Steps)
-+        stoppedReason = $StoppedReason
-+        completed = $Completed
-+        exitCode = $ExitCode
-+        plan = $script:autoGoalPlanPreview
-+        goalPath = $goalRelativePath
-+        queuePath = $queueRelativePath
-+        statePath = $stateRelativePath
-+        promptPath = $promptRelativePath
-+        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-+    }
-+}
-+
-+function Write-AutoGoalResult {
-+    param([object]$Result)
-+
-+    if ($Json) {
-+        $Result | ConvertTo-Json -Depth 30
-+        return
-+    }
-+
-+    foreach ($step in @($Result.steps)) {
-+        Write-Host "Step $($step.step): $($step.name)"
-+        Write-Host "  Command: $($step.command)"
-+        Write-Host "  Executed: $($step.executed)"
-+        Write-Host "  Skipped: $($step.skipped)"
-+        Write-Host "  Exit code: $($step.exitCode)"
-+        Write-Host "  Message: $($step.message)"
-+    }
-+
-+    if ($null -ne $Result.plan) {
-+        Write-Host "Plan preview:"
-+        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)"
-+        Write-Host "  Current task id: $($Result.plan.queue.currentTaskId)"
-+
-+        foreach ($task in @($Result.plan.queue.tasks)) {
-+            Write-Host "  Task $($task.id): $($task.title)"
-+            Write-Host "    Type: $($task.type)"
-+            Write-Host "    Status: $($task.status)"
-+            Write-Host "    Priority: $($task.priority)"
-+            Write-Host "    Likely files: $((@($task.filesLikelyToChange) -join ', '))"
-+            Write-Host "    Verification: $((@($task.verification) -join ' / '))"
-+        }
-+    }
-+
-+    Write-Host "Stopped reason: $($Result.stoppedReason)"
-+    Write-Host "Completed: $($Result.completed)"
-+    Write-Host "Exit code: $($Result.exitCode)"
-+}
-+
-+function Stop-AutoGoal {
-+    param(
-+        [object[]]$Steps,
-+        [string]$StoppedReason,
-+        [bool]$Completed,
-+        [int]$ExitCode
-+    )
-+
-+    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReason $Completed $ExitCode)
-+    exit $ExitCode
-+}
-+
-+function Get-InputPreview {
-+    param([string]$RawInput)
-+
-+    if ($null -eq $RawInput) {
-+        return "<null>"
-+    }
-+
-+    $normalized = $RawInput.Replace("`r", " ").Replace("`n", " ").Trim()
-+
-+    if ($normalized.Length -eq 0) {
-+        return "<empty>"
-+    }
-+
-+    if ($normalized.Length -le 500) {
-+        return $normalized
-+    }
-+
-+    return $normalized.Substring(0, 500)
-+}
-+
-+function Get-GitStatusLines {
-+    $statusOutput = & git status --short 2>&1
-+
-+    if ($LASTEXITCODE -ne 0) {
-+        $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false
- 
-+1 "git status failed: $($statusOutput -join "`n")"
-+        Stop-AutoGoal $script:steps "git_status_failed" $false 1
-+    }
-+
-+    return @($statusOutput | Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
-+}
-+
-+function Get-JsonObjectCandidates {
-+    param([string]$RawInput)
-+
-+    $candidates = @()
-+
-+    for ($start = 0; $start -lt $RawInput.Length; $start++) {
-+        if ($RawInput[$start] -ne "{") {
-+            continue
-+        }
-+
-+        $depth = 0
-+        $inString = $false
-+        $escaped = $false
-+
-+        for ($index = $start; $index -lt $RawInput.Length; $index++) {
-+            $char = $RawInput[$index]
-+
-+            if ($escaped) {
-+                $escaped = $false
-+                continue
-+            }
-+
-+            if ($char -eq "\") {
-+                $escaped = $true
-+                continue
-+            }
-+
-+            if ($char -eq '"') {
-+                $inString = -not $inString
-+                continue
-+            }
-+
-+            if ($inString) {
-+                continue
-+            }
-+
-+            if ($char -eq "{") {
-+                $depth++
-+                continue
-+            }
-+
-+            if ($char -eq "}") {
-+                $depth--
-+
-+                if ($depth -eq 0) {
-+                    $candidates += $RawInput.Substring($start, $index - $start + 1).Trim()
-+                    break
-+                }
-+            }
-+        }
-+    }
-+
-+    return @($candidates | Select-Object -Unique)
-+}
-+
-+function Get-MissingFields {
-+    param(
-+        [object]$InputObject,
-+        [string[]]$RequiredFields
-+    )
-+
-+    $missingFields = @()
-+
-+    foreach ($field in $RequiredFields) {
-+        if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
-+            $missingFields += $field
-+        }
-+    }
-+
-+    return $missingFields
-+}
-+
-+function Get-QueueValidationErrors {
-+    param([object]$Queue)
-+
-+    $errors = @()
-+    $requiredFields = @("goalTitle", "goalSource", "createdAt", "updatedAt", "currentTaskId", "tas
+-1 "git status failed: $($statusOutput -join "`n")"
+-        Stop-AutoGoal $script:steps "git_status_failed" $false 1
+-    }
+-
+-    return @($statusOutput | Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
+-}
+-
+-function Get-JsonObjectCandidates {
+-    param([string]$RawInput)
+-
+-    $candidates = @()
+-
+-    for ($start = 0; $start -lt $RawInput.Length; $start++) {
+-        if ($RawInput[$start] -ne "{") {
+-            continue
+-        }
+-
+-        $depth = 0
+-        $inString = $false
+-        $escaped = $false
+-
+-        for ($index = $start; $index -lt $RawInput.Length; $index++) {
+-            $char = $RawInput[$index]
+-
+-            if ($escaped) {
+-                $escaped = $false
+-                continue
+-            }
+-
+-            if ($char -eq "\") {
+-                $escaped = $true
+-                continue
+-            }
+-
+-            if ($char -eq '"') {
+-                $inString = -not $inString
+-                continue
+-            }
+-
+-            if ($inString) {
+-                continue
+-            }
+-
+-            if ($char -eq "{") {
+-                $depth++
+-                continue
+-            }
+-
+-            if ($char -eq "}") {
+-                $depth--
+-
+-                if ($depth -eq 0) {
+-                    $candidates += $RawInput.Substring($start, $index - $start + 1).Trim()
+-                    break
+-                }
+-            }
+-        }
+-    }
+-
+-    return @($candidates | Select-Object -Unique)
+-}
+-
+-function Get-MissingFields {
+-    param(
+-        [object]$InputObject,
+-        [string[]]$RequiredFields
+-    )
+-
+-    $missingFields = @()
+-
+-    foreach ($field in $RequiredFields) {
+-        if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
+-            $missingFields += $field
+-        }
+-    }
+-
+-    return $missingFields
+-}
+-
+-function Get-QueueValidationErrors {
+-    param([object]$Queue)
+-
+-    $errors = @()
+-    $requiredFields = @("goalTitle", "goalSource", "createdAt", "updatedAt", "currentTaskId", "tas
 k
-+s")
-+
-+    foreach ($field in @(Get-MissingFields $Queue $requiredFields)) {
-+        $errors += "queue missing field: $field"
-+    }
-+
-+    if ($errors.Count -gt 0) {
-+        return $errors
-+    }
-+
-+    if (-not (Test-HasValue $Queue.goalTitle)) {
-+        $errors += "queue.goalTitle must not be empty."
-+    }
-+
-+    if ($Queue.goalSource -ne $goalRelativePath) {
-+        $errors += "queue.goalSource must be $goalRelativePath."
-+    }
-+
-+    if ($Queue.tasks -isnot [System.Collections.IEnumerable] -or $Queue.tasks -is [string]) {
-+        $errors += "queue.tasks must be an array."
-+        return $errors
-+    }
-+
-+    $tasks = @($Queue.tasks)
-+
-+    if ($tasks.Count -lt 1) {
-+        $errors += "queue.tasks must contain at least one task."
-+        return $errors
-+    }
-+
-+    if ($tasks.Count -gt 3) {
-+        $errors += "queue.tasks must contain no more than three tasks."
-+    }
-+
-+    $taskIds = @()
-+
-+    foreach ($task in $tasks) {
-+        $taskRequiredFields = @("id", "title", "description", "type", "status", "priority", "depen
+-s")
+-
+-    foreach ($field in @(Get-MissingFields $Queue $requiredFields)) {
+-        $errors += "queue missing field: $field"
+-    }
+-
+-    if ($errors.Count -gt 0) {
+-        return $errors
+-    }
+-
+-    if (-not (Test-HasValue $Queue.goalTitle)) {
+-        $errors += "queue.goalTitle must not be empty."
+-    }
+-
+-    if ($Queue.goalSource -ne $goalRelativePath) {
+-        $errors += "queue.goalSource must be $goalRelativePath."
+-    }
+-
+-    if ($Queue.tasks -isnot [System.Collections.IEnumerable] -or $Queue.tasks -is [string]) {
+-        $errors += "queue.tasks must be an array."
+-        return $errors
+-    }
+-
+-    $tasks = @($Queue.tasks)
+-
+-    if ($tasks.Count -lt 1) {
+-        $errors += "queue.tasks must contain at least one task."
+-        return $errors
+-    }
+-
+-    if ($tasks.Count -gt 3) {
+-        $errors += "queue.tasks must contain no more than three tasks."
+-    }
+-
+-    $taskIds = @()
+-
+-    foreach ($task in $tasks) {
+-        $taskRequiredFields = @("id", "title", "description", "type", "status", "priority", "depen
 d
-+sOn", "filesLikelyToChange", "verification", "commitMessage")
-+        $taskMissingFields = @(Get-MissingFields $task $taskRequiredFields)
-+
-+        foreach ($field in $taskMissingFields) {
-+            $errors += "task missing field: $field"
-+        }
-+
-+        if ($taskMissingFields.Count -gt 0) {
-+            continue
-+        }
-+
-+        if (-not (Test-HasValue $task.id)) {
-+            $errors += "task.id must not be empty."
-+        } else {
-+            $taskIds += [string]$task.id
-+        }
-+
-+        if (-not (Test-HasValue $task.title)) {
-+            $errors += "task.title must not be empty."
-+        }
-+
-+        if (-not (Test-HasValue $task.description)) {
-+            $errors += "task.description must not be empty."
-+        }
-+
-+        if (@("analysis", "implementation", "documentation", "verification") -notcontains $task.ty
+-sOn", "filesLikelyToChange", "verification", "commitMessage")
+-        $taskMissingFields = @(Get-MissingFields $task $taskRequiredFields)
+-
+-        foreach ($field in $taskMissingFields) {
+-            $errors += "task missing field: $field"
+-        }
+-
+-        if ($taskMissingFields.Count -gt 0) {
+-            continue
+-        }
+-
+-        if (-not (Test-HasValue $task.id)) {
+-            $errors += "task.id must not be empty."
+-        } else {
+-            $taskIds += [string]$task.id
+-        }
+-
+-        if (-not (Test-HasValue $task.title)) {
+-            $errors += "task.title must not be empty."
+-        }
+-
+-        if (-not (Test-HasValue $task.description)) {
+-            $errors += "task.description must not be empty."
+-        }
+-
+-        if (@("analysis", "implementation", "documentation", "verification") -notcontains $task.ty
 p
-+e) {
-+            $errors += "invalid task.type: $($task.type)"
-+        }
-+
-+        if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
-+            $errors += "invalid task.status: $($task.status)"
-+        }
-+
-+        if (@("P0", "P1", "P2") -notcontains $task.priority) {
-+            $errors += "invalid task.priority: $($task.priority)"
-+        }
-+
-+        if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is [strin
+-e) {
+-            $errors += "invalid task.type: $($task.type)"
+-        }
+-
+-        if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
+-            $errors += "invalid task.status: $($task.status)"
+-        }
+-
+-        if (@("P0", "P1", "P2") -notcontains $task.priority) {
+-            $errors += "invalid task.priority: $($task.priority)"
+-        }
+-
+-        if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is [strin
 g
-+]) {
-+            $errors += "task.dependsOn must be an array: $($task.id)"
-+        }
-+
-+        if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.filesLikel
+-]) {
+-            $errors += "task.dependsOn must be an array: $($task.id)"
+-        }
+-
+-        if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.filesLikel
 y
-+ToChange -is [string]) {
-+            $errors += "task.filesLikelyToChange must be an array: $($task.id)"
-+        }
-+
-+        if ($task.verification -isnot [System.Collections.IEnumerable] -or $task.verification -is 
+-ToChange -is [string]) {
+-            $errors += "task.filesLikelyToChange must be an array: $($task.id)"
+-        }
+-
+-        if ($task.verification -isnot [System.Collections.IEnumerable] -or $task.verification -is 
 [
-+string]) {
-+            $errors += "task.verification must be an array: $($task.id)"
-+        }
-+    }
-+
-+    if ((@($tasks | Where-Object { $_.status -eq "in_progress" })).Count -ne 1) {
-+        $errors += "queue.tasks must contain exactly one in_progress task."
-+    }
-+
-+    if (-not (Test-HasValue $Queue.currentTaskId)) {
-+        $errors += "queue.currentTaskId must not be empty."
-+    } elseif ($taskIds -notcontains [string]$Queue.currentTaskId) {
-+        $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+    }
-+
-+    return $errors
-+}
-+
-+function Get-StateValidationErrors {
-+    param(
-+        [object]$State,
-+        [object]$Queue
-+    )
-+
-+    $errors = @()
-+    $requiredFields = @("goalStatus", "currentTaskId", "currentLoop", "maxLoopsPerTask", "repeated
+-string]) {
+-            $errors += "task.verification must be an array: $($task.id)"
+-        }
+-    }
+-
+-    if ((@($tasks | Where-Object { $_.status -eq "in_progress" })).Count -ne 1) {
+-        $errors += "queue.tasks must contain exactly one in_progress task."
+-    }
+-
+-    if (-not (Test-HasValue $Queue.currentTaskId)) {
+-        $errors += "queue.currentTaskId must not be empty."
+-    } elseif ($taskIds -notcontains [string]$Queue.currentTaskId) {
+-        $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-    }
+-
+-    return $errors
+-}
+-
+-function Get-StateValidationErrors {
+-    param(
+-        [object]$State,
+-        [object]$Queue
+-    )
+-
+-    $errors = @()
+-    $requiredFields = @("goalStatus", "currentTaskId", "currentLoop", "maxLoopsPerTask", "repeated
 F
-+ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDecision", "lastR
+-ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDecision", "lastR
 e
-+viewSeverity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
-+
-+    foreach ($field in @(Get-MissingFields $State $requiredFields)) {
-+        $errors += "state missing field: $field"
-+    }
-+
-+    if ($errors.Count -gt 0) {
-+        return $errors
-+    }
-+
-+    if (@("idle", "in_progress", "completed", "blocked") -notcontains $State.goalStatus) {
-+        $errors += "invalid state.goalStatus: $($State.goalStatus)"
-+    }
-+
-+    if ($State.currentTaskId -ne $Queue.currentTaskId) {
-+        $errors += "state.currentTaskId must match queue.currentTaskId."
-+    }
-+
-+    if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+        $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+    }
-+
-+    if (@("not_started", "pass", "revise", "blocked") -notcontains $State.lastReviewDecision) {
-+        $errors += "invalid state.lastReviewDecision: $($State.lastReviewDecision)"
-+    }
-+
-+    return $errors
-+}
-+
-+function Get-AutoGoalValidationErrors {
-+    param([object]$Plan)
-+
-+    $errors = @()
-+
-+    foreach ($field in @(Get-MissingFields $Plan @("goalMarkdown", "queue", "state"))) {
-+        $errors += "plan missing field: $field"
-+    }
-+
-+    if ($errors.Count -gt 0) {
-+        return $errors
-+    }
-+
-+    if (-not (Test-HasValue $Plan.goalMarkdown)) {
-+        $errors += "goalMarkdown must not be empty."
-+    }
-+
-+    $errors += @(Get-QueueValidationErrors $Plan.queue)
-+    $errors += @(Get-StateValidationErrors $Plan.state $Plan.queue)
-+
-+    return $errors
-+}
-+
-+function New-DryRunAutoGoalPlan {
-+    param(
-+        [string]$Title,
-+        [string]$Description
-+    )
-+
-+    $now = (Get-Date).ToUniversalTime().ToString("o")
-+    $safeCommitTitle = $Title.Trim()
-+
-+    $plan = [PSCustomObject][ordered]@{
-+        goalMarkdown = @"
-+# 紐⑺몴
-+
-+$safeCommitTitle
-+
-+## 諛곌꼍
-+
-+$($Description.Trim())
-+
-+## ?깃났 湲곗?
-+
-+- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+
-+## ?쒖빟?ы빆
-+
-+- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+
-+## ?쒖쇅 踰붿쐞
-+
-+- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+
-+## ?섎룞 ?뺤씤
-+
-+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+"@
-+        queue = [PSCustomObject][ordered]@{
-+            goalTitle = $safeCommitTitle
-+            goalSource = $goalRelativePath
-+            createdAt = $now
-+            updatedAt = $now
-+            currentTaskId = "T001"
-+            tasks = @(
-+                [PSCustomObject][ordered]@{
-+                    id = "T001"
-+                    title = $safeCommitTitle
-+                    description = $Description.Trim()
-+                    type = "implementation"
-+                    status = "in_progress"
-+                    priority = "P0"
-+                    dependsOn = @()
-+                    filesLikelyToChange = @()
-+                    verification = @(
-+                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+                    )
-+                    commitMessage = $null
-+                }
-+            )
-+        }
-+        state = [PSCustomObject][ordered]@{
-+            goalStatus = "in_progress"
-+            currentTaskId = "T001"
-+            currentLoop = 0
-+            maxLoopsPerTask = 2
-+            repeatedFailureCount = 0
-+            lastCommand = $null
-+            lastCommandStatus = "not_started"
-+            lastErrorSummary = $null
-+            lastReviewDecision = "not_started"
-+            lastReviewSeverity = $null
-+            lastCommitHash = $null
-+            startedAt = $now
-+            updatedAt = $now
-+            stopReason = $null
-+        }
-+    }
-+
-+    $plan.goalMarkdown = @"
-+# 紐⑺몴
-+
-+$safeCommitTitle
-+
-+## 諛곌꼍
-+
-+$($Description.Trim())
-+
-+## ?깃났 湲곗?
-+
-+- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-+- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+
-+## ?쒖빟?ы빆
-+
-+- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+
-+## ?쒖쇅 踰붿쐞
-+
-+- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+
-+## ?섎룞 ?뺤씤
-+
-+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+"@
-+
-+    $dryRunTask = @($plan.queue.tasks)[0]
-+    $dryRunTask.verification = @(
-+        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+    )
-+
-+    return $plan
-+}
-+
-+function ConvertFrom-CodexAutoGoalOutput {
-+    param([string]$RawInput)
-+
-+    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-+        try {
-+            $parsed = $candidate | ConvertFrom-Json
-+
-+            if ($null -eq $parsed -or $parsed -is [System.Array]) {
-+                continue
-+            }
-+
-+            if (@(Get-AutoGoalValidationErrors $parsed).Count -gt 0) {
-+                continue
-+            }
-+
-+            return [PSCustomObject]@{
-+                Parsed = $parsed
-+                JsonText = ($parsed | ConvertTo-Json -Depth 30)
-+            }
-+        } catch {
-+        }
-+    }
-+
-+    throw "No valid auto-goal JSON object was found in Codex output."
-+}
-+
-+function New-CodexAutoGoalPrompt {
-+    param(
-+        [string]$Title,
-+        [string]$Description
-+    )
-+
-+    return @"
-+You are preparing local AI Dev Loop state files for the repository at $repoRoot.
-+
-+Return exactly one JSON object and no markdown fences or commentary.
-+
-+The user supplied this new goal:
-+Title: $Title
-+Description: $Description
-+
-+Create a small, safe goal plan for this repository. The response object must have these fields:
-+- goalMarkdown: .ai-dev/goal.md에 작성할 마크다운 문자열입니다. "# 목표", "배경", "성공 기준", "제약사항", "범위 제외", "수동 검증" 
-섹
-+션을 한국어로 포함해야 합니다.
-+- queue: an object for .ai-dev/queue.json.
-+- state: an object for .ai-dev/state.json.
-+
-+queue rules:
-+- goalTitle must equal the supplied title.
-+- goalSource must be ".ai-dev/goal.md".
-+- currentTaskId must be "T001".
-+- tasks must contain one to three tasks.
-+- T001 must be status "in_progress"; later tasks, if any, must be "pending".
-+- each task must include id, title, description, type, status, priority, dependsOn, filesLikelyToC
+-viewSeverity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
+-
+-    foreach ($field in @(Get-MissingFields $State $requiredFields)) {
+-        $errors += "state missing field: $field"
+-    }
+-
+-    if ($errors.Count -gt 0) {
+-        return $errors
+-    }
+-
+-    if (@("idle", "in_progress", "completed", "blocked") -notcontains $State.goalStatus) {
+-        $errors += "invalid state.goalStatus: $($State.goalStatus)"
+-    }
+-
+-    if ($State.currentTaskId -ne $Queue.currentTaskId) {
+-        $errors += "state.currentTaskId must match queue.currentTaskId."
+-    }
+-
+-    if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-        $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
+-    }
+-
+-    if (@("not_started", "pass", "revise", "blocked") -notcontains $State.lastReviewDecision) {
+-        $errors += "invalid state.lastReviewDecision: $($State.lastReviewDecision)"
+-    }
+-
+-    return $errors
+-}
+-
+-function Get-AutoGoalValidationErrors {
+-    param([object]$Plan)
+-
+-    $errors = @()
+-
+-    foreach ($field in @(Get-MissingFields $Plan @("goalMarkdown", "queue", "state"))) {
+-        $errors += "plan missing field: $field"
+-    }
+-
+-    if ($errors.Count -gt 0) {
+-        return $errors
+-    }
+-
+-    if (-not (Test-HasValue $Plan.goalMarkdown)) {
+-        $errors += "goalMarkdown must not be empty."
+-    }
+-
+-    $errors += @(Get-QueueValidationErrors $Plan.queue)
+-    $errors += @(Get-StateValidationErrors $Plan.state $Plan.queue)
+-
+-    return $errors
+-}
+-
+-function New-DryRunAutoGoalPlan {
+-    param(
+-        [string]$Title,
+-        [string]$Description
+-    )
+-
+-    $now = (Get-Date).ToUniversalTime().ToString("o")
+-    $safeCommitTitle = $Title.Trim()
+-
+-    $plan = [PSCustomObject][ordered]@{
+-        goalMarkdown = @"
+-# 紐⑺몴
+-
+-$safeCommitTitle
+-
+-## 諛곌꼍
+-
+-$($Description.Trim())
+-
+-## ?깃났 湲곗?
+-
+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-
+-## ?쒖빟?ы빆
+-
+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-
+-## ?쒖쇅 踰붿쐞
+-
+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-
+-## ?섎룞 ?뺤씤
+-
+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-"@
+-        queue = [PSCustomObject][ordered]@{
+-            goalTitle = $safeCommitTitle
+-            goalSource = $goalRelativePath
+-            createdAt = $now
+-            updatedAt = $now
+-            currentTaskId = "T001"
+-            tasks = @(
+-                [PSCustomObject][ordered]@{
+-                    id = "T001"
+-                    title = $safeCommitTitle
+-                    description = $Description.Trim()
+-                    type = "implementation"
+-                    status = "in_progress"
+-                    priority = "P0"
+-                    dependsOn = @()
+-                    filesLikelyToChange = @()
+-                    verification = @(
+-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-                    )
+-                    commitMessage = $null
+-                }
+-            )
+-        }
+-        state = [PSCustomObject][ordered]@{
+-            goalStatus = "in_progress"
+-            currentTaskId = "T001"
+-            currentLoop = 0
+-            maxLoopsPerTask = 2
+-            repeatedFailureCount = 0
+-            lastCommand = $null
+-            lastCommandStatus = "not_started"
+-            lastErrorSummary = $null
+-            lastReviewDecision = "not_started"
+-            lastReviewSeverity = $null
+-            lastCommitHash = $null
+-            startedAt = $now
+-            updatedAt = $now
+-            stopReason = $null
+-        }
+-    }
+-
+-    $plan.goalMarkdown = @"
+-# 紐⑺몴
+-
+-$safeCommitTitle
+-
+-## 諛곌꼍
+-
+-$($Description.Trim())
+-
+-## ?깃났 湲곗?
+-
+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-
+-## ?쒖빟?ы빆
+-
+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-
+-## ?쒖쇅 踰붿쐞
+-
+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-
+-## ?섎룞 ?뺤씤
+-
+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-"@
+-
+-    $dryRunTask = @($plan.queue.tasks)[0]
+-    $dryRunTask.verification = @(
+-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-    )
+-
+-    return $plan
+-}
+-
+-function ConvertFrom-CodexAutoGoalOutput {
+-    param([string]$RawInput)
+-
+-    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
+-        try {
+-            $parsed = $candidate | ConvertFrom-Json
+-
+-            if ($null -eq $parsed -or $parsed -is [System.Array]) {
+-                continue
+-            }
+-
+-            if (@(Get-AutoGoalValidationErrors $parsed).Count -gt 0) {
+-                continue
+-            }
+-
+-            return [PSCustomObject]@{
+-                Parsed = $parsed
+-                JsonText = ($parsed | ConvertTo-Json -Depth 30)
+-            }
+-        } catch {
+-        }
+-    }
+-
+-    throw "No valid auto-goal JSON object was found in Codex output."
+-}
+-
+-function New-CodexAutoGoalPrompt {
+-    param(
+-        [string]$Title,
+-        [string]$Description
+-    )
+-
+-    return @"
+-You are preparing local AI Dev Loop state files for the repository at $repoRoot.
+-
+-Return exactly one JSON object and no markdown fences or commentary.
+-
+-The user supplied this new goal:
+-Title: $Title
+-Description: $Description
+-
+-Create a small, safe goal plan for this repository. The response object must have these fields:
+-- goalMarkdown: .ai-dev/goal.md에 작성할 마크다운 문자열입니다. "# 목표", "배경", "성공 기준", "제약사항", "범위 제외", "수동 검증" 
+섹
+-션을 한국어로 포함해야 합니다.
+-- queue: an object for .ai-dev/queue.json.
+-- state: an object for .ai-dev/state.json.
+-
+-queue rules:
+-- goalTitle must equal the supplied title.
+-- goalSource must be ".ai-dev/goal.md".
+-- currentTaskId must be "T001".
+-- tasks must contain one to three tasks.
+-- T001 must be status "in_progress"; later tasks, if any, must be "pending".
+-- each task must include id, title, description, type, status, priority, dependsOn, filesLikelyToC
 h
-+ange, verification, commitMessage.
-+- type must be one of analysis, implementation, documentation, verification.
-+- priority must be P0, P1, or P2.
-+- dependsOn, filesLikelyToChange, and verification must be arrays.
-+- commitMessage must be null or a short English commit message.
-+
-+state rules:
-+- goalStatus must be "in_progress".
-+- currentTaskId must be "T001".
-+- currentLoop must be 0.
-+- maxLoopsPerTask must be 2.
-+- repeatedFailureCount must be 0.
-+- lastCommand must be null.
-+- lastCommandStatus must be "not_started".
-+- lastErrorSummary must be null.
-+- lastReviewDecision must be "not_started".
-+- lastReviewSeverity must be null.
-+- lastCommitHash must be null.
-+- startedAt and updatedAt must be ISO 8601 strings.
-+- stopReason must be null.
-+
-+Safety rules:
-+- Do not mention server APIs, login, cloud sync, npm install, git reset, git clean, git push, DB d
+-ange, verification, commitMessage.
+-- type must be one of analysis, implementation, documentation, verification.
+-- priority must be P0, P1, or P2.
+-- dependsOn, filesLikelyToChange, and verification must be arrays.
+-- commitMessage must be null or a short English commit message.
+-
+-state rules:
+-- goalStatus must be "in_progress".
+-- currentTaskId must be "T001".
+-- currentLoop must be 0.
+-- maxLoopsPerTask must be 2.
+-- repeatedFailureCount must be 0.
+-- lastCommand must be null.
+-- lastCommandStatus must be "not_started".
+-- lastErrorSummary must be null.
+-- lastReviewDecision must be "not_started".
+-- lastReviewSeverity must be null.
+-- lastCommitHash must be null.
+-- startedAt and updatedAt must be ISO 8601 strings.
+-- stopReason must be null.
+-
+-Safety rules:
+-- Do not mention server APIs, login, cloud sync, npm install, git reset, git clean, git push, DB d
 e
-+letion, or broad rewrites as implementation steps.
-+- Prefer one small implementation task when the goal is small.
-+- Use Korean for user-facing task titles, descriptions, verification, and goalMarkdown.
-+"@
-+}
-+
-+function Invoke-CycleCommand {
-+    param(
-+        [int]$StepNumber,
-+        [string]$Name,
-+        [string]$Command,
-+        [string]$ScriptPath,
-+        [string[]]$Arguments
-+    )
-+
-+    if ($DryRun) {
-+        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: child s
+-letion, or broad rewrites as implementation steps.
+-- Prefer one small implementation task when the goal is small.
+-- Use Korean for user-facing task titles, descriptions, verification, and goalMarkdown.
+-"@
+-}
+-
+-function Invoke-CycleCommand {
+-    param(
+-        [int]$StepNumber,
+-        [string]$Name,
+-        [string]$Command,
+-        [string]$ScriptPath,
+-        [string[]]$Arguments
+-    )
+-
+-    if ($DryRun) {
+-        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: child s
 c
-+ript was not executed."
-+        return
-+    }
-+
-+    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
-+    $exitCode = $LASTEXITCODE
-+    $message = $output.Trim()
-+
-+    if ([string]::IsNullOrWhiteSpace($message)) {
-+        $message = "completed"
-+    }
-+
-+    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-+
-+    if ($exitCode -ne 0) {
-+        Stop-AutoGoal $script:steps "$Name`_failed" $false 1
-+    }
-+}
-+
-+function Get-FullCycleArguments {
-+    $arguments = @("-MaxTasks", ([string]$MaxTasks), "-MaxSteps", ([string]$MaxSteps))
-+
-+    if ($AllowCodex) {
-+        $arguments += "-AllowCodex"
-+    }
-+
-+    if ($AllowReviewCodex) {
-+        $arguments += "-AllowReviewCodex"
-+    }
-+
-+    if ($AllowCommit) {
-+        $arguments += "-AllowCommit"
-+    }
-+
-+    # auto-goal writes goal/queue/state/current prompt/result files after the initial dirty gate.
-+    # The downstream full cycle must tolerate those intended artifacts.
-+    $arguments += "-AllowDirty"
-+
-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -not
+-ript was not executed."
+-        return
+-    }
+-
+-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
+-    $exitCode = $LASTEXITCODE
+-    $message = $output.Trim()
+-
+-    if ([string]::IsNullOrWhiteSpace($message)) {
+-        $message = "completed"
+-    }
+-
+-    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
+-
+-    if ($exitCode -ne 0) {
+-        Stop-AutoGoal $script:steps "$Name`_failed" $false 1
+-    }
+-}
+-
+-function Get-FullCycleArguments {
+-    $arguments = @("-MaxTasks", ([string]$MaxTasks), "-MaxSteps", ([string]$MaxSteps))
+-
+-    if ($AllowCodex) {
+-        $arguments += "-AllowCodex"
+-    }
+-
+-    if ($AllowReviewCodex) {
+-        $arguments += "-AllowReviewCodex"
+-    }
+-
+-    if ($AllowCommit) {
+-        $arguments += "-AllowCommit"
+-    }
+-
+-    # auto-goal writes goal/queue/state/current prompt/result files after the initial dirty gate.
+-    # The downstream full cycle must tolerate those intended artifacts.
+-    $arguments += "-AllowDirty"
+-
+-    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
+-        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -not
  
-+[string]::IsNullOrWhiteSpace($_) })
-+
-+        if ($normalizedFiles.Count -gt 0) {
-+            $arguments += "-CommitFiles"
-+            $arguments += ($normalizedFiles -join ",")
-+        }
-+    }
-+
-+    return $arguments
-+}
-+
-+Set-Location $repoRoot
-+
-+$script:steps = @()
-+$script:autoGoalPlanPreview = $null
-+$resolvedGoalPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $goalRelativePath))
-+$resolvedQueuePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $queueRelativePath))
-+$resolvedStatePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $stateRelativePath))
-+$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $ResultPath))
-+
-+if (-not (Test-HasValue $GoalTitle)) {
-+    $script:steps += New-StepResult 0 "validate-input" "check GoalTitle" $false $false 1 "GoalTitl
+-[string]::IsNullOrWhiteSpace($_) })
+-
+-        if ($normalizedFiles.Count -gt 0) {
+-            $arguments += "-CommitFiles"
+-            $arguments += ($normalizedFiles -join ",")
+-        }
+-    }
+-
+-    return $arguments
+-}
+-
+-Set-Location $repoRoot
+-
+-$script:steps = @()
+-$script:autoGoalPlanPreview = $null
+-$resolvedGoalPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $goalRelativePath))
+-$resolvedQueuePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $queueRelativePath))
+-$resolvedStatePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $stateRelativePath))
+-$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $ResultPath))
+-
+-if (-not (Test-HasValue $GoalTitle)) {
+-    $script:steps += New-StepResult 0 "validate-input" "check GoalTitle" $false $false 1 "GoalTitl
 e
-+ must not be empty."
-+    Stop-AutoGoal $script:steps "invalid_goal_title" $false 1
-+}
-+
-+if (-not (Test-HasValue $GoalDescription)) {
-+    $script:steps += New-StepResult 0 "validate-input" "check GoalDescription" $false $false 1 "Go
+- must not be empty."
+-    Stop-AutoGoal $script:steps "invalid_goal_title" $false 1
+-}
+-
+-if (-not (Test-HasValue $GoalDescription)) {
+-    $script:steps += New-StepResult 0 "validate-input" "check GoalDescription" $false $false 1 "Go
 a
-+lDescription must not be empty."
-+    Stop-AutoGoal $script:steps "invalid_goal_description" $false 1
-+}
-+
-+if ($MaxTasks -lt 1) {
-+    $script:steps += New-StepResult 0 "validate-input" "check MaxTasks" $false $false 1 "MaxTasks 
+-lDescription must not be empty."
+-    Stop-AutoGoal $script:steps "invalid_goal_description" $false 1
+-}
+-
+-if ($MaxTasks -lt 1) {
+-    $script:steps += New-StepResult 0 "validate-input" "check MaxTasks" $false $false 1 "MaxTasks 
 m
-+ust be at least 1."
-+    Stop-AutoGoal $script:steps "max_tasks_must_be_at_least_1" $false 1
-+}
-+
-+if ($MaxSteps -lt 1) {
-+    $script:steps += New-StepResult 0 "validate-input" "check MaxSteps" $false $false 1 "MaxSteps 
+-ust be at least 1."
+-    Stop-AutoGoal $script:steps "max_tasks_must_be_at_least_1" $false 1
+-}
+-
+-if ($MaxSteps -lt 1) {
+-    $script:steps += New-StepResult 0 "validate-input" "check MaxSteps" $false $false 1 "MaxSteps 
 m
-+ust be at least 1."
-+    Stop-AutoGoal $script:steps "max_steps_must_be_at_least_1" $false 1
-+}
-+
-+$makePromptPath = Join-Path $PSScriptRoot "ai-dev-make-prompt.ps1"
-+$autoCycleFullPath = Join-Path $PSScriptRoot "ai-dev-auto-cycle-full.ps1"
-+
-+foreach ($requiredScript in @($makePromptPath, $autoCycleFullPath)) {
-+    if (-not (Test-Path -LiteralPath $requiredScript -PathType Leaf)) {
-+        $script:steps += New-StepResult 0 "prepare" "check required scripts" $false $false 1 "Miss
+-ust be at least 1."
+-    Stop-AutoGoal $script:steps "max_steps_must_be_at_least_1" $false 1
+-}
+-
+-$makePromptPath = Join-Path $PSScriptRoot "ai-dev-make-prompt.ps1"
+-$autoCycleFullPath = Join-Path $PSScriptRoot "ai-dev-auto-cycle-full.ps1"
+-
+-foreach ($requiredScript in @($makePromptPath, $autoCycleFullPath)) {
+-    if (-not (Test-Path -LiteralPath $requiredScript -PathType Leaf)) {
+-        $script:steps += New-StepResult 0 "prepare" "check required scripts" $false $false 1 "Miss
 i
-+ng required script: $requiredScript"
-+        Stop-AutoGoal $script:steps "prepare_failed" $false 1
-+    }
-+}
-+
-+$shouldRunFullCycle = [bool]($AllowRun -or $AllowCodex -or $AllowReviewCodex -or $AllowCommit)
-+$fullCycleArguments = @(Get-FullCycleArguments)
-+$fullCycleCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.p
+-ng required script: $requiredScript"
+-        Stop-AutoGoal $script:steps "prepare_failed" $false 1
+-    }
+-}
+-
+-$shouldRunFullCycle = [bool]($AllowRun -or $AllowCodex -or $AllowReviewCodex -or $AllowCommit)
+-$fullCycleArguments = @(Get-FullCycleArguments)
+-$fullCycleCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.p
 s
-+1 $($fullCycleArguments -join ' ')".Trim()
-+
-+$script:steps += New-StepResult 1 "validate-input" "check GoalTitle/GoalDescription" $false $false
+-1 $($fullCycleArguments -join ' ')".Trim()
+-
+-$script:steps += New-StepResult 1 "validate-input" "check GoalTitle/GoalDescription" $false $false
  
-+0 "Input validation completed: $GoalTitle"
-+
-+if ($DryRun) {
-+    $script:autoGoalPlanPreview = New-DryRunAutoGoalPlan $GoalTitle.Trim() $GoalDescription.Trim()
-+    $previewValidationErrors = @(Get-AutoGoalValidationErrors $script:autoGoalPlanPreview)
-+
-+    if ($previewValidationErrors.Count -gt 0) {
-+        $script:steps += New-StepResult 2 "preview-plan" "local dry-run plan preview" $false $fals
+-0 "Input validation completed: $GoalTitle"
+-
+-if ($DryRun) {
+-    $script:autoGoalPlanPreview = New-DryRunAutoGoalPlan $GoalTitle.Trim() $GoalDescription.Trim()
+-    $previewValidationErrors = @(Get-AutoGoalValidationErrors $script:autoGoalPlanPreview)
+-
+-    if ($previewValidationErrors.Count -gt 0) {
+-        $script:steps += New-StepResult 2 "preview-plan" "local dry-run plan preview" $false $fals
 e
-+ 1 "DryRun preview plan validation failed: $($previewValidationErrors -join '; ')"
-+        Stop-AutoGoal $script:steps "dry_run_preview_invalid" $false 1
-+    }
-+
-+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 0 "D
+- 1 "DryRun preview plan validation failed: $($previewValidationErrors -join '; ')"
+-        Stop-AutoGoal $script:steps "dry_run_preview_invalid" $false 1
+-    }
+-
+-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 0 "D
 r
-+yRun: dirty worktree gate was not executed."
-+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false 
+-yRun: dirty worktree gate was not executed."
+-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false 
 $
-+true 0 "DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, task: $GoalTitl
+-true 0 "DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, task: $GoalTitl
 e
-+"
-+    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation"
+-"
+-    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation"
  
-+$false $true 0 "DryRun: preview goal/queue/state plan passed local schema validation."
-+    $script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, 
+-$false $true 0 "DryRun: preview goal/queue/state plan passed local schema validation."
+-    $script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, 
 $
-+stateRelativePath" $false $true 0 "DryRun: state files were not written."
-+    $script:steps += New-StepResult 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scri
+-stateRelativePath" $false $true 0 "DryRun: state files were not written."
+-    $script:steps += New-StepResult 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scri
 p
-+ts/ai-dev-make-prompt.ps1" $false $true 0 "DryRun: current-task-prompt.md was not generated."
-+
-+    if ($shouldRunFullCycle) {
-+        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "
+-ts/ai-dev-make-prompt.ps1" $false $true 0 "DryRun: current-task-prompt.md was not generated."
+-
+-    if ($shouldRunFullCycle) {
+-        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "
 D
-+ryRun: explicit run option is present, but full cycle was not executed. -AllowRun only invokes the
+-ryRun: explicit run option is present, but full cycle was not executed. -AllowRun only invokes the
  
-+full-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-+    } else {
-+        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "
+-full-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
+-    } else {
+-        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "
 D
-+ryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the ful
+-ryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the ful
 l
-+-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-+    }
-+
-+    Stop-AutoGoal $script:steps "dry_run" $false 0
-+}
-+
-+$statusLines = @(Get-GitStatusLines)
-+
-+if ($statusLines.Count -gt 0 -and -not $AllowDirty) {
-+    $dirtyText = ($statusLines -join "`n")
-+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 1 "W
+--cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
+-    }
+-
+-    Stop-AutoGoal $script:steps "dry_run" $false 0
+-}
+-
+-$statusLines = @(Get-GitStatusLines)
+-
+-if ($statusLines.Count -gt 0 -and -not $AllowDirty) {
+-    $dirtyText = ($statusLines -join "`n")
+-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 1 "W
 o
-+rktree is dirty. Use -AllowDirty only when this is intentional.`n$dirtyText"
-+    Stop-AutoGoal $script:steps "dirty_worktree" $false 1
-+}
-+
-+if ($statusLines.Count -gt 0) {
-+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 "
+-rktree is dirty. Use -AllowDirty only when this is intentional.`n$dirtyText"
+-    Stop-AutoGoal $script:steps "dirty_worktree" $false 1
+-}
+-
+-if ($statusLines.Count -gt 0) {
+-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 "
 A
-+llowDirty is set. DirtyCount: $($statusLines.Count)"
-+} else {
-+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 "
+-llowDirty is set. DirtyCount: $($statusLines.Count)"
+-} else {
+-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 "
 W
-+orktree is clean."
-+}
-+
-+$codexCommand = Get-Command codex -ErrorAction SilentlyContinue
-+
-+if ($null -eq $codexCommand) {
-+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false 
+-orktree is clean."
+-}
+-
+-$codexCommand = Get-Command codex -ErrorAction SilentlyContinue
+-
+-if ($null -eq $codexCommand) {
+-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false 
 $
-+false 1 "Codex CLI was not found."
-+    Stop-AutoGoal $script:steps "codex_not_found" $false 1
-+}
-+
-+$plannerPrompt = New-CodexAutoGoalPrompt $GoalTitle.Trim() $GoalDescription.Trim()
-+$startedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-+$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-+$codexExitCode = $LASTEXITCODE
-+$endedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-+
-+$resultContent = @"
-+# Codex Auto Goal Result
-+
-+## Run
-+
-+- Started at: $startedAt
-+- Ended at: $endedAt
-+- Exit code: $codexExitCode
-+- Goal title: $($GoalTitle.Trim())
-+- Command: codex exec <auto-goal planning prompt>
-+
-+## Output
-+
-+${codeFence}text
-+$codexOutput
-+$codeFence
-+"@
-+
-+[System.IO.File]::WriteAllText($resolvedResultPath, $resultContent, $utf8WithBom)
-+
-+if ($codexExitCode -ne 0) {
-+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $
+-false 1 "Codex CLI was not found."
+-    Stop-AutoGoal $script:steps "codex_not_found" $false 1
+-}
+-
+-$plannerPrompt = New-CodexAutoGoalPrompt $GoalTitle.Trim() $GoalDescription.Trim()
+-$startedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
+-$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
+-$codexExitCode = $LASTEXITCODE
+-$endedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
+-
+-$resultContent = @"
+-# Codex Auto Goal Result
+-
+-## Run
+-
+-- Started at: $startedAt
+-- Ended at: $endedAt
+-- Exit code: $codexExitCode
+-- Goal title: $($GoalTitle.Trim())
+-- Command: codex exec <auto-goal planning prompt>
+-
+-## Output
+-
+-${codeFence}text
+-$codexOutput
+-$codeFence
+-"@
+-
+-[System.IO.File]::WriteAllText($resolvedResultPath, $resultContent, $utf8WithBom)
+-
+-if ($codexExitCode -ne 0) {
+-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $
 f
-+alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
-+    Stop-AutoGoal $script:steps "codex_plan_failed" $false 1
-+}
-+
-+$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $fals
+-alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
+-    Stop-AutoGoal $script:steps "codex_plan_failed" $false 1
+-}
+-
+-$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $fals
 e
-+ 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
-+
-+try {
-+    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-+} catch {
-+    $preview = Get-InputPreview $codexOutput
-+    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation"
+- 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
+-
+-try {
+-    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
+-} catch {
+-    $preview = Get-InputPreview $codexOutput
+-    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation"
  
-+$false $false 1 "Codex plan JSON extraction failed: $($_.Exception.Message) Output preview: $previ
+-$false $false 1 "Codex plan JSON extraction failed: $($_.Exception.Message) Output preview: $previ
 e
-+w"
-+    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
-+}
-+
-+$script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" $fa
+-w"
+-    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
+-}
+-
+-$script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" $fa
 l
-+se $false 0 "goalMarkdown, queue, and state JSON validation completed. currentTaskId: $($autoGoalP
+-se $false 0 "goalMarkdown, queue, and state JSON validation completed. currentTaskId: $($autoGoalP
 l
-+an.Parsed.queue.currentTaskId)"
-+$script:autoGoalPlanPreview = $autoGoalPlan.Parsed
-+
-+$goalMarkdown = [string]$autoGoalPlan.Parsed.goalMarkdown
-+$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 30
-+$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 30
-+
-+[System.IO.File]::WriteAllText($resolvedGoalPath, $goalMarkdown, $utf8WithBom)
-+[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $utf8WithBom)
-+[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $utf8WithBom)
-+
-+$script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, $sta
+-an.Parsed.queue.currentTaskId)"
+-$script:autoGoalPlanPreview = $autoGoalPlan.Parsed
+-
+-$goalMarkdown = [string]$autoGoalPlan.Parsed.goalMarkdown
+-$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 30
+-$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 30
+-
+-[System.IO.File]::WriteAllText($resolvedGoalPath, $goalMarkdown, $utf8WithBom)
+-[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $utf8WithBom)
+-[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $utf8WithBom)
+-
+-$script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, $sta
 t
-+eRelativePath" $true $false 0 "New goal, queue, and state files were written."
-+
-+Invoke-CycleCommand 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-
+-eRelativePath" $true $false 0 "New goal, queue, and state files were written."
+-
+-Invoke-CycleCommand 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-
 p
-+rompt.ps1" $makePromptPath @()
-+
-+if (-not $shouldRunFullCycle) {
-+    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "Full
+-rompt.ps1" $makePromptPath @()
+-
+-if (-not $shouldRunFullCycle) {
+-    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "Full
  
-+cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the full-cycle wrap
+-cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the full-cycle wrap
 p
-+er; implementation and review still require -AllowCodex and -AllowReviewCodex."
-+    Stop-AutoGoal $script:steps "prepared_without_full_cycle" $true 0
-+}
-+
-+Invoke-CycleCommand 7 "auto-cycle-full" $fullCycleCommandText $autoCycleFullPath $fullCycleArgumen
+-er; implementation and review still require -AllowCodex and -AllowReviewCodex."
+-    Stop-AutoGoal $script:steps "prepared_without_full_cycle" $true 0
+-}
+-
+-Invoke-CycleCommand 7 "auto-cycle-full" $fullCycleCommandText $autoCycleFullPath $fullCycleArgumen
 t
-+s
-+
-+Stop-AutoGoal $script:steps "completed" $true 0
-+
-+
-+ succeeded in 3198ms:
-+param(
-+    [string]$ExtraInstructionsPath = ".ai-dev/extra-instructions.md"
-+)
-+
-+. "$PSScriptRoot\ai-dev-env.ps1"
-+
-+$projectRoot = (Get-Location).Path
-+$goalRelativePath = ".ai-dev/goal.md"
-+$queueRelativePath = ".ai-dev/queue.json"
-+$stateRelativePath = ".ai-dev/state.json"
-+$promptRelativePath = ".ai-dev/current-task-prompt.md"
-+
-+$goalPath = Join-Path $projectRoot $goalRelativePath
-+$queuePath = Join-Path $projectRoot $queueRelativePath
-+$statePath = Join-Path $projectRoot $stateRelativePath
-+$promptPath = Join-Path $projectRoot $promptRelativePath
-+
-+function Stop-WithError {
-+    param(
-+        [string]$Message
-+    )
-+
-+    Write-Error $Message
-+    exit 1
-+}
-+
-+function Read-JsonFile {
-+    param(
-+        [string]$Path,
-+        [string]$RelativePath
-+    )
-+
-+    try {
-+        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
-+    } catch {
-+        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
-+    }
-+}
-+
-+function Get-MissingFields {
-+    param(
-+        [object]$InputObject,
-+        [string[]]$RequiredFields
-+    )
-+
-+    $missingFields = @()
-+
-+    foreach ($field in $RequiredFields) {
-+        if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
-+            $missingFields += $field
-+        }
-+    }
-+
-+    return $missingFields
-+}
-+
-+function Stop-ForMissingFields {
-+    param(
-+        [string]$SourceName,
-+        [string[]]$MissingFields
-+    )
-+
-+    if ($MissingFields.Count -eq 0) {
-+        return
-+    }
-+
-+    Write-Host "$SourceName 누락 필드:"
-+    foreach ($field in $MissingFields) {
-+        Write-Host "  - $field"
-+    }
-+
-+    Stop-WithError "$SourceName 필수 필드가 누락되었습니다."
-+}
-+
-+function Test-HasValue {
-+    param(
-+        [object]$Value
-+    )
-+
-+    if ($null -eq $Value) {
-+        return $false
-+    }
-+
-+    if ($Value -is [string]) {
-+        return -not [string]::IsNullOrWhiteSpace($Value)
-+    }
-+
-+    return $true
-+}
-+
-+function Convert-ToMarkdownList {
-+    param(
-+        [object]$Value
-+    )
-+
-+    if ($null -eq $Value -or @($Value).Count -eq 0) {
-+        return "- 없음"
-+    }
-+
-+    return (@($Value) | ForEach-Object { "- $_" }) -join "`r`n"
-+}
-+
-+foreach ($requiredPath in @($goalRelativePath, $queueRelativePath, $stateRelativePath)) {
-+    $fullPath = Join-Path $projectRoot $requiredPath
-+
-+    if (-not (Test-Path -LiteralPath $fullPath -PathType Leaf)) {
-+        Stop-WithError "필수 파일이 없습니다: $requiredPath"
-+    }
-+}
-+
-+$goalContent = Get-Content -Raw -Encoding UTF8 -LiteralPath $goalPath
-+$queue = Read-JsonFile $queuePath $queueRelativePath
-+$state = Read-JsonFile $statePath $stateRelativePath
-+
-+$queueRequiredFields = @(
-+    "goalTitle",
-+    "currentTaskId",
-+    "tasks"
-+)
-+$stateRequiredFields = @(
-+    "goalStatus",
-+    "currentTaskId"
-+)
-+
-+Stop-ForMissingFields $queueRelativePath (Get-MissingFields $queue $queueRequiredFields)
-+Stop-ForMissingFields $stateRelativePath (Get-MissingFields $state $stateRequiredFields)
-+
-+if ($null -eq $queue.tasks -or -not ($queue.tasks -is [System.Collections.IEnumerable])) {
-+    Stop-WithError "$queueRelativePath tasks 필드는 배열이어야 합니다."
-+}
-+
-+$tasks = @($queue.tasks)
-+$currentTaskId = $null
-+
-+if (Test-HasValue $state.currentTaskId) {
-+    $currentTaskId = [string]$state.currentTaskId
-+} elseif (Test-HasValue $queue.currentTaskId) {
-+    $currentTaskId = [string]$queue.currentTaskId
-+} else {
-+    $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -First 1
-+
-+    if ($null -ne $pendingTask) {
-+        $currentTaskId = [string]$pendingTask.id
-+    }
-+}
-+
-+if (-not (Test-HasValue $currentTaskId)) {
-+    Stop-WithError "수행할 task가 없습니다."
-+}
-+
-+$currentTask = $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
-+
-+if ($null -eq $currentTask) {
-+    Stop-WithError "currentTaskId에 해당하는 task를 찾을 수 없습니다: $currentTaskId"
-+}
-+
-+$taskRequiredFields = @(
-+    "id",
-+    "title",
-+    "description",
-+    "type",
-+    "status",
-+    "priority",
-+    "dependsOn",
-+    "filesLikelyToChange",
-+    "verification"
-+)
-+
-+Stop-ForMissingFields "task $currentTaskId" (Get-MissingFields $currentTask $taskRequiredFields)
-+
-+$dependsOnText = Convert-ToMarkdownList $currentTask.dependsOn
-+$likelyFilesText = Convert-ToMarkdownList $currentTask.filesLikelyToChange
-+$verificationText = Convert-ToMarkdownList $currentTask.verification
-+$extraInstructionsContent = $null
-+
-+if (-not [string]::IsNullOrWhiteSpace($ExtraInstructionsPath)) {
-+    $resolvedExtraInstructionsPath = if ([System.IO.Path]::IsPathRooted($ExtraInstructionsPath)) {
-+        $ExtraInstructionsPath
-+    } else {
-+        Join-Path $projectRoot $ExtraInstructionsPath
-+    }
+-s
+-
+-Stop-AutoGoal $script:steps "completed" $true 0
+-
+-
+- succeeded in 3198ms:
+-param(
+-    [string]$ExtraInstructionsPath = ".ai-dev/extra-instructions.md"
+-)
+-
+-. "$PSScriptRoot\ai-dev-env.ps1"
+-
+-$projectRoot = (Get-Location).Path
+-$goalRelativePath = ".ai-dev/goal.md"
+-$queueRelativePath = ".ai-dev/queue.json"
+-$stateRelativePath = ".ai-dev/state.json"
+-$promptRelativePath = ".ai-dev/current-task-prompt.md"
+-
+-$goalPath = Join-Path $projectRoot $goalRelativePath
+-$queuePath = Join-Path $projectRoot $queueRelativePath
+-$statePath = Join-Path $projectRoot $stateRelativePath
+-$promptPath = Join-Path $projectRoot $promptRelativePath
+-
+-function Stop-WithError {
+-    param(
+-        [string]$Message
+-    )
+-
+-    Write-Error $Message
+-    exit 1
+-}
+-
+-function Read-JsonFile {
+-    param(
+-        [string]$Path,
+-        [string]$RelativePath
+-    )
+-
+-    try {
+-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
+-    } catch {
+-        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
+-    }
+-}
+-
+-function Get-MissingFields {
+-    param(
+-        [object]$InputObject,
+-        [string[]]$RequiredFields
+-    )
+-
+-    $missingFields = @()
+-
+-    foreach ($field in $RequiredFields) {
+-        if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
+-            $missingFields += $field
+-        }
+-    }
+-
+-    return $missingFields
+-}
+-
+-function Stop-ForMissingFields {
+-    param(
+-        [string]$SourceName,
+-        [string[]]$MissingFields
+-    )
+-
+-    if ($MissingFields.Count -eq 0) {
+-        return
+-    }
+-
+-    Write-Host "$SourceName 누락 필드:"
+-    foreach ($field in $MissingFields) {
+-        Write-Host "  - $field"
+-    }
+-
+-    Stop-WithError "$SourceName 필수 필드가 누락되었습니다."
+-}
+-
+-function Test-HasValue {
+-    param(
+-        [object]$Value
+-    )
+-
+-    if ($null -eq $Value) {
+-        return $false
+-    }
+-
+-    if ($Value -is [string]) {
+-        return -not [string]::IsNullOrWhiteSpace($Value)
+-    }
+-
+-    return $true
+-}
+-
+-function Convert-ToMarkdownList {
+-    param(
+-        [object]$Value
+-    )
+-
+-    if ($null -eq $Value -or @($Value).Count -eq 0) {
+-        return "- 없음"
+-    }
+-
+-    return (@($Value) | ForEach-Object { "- $_" }) -join "`r`n"
+-}
+-
+-foreach ($requiredPath in @($goalRelativePath, $queueRelativePath, $stateRelativePath)) {
+-    $fullPath = Join-Path $projectRoot $requiredPath
+-
+-    if (-not (Test-Path -LiteralPath $fullPath -PathType Leaf)) {
+-        Stop-WithError "필수 파일이 없습니다: $requiredPath"
+-    }
+-}
+-
+-$goalContent = Get-Content -Raw -Encoding UTF8 -LiteralPath $goalPath
+-$queue = Read-JsonFile $queuePath $queueRelativePath
+-$state = Read-JsonFile $statePath $stateRelativePath
+-
+-$queueRequiredFields = @(
+-    "goalTitle",
+-    "currentTaskId",
+-    "tasks"
+-)
+-$stateRequiredFields = @(
+-    "goalStatus",
+-    "currentTaskId"
+-)
+-
+-Stop-ForMissingFields $queueRelativePath (Get-MissingFields $queue $queueRequiredFields)
+-Stop-ForMissingFields $stateRelativePath (Get-MissingFields $state $stateRequiredFields)
+-
+-if ($null -eq $queue.tasks -or -not ($queue.tasks -is [System.Collections.IEnumerable])) {
+-    Stop-WithError "$queueRelativePath tasks 필드는 배열이어야 합니다."
+-}
+-
+-$tasks = @($queue.tasks)
+-$currentTaskId = $null
+-
+-if (Test-HasValue $state.currentTaskId) {
+-    $currentTaskId = [string]$state.currentTaskId
+-} elseif (Test-HasValue $queue.currentTaskId) {
+-    $currentTaskId = [string]$queue.currentTaskId
+-} else {
+-    $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -First 1
+-
+-    if ($null -ne $pendingTask) {
+-        $currentTaskId = [string]$pendingTask.id
+-    }
+-}
+-
+-if (-not (Test-HasValue $currentTaskId)) {
+-    Stop-WithError "수행할 task가 없습니다."
+-}
+-
+-$currentTask = $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
+-
+-if ($null -eq $currentTask) {
+-    Stop-WithError "currentTaskId에 해당하는 task를 찾을 수 없습니다: $currentTaskId"
+-}
+-
+-$taskRequiredFields = @(
+-    "id",
+-    "title",
+-    "description",
+-    "type",
+-    "status",
+-    "priority",
+-    "dependsOn",
+-    "filesLikelyToChange",
+-    "verification"
+-)
+-
+-Stop-ForMissingFields "task $currentTaskId" (Get-MissingFields $currentTask $taskRequiredFields)
+-
+-$dependsOnText = Convert-ToMarkdownList $currentTask.dependsOn
+-$likelyFilesText = Convert-ToMarkdownList $currentTask.filesLikelyToChange
+-$verificationText = Convert-ToMarkdownList $currentTask.verification
+-$extraInstructionsContent = $null
+-
+-if (-not [string]::IsNullOrWhiteSpace($ExtraInstructionsPath)) {
+-    $resolvedExtraInstructionsPath = if ([System.IO.Path]::IsPathRooted($ExtraInstructionsPath)) {
+-        $ExtraInstructionsPath
+-    } else {
+-        Join-Path $projectRoot $ExtraInstructionsPath
+-    }
+-
+-    if (Test-Path -LiteralPath $resolvedExtraInstructionsPath -PathType Leaf) {
+-        $extraInstructionsContent = Get-Content -Raw -Encoding UTF8 -LiteralPath $resolvedExtraIns
+t
+-ructionsPath
+-    }
+-}
+-
+-$promptContent = @"
+-# Current Task Prompt
+-
+-## Role
+-
+-너는 이 저장소의 자동 개발 에이전트다.
+-
+-## Goal
+-
+-$goalContent
++## 수동 검증
++- 업무 화면의 검색창에 더 명확한 placeholder 문구가 표시되는지 확인한다.
++- 검색어 입력과 기존 필터링 동작이 그대로 유지되는지 확인한다.
  
--codex
--커밋 결과 확인 게이트를 추가했으므로 README 단계 번호도 맞춰 갱신하겠습니다.
--apply patch
--patch: completed
--D:\ai-apps\planpilot-local\.ai-dev\README.md
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..1bdb85799839984c1deb03a6eba84a23cfc01093
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..84c3a6c8713a84a4304f6cc025a4949946302bb5
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
-- 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
--+    }
--+
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,97 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
--
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..1bdb85799839984c1deb03a6eba84a23cfc01093
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..84c3a6c8713a84a4304f6cc025a4949946302bb5
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
+ ## Current Task
+ 
+-- Task ID: $($currentTask.id)
+-- Title: $($currentTask.title)
+-- Description: $($currentTask.description)
+-- Type: $($currentTask.type)
+-- Status: $($currentTask.status)
+-- Priority: $($currentTask.priority)
++- Task ID: T001
++- Title: 업무 검색창 placeholder 문구 수정
++- Description: 업무 검색창 입력 요소를 찾아 placeholder 문구만 더 명확한 한국어 표현으로 변경한다.
++- Type: implementation
++- Status: in_progress
++- Priority: P1
+ - Depends on:
+-$dependsOnText
++- 없음
+ 
+ ## Task Scope
+ 
+@@ -2848,13 +91,14 @@ $dependsOnText
+ 
+ ## Likely Files
+ 
+-$likelyFilesText
++- src/App.tsx
+ 
+ ## Verification
+ 
+-$verificationText
++- 업무 검색창 placeholder 문구가 변경되었는지 확인한다.
++- 검색 입력 동작과 기존 필터링 동작이 유지되는지 확인한다.
+ 
+-- 필요한 경우 ``npm run build``는 사람이 별도로 실행한다.
++- 필요한 경우 `npm run build`는 사람이 별도로 실행한다.
+ - 이 프롬프트는 자동으로 build, test, lint를 실행하라고 지시하지 않는다.
+ 
+ ## Required Output
+@@ -2864,12 +108,12 @@ $verificationText
+ - 검증 방법
+ - 남은 위험
+ - 다음 task로 넘어가도 되는지 여부
+-- ``.ai-dev/loop-log.md``에 기록할 작업 요약
++- `.ai-dev/loop-log.md`에 기록할 작업 요약
+ 
+ ## Hard Rules
+ 
+-- ``src`` 외 파일을 수정해야 하는 경우 이유를 기록한다.
+-- ``package.json``과 ``package-lock.json``은 수정하지 않는다. 꼭 필요하면 중단하고 이유만 기록한다.
++- `src` 외 파일을 수정해야 하는 경우 이유를 기록한다.
++- `package.json`과 `package-lock.json`은 수정하지 않는다. 꼭 필요하면 중단하고 이유만 기록한다.
+ - 실제 DB 삭제 또는 초기화를 하지 않는다.
+ - 사용자 데이터 복원 또는 덮어쓰기를 하지 않는다.
+ - 현재 task와 무관한 UI 전면 개편을 하지 않는다.
+@@ -2885,2673 +129,548 @@ $verificationText
+ - 패키지 추가가 필요한 경우
+ - 데이터 삭제 또는 마이그레이션이 필요한 경우
+ - 같은 오류가 반복되는 경우
+-"@
+-
+-if (-not [string]::IsNullOrWhiteSpace($extraInstructionsContent)) {
+-    $promptContent += @"
+-
+-
+-## Extra Instructions
+-
+-$extraInstructionsContent
+-"@
+-}
+-
+-$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
+-[System.IO.File]::WriteAllText($promptPath, $promptContent, $utf8WithBom)
+-
+-Write-Host "생성된 프롬프트 파일: $promptRelativePath"
+-Write-Host "Current task id: $($currentTask.id)"
+-Write-Host "Current task title: $($currentTask.title)"
+ 
+ codex
+-현재 스크립트는 이미 주요 흐름을 갖고 있지만, DryRun 미리보기의 한국어 문자열이 깨져 있고 생성 JSON 검증이 프롬프트 규칙 일부를 충분히 강제하지 않습니다. 파일 작
+성
+- 전에 이 두 부분을 좁게 보강하겠습니다.
++요구사항은 검색창의 `placeholder` 문구만 바꾸는 좁은 변경입니다. 이제 `src/App.tsx`에서 업무 검색 입력 요소만 찾아 문구를 확인하겠습니다.
+ exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralP
+a
+-th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern 'New-DryRunAutoGoalPlan'
+,
+-'Get-QueueValidationErrors','currentTaskId must be' -Context 0,80" in D:\ai-apps\planpilot-local
++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Path 'D:
+\
++\ai-apps\\planpilot-local\\src\\App.tsx' -Pattern 'placeholder|검색'" in D:\ai-apps\planpilot-local
++ succeeded in 1197ms:
++codex
++직접 검색 결과가 없어서, 해당 파일 안의 입력 요소 위치를 더 좁혀 확인하겠습니다. 변경은 발견한 placeholder 한 줄에만 적용하겠습니다.
+ exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralP
+a
+-th 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md' -Pattern '목표 입력 기반 자동 실행' -Context 0,120" in
+ 
+-D:\ai-apps\planpilot-local
+- succeeded in 2293ms:
+-
+-> scripts\ai-dev-auto-goal.ps1:262:function Get-QueueValidationErrors {
+-  scripts\ai-dev-auto-goal.ps1:263:    param([object]$Queue)
+-  scripts\ai-dev-auto-goal.ps1:264:
+-  scripts\ai-dev-auto-goal.ps1:265:    $errors = @()
+-  scripts\ai-dev-auto-goal.ps1:266:    $requiredFields = @("goalTitle", "goalSource", "createdAt",
+ 
+-"updatedAt", "curren
+-tTaskId", "tasks")
+-  scripts\ai-dev-auto-goal.ps1:267:
+-  scripts\ai-dev-auto-goal.ps1:268:    foreach ($field in @(Get-MissingFields $Queue $requiredFiel
+d
+-s)) {
+-  scripts\ai-dev-auto-goal.ps1:269:        $errors += "queue missing field: $field"
+-  scripts\ai-dev-auto-goal.ps1:270:    }
+-  scripts\ai-dev-auto-goal.ps1:271:
+-  scripts\ai-dev-auto-goal.ps1:272:    if ($errors.Count -gt 0) {
+-  scripts\ai-dev-auto-goal.ps1:273:        return $errors
+-  scripts\ai-dev-auto-goal.ps1:274:    }
+-  scripts\ai-dev-auto-goal.ps1:275:
+-  scripts\ai-dev-auto-goal.ps1:276:    if (-not (Test-HasValue $Queue.goalTitle)) {
+-  scripts\ai-dev-auto-goal.ps1:277:        $errors += "queue.goalTitle must not be empty."
+-  scripts\ai-dev-auto-goal.ps1:278:    }
+-  scripts\ai-dev-auto-goal.ps1:279:
+-  scripts\ai-dev-auto-goal.ps1:280:    if ($Queue.goalSource -ne $goalRelativePath) {
+-  scripts\ai-dev-auto-goal.ps1:281:        $errors += "queue.goalSource must be $goalRelativePath.
+"
+-  scripts\ai-dev-auto-goal.ps1:282:    }
+-  scripts\ai-dev-auto-goal.ps1:283:
+-  scripts\ai-dev-auto-goal.ps1:284:    if ($Queue.tasks -isnot [System.Collections.IEnumerable] -o
+r
+- $Queue.tasks -is [s
+-tring]) {
+-  scripts\ai-dev-auto-goal.ps1:285:        $errors += "queue.tasks must be an array."
+-  scripts\ai-dev-auto-goal.ps1:286:        return $errors
+-  scripts\ai-dev-auto-goal.ps1:287:    }
+-  scripts\ai-dev-auto-goal.ps1:288:
+-  scripts\ai-dev-auto-goal.ps1:289:    $tasks = @($Queue.tasks)
+-  scripts\ai-dev-auto-goal.ps1:290:
+-  scripts\ai-dev-auto-goal.ps1:291:    if ($tasks.Count -lt 1) {
+-  scripts\ai-dev-auto-goal.ps1:292:        $errors += "queue.tasks must contain at least one task.
+"
+-  scripts\ai-dev-auto-goal.ps1:293:        return $errors
+-  scripts\ai-dev-auto-goal.ps1:294:    }
+-  scripts\ai-dev-auto-goal.ps1:295:
+-  scripts\ai-dev-auto-goal.ps1:296:    if ($tasks.Count -gt 3) {
+-  scripts\ai-dev-auto-goal.ps1:297:        $errors += "queue.tasks must contain no more than three
+ 
+-tasks."
+-  scripts\ai-dev-auto-goal.ps1:298:    }
+-  scripts\ai-dev-auto-goal.ps1:299:
+-  scripts\ai-dev-auto-goal.ps1:300:    $taskIds = @()
+-  scripts\ai-dev-auto-goal.ps1:301:
+-  scripts\ai-dev-auto-goal.ps1:302:    foreach ($task in $tasks) {
+-  scripts\ai-dev-auto-goal.ps1:303:        $taskRequiredFields = @("id", "title", "description", "
+t
+-ype", "status", "pri
+-ority", "dependsOn", "filesLikelyToChange", "verification", "commitMessage")
+-  scripts\ai-dev-auto-goal.ps1:304:        $taskMissingFields = @(Get-MissingFields $task $taskReq
+u
+-iredFields)
+-  scripts\ai-dev-auto-goal.ps1:305:
+-  scripts\ai-dev-auto-goal.ps1:306:        foreach ($field in $taskMissingFields) {
+-  scripts\ai-dev-auto-goal.ps1:307:            $errors += "task missing field: $field"
+-  scripts\ai-dev-auto-goal.ps1:308:        }
+-  scripts\ai-dev-auto-goal.ps1:309:
+-  scripts\ai-dev-auto-goal.ps1:310:        if ($taskMissingFields.Count -gt 0) {
+-  scripts\ai-dev-auto-goal.ps1:311:            continue
+-  scripts\ai-dev-auto-goal.ps1:312:        }
+-  scripts\ai-dev-auto-goal.ps1:313:
+-  scripts\ai-dev-auto-goal.ps1:314:        if (-not (Test-HasValue $task.id)) {
+-  scripts\ai-dev-auto-goal.ps1:315:            $errors += "task.id must not be empty."
+-  scripts\ai-dev-auto-goal.ps1:316:        } else {
+-  scripts\ai-dev-auto-goal.ps1:317:            $taskIds += [string]$task.id
+-  scripts\ai-dev-auto-goal.ps1:318:        }
+-  scripts\ai-dev-auto-goal.ps1:319:
+-  scripts\ai-dev-auto-goal.ps1:320:        if (-not (Test-HasValue $task.title)) {
+-  scripts\ai-dev-auto-goal.ps1:321:            $errors += "task.title must not be empty."
+-  scripts\ai-dev-auto-goal.ps1:322:        }
+-  scripts\ai-dev-auto-goal.ps1:323:
+-  scripts\ai-dev-auto-goal.ps1:324:        if (-not (Test-HasValue $task.description)) {
+-  scripts\ai-dev-auto-goal.ps1:325:            $errors += "task.description must not be empty."
+-  scripts\ai-dev-auto-goal.ps1:326:        }
+-  scripts\ai-dev-auto-goal.ps1:327:
+-  scripts\ai-dev-auto-goal.ps1:328:        if (@("analysis", "implementation", "documentation", "v
+e
+-rification") -notcon
+-tains $task.type) {
+-  scripts\ai-dev-auto-goal.ps1:329:            $errors += "invalid task.type: $($task.type)"
+-  scripts\ai-dev-auto-goal.ps1:330:        }
+-  scripts\ai-dev-auto-goal.ps1:331:
+-  scripts\ai-dev-auto-goal.ps1:332:        if (@("pending", "in_progress", "done", "blocked") -not
+c
+-ontains $task.status
+-) {
+-  scripts\ai-dev-auto-goal.ps1:333:            $errors += "invalid task.status: $($task.status)"
+-  scripts\ai-dev-auto-goal.ps1:334:        }
+-  scripts\ai-dev-auto-goal.ps1:335:
+-  scripts\ai-dev-auto-goal.ps1:336:        if (@("P0", "P1", "P2") -notcontains $task.priority) {
+-  scripts\ai-dev-auto-goal.ps1:337:            $errors += "invalid task.priority: $($task.priority
+)
+-"
+-  scripts\ai-dev-auto-goal.ps1:338:        }
+-  scripts\ai-dev-auto-goal.ps1:339:
+-  scripts\ai-dev-auto-goal.ps1:340:        if ($task.dependsOn -isnot [System.Collections.IEnumera
+b
+-le] -or $task.depend
+-sOn -is [string]) {
+-  scripts\ai-dev-auto-goal.ps1:341:            $errors += "task.dependsOn must be an array: $($tas
+k
+-.id)"
+-  scripts\ai-dev-auto-goal.ps1:342:        }
+-> scripts\ai-dev-auto-goal.ps1:419:    $errors += @(Get-QueueValidationErrors $Plan.queue)
+-  scripts\ai-dev-auto-goal.ps1:420:    $errors += @(Get-StateValidationErrors $Plan.state $Plan.qu
+e
+-ue)
+-  scripts\ai-dev-auto-goal.ps1:421:
+-  scripts\ai-dev-auto-goal.ps1:422:    return $errors
+-  scripts\ai-dev-auto-goal.ps1:423:}
+-  scripts\ai-dev-auto-goal.ps1:424:
+-> scripts\ai-dev-auto-goal.ps1:425:function New-DryRunAutoGoalPlan {
+-  scripts\ai-dev-auto-goal.ps1:426:    param(
+-  scripts\ai-dev-auto-goal.ps1:427:        [string]$Title,
+-  scripts\ai-dev-auto-goal.ps1:428:        [string]$Description
+-  scripts\ai-dev-auto-goal.ps1:429:    )
+-  scripts\ai-dev-auto-goal.ps1:430:
+-  scripts\ai-dev-auto-goal.ps1:431:    $now = (Get-Date).ToUniversalTime().ToString("o")
+-  scripts\ai-dev-auto-goal.ps1:432:    $safeCommitTitle = $Title.Trim()
+-  scripts\ai-dev-auto-goal.ps1:433:
+-  scripts\ai-dev-auto-goal.ps1:434:    $plan = [PSCustomObject][ordered]@{
+-  scripts\ai-dev-auto-goal.ps1:435:        goalMarkdown = @"
+-  scripts\ai-dev-auto-goal.ps1:436:# 紐⑺몴
+-  scripts\ai-dev-auto-goal.ps1:437:
+-  scripts\ai-dev-auto-goal.ps1:438:$safeCommitTitle
+-  scripts\ai-dev-auto-goal.ps1:439:
+-  scripts\ai-dev-auto-goal.ps1:440:## 諛곌꼍
+-  scripts\ai-dev-auto-goal.ps1:441:
+-  scripts\ai-dev-auto-goal.ps1:442:$($Description.Trim())
+-  scripts\ai-dev-auto-goal.ps1:443:
+-  scripts\ai-dev-auto-goal.ps1:444:## ?깃났 湲곗?
+-  scripts\ai-dev-auto-goal.ps1:445:
+-  scripts\ai-dev-auto-goal.ps1:446:- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+-  scripts\ai-dev-auto-goal.ps1:447:- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+-  scripts\ai-dev-auto-goal.ps1:448:- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-  scripts\ai-dev-auto-goal.ps1:449:
+-  scripts\ai-dev-auto-goal.ps1:450:## ?쒖빟?ы빆
+-  scripts\ai-dev-auto-goal.ps1:451:
+-  scripts\ai-dev-auto-goal.ps1:452:- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜
+?
+-섏? ?딅뒗??
+-  scripts\ai-dev-auto-goal.ps1:453:- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-  scripts\ai-dev-auto-goal.ps1:454:
+-  scripts\ai-dev-auto-goal.ps1:455:## ?쒖쇅 踰붿쐞
+-  scripts\ai-dev-auto-goal.ps1:456:
+-  scripts\ai-dev-auto-goal.ps1:457:- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-  scripts\ai-dev-auto-goal.ps1:458:
+-  scripts\ai-dev-auto-goal.ps1:459:## ?섎룞 ?뺤씤
+-  scripts\ai-dev-auto-goal.ps1:460:
+-  scripts\ai-dev-auto-goal.ps1:461:- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+-  scripts\ai-dev-auto-goal.ps1:462:- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+-  scripts\ai-dev-auto-goal.ps1:463:- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-  scripts\ai-dev-auto-goal.ps1:464:"@
+-  scripts\ai-dev-auto-goal.ps1:465:        queue = [PSCustomObject][ordered]@{
+-  scripts\ai-dev-auto-goal.ps1:466:            goalTitle = $safeCommitTitle
+-  scripts\ai-dev-auto-goal.ps1:467:            goalSource = $goalRelativePath
+-  scripts\ai-dev-auto-goal.ps1:468:            createdAt = $now
+-  scripts\ai-dev-auto-goal.ps1:469:            updatedAt = $now
+-  scripts\ai-dev-auto-goal.ps1:470:            currentTaskId = "T001"
+-  scripts\ai-dev-auto-goal.ps1:471:            tasks = @(
+-  scripts\ai-dev-auto-goal.ps1:472:                [PSCustomObject][ordered]@{
+-  scripts\ai-dev-auto-goal.ps1:473:                    id = "T001"
+-  scripts\ai-dev-auto-goal.ps1:474:                    title = $safeCommitTitle
+-  scripts\ai-dev-auto-goal.ps1:475:                    description = $Description.Trim()
+-  scripts\ai-dev-auto-goal.ps1:476:                    type = "implementation"
+-  scripts\ai-dev-auto-goal.ps1:477:                    status = "in_progress"
+-  scripts\ai-dev-auto-goal.ps1:478:                    priority = "P0"
+-  scripts\ai-dev-auto-goal.ps1:479:                    dependsOn = @()
+-  scripts\ai-dev-auto-goal.ps1:480:                    filesLikelyToChange = @()
+-  scripts\ai-dev-auto-goal.ps1:481:                    verification = @(
+-  scripts\ai-dev-auto-goal.ps1:482:                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+-  scripts\ai-dev-auto-goal.ps1:483:                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-  scripts\ai-dev-auto-goal.ps1:484:                    )
+-  scripts\ai-dev-auto-goal.ps1:485:                    commitMessage = $null
+-  scripts\ai-dev-auto-goal.ps1:486:                }
+-  scripts\ai-dev-auto-goal.ps1:487:            )
+-  scripts\ai-dev-auto-goal.ps1:488:        }
+-  scripts\ai-dev-auto-goal.ps1:489:        state = [PSCustomObject][ordered]@{
+-  scripts\ai-dev-auto-goal.ps1:490:            goalStatus = "in_progress"
+-  scripts\ai-dev-auto-goal.ps1:491:            currentTaskId = "T001"
+-  scripts\ai-dev-auto-goal.ps1:492:            currentLoop = 0
+-  scripts\ai-dev-auto-goal.ps1:493:            maxLoopsPerTask = 2
+-  scripts\ai-dev-auto-goal.ps1:494:            repeatedFailureCount = 0
+-  scripts\ai-dev-auto-goal.ps1:495:            lastCommand = $null
+-  scripts\ai-dev-auto-goal.ps1:496:            lastCommandStatus = "not_started"
+-  scripts\ai-dev-auto-goal.ps1:497:            lastErrorSummary = $null
+-  scripts\ai-dev-auto-goal.ps1:498:            lastReviewDecision = "not_started"
+-  scripts\ai-dev-auto-goal.ps1:499:            lastReviewSeverity = $null
+-  scripts\ai-dev-auto-goal.ps1:500:            lastCommitHash = $null
+-  scripts\ai-dev-auto-goal.ps1:501:            startedAt = $now
+-  scripts\ai-dev-auto-goal.ps1:502:            updatedAt = $now
+-  scripts\ai-dev-auto-goal.ps1:503:            stopReason = $null
+-  scripts\ai-dev-auto-goal.ps1:504:        }
+-  scripts\ai-dev-auto-goal.ps1:505:    }
+-> scripts\ai-dev-auto-goal.ps1:596:- currentTaskId must be "T001".
+-  scripts\ai-dev-auto-goal.ps1:597:- tasks must contain one to three tasks.
+-  scripts\ai-dev-auto-goal.ps1:598:- T001 must be status "in_progress"; later tasks, if any, must 
+b
+-e "pending".
+-  scripts\ai-dev-auto-goal.ps1:599:- each task must include id, title, description, type, status, 
+p
+-riority, dependsOn, 
+-filesLikelyToChange, verification, commitMessage.
+-  scripts\ai-dev-auto-goal.ps1:600:- type must be one of analysis, implementation, documentation, 
+v
+-erification.
+-  scripts\ai-dev-auto-goal.ps1:601:- priority must be P0, P1, or P2.
+-  scripts\ai-dev-auto-goal.ps1:602:- dependsOn, filesLikelyToChange, and verification must be arra
+y
+-s.
+-  scripts\ai-dev-auto-goal.ps1:603:- commitMessage must be null or a short English commit message.
+-  scripts\ai-dev-auto-goal.ps1:604:
+-  scripts\ai-dev-auto-goal.ps1:605:state rules:
+-  scripts\ai-dev-auto-goal.ps1:606:- goalStatus must be "in_progress".
+-> scripts\ai-dev-auto-goal.ps1:607:- currentTaskId must be "T001".
+-  scripts\ai-dev-auto-goal.ps1:608:- currentLoop must be 0.
+-  scripts\ai-dev-auto-goal.ps1:609:- maxLoopsPerTask must be 2.
+-  scripts\ai-dev-auto-goal.ps1:610:- repeatedFailureCount must be 0.
+-  scripts\ai-dev-auto-goal.ps1:611:- lastCommand must be null.
+-  scripts\ai-dev-auto-goal.ps1:612:- lastCommandStatus must be "not_started".
+-  scripts\ai-dev-auto-goal.ps1:613:- lastErrorSummary must be null.
+-  scripts\ai-dev-auto-goal.ps1:614:- lastReviewDecision must be "not_started".
+-  scripts\ai-dev-auto-goal.ps1:615:- lastReviewSeverity must be null.
+-  scripts\ai-dev-auto-goal.ps1:616:- lastCommitHash must be null.
+-  scripts\ai-dev-auto-goal.ps1:617:- startedAt and updatedAt must be ISO 8601 strings.
+-  scripts\ai-dev-auto-goal.ps1:618:- stopReason must be null.
+-  scripts\ai-dev-auto-goal.ps1:619:
+-  scripts\ai-dev-auto-goal.ps1:620:Safety rules:
+-  scripts\ai-dev-auto-goal.ps1:621:- Do not mention server APIs, login, cloud sync, npm install, g
+i
+-t reset, git clean, 
+-git push, DB deletion, or broad rewrites as implementation steps.
+-  scripts\ai-dev-auto-goal.ps1:622:- Prefer one small implementation task when the goal is small.
+-  scripts\ai-dev-auto-goal.ps1:623:- Use Korean for user-facing task titles, descriptions, verific
+a
+-tion, and goalMarkdo
+-wn.
+-  scripts\ai-dev-auto-goal.ps1:624:"@
+-  scripts\ai-dev-auto-goal.ps1:625:}
+-  scripts\ai-dev-auto-goal.ps1:626:
+-  scripts\ai-dev-auto-goal.ps1:627:function Invoke-CycleCommand {
+-  scripts\ai-dev-auto-goal.ps1:628:    param(
+-  scripts\ai-dev-auto-goal.ps1:629:        [int]$StepNumber,
+-  scripts\ai-dev-auto-goal.ps1:630:        [string]$Name,
+-  scripts\ai-dev-auto-goal.ps1:631:        [string]$Command,
+-  scripts\ai-dev-auto-goal.ps1:632:        [string]$ScriptPath,
+-  scripts\ai-dev-auto-goal.ps1:633:        [string[]]$Arguments
+-  scripts\ai-dev-auto-goal.ps1:634:    )
+-  scripts\ai-dev-auto-goal.ps1:635:
+-  scripts\ai-dev-auto-goal.ps1:636:    if ($DryRun) {
+-  scripts\ai-dev-auto-goal.ps1:637:        $script:steps += New-StepResult $StepNumber $Name $Comm
+a
+-nd $false $true 0 "D
+-ryRun: child script was not executed."
+-  scripts\ai-dev-auto-goal.ps1:638:        return
+-  scripts\ai-dev-auto-goal.ps1:639:    }
+-  scripts\ai-dev-auto-goal.ps1:640:
+-  scripts\ai-dev-auto-goal.ps1:641:    $output = & powershell -ExecutionPolicy Bypass -File $Scrip
+t
+-Path @Arguments 2>&1
+- | Out-String
+-  scripts\ai-dev-auto-goal.ps1:642:    $exitCode = $LASTEXITCODE
+-  scripts\ai-dev-auto-goal.ps1:643:    $message = $output.Trim()
+-  scripts\ai-dev-auto-goal.ps1:644:
+-  scripts\ai-dev-auto-goal.ps1:645:    if ([string]::IsNullOrWhiteSpace($message)) {
+-  scripts\ai-dev-auto-goal.ps1:646:        $message = "completed"
+-  scripts\ai-dev-auto-goal.ps1:647:    }
+-  scripts\ai-dev-auto-goal.ps1:648:
+-  scripts\ai-dev-auto-goal.ps1:649:    $script:steps += New-StepResult $StepNumber $Name $Command 
+$
+-true $false $exitCod
+-e $message
+-  scripts\ai-dev-auto-goal.ps1:650:
+-  scripts\ai-dev-auto-goal.ps1:651:    if ($exitCode -ne 0) {
+-  scripts\ai-dev-auto-goal.ps1:652:        Stop-AutoGoal $script:steps "$Name`_failed" $false 1
+-  scripts\ai-dev-auto-goal.ps1:653:    }
+-  scripts\ai-dev-auto-goal.ps1:654:}
+-  scripts\ai-dev-auto-goal.ps1:655:
+-  scripts\ai-dev-auto-goal.ps1:656:function Get-FullCycleArguments {
+-  scripts\ai-dev-auto-goal.ps1:657:    $arguments = @("-MaxTasks", ([string]$MaxTasks), "-MaxSteps
+"
+-, ([string]$MaxSteps
+-))
+-  scripts\ai-dev-auto-goal.ps1:658:
+-  scripts\ai-dev-auto-goal.ps1:659:    if ($AllowCodex) {
+-  scripts\ai-dev-auto-goal.ps1:660:        $arguments += "-AllowCodex"
+-  scripts\ai-dev-auto-goal.ps1:661:    }
+-  scripts\ai-dev-auto-goal.ps1:662:
+-  scripts\ai-dev-auto-goal.ps1:663:    if ($AllowReviewCodex) {
+-  scripts\ai-dev-auto-goal.ps1:664:        $arguments += "-AllowReviewCodex"
+-  scripts\ai-dev-auto-goal.ps1:665:    }
+-  scripts\ai-dev-auto-goal.ps1:666:
+-  scripts\ai-dev-auto-goal.ps1:667:    if ($AllowCommit) {
+-  scripts\ai-dev-auto-goal.ps1:668:        $arguments += "-AllowCommit"
+-  scripts\ai-dev-auto-goal.ps1:669:    }
+-  scripts\ai-dev-auto-goal.ps1:670:
+-  scripts\ai-dev-auto-goal.ps1:671:    # auto-goal writes goal/queue/state/current prompt/result f
+i
+-les after the initia
+-l dirty gate.
+-  scripts\ai-dev-auto-goal.ps1:672:    # The downstream full cycle must tolerate those intended ar
+t
+-ifacts.
+-  scripts\ai-dev-auto-goal.ps1:673:    $arguments += "-AllowDirty"
+-  scripts\ai-dev-auto-goal.ps1:674:
+-  scripts\ai-dev-auto-goal.ps1:675:    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
+-  scripts\ai-dev-auto-goal.ps1:676:        $normalizedFiles = @($CommitFiles | ForEach-Object { $_
+ 
+--split "," } | Where
+--Object { -not [string]::IsNullOrWhiteSpace($_) })
+-  scripts\ai-dev-auto-goal.ps1:677:
+-  scripts\ai-dev-auto-goal.ps1:678:        if ($normalizedFiles.Count -gt 0) {
+-  scripts\ai-dev-auto-goal.ps1:679:            $arguments += "-CommitFiles"
+-  scripts\ai-dev-auto-goal.ps1:680:            $arguments += ($normalizedFiles -join ",")
+-  scripts\ai-dev-auto-goal.ps1:681:        }
+-  scripts\ai-dev-auto-goal.ps1:682:    }
+-  scripts\ai-dev-auto-goal.ps1:683:
+-  scripts\ai-dev-auto-goal.ps1:684:    return $arguments
+-  scripts\ai-dev-auto-goal.ps1:685:}
+-  scripts\ai-dev-auto-goal.ps1:686:
+-  scripts\ai-dev-auto-goal.ps1:687:Set-Location $repoRoot
+-> scripts\ai-dev-auto-goal.ps1:733:    $script:autoGoalPlanPreview = New-DryRunAutoGoalPlan $GoalT
+i
+-tle.Trim() $GoalDesc
+-ription.Trim()
+-  scripts\ai-dev-auto-goal.ps1:734:    $previewValidationErrors = @(Get-AutoGoalValidationErrors $
+s
+-cript:autoGoalPlanPr
+-eview)
+-  scripts\ai-dev-auto-goal.ps1:735:
+-  scripts\ai-dev-auto-goal.ps1:736:    if ($previewValidationErrors.Count -gt 0) {
+-  scripts\ai-dev-auto-goal.ps1:737:        $script:steps += New-StepResult 2 "preview-plan" "local
+ 
+-dry-run plan preview
+-" $false $false 1 "DryRun preview plan validation failed: $($previewValidationErrors -join '; ')"
+-  scripts\ai-dev-auto-goal.ps1:738:        Stop-AutoGoal $script:steps "dry_run_preview_invalid" $
+f
+-alse 1
+-  scripts\ai-dev-auto-goal.ps1:739:    }
+-  scripts\ai-dev-auto-goal.ps1:740:
+-  scripts\ai-dev-auto-goal.ps1:741:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
+t
+- status --short" $fa
+-lse $true 0 "DryRun: dirty worktree gate was not executed."
+-  scripts\ai-dev-auto-goal.ps1:742:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <
+a
+-uto-goal planning pr
+-ompt>" $false $true 0 "DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, 
+t
+-ask: $GoalTitle"
+-  scripts\ai-dev-auto-goal.ps1:743:    $script:steps += New-StepResult 4 "validate-generated-json"
+ 
+-"goal/queue/state JS
+-ON validation" $false $true 0 "DryRun: preview goal/queue/state plan passed local schema validatio
+n
+-."
+-  scripts\ai-dev-auto-goal.ps1:744:    $script:steps += New-StepResult 5 "write-state-files" "$goa
+l
+-RelativePath, $queue
+-RelativePath, $stateRelativePath" $false $true 0 "DryRun: state files were not written."
+-  scripts\ai-dev-auto-goal.ps1:745:    $script:steps += New-StepResult 6 "make-prompt" "powershell
+ 
+--ExecutionPolicy Byp
+-ass -File scripts/ai-dev-make-prompt.ps1" $false $true 0 "DryRun: current-task-prompt.md was not g
+e
+-nerated."
+-  scripts\ai-dev-auto-goal.ps1:746:
+-  scripts\ai-dev-auto-goal.ps1:747:    if ($shouldRunFullCycle) {
+-  scripts\ai-dev-auto-goal.ps1:748:        $script:steps += New-StepResult 7 "auto-cycle-full" $fu
+l
+-lCycleCommandText $f
+-alse $true 0 "DryRun: explicit run option is present, but full cycle was not executed. -AllowRun o
+n
+-ly invokes the full-
+-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
+-  scripts\ai-dev-auto-goal.ps1:749:    } else {
+-  scripts\ai-dev-auto-goal.ps1:750:        $script:steps += New-StepResult 7 "auto-cycle-full" $fu
+l
+-lCycleCommandText $f
+-alse $true 0 "DryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only 
+i
+-nvokes the full-cycl
+-e wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
+-  scripts\ai-dev-auto-goal.ps1:751:    }
+-  scripts\ai-dev-auto-goal.ps1:752:
+-  scripts\ai-dev-auto-goal.ps1:753:    Stop-AutoGoal $script:steps "dry_run" $false 0
+-  scripts\ai-dev-auto-goal.ps1:754:}
+-  scripts\ai-dev-auto-goal.ps1:755:
+-  scripts\ai-dev-auto-goal.ps1:756:$statusLines = @(Get-GitStatusLines)
+-  scripts\ai-dev-auto-goal.ps1:757:
+-  scripts\ai-dev-auto-goal.ps1:758:if ($statusLines.Count -gt 0 -and -not $AllowDirty) {
+-  scripts\ai-dev-auto-goal.ps1:759:    $dirtyText = ($statusLines -join "`n")
+-  scripts\ai-dev-auto-goal.ps1:760:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
+t
+- status --short" $fa
+-lse $true 1 "Worktree is dirty. Use -AllowDirty only when this is intentional.`n$dirtyText"
+-  scripts\ai-dev-auto-goal.ps1:761:    Stop-AutoGoal $script:steps "dirty_worktree" $false 1
+-  scripts\ai-dev-auto-goal.ps1:762:}
+-  scripts\ai-dev-auto-goal.ps1:763:
+-  scripts\ai-dev-auto-goal.ps1:764:if ($statusLines.Count -gt 0) {
+-  scripts\ai-dev-auto-goal.ps1:765:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
+t
+- status --short" $fa
+-lse $false 0 "AllowDirty is set. DirtyCount: $($statusLines.Count)"
+-  scripts\ai-dev-auto-goal.ps1:766:} else {
+-  scripts\ai-dev-auto-goal.ps1:767:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
+t
+- status --short" $fa
+-lse $false 0 "Worktree is clean."
+-  scripts\ai-dev-auto-goal.ps1:768:}
+-  scripts\ai-dev-auto-goal.ps1:769:
+-  scripts\ai-dev-auto-goal.ps1:770:$codexCommand = Get-Command codex -ErrorAction SilentlyContinue
+-  scripts\ai-dev-auto-goal.ps1:771:
+-  scripts\ai-dev-auto-goal.ps1:772:if ($null -eq $codexCommand) {
+-  scripts\ai-dev-auto-goal.ps1:773:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <
+a
+-uto-goal planning pr
+-ompt>" $false $false 1 "Codex CLI was not found."
+-  scripts\ai-dev-auto-goal.ps1:774:    Stop-AutoGoal $script:steps "codex_not_found" $false 1
+-  scripts\ai-dev-auto-goal.ps1:775:}
+-  scripts\ai-dev-auto-goal.ps1:776:
+-  scripts\ai-dev-auto-goal.ps1:777:$plannerPrompt = New-CodexAutoGoalPrompt $GoalTitle.Trim() $Goa
+l
+-Description.Trim()
+-  scripts\ai-dev-auto-goal.ps1:778:$startedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
+-  scripts\ai-dev-auto-goal.ps1:779:$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
+-  scripts\ai-dev-auto-goal.ps1:780:$codexExitCode = $LASTEXITCODE
+-  scripts\ai-dev-auto-goal.ps1:781:$endedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
+-  scripts\ai-dev-auto-goal.ps1:782:
+-  scripts\ai-dev-auto-goal.ps1:783:$resultContent = @"
+-  scripts\ai-dev-auto-goal.ps1:784:# Codex Auto Goal Result
+-  scripts\ai-dev-auto-goal.ps1:785:
+-  scripts\ai-dev-auto-goal.ps1:786:## Run
+-  scripts\ai-dev-auto-goal.ps1:787:
+-  scripts\ai-dev-auto-goal.ps1:788:- Started at: $startedAt
+-  scripts\ai-dev-auto-goal.ps1:789:- Ended at: $endedAt
+-  scripts\ai-dev-auto-goal.ps1:790:- Exit code: $codexExitCode
+-  scripts\ai-dev-auto-goal.ps1:791:- Goal title: $($GoalTitle.Trim())
+-  scripts\ai-dev-auto-goal.ps1:792:- Command: codex exec <auto-goal planning prompt>
+-  scripts\ai-dev-auto-goal.ps1:793:
+-  scripts\ai-dev-auto-goal.ps1:794:## Output
+-  scripts\ai-dev-auto-goal.ps1:795:
+-  scripts\ai-dev-auto-goal.ps1:796:${codeFence}text
+-  scripts\ai-dev-auto-goal.ps1:797:$codexOutput
+-  scripts\ai-dev-auto-goal.ps1:798:$codeFence
+-  scripts\ai-dev-auto-goal.ps1:799:"@
+-  scripts\ai-dev-auto-goal.ps1:800:
+-  scripts\ai-dev-auto-goal.ps1:801:[System.IO.File]::WriteAllText($resolvedResultPath, $resultCont
+e
+-nt, $utf8WithBom)
+-  scripts\ai-dev-auto-goal.ps1:802:
+-  scripts\ai-dev-auto-goal.ps1:803:if ($codexExitCode -ne 0) {
+-  scripts\ai-dev-auto-goal.ps1:804:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <
+a
+-uto-goal planning pr
+-ompt>" $true $false 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativePath $ResultPat
+h
+-)"
+-  scripts\ai-dev-auto-goal.ps1:805:    Stop-AutoGoal $script:steps "codex_plan_failed" $false 1
+-  scripts\ai-dev-auto-goal.ps1:806:}
+-  scripts\ai-dev-auto-goal.ps1:807:
+-  scripts\ai-dev-auto-goal.ps1:808:$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto
+-
+-goal planning prompt
+->" $true $false 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativePath $ResultPath
+)
+-"
+-  scripts\ai-dev-auto-goal.ps1:809:
+-  scripts\ai-dev-auto-goal.ps1:810:try {
+-  scripts\ai-dev-auto-goal.ps1:811:    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutpu
+t
+-  scripts\ai-dev-auto-goal.ps1:812:} catch {
+-  scripts\ai-dev-auto-goal.ps1:813:    $preview = Get-InputPreview $codexOutput
+-
+-
+-
+- succeeded in 2311ms:
+-
+-> .ai-dev\README.md:750:### 목표 입력 기반 자동 실행
+-  .ai-dev\README.md:751:
+-  .ai-dev\README.md:752:최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게
+ 
+-하는 것이다.
+-  .ai-dev\README.md:753:
+-  .ai-dev\README.md:754:먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
+-  .ai-dev\README.md:755:
+-  .ai-dev\README.md:756:```powershell
+-  .ai-dev\README.md:757:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
+o
+-alTitle "업무 검색 결과 하이
+-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
+-  .ai-dev\README.md:758:```
+-  .ai-dev\README.md:759:
+-  .ai-dev\README.md:760:자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
+-  .ai-dev\README.md:761:DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 
+파
+-일 작성 전에 생성될 goal/tas
+-k 계획을 검토할 수 있다.
+-  .ai-dev\README.md:762:
+-  .ai-dev\README.md:763:```powershell
+-  .ai-dev\README.md:764:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
+o
+-alTitle "업무 검색 결과 하이
+-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
+-  .ai-dev\README.md:765:```
+-  .ai-dev\README.md:766:
+-  .ai-dev\README.md:767:실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이
+ 
+-모드는 `ai-dev-auto-cyc
+-le-full.ps1`을 호출하지 않는다.
+-  .ai-dev\README.md:768:
+-  .ai-dev\README.md:769:```powershell
+-  .ai-dev\README.md:770:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
+o
+-alTitle "업무 검색 결과 하이
+-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
+-  .ai-dev\README.md:771:```
+-  .ai-dev\README.md:772:
+-  .ai-dev\README.md:773:생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowRe
+v
+-iewCodex`, `-AllowCo
+-mmit` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다.
+ 
+-실제 구현까지 허용하려면 `-Allo
+-wCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하게 `-AllowCodex -
+A
+-llowReviewCodex -All
+-owCommit`을 함께 지정한다.
+-  .ai-dev\README.md:774:
+-  .ai-dev\README.md:775:```powershell
+-  .ai-dev\README.md:776:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
+o
+-alTitle "업무 검색 결과 하이
+-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -
+A
+-llowCommit -MaxTasks
+- 1
+-  .ai-dev\README.md:777:```
+-  .ai-dev\README.md:778:
+-  .ai-dev\README.md:779:동작:
+-  .ai-dev\README.md:780:
+-  .ai-dev\README.md:781:1. `GoalTitle`과 `GoalDescription`을 검증한다.
+-  .ai-dev\README.md:782:2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
+-  .ai-dev\README.md:783:3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
+-  .ai-dev\README.md:784:4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다
+.
+-  .ai-dev\README.md:785:5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
+-  .ai-dev\README.md:786:6. `.ai-dev/current-task-prompt.md`를 생성한다.
+-  .ai-dev\README.md:787:7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
+-  .ai-dev\README.md:788:8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-
+t
+-ask-prompt.md`, 결과 파
+-일 때문에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트
+는
+- auto-goal 시작 전에 별도로
+- 유지한다.
+-  .ai-dev\README.md:789:
+-  .ai-dev\README.md:790:안전 기준:
+-  .ai-dev\README.md:791:
+-  .ai-dev\README.md:792:- `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
+-  .ai-dev\README.md:793:- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
+-  .ai-dev\README.md:794:- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
+-  .ai-dev\README.md:795:- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 
+않
+-는다.
+-  .ai-dev\README.md:796:- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowC
+o
+-mmit` 중 하나가 있을 때만 수행
+-한다.
+-  .ai-dev\README.md:797:- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCod
+e
+-x`, `-AllowReviewCod
+-ex`, `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
+-  .ai-dev\README.md:798:- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커
+밋
+-하지 않는다.
+-  .ai-dev\README.md:799:- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
+-  .ai-dev\README.md:800:
+-  .ai-dev\README.md:801:## 운영 원칙
+-  .ai-dev\README.md:802:
+-  .ai-dev\README.md:803:- 저장소의 `AGENTS.md`, 사용자 지시, 보안 정책이 자동 개발 루프보다 우선한다.
+-  .ai-dev\README.md:804:- 한 번에 하나의 task만 수행한다.
+-  .ai-dev\README.md:805:- task 단위로 구현, 검증, 리뷰, 커밋한다.
+-  .ai-dev\README.md:806:- 현재 task 범위 밖 파일을 임의로 수정하지 않는다.
+-  .ai-dev\README.md:807:- 검증하지 않은 결과를 통과로 기록하지 않는다.
+-  .ai-dev\README.md:808:- 실제 DB 삭제, 초기화, 복원, 대규모 리팩터링은 초기 실험 범위에서 제외한다.
+-  .ai-dev\README.md:809:
+-  .ai-dev\README.md:810:## 다음 단계
+-  .ai-dev\README.md:811:
+-  .ai-dev\README.md:812:실제 자동 개발 루프 실행기와 상태 갱신 로직은 별도 정책 검토 후 추가한다.
+-  .ai-dev\README.md:813:
+-  .ai-dev\README.md:814:세부 기준은 `docs/ai-dev-loop-policy.md`를 따른다.
+-
+-
++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "input|placeholde
+r
++|search|Search|query|filter" src/App.tsx' in D:\ai-apps\planpilot-local
++ succeeded in 1381ms:
++11:import { filterTasks, sortTasks, type TaskSortOrder } from "./utils/taskFilters";
++28:  const [taskSearchQuery, setTaskSearchQuery] = useState("");
++112:  const filteredTasks = filterTasks(tasks, {
++115:    taskSearchQuery,
++119:  const sortedTasks = sortTasks(filteredTasks, taskSortOrder);
++154:            filteredTasks={filteredTasks}
++159:            taskSearchQuery={taskSearchQuery}
++173:            onTaskSearchQueryChange={setTaskSearchQuery}
+ 
+ codex
+-수정 대상은 자동화 스크립트 하나입니다. README에는 이미 목표 입력 기반 실행 문서가 있어, 스크립트 동작 보강 후 문서와 실제 동작이 어긋나는 부분이 있는지만 다시 확인
+하
+-겠습니다.
++검색 입력은 `App.tsx` 안에 직접 있는 것이 아니라 하위 컴포넌트로 전달되는 구조입니다. 범위를 지키기 위해 `App.tsx`에서 연결된 컴포넌트 파일명만 확인한 뒤, 
+그
++ 파일의 placeholder 한 줄만 수정하겠습니다.
+ exec
+ "C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
+h
+- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' | Select-Object -Skip 330 -First 23
+0
+-" in D:\ai-apps\planpilot-local
+- succeeded in 1784ms:
+-
+-        if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
+-            $errors += "invalid task.status: $($task.status)"
+-        }
+-
+-        if (@("P0", "P1", "P2") -notcontains $task.priority) {
+-            $errors += "invalid task.priority: $($task.priority)"
+-        }
+-
+-        if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is [strin
+g
+-]) {
+-            $errors += "task.dependsOn must be an array: $($task.id)"
+-        }
+-
+-        if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.filesLikel
+y
+-ToChange -is [string]) {
+-            $errors += "task.filesLikelyToChange must be an array: $($task.id)"
+-        }
+-
+-        if ($task.verification -isnot [System.Collections.IEnumerable] -or $task.verification -is 
+[
+-string]) {
+-            $errors += "task.verification must be an array: $($task.id)"
+-        }
+-    }
+-
+-    if ((@($tasks | Where-Object { $_.status -eq "in_progress" })).Count -ne 1) {
+-        $errors += "queue.tasks must contain exactly one in_progress task."
+-    }
+-
+-    if (-not (Test-HasValue $Queue.currentTaskId)) {
+-        $errors += "queue.currentTaskId must not be empty."
+-    } elseif ($taskIds -notcontains [string]$Queue.currentTaskId) {
+-        $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-    }
+-
+-    return $errors
+-}
+-
+-function Get-StateValidationErrors {
+-    param(
+-        [object]$State,
+-        [object]$Queue
+-    )
+-
+-    $errors = @()
+-    $requiredFields = @("goalStatus", "currentTaskId", "currentLoop", "maxLoopsPerTask", "repeated
+F
+-ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDecision", "lastR
+e
+-viewSeverity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
+-
+-    foreach ($field in @(Get-MissingFields $State $requiredFields)) {
+-        $errors += "state missing field: $field"
+-    }
+-
+-    if ($errors.Count -gt 0) {
+-        return $errors
+-    }
+-
+-    if (@("idle", "in_progress", "completed", "blocked") -notcontains $State.goalStatus) {
+-        $errors += "invalid state.goalStatus: $($State.goalStatus)"
+-    }
+-
+-    if ($State.currentTaskId -ne $Queue.currentTaskId) {
+-        $errors += "state.currentTaskId must match queue.currentTaskId."
+-    }
+-
+-    if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-        $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
+-    }
+-
+-    if (@("not_started", "pass", "revise", "blocked") -notcontains $State.lastReviewDecision) {
+-        $errors += "invalid state.lastReviewDecision: $($State.lastReviewDecision)"
+-    }
+-
+-    return $errors
+-}
+-
+-function Get-AutoGoalValidationErrors {
+-    param([object]$Plan)
+-
+-    $errors = @()
+-
+-    foreach ($field in @(Get-MissingFields $Plan @("goalMarkdown", "queue", "state"))) {
+-        $errors += "plan missing field: $field"
+-    }
+-
+-    if ($errors.Count -gt 0) {
+-        return $errors
+-    }
+-
+-    if (-not (Test-HasValue $Plan.goalMarkdown)) {
+-        $errors += "goalMarkdown must not be empty."
+-    }
+-
+-    $errors += @(Get-QueueValidationErrors $Plan.queue)
+-    $errors += @(Get-StateValidationErrors $Plan.state $Plan.queue)
+-
+-    return $errors
+-}
+-
+-function New-DryRunAutoGoalPlan {
+-    param(
+-        [string]$Title,
+-        [string]$Description
+-    )
+-
+-    $now = (Get-Date).ToUniversalTime().ToString("o")
+-    $safeCommitTitle = $Title.Trim()
+-
+-    $plan = [PSCustomObject][ordered]@{
+-        goalMarkdown = @"
+-# 紐⑺몴
+-
+-$safeCommitTitle
+-
+-## 諛곌꼍
+-
+-$($Description.Trim())
+-
+-## ?깃났 湲곗?
+-
+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-
+-## ?쒖빟?ы빆
+-
+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-
+-## ?쒖쇅 踰붿쐞
+-
+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-
+-## ?섎룞 ?뺤씤
+-
+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-"@
+-        queue = [PSCustomObject][ordered]@{
+-            goalTitle = $safeCommitTitle
+-            goalSource = $goalRelativePath
+-            createdAt = $now
+-            updatedAt = $now
+-            currentTaskId = "T001"
+-            tasks = @(
+-                [PSCustomObject][ordered]@{
+-                    id = "T001"
+-                    title = $safeCommitTitle
+-                    description = $Description.Trim()
+-                    type = "implementation"
+-                    status = "in_progress"
+-                    priority = "P0"
+-                    dependsOn = @()
+-                    filesLikelyToChange = @()
+-                    verification = @(
+-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-                    )
+-                    commitMessage = $null
+-                }
+-            )
+-        }
+-        state = [PSCustomObject][ordered]@{
+-            goalStatus = "in_progress"
+-            currentTaskId = "T001"
+-            currentLoop = 0
+-            maxLoopsPerTask = 2
+-            repeatedFailureCount = 0
+-            lastCommand = $null
+-            lastCommandStatus = "not_started"
+-            lastErrorSummary = $null
+-            lastReviewDecision = "not_started"
+-            lastReviewSeverity = $null
+-            lastCommitHash = $null
+-            startedAt = $now
+-            updatedAt = $now
+-            stopReason = $null
+-        }
+-    }
+-
+-    $plan.goalMarkdown = @"
+-# 紐⑺몴
+-
+-$safeCommitTitle
+-
+-## 諛곌꼍
+-
+-$($Description.Trim())
+-
+-## ?깃났 湲곗?
+-
+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-
+-## ?쒖빟?ы빆
+-
+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-
+-## ?쒖쇅 踰붿쐞
+-
+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-
+-## ?섎룞 ?뺤씤
+-
+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-"@
+-
+-    $dryRunTask = @($plan.queue.tasks)[0]
+-    $dryRunTask.verification = @(
+-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-    )
+-
+-    return $plan
+-}
+-
+-function ConvertFrom-CodexAutoGoalOutput {
+-    param([string]$RawInput)
+-
+-    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
+-        try {
+-            $parsed = $candidate | ConvertFrom-Json
+-
+-            if ($null -eq $parsed -or $parsed -is [System.Array]) {
+-                continue
+-            }
+-
+-            if (@(Get-AutoGoalValidationErrors $parsed).Count -gt 0) {
+-                continue
+-            }
+-
+-codex
+-이제 `ai-dev-auto-goal.ps1`만 수정합니다. DryRun 미리보기 텍스트를 정상 한국어로 복구하고, Codex가 만든 queue/state가 `T001` 시작 
+규
+-칙을 벗어나면 파일을 쓰지 않도록 검증을 강화합니다.
+-apply patch
+-patch: completed
+-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1
+-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
+-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..40e87615fb5a517df15ebc3cce7963afb27cf9cd
+---- a/scripts/ai-dev-auto-goal.ps1
+-+++ b/scripts/ai-dev-auto-goal.ps1
+-@@ -297,6 +297,10 @@
+-         $errors += "queue.tasks must contain no more than three tasks."
+-     }
 - 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
+-+    if ($Queue.currentTaskId -ne "T001") {
+-+        $errors += "queue.currentTaskId must be T001."
+-+    }
+-+
+-     $taskIds = @()
 - 
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
+-     foreach ($task in $tasks) {
+-@@ -360,6 +364,19 @@
+-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-     }
 - 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
+-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
+-+    if ($null -eq $firstTask) {
+-+        $errors += "queue.tasks must include T001."
+-+    } elseif ($firstTask.status -ne "in_progress") {
+-+        $errors += "T001 must be in_progress."
 -+    }
 -+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
+-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
+-+        if ($laterTask.status -ne "pending") {
+-+            $errors += "later tasks must be pending: $($laterTask.id)"
 -+        }
 -+    }
 -+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
+-     return $errors
+- }
+- 
+-@@ -388,6 +405,10 @@
+-         $errors += "state.currentTaskId must match queue.currentTaskId."
+-     }
+- 
+-+    if ($State.currentTaskId -ne "T001") {
+-+        $errors += "state.currentTaskId must be T001."
 -+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
 -+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
+-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
+-     }
+-
+-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
+-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..40e87615fb5a517df15ebc3cce7963afb27cf9cd
+---- a/scripts/ai-dev-auto-goal.ps1
+-+++ b/scripts/ai-dev-auto-goal.ps1
+-@@ -297,6 +297,10 @@
+-         $errors += "queue.tasks must contain no more than three tasks."
+-     }
+- 
+-+    if ($Queue.currentTaskId -ne "T001") {
+-+        $errors += "queue.currentTaskId must be T001."
 -+    }
 -+
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
+-     $taskIds = @()
+- 
+-     foreach ($task in $tasks) {
+-@@ -360,6 +364,19 @@
+-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-     }
+- 
+-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
+-+    if ($null -eq $firstTask) {
+-+        $errors += "queue.tasks must include T001."
+-+    } elseif ($firstTask.status -ne "in_progress") {
+-+        $errors += "T001 must be in_progress."
+-+    }
 -+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
+-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
+-+        if ($laterTask.status -ne "pending") {
+-+            $errors += "later tasks must be pending: $($laterTask.id)"
+-+        }
 -+    }
--+}
 -+
-- Set-Location $repoRoot
+-     return $errors
+- }
 - 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
+-@@ -388,6 +405,10 @@
+-         $errors += "state.currentTaskId must match queue.currentTaskId."
 -     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
 - 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
+-+    if ($State.currentTaskId -ne "T001") {
+-+        $errors += "state.currentTaskId must be T001."
+-+    }
+-+
+-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
+-     }
+-
+-codex
+-검증 규칙 보강은 적용했습니다. 이어서 DryRun 계획 생성 함수의 깨진 문자열을 정상 한국어 미리보기로 교체합니다.
+-apply patch
+-patch: completed
+-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1
+-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
+-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
+---- a/scripts/ai-dev-auto-goal.ps1
+-+++ b/scripts/ai-dev-auto-goal.ps1
+-@@ -297,6 +297,10 @@
+-         $errors += "queue.tasks must contain no more than three tasks."
+-     }
 - 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
+-+    if ($Queue.currentTaskId -ne "T001") {
+-+        $errors += "queue.currentTaskId must be T001."
+-+    }
+-+
+-     $taskIds = @()
 - 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,97 @@
-- }
+-     foreach ($task in $tasks) {
+-@@ -360,6 +364,19 @@
+-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-     }
 - 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
+-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
+-+    if ($null -eq $firstTask) {
+-+        $errors += "queue.tasks must include T001."
+-+    } elseif ($firstTask.status -ne "in_progress") {
+-+        $errors += "T001 must be in_progress."
 -+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
 -+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
+-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
+-+        if ($laterTask.status -ne "pending") {
+-+            $errors += "later tasks must be pending: $($laterTask.id)"
+-+        }
+-+    }
 -+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
+-     return $errors
+- }
+- 
+-@@ -388,6 +405,10 @@
+-         $errors += "state.currentTaskId must match queue.currentTaskId."
+-     }
+- 
+-+    if ($State.currentTaskId -ne "T001") {
+-+        $errors += "state.currentTaskId must be T001."
 -+    }
 -+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
+-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
+-     }
+-@@ -429,41 +450,44 @@
+-     )
+- 
+-     $now = (Get-Date).ToUniversalTime().ToString("o")
+--    $safeCommitTitle = $Title.Trim()
+-+    $safeTitle = $Title.Trim()
+-+    $safeDescription = $Description.Trim()
+- 
+-     $plan = [PSCustomObject][ordered]@{
+-         goalMarkdown = @"
+--# 紐⑺몴
+-+# 목표
+- 
+--$safeCommitTitle
+-+$safeTitle
+- 
+--## 諛곌꼍
+-+## 배경
+- 
+--$($Description.Trim())
+-+$safeDescription
+- 
+--## ?깃났 湲곗?
+-+## 성공 기준
+- 
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-+- 입력한 목표에서 현재 작업을 준비한다.
+-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
+-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
+- 
+--## ?쒖빟?ы빆
+-+## 제약사항
+- 
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
+-+- 저장소 정책과 현재 작업 범위를 따른다.
+- 
+--## ?쒖쇅 踰붿쐞
+-+## 범위 제외
+- 
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-+- 패키지 변경
+-+- DB 삭제 또는 초기화
+-+- 광범위한 리팩터링
+- 
+--## ?섎룞 ?뺤씤
+-+## 수동 검증
+- 
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-+- DryRun 계획과 JSON 출력을 검토한다.
+-+- 현재 작업 프롬프트 생성 여부를 확인한다.
+-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
+- "@
+-         queue = [PSCustomObject][ordered]@{
+--            goalTitle = $safeCommitTitle
+-+            goalTitle = $safeTitle
+-             goalSource = $goalRelativePath
+-             createdAt = $now
+-             updatedAt = $now
+-@@ -471,16 +495,16 @@
+-             tasks = @(
+-                 [PSCustomObject][ordered]@{
+-                     id = "T001"
+--                    title = $safeCommitTitle
+--                    description = $Description.Trim()
+-+                    title = $safeTitle
+-+                    description = $safeDescription
+-                     type = "implementation"
+-                     status = "in_progress"
+-                     priority = "P0"
+-                     dependsOn = @()
+-                     filesLikelyToChange = @()
+-                     verification = @(
+--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
+-+                        "필요한 경우 npm run build를 별도로 실행한다."
+-                     )
+-                     commitMessage = $null
+-                 }
+-@@ -503,43 +527,6 @@
+-             stopReason = $null
+-         }
+-     }
+--
+--    $plan.goalMarkdown = @"
+--# 紐⑺몴
+--
+--$safeCommitTitle
+--
+--## 諛곌꼍
+--
+--$($Description.Trim())
+--
+--## ?깃났 湲곗?
+--
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+--
+--## ?쒖빟?ы빆
+--
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+--
+--## ?쒖쇅 踰붿쐞
+--
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+--
+--## ?섎룞 ?뺤씤
+--
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+--"@
+--
+--    $dryRunTask = @($plan.queue.tasks)[0]
+--    $dryRunTask.verification = @(
+--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+--    )
+- 
+-     return $plan
+- }
+-
+-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
+-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
+---- a/scripts/ai-dev-auto-goal.ps1
+-+++ b/scripts/ai-dev-auto-goal.ps1
+-@@ -297,6 +297,10 @@
+-         $errors += "queue.tasks must contain no more than three tasks."
+-     }
+- 
+-+    if ($Queue.currentTaskId -ne "T001") {
+-+        $errors += "queue.currentTaskId must be T001."
 -+    }
 -+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
+-     $taskIds = @()
+- 
+-     foreach ($task in $tasks) {
+-@@ -360,6 +364,19 @@
+-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-     }
+- 
+-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
+-+    if ($null -eq $firstTask) {
+-+        $errors += "queue.tasks must include T001."
+-+    } elseif ($firstTask.status -ne "in_progress") {
+-+        $errors += "T001 must be in_progress."
+-+    }
 -+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
+-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
+-+        if ($laterTask.status -ne "pending") {
+-+            $errors += "later tasks must be pending: $($laterTask.id)"
+-+        }
+-+    }
 -+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
+-     return $errors
 - }
 - 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
+-@@ -388,6 +405,10 @@
+-         $errors += "state.currentTaskId must match queue.currentTaskId."
+-     }
+- 
+-+    if ($State.currentTaskId -ne "T001") {
+-+        $errors += "state.currentTaskId must be T001."
+-+    }
 -+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+    if (Test-Path -LiteralPath $resolvedExtraInstructionsPath -PathType Leaf) {
-+        $extraInstructionsContent = Get-Content -Raw -Encoding UTF8 -LiteralPath $resolvedExtraIns
-t
-+ructionsPath
-+    }
-+}
+-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
+-     }
+-@@ -429,41 +450,44 @@
+-     )
+- 
+-     $now = (Get-Date).ToUniversalTime().ToString("o")
+--    $safeCommitTitle = $Title.Trim()
+-+    $safeTitle = $Title.Trim()
+-+    $safeDescription = $Description.Trim()
+- 
+-     $plan = [PSCustomObject][ordered]@{
+-         goalMarkdown = @"
+--# 紐⑺몴
+-+# 목표
+- 
+--$safeCommitTitle
+-+$safeTitle
+- 
+--## 諛곌꼍
+-+## 배경
+- 
+--$($Description.Trim())
+-+$safeDescription
+- 
+--## ?깃났 湲곗?
+-+## 성공 기준
+- 
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-+- 입력한 목표에서 현재 작업을 준비한다.
+-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
+-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
+- 
+--## ?쒖빟?ы빆
+-+## 제약사항
+- 
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
+-+- 저장소 정책과 현재 작업 범위를 따른다.
+- 
+--## ?쒖쇅 踰붿쐞
+-+## 범위 제외
+- 
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-+- 패키지 변경
+-+- DB 삭제 또는 초기화
+-+- 광범위한 리팩터링
+- 
+--## ?섎룞 ?뺤씤
+-+## 수동 검증
+- 
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-+- DryRun 계획과 JSON 출력을 검토한다.
+-+- 현재 작업 프롬프트 생성 여부를 확인한다.
+-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
+- "@
+-         queue = [PSCustomObject][ordered]@{
+--            goalTitle = $safeCommitTitle
+-+            goalTitle = $safeTitle
+-             goalSource = $goalRelativePath
+-             createdAt = $now
+-             updatedAt = $now
+-@@ -471,16 +495,16 @@
+-             tasks = @(
+-                 [PSCustomObject][ordered]@{
+-                     id = "T001"
+--                    title = $safeCommitTitle
+--                    description = $Description.Trim()
+-+                    title = $safeTitle
+-+                    description = $safeDescription
+-                     type = "implementation"
+-                     status = "in_progress"
+-                     priority = "P0"
+-                     dependsOn = @()
+-                     filesLikelyToChange = @()
+-                     verification = @(
+--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
+-+                        "필요한 경우 npm run build를 별도로 실행한다."
+-                     )
+-                     commitMessage = $null
+-                 }
+-@@ -503,43 +527,6 @@
+-             stopReason = $null
+-         }
+-     }
+--
+--    $plan.goalMarkdown = @"
+--# 紐⑺몴
+--
+--$safeCommitTitle
+--
+--## 諛곌꼍
+--
+--$($Description.Trim())
+--
+--## ?깃났 湲곗?
+--
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+--
+--## ?쒖빟?ы빆
+--
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+--
+--## ?쒖쇅 踰붿쐞
+--
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+--
+--## ?섎룞 ?뺤씤
+--
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+--"@
+--
+--    $dryRunTask = @($plan.queue.tasks)[0]
+--    $dryRunTask.verification = @(
+--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+--    )
+- 
+-     return $plan
+- }
++ 'D:\\ai-apps\\planpilot-local\\src\\App.tsx' -TotalCount 220" in D:\ai-apps\planpilot-local
++ succeeded in 1111ms:
++import { useEffect, useMemo, useState } from "react";
++import { RuleBasedAIProvider } from "./ai/RuleBasedAIProvider";
++import { useProjectActions } from "./hooks/useProjectActions";
++import { useProjectFormState } from "./hooks/useProjectFormState";
++import { useTaskActions } from "./hooks/useTaskActions";
++import { useTaskFormState } from "./hooks/useTaskFormState";
++import { useStore } from "./store";
++import { startOfToday } from "./utils/dateUtils";
++import { getProjectName } from "./utils/projectLookup";
++import { getOverdueTasks, getUpcomingTasks } from "./utils/taskDates";
++import { filterTasks, sortTasks, type TaskSortOrder } from "./utils/taskFilters";
++import { getTaskSummary } from "./utils/taskSummary";
++import { SettingsView } from "./views/SettingsView";
++import { TasksView } from "./views/TasksView";
++import { ProjectsView } from "./views/ProjectsView";
++import { TodayView } from "./views/TodayView";
++import type { Task } from "./types";
++import "./App.css";
++
++type Tab = "today" | "tasks" | "projects" | "settings";
++
++function App() {
++  const [activeTab, setActiveTab] = useState<Tab>("today");
++  const [recommendedTasks, setRecommendedTasks] = useState<Task[]>([]);
++  const [selectedProjectFilter, setSelectedProjectFilter] = useState("all");
++  const [showCompletedTasks, setShowCompletedTasks] = useState(true);
++  const [taskSortOrder, setTaskSortOrder] = useState<TaskSortOrder>("none");
++  const [taskSearchQuery, setTaskSearchQuery] = useState("");
++
++  const { tasks, projects, appSettings, initializeApp, addTask, updateTask, deleteTask, deleteProj
+e
++ct, addProject, updateProject } = useStore();
++  const taskFormState = useTaskFormState();
++  const {
++    editingTaskId,
++    editTaskTitle,
++    editTaskDueDate,
++    editTaskPriority,
++    editTaskMemo,
++    editTaskProjectId,
++    newTaskTitle,
++    newTaskDueDate,
++    newTaskPriority,
++    newTaskProjectId,
++    newTaskMemo,
++    isTaskFormOpen,
++    setEditTaskTitle,
++    setEditTaskDueDate,
++    setEditTaskPriority,
++    setEditTaskMemo,
++    setEditTaskProjectId,
++    setNewTaskTitle,
++    setNewTaskDueDate,
++    setNewTaskPriority,
++    setNewTaskProjectId,
++    setNewTaskMemo,
++    setIsTaskFormOpen,
++    resetNewTaskForm,
++    resetEditTaskForm,
++    startEditTask,
++  } = taskFormState;
++  const { handleAddTask, handleSaveEditTask, handleToggleTaskDone, handleDeleteTask } = useTaskAct
+i
++ons({
++    addTask,
++    updateTask,
++    deleteTask,
++    newTaskTitle,
++    newTaskDueDate,
++    newTaskPriority,
++    newTaskProjectId,
++    newTaskMemo,
++    editTaskTitle,
++    editTaskDueDate,
++    editTaskPriority,
++    editTaskMemo,
++    editTaskProjectId,
++    resetNewTaskForm,
++    resetEditTaskForm,
++  });
++  const projectFormState = useProjectFormState();
++  const {
++    editingProjectId,
++    editProjectName,
++    editProjectDescription,
++    newProjectName,
++    newProjectDescription,
++    setEditProjectName,
++    setEditProjectDescription,
++    setNewProjectName,
++    setNewProjectDescription,
++    resetNewProjectForm,
++    resetEditProjectForm,
++    startEditProject,
++  } = projectFormState;
++  const { handleAddProject, handleSaveEditProject, handleDeleteProject } = useProjectActions({
++    addProject,
++    updateProject,
++    deleteProject,
++    projects,
++    tasks,
++    newProjectName,
++    newProjectDescription,
++    editProjectName,
++    editProjectDescription,
++    resetNewProjectForm,
++    resetEditProjectForm,
++  });
++
++  const today = startOfToday();
++
++  const overdueTasks = getOverdueTasks(tasks, today);
++  const upcomingTasks = getUpcomingTasks(tasks, today, 7);
++  const taskSummary = getTaskSummary(tasks);
++
++  const filteredTasks = filterTasks(tasks, {
++    selectedProjectFilter,
++    showCompletedTasks,
++    taskSearchQuery,
++    projects,
++  });
++
++  const sortedTasks = sortTasks(filteredTasks, taskSortOrder);
++
++  const aiProvider = useMemo(() => new RuleBasedAIProvider(), []);
++
++  useEffect(() => {
++    void initializeApp();
++  }, [initializeApp]);
++
++  useEffect(() => {
++    void aiProvider.suggestTodayTasks(tasks, 3).then(setRecommendedTasks);
++  }, [aiProvider, tasks]);
++
++  return (
++    <div className="app-shell">
++      <header className="app-header">
++        <p className="eyebrow">Privacy-first local planner</p>
++        <h1>PlanPilot Local</h1>
++        <p>?쒕쾭 ?놁씠 濡쒖뺄????λ릺??媛쒖씤 ?쇱젙쨌?낅Т 愿由???/p>
++      </header>
++
++      <main className="app-main">
++{activeTab === "today" && (
++  <TodayView
++    totalTasks={taskSummary.totalTasks}
++    completedTasks={taskSummary.completedTasks}
++    recommendedTasks={recommendedTasks}
++    overdueTasks={overdueTasks}
++    upcomingTasks={upcomingTasks}
++    getProjectName={(projectId) => getProjectName(projects, projectId)}
++  />
++)}
++
++        {activeTab === "tasks" && (
++          <TasksView
++            projects={projects}
++            filteredTasks={filteredTasks}
++            sortedTasks={sortedTasks}
++            selectedProjectFilter={selectedProjectFilter}
++            showCompletedTasks={showCompletedTasks}
++            taskSortOrder={taskSortOrder}
++            taskSearchQuery={taskSearchQuery}
++            isTaskFormOpen={isTaskFormOpen}
++            newTaskTitle={newTaskTitle}
++            newTaskMemo={newTaskMemo}
++            newTaskDueDate={newTaskDueDate}
++            newTaskPriority={newTaskPriority}
++            newTaskProjectId={newTaskProjectId}
++            editingTaskId={editingTaskId}
++            editTaskTitle={editTaskTitle}
++            editTaskMemo={editTaskMemo}
++            editTaskDueDate={editTaskDueDate}
++            editTaskPriority={editTaskPriority}
++            editTaskProjectId={editTaskProjectId}
++            getProjectName={(projectId) => getProjectName(projects, projectId)}
++            onTaskSearchQueryChange={setTaskSearchQuery}
++            onShowCompletedTasksChange={setShowCompletedTasks}
++            onTaskSortOrderChange={setTaskSortOrder}
++            onSelectedProjectFilterChange={setSelectedProjectFilter}
++            onTaskFormOpenChange={setIsTaskFormOpen}
++            onNewTaskTitleChange={setNewTaskTitle}
++            onNewTaskMemoChange={setNewTaskMemo}
++            onNewTaskDueDateChange={setNewTaskDueDate}
++            onNewTaskPriorityChange={setNewTaskPriority}
++            onNewTaskProjectIdChange={setNewTaskProjectId}
++            onEditTaskTitleChange={setEditTaskTitle}
++            onEditTaskMemoChange={setEditTaskMemo}
++            onEditTaskDueDateChange={setEditTaskDueDate}
++            onEditTaskPriorityChange={setEditTaskPriority}
++            onEditTaskProjectIdChange={setEditTaskProjectId}
++            onAddTask={handleAddTask}
++            onSaveEditTask={handleSaveEditTask}
++            onCancelEditTask={resetEditTaskForm}
++            onToggleTaskDone={handleToggleTaskDone}
++            onDeleteTask={handleDeleteTask}
++            onStartEditTask={startEditTask}
++          />
++        )}
++
++        {activeTab === "projects" && (
++          <ProjectsView
++            projects={projects}
++            tasks={tasks}
++            editingProjectId={editingProjectId}
++            editProjectName={editProjectName}
++            editProjectDescription={editProjectDescription}
++            newProjectName={newProjectName}
++            newProjectDescription={newProjectDescription}
++            onNewProjectNameChange={setNewProjectName}
++            onNewProjectDescriptionChange={setNewProjectDescription}
++            onEditProjectNameChange={setEditProjectName}
++            onEditProjectDescriptionChange={setEditProjectDescription}
++            onAddProject={handleAddProject}
++            onSaveEditProject={handleSaveEditProject}
++            onCancelEditProject={resetEditProjectForm}
++            onDeleteProject={handleDeleteProject}
++            onStartEditProject={startEditProject}
++          />
++        )}
++
++        {activeTab === "settings" && (
++          <SettingsView
++            appSettings={appSettings}
  
--codex
--이제 최종 형태로 문법과 DryRun을 다시 확인하겠습니다. 이 검증은 build/test/lint, git commit, 브라우저 실행을 하지 않습니다.
+ codex
+-수정은 완료되었습니다. 이제 PowerShell 파서로 문법을 확인하고, DryRun/JSON/빈 입력 오류 경로가 요구사항대로 동작하는지 실행 수준에서 검증하겠습니다.
++`TasksView`가 실제 업무 화면 UI를 담당합니다. 이 파일에서 검색 입력의 표시 문구만 바꾸겠습니다.
+ exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
+l
+-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription 
+'
+-검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun" in D:\ai-apps\planpilot-local
+-exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
+l
+-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription 
+'
+-검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun -Json" in D:\ai-apps\planpilot-local
 -exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$tokens = $null; $parse
--Errors = $null; $null = [System.Management.Automation.Language.Parser]::ParseFile('"'scripts/ai-de
--v-auto-cycle-full.ps1', [ref]"'$tokens, [ref]$parseErrors); if ($parseErrors.Count -gt 0) { $parse
--Errors | ForEach-Object { $_.Message }; exit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-l
--ocal
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
+l
+-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '' -GoalDescription '설명' -DryRun -Jso
+n
+-" in D:\ai-apps\planpilot-local
 -exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'powershell -ExecutionPo
--licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -MaxSteps 20 -Json' in D:
--\ai-apps\planpilot-local
-- succeeded in 1813ms:
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$tokens=$null; $errors=
+$
+-null; [System.Management.Automation.Language.Parser]::ParseFile('"'D:\\ai-apps\\planpilot-local\\s
+c
+-ripts\\ai-dev-auto-goal.ps1',[ref]"'$tokens,[ref]$errors) > $null; if ($errors.Count -gt 0) { $err
+o
+-rs | ForEach-Object { $_.Message }; exit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-local
+- succeeded in 3046ms:
 -syntax ok
-+$promptContent = @"
-+# Current Task Prompt
- 
-- succeeded in 3056ms:
+-
+- exited 1 in 4136ms:
+-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1 : Missing an argument for parameter 'GoalT
+i
+-tle'. Specify a para
+-meter of type 'System.String' and try again.
+-    + CategoryInfo          : InvalidArgument: (:) [ai-dev-auto-goal.ps1], ParentContainsErrorReco
+r
+-dException
+-    + FullyQualifiedErrorId : MissingArgument,ai-dev-auto-goal.ps1
+- 
+-
+-2026-06-09T13:59:38.985059Z ERROR codex_core::tools::router: error=Exit code: 1
+-Wall time: 4.1 seconds
+-Output:
+-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1 : Missing an argument for parameter 'GoalT
+i
+-tle'. Specify a para
+-meter of type 'System.String' and try again.
+-    + CategoryInfo          : InvalidArgument: (:) [ai-dev-auto-goal.ps1], ParentContainsErrorReco
+r
+-dException
+-    + FullyQualifiedErrorId : MissingArgument,ai-dev-auto-goal.ps1
+- 
+-
+- succeeded in 4962ms:
 -{
 -    "steps":  [
 -                  {
 -                      "step":  1,
--                      "name":  "make-prompt",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pr
--ompt.ps1",
+-                      "name":  "validate-input",
+-                      "command":  "check GoalTitle/GoalDescription",
 -                      "executed":  false,
--                      "skipped":  true,
+-                      "skipped":  false,
 -                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
+-                      "message":  "Input validation completed: 업무 검색 결과 하이라이트 추가"
 -                  },
 -                  {
 -                      "step":  2,
--                      "name":  "run-codex",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-cod
--ex.ps1",
+-                      "name":  "dirty-worktree-gate",
+-                      "command":  "git status --short",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
+-                      "message":  "DryRun: dirty worktree gate was not executed."
 -                  },
 -                  {
 -                      "step":  3,
--                      "name":  "check",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.p
--s1 -BuildOnly",
+-                      "name":  "plan-goal",
+-                      "command":  "codex exec \u003cauto-goal planning prompt\u003e",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
+-                      "message":  "DryRun: Codex goal planning was not executed. Preview currentTa
+s
+-kId: T001, task: 업무 검색 결과 하이라이트 추가"
 -                  },
 -                  {
 -                      "step":  4,
--                      "name":  "save-diff",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-di
--ff.ps1",
+-                      "name":  "validate-generated-json",
+-                      "command":  "goal/queue/state JSON validation",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
+-                      "message":  "DryRun: preview goal/queue/state plan passed local schema valid
+a
+-tion."
 -                  },
 -                  {
 -                      "step":  5,
--                      "name":  "make-review-prompt",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-re
--view-prompt.ps1 -Strict",
+-                      "name":  "write-state-files",
+-                      "command":  ".ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
+-                      "message":  "DryRun: state files were not written."
 -                  },
 -                  {
 -                      "step":  6,
--                      "name":  "run-review-codex",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-rev
--iew-codex.ps1 -AllowDirty -SaveReview",
+-                      "name":  "make-prompt",
+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pr
+o
+-mpt.ps1",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
+-                      "message":  "DryRun: current-task-prompt.md was not generated."
 -                  },
 -                  {
 -                      "step":  7,
--                      "name":  "review-gate",
--                      "command":  "state.lastReviewDecision 확인",
+-                      "name":  "auto-cycle-full",
+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cy
+c
+-le-full.ps1 -MaxTasks 1 -MaxSteps 20 -AllowDirty",
+-                      "executed":  false,
+-                      "skipped":  true,
+-                      "exitCode":  0,
+-                      "message":  "DryRun: full cycle requires -AllowRun or explicit execution opt
+i
+-ons. -AllowRun only invokes the full-cycle wrapper; implementation and review still require -Allow
+C
+-odex and -AllowReviewCodex."
+-                  }
+-              ],
+-    "stoppedReason":  "dry_run",
+-    "completed":  false,
+-    "exitCode":  0,
+-    "plan":  {
+-                 "goalMarkdown":  "# 목표\n\n업무 검색 결과 하이라이트 추가\n\n## 배경\n\n검색어와 일치하는 업무 제목/메모/프로젝트명을
+ 
+-화면에서 강조 표시한다.\n\n## 성공 기준\n\n- 입력한 목표에서 현재 작업을 준비한다.\n- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.\n-
+ 
+-full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.\n\n## 제약사항\n\n- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 g
+i
+-t 명령은 사용하지 않는다.\n- 저장소 정책과 현재 작업 범위를 따른다.\n\n## 범위 제외\n\n- 패키지 변경\n- DB 삭제 또는 초기화\n- 광범위한 리팩터링\n\n
+#
+-# 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재 작업 프롬프트 생성 여부를 확인한다.\n- 필요한 경우 빌드와 검증은 별도로 실행한다.",
+-                 "queue":  {
+-                               "goalTitle":  "업무 검색 결과 하이라이트 추가",
+-                               "goalSource":  ".ai-dev/goal.md",
+-                               "createdAt":  "2026-06-09T13:59:38.3307470Z",
+-                               "updatedAt":  "2026-06-09T13:59:38.3307470Z",
+-                               "currentTaskId":  "T001",
+-                               "tasks":  [
+-                                             {
+-                                                 "id":  "T001",
+-                                                 "title":  "업무 검색 결과 하이라이트 추가",
+-                                                 "description":  "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강
+조
+- 표시한다.",
+-                                                 "type":  "implementation",
+-                                                 "status":  "in_progress",
+-                                                 "priority":  "P0",
+-                                                 "dependsOn":  [
+-
+-                                                               ],
+-                                                 "filesLikelyToChange":  [
+-
+-                                                                         ],
+-                                                 "verification":  [
+-                                                                      "변경 범위가 현재 목표와 일치하는지 확인한다.",
+-                                                                      "필요한 경우 npm run build를 별도로 실
+행
+-한다."
+-                                                                  ],
+-                                                 "commitMessage":  null
+-                                             }
+-                                         ]
+-                           },
+-                 "state":  {
+-                               "goalStatus":  "in_progress",
+-                               "currentTaskId":  "T001",
+-                               "currentLoop":  0,
+-                               "maxLoopsPerTask":  2,
+-                               "repeatedFailureCount":  0,
+-                               "lastCommand":  null,
+-                               "lastCommandStatus":  "not_started",
+-                               "lastErrorSummary":  null,
+-                               "lastReviewDecision":  "not_started",
+-                               "lastReviewSeverity":  null,
+-                               "lastCommitHash":  null,
+-                               "startedAt":  "2026-06-09T13:59:38.3307470Z",
+-                               "updatedAt":  "2026-06-09T13:59:38.3307470Z",
+-                               "stopReason":  null
+-                           }
+-             },
+-    "goalPath":  ".ai-dev/goal.md",
+-    "queuePath":  ".ai-dev/queue.json",
+-    "statePath":  ".ai-dev/state.json",
+-    "promptPath":  ".ai-dev/current-task-prompt.md",
+-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+-}
+-
+- succeeded in 5085ms:
+-Step 1: validate-input
+-  Command: check GoalTitle/GoalDescription
+-  Executed: False
+-  Skipped: False
+-  Exit code: 0
+-  Message: Input validation completed: 업무 검색 결과 하이라이트 추가
+-Step 2: dirty-worktree-gate
+-  Command: git status --short
+-  Executed: False
+-  Skipped: True
+-  Exit code: 0
+-  Message: DryRun: dirty worktree gate was not executed.
+-Step 3: plan-goal
+-  Command: codex exec <auto-goal planning prompt>
+-  Executed: False
+-  Skipped: True
+-  Exit code: 0
+-  Message: DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, task: 업무 검색 
+결
+-과 하이라이트 추가
+-Step 4: validate-generated-json
+-  Command: goal/queue/state JSON validation
+-  Executed: False
+-  Skipped: True
+-  Exit code: 0
+-  Message: DryRun: preview goal/queue/state plan passed local schema validation.
+-Step 5: write-state-files
+-  Command: .ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json
+-  Executed: False
+-  Skipped: True
+-  Exit code: 0
+-  Message: DryRun: state files were not written.
+-Step 6: make-prompt
+-  Command: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
+-  Executed: False
+-  Skipped: True
+-  Exit code: 0
+-  Message: DryRun: current-task-prompt.md was not generated.
+-Step 7: auto-cycle-full
+-  Command: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -MaxTasks 1
+ 
+--MaxSteps 20 -AllowDirty
+-  Executed: False
+-  Skipped: True
+-  Exit code: 0
+-  Message: DryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only inv
+o
+-kes the full-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCo
+d
+-ex.
+-Plan preview:
+-  Goal title: 업무 검색 결과 하이라이트 추가
+-  Current task id: T001
+-  Task T001: 업무 검색 결과 하이라이트 추가
+-    Type: implementation
+-    Status: in_progress
+-    Priority: P0
+-    Likely files: 
+-    Verification: 변경 범위가 현재 목표와 일치하는지 확인한다. / 필요한 경우 npm run build를 별도로 실행한다.
+-Stopped reason: dry_run
+-Completed: False
+-Exit code: 0
+-
+-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
+-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
+---- a/scripts/ai-dev-auto-goal.ps1
+-+++ b/scripts/ai-dev-auto-goal.ps1
+-@@ -297,6 +297,10 @@
+-         $errors += "queue.tasks must contain no more than three tasks."
+-     }
+- 
+-+    if ($Queue.currentTaskId -ne "T001") {
+-+        $errors += "queue.currentTaskId must be T001."
+-+    }
+-+
+-     $taskIds = @()
+- 
+-     foreach ($task in $tasks) {
+-@@ -360,6 +364,19 @@
+-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-     }
+- 
+-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
+-+    if ($null -eq $firstTask) {
+-+        $errors += "queue.tasks must include T001."
+-+    } elseif ($firstTask.status -ne "in_progress") {
+-+        $errors += "T001 must be in_progress."
+-+    }
+-+
+-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
+-+        if ($laterTask.status -ne "pending") {
+-+            $errors += "later tasks must be pending: $($laterTask.id)"
+-+        }
+-+    }
+-+
+-     return $errors
+- }
+- 
+-@@ -388,6 +405,10 @@
+-         $errors += "state.currentTaskId must match queue.currentTaskId."
+-     }
+- 
+-+    if ($State.currentTaskId -ne "T001") {
+-+        $errors += "state.currentTaskId must be T001."
+-+    }
+-+
+-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
+-     }
+-@@ -429,41 +450,44 @@
+-     )
+- 
+-     $now = (Get-Date).ToUniversalTime().ToString("o")
+--    $safeCommitTitle = $Title.Trim()
+-+    $safeTitle = $Title.Trim()
+-+    $safeDescription = $Description.Trim()
+- 
+-     $plan = [PSCustomObject][ordered]@{
+-         goalMarkdown = @"
+--# 紐⑺몴
+-+# 목표
+- 
+--$safeCommitTitle
+-+$safeTitle
+- 
+--## 諛곌꼍
+-+## 배경
+- 
+--$($Description.Trim())
+-+$safeDescription
+- 
+--## ?깃났 湲곗?
+-+## 성공 기준
+- 
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-+- 입력한 목표에서 현재 작업을 준비한다.
+-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
+-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
+- 
+--## ?쒖빟?ы빆
+-+## 제약사항
+- 
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
+-+- 저장소 정책과 현재 작업 범위를 따른다.
+- 
+--## ?쒖쇅 踰붿쐞
+-+## 범위 제외
+- 
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-+- 패키지 변경
+-+- DB 삭제 또는 초기화
+-+- 광범위한 리팩터링
+- 
+--## ?섎룞 ?뺤씤
+-+## 수동 검증
+- 
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-+- DryRun 계획과 JSON 출력을 검토한다.
+-+- 현재 작업 프롬프트 생성 여부를 확인한다.
+-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
+- "@
+-         queue = [PSCustomObject][ordered]@{
+--            goalTitle = $safeCommitTitle
+-+            goalTitle = $safeTitle
+-             goalSource = $goalRelativePath
+-             createdAt = $now
+-             updatedAt = $now
+-@@ -471,16 +495,16 @@
+-             tasks = @(
+-                 [PSCustomObject][ordered]@{
+-                     id = "T001"
+--                    title = $safeCommitTitle
+--                    description = $Description.Trim()
+-+                    title = $safeTitle
+-+                    description = $safeDescription
+-                     type = "implementation"
+-                     status = "in_progress"
+-                     priority = "P0"
+-                     dependsOn = @()
+-                     filesLikelyToChange = @()
+-                     verification = @(
+--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
+-+                        "필요한 경우 npm run build를 별도로 실행한다."
+-                     )
+-                     commitMessage = $null
+-                 }
+-@@ -503,43 +527,6 @@
+-             stopReason = $null
+-         }
+-     }
+--
+--    $plan.goalMarkdown = @"
+--# 紐⑺몴
+--
+--$safeCommitTitle
+--
+--## 諛곌꼍
+--
+--$($Description.Trim())
+--
+--## ?깃났 湲곗?
+--
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+--
+--## ?쒖빟?ы빆
+--
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+--
+--## ?쒖쇅 踰붿쐞
+--
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+--
+--## ?섎룞 ?뺤씤
+--
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+--"@
+--
+--    $dryRunTask = @($plan.queue.tasks)[0]
+--    $dryRunTask.verification = @(
+--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+--    )
+- 
+-     return $plan
+- }
++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "placeholder|sear
+c
++h|검색|taskSearchQuery|input" src/views/TasksView.tsx' in D:\ai-apps\planpilot-local
++ succeeded in 1404ms:
++14:  taskSearchQuery: string;
++58:  taskSearchQuery,
++94:  const hasSearchQuery = taskSearchQuery.trim().length > 0;
++99:      ? "검색 결과가 없습니다."
++110:        업무 검색
++111:        <input
++113:          value={taskSearchQuery}
++115:          placeholder="업무 제목 검색"
++120:        <input
++186:          {hasSearchQuery && <p>다른 검색어를 입력해보세요.</p>}
+ 
+ codex
+-DryRun과 JSON 출력은 정상입니다. 빈 문자열 테스트는 PowerShell 인수 파서가 `''`를 값 없는 파라미터로 처리해 스크립트 내부 검증까지 들어가지 않았고, 공
+백
+- 값과 생략 값 경로로 입력 검증을 다시 확인하겠습니다.
+-exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
+l
+-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle ' ' -GoalDescription '설명' -DryRun -Js
+o
+-n" in D:\ai-apps\planpilot-local
++검색창 위치를 확인했습니다. 기존 검색 값과 `onChange`는 그대로 두고 `placeholder` 문자열만 더 구체적인 한국어로 바꿉니다.
+ exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
+l
+-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription 
+'
+-검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun -AllowRun -Json" in D:\ai-apps\planpilot-local
+-exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
+l
+-icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalDescription '설명' -DryRun -Json" in D:\ai-ap
+p
+-s\planpilot-local
+- exited 1 in 4098ms:
+-{
+-    "steps":  [
+-                  {
+-                      "step":  0,
+-                      "name":  "validate-input",
+-                      "command":  "check GoalTitle",
+-                      "executed":  false,
+-                      "skipped":  false,
+-                      "exitCode":  1,
+-                      "message":  "GoalTitle must not be empty."
+-                  }
+-              ],
+-    "stoppedReason":  "invalid_goal_title",
+-    "completed":  false,
+-    "exitCode":  1,
+-    "plan":  null,
+-    "goalPath":  ".ai-dev/goal.md",
+-    "queuePath":  ".ai-dev/queue.json",
+-    "statePath":  ".ai-dev/state.json",
+-    "promptPath":  ".ai-dev/current-task-prompt.md",
+-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+-}
+-
+-2026-06-09T13:59:56.754354Z ERROR codex_core::tools::router: error=Exit code: 1
+-Wall time: 4.1 seconds
+-Output:
+-{
+-    "steps":  [
+-                  {
+-                      "step":  0,
+-                      "name":  "validate-input",
+-                      "command":  "check GoalTitle",
+-                      "executed":  false,
+-                      "skipped":  false,
+-                      "exitCode":  1,
+-                      "message":  "GoalTitle must not be empty."
+-                  }
+-              ],
+-    "stoppedReason":  "invalid_goal_title",
+-    "completed":  false,
+-    "exitCode":  1,
+-    "plan":  null,
+-    "goalPath":  ".ai-dev/goal.md",
+-    "queuePath":  ".ai-dev/queue.json",
+-    "statePath":  ".ai-dev/state.json",
+-    "promptPath":  ".ai-dev/current-task-prompt.md",
+-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+-}
+-
+- exited 1 in 4252ms:
+-{
+-    "steps":  [
+-                  {
+-                      "step":  0,
+-                      "name":  "validate-input",
+-                      "command":  "check GoalTitle",
+-                      "executed":  false,
+-                      "skipped":  false,
+-                      "exitCode":  1,
+-                      "message":  "GoalTitle must not be empty."
+-                  }
+-              ],
+-    "stoppedReason":  "invalid_goal_title",
+-    "completed":  false,
+-    "exitCode":  1,
+-    "plan":  null,
+-    "goalPath":  ".ai-dev/goal.md",
+-    "queuePath":  ".ai-dev/queue.json",
+-    "statePath":  ".ai-dev/state.json",
+-    "promptPath":  ".ai-dev/current-task-prompt.md",
+-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+-}
+-
+- succeeded in 4254ms:
+-{
+-    "steps":  [
+-                  {
+-                      "step":  1,
+-                      "name":  "validate-input",
+-                      "command":  "check GoalTitle/GoalDescription",
+-                      "executed":  false,
+-                      "skipped":  false,
+-                      "exitCode":  0,
+-                      "message":  "Input validation completed: 업무 검색 결과 하이라이트 추가"
+-                  },
+-                  {
+-                      "step":  2,
+-                      "name":  "dirty-worktree-gate",
+-                      "command":  "git status --short",
+-                      "executed":  false,
+-                      "skipped":  true,
+-                      "exitCode":  0,
+-                      "message":  "DryRun: dirty worktree gate was not executed."
+-                  },
+-                  {
+-                      "step":  3,
+-                      "name":  "plan-goal",
+-                      "command":  "codex exec \u003cauto-goal planning prompt\u003e",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
+-                      "message":  "DryRun: Codex goal planning was not executed. Preview currentTa
+s
+-kId: T001, task: 업무 검색 결과 하이라이트 추가"
 -                  },
 -                  {
--                      "step":  8,
--                      "name":  "package-change-gate",
--                      "command":  "git status --porcelain -- package.json package-lock.json",
+-                      "step":  4,
+-                      "name":  "validate-generated-json",
+-                      "command":  "goal/queue/state JSON validation",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
+-                      "message":  "DryRun: preview goal/queue/state plan passed local schema valid
+a
+-tion."
 -                  },
 -                  {
--                      "step":  9,
--                      "name":  "commit",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.
--ps1",
+-                      "step":  5,
+-                      "name":  "write-state-files",
+-                      "command":  ".ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: git add/commit을 실행하지 않았습니다."
+-                      "message":  "DryRun: state files were not written."
 -                  },
 -                  {
--                      "step":  10,
--                      "name":  "commit-result-gate",
--                      "command":  "state.lastCommand/lastCommitHash 확인",
+-                      "step":  6,
+-                      "name":  "make-prompt",
+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pr
+o
+-mpt.ps1",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
+-                      "message":  "DryRun: current-task-prompt.md was not generated."
 -                  },
 -                  {
--                      "step":  11,
--                      "name":  "complete-task",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complet
--e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
+-                      "step":  7,
+-                      "name":  "auto-cycle-full",
+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cy
+c
+-le-full.ps1 -MaxTasks 1 -MaxSteps 20 -AllowDirty",
 -                      "executed":  false,
 -                      "skipped":  true,
 -                      "exitCode":  0,
--                      "message":  "DryRun: task 완료 처리를 실행하지 않았습니다."
+-                      "message":  "DryRun: explicit run option is present, but full cycle was not 
+e
+-xecuted. -AllowRun only invokes the full-cycle wrapper; implementation and review still require -A
+l
+-lowCodex and -AllowReviewCodex."
 -                  }
 -              ],
 -    "stoppedReason":  "dry_run",
 -    "completed":  false,
--    "exitCode":  0
-+## Role
-+
-+너는 이 저장소의 자동 개발 에이전트다.
-+
-+## Goal
-+
-+$goalContent
-+
-+## Current Task
-+
-+- Task ID: $($currentTask.id)
-+- Title: $($currentTask.title)
-+- Description: $($currentTask.description)
-+- Type: $($currentTask.type)
-+- Status: $($currentTask.status)
-+- Priority: $($currentTask.priority)
-+- Depends on:
-+$dependsOnText
-+
-+## Task Scope
-+
-+- 현재 task만 수행한다.
-+- 현재 task 범위를 벗어나는 리팩터링을 하지 않는다.
-+- 다음 task를 미리 구현하지 않는다.
-+- 현재 task의 완료 기준과 검증 방법을 먼저 확인한다.
-+
-+## Likely Files
-+
-+$likelyFilesText
-+
-+## Verification
-+
-+$verificationText
-+
-+- 필요한 경우 ``npm run build``는 사람이 별도로 실행한다.
-+- 이 프롬프트는 자동으로 build, test, lint를 실행하라고 지시하지 않는다.
-+
-+## Required Output
-+
-+- 변경한 파일 목록
-+- 구현 또는 분석 내용 요약
-+- 검증 방법
-+- 남은 위험
-+- 다음 task로 넘어가도 되는지 여부
-+- ``.ai-dev/loop-log.md``에 기록할 작업 요약
-+
-+## Hard Rules
-+
-+- ``src`` 외 파일을 수정해야 하는 경우 이유를 기록한다.
-+- ``package.json``과 ``package-lock.json``은 수정하지 않는다. 꼭 필요하면 중단하고 이유만 기록한다.
-+- 실제 DB 삭제 또는 초기화를 하지 않는다.
-+- 사용자 데이터 복원 또는 덮어쓰기를 하지 않는다.
-+- 현재 task와 무관한 UI 전면 개편을 하지 않는다.
-+- 대규모 리팩터링을 하지 않는다.
-+- git commit을 실행하지 않는다.
-+- git reset, git checkout, git clean을 실행하지 않는다.
-+- npm install을 실행하지 않는다.
-+
-+## Stop Conditions
-+
-+- 요구사항이 충돌하는 경우
-+- 현재 task 범위 밖 수정이 필요한 경우
-+- 패키지 추가가 필요한 경우
-+- 데이터 삭제 또는 마이그레이션이 필요한 경우
-+- 같은 오류가 반복되는 경우
-+"@
-+
-+if (-not [string]::IsNullOrWhiteSpace($extraInstructionsContent)) {
-+    $promptContent += @"
-+
-+
-+## Extra Instructions
-+
-+$extraInstructionsContent
-+"@
- }
+-    "exitCode":  0,
+-    "plan":  {
+-                 "goalMarkdown":  "# 목표\n\n업무 검색 결과 하이라이트 추가\n\n## 배경\n\n검색어와 일치하는 업무 제목/메모/프로젝트명을
  
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..1bdb85799839984c1deb03a6eba84a23cfc01093
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
+-화면에서 강조 표시한다.\n\n## 성공 기준\n\n- 입력한 목표에서 현재 작업을 준비한다.\n- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.\n-
+ 
+-full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.\n\n## 제약사항\n\n- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 g
+i
+-t 명령은 사용하지 않는다.\n- 저장소 정책과 현재 작업 범위를 따른다.\n\n## 범위 제외\n\n- 패키지 변경\n- DB 삭제 또는 초기화\n- 광범위한 리팩터링\n\n
+#
+-# 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재 작업 프롬프트 생성 여부를 확인한다.\n- 필요한 경우 빌드와 검증은 별도로 실행한다.",
+-                 "queue":  {
+-                               "goalTitle":  "업무 검색 결과 하이라이트 추가",
+-                               "goalSource":  ".ai-dev/goal.md",
+-                               "createdAt":  "2026-06-09T13:59:55.7497650Z",
+-                               "updatedAt":  "2026-06-09T13:59:55.7497650Z",
+-                               "currentTaskId":  "T001",
+-                               "tasks":  [
+-                                             {
+-                                                 "id":  "T001",
+-                                                 "title":  "업무 검색 결과 하이라이트 추가",
+-                                                 "description":  "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강
+조
+- 표시한다.",
+-                                                 "type":  "implementation",
+-                                                 "status":  "in_progress",
+-                                                 "priority":  "P0",
+-                                                 "dependsOn":  [
+-
+-                                                               ],
+-                                                 "filesLikelyToChange":  [
+-
+-                                                                         ],
+-                                                 "verification":  [
+-                                                                      "변경 범위가 현재 목표와 일치하는지 확인한다.",
+-                                                                      "필요한 경우 npm run build를 별도로 실
+행
+-한다."
+-                                                                  ],
+-                                                 "commitMessage":  null
+-                                             }
+-                                         ]
+-                           },
+-                 "state":  {
+-                               "goalStatus":  "in_progress",
+-                               "currentTaskId":  "T001",
+-                               "currentLoop":  0,
+-                               "maxLoopsPerTask":  2,
+-                               "repeatedFailureCount":  0,
+-                               "lastCommand":  null,
+-                               "lastCommandStatus":  "not_started",
+-                               "lastErrorSummary":  null,
+-                               "lastReviewDecision":  "not_started",
+-                               "lastReviewSeverity":  null,
+-                               "lastCommitHash":  null,
+-                               "startedAt":  "2026-06-09T13:59:55.7497650Z",
+-                               "updatedAt":  "2026-06-09T13:59:55.7497650Z",
+-                               "stopReason":  null
+-                           }
+-             },
+-    "goalPath":  ".ai-dev/goal.md",
+-    "queuePath":  ".ai-dev/queue.json",
+-    "statePath":  ".ai-dev/state.json",
+-    "promptPath":  ".ai-dev/current-task-prompt.md",
+-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+-}
+-
+-2026-06-09T13:59:56.936113Z ERROR codex_core::tools::router: error=Exit code: 1
+-Wall time: 4.3 seconds
+-Output:
+-{
+-    "steps":  [
+-                  {
+-                      "step":  0,
+-                      "name":  "validate-input",
+-                      "command":  "check GoalTitle",
+-                      "executed":  false,
+-                      "skipped":  false,
+-                      "exitCode":  1,
+-                      "message":  "GoalTitle must not be empty."
+-                  }
+-              ],
+-    "stoppedReason":  "invalid_goal_title",
+-    "completed":  false,
+-    "exitCode":  1,
+-    "plan":  null,
+-    "goalPath":  ".ai-dev/goal.md",
+-    "queuePath":  ".ai-dev/queue.json",
+-    "statePath":  ".ai-dev/state.json",
+-    "promptPath":  ".ai-dev/current-task-prompt.md",
+-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+-}
++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
+h
++ 'D:\\ai-apps\\planpilot-local\\src\\views\\TasksView.tsx' -TotalCount 140" in D:\ai-apps\planpilo
+t
++-local
++ succeeded in 1162ms:
++import type { FormEvent } from "react";
++import { TaskCard } from "../components/TaskCard";
++import { TaskForm } from "../components/TaskForm";
++import type { Project, Task } from "../types";
++import type { TaskSortOrder } from "../utils/taskFilters";
++
++type TasksViewProps = {
++  projects: Project[];
++  filteredTasks: Task[];
++  sortedTasks: Task[];
++  selectedProjectFilter: string;
++  showCompletedTasks: boolean;
++  taskSortOrder: TaskSortOrder;
++  taskSearchQuery: string;
++  isTaskFormOpen: boolean;
++  newTaskTitle: string;
++  newTaskMemo: string;
++  newTaskDueDate: string;
++  newTaskPriority: Task["priority"];
++  newTaskProjectId: string;
++  editingTaskId: string | null;
++  editTaskTitle: string;
++  editTaskMemo: string;
++  editTaskDueDate: string;
++  editTaskPriority: Task["priority"];
++  editTaskProjectId: string;
++  getProjectName: (projectId: string) => string;
++  onTaskSearchQueryChange: (value: string) => void;
++  onShowCompletedTasksChange: (value: boolean) => void;
++  onTaskSortOrderChange: (value: TaskSortOrder) => void;
++  onSelectedProjectFilterChange: (value: string) => void;
++  onTaskFormOpenChange: (updater: (current: boolean) => boolean) => void;
++  onNewTaskTitleChange: (value: string) => void;
++  onNewTaskMemoChange: (value: string) => void;
++  onNewTaskDueDateChange: (value: string) => void;
++  onNewTaskPriorityChange: (value: Task["priority"]) => void;
++  onNewTaskProjectIdChange: (value: string) => void;
++  onEditTaskTitleChange: (value: string) => void;
++  onEditTaskMemoChange: (value: string) => void;
++  onEditTaskDueDateChange: (value: string) => void;
++  onEditTaskPriorityChange: (value: Task["priority"]) => void;
++  onEditTaskProjectIdChange: (value: string) => void;
++  onAddTask: (event: FormEvent<HTMLFormElement>) => void;
++  onSaveEditTask: (task: Task) => void;
++  onCancelEditTask: () => void;
++  onToggleTaskDone: (task: Task) => void;
++  onDeleteTask: (task: Task) => void;
++  onStartEditTask: (task: Task) => void;
++};
++
++export function TasksView({
++  projects,
++  filteredTasks,
++  sortedTasks,
++  selectedProjectFilter,
++  showCompletedTasks,
++  taskSortOrder,
++  taskSearchQuery,
++  isTaskFormOpen,
++  newTaskTitle,
++  newTaskMemo,
++  newTaskDueDate,
++  newTaskPriority,
++  newTaskProjectId,
++  editingTaskId,
++  editTaskTitle,
++  editTaskMemo,
++  editTaskDueDate,
++  editTaskPriority,
++  editTaskProjectId,
++  getProjectName,
++  onTaskSearchQueryChange,
++  onShowCompletedTasksChange,
++  onTaskSortOrderChange,
++  onSelectedProjectFilterChange,
++  onTaskFormOpenChange,
++  onNewTaskTitleChange,
++  onNewTaskMemoChange,
++  onNewTaskDueDateChange,
++  onNewTaskPriorityChange,
++  onNewTaskProjectIdChange,
++  onEditTaskTitleChange,
++  onEditTaskMemoChange,
++  onEditTaskDueDateChange,
++  onEditTaskPriorityChange,
++  onEditTaskProjectIdChange,
++  onAddTask,
++  onSaveEditTask,
++  onCancelEditTask,
++  onToggleTaskDone,
++  onDeleteTask,
++  onStartEditTask,
++}: TasksViewProps) {
++  const hasSearchQuery = taskSearchQuery.trim().length > 0;
++  const hasProjectFilter = selectedProjectFilter !== "all";
++  const hasVisibilityFilter = !showCompletedTasks;
++  const emptyMessage =
++    hasSearchQuery
++      ? "寃??寃곌낵媛 ?놁뒿?덈떎."
++      : hasProjectFilter || hasVisibilityFilter
++        ? "?꾩옱 ?꾪꽣 議곌굔??留욌뒗 ?낅Т媛 ?놁뒿?덈떎."
++        : "?깅줉???낅Т媛 ?놁뒿?덈떎.";
++
++  return (
++    <section className="screen-card">
++      <h2>?꾩껜 ?낅Т</h2>
++      <p className="summary">珥?{filteredTasks.length}媛?/p>
++
++      <label>
++        ?낅Т 寃??        <input
++          type="text"
++          value={taskSearchQuery}
++          onChange={(event) => onTaskSearchQueryChange(event.target.value)}
++          placeholder="?낅Т ?쒕ぉ 寃??
++        />
++      </label>
++
++      <label>
++        <input
++          type="checkbox"
++          checked={showCompletedTasks}
++          onChange={(event) => onShowCompletedTasksChange(event.target.checked)}
++        />
++        ?꾨즺 ?낅Т ?쒖떆
++      </label>
++
++      <label>
++        ?뺣젹
++        <select
++          value={taskSortOrder}
++          onChange={(event) =>
++            onTaskSortOrderChange(event.target.value as TaskSortOrder)
++          }
++        >
++          <option value="none">湲곕낯 ?쒖꽌</option>
++          <option value="dueDateAsc">留덇컧??鍮좊Ⅸ ??/option>
++        </select>
++      </label>
++
++      <label>
+ 
+-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
+-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
+---- a/scripts/ai-dev-auto-goal.ps1
+-+++ b/scripts/ai-dev-auto-goal.ps1
+-@@ -297,6 +297,10 @@
+-         $errors += "queue.tasks must contain no more than three tasks."
+-     }
 - 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
+-+    if ($Queue.currentTaskId -ne "T001") {
+-+        $errors += "queue.currentTaskId must be T001."
+-+    }
+-+
+-     $taskIds = @()
 - 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
+-     foreach ($task in $tasks) {
+-@@ -360,6 +364,19 @@
+-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-     }
 - 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
+-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
+-+    if ($null -eq $firstTask) {
+-+        $errors += "queue.tasks must include T001."
+-+    } elseif ($firstTask.status -ne "in_progress") {
+-+        $errors += "T001 must be in_progress."
+-+    }
 -+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
+-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
+-+        if ($laterTask.status -ne "pending") {
+-+            $errors += "later tasks must be pending: $($laterTask.id)"
+-+        }
+-+    }
 -+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..84c3a6c8713a84a4304f6cc025a4949946302bb5
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
+-     return $errors
 - }
 - 
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
+-@@ -388,6 +405,10 @@
+-         $errors += "state.currentTaskId must match queue.currentTaskId."
+-     }
 - 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
--+        }
+-+    if ($State.currentTaskId -ne "T001") {
+-+        $errors += "state.currentTaskId must be T001."
 -+    }
 -+
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
+-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
 -     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,97 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
--+
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-- }
-- 
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
-+$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-+[System.IO.File]::WriteAllText($promptPath, $promptContent, $utf8WithBom)
-+
-+Write-Host "생성된 프롬프트 파일: $promptRelativePath"
-+Write-Host "Current task id: $($currentTask.id)"
-+Write-Host "Current task title: $($currentTask.title)"
-+
+-@@ -429,41 +450,44 @@
+-     )
+- 
+-     $now = (Get-Date).ToUniversalTime().ToString("o")
+--    $safeCommitTitle = $Title.Trim()
+-+    $safeTitle = $Title.Trim()
+-+    $safeDescription = $Description.Trim()
+- 
+-     $plan = [PSCustomObject][ordered]@{
+-         goalMarkdown = @"
+--# 紐⑺몴
+-+# 목표
+- 
+--$safeCommitTitle
+-+$safeTitle
+- 
+--## 諛곌꼍
+-+## 배경
+- 
+--$($Description.Trim())
+-+$safeDescription
+- 
+--## ?깃났 湲곗?
+-+## 성공 기준
+- 
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-+- 입력한 목표에서 현재 작업을 준비한다.
+-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
+-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
+- 
+--## ?쒖빟?ы빆
+-+## 제약사항
+- 
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
+-+- 저장소 정책과 현재 작업 범위를 따른다.
+- 
+--## ?쒖쇅 踰붿쐞
+-+## 범위 제외
+- 
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-+- 패키지 변경
+-+- DB 삭제 또는 초기화
+-+- 광범위한 리팩터링
+- 
+--## ?섎룞 ?뺤씤
+-+## 수동 검증
+- 
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-+- DryRun 계획과 JSON 출력을 검토한다.
+-+- 현재 작업 프롬프트 생성 여부를 확인한다.
+-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
+- "@
+-         queue = [PSCustomObject][ordered]@{
+--            goalTitle = $safeCommitTitle
+-+            goalTitle = $safeTitle
+-             goalSource = $goalRelativePath
+-             createdAt = $now
+-             updatedAt = $now
+-@@ -471,16 +495,16 @@
+-             tasks = @(
+-                 [PSCustomObject][ordered]@{
+-                     id = "T001"
+--                    title = $safeCommitTitle
+--                    description = $Description.Trim()
+-+                    title = $safeTitle
+-+                    description = $safeDescription
+-                     type = "implementation"
+-                     status = "in_progress"
+-                     priority = "P0"
+-                     dependsOn = @()
+-                     filesLikelyToChange = @()
+-                     verification = @(
+--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
+-+                        "필요한 경우 npm run build를 별도로 실행한다."
+-                     )
+-                     commitMessage = $null
+-                 }
+-@@ -503,43 +527,6 @@
+-             stopReason = $null
+-         }
+-     }
+--
+--    $plan.goalMarkdown = @"
+--# 紐⑺몴
+--
+--$safeCommitTitle
+--
+--## 諛곌꼍
+--
+--$($Description.Trim())
+--
+--## ?깃났 湲곗?
+--
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+--
+--## ?쒖빟?ы빆
+--
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+--
+--## ?쒖쇅 踰붿쐞
+--
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+--
+--## ?섎룞 ?뺤씤
+--
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+--"@
+--
+--    $dryRunTask = @($plan.queue.tasks)[0]
+--    $dryRunTask.verification = @(
+--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+--    )
 +codex
-+현재 스크립트는 이미 주요 흐름을 갖고 있지만, DryRun 미리보기의 한국어 문자열이 깨져 있고 생성 JSON 검증이 프롬프트 규칙 일부를 충분히 강제하지 않습니다. 파일 작
-성
-+ 전에 이 두 부분을 좁게 보강하겠습니다.
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralP
++PowerShell 표시 인코딩 때문에 파일 출력 일부가 깨져 보이지만, 검색 도구 기준으로 실제 파일의 한국어 문자열은 유지되어 있습니다. 인코딩을 건드리지 않도록 패치로 해
+당
++ 문자열 하나만 교체하겠습니다.
++apply patch
++patch: completed
++D:\ai-apps\planpilot-local\src\views\TasksView.tsx
++diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
++index f2f7fd812594455482fd78512b1a830e5d5dfe7a..7167cf7befdbe0130902ab6d86eb19a9eab5b300
++--- a/src/views/TasksView.tsx
+++++ b/src/views/TasksView.tsx
++@@ -112,7 +112,7 @@
++           type="text"
++           value={taskSearchQuery}
++           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
++-          placeholder="업무 제목 검색"
+++          placeholder="업무 제목이나 메모로 검색"
++         />
++       </label>
++ 
++
++diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
++index f2f7fd812594455482fd78512b1a830e5d5dfe7a..7167cf7befdbe0130902ab6d86eb19a9eab5b300
++--- a/src/views/TasksView.tsx
+++++ b/src/views/TasksView.tsx
++@@ -112,7 +112,7 @@
++           type="text"
++           value={taskSearchQuery}
++           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
++-          placeholder="업무 제목 검색"
+++          placeholder="업무 제목이나 메모로 검색"
++         />
++       </label>
+  
+-     return $plan
+- }
+ 
+ codex
+-검증 결과 DryRun은 파일을 쓰지 않고 계획과 단계만 출력했고, JSON 모드는 텍스트와 섞이지 않았습니다. 마지막으로 수정된 함수 주변을 다시 읽어 의도치 않은 중복이나 
+깨
+-진 문자열이 남아 있는지 확인하겠습니다.
+-exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralP
 a
-+th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern 'New-DryRunAutoGoalPlan'
-,
-+'Get-QueueValidationErrors','currentTaskId must be' -Context 0,80" in D:\ai-apps\planpilot-local
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralP
+-th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern 'queue.currentTaskId mus
+t
+- be T001','T001 must be in_progress','function New-DryRunAutoGoalPlan' -Context 0,35" in D:\ai-app
+s
+-\planpilot-local
++문구 변경은 적용됐습니다. 이제 같은 파일에서 placeholder와 검색 상태 연결이 그대로인지 텍스트 수준으로 확인하겠습니다. build/lint는 지시상 실행하지 않습니다
+.
+ exec
+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralP
 a
-+th 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md' -Pattern '목표 입력 기반 자동 실행' -Context 0,120" in
- 
-+D:\ai-apps\planpilot-local
-+ succeeded in 2293ms:
-+
-+> scripts\ai-dev-auto-goal.ps1:262:function Get-QueueValidationErrors {
-+  scripts\ai-dev-auto-goal.ps1:263:    param([object]$Queue)
-+  scripts\ai-dev-auto-goal.ps1:264:
-+  scripts\ai-dev-auto-goal.ps1:265:    $errors = @()
-+  scripts\ai-dev-auto-goal.ps1:266:    $requiredFields = @("goalTitle", "goalSource", "createdAt",
- 
-+"updatedAt", "curren
-+tTaskId", "tasks")
-+  scripts\ai-dev-auto-goal.ps1:267:
-+  scripts\ai-dev-auto-goal.ps1:268:    foreach ($field in @(Get-MissingFields $Queue $requiredFiel
-d
-+s)) {
-+  scripts\ai-dev-auto-goal.ps1:269:        $errors += "queue missing field: $field"
-+  scripts\ai-dev-auto-goal.ps1:270:    }
-+  scripts\ai-dev-auto-goal.ps1:271:
-+  scripts\ai-dev-auto-goal.ps1:272:    if ($errors.Count -gt 0) {
-+  scripts\ai-dev-auto-goal.ps1:273:        return $errors
-+  scripts\ai-dev-auto-goal.ps1:274:    }
-+  scripts\ai-dev-auto-goal.ps1:275:
-+  scripts\ai-dev-auto-goal.ps1:276:    if (-not (Test-HasValue $Queue.goalTitle)) {
-+  scripts\ai-dev-auto-goal.ps1:277:        $errors += "queue.goalTitle must not be empty."
-+  scripts\ai-dev-auto-goal.ps1:278:    }
-+  scripts\ai-dev-auto-goal.ps1:279:
-+  scripts\ai-dev-auto-goal.ps1:280:    if ($Queue.goalSource -ne $goalRelativePath) {
-+  scripts\ai-dev-auto-goal.ps1:281:        $errors += "queue.goalSource must be $goalRelativePath.
-"
-+  scripts\ai-dev-auto-goal.ps1:282:    }
-+  scripts\ai-dev-auto-goal.ps1:283:
-+  scripts\ai-dev-auto-goal.ps1:284:    if ($Queue.tasks -isnot [System.Collections.IEnumerable] -o
-r
-+ $Queue.tasks -is [s
-+tring]) {
-+  scripts\ai-dev-auto-goal.ps1:285:        $errors += "queue.tasks must be an array."
-+  scripts\ai-dev-auto-goal.ps1:286:        return $errors
-+  scripts\ai-dev-auto-goal.ps1:287:    }
-+  scripts\ai-dev-auto-goal.ps1:288:
-+  scripts\ai-dev-auto-goal.ps1:289:    $tasks = @($Queue.tasks)
-+  scripts\ai-dev-auto-goal.ps1:290:
-+  scripts\ai-dev-auto-goal.ps1:291:    if ($tasks.Count -lt 1) {
-+  scripts\ai-dev-auto-goal.ps1:292:        $errors += "queue.tasks must contain at least one task.
-"
-+  scripts\ai-dev-auto-goal.ps1:293:        return $errors
-+  scripts\ai-dev-auto-goal.ps1:294:    }
-+  scripts\ai-dev-auto-goal.ps1:295:
-+  scripts\ai-dev-auto-goal.ps1:296:    if ($tasks.Count -gt 3) {
-+  scripts\ai-dev-auto-goal.ps1:297:        $errors += "queue.tasks must contain no more than three
- 
-+tasks."
-+  scripts\ai-dev-auto-goal.ps1:298:    }
-+  scripts\ai-dev-auto-goal.ps1:299:
-+  scripts\ai-dev-auto-goal.ps1:300:    $taskIds = @()
-+  scripts\ai-dev-auto-goal.ps1:301:
-+  scripts\ai-dev-auto-goal.ps1:302:    foreach ($task in $tasks) {
-+  scripts\ai-dev-auto-goal.ps1:303:        $taskRequiredFields = @("id", "title", "description", "
+-th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern '紐|諛|쒖|꾩|蹂|湲|젣|뺤|⑦'" in 
+D
+-:\ai-apps\planpilot-local
+- succeeded in 2011ms:
+- succeeded in 2149ms:
+-
+-> scripts\ai-dev-auto-goal.ps1:301:        $errors += "queue.currentTaskId must be T001."
+-  scripts\ai-dev-auto-goal.ps1:302:    }
+-  scripts\ai-dev-auto-goal.ps1:303:
+-  scripts\ai-dev-auto-goal.ps1:304:    $taskIds = @()
+-  scripts\ai-dev-auto-goal.ps1:305:
+-  scripts\ai-dev-auto-goal.ps1:306:    foreach ($task in $tasks) {
+-  scripts\ai-dev-auto-goal.ps1:307:        $taskRequiredFields = @("id", "title", "description", "
 t
-+ype", "status", "pri
-+ority", "dependsOn", "filesLikelyToChange", "verification", "commitMessage")
-+  scripts\ai-dev-auto-goal.ps1:304:        $taskMissingFields = @(Get-MissingFields $task $taskReq
+-ype", "status", "pri
+-ority", "dependsOn", "filesLikelyToChange", "verification", "commitMessage")
+-  scripts\ai-dev-auto-goal.ps1:308:        $taskMissingFields = @(Get-MissingFields $task $taskReq
 u
-+iredFields)
-+  scripts\ai-dev-auto-goal.ps1:305:
-+  scripts\ai-dev-auto-goal.ps1:306:        foreach ($field in $taskMissingFields) {
-+  scripts\ai-dev-auto-goal.ps1:307:            $errors += "task missing field: $field"
-+  scripts\ai-dev-auto-goal.ps1:308:        }
-+  scripts\ai-dev-auto-goal.ps1:309:
-+  scripts\ai-dev-auto-goal.ps1:310:        if ($taskMissingFields.Count -gt 0) {
-+  scripts\ai-dev-auto-goal.ps1:311:            continue
-+  scripts\ai-dev-auto-goal.ps1:312:        }
-+  scripts\ai-dev-auto-goal.ps1:313:
-+  scripts\ai-dev-auto-goal.ps1:314:        if (-not (Test-HasValue $task.id)) {
-+  scripts\ai-dev-auto-goal.ps1:315:            $errors += "task.id must not be empty."
-+  scripts\ai-dev-auto-goal.ps1:316:        } else {
-+  scripts\ai-dev-auto-goal.ps1:317:            $taskIds += [string]$task.id
-+  scripts\ai-dev-auto-goal.ps1:318:        }
-+  scripts\ai-dev-auto-goal.ps1:319:
-+  scripts\ai-dev-auto-goal.ps1:320:        if (-not (Test-HasValue $task.title)) {
-+  scripts\ai-dev-auto-goal.ps1:321:            $errors += "task.title must not be empty."
-+  scripts\ai-dev-auto-goal.ps1:322:        }
-+  scripts\ai-dev-auto-goal.ps1:323:
-+  scripts\ai-dev-auto-goal.ps1:324:        if (-not (Test-HasValue $task.description)) {
-+  scripts\ai-dev-auto-goal.ps1:325:            $errors += "task.description must not be empty."
-+  scripts\ai-dev-auto-goal.ps1:326:        }
-+  scripts\ai-dev-auto-goal.ps1:327:
-+  scripts\ai-dev-auto-goal.ps1:328:        if (@("analysis", "implementation", "documentation", "v
+-iredFields)
+-  scripts\ai-dev-auto-goal.ps1:309:
+-  scripts\ai-dev-auto-goal.ps1:310:        foreach ($field in $taskMissingFields) {
+-  scripts\ai-dev-auto-goal.ps1:311:            $errors += "task missing field: $field"
+-  scripts\ai-dev-auto-goal.ps1:312:        }
+-  scripts\ai-dev-auto-goal.ps1:313:
+-  scripts\ai-dev-auto-goal.ps1:314:        if ($taskMissingFields.Count -gt 0) {
+-  scripts\ai-dev-auto-goal.ps1:315:            continue
+-  scripts\ai-dev-auto-goal.ps1:316:        }
+-  scripts\ai-dev-auto-goal.ps1:317:
+-  scripts\ai-dev-auto-goal.ps1:318:        if (-not (Test-HasValue $task.id)) {
+-  scripts\ai-dev-auto-goal.ps1:319:            $errors += "task.id must not be empty."
+-  scripts\ai-dev-auto-goal.ps1:320:        } else {
+-  scripts\ai-dev-auto-goal.ps1:321:            $taskIds += [string]$task.id
+-  scripts\ai-dev-auto-goal.ps1:322:        }
+-  scripts\ai-dev-auto-goal.ps1:323:
+-  scripts\ai-dev-auto-goal.ps1:324:        if (-not (Test-HasValue $task.title)) {
+-  scripts\ai-dev-auto-goal.ps1:325:            $errors += "task.title must not be empty."
+-  scripts\ai-dev-auto-goal.ps1:326:        }
+-  scripts\ai-dev-auto-goal.ps1:327:
+-  scripts\ai-dev-auto-goal.ps1:328:        if (-not (Test-HasValue $task.description)) {
+-  scripts\ai-dev-auto-goal.ps1:329:            $errors += "task.description must not be empty."
+-  scripts\ai-dev-auto-goal.ps1:330:        }
+-  scripts\ai-dev-auto-goal.ps1:331:
+-  scripts\ai-dev-auto-goal.ps1:332:        if (@("analysis", "implementation", "documentation", "v
 e
-+rification") -notcon
-+tains $task.type) {
-+  scripts\ai-dev-auto-goal.ps1:329:            $errors += "invalid task.type: $($task.type)"
-+  scripts\ai-dev-auto-goal.ps1:330:        }
-+  scripts\ai-dev-auto-goal.ps1:331:
-+  scripts\ai-dev-auto-goal.ps1:332:        if (@("pending", "in_progress", "done", "blocked") -not
+-rification") -notcon
+-tains $task.type) {
+-  scripts\ai-dev-auto-goal.ps1:333:            $errors += "invalid task.type: $($task.type)"
+-  scripts\ai-dev-auto-goal.ps1:334:        }
+-  scripts\ai-dev-auto-goal.ps1:335:
+-  scripts\ai-dev-auto-goal.ps1:336:        if (@("pending", "in_progress", "done", "blocked") -not
 c
-+ontains $task.status
-+) {
-+  scripts\ai-dev-auto-goal.ps1:333:            $errors += "invalid task.status: $($task.status)"
-+  scripts\ai-dev-auto-goal.ps1:334:        }
-+  scripts\ai-dev-auto-goal.ps1:335:
-+  scripts\ai-dev-auto-goal.ps1:336:        if (@("P0", "P1", "P2") -notcontains $task.priority) {
-+  scripts\ai-dev-auto-goal.ps1:337:            $errors += "invalid task.priority: $($task.priority
-)
-+"
-+  scripts\ai-dev-auto-goal.ps1:338:        }
-+  scripts\ai-dev-auto-goal.ps1:339:
-+  scripts\ai-dev-auto-goal.ps1:340:        if ($task.dependsOn -isnot [System.Collections.IEnumera
-b
-+le] -or $task.depend
-+sOn -is [string]) {
-+  scripts\ai-dev-auto-goal.ps1:341:            $errors += "task.dependsOn must be an array: $($tas
-k
-+.id)"
-+  scripts\ai-dev-auto-goal.ps1:342:        }
-+> scripts\ai-dev-auto-goal.ps1:419:    $errors += @(Get-QueueValidationErrors $Plan.queue)
-+  scripts\ai-dev-auto-goal.ps1:420:    $errors += @(Get-StateValidationErrors $Plan.state $Plan.qu
-e
-+ue)
-+  scripts\ai-dev-auto-goal.ps1:421:
-+  scripts\ai-dev-auto-goal.ps1:422:    return $errors
-+  scripts\ai-dev-auto-goal.ps1:423:}
-+  scripts\ai-dev-auto-goal.ps1:424:
-+> scripts\ai-dev-auto-goal.ps1:425:function New-DryRunAutoGoalPlan {
-+  scripts\ai-dev-auto-goal.ps1:426:    param(
-+  scripts\ai-dev-auto-goal.ps1:427:        [string]$Title,
-+  scripts\ai-dev-auto-goal.ps1:428:        [string]$Description
-+  scripts\ai-dev-auto-goal.ps1:429:    )
-+  scripts\ai-dev-auto-goal.ps1:430:
-+  scripts\ai-dev-auto-goal.ps1:431:    $now = (Get-Date).ToUniversalTime().ToString("o")
-+  scripts\ai-dev-auto-goal.ps1:432:    $safeCommitTitle = $Title.Trim()
-+  scripts\ai-dev-auto-goal.ps1:433:
-+  scripts\ai-dev-auto-goal.ps1:434:    $plan = [PSCustomObject][ordered]@{
-+  scripts\ai-dev-auto-goal.ps1:435:        goalMarkdown = @"
-+  scripts\ai-dev-auto-goal.ps1:436:# 紐⑺몴
-+  scripts\ai-dev-auto-goal.ps1:437:
-+  scripts\ai-dev-auto-goal.ps1:438:$safeCommitTitle
-+  scripts\ai-dev-auto-goal.ps1:439:
-+  scripts\ai-dev-auto-goal.ps1:440:## 諛곌꼍
-+  scripts\ai-dev-auto-goal.ps1:441:
-+  scripts\ai-dev-auto-goal.ps1:442:$($Description.Trim())
-+  scripts\ai-dev-auto-goal.ps1:443:
-+  scripts\ai-dev-auto-goal.ps1:444:## ?깃났 湲곗?
-+  scripts\ai-dev-auto-goal.ps1:445:
-+  scripts\ai-dev-auto-goal.ps1:446:- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+  scripts\ai-dev-auto-goal.ps1:447:- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+  scripts\ai-dev-auto-goal.ps1:448:- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+  scripts\ai-dev-auto-goal.ps1:449:
-+  scripts\ai-dev-auto-goal.ps1:450:## ?쒖빟?ы빆
-+  scripts\ai-dev-auto-goal.ps1:451:
-+  scripts\ai-dev-auto-goal.ps1:452:- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜
-?
-+섏? ?딅뒗??
-+  scripts\ai-dev-auto-goal.ps1:453:- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+  scripts\ai-dev-auto-goal.ps1:454:
-+  scripts\ai-dev-auto-goal.ps1:455:## ?쒖쇅 踰붿쐞
-+  scripts\ai-dev-auto-goal.ps1:456:
-+  scripts\ai-dev-auto-goal.ps1:457:- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+  scripts\ai-dev-auto-goal.ps1:458:
-+  scripts\ai-dev-auto-goal.ps1:459:## ?섎룞 ?뺤씤
-+  scripts\ai-dev-auto-goal.ps1:460:
-+  scripts\ai-dev-auto-goal.ps1:461:- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+  scripts\ai-dev-auto-goal.ps1:462:- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+  scripts\ai-dev-auto-goal.ps1:463:- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+  scripts\ai-dev-auto-goal.ps1:464:"@
-+  scripts\ai-dev-auto-goal.ps1:465:        queue = [PSCustomObject][ordered]@{
-+  scripts\ai-dev-auto-goal.ps1:466:            goalTitle = $safeCommitTitle
-+  scripts\ai-dev-auto-goal.ps1:467:            goalSource = $goalRelativePath
-+  scripts\ai-dev-auto-goal.ps1:468:            createdAt = $now
-+  scripts\ai-dev-auto-goal.ps1:469:            updatedAt = $now
-+  scripts\ai-dev-auto-goal.ps1:470:            currentTaskId = "T001"
-+  scripts\ai-dev-auto-goal.ps1:471:            tasks = @(
-+  scripts\ai-dev-auto-goal.ps1:472:                [PSCustomObject][ordered]@{
-+  scripts\ai-dev-auto-goal.ps1:473:                    id = "T001"
-+  scripts\ai-dev-auto-goal.ps1:474:                    title = $safeCommitTitle
-+  scripts\ai-dev-auto-goal.ps1:475:                    description = $Description.Trim()
-+  scripts\ai-dev-auto-goal.ps1:476:                    type = "implementation"
-+  scripts\ai-dev-auto-goal.ps1:477:                    status = "in_progress"
-+  scripts\ai-dev-auto-goal.ps1:478:                    priority = "P0"
-+  scripts\ai-dev-auto-goal.ps1:479:                    dependsOn = @()
-+  scripts\ai-dev-auto-goal.ps1:480:                    filesLikelyToChange = @()
-+  scripts\ai-dev-auto-goal.ps1:481:                    verification = @(
-+  scripts\ai-dev-auto-goal.ps1:482:                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+  scripts\ai-dev-auto-goal.ps1:483:                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+  scripts\ai-dev-auto-goal.ps1:484:                    )
-+  scripts\ai-dev-auto-goal.ps1:485:                    commitMessage = $null
-+  scripts\ai-dev-auto-goal.ps1:486:                }
-+  scripts\ai-dev-auto-goal.ps1:487:            )
-+  scripts\ai-dev-auto-goal.ps1:488:        }
-+  scripts\ai-dev-auto-goal.ps1:489:        state = [PSCustomObject][ordered]@{
-+  scripts\ai-dev-auto-goal.ps1:490:            goalStatus = "in_progress"
-+  scripts\ai-dev-auto-goal.ps1:491:            currentTaskId = "T001"
-+  scripts\ai-dev-auto-goal.ps1:492:            currentLoop = 0
-+  scripts\ai-dev-auto-goal.ps1:493:            maxLoopsPerTask = 2
-+  scripts\ai-dev-auto-goal.ps1:494:            repeatedFailureCount = 0
-+  scripts\ai-dev-auto-goal.ps1:495:            lastCommand = $null
-+  scripts\ai-dev-auto-goal.ps1:496:            lastCommandStatus = "not_started"
-+  scripts\ai-dev-auto-goal.ps1:497:            lastErrorSummary = $null
-+  scripts\ai-dev-auto-goal.ps1:498:            lastReviewDecision = "not_started"
-+  scripts\ai-dev-auto-goal.ps1:499:            lastReviewSeverity = $null
-+  scripts\ai-dev-auto-goal.ps1:500:            lastCommitHash = $null
-+  scripts\ai-dev-auto-goal.ps1:501:            startedAt = $now
-+  scripts\ai-dev-auto-goal.ps1:502:            updatedAt = $now
-+  scripts\ai-dev-auto-goal.ps1:503:            stopReason = $null
-+  scripts\ai-dev-auto-goal.ps1:504:        }
-+  scripts\ai-dev-auto-goal.ps1:505:    }
-+> scripts\ai-dev-auto-goal.ps1:596:- currentTaskId must be "T001".
-+  scripts\ai-dev-auto-goal.ps1:597:- tasks must contain one to three tasks.
-+  scripts\ai-dev-auto-goal.ps1:598:- T001 must be status "in_progress"; later tasks, if any, must 
-b
-+e "pending".
-+  scripts\ai-dev-auto-goal.ps1:599:- each task must include id, title, description, type, status, 
-p
-+riority, dependsOn, 
-+filesLikelyToChange, verification, commitMessage.
-+  scripts\ai-dev-auto-goal.ps1:600:- type must be one of analysis, implementation, documentation, 
-v
-+erification.
-+  scripts\ai-dev-auto-goal.ps1:601:- priority must be P0, P1, or P2.
-+  scripts\ai-dev-auto-goal.ps1:602:- dependsOn, filesLikelyToChange, and verification must be arra
-y
-+s.
-+  scripts\ai-dev-auto-goal.ps1:603:- commitMessage must be null or a short English commit message.
-+  scripts\ai-dev-auto-goal.ps1:604:
-+  scripts\ai-dev-auto-goal.ps1:605:state rules:
-+  scripts\ai-dev-auto-goal.ps1:606:- goalStatus must be "in_progress".
-+> scripts\ai-dev-auto-goal.ps1:607:- currentTaskId must be "T001".
-+  scripts\ai-dev-auto-goal.ps1:608:- currentLoop must be 0.
-+  scripts\ai-dev-auto-goal.ps1:609:- maxLoopsPerTask must be 2.
-+  scripts\ai-dev-auto-goal.ps1:610:- repeatedFailureCount must be 0.
-+  scripts\ai-dev-auto-goal.ps1:611:- lastCommand must be null.
-+  scripts\ai-dev-auto-goal.ps1:612:- lastCommandStatus must be "not_started".
-+  scripts\ai-dev-auto-goal.ps1:613:- lastErrorSummary must be null.
-+  scripts\ai-dev-auto-goal.ps1:614:- lastReviewDecision must be "not_started".
-+  scripts\ai-dev-auto-goal.ps1:615:- lastReviewSeverity must be null.
-+  scripts\ai-dev-auto-goal.ps1:616:- lastCommitHash must be null.
-+  scripts\ai-dev-auto-goal.ps1:617:- startedAt and updatedAt must be ISO 8601 strings.
-+  scripts\ai-dev-auto-goal.ps1:618:- stopReason must be null.
-+  scripts\ai-dev-auto-goal.ps1:619:
-+  scripts\ai-dev-auto-goal.ps1:620:Safety rules:
-+  scripts\ai-dev-auto-goal.ps1:621:- Do not mention server APIs, login, cloud sync, npm install, g
-i
-+t reset, git clean, 
-+git push, DB deletion, or broad rewrites as implementation steps.
-+  scripts\ai-dev-auto-goal.ps1:622:- Prefer one small implementation task when the goal is small.
-+  scripts\ai-dev-auto-goal.ps1:623:- Use Korean for user-facing task titles, descriptions, verific
-a
-+tion, and goalMarkdo
-+wn.
-+  scripts\ai-dev-auto-goal.ps1:624:"@
-+  scripts\ai-dev-auto-goal.ps1:625:}
-+  scripts\ai-dev-auto-goal.ps1:626:
-+  scripts\ai-dev-auto-goal.ps1:627:function Invoke-CycleCommand {
-+  scripts\ai-dev-auto-goal.ps1:628:    param(
-+  scripts\ai-dev-auto-goal.ps1:629:        [int]$StepNumber,
-+  scripts\ai-dev-auto-goal.ps1:630:        [string]$Name,
-+  scripts\ai-dev-auto-goal.ps1:631:        [string]$Command,
-+  scripts\ai-dev-auto-goal.ps1:632:        [string]$ScriptPath,
-+  scripts\ai-dev-auto-goal.ps1:633:        [string[]]$Arguments
-+  scripts\ai-dev-auto-goal.ps1:634:    )
-+  scripts\ai-dev-auto-goal.ps1:635:
-+  scripts\ai-dev-auto-goal.ps1:636:    if ($DryRun) {
-+  scripts\ai-dev-auto-goal.ps1:637:        $script:steps += New-StepResult $StepNumber $Name $Comm
-a
-+nd $false $true 0 "D
-+ryRun: child script was not executed."
-+  scripts\ai-dev-auto-goal.ps1:638:        return
-+  scripts\ai-dev-auto-goal.ps1:639:    }
-+  scripts\ai-dev-auto-goal.ps1:640:
-+  scripts\ai-dev-auto-goal.ps1:641:    $output = & powershell -ExecutionPolicy Bypass -File $Scrip
-t
-+Path @Arguments 2>&1
-+ | Out-String
-+  scripts\ai-dev-auto-goal.ps1:642:    $exitCode = $LASTEXITCODE
-+  scripts\ai-dev-auto-goal.ps1:643:    $message = $output.Trim()
-+  scripts\ai-dev-auto-goal.ps1:644:
-+  scripts\ai-dev-auto-goal.ps1:645:    if ([string]::IsNullOrWhiteSpace($message)) {
-+  scripts\ai-dev-auto-goal.ps1:646:        $message = "completed"
-+  scripts\ai-dev-auto-goal.ps1:647:    }
-+  scripts\ai-dev-auto-goal.ps1:648:
-+  scripts\ai-dev-auto-goal.ps1:649:    $script:steps += New-StepResult $StepNumber $Name $Command 
-$
-+true $false $exitCod
-+e $message
-+  scripts\ai-dev-auto-goal.ps1:650:
-+  scripts\ai-dev-auto-goal.ps1:651:    if ($exitCode -ne 0) {
-+  scripts\ai-dev-auto-goal.ps1:652:        Stop-AutoGoal $script:steps "$Name`_failed" $false 1
-+  scripts\ai-dev-auto-goal.ps1:653:    }
-+  scripts\ai-dev-auto-goal.ps1:654:}
-+  scripts\ai-dev-auto-goal.ps1:655:
-+  scripts\ai-dev-auto-goal.ps1:656:function Get-FullCycleArguments {
-+  scripts\ai-dev-auto-goal.ps1:657:    $arguments = @("-MaxTasks", ([string]$MaxTasks), "-MaxSteps
+-ontains $task.status
+-) {
+-> scripts\ai-dev-auto-goal.ps1:371:        $errors += "T001 must be in_progress."
+-  scripts\ai-dev-auto-goal.ps1:372:    }
+-  scripts\ai-dev-auto-goal.ps1:373:
+-  scripts\ai-dev-auto-goal.ps1:374:    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne 
 "
-+, ([string]$MaxSteps
-+))
-+  scripts\ai-dev-auto-goal.ps1:658:
-+  scripts\ai-dev-auto-goal.ps1:659:    if ($AllowCodex) {
-+  scripts\ai-dev-auto-goal.ps1:660:        $arguments += "-AllowCodex"
-+  scripts\ai-dev-auto-goal.ps1:661:    }
-+  scripts\ai-dev-auto-goal.ps1:662:
-+  scripts\ai-dev-auto-goal.ps1:663:    if ($AllowReviewCodex) {
-+  scripts\ai-dev-auto-goal.ps1:664:        $arguments += "-AllowReviewCodex"
-+  scripts\ai-dev-auto-goal.ps1:665:    }
-+  scripts\ai-dev-auto-goal.ps1:666:
-+  scripts\ai-dev-auto-goal.ps1:667:    if ($AllowCommit) {
-+  scripts\ai-dev-auto-goal.ps1:668:        $arguments += "-AllowCommit"
-+  scripts\ai-dev-auto-goal.ps1:669:    }
-+  scripts\ai-dev-auto-goal.ps1:670:
-+  scripts\ai-dev-auto-goal.ps1:671:    # auto-goal writes goal/queue/state/current prompt/result f
-i
-+les after the initia
-+l dirty gate.
-+  scripts\ai-dev-auto-goal.ps1:672:    # The downstream full cycle must tolerate those intended ar
-t
-+ifacts.
-+  scripts\ai-dev-auto-goal.ps1:673:    $arguments += "-AllowDirty"
-+  scripts\ai-dev-auto-goal.ps1:674:
-+  scripts\ai-dev-auto-goal.ps1:675:    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+  scripts\ai-dev-auto-goal.ps1:676:        $normalizedFiles = @($CommitFiles | ForEach-Object { $_
- 
-+-split "," } | Where
-+-Object { -not [string]::IsNullOrWhiteSpace($_) })
-+  scripts\ai-dev-auto-goal.ps1:677:
-+  scripts\ai-dev-auto-goal.ps1:678:        if ($normalizedFiles.Count -gt 0) {
-+  scripts\ai-dev-auto-goal.ps1:679:            $arguments += "-CommitFiles"
-+  scripts\ai-dev-auto-goal.ps1:680:            $arguments += ($normalizedFiles -join ",")
-+  scripts\ai-dev-auto-goal.ps1:681:        }
-+  scripts\ai-dev-auto-goal.ps1:682:    }
-+  scripts\ai-dev-auto-goal.ps1:683:
-+  scripts\ai-dev-auto-goal.ps1:684:    return $arguments
-+  scripts\ai-dev-auto-goal.ps1:685:}
-+  scripts\ai-dev-auto-goal.ps1:686:
-+  scripts\ai-dev-auto-goal.ps1:687:Set-Location $repoRoot
-+> scripts\ai-dev-auto-goal.ps1:733:    $script:autoGoalPlanPreview = New-DryRunAutoGoalPlan $GoalT
-i
-+tle.Trim() $GoalDesc
-+ription.Trim()
-+  scripts\ai-dev-auto-goal.ps1:734:    $previewValidationErrors = @(Get-AutoGoalValidationErrors $
+-T001" })) {
+-  scripts\ai-dev-auto-goal.ps1:375:        if ($laterTask.status -ne "pending") {
+-  scripts\ai-dev-auto-goal.ps1:376:            $errors += "later tasks must be pending: $($laterTa
 s
-+cript:autoGoalPlanPr
-+eview)
-+  scripts\ai-dev-auto-goal.ps1:735:
-+  scripts\ai-dev-auto-goal.ps1:736:    if ($previewValidationErrors.Count -gt 0) {
-+  scripts\ai-dev-auto-goal.ps1:737:        $script:steps += New-StepResult 2 "preview-plan" "local
- 
-+dry-run plan preview
-+" $false $false 1 "DryRun preview plan validation failed: $($previewValidationErrors -join '; ')"
-+  scripts\ai-dev-auto-goal.ps1:738:        Stop-AutoGoal $script:steps "dry_run_preview_invalid" $
-f
-+alse 1
-+  scripts\ai-dev-auto-goal.ps1:739:    }
-+  scripts\ai-dev-auto-goal.ps1:740:
-+  scripts\ai-dev-auto-goal.ps1:741:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
-t
-+ status --short" $fa
-+lse $true 0 "DryRun: dirty worktree gate was not executed."
-+  scripts\ai-dev-auto-goal.ps1:742:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <
-a
-+uto-goal planning pr
-+ompt>" $false $true 0 "DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, 
-t
-+ask: $GoalTitle"
-+  scripts\ai-dev-auto-goal.ps1:743:    $script:steps += New-StepResult 4 "validate-generated-json"
- 
-+"goal/queue/state JS
-+ON validation" $false $true 0 "DryRun: preview goal/queue/state plan passed local schema validatio
-n
-+."
-+  scripts\ai-dev-auto-goal.ps1:744:    $script:steps += New-StepResult 5 "write-state-files" "$goa
-l
-+RelativePath, $queue
-+RelativePath, $stateRelativePath" $false $true 0 "DryRun: state files were not written."
-+  scripts\ai-dev-auto-goal.ps1:745:    $script:steps += New-StepResult 6 "make-prompt" "powershell
- 
-+-ExecutionPolicy Byp
-+ass -File scripts/ai-dev-make-prompt.ps1" $false $true 0 "DryRun: current-task-prompt.md was not g
-e
-+nerated."
-+  scripts\ai-dev-auto-goal.ps1:746:
-+  scripts\ai-dev-auto-goal.ps1:747:    if ($shouldRunFullCycle) {
-+  scripts\ai-dev-auto-goal.ps1:748:        $script:steps += New-StepResult 7 "auto-cycle-full" $fu
-l
-+lCycleCommandText $f
-+alse $true 0 "DryRun: explicit run option is present, but full cycle was not executed. -AllowRun o
+-k.id)"
+-  scripts\ai-dev-auto-goal.ps1:377:        }
+-  scripts\ai-dev-auto-goal.ps1:378:    }
+-  scripts\ai-dev-auto-goal.ps1:379:
+-  scripts\ai-dev-auto-goal.ps1:380:    return $errors
+-  scripts\ai-dev-auto-goal.ps1:381:}
+-  scripts\ai-dev-auto-goal.ps1:382:
+-  scripts\ai-dev-auto-goal.ps1:383:function Get-StateValidationErrors {
+-  scripts\ai-dev-auto-goal.ps1:384:    param(
+-  scripts\ai-dev-auto-goal.ps1:385:        [object]$State,
+-  scripts\ai-dev-auto-goal.ps1:386:        [object]$Queue
+-  scripts\ai-dev-auto-goal.ps1:387:    )
+-  scripts\ai-dev-auto-goal.ps1:388:
+-  scripts\ai-dev-auto-goal.ps1:389:    $errors = @()
+-  scripts\ai-dev-auto-goal.ps1:390:    $requiredFields = @("goalStatus", "currentTaskId", "current
+L
+-oop", "maxLoopsPerTa
+-sk", "repeatedFailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDe
+c
+-ision", "lastReviewS
+-everity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
+-  scripts\ai-dev-auto-goal.ps1:391:
+-  scripts\ai-dev-auto-goal.ps1:392:    foreach ($field in @(Get-MissingFields $State $requiredFiel
+d
+-s)) {
+-  scripts\ai-dev-auto-goal.ps1:393:        $errors += "state missing field: $field"
+-  scripts\ai-dev-auto-goal.ps1:394:    }
+-  scripts\ai-dev-auto-goal.ps1:395:
+-  scripts\ai-dev-auto-goal.ps1:396:    if ($errors.Count -gt 0) {
+-  scripts\ai-dev-auto-goal.ps1:397:        return $errors
+-  scripts\ai-dev-auto-goal.ps1:398:    }
+-  scripts\ai-dev-auto-goal.ps1:399:
+-  scripts\ai-dev-auto-goal.ps1:400:    if (@("idle", "in_progress", "completed", "blocked") -notco
 n
-+ly invokes the full-
-+cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-+  scripts\ai-dev-auto-goal.ps1:749:    } else {
-+  scripts\ai-dev-auto-goal.ps1:750:        $script:steps += New-StepResult 7 "auto-cycle-full" $fu
-l
-+lCycleCommandText $f
-+alse $true 0 "DryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only 
-i
-+nvokes the full-cycl
-+e wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-+  scripts\ai-dev-auto-goal.ps1:751:    }
-+  scripts\ai-dev-auto-goal.ps1:752:
-+  scripts\ai-dev-auto-goal.ps1:753:    Stop-AutoGoal $script:steps "dry_run" $false 0
-+  scripts\ai-dev-auto-goal.ps1:754:}
-+  scripts\ai-dev-auto-goal.ps1:755:
-+  scripts\ai-dev-auto-goal.ps1:756:$statusLines = @(Get-GitStatusLines)
-+  scripts\ai-dev-auto-goal.ps1:757:
-+  scripts\ai-dev-auto-goal.ps1:758:if ($statusLines.Count -gt 0 -and -not $AllowDirty) {
-+  scripts\ai-dev-auto-goal.ps1:759:    $dirtyText = ($statusLines -join "`n")
-+  scripts\ai-dev-auto-goal.ps1:760:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
-t
-+ status --short" $fa
-+lse $true 1 "Worktree is dirty. Use -AllowDirty only when this is intentional.`n$dirtyText"
-+  scripts\ai-dev-auto-goal.ps1:761:    Stop-AutoGoal $script:steps "dirty_worktree" $false 1
-+  scripts\ai-dev-auto-goal.ps1:762:}
-+  scripts\ai-dev-auto-goal.ps1:763:
-+  scripts\ai-dev-auto-goal.ps1:764:if ($statusLines.Count -gt 0) {
-+  scripts\ai-dev-auto-goal.ps1:765:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
-t
-+ status --short" $fa
-+lse $false 0 "AllowDirty is set. DirtyCount: $($statusLines.Count)"
-+  scripts\ai-dev-auto-goal.ps1:766:} else {
-+  scripts\ai-dev-auto-goal.ps1:767:    $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
-t
-+ status --short" $fa
-+lse $false 0 "Worktree is clean."
-+  scripts\ai-dev-auto-goal.ps1:768:}
-+  scripts\ai-dev-auto-goal.ps1:769:
-+  scripts\ai-dev-auto-goal.ps1:770:$codexCommand = Get-Command codex -ErrorAction SilentlyContinue
-+  scripts\ai-dev-auto-goal.ps1:771:
-+  scripts\ai-dev-auto-goal.ps1:772:if ($null -eq $codexCommand) {
-+  scripts\ai-dev-auto-goal.ps1:773:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <
-a
-+uto-goal planning pr
-+ompt>" $false $false 1 "Codex CLI was not found."
-+  scripts\ai-dev-auto-goal.ps1:774:    Stop-AutoGoal $script:steps "codex_not_found" $false 1
-+  scripts\ai-dev-auto-goal.ps1:775:}
-+  scripts\ai-dev-auto-goal.ps1:776:
-+  scripts\ai-dev-auto-goal.ps1:777:$plannerPrompt = New-CodexAutoGoalPrompt $GoalTitle.Trim() $Goa
-l
-+Description.Trim()
-+  scripts\ai-dev-auto-goal.ps1:778:$startedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-+  scripts\ai-dev-auto-goal.ps1:779:$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-+  scripts\ai-dev-auto-goal.ps1:780:$codexExitCode = $LASTEXITCODE
-+  scripts\ai-dev-auto-goal.ps1:781:$endedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-+  scripts\ai-dev-auto-goal.ps1:782:
-+  scripts\ai-dev-auto-goal.ps1:783:$resultContent = @"
-+  scripts\ai-dev-auto-goal.ps1:784:# Codex Auto Goal Result
-+  scripts\ai-dev-auto-goal.ps1:785:
-+  scripts\ai-dev-auto-goal.ps1:786:## Run
-+  scripts\ai-dev-auto-goal.ps1:787:
-+  scripts\ai-dev-auto-goal.ps1:788:- Started at: $startedAt
-+  scripts\ai-dev-auto-goal.ps1:789:- Ended at: $endedAt
-+  scripts\ai-dev-auto-goal.ps1:790:- Exit code: $codexExitCode
-+  scripts\ai-dev-auto-goal.ps1:791:- Goal title: $($GoalTitle.Trim())
-+  scripts\ai-dev-auto-goal.ps1:792:- Command: codex exec <auto-goal planning prompt>
-+  scripts\ai-dev-auto-goal.ps1:793:
-+  scripts\ai-dev-auto-goal.ps1:794:## Output
-+  scripts\ai-dev-auto-goal.ps1:795:
-+  scripts\ai-dev-auto-goal.ps1:796:${codeFence}text
-+  scripts\ai-dev-auto-goal.ps1:797:$codexOutput
-+  scripts\ai-dev-auto-goal.ps1:798:$codeFence
-+  scripts\ai-dev-auto-goal.ps1:799:"@
-+  scripts\ai-dev-auto-goal.ps1:800:
-+  scripts\ai-dev-auto-goal.ps1:801:[System.IO.File]::WriteAllText($resolvedResultPath, $resultCont
-e
-+nt, $utf8WithBom)
-+  scripts\ai-dev-auto-goal.ps1:802:
-+  scripts\ai-dev-auto-goal.ps1:803:if ($codexExitCode -ne 0) {
-+  scripts\ai-dev-auto-goal.ps1:804:    $script:steps += New-StepResult 3 "plan-goal" "codex exec <
-a
-+uto-goal planning pr
-+ompt>" $true $false 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativePath $ResultPat
-h
- )"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+  scripts\ai-dev-auto-goal.ps1:805:    Stop-AutoGoal $script:steps "codex_plan_failed" $false 1
-+  scripts\ai-dev-auto-goal.ps1:806:}
-+  scripts\ai-dev-auto-goal.ps1:807:
-+  scripts\ai-dev-auto-goal.ps1:808:$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto
--
-+goal planning prompt
-+>" $true $false 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativePath $ResultPath
-)
-+"
-+  scripts\ai-dev-auto-goal.ps1:809:
-+  scripts\ai-dev-auto-goal.ps1:810:try {
-+  scripts\ai-dev-auto-goal.ps1:811:    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutpu
-t
-+  scripts\ai-dev-auto-goal.ps1:812:} catch {
-+  scripts\ai-dev-auto-goal.ps1:813:    $preview = Get-InputPreview $codexOutput
-+
-+
-+
-+ succeeded in 2311ms:
-+
-+> .ai-dev\README.md:750:### 목표 입력 기반 자동 실행
-+  .ai-dev\README.md:751:
-+  .ai-dev\README.md:752:최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게
- 
-+하는 것이다.
-+  .ai-dev\README.md:753:
-+  .ai-dev\README.md:754:먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-+  .ai-dev\README.md:755:
-+  .ai-dev\README.md:756:```powershell
-+  .ai-dev\README.md:757:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
-o
-+alTitle "업무 검색 결과 하이
-+라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-+  .ai-dev\README.md:758:```
-+  .ai-dev\README.md:759:
-+  .ai-dev\README.md:760:자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-+  .ai-dev\README.md:761:DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 
-파
-+일 작성 전에 생성될 goal/tas
-+k 계획을 검토할 수 있다.
-+  .ai-dev\README.md:762:
-+  .ai-dev\README.md:763:```powershell
-+  .ai-dev\README.md:764:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
-o
-+alTitle "업무 검색 결과 하이
-+라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-+  .ai-dev\README.md:765:```
-+  .ai-dev\README.md:766:
-+  .ai-dev\README.md:767:실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이
- 
-+모드는 `ai-dev-auto-cyc
-+le-full.ps1`을 호출하지 않는다.
-+  .ai-dev\README.md:768:
-+  .ai-dev\README.md:769:```powershell
-+  .ai-dev\README.md:770:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
-o
-+alTitle "업무 검색 결과 하이
-+라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-+  .ai-dev\README.md:771:```
-+  .ai-dev\README.md:772:
-+  .ai-dev\README.md:773:생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowRe
-v
-+iewCodex`, `-AllowCo
-+mmit` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다.
- 
-+실제 구현까지 허용하려면 `-Allo
-+wCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하게 `-AllowCodex -
-A
-+llowReviewCodex -All
-+owCommit`을 함께 지정한다.
-+  .ai-dev\README.md:774:
-+  .ai-dev\README.md:775:```powershell
-+  .ai-dev\README.md:776:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -G
-o
-+alTitle "업무 검색 결과 하이
-+라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -
-A
-+llowCommit -MaxTasks
-+ 1
-+  .ai-dev\README.md:777:```
-+  .ai-dev\README.md:778:
-+  .ai-dev\README.md:779:동작:
-+  .ai-dev\README.md:780:
-+  .ai-dev\README.md:781:1. `GoalTitle`과 `GoalDescription`을 검증한다.
-+  .ai-dev\README.md:782:2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
-+  .ai-dev\README.md:783:3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-+  .ai-dev\README.md:784:4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다
-.
-+  .ai-dev\README.md:785:5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
-+  .ai-dev\README.md:786:6. `.ai-dev/current-task-prompt.md`를 생성한다.
-+  .ai-dev\README.md:787:7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-+  .ai-dev\README.md:788:8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-
+-tains $State.goalSta
+-tus) {
+-  scripts\ai-dev-auto-goal.ps1:401:        $errors += "invalid state.goalStatus: $($State.goalStat
+u
+-s)"
+-  scripts\ai-dev-auto-goal.ps1:402:    }
+-  scripts\ai-dev-auto-goal.ps1:403:
+-  scripts\ai-dev-auto-goal.ps1:404:    if ($State.currentTaskId -ne $Queue.currentTaskId) {
+-  scripts\ai-dev-auto-goal.ps1:405:        $errors += "state.currentTaskId must match queue.curren
 t
-+ask-prompt.md`, 결과 파
-+일 때문에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트
-는
-+ auto-goal 시작 전에 별도로
-+ 유지한다.
-+  .ai-dev\README.md:789:
-+  .ai-dev\README.md:790:안전 기준:
-+  .ai-dev\README.md:791:
-+  .ai-dev\README.md:792:- `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
-+  .ai-dev\README.md:793:- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
-+  .ai-dev\README.md:794:- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-+  .ai-dev\README.md:795:- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 
-않
-+는다.
-+  .ai-dev\README.md:796:- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowC
-o
-+mmit` 중 하나가 있을 때만 수행
-+한다.
-+  .ai-dev\README.md:797:- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCod
-e
-+x`, `-AllowReviewCod
-+ex`, `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
-+  .ai-dev\README.md:798:- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커
-밋
-+하지 않는다.
-+  .ai-dev\README.md:799:- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
-+  .ai-dev\README.md:800:
-+  .ai-dev\README.md:801:## 운영 원칙
-+  .ai-dev\README.md:802:
-+  .ai-dev\README.md:803:- 저장소의 `AGENTS.md`, 사용자 지시, 보안 정책이 자동 개발 루프보다 우선한다.
-+  .ai-dev\README.md:804:- 한 번에 하나의 task만 수행한다.
-+  .ai-dev\README.md:805:- task 단위로 구현, 검증, 리뷰, 커밋한다.
-+  .ai-dev\README.md:806:- 현재 task 범위 밖 파일을 임의로 수정하지 않는다.
-+  .ai-dev\README.md:807:- 검증하지 않은 결과를 통과로 기록하지 않는다.
-+  .ai-dev\README.md:808:- 실제 DB 삭제, 초기화, 복원, 대규모 리팩터링은 초기 실험 범위에서 제외한다.
-+  .ai-dev\README.md:809:
-+  .ai-dev\README.md:810:## 다음 단계
-+  .ai-dev\README.md:811:
-+  .ai-dev\README.md:812:실제 자동 개발 루프 실행기와 상태 갱신 로직은 별도 정책 검토 후 추가한다.
-+  .ai-dev\README.md:813:
-+  .ai-dev\README.md:814:세부 기준은 `docs/ai-dev-loop-policy.md`를 따른다.
-+
-+
- 
- codex
--요구사항의 “선택 파일 커밋”을 더 엄격히 해석하는 편이 안전합니다. full cycle에서 `-AllowCommit`만으로 전체 변경을 커밋하지 않고, `-CommitFile
--s`가 지정된 경우에만 커밋 스크립트로 넘기도록 좁히겠습니다.
--apply patch
--patch: completed
--D:\ai-apps\planpilot-local\scripts\ai-dev-auto-cycle-full.ps1
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..1bdb85799839984c1deb03a6eba84a23cfc01093
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
+-TaskId."
+-  scripts\ai-dev-auto-goal.ps1:406:    }
+-> scripts\ai-dev-auto-goal.ps1:446:function New-DryRunAutoGoalPlan {
+-  scripts\ai-dev-auto-goal.ps1:447:    param(
+-  scripts\ai-dev-auto-goal.ps1:448:        [string]$Title,
+-  scripts\ai-dev-auto-goal.ps1:449:        [string]$Description
+-  scripts\ai-dev-auto-goal.ps1:450:    )
+-  scripts\ai-dev-auto-goal.ps1:451:
+-  scripts\ai-dev-auto-goal.ps1:452:    $now = (Get-Date).ToUniversalTime().ToString("o")
+-  scripts\ai-dev-auto-goal.ps1:453:    $safeTitle = $Title.Trim()
+-  scripts\ai-dev-auto-goal.ps1:454:    $safeDescription = $Description.Trim()
+-  scripts\ai-dev-auto-goal.ps1:455:
+-  scripts\ai-dev-auto-goal.ps1:456:    $plan = [PSCustomObject][ordered]@{
+-  scripts\ai-dev-auto-goal.ps1:457:        goalMarkdown = @"
+-  scripts\ai-dev-auto-goal.ps1:458:# 목표
+-  scripts\ai-dev-auto-goal.ps1:459:
+-  scripts\ai-dev-auto-goal.ps1:460:$safeTitle
+-  scripts\ai-dev-auto-goal.ps1:461:
+-  scripts\ai-dev-auto-goal.ps1:462:## 배경
+-  scripts\ai-dev-auto-goal.ps1:463:
+-  scripts\ai-dev-auto-goal.ps1:464:$safeDescription
+-  scripts\ai-dev-auto-goal.ps1:465:
+-  scripts\ai-dev-auto-goal.ps1:466:## 성공 기준
+-  scripts\ai-dev-auto-goal.ps1:467:
+-  scripts\ai-dev-auto-goal.ps1:468:- 입력한 목표에서 현재 작업을 준비한다.
+-  scripts\ai-dev-auto-goal.ps1:469:- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
+-  scripts\ai-dev-auto-goal.ps1:470:- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
+-  scripts\ai-dev-auto-goal.ps1:471:
+-  scripts\ai-dev-auto-goal.ps1:472:## 제약사항
+-  scripts\ai-dev-auto-goal.ps1:473:
+-  scripts\ai-dev-auto-goal.ps1:474:- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
+-  scripts\ai-dev-auto-goal.ps1:475:- 저장소 정책과 현재 작업 범위를 따른다.
+-  scripts\ai-dev-auto-goal.ps1:476:
+-  scripts\ai-dev-auto-goal.ps1:477:## 범위 제외
+-  scripts\ai-dev-auto-goal.ps1:478:
+-  scripts\ai-dev-auto-goal.ps1:479:- 패키지 변경
+-  scripts\ai-dev-auto-goal.ps1:480:- DB 삭제 또는 초기화
+-  scripts\ai-dev-auto-goal.ps1:481:- 광범위한 리팩터링
+-
+-
+-
+-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
+-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
+---- a/scripts/ai-dev-auto-goal.ps1
+-+++ b/scripts/ai-dev-auto-goal.ps1
+-@@ -297,6 +297,10 @@
+-         $errors += "queue.tasks must contain no more than three tasks."
+-     }
 - 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
+-+    if ($Queue.currentTaskId -ne "T001") {
+-+        $errors += "queue.currentTaskId must be T001."
+-+    }
+-+
+-     $taskIds = @()
 - 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
+-     foreach ($task in $tasks) {
+-@@ -360,6 +364,19 @@
+-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-     }
 - 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
+-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
+-+    if ($null -eq $firstTask) {
+-+        $errors += "queue.tasks must include T001."
+-+    } elseif ($firstTask.status -ne "in_progress") {
+-+        $errors += "T001 must be in_progress."
+-+    }
 -+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
+-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
+-+        if ($laterTask.status -ne "pending") {
+-+            $errors += "later tasks must be pending: $($laterTask.id)"
+-+        }
+-+    }
 -+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..effe1a6dc47f7fb626e50e31b78fca6d3085ab76
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
+-     return $errors
 - }
 - 
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
+-@@ -388,6 +405,10 @@
+-         $errors += "state.currentTaskId must match queue.currentTaskId."
+-     }
 - 
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
+-+    if ($State.currentTaskId -ne "T001") {
+-+        $errors += "state.currentTaskId must be T001."
 -+    }
 -+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
+-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
+-     }
+-@@ -429,41 +450,44 @@
+-     )
+- 
+-     $now = (Get-Date).ToUniversalTime().ToString("o")
+--    $safeCommitTitle = $Title.Trim()
+-+    $safeTitle = $Title.Trim()
+-+    $safeDescription = $Description.Trim()
++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'rg -n "placeholder|task
+S
++earchQuery|onTaskSearchQueryChange" src/views/TasksView.tsx' in D:\ai-apps\planpilot-local
++ succeeded in 1678ms:
++14:  taskSearchQuery: string;
++28:  onTaskSearchQueryChange: (value: string) => void;
++58:  taskSearchQuery,
++72:  onTaskSearchQueryChange,
++94:  const hasSearchQuery = taskSearchQuery.trim().length > 0;
++113:          value={taskSearchQuery}
++114:          onChange={(event) => onTaskSearchQueryChange(event.target.value)}
++115:          placeholder="업무 제목이나 메모로 검색"
++
++diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
++index f2f7fd812594455482fd78512b1a830e5d5dfe7a..7167cf7befdbe0130902ab6d86eb19a9eab5b300
++--- a/src/views/TasksView.tsx
+++++ b/src/views/TasksView.tsx
++@@ -112,7 +112,7 @@
++           type="text"
++           value={taskSearchQuery}
++           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
++-          placeholder="업무 제목 검색"
+++          placeholder="업무 제목이나 메모로 검색"
++         />
++       </label>
+  
+-     $plan = [PSCustomObject][ordered]@{
+-         goalMarkdown = @"
+--# 紐⑺몴
+-+# 목표
+- 
+--$safeCommitTitle
+-+$safeTitle
+- 
+--## 諛곌꼍
+-+## 배경
+- 
+--$($Description.Trim())
+-+$safeDescription
+- 
+--## ?깃났 湲곗?
+-+## 성공 기준
+- 
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-+- 입력한 목표에서 현재 작업을 준비한다.
+-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
+-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
+- 
+--## ?쒖빟?ы빆
+-+## 제약사항
+- 
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
+-+- 저장소 정책과 현재 작업 범위를 따른다.
+- 
+--## ?쒖쇅 踰붿쐞
+-+## 범위 제외
+- 
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-+- 패키지 변경
+-+- DB 삭제 또는 초기화
+-+- 광범위한 리팩터링
+- 
+--## ?섎룞 ?뺤씤
+-+## 수동 검증
+- 
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-+- DryRun 계획과 JSON 출력을 검토한다.
+-+- 현재 작업 프롬프트 생성 여부를 확인한다.
+-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
+- "@
+-         queue = [PSCustomObject][ordered]@{
+--            goalTitle = $safeCommitTitle
+-+            goalTitle = $safeTitle
+-             goalSource = $goalRelativePath
+-             createdAt = $now
+-             updatedAt = $now
+-@@ -471,16 +495,16 @@
+-             tasks = @(
+-                 [PSCustomObject][ordered]@{
+-                     id = "T001"
+--                    title = $safeCommitTitle
+--                    description = $Description.Trim()
+-+                    title = $safeTitle
+-+                    description = $safeDescription
+-                     type = "implementation"
+-                     status = "in_progress"
+-                     priority = "P0"
+-                     dependsOn = @()
+-                     filesLikelyToChange = @()
+-                     verification = @(
+--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
+-+                        "필요한 경우 npm run build를 별도로 실행한다."
+-                     )
+-                     commitMessage = $null
+-                 }
+-@@ -503,43 +527,6 @@
+-             stopReason = $null
+-         }
+-     }
+--
+--    $plan.goalMarkdown = @"
+--# 紐⑺몴
+--
+--$safeCommitTitle
+--
+--## 諛곌꼍
+--
+--$($Description.Trim())
+--
+--## ?깃났 湲곗?
+--
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+--
+--## ?쒖빟?ы빆
+--
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+--
+--## ?쒖쇅 踰붿쐞
+--
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+--
+--## ?섎룞 ?뺤씤
+--
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+--"@
+--
+--    $dryRunTask = @($plan.queue.tasks)[0]
+--    $dryRunTask.verification = @(
+--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+--    )
+- 
+-     return $plan
+- }
+ 
+ codex
+-완료했습니다.
++변경 완료했습니다.
+ 
+ 변경한 파일:
+-- [scripts/ai-dev-auto-goal.ps1](D:/ai-apps/planpilot-local/scripts/ai-dev-auto-goal.ps1:301)
++- [src/views/TasksView.tsx](D:/ai-apps/planpilot-local/src/views/TasksView.tsx:115)
+ 
+-구현 요약:
+-- DryRun goal/task 미리보기의 깨진 한국어 문자열을 정상 한국어로 복구했습니다.
+-- Codex가 생성한 `queue/state` 검증을 강화했습니다.
+-  - `queue.currentTaskId`는 `T001`이어야 함
+-  - `state.currentTaskId`는 `T001`이어야 함
+-  - `T001` task가 존재하고 `in_progress`여야 함
+-  - 이후 task는 `pending`이어야 함
+-- `.ai-dev/README.md`에는 이미 `ai-dev-auto-goal.ps1` 사용법과 안전 기준이 문서화되어 있어 수정하지 않았습니다.
++요약:
++- 업무 검색창 placeholder를 `업무 제목 검색`에서 `업무 제목이나 메모로 검색`으로 변경했습니다.
++- `value={taskSearchQuery}`와 `onChange={(event) => onTaskSearchQueryChange(event.target.value)}`는 
+그
++대로 유지했습니다.
++- 검색 동작, 상태 관리, 구조 변경은 하지 않았습니다.
+ 
+ 검증:
+-- PowerShell 문법 검증 통과
+-- `-DryRun` 텍스트 출력 확인
+-- `-DryRun -Json` 순수 JSON 출력 확인
+-- 공백/누락 `GoalTitle` 오류 종료 확인
+-- `-DryRun -AllowRun -Json`에서 full-cycle 호출이 실제 실행되지 않고 단계만 표시되는 것 확인
+-
+-실행하지 않은 검증:
+-- `npm run build`, `npm run lint`, `npm run test`는 지시 범위상 실행하지 않았습니다.
+-- 실제 Codex CLI 실행 및 실제 full cycle 실행은 파일 작성/외부 실행을 수반하므로 실행하지 않았습니다.
++- `rg`로 placeholder 변경과 기존 검색 상태 연결이 유지된 것을 확인했습니다.
++- 지시상 `npm run build`, `npm run lint`, 브라우저 확인은 실행하지 않았습니다.
+ 
+ 남은 위험:
+-- 실제 `codex exec` 출력 품질과 실제 dirty worktree 게이트는 실사용 환경에서 별도 확인이 필요합니다.
++- 실제 화면 렌더링은 수동 검증이 필요합니다.
+ 
+-다음 task로 넘어가도 됩니다.
++다음 task 진행 여부:
++- 현재 task 기준으로 완료되었으므로 다음 task로 넘어가도 됩니다.
+ 
+ `.ai-dev/loop-log.md` 기록용 요약:
+-`T007: ai-dev-auto-goal.ps1 DryRun 미리보기 문자열을 복구하고 goal/queue/state 생성 검증을 T001 시작 규칙에 맞게 강화했다. Pow
+e
+-rShell 문법, DryRun, Json 출력, 빈 GoalTitle 오류 경로를 확인했다.`
+-diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
+-index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
+---- a/scripts/ai-dev-auto-goal.ps1
+-+++ b/scripts/ai-dev-auto-goal.ps1
+-@@ -297,6 +297,10 @@
+-         $errors += "queue.tasks must contain no more than three tasks."
+-     }
+- 
+-+    if ($Queue.currentTaskId -ne "T001") {
+-+        $errors += "queue.currentTaskId must be T001."
 -+    }
 -+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
+-     $taskIds = @()
+- 
+-     foreach ($task in $tasks) {
+-@@ -360,6 +364,19 @@
+-         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
+-     }
+- 
+-+    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
+-+    if ($null -eq $firstTask) {
+-+        $errors += "queue.tasks must include T001."
+-+    } elseif ($firstTask.status -ne "in_progress") {
+-+        $errors += "T001 must be in_progress."
 -+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
 -+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
+-+    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
+-+        if ($laterTask.status -ne "pending") {
+-+            $errors += "later tasks must be pending: $($laterTask.id)"
 -+        }
 -+    }
 -+
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
+-     return $errors
+- }
++- T001: `src/views/TasksView.tsx`의 업무 검색창 placeholder를 `업무 제목이나 메모로 검색`으로 변경했고, 기존 검색 입력 상태 및 필터링 
+연
++결은 유지함. 빌드/린트는 실행하지 않음.
++diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
++index f2f7fd812594455482fd78512b1a830e5d5dfe7a..7167cf7befdbe0130902ab6d86eb19a9eab5b300
++--- a/src/views/TasksView.tsx
+++++ b/src/views/TasksView.tsx
++@@ -112,7 +112,7 @@
++           type="text"
++           value={taskSearchQuery}
++           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
++-          placeholder="업무 제목 검색"
+++          placeholder="업무 제목이나 메모로 검색"
++         />
++       </label>
+  
+-@@ -388,6 +405,10 @@
+-         $errors += "state.currentTaskId must match queue.currentTaskId."
+-     }
+- 
+-+    if ($State.currentTaskId -ne "T001") {
+-+        $errors += "state.currentTaskId must be T001."
 -+    }
--+}
 -+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+수정 대상은 자동화 스크립트 하나입니다. README에는 이미 목표 입력 기반 실행 문서가 있어, 스크립트 동작 보강 후 문서와 실제 동작이 어긋나는 부분이 있는지만 다시 확인
-하
-+겠습니다.
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
-h
-+ 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' | Select-Object -Skip 330 -First 23
-0
-+" in D:\ai-apps\planpilot-local
-+ succeeded in 1784ms:
-+
-+        if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
-+            $errors += "invalid task.status: $($task.status)"
-+        }
-+
-+        if (@("P0", "P1", "P2") -notcontains $task.priority) {
-+            $errors += "invalid task.priority: $($task.priority)"
-+        }
-+
-+        if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is [strin
-g
-+]) {
-+            $errors += "task.dependsOn must be an array: $($task.id)"
-+        }
-+
-+        if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.filesLikel
-y
-+ToChange -is [string]) {
-+            $errors += "task.filesLikelyToChange must be an array: $($task.id)"
-+        }
-+
-+        if ($task.verification -isnot [System.Collections.IEnumerable] -or $task.verification -is 
-[
-+string]) {
-+            $errors += "task.verification must be an array: $($task.id)"
-+        }
-+    }
-+
-+    if ((@($tasks | Where-Object { $_.status -eq "in_progress" })).Count -ne 1) {
-+        $errors += "queue.tasks must contain exactly one in_progress task."
-+    }
-+
-+    if (-not (Test-HasValue $Queue.currentTaskId)) {
-+        $errors += "queue.currentTaskId must not be empty."
-+    } elseif ($taskIds -notcontains [string]$Queue.currentTaskId) {
-+        $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+    }
-+
-+    return $errors
-+}
-+
-+function Get-StateValidationErrors {
-+    param(
-+        [object]$State,
-+        [object]$Queue
-+    )
-+
-+    $errors = @()
-+    $requiredFields = @("goalStatus", "currentTaskId", "currentLoop", "maxLoopsPerTask", "repeated
-F
-+ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDecision", "lastR
-e
-+viewSeverity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
-+
-+    foreach ($field in @(Get-MissingFields $State $requiredFields)) {
-+        $errors += "state missing field: $field"
-+    }
-+
-+    if ($errors.Count -gt 0) {
-+        return $errors
-+    }
-+
-+    if (@("idle", "in_progress", "completed", "blocked") -notcontains $State.goalStatus) {
-+        $errors += "invalid state.goalStatus: $($State.goalStatus)"
-+    }
-+
-+    if ($State.currentTaskId -ne $Queue.currentTaskId) {
-+        $errors += "state.currentTaskId must match queue.currentTaskId."
-+    }
-+
-+    if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+        $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+    }
-+
-+    if (@("not_started", "pass", "revise", "blocked") -notcontains $State.lastReviewDecision) {
-+        $errors += "invalid state.lastReviewDecision: $($State.lastReviewDecision)"
-+    }
-+
-+    return $errors
-+}
-+
-+function Get-AutoGoalValidationErrors {
-+    param([object]$Plan)
-+
-+    $errors = @()
-+
-+    foreach ($field in @(Get-MissingFields $Plan @("goalMarkdown", "queue", "state"))) {
-+        $errors += "plan missing field: $field"
-+    }
-+
-+    if ($errors.Count -gt 0) {
-+        return $errors
-+    }
-+
-+    if (-not (Test-HasValue $Plan.goalMarkdown)) {
-+        $errors += "goalMarkdown must not be empty."
-+    }
-+
-+    $errors += @(Get-QueueValidationErrors $Plan.queue)
-+    $errors += @(Get-StateValidationErrors $Plan.state $Plan.queue)
-+
-+    return $errors
-+}
-+
-+function New-DryRunAutoGoalPlan {
-+    param(
-+        [string]$Title,
-+        [string]$Description
-+    )
-+
-+    $now = (Get-Date).ToUniversalTime().ToString("o")
-+    $safeCommitTitle = $Title.Trim()
-+
-+    $plan = [PSCustomObject][ordered]@{
-+        goalMarkdown = @"
-+# 紐⑺몴
-+
-+$safeCommitTitle
-+
-+## 諛곌꼍
-+
-+$($Description.Trim())
-+
-+## ?깃났 湲곗?
-+
-+- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+
-+## ?쒖빟?ы빆
-+
-+- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+
-+## ?쒖쇅 踰붿쐞
-+
-+- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+
-+## ?섎룞 ?뺤씤
-+
-+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+"@
-+        queue = [PSCustomObject][ordered]@{
-+            goalTitle = $safeCommitTitle
-+            goalSource = $goalRelativePath
-+            createdAt = $now
-+            updatedAt = $now
-+            currentTaskId = "T001"
-+            tasks = @(
-+                [PSCustomObject][ordered]@{
-+                    id = "T001"
-+                    title = $safeCommitTitle
-+                    description = $Description.Trim()
-+                    type = "implementation"
-+                    status = "in_progress"
-+                    priority = "P0"
-+                    dependsOn = @()
-+                    filesLikelyToChange = @()
-+                    verification = @(
-+                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+                    )
-+                    commitMessage = $null
-+                }
-+            )
-+        }
-+        state = [PSCustomObject][ordered]@{
-+            goalStatus = "in_progress"
-+            currentTaskId = "T001"
-+            currentLoop = 0
-+            maxLoopsPerTask = 2
-+            repeatedFailureCount = 0
-+            lastCommand = $null
-+            lastCommandStatus = "not_started"
-+            lastErrorSummary = $null
-+            lastReviewDecision = "not_started"
-+            lastReviewSeverity = $null
-+            lastCommitHash = $null
-+            startedAt = $now
-+            updatedAt = $now
-+            stopReason = $null
-+        }
-+    }
-+
-+    $plan.goalMarkdown = @"
-+# 紐⑺몴
-+
-+$safeCommitTitle
-+
-+## 諛곌꼍
-+
-+$($Description.Trim())
-+
-+## ?깃났 湲곗?
-+
-+- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-+- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+
-+## ?쒖빟?ы빆
-+
-+- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+
-+## ?쒖쇅 踰붿쐞
-+
-+- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+
-+## ?섎룞 ?뺤씤
-+
-+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+"@
-+
-+    $dryRunTask = @($plan.queue.tasks)[0]
-+    $dryRunTask.verification = @(
-+        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+    )
-+
-+    return $plan
-+}
-+
-+function ConvertFrom-CodexAutoGoalOutput {
-+    param([string]$RawInput)
-+
-+    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-+        try {
-+            $parsed = $candidate | ConvertFrom-Json
-+
-+            if ($null -eq $parsed -or $parsed -is [System.Array]) {
-+                continue
-+            }
-+
-+            if (@(Get-AutoGoalValidationErrors $parsed).Count -gt 0) {
-+                continue
-+            }
-+
-+codex
-+이제 `ai-dev-auto-goal.ps1`만 수정합니다. DryRun 미리보기 텍스트를 정상 한국어로 복구하고, Codex가 만든 queue/state가 `T001` 시작 
-규
-+칙을 벗어나면 파일을 쓰지 않도록 검증을 강화합니다.
-+apply patch
-+patch: completed
-+D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1
-+diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-+index 21176dc6aaf035988d7455fbb97b39eaff37ac50..40e87615fb5a517df15ebc3cce7963afb27cf9cd
-+--- a/scripts/ai-dev-auto-goal.ps1
-++++ b/scripts/ai-dev-auto-goal.ps1
-+@@ -297,6 +297,10 @@
-+         $errors += "queue.tasks must contain no more than three tasks."
-      }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-  
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,103 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
-++    if ($Queue.currentTaskId -ne "T001") {
-++        $errors += "queue.currentTaskId must be T001."
-++    }
- +
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
-+     $taskIds = @()
-  
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-- }
-+     foreach ($task in $tasks) {
-+@@ -360,6 +364,19 @@
-+         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+     }
-  
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -CommitFiles <files> -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
-++    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-++    if ($null -eq $firstTask) {
-++        $errors += "queue.tasks must include T001."
-++    } elseif ($firstTask.status -ne "in_progress") {
-++        $errors += "T001 must be in_progress."
- +    }
- +
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-++    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-++        if ($laterTask.status -ne "pending") {
-++            $errors += "later tasks must be pending: $($laterTask.id)"
-++        }
- +    }
- +
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+
--+if ($commitArguments.Count -eq 0) {
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1 -Files <files>" $false $true 1 "자동 커밋에는 선택 파일 목록이 필요합니다. -CommitFiles 
--scripts/example.ps1,.ai-dev/README.md 형식으로 지정하세요."
--+    Stop-Cycle $script:steps "commit_files_required" $false 1
--+}
--+
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
--+}
--+
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
--
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..1bdb85799839984c1deb03a6eba84a23cfc01093
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정하
--면 선택한 파일만 커밋 대상으로 전달한다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..effe1a6dc47f7fb626e50e31b78fca6d3085ab76
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-+     return $errors
-  }
-  
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
-+@@ -388,6 +405,10 @@
-+         $errors += "state.currentTaskId must match queue.currentTaskId."
-+     }
-  
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-++    if ($State.currentTaskId -ne "T001") {
-++        $errors += "state.currentTaskId must be T001."
- +    }
- +
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
-+     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+     }
-+
-+diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-+index 21176dc6aaf035988d7455fbb97b39eaff37ac50..40e87615fb5a517df15ebc3cce7963afb27cf9cd
-+--- a/scripts/ai-dev-auto-goal.ps1
-++++ b/scripts/ai-dev-auto-goal.ps1
-+@@ -297,6 +297,10 @@
-+         $errors += "queue.tasks must contain no more than three tasks."
-+     }
-+ 
-++    if ($Queue.currentTaskId -ne "T001") {
-++        $errors += "queue.currentTaskId must be T001."
- +    }
- +
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
-+     $taskIds = @()
-+ 
-+     foreach ($task in $tasks) {
-+@@ -360,6 +364,19 @@
-+         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+     }
-+ 
-++    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-++    if ($null -eq $firstTask) {
-++        $errors += "queue.tasks must include T001."
-++    } elseif ($firstTask.status -ne "in_progress") {
-++        $errors += "T001 must be in_progress."
- +    }
--+}
- +
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
-++    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-++        if ($laterTask.status -ne "pending") {
-++            $errors += "later tasks must be pending: $($laterTask.id)"
- +        }
- +    }
- +
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,103 @@
-+     return $errors
-  }
-  
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-- }
-+@@ -388,6 +405,10 @@
-+         $errors += "state.currentTaskId must match queue.currentTaskId."
-+     }
-  
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -CommitFiles <files> -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-++    if ($State.currentTaskId -ne "T001") {
-++        $errors += "state.currentTaskId must be T001."
- +    }
- +
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+
--+if ($commitArguments.Count -eq 0) {
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1 -Files <files>" $false $true 1 "자동 커밋에는 선택 파일 목록이 필요합니다. -CommitFiles 
--scripts/example.ps1,.ai-dev/README.md 형식으로 지정하세요."
--+    Stop-Cycle $script:steps "commit_files_required" $false 1
--+}
--+
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
--+}
--+
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+     }
- 
- codex
--문서도 같은 정책으로 맞춰 `-AllowCommit`에는 반드시 `-CommitFiles`가 필요하다고 명시하겠습니다.
-+검증 규칙 보강은 적용했습니다. 이어서 DryRun 계획 생성 함수의 깨진 문자열을 정상 한국어 미리보기로 교체합니다.
- apply patch
- patch: completed
--D:\ai-apps\planpilot-local\.ai-dev\README.md
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..53d98da00e94782e631bca47c1b7ebdf54159c39
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`과 `-CommitFiles`가 있을 때만 `ai-dev-commit.ps1 -Files`를 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-+D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1
-+diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-+index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
-+--- a/scripts/ai-dev-auto-goal.ps1
-++++ b/scripts/ai-dev-auto-goal.ps1
-+@@ -297,6 +297,10 @@
-+         $errors += "queue.tasks must contain no more than three tasks."
-+     }
-  
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`과 `-CommitFiles`를 함께 지정한다. 
--full cycle은 안전을 위해 선택 파일 목록이 없으면 자동 커밋하지 않는다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
-++    if ($Queue.currentTaskId -ne "T001") {
-++        $errors += "queue.currentTaskId must be T001."
-++    }
- +
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..effe1a6dc47f7fb626e50e31b78fca6d3085ab76
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-+     $taskIds = @()
-  
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
-+     foreach ($task in $tasks) {
-+@@ -360,6 +364,19 @@
-+         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+     }
-  
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
-++    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-++    if ($null -eq $firstTask) {
-++        $errors += "queue.tasks must include T001."
-++    } elseif ($firstTask.status -ne "in_progress") {
-++        $errors += "T001 must be in_progress."
- +    }
- +
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
-++    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-++        if ($laterTask.status -ne "pending") {
-++            $errors += "later tasks must be pending: $($laterTask.id)"
- +        }
- +    }
- +
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
+-     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
+-         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
 -     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,103 @@
-+     return $errors
-  }
-  
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
-+@@ -388,6 +405,10 @@
-+         $errors += "state.currentTaskId must match queue.currentTaskId."
-+     }
-  
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
-++    if ($State.currentTaskId -ne "T001") {
-++        $errors += "state.currentTaskId must be T001."
- +    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-++
-+     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+     }
-+@@ -429,41 +450,44 @@
-+     )
-+ 
-+     $now = (Get-Date).ToUniversalTime().ToString("o")
-+-    $safeCommitTitle = $Title.Trim()
-++    $safeTitle = $Title.Trim()
-++    $safeDescription = $Description.Trim()
-+ 
-+     $plan = [PSCustomObject][ordered]@{
-+         goalMarkdown = @"
-+-# 紐⑺몴
-++# 목표
-+ 
-+-$safeCommitTitle
-++$safeTitle
-+ 
-+-## 諛곌꼍
-++## 배경
-+ 
-+-$($Description.Trim())
-++$safeDescription
-+ 
-+-## ?깃났 湲곗?
-++## 성공 기준
-+ 
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++- 입력한 목표에서 현재 작업을 준비한다.
-++- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-++- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-+ 
-+-## ?쒖빟?ы빆
-++## 제약사항
-+ 
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-++- 저장소 정책과 현재 작업 범위를 따른다.
-+ 
-+-## ?쒖쇅 踰붿쐞
-++## 범위 제외
-+ 
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++- 패키지 변경
-++- DB 삭제 또는 초기화
-++- 광범위한 리팩터링
-+ 
-+-## ?섎룞 ?뺤씤
-++## 수동 검증
-+ 
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++- DryRun 계획과 JSON 출력을 검토한다.
-++- 현재 작업 프롬프트 생성 여부를 확인한다.
-++- 필요한 경우 빌드와 검증은 별도로 실행한다.
-+ "@
-+         queue = [PSCustomObject][ordered]@{
-+-            goalTitle = $safeCommitTitle
-++            goalTitle = $safeTitle
-+             goalSource = $goalRelativePath
-+             createdAt = $now
-+             updatedAt = $now
-+@@ -471,16 +495,16 @@
-+             tasks = @(
-+                 [PSCustomObject][ordered]@{
-+                     id = "T001"
-+-                    title = $safeCommitTitle
-+-                    description = $Description.Trim()
-++                    title = $safeTitle
-++                    description = $safeDescription
-+                     type = "implementation"
-+                     status = "in_progress"
-+                     priority = "P0"
-+                     dependsOn = @()
-+                     filesLikelyToChange = @()
-+                     verification = @(
-+-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-++                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-++                        "필요한 경우 npm run build를 별도로 실행한다."
-+                     )
-+                     commitMessage = $null
-+                 }
-+@@ -503,43 +527,6 @@
-+             stopReason = $null
-+         }
-+     }
-+-
-+-    $plan.goalMarkdown = @"
-+-# 紐⑺몴
-+-
-+-$safeCommitTitle
-+-
-+-## 諛곌꼍
-+-
-+-$($Description.Trim())
-+-
-+-## ?깃났 湲곗?
-+-
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+-
-+-## ?쒖빟?ы빆
-+-
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+-
-+-## ?쒖쇅 踰붿쐞
-+-
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+-
-+-## ?섎룞 ?뺤씤
-+-
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+-"@
-+-
-+-    $dryRunTask = @($plan.queue.tasks)[0]
-+-    $dryRunTask.verification = @(
-+-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+-    )
-+ 
-+     return $plan
-  }
-+
-+diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-+index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
-+--- a/scripts/ai-dev-auto-goal.ps1
-++++ b/scripts/ai-dev-auto-goal.ps1
-+@@ -297,6 +297,10 @@
-+         $errors += "queue.tasks must contain no more than three tasks."
-+     }
-  
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -CommitFiles <files> -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-++    if ($Queue.currentTaskId -ne "T001") {
-++        $errors += "queue.currentTaskId must be T001."
- +    }
- +
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+
--+if ($commitArguments.Count -eq 0) {
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1 -Files <files>" $false $true 1 "자동 커밋에는 선택 파일 목록이 필요합니다. -CommitFiles 
--scripts/example.ps1,.ai-dev/README.md 형식으로 지정하세요."
--+    Stop-Cycle $script:steps "commit_files_required" $false 1
--+}
--+
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
--+}
--+
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
--
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..53d98da00e94782e631bca47c1b7ebdf54159c39
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`과 `-CommitFiles`가 있을 때만 `ai-dev-commit.ps1 -Files`를 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-- 
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`과 `-CommitFiles`를 함께 지정한다. 
--full cycle은 안전을 위해 선택 파일 목록이 없으면 자동 커밋하지 않는다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
--+
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-+     $taskIds = @()
-  
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..effe1a6dc47f7fb626e50e31b78fca6d3085ab76
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
-+     foreach ($task in $tasks) {
-+@@ -360,6 +364,19 @@
-+         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+     }
-  
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
-++    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-++    if ($null -eq $firstTask) {
-++        $errors += "queue.tasks must include T001."
-++    } elseif ($firstTask.status -ne "in_progress") {
-++        $errors += "T001 must be in_progress."
- +    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
- +
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
-++    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-++        if ($laterTask.status -ne "pending") {
-++            $errors += "later tasks must be pending: $($laterTask.id)"
- +        }
- +    }
- +
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
+-@@ -429,41 +450,44 @@
+-     )
+- 
+-     $now = (Get-Date).ToUniversalTime().ToString("o")
+--    $safeCommitTitle = $Title.Trim()
+-+    $safeTitle = $Title.Trim()
+-+    $safeDescription = $Description.Trim()
+- 
+-     $plan = [PSCustomObject][ordered]@{
+-         goalMarkdown = @"
+--# 紐⑺몴
+-+# 목표
+- 
+--$safeCommitTitle
+-+$safeTitle
+- 
+--## 諛곌꼍
+-+## 배경
+- 
+--$($Description.Trim())
+-+$safeDescription
+- 
+--## ?깃났 湲곗?
+-+## 성공 기준
+- 
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+-+- 입력한 목표에서 현재 작업을 준비한다.
+-+- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
+-+- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
+- 
+--## ?쒖빟?ы빆
+-+## 제약사항
+- 
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+-+- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
+-+- 저장소 정책과 현재 작업 범위를 따른다.
+- 
+--## ?쒖쇅 踰붿쐞
+-+## 범위 제외
+- 
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+-+- 패키지 변경
+-+- DB 삭제 또는 초기화
+-+- 광범위한 리팩터링
+- 
+--## ?섎룞 ?뺤씤
+-+## 수동 검증
+- 
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+-+- DryRun 계획과 JSON 출력을 검토한다.
+-+- 현재 작업 프롬프트 생성 여부를 확인한다.
+-+- 필요한 경우 빌드와 검증은 별도로 실행한다.
+- "@
+-         queue = [PSCustomObject][ordered]@{
+--            goalTitle = $safeCommitTitle
+-+            goalTitle = $safeTitle
+-             goalSource = $goalRelativePath
+-             createdAt = $now
+-             updatedAt = $now
+-@@ -471,16 +495,16 @@
+-             tasks = @(
+-                 [PSCustomObject][ordered]@{
+-                     id = "T001"
+--                    title = $safeCommitTitle
+--                    description = $Description.Trim()
+-+                    title = $safeTitle
+-+                    description = $safeDescription
+-                     type = "implementation"
+-                     status = "in_progress"
+-                     priority = "P0"
+-                     dependsOn = @()
+-                     filesLikelyToChange = @()
+-                     verification = @(
+--                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+-+                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
+-+                        "필요한 경우 npm run build를 별도로 실행한다."
+-                     )
+-                     commitMessage = $null
+-                 }
+-@@ -503,43 +527,6 @@
+-             stopReason = $null
+-         }
 -     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
+--
+--    $plan.goalMarkdown = @"
+--# 紐⑺몴
+--
+--$safeCommitTitle
+--
+--## 諛곌꼍
+--
+--$($Description.Trim())
+--
+--## ?깃났 湲곗?
+--
+--- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
+--- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
+--- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
+--
+--## ?쒖빟?ы빆
+--
+--- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
+--- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
+--
+--## ?쒖쇅 踰붿쐞
+--
+--- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
+--
+--## ?섎룞 ?뺤씤
+--
+--- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
+--- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
+--- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
+--"@
+--
+--    $dryRunTask = @($plan.queue.tasks)[0]
+--    $dryRunTask.verification = @(
+--        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
+--        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
+--    )
+- 
+-     return $plan
 - }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-+     return $errors
-  }
-  
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,103 @@
-- }
-- 
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-- }
-+@@ -388,6 +405,10 @@
-+         $errors += "state.currentTaskId must match queue.currentTaskId."
-+     }
-  
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -CommitFiles <files> -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
--+    }
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-++    if ($State.currentTaskId -ne "T001") {
-++        $errors += "state.currentTaskId must be T001."
- +    }
- +
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+
--+if ($commitArguments.Count -eq 0) {
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1 -Files <files>" $false $true 1 "자동 커밋에는 선택 파일 목록이 필요합니다. -CommitFiles 
--scripts/example.ps1,.ai-dev/README.md 형식으로 지정하세요."
--+    Stop-Cycle $script:steps "commit_files_required" $false 1
--+}
--+
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
--+}
--+
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+     }
-+@@ -429,41 +450,44 @@
-+     )
-+ 
-+     $now = (Get-Date).ToUniversalTime().ToString("o")
-+-    $safeCommitTitle = $Title.Trim()
-++    $safeTitle = $Title.Trim()
-++    $safeDescription = $Description.Trim()
-+ 
-+     $plan = [PSCustomObject][ordered]@{
-+         goalMarkdown = @"
-+-# 紐⑺몴
-++# 목표
-+ 
-+-$safeCommitTitle
-++$safeTitle
-+ 
-+-## 諛곌꼍
-++## 배경
-+ 
-+-$($Description.Trim())
-++$safeDescription
-+ 
-+-## ?깃났 湲곗?
-++## 성공 기준
-+ 
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++- 입력한 목표에서 현재 작업을 준비한다.
-++- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-++- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-+ 
-+-## ?쒖빟?ы빆
-++## 제약사항
-+ 
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-++- 저장소 정책과 현재 작업 범위를 따른다.
-+ 
-+-## ?쒖쇅 踰붿쐞
-++## 범위 제외
-+ 
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++- 패키지 변경
-++- DB 삭제 또는 초기화
-++- 광범위한 리팩터링
-+ 
-+-## ?섎룞 ?뺤씤
-++## 수동 검증
-+ 
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++- DryRun 계획과 JSON 출력을 검토한다.
-++- 현재 작업 프롬프트 생성 여부를 확인한다.
-++- 필요한 경우 빌드와 검증은 별도로 실행한다.
-+ "@
-+         queue = [PSCustomObject][ordered]@{
-+-            goalTitle = $safeCommitTitle
-++            goalTitle = $safeTitle
-+             goalSource = $goalRelativePath
-+             createdAt = $now
-+             updatedAt = $now
-+@@ -471,16 +495,16 @@
-+             tasks = @(
-+                 [PSCustomObject][ordered]@{
-+                     id = "T001"
-+-                    title = $safeCommitTitle
-+-                    description = $Description.Trim()
-++                    title = $safeTitle
-++                    description = $safeDescription
-+                     type = "implementation"
-+                     status = "in_progress"
-+                     priority = "P0"
-+                     dependsOn = @()
-+                     filesLikelyToChange = @()
-+                     verification = @(
-+-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-++                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-++                        "필요한 경우 npm run build를 별도로 실행한다."
-+                     )
-+                     commitMessage = $null
-+                 }
-+@@ -503,43 +527,6 @@
-+             stopReason = $null
-+         }
-+     }
-+-
-+-    $plan.goalMarkdown = @"
-+-# 紐⑺몴
-+-
-+-$safeCommitTitle
-+-
-+-## 諛곌꼍
-+-
-+-$($Description.Trim())
-+-
-+-## ?깃났 湲곗?
-+-
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+-
-+-## ?쒖빟?ы빆
-+-
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+-
-+-## ?쒖쇅 踰붿쐞
-+-
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+-
-+-## ?섎룞 ?뺤씤
-+-
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+-"@
-+-
-+-    $dryRunTask = @($plan.queue.tasks)[0]
-+-    $dryRunTask.verification = @(
-+-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+-    )
-+ 
-+     return $plan
-+ }
- 
- codex
--선택 파일 필수 정책까지 반영했습니다. 마지막으로 문법 검증과 DryRun을 한 번 더 확인하겠습니다.
-+수정은 완료되었습니다. 이제 PowerShell 파서로 문법을 확인하고, DryRun/JSON/빈 입력 오류 경로가 요구사항대로 동작하는지 실행 수준에서 검증하겠습니다.
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
-l
-+icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription 
-'
-+검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun" in D:\ai-apps\planpilot-local
- exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$tokens = $null; $parse
--Errors = $null; $null = [System.Management.Automation.Language.Parser]::ParseFile('"'scripts/ai-de
--v-auto-cycle-full.ps1', [ref]"'$tokens, [ref]$parseErrors); if ($parseErrors.Count -gt 0) { $parse
--Errors | ForEach-Object { $_.Message }; exit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-l
--ocal
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
-l
-+icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription 
-'
-+검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun -Json" in D:\ai-apps\planpilot-local
- exec
--"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'powershell -ExecutionPo
--licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -MaxSteps 20 -Json' in D:
--\ai-apps\planpilot-local
-- succeeded in 1694ms:
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
-l
-+icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '' -GoalDescription '설명' -DryRun -Jso
-n
-+" in D:\ai-apps\planpilot-local
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$tokens=$null; $errors=
-$
-+null; [System.Management.Automation.Language.Parser]::ParseFile('"'D:\\ai-apps\\planpilot-local\\s
-c
-+ripts\\ai-dev-auto-goal.ps1',[ref]"'$tokens,[ref]$errors) > $null; if ($errors.Count -gt 0) { $err
-o
-+rs | ForEach-Object { $_.Message }; exit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-local
-+ succeeded in 3046ms:
- syntax ok
  
-- succeeded in 2932ms:
-+ exited 1 in 4136ms:
-+D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1 : Missing an argument for parameter 'GoalT
-i
-+tle'. Specify a para
-+meter of type 'System.String' and try again.
-+    + CategoryInfo          : InvalidArgument: (:) [ai-dev-auto-goal.ps1], ParentContainsErrorReco
-r
-+dException
-+    + FullyQualifiedErrorId : MissingArgument,ai-dev-auto-goal.ps1
-+ 
-+
-+2026-06-09T13:59:38.985059Z ERROR codex_core::tools::router: error=Exit code: 1
-+Wall time: 4.1 seconds
-+Output:
-+D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1 : Missing an argument for parameter 'GoalT
-i
-+tle'. Specify a para
-+meter of type 'System.String' and try again.
-+    + CategoryInfo          : InvalidArgument: (:) [ai-dev-auto-goal.ps1], ParentContainsErrorReco
-r
-+dException
-+    + FullyQualifiedErrorId : MissingArgument,ai-dev-auto-goal.ps1
-+ 
-+
-+ succeeded in 4962ms:
- {
-     "steps":  [
-                   {
-                       "step":  1,
--                      "name":  "make-prompt",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pr
--ompt.ps1",
-+                      "name":  "validate-input",
-+                      "command":  "check GoalTitle/GoalDescription",
-                       "executed":  false,
--                      "skipped":  true,
-+                      "skipped":  false,
-                       "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-+                      "message":  "Input validation completed: 업무 검색 결과 하이라이트 추가"
-                   },
-                   {
-                       "step":  2,
--                      "name":  "run-codex",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-cod
--ex.ps1",
-+                      "name":  "dirty-worktree-gate",
-+                      "command":  "git status --short",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-+                      "message":  "DryRun: dirty worktree gate was not executed."
-                   },
-                   {
-                       "step":  3,
--                      "name":  "check",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.p
--s1 -BuildOnly",
-+                      "name":  "plan-goal",
-+                      "command":  "codex exec \u003cauto-goal planning prompt\u003e",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-+                      "message":  "DryRun: Codex goal planning was not executed. Preview currentTa
-s
-+kId: T001, task: 업무 검색 결과 하이라이트 추가"
-                   },
-                   {
-                       "step":  4,
--                      "name":  "save-diff",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-di
--ff.ps1",
-+                      "name":  "validate-generated-json",
-+                      "command":  "goal/queue/state JSON validation",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-+                      "message":  "DryRun: preview goal/queue/state plan passed local schema valid
-a
-+tion."
-                   },
-                   {
-                       "step":  5,
--                      "name":  "make-review-prompt",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-re
--view-prompt.ps1 -Strict",
-+                      "name":  "write-state-files",
-+                      "command":  ".ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-+                      "message":  "DryRun: state files were not written."
-                   },
-                   {
-                       "step":  6,
--                      "name":  "run-review-codex",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-rev
--iew-codex.ps1 -AllowDirty -SaveReview",
-+                      "name":  "make-prompt",
-+                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pr
-o
-+mpt.ps1",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-+                      "message":  "DryRun: current-task-prompt.md was not generated."
-                   },
-                   {
-                       "step":  7,
--                      "name":  "review-gate",
--                      "command":  "state.lastReviewDecision 확인",
-+                      "name":  "auto-cycle-full",
-+                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cy
-c
-+le-full.ps1 -MaxTasks 1 -MaxSteps 20 -AllowDirty",
-+                      "executed":  false,
-+                      "skipped":  true,
-+                      "exitCode":  0,
-+                      "message":  "DryRun: full cycle requires -AllowRun or explicit execution opt
-i
-+ons. -AllowRun only invokes the full-cycle wrapper; implementation and review still require -Allow
-C
-+odex and -AllowReviewCodex."
-+                  }
-+              ],
-+    "stoppedReason":  "dry_run",
-+    "completed":  false,
-+    "exitCode":  0,
-+    "plan":  {
-+                 "goalMarkdown":  "# 목표\n\n업무 검색 결과 하이라이트 추가\n\n## 배경\n\n검색어와 일치하는 업무 제목/메모/프로젝트명을
+ tokens used
+-완료했습니다.
++33,078
++변경 완료했습니다.
  
-+화면에서 강조 표시한다.\n\n## 성공 기준\n\n- 입력한 목표에서 현재 작업을 준비한다.\n- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.\n-
+ 변경한 파일:
+-- [scripts/ai-dev-auto-goal.ps1](D:/ai-apps/planpilot-local/scripts/ai-dev-auto-goal.ps1:301)
++- [src/views/TasksView.tsx](D:/ai-apps/planpilot-local/src/views/TasksView.tsx:115)
+ 
+-구현 요약:
+-- DryRun goal/task 미리보기의 깨진 한국어 문자열을 정상 한국어로 복구했습니다.
+-- Codex가 생성한 `queue/state` 검증을 강화했습니다.
+-  - `queue.currentTaskId`는 `T001`이어야 함
+-  - `state.currentTaskId`는 `T001`이어야 함
+-  - `T001` task가 존재하고 `in_progress`여야 함
+-  - 이후 task는 `pending`이어야 함
+-- `.ai-dev/README.md`에는 이미 `ai-dev-auto-goal.ps1` 사용법과 안전 기준이 문서화되어 있어 수정하지 않았습니다.
++요약:
++- 업무 검색창 placeholder를 `업무 제목 검색`에서 `업무 제목이나 메모로 검색`으로 변경했습니다.
++- `value={taskSearchQuery}`와 `onChange={(event) => onTaskSearchQueryChange(event.target.value)}`는 
+그대로 유지했습니다.
++- 검색 동작, 상태 관리, 구조 변경은 하지 않았습니다.
  
-+full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.\n\n## 제약사항\n\n- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 g
-i
-+t 명령은 사용하지 않는다.\n- 저장소 정책과 현재 작업 범위를 따른다.\n\n## 범위 제외\n\n- 패키지 변경\n- DB 삭제 또는 초기화\n- 광범위한 리팩터링\n\n
-#
-+# 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재 작업 프롬프트 생성 여부를 확인한다.\n- 필요한 경우 빌드와 검증은 별도로 실행한다.",
-+                 "queue":  {
-+                               "goalTitle":  "업무 검색 결과 하이라이트 추가",
-+                               "goalSource":  ".ai-dev/goal.md",
-+                               "createdAt":  "2026-06-09T13:59:38.3307470Z",
-+                               "updatedAt":  "2026-06-09T13:59:38.3307470Z",
-+                               "currentTaskId":  "T001",
-+                               "tasks":  [
-+                                             {
-+                                                 "id":  "T001",
-+                                                 "title":  "업무 검색 결과 하이라이트 추가",
-+                                                 "description":  "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강
-조
-+ 표시한다.",
-+                                                 "type":  "implementation",
-+                                                 "status":  "in_progress",
-+                                                 "priority":  "P0",
-+                                                 "dependsOn":  [
-+
-+                                                               ],
-+                                                 "filesLikelyToChange":  [
-+
-+                                                                         ],
-+                                                 "verification":  [
-+                                                                      "변경 범위가 현재 목표와 일치하는지 확인한다.",
-+                                                                      "필요한 경우 npm run build를 별도로 실
-행
-+한다."
-+                                                                  ],
-+                                                 "commitMessage":  null
-+                                             }
-+                                         ]
-+                           },
-+                 "state":  {
-+                               "goalStatus":  "in_progress",
-+                               "currentTaskId":  "T001",
-+                               "currentLoop":  0,
-+                               "maxLoopsPerTask":  2,
-+                               "repeatedFailureCount":  0,
-+                               "lastCommand":  null,
-+                               "lastCommandStatus":  "not_started",
-+                               "lastErrorSummary":  null,
-+                               "lastReviewDecision":  "not_started",
-+                               "lastReviewSeverity":  null,
-+                               "lastCommitHash":  null,
-+                               "startedAt":  "2026-06-09T13:59:38.3307470Z",
-+                               "updatedAt":  "2026-06-09T13:59:38.3307470Z",
-+                               "stopReason":  null
-+                           }
-+             },
-+    "goalPath":  ".ai-dev/goal.md",
-+    "queuePath":  ".ai-dev/queue.json",
-+    "statePath":  ".ai-dev/state.json",
-+    "promptPath":  ".ai-dev/current-task-prompt.md",
-+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-+}
-+
-+ succeeded in 5085ms:
-+Step 1: validate-input
-+  Command: check GoalTitle/GoalDescription
-+  Executed: False
-+  Skipped: False
-+  Exit code: 0
-+  Message: Input validation completed: 업무 검색 결과 하이라이트 추가
-+Step 2: dirty-worktree-gate
-+  Command: git status --short
-+  Executed: False
-+  Skipped: True
-+  Exit code: 0
-+  Message: DryRun: dirty worktree gate was not executed.
-+Step 3: plan-goal
-+  Command: codex exec <auto-goal planning prompt>
-+  Executed: False
-+  Skipped: True
-+  Exit code: 0
-+  Message: DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, task: 업무 검색 
-결
-+과 하이라이트 추가
-+Step 4: validate-generated-json
-+  Command: goal/queue/state JSON validation
-+  Executed: False
-+  Skipped: True
-+  Exit code: 0
-+  Message: DryRun: preview goal/queue/state plan passed local schema validation.
-+Step 5: write-state-files
-+  Command: .ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json
-+  Executed: False
-+  Skipped: True
-+  Exit code: 0
-+  Message: DryRun: state files were not written.
-+Step 6: make-prompt
-+  Command: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
-+  Executed: False
-+  Skipped: True
-+  Exit code: 0
-+  Message: DryRun: current-task-prompt.md was not generated.
-+Step 7: auto-cycle-full
-+  Command: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -MaxTasks 1
+ 검증:
+-- PowerShell 문법 검증 통과
+-- `-DryRun` 텍스트 출력 확인
+-- `-DryRun -Json` 순수 JSON 출력 확인
+-- 공백/누락 `GoalTitle` 오류 종료 확인
+-- `-DryRun -AllowRun -Json`에서 full-cycle 호출이 실제 실행되지 않고 단계만 표시되는 것 확인
+-
+-실행하지 않은 검증:
+-- `npm run build`, `npm run lint`, `npm run test`는 지시 범위상 실행하지 않았습니다.
+-- 실제 Codex CLI 실행 및 실제 full cycle 실행은 파일 작성/외부 실행을 수반하므로 실행하지 않았습니다.
++- `rg`로 placeholder 변경과 기존 검색 상태 연결이 유지된 것을 확인했습니다.
++- 지시상 `npm run build`, `npm run lint`, 브라우저 확인은 실행하지 않았습니다.
  
-+-MaxSteps 20 -AllowDirty
-+  Executed: False
-+  Skipped: True
-+  Exit code: 0
-+  Message: DryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only inv
-o
-+kes the full-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCo
-d
-+ex.
-+Plan preview:
-+  Goal title: 업무 검색 결과 하이라이트 추가
-+  Current task id: T001
-+  Task T001: 업무 검색 결과 하이라이트 추가
-+    Type: implementation
-+    Status: in_progress
-+    Priority: P0
-+    Likely files: 
-+    Verification: 변경 범위가 현재 목표와 일치하는지 확인한다. / 필요한 경우 npm run build를 별도로 실행한다.
-+Stopped reason: dry_run
-+Completed: False
-+Exit code: 0
-+
-+diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-+index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
-+--- a/scripts/ai-dev-auto-goal.ps1
-++++ b/scripts/ai-dev-auto-goal.ps1
-+@@ -297,6 +297,10 @@
-+         $errors += "queue.tasks must contain no more than three tasks."
-+     }
-+ 
-++    if ($Queue.currentTaskId -ne "T001") {
-++        $errors += "queue.currentTaskId must be T001."
-++    }
-++
-+     $taskIds = @()
-+ 
-+     foreach ($task in $tasks) {
-+@@ -360,6 +364,19 @@
-+         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+     }
-+ 
-++    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-++    if ($null -eq $firstTask) {
-++        $errors += "queue.tasks must include T001."
-++    } elseif ($firstTask.status -ne "in_progress") {
-++        $errors += "T001 must be in_progress."
-++    }
-++
-++    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-++        if ($laterTask.status -ne "pending") {
-++            $errors += "later tasks must be pending: $($laterTask.id)"
-++        }
-++    }
-++
-+     return $errors
-+ }
-+ 
-+@@ -388,6 +405,10 @@
-+         $errors += "state.currentTaskId must match queue.currentTaskId."
-+     }
-+ 
-++    if ($State.currentTaskId -ne "T001") {
-++        $errors += "state.currentTaskId must be T001."
-++    }
-++
-+     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+     }
-+@@ -429,41 +450,44 @@
-+     )
-+ 
-+     $now = (Get-Date).ToUniversalTime().ToString("o")
-+-    $safeCommitTitle = $Title.Trim()
-++    $safeTitle = $Title.Trim()
-++    $safeDescription = $Description.Trim()
-+ 
-+     $plan = [PSCustomObject][ordered]@{
-+         goalMarkdown = @"
-+-# 紐⑺몴
-++# 목표
-+ 
-+-$safeCommitTitle
-++$safeTitle
-+ 
-+-## 諛곌꼍
-++## 배경
-+ 
-+-$($Description.Trim())
-++$safeDescription
-+ 
-+-## ?깃났 湲곗?
-++## 성공 기준
-+ 
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++- 입력한 목표에서 현재 작업을 준비한다.
-++- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-++- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-+ 
-+-## ?쒖빟?ы빆
-++## 제약사항
-+ 
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-++- 저장소 정책과 현재 작업 범위를 따른다.
-+ 
-+-## ?쒖쇅 踰붿쐞
-++## 범위 제외
-+ 
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++- 패키지 변경
-++- DB 삭제 또는 초기화
-++- 광범위한 리팩터링
-+ 
-+-## ?섎룞 ?뺤씤
-++## 수동 검증
-+ 
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++- DryRun 계획과 JSON 출력을 검토한다.
-++- 현재 작업 프롬프트 생성 여부를 확인한다.
-++- 필요한 경우 빌드와 검증은 별도로 실행한다.
-+ "@
-+         queue = [PSCustomObject][ordered]@{
-+-            goalTitle = $safeCommitTitle
-++            goalTitle = $safeTitle
-+             goalSource = $goalRelativePath
-+             createdAt = $now
-+             updatedAt = $now
-+@@ -471,16 +495,16 @@
-+             tasks = @(
-+                 [PSCustomObject][ordered]@{
-+                     id = "T001"
-+-                    title = $safeCommitTitle
-+-                    description = $Description.Trim()
-++                    title = $safeTitle
-++                    description = $safeDescription
-+                     type = "implementation"
-+                     status = "in_progress"
-+                     priority = "P0"
-+                     dependsOn = @()
-+                     filesLikelyToChange = @()
-+                     verification = @(
-+-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-++                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-++                        "필요한 경우 npm run build를 별도로 실행한다."
-+                     )
-+                     commitMessage = $null
-+                 }
-+@@ -503,43 +527,6 @@
-+             stopReason = $null
-+         }
-+     }
-+-
-+-    $plan.goalMarkdown = @"
-+-# 紐⑺몴
-+-
-+-$safeCommitTitle
-+-
-+-## 諛곌꼍
-+-
-+-$($Description.Trim())
-+-
-+-## ?깃났 湲곗?
-+-
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+-
-+-## ?쒖빟?ы빆
-+-
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+-
-+-## ?쒖쇅 踰붿쐞
-+-
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+-
-+-## ?섎룞 ?뺤씤
-+-
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+-"@
-+-
-+-    $dryRunTask = @($plan.queue.tasks)[0]
-+-    $dryRunTask.verification = @(
-+-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+-    )
-+ 
-+     return $plan
-+ }
-+
-+codex
-+DryRun과 JSON 출력은 정상입니다. 빈 문자열 테스트는 PowerShell 인수 파서가 `''`를 값 없는 파라미터로 처리해 스크립트 내부 검증까지 들어가지 않았고, 공
-백
-+ 값과 생략 값 경로로 입력 검증을 다시 확인하겠습니다.
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
-l
-+icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle ' ' -GoalDescription '설명' -DryRun -Js
-o
-+n" in D:\ai-apps\planpilot-local
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
-l
-+icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '업무 검색 결과 하이라이트 추가' -GoalDescription 
-'
-+검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun -AllowRun -Json" in D:\ai-apps\planpilot-local
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPo
-l
-+icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalDescription '설명' -DryRun -Json" in D:\ai-ap
-p
-+s\planpilot-local
-+ exited 1 in 4098ms:
-+{
-+    "steps":  [
-+                  {
-+                      "step":  0,
-+                      "name":  "validate-input",
-+                      "command":  "check GoalTitle",
-+                      "executed":  false,
-+                      "skipped":  false,
-+                      "exitCode":  1,
-+                      "message":  "GoalTitle must not be empty."
-+                  }
-+              ],
-+    "stoppedReason":  "invalid_goal_title",
-+    "completed":  false,
-+    "exitCode":  1,
-+    "plan":  null,
-+    "goalPath":  ".ai-dev/goal.md",
-+    "queuePath":  ".ai-dev/queue.json",
-+    "statePath":  ".ai-dev/state.json",
-+    "promptPath":  ".ai-dev/current-task-prompt.md",
-+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-+}
-+
-+2026-06-09T13:59:56.754354Z ERROR codex_core::tools::router: error=Exit code: 1
-+Wall time: 4.1 seconds
-+Output:
-+{
-+    "steps":  [
-+                  {
-+                      "step":  0,
-+                      "name":  "validate-input",
-+                      "command":  "check GoalTitle",
-+                      "executed":  false,
-+                      "skipped":  false,
-+                      "exitCode":  1,
-+                      "message":  "GoalTitle must not be empty."
-+                  }
-+              ],
-+    "stoppedReason":  "invalid_goal_title",
-+    "completed":  false,
-+    "exitCode":  1,
-+    "plan":  null,
-+    "goalPath":  ".ai-dev/goal.md",
-+    "queuePath":  ".ai-dev/queue.json",
-+    "statePath":  ".ai-dev/state.json",
-+    "promptPath":  ".ai-dev/current-task-prompt.md",
-+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-+}
-+
-+ exited 1 in 4252ms:
-+{
-+    "steps":  [
-+                  {
-+                      "step":  0,
-+                      "name":  "validate-input",
-+                      "command":  "check GoalTitle",
-+                      "executed":  false,
-+                      "skipped":  false,
-+                      "exitCode":  1,
-+                      "message":  "GoalTitle must not be empty."
-+                  }
-+              ],
-+    "stoppedReason":  "invalid_goal_title",
-+    "completed":  false,
-+    "exitCode":  1,
-+    "plan":  null,
-+    "goalPath":  ".ai-dev/goal.md",
-+    "queuePath":  ".ai-dev/queue.json",
-+    "statePath":  ".ai-dev/state.json",
-+    "promptPath":  ".ai-dev/current-task-prompt.md",
-+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-+}
-+
-+ succeeded in 4254ms:
-+{
-+    "steps":  [
-+                  {
-+                      "step":  1,
-+                      "name":  "validate-input",
-+                      "command":  "check GoalTitle/GoalDescription",
-+                      "executed":  false,
-+                      "skipped":  false,
-+                      "exitCode":  0,
-+                      "message":  "Input validation completed: 업무 검색 결과 하이라이트 추가"
-+                  },
-+                  {
-+                      "step":  2,
-+                      "name":  "dirty-worktree-gate",
-+                      "command":  "git status --short",
-+                      "executed":  false,
-+                      "skipped":  true,
-+                      "exitCode":  0,
-+                      "message":  "DryRun: dirty worktree gate was not executed."
-+                  },
-+                  {
-+                      "step":  3,
-+                      "name":  "plan-goal",
-+                      "command":  "codex exec \u003cauto-goal planning prompt\u003e",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+                      "message":  "DryRun: Codex goal planning was not executed. Preview currentTa
-s
-+kId: T001, task: 업무 검색 결과 하이라이트 추가"
-                   },
-                   {
--                      "step":  8,
--                      "name":  "package-change-gate",
--                      "command":  "git status --porcelain -- package.json package-lock.json",
-+                      "step":  4,
-+                      "name":  "validate-generated-json",
-+                      "command":  "goal/queue/state JSON validation",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+                      "message":  "DryRun: preview goal/queue/state plan passed local schema valid
-a
-+tion."
-                   },
-                   {
--                      "step":  9,
--                      "name":  "commit",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.
--ps1",
-+                      "step":  5,
-+                      "name":  "write-state-files",
-+                      "command":  ".ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: git add/commit을 실행하지 않았습니다."
-+                      "message":  "DryRun: state files were not written."
-                   },
-                   {
--                      "step":  10,
--                      "name":  "commit-result-gate",
--                      "command":  "state.lastCommand/lastCommitHash 확인",
-+                      "step":  6,
-+                      "name":  "make-prompt",
-+                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pr
-o
-+mpt.ps1",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
-+                      "message":  "DryRun: current-task-prompt.md was not generated."
-                   },
-                   {
--                      "step":  11,
--                      "name":  "complete-task",
--                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complet
--e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-+                      "step":  7,
-+                      "name":  "auto-cycle-full",
-+                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cy
-c
-+le-full.ps1 -MaxTasks 1 -MaxSteps 20 -AllowDirty",
-                       "executed":  false,
-                       "skipped":  true,
-                       "exitCode":  0,
--                      "message":  "DryRun: task 완료 처리를 실행하지 않았습니다."
-+                      "message":  "DryRun: explicit run option is present, but full cycle was not 
-e
-+xecuted. -AllowRun only invokes the full-cycle wrapper; implementation and review still require -A
-l
-+lowCodex and -AllowReviewCodex."
-                   }
-               ],
-     "stoppedReason":  "dry_run",
-     "completed":  false,
--    "exitCode":  0
-+    "exitCode":  0,
-+    "plan":  {
-+                 "goalMarkdown":  "# 목표\n\n업무 검색 결과 하이라이트 추가\n\n## 배경\n\n검색어와 일치하는 업무 제목/메모/프로젝트명을
+ 남은 위험:
+-- 실제 `codex exec` 출력 품질과 실제 dirty worktree 게이트는 실사용 환경에서 별도 확인이 필요합니다.
++- 실제 화면 렌더링은 수동 검증이 필요합니다.
  
-+화면에서 강조 표시한다.\n\n## 성공 기준\n\n- 입력한 목표에서 현재 작업을 준비한다.\n- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.\n-
+-다음 task로 넘어가도 됩니다.
++다음 task 진행 여부:
++- 현재 task 기준으로 완료되었으므로 다음 task로 넘어가도 됩니다.
  
-+full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.\n\n## 제약사항\n\n- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 g
-i
-+t 명령은 사용하지 않는다.\n- 저장소 정책과 현재 작업 범위를 따른다.\n\n## 범위 제외\n\n- 패키지 변경\n- DB 삭제 또는 초기화\n- 광범위한 리팩터링\n\n
-#
-+# 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재 작업 프롬프트 생성 여부를 확인한다.\n- 필요한 경우 빌드와 검증은 별도로 실행한다.",
-+                 "queue":  {
-+                               "goalTitle":  "업무 검색 결과 하이라이트 추가",
-+                               "goalSource":  ".ai-dev/goal.md",
-+                               "createdAt":  "2026-06-09T13:59:55.7497650Z",
-+                               "updatedAt":  "2026-06-09T13:59:55.7497650Z",
-+                               "currentTaskId":  "T001",
-+                               "tasks":  [
-+                                             {
-+                                                 "id":  "T001",
-+                                                 "title":  "업무 검색 결과 하이라이트 추가",
-+                                                 "description":  "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강
-조
-+ 표시한다.",
-+                                                 "type":  "implementation",
-+                                                 "status":  "in_progress",
-+                                                 "priority":  "P0",
-+                                                 "dependsOn":  [
-+
-+                                                               ],
-+                                                 "filesLikelyToChange":  [
-+
-+                                                                         ],
-+                                                 "verification":  [
-+                                                                      "변경 범위가 현재 목표와 일치하는지 확인한다.",
-+                                                                      "필요한 경우 npm run build를 별도로 실
-행
-+한다."
-+                                                                  ],
-+                                                 "commitMessage":  null
-+                                             }
-+                                         ]
-+                           },
-+                 "state":  {
-+                               "goalStatus":  "in_progress",
-+                               "currentTaskId":  "T001",
-+                               "currentLoop":  0,
-+                               "maxLoopsPerTask":  2,
-+                               "repeatedFailureCount":  0,
-+                               "lastCommand":  null,
-+                               "lastCommandStatus":  "not_started",
-+                               "lastErrorSummary":  null,
-+                               "lastReviewDecision":  "not_started",
-+                               "lastReviewSeverity":  null,
-+                               "lastCommitHash":  null,
-+                               "startedAt":  "2026-06-09T13:59:55.7497650Z",
-+                               "updatedAt":  "2026-06-09T13:59:55.7497650Z",
-+                               "stopReason":  null
-+                           }
-+             },
-+    "goalPath":  ".ai-dev/goal.md",
-+    "queuePath":  ".ai-dev/queue.json",
-+    "statePath":  ".ai-dev/state.json",
-+    "promptPath":  ".ai-dev/current-task-prompt.md",
-+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
- }
- 
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..53d98da00e94782e631bca47c1b7ebdf54159c39
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`과 `-CommitFiles`가 있을 때만 `ai-dev-commit.ps1 -Files`를 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-+2026-06-09T13:59:56.936113Z ERROR codex_core::tools::router: error=Exit code: 1
-+Wall time: 4.3 seconds
-+Output:
-+{
-+    "steps":  [
-+                  {
-+                      "step":  0,
-+                      "name":  "validate-input",
-+                      "command":  "check GoalTitle",
-+                      "executed":  false,
-+                      "skipped":  false,
-+                      "exitCode":  1,
-+                      "message":  "GoalTitle must not be empty."
-+                  }
-+              ],
-+    "stoppedReason":  "invalid_goal_title",
-+    "completed":  false,
-+    "exitCode":  1,
-+    "plan":  null,
-+    "goalPath":  ".ai-dev/goal.md",
-+    "queuePath":  ".ai-dev/queue.json",
-+    "statePath":  ".ai-dev/state.json",
-+    "promptPath":  ".ai-dev/current-task-prompt.md",
-+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-+}
-+
-+diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-+index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
-+--- a/scripts/ai-dev-auto-goal.ps1
-++++ b/scripts/ai-dev-auto-goal.ps1
-+@@ -297,6 +297,10 @@
-+         $errors += "queue.tasks must contain no more than three tasks."
-+     }
-  
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`과 `-CommitFiles`를 함께 지정한다. 
--full cycle은 안전을 위해 선택 파일 목록이 없으면 자동 커밋하지 않는다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
-++    if ($Queue.currentTaskId -ne "T001") {
-++        $errors += "queue.currentTaskId must be T001."
-++    }
- +
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..effe1a6dc47f7fb626e50e31b78fca6d3085ab76
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-+     $taskIds = @()
-  
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-- 
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
-+     foreach ($task in $tasks) {
-+@@ -360,6 +364,19 @@
-+         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+     }
-  
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
--+    }
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
-++    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-++    if ($null -eq $firstTask) {
-++        $errors += "queue.tasks must include T001."
-++    } elseif ($firstTask.status -ne "in_progress") {
-++        $errors += "T001 must be in_progress."
- +    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
- +
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
-++    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-++        if ($laterTask.status -ne "pending") {
-++            $errors += "later tasks must be pending: $($laterTask.id)"
- +        }
- +    }
- +
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-+     return $errors
-  }
-  
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-+@@ -388,6 +405,10 @@
-+         $errors += "state.currentTaskId must match queue.currentTaskId."
-+     }
-  
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,103 @@
-++    if ($State.currentTaskId -ne "T001") {
-++        $errors += "state.currentTaskId must be T001."
-++    }
-++
-+     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+     }
-+@@ -429,41 +450,44 @@
-+     )
-+ 
-+     $now = (Get-Date).ToUniversalTime().ToString("o")
-+-    $safeCommitTitle = $Title.Trim()
-++    $safeTitle = $Title.Trim()
-++    $safeDescription = $Description.Trim()
-+ 
-+     $plan = [PSCustomObject][ordered]@{
-+         goalMarkdown = @"
-+-# 紐⑺몴
-++# 목표
-+ 
-+-$safeCommitTitle
-++$safeTitle
-+ 
-+-## 諛곌꼍
-++## 배경
-+ 
-+-$($Description.Trim())
-++$safeDescription
-+ 
-+-## ?깃났 湲곗?
-++## 성공 기준
-+ 
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++- 입력한 목표에서 현재 작업을 준비한다.
-++- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-++- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-+ 
-+-## ?쒖빟?ы빆
-++## 제약사항
-+ 
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-++- 저장소 정책과 현재 작업 범위를 따른다.
-+ 
-+-## ?쒖쇅 踰붿쐞
-++## 범위 제외
-+ 
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++- 패키지 변경
-++- DB 삭제 또는 초기화
-++- 광범위한 리팩터링
-+ 
-+-## ?섎룞 ?뺤씤
-++## 수동 검증
-+ 
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++- DryRun 계획과 JSON 출력을 검토한다.
-++- 현재 작업 프롬프트 생성 여부를 확인한다.
-++- 필요한 경우 빌드와 검증은 별도로 실행한다.
-+ "@
-+         queue = [PSCustomObject][ordered]@{
-+-            goalTitle = $safeCommitTitle
-++            goalTitle = $safeTitle
-+             goalSource = $goalRelativePath
-+             createdAt = $now
-+             updatedAt = $now
-+@@ -471,16 +495,16 @@
-+             tasks = @(
-+                 [PSCustomObject][ordered]@{
-+                     id = "T001"
-+-                    title = $safeCommitTitle
-+-                    description = $Description.Trim()
-++                    title = $safeTitle
-++                    description = $safeDescription
-+                     type = "implementation"
-+                     status = "in_progress"
-+                     priority = "P0"
-+                     dependsOn = @()
-+                     filesLikelyToChange = @()
-+                     verification = @(
-+-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-++                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-++                        "필요한 경우 npm run build를 별도로 실행한다."
-+                     )
-+                     commitMessage = $null
-+                 }
-+@@ -503,43 +527,6 @@
-+             stopReason = $null
-+         }
-+     }
-+-
-+-    $plan.goalMarkdown = @"
-+-# 紐⑺몴
-+-
-+-$safeCommitTitle
-+-
-+-## 諛곌꼍
-+-
-+-$($Description.Trim())
-+-
-+-## ?깃났 湲곗?
-+-
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+-
-+-## ?쒖빟?ы빆
-+-
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+-
-+-## ?쒖쇅 踰붿쐞
-+-
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+-
-+-## ?섎룞 ?뺤씤
-+-
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+-"@
-+-
-+-    $dryRunTask = @($plan.queue.tasks)[0]
-+-    $dryRunTask.verification = @(
-+-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+-    )
-+ 
-+     return $plan
-  }
-+
-+codex
-+검증 결과 DryRun은 파일을 쓰지 않고 계획과 단계만 출력했고, JSON 모드는 텍스트와 섞이지 않았습니다. 마지막으로 수정된 함수 주변을 다시 읽어 의도치 않은 중복이나 
-깨
-+진 문자열이 남아 있는지 확인하겠습니다.
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralP
-a
-+th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern 'queue.currentTaskId mus
-t
-+ be T001','T001 must be in_progress','function New-DryRunAutoGoalPlan' -Context 0,35" in D:\ai-app
-s
-+\planpilot-local
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralP
-a
-+th 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -Pattern '紐|諛|쒖|꾩|蹂|湲|젣|뺤|⑦'" in 
-D
-+:\ai-apps\planpilot-local
-+ succeeded in 2011ms:
-+ succeeded in 2149ms:
-+
-+> scripts\ai-dev-auto-goal.ps1:301:        $errors += "queue.currentTaskId must be T001."
-+  scripts\ai-dev-auto-goal.ps1:302:    }
-+  scripts\ai-dev-auto-goal.ps1:303:
-+  scripts\ai-dev-auto-goal.ps1:304:    $taskIds = @()
-+  scripts\ai-dev-auto-goal.ps1:305:
-+  scripts\ai-dev-auto-goal.ps1:306:    foreach ($task in $tasks) {
-+  scripts\ai-dev-auto-goal.ps1:307:        $taskRequiredFields = @("id", "title", "description", "
-t
-+ype", "status", "pri
-+ority", "dependsOn", "filesLikelyToChange", "verification", "commitMessage")
-+  scripts\ai-dev-auto-goal.ps1:308:        $taskMissingFields = @(Get-MissingFields $task $taskReq
-u
-+iredFields)
-+  scripts\ai-dev-auto-goal.ps1:309:
-+  scripts\ai-dev-auto-goal.ps1:310:        foreach ($field in $taskMissingFields) {
-+  scripts\ai-dev-auto-goal.ps1:311:            $errors += "task missing field: $field"
-+  scripts\ai-dev-auto-goal.ps1:312:        }
-+  scripts\ai-dev-auto-goal.ps1:313:
-+  scripts\ai-dev-auto-goal.ps1:314:        if ($taskMissingFields.Count -gt 0) {
-+  scripts\ai-dev-auto-goal.ps1:315:            continue
-+  scripts\ai-dev-auto-goal.ps1:316:        }
-+  scripts\ai-dev-auto-goal.ps1:317:
-+  scripts\ai-dev-auto-goal.ps1:318:        if (-not (Test-HasValue $task.id)) {
-+  scripts\ai-dev-auto-goal.ps1:319:            $errors += "task.id must not be empty."
-+  scripts\ai-dev-auto-goal.ps1:320:        } else {
-+  scripts\ai-dev-auto-goal.ps1:321:            $taskIds += [string]$task.id
-+  scripts\ai-dev-auto-goal.ps1:322:        }
-+  scripts\ai-dev-auto-goal.ps1:323:
-+  scripts\ai-dev-auto-goal.ps1:324:        if (-not (Test-HasValue $task.title)) {
-+  scripts\ai-dev-auto-goal.ps1:325:            $errors += "task.title must not be empty."
-+  scripts\ai-dev-auto-goal.ps1:326:        }
-+  scripts\ai-dev-auto-goal.ps1:327:
-+  scripts\ai-dev-auto-goal.ps1:328:        if (-not (Test-HasValue $task.description)) {
-+  scripts\ai-dev-auto-goal.ps1:329:            $errors += "task.description must not be empty."
-+  scripts\ai-dev-auto-goal.ps1:330:        }
-+  scripts\ai-dev-auto-goal.ps1:331:
-+  scripts\ai-dev-auto-goal.ps1:332:        if (@("analysis", "implementation", "documentation", "v
-e
-+rification") -notcon
-+tains $task.type) {
-+  scripts\ai-dev-auto-goal.ps1:333:            $errors += "invalid task.type: $($task.type)"
-+  scripts\ai-dev-auto-goal.ps1:334:        }
-+  scripts\ai-dev-auto-goal.ps1:335:
-+  scripts\ai-dev-auto-goal.ps1:336:        if (@("pending", "in_progress", "done", "blocked") -not
-c
-+ontains $task.status
-+) {
-+> scripts\ai-dev-auto-goal.ps1:371:        $errors += "T001 must be in_progress."
-+  scripts\ai-dev-auto-goal.ps1:372:    }
-+  scripts\ai-dev-auto-goal.ps1:373:
-+  scripts\ai-dev-auto-goal.ps1:374:    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne 
-"
-+T001" })) {
-+  scripts\ai-dev-auto-goal.ps1:375:        if ($laterTask.status -ne "pending") {
-+  scripts\ai-dev-auto-goal.ps1:376:            $errors += "later tasks must be pending: $($laterTa
-s
-+k.id)"
-+  scripts\ai-dev-auto-goal.ps1:377:        }
-+  scripts\ai-dev-auto-goal.ps1:378:    }
-+  scripts\ai-dev-auto-goal.ps1:379:
-+  scripts\ai-dev-auto-goal.ps1:380:    return $errors
-+  scripts\ai-dev-auto-goal.ps1:381:}
-+  scripts\ai-dev-auto-goal.ps1:382:
-+  scripts\ai-dev-auto-goal.ps1:383:function Get-StateValidationErrors {
-+  scripts\ai-dev-auto-goal.ps1:384:    param(
-+  scripts\ai-dev-auto-goal.ps1:385:        [object]$State,
-+  scripts\ai-dev-auto-goal.ps1:386:        [object]$Queue
-+  scripts\ai-dev-auto-goal.ps1:387:    )
-+  scripts\ai-dev-auto-goal.ps1:388:
-+  scripts\ai-dev-auto-goal.ps1:389:    $errors = @()
-+  scripts\ai-dev-auto-goal.ps1:390:    $requiredFields = @("goalStatus", "currentTaskId", "current
-L
-+oop", "maxLoopsPerTa
-+sk", "repeatedFailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDe
-c
-+ision", "lastReviewS
-+everity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
-+  scripts\ai-dev-auto-goal.ps1:391:
-+  scripts\ai-dev-auto-goal.ps1:392:    foreach ($field in @(Get-MissingFields $State $requiredFiel
-d
-+s)) {
-+  scripts\ai-dev-auto-goal.ps1:393:        $errors += "state missing field: $field"
-+  scripts\ai-dev-auto-goal.ps1:394:    }
-+  scripts\ai-dev-auto-goal.ps1:395:
-+  scripts\ai-dev-auto-goal.ps1:396:    if ($errors.Count -gt 0) {
-+  scripts\ai-dev-auto-goal.ps1:397:        return $errors
-+  scripts\ai-dev-auto-goal.ps1:398:    }
-+  scripts\ai-dev-auto-goal.ps1:399:
-+  scripts\ai-dev-auto-goal.ps1:400:    if (@("idle", "in_progress", "completed", "blocked") -notco
-n
-+tains $State.goalSta
-+tus) {
-+  scripts\ai-dev-auto-goal.ps1:401:        $errors += "invalid state.goalStatus: $($State.goalStat
-u
-+s)"
-+  scripts\ai-dev-auto-goal.ps1:402:    }
-+  scripts\ai-dev-auto-goal.ps1:403:
-+  scripts\ai-dev-auto-goal.ps1:404:    if ($State.currentTaskId -ne $Queue.currentTaskId) {
-+  scripts\ai-dev-auto-goal.ps1:405:        $errors += "state.currentTaskId must match queue.curren
-t
-+TaskId."
-+  scripts\ai-dev-auto-goal.ps1:406:    }
-+> scripts\ai-dev-auto-goal.ps1:446:function New-DryRunAutoGoalPlan {
-+  scripts\ai-dev-auto-goal.ps1:447:    param(
-+  scripts\ai-dev-auto-goal.ps1:448:        [string]$Title,
-+  scripts\ai-dev-auto-goal.ps1:449:        [string]$Description
-+  scripts\ai-dev-auto-goal.ps1:450:    )
-+  scripts\ai-dev-auto-goal.ps1:451:
-+  scripts\ai-dev-auto-goal.ps1:452:    $now = (Get-Date).ToUniversalTime().ToString("o")
-+  scripts\ai-dev-auto-goal.ps1:453:    $safeTitle = $Title.Trim()
-+  scripts\ai-dev-auto-goal.ps1:454:    $safeDescription = $Description.Trim()
-+  scripts\ai-dev-auto-goal.ps1:455:
-+  scripts\ai-dev-auto-goal.ps1:456:    $plan = [PSCustomObject][ordered]@{
-+  scripts\ai-dev-auto-goal.ps1:457:        goalMarkdown = @"
-+  scripts\ai-dev-auto-goal.ps1:458:# 목표
-+  scripts\ai-dev-auto-goal.ps1:459:
-+  scripts\ai-dev-auto-goal.ps1:460:$safeTitle
-+  scripts\ai-dev-auto-goal.ps1:461:
-+  scripts\ai-dev-auto-goal.ps1:462:## 배경
-+  scripts\ai-dev-auto-goal.ps1:463:
-+  scripts\ai-dev-auto-goal.ps1:464:$safeDescription
-+  scripts\ai-dev-auto-goal.ps1:465:
-+  scripts\ai-dev-auto-goal.ps1:466:## 성공 기준
-+  scripts\ai-dev-auto-goal.ps1:467:
-+  scripts\ai-dev-auto-goal.ps1:468:- 입력한 목표에서 현재 작업을 준비한다.
-+  scripts\ai-dev-auto-goal.ps1:469:- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-+  scripts\ai-dev-auto-goal.ps1:470:- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-+  scripts\ai-dev-auto-goal.ps1:471:
-+  scripts\ai-dev-auto-goal.ps1:472:## 제약사항
-+  scripts\ai-dev-auto-goal.ps1:473:
-+  scripts\ai-dev-auto-goal.ps1:474:- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-+  scripts\ai-dev-auto-goal.ps1:475:- 저장소 정책과 현재 작업 범위를 따른다.
-+  scripts\ai-dev-auto-goal.ps1:476:
-+  scripts\ai-dev-auto-goal.ps1:477:## 범위 제외
-+  scripts\ai-dev-auto-goal.ps1:478:
-+  scripts\ai-dev-auto-goal.ps1:479:- 패키지 변경
-+  scripts\ai-dev-auto-goal.ps1:480:- DB 삭제 또는 초기화
-+  scripts\ai-dev-auto-goal.ps1:481:- 광범위한 리팩터링
-+
-+
-+
-+diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-+index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
-+--- a/scripts/ai-dev-auto-goal.ps1
-++++ b/scripts/ai-dev-auto-goal.ps1
-+@@ -297,6 +297,10 @@
-+         $errors += "queue.tasks must contain no more than three tasks."
-+     }
-  
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
-++    if ($Queue.currentTaskId -ne "T001") {
-++        $errors += "queue.currentTaskId must be T001."
-++    }
- +
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
-+     $taskIds = @()
-  
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-- }
-+     foreach ($task in $tasks) {
-+@@ -360,6 +364,19 @@
-+         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+     }
-  
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -CommitFiles <files> -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
-++    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-++    if ($null -eq $firstTask) {
-++        $errors += "queue.tasks must include T001."
-++    } elseif ($firstTask.status -ne "in_progress") {
-++        $errors += "T001 must be in_progress."
- +    }
- +
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-++    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-++        if ($laterTask.status -ne "pending") {
-++            $errors += "later tasks must be pending: $($laterTask.id)"
-++        }
- +    }
- +
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+
--+if ($commitArguments.Count -eq 0) {
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1 -Files <files>" $false $true 1 "자동 커밋에는 선택 파일 목록이 필요합니다. -CommitFiles 
--scripts/example.ps1,.ai-dev/README.md 형식으로 지정하세요."
--+    Stop-Cycle $script:steps "commit_files_required" $false 1
--+}
--+
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
--+}
--+
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
-+     return $errors
-+ }
-+ 
-+@@ -388,6 +405,10 @@
-+         $errors += "state.currentTaskId must match queue.currentTaskId."
-+     }
-+ 
-++    if ($State.currentTaskId -ne "T001") {
-++        $errors += "state.currentTaskId must be T001."
-++    }
- +
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+     }
-+@@ -429,41 +450,44 @@
-+     )
-+ 
-+     $now = (Get-Date).ToUniversalTime().ToString("o")
-+-    $safeCommitTitle = $Title.Trim()
-++    $safeTitle = $Title.Trim()
-++    $safeDescription = $Description.Trim()
-+ 
-+     $plan = [PSCustomObject][ordered]@{
-+         goalMarkdown = @"
-+-# 紐⑺몴
-++# 목표
-+ 
-+-$safeCommitTitle
-++$safeTitle
-+ 
-+-## 諛곌꼍
-++## 배경
-+ 
-+-$($Description.Trim())
-++$safeDescription
-+ 
-+-## ?깃났 湲곗?
-++## 성공 기준
-+ 
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++- 입력한 목표에서 현재 작업을 준비한다.
-++- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-++- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-+ 
-+-## ?쒖빟?ы빆
-++## 제약사항
-+ 
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-++- 저장소 정책과 현재 작업 범위를 따른다.
-+ 
-+-## ?쒖쇅 踰붿쐞
-++## 범위 제외
-+ 
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++- 패키지 변경
-++- DB 삭제 또는 초기화
-++- 광범위한 리팩터링
-+ 
-+-## ?섎룞 ?뺤씤
-++## 수동 검증
-+ 
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++- DryRun 계획과 JSON 출력을 검토한다.
-++- 현재 작업 프롬프트 생성 여부를 확인한다.
-++- 필요한 경우 빌드와 검증은 별도로 실행한다.
-+ "@
-+         queue = [PSCustomObject][ordered]@{
-+-            goalTitle = $safeCommitTitle
-++            goalTitle = $safeTitle
-+             goalSource = $goalRelativePath
-+             createdAt = $now
-+             updatedAt = $now
-+@@ -471,16 +495,16 @@
-+             tasks = @(
-+                 [PSCustomObject][ordered]@{
-+                     id = "T001"
-+-                    title = $safeCommitTitle
-+-                    description = $Description.Trim()
-++                    title = $safeTitle
-++                    description = $safeDescription
-+                     type = "implementation"
-+                     status = "in_progress"
-+                     priority = "P0"
-+                     dependsOn = @()
-+                     filesLikelyToChange = @()
-+                     verification = @(
-+-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-++                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-++                        "필요한 경우 npm run build를 별도로 실행한다."
-+                     )
-+                     commitMessage = $null
-+                 }
-+@@ -503,43 +527,6 @@
-+             stopReason = $null
-+         }
-+     }
-+-
-+-    $plan.goalMarkdown = @"
-+-# 紐⑺몴
-+-
-+-$safeCommitTitle
-+-
-+-## 諛곌꼍
-+-
-+-$($Description.Trim())
-+-
-+-## ?깃났 湲곗?
-+-
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+-
-+-## ?쒖빟?ы빆
-+-
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+-
-+-## ?쒖쇅 踰붿쐞
-+-
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+-
-+-## ?섎룞 ?뺤씤
-+-
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+-"@
-+-
-+-    $dryRunTask = @($plan.queue.tasks)[0]
-+-    $dryRunTask.verification = @(
-+-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+-    )
-+ 
-+     return $plan
-+ }
- 
- codex
--T005 구현 완료했습니다.
-+완료했습니다.
- 
- 변경한 파일:
--- [scripts/ai-dev-auto-cycle-full.ps1](D:/ai-apps/planpilot-local/scripts/ai-dev-auto-cycle-full.p
--s1)
--- [.ai-dev/README.md](D:/ai-apps/planpilot-local/.ai-dev/README.md)
-+- [scripts/ai-dev-auto-goal.ps1](D:/ai-apps/planpilot-local/scripts/ai-dev-auto-goal.ps1:301)
- 
- 구현 요약:
--- full auto-cycle에 `review-gate`, `package-change-gate`, `commit-result-gate`를 추가했습니다.
--- 리뷰 저장 결과가 `passed`이고 `lastReviewDecision`이 `pass`일 때만 커밋 단계로 진행합니다.
--- `review-response.json`의 `next_step`이 있으면 `complete_task`일 때만 진행합니다.
--- `package.json` 또는 `package-lock.json` 변경 감지 시 `package_files_changed`로 중단합니다.
--- `-AllowCommit`만으로는 커밋하지 않고, `-CommitFiles`가 지정된 경우에만 `ai-dev-commit.ps1 -Files`로 선택 파일 커밋을 수행합니다
--.
--- 커밋 후 `state.lastCommand=commit`, `lastCommandStatus=passed`, `lastCommitHash`가 확인될 때만 `complete-
--task`를 실행합니다.
--- README의 full auto-cycle 설명을 T005 기준으로 갱신했습니다.
-+- DryRun goal/task 미리보기의 깨진 한국어 문자열을 정상 한국어로 복구했습니다.
-+- Codex가 생성한 `queue/state` 검증을 강화했습니다.
-+  - `queue.currentTaskId`는 `T001`이어야 함
-+  - `state.currentTaskId`는 `T001`이어야 함
-+  - `T001` task가 존재하고 `in_progress`여야 함
-+  - 이후 task는 `pending`이어야 함
-+- `.ai-dev/README.md`에는 이미 `ai-dev-auto-goal.ps1` 사용법과 안전 기준이 문서화되어 있어 수정하지 않았습니다.
- 
- 검증:
--- PowerShell 문법 검증 통과:
--  - `Parser.ParseFile('scripts/ai-dev-auto-cycle-full.ps1', ...)`
--- DryRun 확인 통과:
--  - `powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks
-- 1 -MaxSteps 20 -Json`
--  - 11단계가 표시되고, 하위 스크립트/build/git commit/complete-task는 실행되지 않았습니다.
--- `npm run build`, `npm run test`, `npm run lint`, 실제 git commit은 실행하지 않았습니다.
-+- PowerShell 문법 검증 통과
-+- `-DryRun` 텍스트 출력 확인
-+- `-DryRun -Json` 순수 JSON 출력 확인
-+- 공백/누락 `GoalTitle` 오류 종료 확인
-+- `-DryRun -AllowRun -Json`에서 full-cycle 호출이 실제 실행되지 않고 단계만 표시되는 것 확인
-+
-+실행하지 않은 검증:
-+- `npm run build`, `npm run lint`, `npm run test`는 지시 범위상 실행하지 않았습니다.
-+- 실제 Codex CLI 실행 및 실제 full cycle 실행은 파일 작성/외부 실행을 수반하므로 실행하지 않았습니다.
- 
- 남은 위험:
--- 실제 pass 리뷰 이후의 end-to-end 커밋/complete-task 실행은 T006의 작은 task 검증에서 확인해야 합니다.
--- package 변경, build 실패, non-pass 리뷰 중단은 현재 코드 게이트와 흐름 기준으로 확인했으며 실제 시나리오 실행은 하지 않았습니다.
-+- 실제 `codex exec` 출력 품질과 실제 dirty worktree 게이트는 실사용 환경에서 별도 확인이 필요합니다.
- 
- 다음 task로 넘어가도 됩니다.
- 
- `.ai-dev/loop-log.md` 기록용 요약:
--```markdown
--## T005 - 자동 커밋과 task 완료 연결
--
--- `ai-dev-auto-cycle-full.ps1`에 리뷰 pass 게이트, package 파일 변경 게이트, 커밋 결과 확인 게이트를 추가했다.
--- `-AllowCommit`과 `-CommitFiles`가 있을 때만 선택 파일 커밋을 실행하도록 제한했다.
--- 실제 commit hash가 state에 기록된 경우에만 `ai-dev-complete-task.ps1`을 실행하도록 연결했다.
--- `.ai-dev/README.md`의 full auto-cycle 설명을 T005 기준으로 갱신했다.
--- PowerShell 문법 검증과 full cycle DryRun을 확인했다.
--```
--diff --git a/.ai-dev/README.md b/.ai-dev/README.md
--index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..53d98da00e94782e631bca47c1b7ebdf54159c39
----- a/.ai-dev/README.md
--+++ b/.ai-dev/README.md
--@@ -668,9 +668,9 @@
-- 
-- ### full auto-cycle 초안
-- 
---`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
--+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
--ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-- 
---T004 버전의 실행 단계:
--+T005 버전의 실행 단계:
-- 
-- 1. `ai-dev-make-prompt.ps1`
-- 2. `ai-dev-run-codex.ps1`
--@@ -678,6 +678,11 @@
-- 4. `ai-dev-save-diff.ps1`
-- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
--+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
--+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
--+9. `-AllowCommit`과 `-CommitFiles`가 있을 때만 `ai-dev-commit.ps1 -Files`를 실행한다.
--+10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
--+11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-- 
-- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-- 
--@@ -691,7 +696,7 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-- ```
-- 
---기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
--리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
--+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전을
-- 위해 현재 task 한 개만 다룬다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
--MaxSteps 20
--@@ -709,13 +714,19 @@
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -MaxTasks 1
-- ```
-+`T007: ai-dev-auto-goal.ps1 DryRun 미리보기 문자열을 복구하고 goal/queue/state 생성 검증을 T001 시작 규칙에 맞게 강화했다. Pow
-e
-+rShell 문법, DryRun, Json 출력, 빈 GoalTitle 오류 경로를 확인했다.`
-+diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-+index 21176dc6aaf035988d7455fbb97b39eaff37ac50..e2d571744ecdebfdaa1c9c4e5e49c5e79ca82c09
-+--- a/scripts/ai-dev-auto-goal.ps1
-++++ b/scripts/ai-dev-auto-goal.ps1
-+@@ -297,6 +297,10 @@
-+         $errors += "queue.tasks must contain no more than three tasks."
-+     }
-  
--+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`과 `-CommitFiles`를 함께 지정한다. 
--full cycle은 안전을 위해 선택 파일 목록이 없으면 자동 커밋하지 않는다.
--+
--+```powershell
--+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 
--1
--+```
-++    if ($Queue.currentTaskId -ne "T001") {
-++        $errors += "queue.currentTaskId must be T001."
-++    }
- +
-- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
--odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-- 
-- ```powershell
-- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
--iewCodex -AllowDirty -MaxTasks 1
-- ```
-- 
---`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
--결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
--+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
--te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package
---lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 
--`commit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-- 
-- ### 목표 입력 기반 자동 실행
-- 
--diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
--index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..effe1a6dc47f7fb626e50e31b78fca6d3085ab76
----- a/scripts/ai-dev-auto-cycle-full.ps1
--+++ b/scripts/ai-dev-auto-cycle-full.ps1
--@@ -6,7 +6,8 @@
--     [switch]$AllowCodex,
--     [switch]$AllowReviewCodex,
--     [switch]$AllowCommit,
---    [switch]$AllowDirty
--+    [switch]$AllowDirty,
--+    [string[]]$CommitFiles
-- )
-- 
-- . $PSScriptRoot\ai-dev-env.ps1
--@@ -14,8 +15,10 @@
-- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-- $queueRelativePath = ".ai-dev/queue.json"
-- $stateRelativePath = ".ai-dev/state.json"
--+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-- $queuePath = Join-Path $repoRoot $queueRelativePath
-- $statePath = Join-Path $repoRoot $stateRelativePath
--+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-- 
-- function Test-HasValue {
--     param(
--@@ -141,9 +144,6 @@
--     Write-Host "Completed: $($Result.completed)"
--     Write-Host "Exit code: $($Result.exitCode)"
-- 
---    if ($AllowCommit) {
---        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
---    }
-- }
-+     $taskIds = @()
-  
-- function Stop-Cycle {
--@@ -202,6 +202,71 @@
--     return $scriptPath
-- }
-+     foreach ($task in $tasks) {
-+@@ -360,6 +364,19 @@
-+         $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-+     }
-  
--+function Invoke-GitCapture {
--+    param(
--+        [string[]]$Arguments,
--+        [string]$DisplayName
--+    )
--+
--+    $output = & git @Arguments 2>&1 | Out-String
--+    $exitCode = $LASTEXITCODE
--+
--+    if ($exitCode -ne 0) {
--+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
--+    }
--+
--+    return $output.TrimEnd()
--+}
--+
--+function Test-PackageFileChanged {
--+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
--on") "git status --porcelain -- package.json package-lock.json"
--+    return -not [string]::IsNullOrWhiteSpace($status)
--+}
--+
--+function Get-ReviewGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+    $nextStep = $null
--+
--+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
--+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
--+
--+        if (Test-HasValue $reviewResponse.next_step) {
--+            $nextStep = [string]$reviewResponse.next_step
--+        }
-++    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-++    if ($null -eq $firstTask) {
-++        $errors += "queue.tasks must include T001."
-++    } elseif ($firstTask.status -ne "in_progress") {
-++        $errors += "T001 must be in_progress."
- +    }
- +
--+    return [PSCustomObject][ordered]@{
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        decision = [string]$state.lastReviewDecision
--+        nextStep = $nextStep
--+    }
--+}
--+
--+function Get-CommitArguments {
--+    $arguments = @()
--+
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
--t [string]::IsNullOrWhiteSpace($_) })
--+
--+        if ($normalizedFiles.Count -gt 0) {
--+            $arguments += "-Files"
--+            $arguments += ($normalizedFiles -join ",")
-++    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-++        if ($laterTask.status -ne "pending") {
-++            $errors += "later tasks must be pending: $($laterTask.id)"
- +        }
- +    }
- +
--+    return $arguments
--+}
--+
--+function Get-CommitGate {
--+    $state = Read-JsonFile $statePath $stateRelativePath
--+
--+    return [PSCustomObject][ordered]@{
--+        lastCommand = [string]$state.lastCommand
--+        lastCommandStatus = [string]$state.lastCommandStatus
--+        lastCommitHash = [string]$state.lastCommitHash
--+    }
--+}
--+
-- Set-Location $repoRoot
-- 
-- $script:steps = @()
--@@ -249,6 +314,8 @@
--         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
--         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
--         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
--+        commit = Get-ScriptPath "ai-dev-commit.ps1"
--+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
--     }
-- } catch {
--     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
--sage
--@@ -261,7 +328,12 @@
--     "check",
--     "save-diff",
--     "make-review-prompt",
---    "run-review-codex"
--+    "run-review-codex",
--+    "review-gate",
--+    "package-change-gate",
--+    "commit",
--+    "commit-result-gate",
--+    "complete-task"
-- )
-- 
-- if ($plannedSteps.Count -gt $MaxSteps) {
--@@ -269,14 +341,14 @@
-- }
-- 
-- if ($MaxTasks -gt 1) {
---    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
--ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
--+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full cy
--cle은 안전을 위해 현재 task 하나만 실행합니다."
-- }
-- 
-- $stepNumber = 1
-- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
--i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-- $stepNumber++
-- 
---if (-not $AllowCodex) {
--+if (-not $AllowCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -303,7 +375,7 @@
-- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
--ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-- $stepNumber++
-- 
---if (-not $AllowReviewCodex) {
--+if (-not $AllowReviewCodex -and -not $DryRun) {
--     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
--wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
--     if ($AllowDirty) {
--         $command = "$command -AllowDirty"
--@@ -314,11 +386,103 @@
-+     return $errors
-  }
-  
-- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
--pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
--y", "-SaveReview")
--+$stepNumber++
--+
--+if ($DryRun) {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
--+    $stepNumber++
--+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Bypa
--ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "D
--ryRun: task 완료 처리를 실행하지 않았습니다."
--+    Stop-Cycle $script:steps "dry_run" $false 0
--+}
--+
--+try {
--+    $reviewGate = Get-ReviewGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false $
--false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "review_gate_failed" $false 1
--+}
--+
--+if ($reviewGate.lastCommandStatus -ne "passed") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $false
-- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
--+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
--+}
--+
--+if ($reviewGate.decision -ne "pass") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
--e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
--+    Stop-Cycle $script:steps "review_not_pass" $false 0
--+}
-- 
---$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
---if ($AllowCommit) {
---    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
--+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
--+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_s
--tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($revi
--ewGate.nextStep)"
--+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
--+}
--+
--+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $f
--alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
--+$stepNumber++
--+
--+try {
--+    if (Test-PackageFileChanged) {
--+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
-- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동 
--커밋하지 않습니다."
--+        Stop-Cycle $script:steps "package_files_changed" $false 1
--+    }
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
--package.json package-lock.json" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-- }
-+@@ -388,6 +405,10 @@
-+         $errors += "state.currentTaskId must match queue.currentTaskId."
-+     }
-  
---$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
--mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
---Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
--+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pack
--age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
--+$stepNumber++
--+
--+if (-not $AllowCommit) {
--+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1
-- -AllowCodex -AllowReviewCodex -AllowCommit -CommitFiles <files> -MaxTasks $MaxTasks"
--+    if ($AllowDirty) {
--+        $commitCommand = "$commitCommand -AllowDirty"
-++    if ($State.currentTaskId -ne "T001") {
-++        $errors += "state.currentTaskId must be T001."
- +    }
- +
--+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
--+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
--+    }
--+
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
--+    Stop-Cycle $script:steps "allow_commit_required" $false 1
--+}
--+
--+$commitArguments = Get-CommitArguments
--+
--+if ($commitArguments.Count -eq 0) {
--+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fil
--e scripts/ai-dev-commit.ps1 -Files <files>" $false $true 1 "자동 커밋에는 선택 파일 목록이 필요합니다. -CommitFiles 
--scripts/example.ps1,.ai-dev/README.md 형식으로 지정하세요."
--+    Stop-Cycle $script:steps "commit_files_required" $false 1
--+}
--+
--+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
--+if ($commitArguments.Count -gt 0) {
--+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
--+}
--+
--+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
--+$stepNumber++
--+
--+try {
--+    $commitGate = Get-CommitGate
--+} catch {
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $false 1 $_.Exception.Message
--+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
--+}
--+
--+if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -not 
--(Test-HasValue $commitGate.lastCommitHash)) {
--+    $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCommand
--), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitHash
--)"
--+    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
--tHash 확인" $false $true 1 $message
--+    Stop-Cycle $script:steps "commit_not_confirmed" $false 1
--+}
--+
--+$script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommitHas
--h 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
--+$stepNumber++
--+
--+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scripts
--/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Res
--ultSummary", "리뷰 pass 후 자동 커밋 완료")
--+Stop-Cycle $script:steps "completed" $true 0
-+     if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-+         $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-+     }
-+@@ -429,41 +450,44 @@
-+     )
-+ 
-+     $now = (Get-Date).ToUniversalTime().ToString("o")
-+-    $safeCommitTitle = $Title.Trim()
-++    $safeTitle = $Title.Trim()
-++    $safeDescription = $Description.Trim()
-+ 
-+     $plan = [PSCustomObject][ordered]@{
-+         goalMarkdown = @"
-+-# 紐⑺몴
-++# 목표
-+ 
-+-$safeCommitTitle
-++$safeTitle
-+ 
-+-## 諛곌꼍
-++## 배경
-+ 
-+-$($Description.Trim())
-++$safeDescription
-+ 
-+-## ?깃났 湲곗?
-++## 성공 기준
-+ 
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++- 입력한 목표에서 현재 작업을 준비한다.
-++- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-++- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-+ 
-+-## ?쒖빟?ы빆
-++## 제약사항
-+ 
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-++- 저장소 정책과 현재 작업 범위를 따른다.
-+ 
-+-## ?쒖쇅 踰붿쐞
-++## 범위 제외
-+ 
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++- 패키지 변경
-++- DB 삭제 또는 초기화
-++- 광범위한 리팩터링
-+ 
-+-## ?섎룞 ?뺤씤
-++## 수동 검증
-+ 
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++- DryRun 계획과 JSON 출력을 검토한다.
-++- 현재 작업 프롬프트 생성 여부를 확인한다.
-++- 필요한 경우 빌드와 검증은 별도로 실행한다.
-+ "@
-+         queue = [PSCustomObject][ordered]@{
-+-            goalTitle = $safeCommitTitle
-++            goalTitle = $safeTitle
-+             goalSource = $goalRelativePath
-+             createdAt = $now
-+             updatedAt = $now
-+@@ -471,16 +495,16 @@
-+             tasks = @(
-+                 [PSCustomObject][ordered]@{
-+                     id = "T001"
-+-                    title = $safeCommitTitle
-+-                    description = $Description.Trim()
-++                    title = $safeTitle
-++                    description = $safeDescription
-+                     type = "implementation"
-+                     status = "in_progress"
-+                     priority = "P0"
-+                     dependsOn = @()
-+                     filesLikelyToChange = @()
-+                     verification = @(
-+-                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-++                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-++                        "필요한 경우 npm run build를 별도로 실행한다."
-+                     )
-+                     commitMessage = $null
-+                 }
-+@@ -503,43 +527,6 @@
-+             stopReason = $null
-+         }
-+     }
-+-
-+-    $plan.goalMarkdown = @"
-+-# 紐⑺몴
-+-
-+-$safeCommitTitle
-+-
-+-## 諛곌꼍
-+-
-+-$($Description.Trim())
-+-
-+-## ?깃났 湲곗?
-+-
-+-- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븳??
-+-- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-+-- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-+-
-+-## ?쒖빟?ы빆
-+-
-+-- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?꾪뿕??git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-+-- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-+-
-+-## ?쒖쇅 踰붿쐞
-+-
-+-- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-+-
-+-## ?섎룞 ?뺤씤
-+-
-+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-+-- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-+-- ?꾩슂??寃쎌슦 鍮뚮뱶? 寃利앹? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-+-"@
-+-
-+-    $dryRunTask = @($plan.queue.tasks)[0]
-+-    $dryRunTask.verification = @(
-+-        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-+-        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-+-    )
-+ 
-+     return $plan
-+ }
- 
- tokens used
--63,636
--T005 구현 완료했습니다.
-+완료했습니다.
- 
- 변경한 파일:
--- [scripts/ai-dev-auto-cycle-full.ps1](D:/ai-apps/planpilot-local/scripts/ai-dev-auto-cycle-full.p
-s1)
--- [.ai-dev/README.md](D:/ai-apps/planpilot-local/.ai-dev/README.md)
-+- [scripts/ai-dev-auto-goal.ps1](D:/ai-apps/planpilot-local/scripts/ai-dev-auto-goal.ps1:301)
- 
- 구현 요약:
--- full auto-cycle에 `review-gate`, `package-change-gate`, `commit-result-gate`를 추가했습니다.
--- 리뷰 저장 결과가 `passed`이고 `lastReviewDecision`이 `pass`일 때만 커밋 단계로 진행합니다.
--- `review-response.json`의 `next_step`이 있으면 `complete_task`일 때만 진행합니다.
--- `package.json` 또는 `package-lock.json` 변경 감지 시 `package_files_changed`로 중단합니다.
--- `-AllowCommit`만으로는 커밋하지 않고, `-CommitFiles`가 지정된 경우에만 `ai-dev-commit.ps1 -Files`로 선택 파일 커밋을 수행합니다
-.
--- 커밋 후 `state.lastCommand=commit`, `lastCommandStatus=passed`, `lastCommitHash`가 확인될 때만 `complete-
-task`를 실행합니다.
--- README의 full auto-cycle 설명을 T005 기준으로 갱신했습니다.
-+- DryRun goal/task 미리보기의 깨진 한국어 문자열을 정상 한국어로 복구했습니다.
-+- Codex가 생성한 `queue/state` 검증을 강화했습니다.
-+  - `queue.currentTaskId`는 `T001`이어야 함
-+  - `state.currentTaskId`는 `T001`이어야 함
-+  - `T001` task가 존재하고 `in_progress`여야 함
-+  - 이후 task는 `pending`이어야 함
-+- `.ai-dev/README.md`에는 이미 `ai-dev-auto-goal.ps1` 사용법과 안전 기준이 문서화되어 있어 수정하지 않았습니다.
- 
- 검증:
--- PowerShell 문법 검증 통과:
--  - `Parser.ParseFile('scripts/ai-dev-auto-cycle-full.ps1', ...)`
--- DryRun 확인 통과:
--  - `powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks
- 1 -MaxSteps 20 -Json`
--  - 11단계가 표시되고, 하위 스크립트/build/git commit/complete-task는 실행되지 않았습니다.
--- `npm run build`, `npm run test`, `npm run lint`, 실제 git commit은 실행하지 않았습니다.
-+- PowerShell 문법 검증 통과
-+- `-DryRun` 텍스트 출력 확인
-+- `-DryRun -Json` 순수 JSON 출력 확인
-+- 공백/누락 `GoalTitle` 오류 종료 확인
-+- `-DryRun -AllowRun -Json`에서 full-cycle 호출이 실제 실행되지 않고 단계만 표시되는 것 확인
-+
-+실행하지 않은 검증:
-+- `npm run build`, `npm run lint`, `npm run test`는 지시 범위상 실행하지 않았습니다.
-+- 실제 Codex CLI 실행 및 실제 full cycle 실행은 파일 작성/외부 실행을 수반하므로 실행하지 않았습니다.
- 
- 남은 위험:
--- 실제 pass 리뷰 이후의 end-to-end 커밋/complete-task 실행은 T006의 작은 task 검증에서 확인해야 합니다.
--- package 변경, build 실패, non-pass 리뷰 중단은 현재 코드 게이트와 흐름 기준으로 확인했으며 실제 시나리오 실행은 하지 않았습니다.
-+- 실제 `codex exec` 출력 품질과 실제 dirty worktree 게이트는 실사용 환경에서 별도 확인이 필요합니다.
- 
- 다음 task로 넘어가도 됩니다.
- 
- `.ai-dev/loop-log.md` 기록용 요약:
--```markdown
--## T005 - 자동 커밋과 task 완료 연결
--
--- `ai-dev-auto-cycle-full.ps1`에 리뷰 pass 게이트, package 파일 변경 게이트, 커밋 결과 확인 게이트를 추가했다.
--- `-AllowCommit`과 `-CommitFiles`가 있을 때만 선택 파일 커밋을 실행하도록 제한했다.
--- 실제 commit hash가 state에 기록된 경우에만 `ai-dev-complete-task.ps1`을 실행하도록 연결했다.
--- `.ai-dev/README.md`의 full auto-cycle 설명을 T005 기준으로 갱신했다.
--- PowerShell 문법 검증과 full cycle DryRun을 확인했다.
--```
-+`T007: ai-dev-auto-goal.ps1 DryRun 미리보기 문자열을 복구하고 goal/queue/state 생성 검증을 T001 시작 규칙에 맞게 강화했다. Pow
-erShell 문법, DryRun, Json 출력, 빈 GoalTitle 오류 경로를 확인했다.`
-+71,363
+ `.ai-dev/loop-log.md` 기록용 요약:
+-`T007: ai-dev-auto-goal.ps1 DryRun 미리보기 문자열을 복구하고 goal/queue/state 생성 검증을 T001 시작 규칙에 맞게 강화했다. Pow
+erShell 문법, DryRun, Json 출력, 빈 GoalTitle 오류 경로를 확인했다.`
+-71,363
++- T001: `src/views/TasksView.tsx`의 업무 검색창 placeholder를 `업무 제목이나 메모로 검색`으로 변경했고, 기존 검색 입력 상태 및 필터링 
+연결은 유지함. 빌드/린트는 실행하지 않음.
  
  ```
 \ No newline at end of file
-diff --git a/.ai-dev/codex-review-result.md b/.ai-dev/codex-review-result.md
-index e0b4b4a..e8dba72 100644
---- a/.ai-dev/codex-review-result.md
-+++ b/.ai-dev/codex-review-result.md
-@@ -2,28 +2,25757 @@
- 
- ## Run
- 
--- Started at: 2026-06-07 23:04:34
--- Ended at: 2026-06-07 23:04:35
--- Exit code: 2
-+- Started at: 2026-06-09 22:51:24
-+- Ended at: 2026-06-09 22:52:24
-+- Exit code: 0
- - Review prompt: .ai-dev/review-prompt.md
- - Review response: .ai-dev/review-response.json
--- Command: codex exec <content from .ai-dev/review-prompt.md plus review safety rules>
-+- Command: codex exec <short wrapper pointing to .ai-dev/review-prompt.md>
- 
- ## Output
- 
- ```text
--node.exe : error: unexpected argument 'run' found
-+node.exe : OpenAI Codex v0.133.0
- At C:\Users\SECUI\AppData\Roaming\npm\codex.ps1:24 char:5
- +     & "node$exe"  "$basedir/node_modules/@openai/codex/bin/codex.js"  ...
- +     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
--    + CategoryInfo          : NotSpecified: (error: unexpected argument 'run' found:String) [], R 
--   emoteException
-+    + CategoryInfo          : NotSpecified: (OpenAI Codex v0.133.0:String) [], RemoteException
-     + FullyQualifiedErrorId : NativeCommandError
-  
-+--------
-+workdir: D:\ai-apps\planpilot-local
-+model: gpt-5.5
-+provider: openai
-+approval: never
-+sandbox: workspace-write [workdir, /tmp, $TMPDIR]
-+reasoning effort: medium
-+reasoning summaries: none
-+session id: 019eaca6-faae-7182-9b01-2fa69e452952
-+--------
-+user
-+Read and follow the full review prompt at this absolute file path: D:\ai-apps\planpilot-local\.ai-
-d
-+ev\review-prompt.md
-+codex
-+리뷰 지시문을 먼저 읽고, 그 범위 안에서만 필요한 파일을 확인하겠습니다. Git/빌드/테스트는 지시문이나 사용자 허용 없이는 실행하지 않겠습니다.
-+exec
-+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPat
-h
-+ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\review-prompt.md'" in D:\ai-apps\planpilot-local
-+ succeeded in 2758ms:
-+# AI Dev Review Prompt
- 
--Usage: codex exec [OPTIONS] [PROMPT]
--       codex exec [OPTIONS] <COMMAND> [ARGS]
-+## Role
- 
--For more information, try '--help'.
-+너는 이 저장소의 엄격한 코드 리뷰어다.
-+
-+## Review Goal
-+
-+- 현재 task의 변경사항이 목표와 일치하는지 검토한다.
-+- 빌드/테스트 결과와 git diff를 함께 검토한다.
-+- 다음 task 범위까지 미리 구현했는지 확인한다.
-+
-+## Project Goal
-+
-+# Goal
-+
-+AI Dev Loop Codex CLI 완전 자동화 도입
-+
-+## Background
-+
-+Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완료 처리를 가능한 한 end-to-end로 자동화한다.
-+
-+`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 
-생
-+성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
-+
-+초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commit
- 
-+금지 정책을 적용한다.
-+
-+## Success Criteria
-+
-+- Codex CLI 실행 정책이 문서화된다.
-+- `current-task-prompt.md`를 Codex CLI에 전달하는 `ai-dev-run-codex.ps1`이 추가된다.
-+- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-review-codex.ps1`이 추가된다.
-+- Codex 구현, build/check, diff, Codex 리뷰, save-review 흐름이 연결된다.
-+- pass 리뷰에서만 자동 커밋과 complete-task를 수행하는 full cycle 초안이 추가된다.
-+- git status가 dirty이면 Codex 실행을 중단한다.
-+- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-+- build 실패 시 자동 커밋하지 않는다.
-+- git reset, git clean, npm install은 자동 실행하지 않는다.
-+- PowerShell 문법 검증이 통과한다.
-+- 작은 앱 기능 task로 MaxTasks 1 end-to-end 검증이 가능하다.
-+
-+## Constraints
-+
-+- Codex CLI는 구현자와 리뷰어로 사용한다.
-+- GPT API 키 없이 진행한다.
-+- Cline은 삭제되어 사용하지 않는다.
-+- Copilot CLI와 gh는 현재 사용하지 않는다.
-+- 자동화는 명시적 Allow 옵션 없이는 위험한 단계를 실행하지 않는다.
-+- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
-+- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-+- build/check 실패 시 자동 커밋하지 않는다.
-+- git reset, git clean, npm install은 자동 실행하지 않는다.
-+- src 코드는 각 구현 task에서만 수정한다.
-+
-+## Out of Scope
-+
-+- GPT API 직접 호출
-+- Cline 사용
-+- Copilot CLI 사용
-+- gh 또는 GitHub PR 자동 연동
-+- git push 자동화
-+- git reset/git clean 같은 destructive git 명령
-+- npm install 자동 실행
-+- package 대량 교체
-+- DB 삭제, 초기화, 복원, 마이그레이션 자동화
-+
-+## Manual Verification
-+
-+- Codex CLI 실행 정책이 문서화되어 있다.
-+- Codex 구현 실행 스크립트가 DryRun과 dirty worktree 중단을 지원한다.
-+- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
-+- full auto-cycle DryRun이 전체 단계를 보여준다.
-+- full auto-cycle은 MaxTasks 1 제한과 Allow 옵션을 가진다.
-+- pass 리뷰가 아니면 자동 커밋하지 않는다.
-+- build 실패 시 자동 커밋하지 않는다.
-+- package 파일 변경 감지 시 자동 커밋하지 않는다.
-+- PowerShell 문법 검증이 통과한다.
-+- 작은 task로 MaxTasks 1 end-to-end 검증이 가능하다.
-+
-+
-+## Current Task
-+
-+- Task ID: T007
-+- Title: 목표 입력 기반 자동 goal 실행 스크립트 추가
-+- Description: 사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/queue/state를 생성하고, curren
-t
-+-task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다.
-+- Type: implementation
-+- Status: in_progress
-+- Priority: P0
-+- Depends on:
-+- T005
-+- Verification:
-+- DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인
-+- Json 출력이 가능한지 확인
-+- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-+- auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인
-+- GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인
-+- PowerShell 문법 검증
-+
-+## Test Result
-+
-+# AI Dev Test Result
-+
-+## 2026-06-09 22:51:08
-+
-+- Overall result: passed
-+- Current task: T007
-+- Commands:
-+  - npm run build: passed
-+  - npm run test: skipped
-+  - npm run lint: skipped
-+
-+### npm run build
-+
-+- Status: passed
-+- Exit code: 0
-+
-+```text
-+
-+> planpilot-local@0.0.0 build
-+> tsc -b && vite build
-+
-+[36mvite v8.0.10 [32mbuilding client environment for production...[36m[39m
-+[2K
-+transforming...✓ 48 modules transformed.
-+rendering chunks...
-+computing gzip size...
-+dist/index.html                   0.46 kB │ gzip:  0.29 kB
-+dist/assets/index-DvjxWt30.css    5.69 kB │ gzip:  1.93 kB
-+dist/assets/index-C8DmBEYa.js   315.70 kB │ gzip: 99.56 kB
-+
-+[32m✓ built in 288ms[39m
-+```
-+### npm run test
-+
-+- Status: skipped
-+- Exit code: 없음
-+
-+```text
-+-BuildOnly 옵션으로 건너뛰었습니다.
-+```
-+### npm run lint
-+
-+- Status: skipped
-+- Exit code: 없음
-+
-+```text
-+-BuildOnly 옵션으로 건너뛰었습니다.
-+```
-+
-+## Diff To Review
-+
-+# AI Dev Diff
-+
-+## Generated At
-+
-+2026-06-09 22:51:20
-+
-+## Git Status
-+
-+```text
-+ M .ai-dev/README.md
-+ M .ai-dev/codex-result.md
-+ M .ai-dev/codex-review-result.md
-+ M .ai-dev/current-task-prompt.md
-+ M .ai-dev/diff.md
-+ M .ai-dev/review-prompt.md
-+ M .ai-dev/review-response.json
-+ M .ai-dev/review.md
-+ M .ai-dev/state.json
-+ M .ai-dev/test-result.md
-+?? scripts/ai-dev-auto-goal.ps1
-+```
-+
-+## Unstaged Diff Stat
-+
-+```text
-+ .ai-dev/README.md              |    40 +-
-+ .ai-dev/codex-result.md        | 13217 +++++++-------------
-+ .ai-dev/codex-review-result.md | 25589 ++++++++++++++++++++++++++++++++++++++-
-+ .ai-dev/state.json             |     8 +-
-+ 4 files changed, 30094 insertions(+), 8760 deletions(-)
-+```
-+
-+## Unstaged Diff
-+
-+```text
-+diff --git a/.ai-dev/README.md b/.ai-dev/README.md
-+index 6f70632..8bf4b1a 100644
-+--- a/.ai-dev/README.md
-++++ b/.ai-dev/README.md
-+@@ -751,28 +751,52 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 
--
-+All
-+ 
-+ 최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 하는 것이다.
-+ 
-+-예상 최종 명령:
-++먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+ 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-++```
-++
-++자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-++DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 파일 작성 전에 생성될 goal/task 
-계
-+획을 검토할 수 있다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+ 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-++```
-++
-++실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이 모드는 `ai-dev-auto-cycle
--
-+full.ps1`을 호출하지 않는다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+ 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-++```
-++
-++생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowReviewCodex`, `-AllowComm
-i
-+t` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다. 실제
- 
-+구현까지 허용하려면 `-AllowCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일
-하
-+게 `-AllowCodex -AllowReviewCodex -AllowCommit`을 함께 지정한다.
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+ 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -All
-o
-+wCommit -MaxTasks 1
-+ ```
-+ 
-+-예정 동작:
-++동작:
-+ 
-+ 1. `GoalTitle`과 `GoalDescription`을 검증한다.
-+ 2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
-+-3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
-+-4. `.ai-dev/current-task-prompt.md`를 생성한다.
-+-5. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-++3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-++4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다.
-++5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
-++6. `.ai-dev/current-task-prompt.md`를 생성한다.
-++7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-++8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-task-prompt.md`, 결과 파일 
-때
-+문에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트는 a
-u
-+to-goal 시작 전에 별도로 유지한다.
-+ 
-+ 안전 기준:
-+ 
-+ - `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
-+-- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시한다.
-++- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
-+ - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-+-- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-+-- full cycle 호출은 `-AllowRun` 또는 그에 준하는 명시적 실행 옵션이 있을 때만 수행한다.
-++- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 않는다.
-++- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommit` 중 하나가 있을 때만 수행한다
-.
-++- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCodex`, `-AllowReviewCodex
-`
-+, `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
-+ - package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커밋하지 않는다.
-++- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
-+ 
-+ ## 운영 원칙
-+ 
-+diff --git a/.ai-dev/codex-result.md b/.ai-dev/codex-result.md
-+index 22a164a..6482f74 100644
-+--- a/.ai-dev/codex-result.md
-++++ b/.ai-dev/codex-result.md
-+@@ -2,11 +2,11 @@
-+ 
-+ ## Run
-+ 
-+-- Started at: 2026-06-07 22:58:54
-+-- Ended at: 2026-06-07 23:04:11
-++- Started at: 2026-06-09 22:45:10
-++- Ended at: 2026-06-09 22:51:00
-+ - Exit code: 0
-+ - Prompt: .ai-dev/current-task-prompt.md
-+-- Command: codex exec <content from .ai-dev/current-task-prompt.md plus safety rules>
-++- Command: codex exec <short wrapper pointing to .ai-dev/current-task-prompt.md>
-+ 
-+ ## Output
-+ 
-+@@ -26,9 +26,17 @@ approval: never
-+ sandbox: workspace-write [workdir, /tmp, $TMPDIR]
-+ reasoning effort: medium
-+ reasoning summaries: none
-+-session id: 019ea261-2436-7163-86a0-ea5663975656
-++session id: 019eaca1-44bc-7850-8d16-c0b79da16486
-+ --------
-+ user
-++Read and follow the full task prompt at this absolute file path: D:\ai-apps\planpilot-local\.ai-d
-e
-+v
-++\current-task-prompt.md
-++codex
-++작업 지시 파일을 먼저 읽어서 범위와 제약을 확인하겠습니다. 지정된 프롬프트 파일만 읽습니다.
-++exec
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPa
-t
-+h
-++ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\current-task-prompt.md'" in D:\ai-apps\planpilot-local
-++ succeeded in 1600ms:
-+ # Current Task Prompt
-+ 
-+ ## Role
-+@@ -45,11 +53,11 @@ AI Dev Loop Codex CLI 완전 자동화 도입
-+ 
-+ Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완료 처리를 가능한 한 end-to-end로 자동화한다.
-+ 
-+-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를
- 
-+-생성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
-++`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를
- 
-+생
-++성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
-+ 
-+-초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commi
-t
-+- 금지 정책을 적용한다.
-++초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commi
-t
-+ 
-++금지 정책을 적용한다.
-+ 
-+ ## Success Criteria
-+ 
-+@@ -106,14 +114,15 @@ Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완
-+ 
-+ ## Current Task
-+ 
-+-- Task ID: T005
-+-- Title: 자동 커밋과 task 완료 연결
-+-- Description: 리뷰 결과가 pass일 때만 선택 파일 커밋과 complete-task를 수행하도록 full auto-cycle에 연결한다.
-++- Task ID: T007
-++- Title: 목표 입력 기반 자동 goal 실행 스크립트 추가
-++- Description: 사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/queue/state를 생성하고, curre
-n
-+t
-++-task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다.
-+ - Type: implementation
-+ - Status: in_progress
-+ - Priority: P0
-+ - Depends on:
-+-- T004
-++- T005
-+ 
-+ ## Task Scope
-+ 
-+@@ -124,15 +133,16 @@ Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완
-+ 
-+ ## Likely Files
-+ 
-+-- scripts/ai-dev-auto-cycle-full.ps1
-++- scripts/ai-dev-auto-goal.ps1
-+ - .ai-dev/README.md
-+ 
-+ ## Verification
-+ 
-+-- pass가 아니면 commit하지 않는지 확인
-+-- build 실패 시 commit하지 않는지 확인
-+-- package 파일 변경 감지 시 중단하는지 확인
-+-- complete-task가 pass 이후에만 실행되는지 확인
-++- DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인
-++- Json 출력이 가능한지 확인
-++- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-++- auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인
-++- GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인
-+ - PowerShell 문법 검증
-+ 
-+ - 필요한 경우 `npm run build`는 사람이 별도로 실행한다.
-+@@ -167,557 +177,195 @@ Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완
-+ - 데이터 삭제 또는 마이그레이션이 필요한 경우
-+ - 같은 오류가 반복되는 경우
-+ 
-+-## Codex CLI Additional Safety Rules
-+-
-+-- git commit을 실행하지 않는다.
-+-- git reset, git checkout, git clean을 실행하지 않는다.
-+-- npm install을 실행하지 않는다.
-+-- package.json 또는 package-lock.json을 수정하지 않는다. 꼭 필요하면 작업을 중단하고 이유만 기록한다.
-+-- 현재 task 범위 밖 작업을 하지 않는다.
-+-- build, test, lint는 이 프롬프트가 명시적으로 요청하지 않는 한 실행하지 않는다.
-+ codex
-+-작업 범위가 `scripts/ai-dev-auto-cycle-full.ps1`와 `.ai-dev/README.md`로 보입니다. 먼저 현재 스크립트와 문서의 구조를 읽고, T
-0
-+-05 완료 조건에 이미 맞춰진 부분과 빠진 연결 지점만 확인하겠습니다.
-++T007 범위는 `scripts/ai-dev-auto-goal.ps1` 추가와 `.ai-dev/README.md` 문서 보강입니다. `src`는 건드리지 않고, 기존 AI d
-e
-+v
-++ 스크립트 구조를 확인한 뒤 같은 방식으로 붙이겠습니다.
-+ exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path scri
-p
-+-ts/ai-dev-auto-cycle-full.ps1' in D:\ai-apps\planpilot-local
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Literal
-P
-+a
-++th 'D:\\ai-apps\\planpilot-local\\scripts' | Select-Object -ExpandProperty Name" in D:\ai-apps\pl
-a
-+n
-++pilot-local
-+ exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path .ai-
-d
-+-ev/README.md' in D:\ai-apps\planpilot-local
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPa
-t
-+h
-++ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md'" in D:\ai-apps\planpilot-local
-+ exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-ChildItem -Path sc
-r
-+-ipts -Filter *.ps1' in D:\ai-apps\planpilot-local
-+- succeeded in 3593ms:
-+-param(
-+-    [int]$MaxTasks = 1,
-+-    [int]$MaxSteps = 20,
-+-    [switch]$DryRun,
-+-    [switch]$Json,
-+-    [switch]$AllowCodex,
-+-    [switch]$AllowReviewCodex,
-+-    [switch]$AllowCommit,
-+-    [switch]$AllowDirty
-+-)
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-ChildItem -Literal
-P
-+a
-++th 'D:\\ai-apps\\planpilot-local\\.ai-dev' | Select-Object -ExpandProperty Name" in D:\ai-apps\pl
-a
-+n
-++pilot-local
-++ succeeded in 2587ms:
-++ai-dev-auto-cycle-full.ps1
-++ai-dev-auto-cycle.ps1
-++ai-dev-auto-goal.ps1
-++ai-dev-auto-step.ps1
-++ai-dev-check.ps1
-++ai-dev-commit.ps1
-++ai-dev-complete-task.ps1
-++ai-dev-copy-review-prompt.ps1
-++ai-dev-current-task.ps1
-++ai-dev-env.ps1
-++ai-dev-init.ps1
-++ai-dev-make-prompt.ps1
-++ai-dev-make-review-prompt.ps1
-++ai-dev-make-revise-prompt.ps1
-++ai-dev-manual-cycle.ps1
-++ai-dev-next.ps1
-++ai-dev-run-codex.ps1
-++ai-dev-run-review-codex.ps1
-++ai-dev-save-diff.ps1
-++ai-dev-save-review.ps1
-++ai-dev-status.ps1
-++monitor-and-dev-loop.ps1
-++
-++ succeeded in 2572ms:
-++backlog.md
-++codex-result.md
-++codex-review-result.md
-++current-task-prompt.md
-++diff.md
-++extra-instructions.example.md
-++goal.md
-++loop-log.md
-++plan.md
-++queue.example.json
-++queue.json
-++README.md
-++review-prompt.md
-++review-response.json
-++review.md
-++state.example.json
-++state.json
-++test-result.md
-++
-++ succeeded in 2676ms:
-++# PlanPilot Local AI 개발 입력 폴더
-+ 
-+-. $PSScriptRoot\ai-dev-env.ps1
-++이 폴더는 PlanPilot Local의 Level 5 AI 자동 개발 루프에서 목표와 후속 작업 후보를 관리하기 위한 기본 공간이다.
-+ 
-+-$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+-$queueRelativePath = ".ai-dev/queue.json"
-+-$stateRelativePath = ".ai-dev/state.json"
-+-$queuePath = Join-Path $repoRoot $queueRelativePath
-+-$statePath = Join-Path $repoRoot $stateRelativePath
-++현재 단계에서는 사용자 입력 템플릿과 수동 AI Dev 루프 보조 스크립트를 제공한다. Codex CLI 완전 자동화 실행기는 다음 목표에서 단계적으로 도입하며, 각 task
- 
-+전
-++환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-+ 
-+-function Test-HasValue {
-+-    param(
-+-        [object]$Value
-+-    )
-++## 사람이 직접 관리하는 파일
-+ 
-+-    if ($null -eq $Value) {
-+-        return $false
-+-    }
-++### `goal.md`
-+ 
-+-    if ($Value -is [string]) {
-+-        return -not [string]::IsNullOrWhiteSpace($Value)
-+-    }
-++현재 자동 개발 루프가 수행할 하나의 목표를 작성한다.
-+ 
-+-    return $true
-+-}
-++- 목표는 결과 중심으로 작성한다.
-++- 완료 기준, 제약 조건, 범위 밖 항목, 수동 검증 방법을 함께 적는다.
-++- 한 번에 하나의 명확한 목표만 유지한다.
-++- 저장소의 `AGENTS.md`와 `docs/ai-dev-loop-policy.md`를 위반하는 목표는 실행하지 않는다.
-+ 
-+-function Read-JsonFile {
-+-    param(
-+-        [string]$Path,
-+-        [string]$RelativePath
-+-    )
-++### `backlog.md`
-+ 
-+-    try {
-+-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
-+-    } catch {
-+-        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
-+-    }
-+-}
-++추후 검토할 목표 후보와 아이디어를 우선순위별로 기록한다.
-+ 
-+-function Get-CurrentTask {
-+-    param(
-+-        [object]$Queue,
-+-        [object]$State
-+-    )
-++- backlog 항목은 자동 실행이 확정된 task가 아니다.
-++- 현재 단계에서는 AI가 backlog에서 다음 목표를 자동 선택하지 않는다.
-++- 실행할 항목은 사용자가 검토한 뒤 `goal.md`로 옮긴다.
-+ 
-+-    $tasks = @($Queue.tasks)
-+-    $currentTaskId = $null
-++## AI가 추후 생성하거나 수정할 파일
-+ 
-+-    if (Test-HasValue $State.currentTaskId) {
-+-        $currentTaskId = [string]$State.currentTaskId
-+-    } elseif (Test-HasValue $Queue.currentTaskId) {
-+-        $currentTaskId = [string]$Queue.currentTaskId
-+-    } else {
-+-        $inProgressTask = $tasks | Where-Object { $_.status -eq "in_progress" } | Select-Object -
-F
-+-irst 1
-++실제 자동 개발 루프를 구현하는 단계에서는 AI가 다음 파일을 생성하거나 갱신할 수 있다.
-+ 
-+-        if ($null -ne $inProgressTask) {
-+-            $currentTaskId = [string]$inProgressTask.id
-+-        } else {
-+-            $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -Fir
-s
-+-t 1
-++- `plan.md`
-++- `queue.json`
-++- `state.json`
-++- `test-result.md`
-++- `review.md`
-++- `loop-log.md`
-+ 
-+-            if ($null -ne $pendingTask) {
-+-                $currentTaskId = [string]$pendingTask.id
-+-            }
-+-        }
-+-    }
-++이 파일들은 목표 분석, task queue, 실행 상태, 검증 결과, 리뷰 결과, 실행 이력을 기록하기 위한 파일이다.
-+ 
-+-    if (-not (Test-HasValue $currentTaskId)) {
-+-        return $null
-+-    }
-++현재 단계에서는 위 파일을 만들지 않는다.
-+ 
-+-    return $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
-+-}
-++## 구조 예시 파일
-+ 
-+-function New-StepResult {
-+-    param(
-+-        [int]$Step,
-+-        [string]$Name,
-+-        [string]$Command,
-+-        [bool]$Executed,
-+-        [bool]$Skipped,
-+-        [int]$ExitCode,
-+-        [string]$Message
-+-    )
-++- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시다.
-++- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시다.
-+ 
-+-    return [PSCustomObject][ordered]@{
-+-        step = $Step
-+-        name = $Name
-+-        command = $Command
-+-        executed = $Executed
-+-        skipped = $Skipped
-+-        exitCode = $ExitCode
-+-        message = $Message
-+-    }
-+-}
-++예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복사 원본으로 사용한다.
-+ 
-+-function New-CycleResult {
-+-    param(
-+-        [object[]]$Steps,
-+-        [string]$StoppedReason,
-+-        [bool]$Completed,
-+-        [int]$ExitCode
-+-    )
-++## UTF-8 콘솔 설정
-+ 
-+-    return [PSCustomObject][ordered]@{
-+-        steps = @($Steps)
-+-        stoppedReason = $StoppedReason
-+-        completed = $Completed
-+-        exitCode = $ExitCode
-+-    }
-+-}
-++AI Dev PowerShell 스크립트의 UTF-8 콘솔 설정은 `scripts/ai-dev-env.ps1`에서 자동 처리한다. 사용자가 매번 `chcp 65001`을 직접
- 
-+실
-++행할 필요는 없다.
-+ 
-+-function Write-CycleResult {
-+-    param(
-+-        [object]$Result
-+-    )
-++기존에 한글이 깨진 `.ai-dev/*.md` 상태 파일은 필요한 경우 다음 명령으로 재초기화할 수 있다.
-+ 
-+-    if ($Json) {
-+-        $Result | ConvertTo-Json -Depth 20
-+-        return
-+-    }
-++```powershell
-++powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-init.ps1 -Force
-++```
-+ 
-+-    foreach ($step in @($Result.steps)) {
-+-        Write-Host "Step $($step.step): $($step.name)"
-+-        Write-Host "  Command: $($step.command)"
-+-        Write-Host "  Executed: $($step.executed)"
-+-        Write-Host "  Skipped: $($step.skipped)"
-+-        Write-Host "  Exit code: $($step.exitCode)"
-+-        Write-Host "  Message: $($step.message)"
-+-    }
-++## 실행 파일 초기화
-+ 
-+-    Write-Host "Stopped reason: $($Result.stoppedReason)"
-+-    Write-Host "Completed: $($Result.completed)"
-+-    Write-Host "Exit code: $($Result.exitCode)"
-++프로젝트 루트에서 다음 명령을 실행하면 실제 실행용 상태 파일과 기본 Markdown 템플릿을 생성한다.
-+ 
-+-    if ($AllowCommit) {
-+-        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+-    }
-+-}
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1
-++```
-+ 
-+-function Stop-Cycle {
-+-    param(
-+-        [object[]]$Steps,
-+-        [string]$StoppedReason,
-+-        [bool]$Completed,
-+-        [int]$ExitCode
-+-    )
-++이미 생성된 파일을 덮어쓰려면 `-Force` 옵션을 사용한다.
-+ 
-+-    $result = New-CycleResult $Steps $StoppedReason $Completed $ExitCode
-+-    Write-CycleResult $result
-+-    exit $ExitCode
-+-}
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1 -Force
-++```
-+ 
-+-function Invoke-CycleCommand {
-+-    param(
-+-        [int]$StepNumber,
-+-        [string]$Name,
-+-        [string]$Command,
-+-        [string]$ScriptPath,
-+-        [string[]]$Arguments
-+-    )
-++초기화 스크립트는 Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
-+ 
-+-    if ($DryRun) {
-+-        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: 하위 스크립
-트
-+-를 실행하지 않았습니다."
-+-        return
-+-    }
-++## 현재 task 확인
-+ 
-+-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
-+-    $exitCode = $LASTEXITCODE
-+-    $message = $output.Trim()
-++`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-+ 
-+-    if ([string]::IsNullOrWhiteSpace($message)) {
-+-        $message = "완료"
-+-    }
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1
-++```
-+ 
-+-    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-++현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-+ 
-+-    if ($exitCode -ne 0) {
-+-        Stop-Cycle $script:steps "$Name`_failed" $false 1
-+-    }
-+-}
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1 -Json
-++```
-+ 
-+-function Get-ScriptPath {
-+-    param(
-+-        [string]$Name
-+-    )
-++현재 task 확인 스크립트는 상태 파일을 수정하지 않으며, Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
-+ 
-+-    $scriptPath = Join-Path $PSScriptRoot $Name
-++## 현재 task 프롬프트 생성
-+ 
-+-    if (-not (Test-Path -LiteralPath $scriptPath -PathType Leaf)) {
-+-        throw "필수 스크립트를 찾을 수 없습니다: scripts/$Name"
-+-    }
-++`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai-dev/current-task-prompt.md` 파일
-을
-+ 
-++생성한다.
-+ 
-+-    return $scriptPath
-+-}
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
-++```
-+ 
-+-Set-Location $repoRoot
-++프롬프트 생성 스크립트는 기존 `current-task-prompt.md`를 현재 task 기준으로 덮어쓴다. Codex 실행, GPT 리뷰, build/test 실행, gi
-t
-+ 
-++commit은 수행하지 않는다.
-+ 
-+-$script:steps = @()
-++기본 추가 지시 파일인 `.ai-dev/extra-instructions.md`가 있으면 해당 내용을 프롬프트 끝의 `Extra Instructions` 섹션에 포함한다. 이
- 
-+파
-++일이 없으면 기존과 동일하게 goal, queue, state만 사용해 프롬프트를 생성한다.
-+ 
-+-if ($MaxTasks -lt 1) {
-+-    Stop-Cycle $script:steps "max_tasks_must_be_at_least_1" $false 1
-+-}
-++다른 추가 지시 파일을 사용하려면 `-ExtraInstructionsPath`를 지정한다.
-+ 
-+-if ($MaxSteps -lt 1) {
-+-    Stop-Cycle $script:steps "max_steps_must_be_at_least_1" $false 1
-+-}
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1 -ExtraInstructionsPath .a
-i
-+-
-++dev/extra-instructions.example.md
-++```
-+ 
-+-try {
-+-    foreach ($requiredPath in @($queuePath, $statePath)) {
-+-        if (-not (Test-Path -LiteralPath $requiredPath -PathType Leaf)) {
-+-            throw "필수 상태 파일이 없습니다: $requiredPath"
-+-        }
-+-    }
-++추가 지시는 goal, queue, state 내용을 덮어쓰지 않는다. `current-task-prompt.md`를 직접 수정하지 말고, 반복해서 필요한 보조 지시는 `.a
-i
-+-
-++dev/extra-instructions.md` 또는 별도 파일에 작성한다.
-+ 
-+-    $queue = Read-JsonFile $queuePath $queueRelativePath
-+-    $state = Read-JsonFile $statePath $stateRelativePath
-++## task 완료 처리
-+ 
-+-    if ($state.goalStatus -eq "completed") {
-+-        Stop-Cycle $script:steps "goal_completed" $true 0
-+-    }
-++현재 task를 완료 처리하고 다음 pending task로 이동하려면 다음 명령을 실행한다.
-+ 
-+-    if (-not ($queue.PSObject.Properties.Name -contains "tasks") -or $null -eq $queue.tasks) {
-+-        Stop-Cycle $script:steps "no_task" $false 0
-+-    }
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary "T001 분석
- 
-+완
-++료"
-++```
-+ 
-+-    $currentTask = Get-CurrentTask $queue $state
-+-
-+-    if ($null -eq $currentTask) {
-+-        Stop-Cycle $script:steps "no_task" $false 0
-+-    }
-+-
-+-    if ($currentTask.status -eq "done") {
-+-        Stop-Cycle $script:steps "current_task_done" $true 0
-+-    }
-+-
-+-    $scriptPaths = @{
-+-        makePrompt = Get-ScriptPath "ai-dev-make-prompt.ps1"
-+-        runCodex = Get-ScriptPath "ai-dev-run-codex.ps1"
-+-        check = Get-ScriptPath "ai-dev-check.ps1"
-+-        saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-        makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-        runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-    }
-+-} catch {
-+-    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
-s
-+-age
-+-    Stop-Cycle $script:steps "prepare_failed" $false 1
-+-}
-+-
-+-$plannedSteps = @(
-+-    "make-prompt",
-+-    "run-codex",
-+-    "check",
-+-    "save-diff",
-+-    "make-review-prompt",
-+-    "run-review-codex"
-+-)
-+-
-+-if ($plannedSteps.Count -gt $MaxSteps) {
-+-    Stop-Cycle $script:steps "max_steps_too_small_for_full_cycle" $false 1
-+-}
-+-
-+-if ($MaxTasks -gt 1) {
-+-    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 t
-a
-+-sk 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-}
-+-
-+-$stepNumber = 1
-+-Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/a
-i
-+--dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-+-$stepNumber++
-+-
-+-if (-not $AllowCodex) {
-+-    $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
-w
-+-Codex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-    if ($AllowDirty) {
-+-        $command = "$command -AllowDirty"
-+-    }
-+-
-+-    $script:steps += New-StepResult $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass -
-F
-+-ile scripts/ai-dev-run-codex.ps1" $false $true 1 "Codex 구현 실행에는 -AllowCodex가 필요합니다. 추천 명령: $comma
-n
-+-d"
-+-    Stop-Cycle $script:steps "allow_codex_required" $false 1
-+-}
-+-
-+-$runCodexArguments = @()
-+-if ($AllowDirty) {
-+-    $runCodexArguments += "-AllowDirty"
-+-}
-+-
-+-Invoke-CycleCommand $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass -File scripts/ai-
-d
-+-ev-run-codex.ps1" $scriptPaths.runCodex $runCodexArguments
-+-$stepNumber++
-+-
-+-Invoke-CycleCommand $stepNumber "check" "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-
-c
-+-heck.ps1 -BuildOnly" $scriptPaths.check @("-BuildOnly")
-+-$stepNumber++
-+-
-+-Invoke-CycleCommand $stepNumber "save-diff" "powershell -ExecutionPolicy Bypass -File scripts/ai-
-d
-+-ev-save-diff.ps1" $scriptPaths.saveDiff @()
-+-$stepNumber++
-+-
-+-Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File sc
-r
-+-ipts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-+-$stepNumber++
-+-
-+-if (-not $AllowReviewCodex) {
-+-    $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -Allo
-w
-+-Codex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-    if ($AllowDirty) {
-+-        $command = "$command -AllowDirty"
-+-    }
-+-
-+-    $script:steps += New-StepResult $stepNumber "run-review-codex" "powershell -ExecutionPolicy B
-y
-+-pass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $false $true 1 "Codex 리뷰 
-실
-+-행에는 -AllowReviewCodex가 필요합니다. 추천 명령: $command"
-+-    Stop-Cycle $script:steps "allow_review_codex_required" $false 1
-+-}
-+-
-+-Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scri
-p
-+-ts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDirt
-y
-+-", "-SaveReview")
-+-
-+-$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+-if ($AllowCommit) {
-+-    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-}
-+-
-+-$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-co
-m
-+-mit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+-Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-
-+- succeeded in 3595ms:
-+-
-+-
-+-    디렉터리: D:\ai-apps\planpilot-local\scripts
-+-
-+-
-+-Mode                 LastWriteTime         Length Name                                           
- 
-+-                     
-+-----                 -------------         ------ ----                                           
- 
-+-                     
-+--a----        2026-06-07  오후 10:30           9919 ai-dev-auto-cycle-full.ps1                     
- 
-+-                     
-+--a----        2026-06-07   오후 3:14           5451 ai-dev-auto-cycle.ps1                          
- 
-+-                     
-+--a----        2026-06-07   오후 3:03           8471 ai-dev-auto-step.ps1                           
- 
-+-                     
-+--a----        2026-06-07   오후 4:07           7762 ai-dev-check.ps1                               
- 
-+-                     
-+--a----        2026-06-03  오후 11:54          10635 ai-dev-commit.ps1                              
- 
-+-                     
-+--a----        2026-06-03  오후 10:14           5440 ai-dev-complete-task.ps1                       
- 
-+-                     
-+--a----        2026-06-04   오후 4:24           3805 ai-dev-copy-review-prompt.ps1                  
- 
-+-                     
-+--a----        2026-06-03  오후 10:14           5446 ai-dev-current-task.ps1                        
- 
-+-                     
-+--a----        2026-06-03  오후 10:14            309 ai-dev-env.ps1                                 
- 
-+-                     
-+--a----        2026-06-03  오후 10:14           3085 ai-dev-init.ps1                                
- 
-+-                     
-+--a----        2026-06-04  오전 12:01           7243 ai-dev-make-prompt.ps1                         
- 
-+-                     
-+--a----        2026-06-03  오후 10:14           8050 ai-dev-make-review-prompt.ps1                  
- 
-+-                     
-+--a----        2026-06-03  오후 10:14          11227 ai-dev-make-revise-prompt.ps1                  
- 
-+-                     
-+--a----        2026-06-03  오후 10:14           2692 ai-dev-manual-cycle.ps1                        
- 
-+-                     
-+--a----        2026-06-03  오후 10:14          12771 ai-dev-next.ps1                                
- 
-+-                     
-+--a----        2026-06-07   오후 9:55           6510 ai-dev-run-codex.ps1                           
- 
-+-                     
-+--a----        2026-06-07  오후 10:19          12076 ai-dev-run-review-codex.ps1                    
- 
-+-                     
-+--a----        2026-06-03  오후 11:44          10302 ai-dev-save-diff.ps1                           
- 
-+-                     
-+--a----        2026-06-07   오후 3:57          13077 ai-dev-save-review.ps1                         
- 
-+-                     
-+--a----        2026-06-03  오후 10:14          10013 ai-dev-status.ps1                              
- 
-+-                     
-+--a----        2026-05-24   오후 1:50           9682 monitor-and-dev-loop.ps1                       
- 
-+-                     
-+-
-+-
-+-
-+- succeeded in 3698ms:
-+-# PlanPilot Local AI 개발 입력 폴더
-+-
-+-이 폴더는 PlanPilot Local의 Level 5 AI 자동 개발 루프에서 목표와 후속 작업 후보를 관리하기 위한 기본 공간이다.
-+-
-+-현재 단계에서는 사용자 입력 템플릿과 수동 AI Dev 루프 보조 스크립트를 제공한다. Codex CLI 완전 자동화 실행기는 다음 목표에서 단계적으로 도입하며, 각 task
- 
-+-전환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-+-
-+-## 사람이 직접 관리하는 파일
-+-
-+-### `goal.md`
-+-
-+-현재 자동 개발 루프가 수행할 하나의 목표를 작성한다.
-+-
-+-- 목표는 결과 중심으로 작성한다.
-+-- 완료 기준, 제약 조건, 범위 밖 항목, 수동 검증 방법을 함께 적는다.
-+-- 한 번에 하나의 명확한 목표만 유지한다.
-+-- 저장소의 `AGENTS.md`와 `docs/ai-dev-loop-policy.md`를 위반하는 목표는 실행하지 않는다.
-+-
-+-### `backlog.md`
-+-
-+-추후 검토할 목표 후보와 아이디어를 우선순위별로 기록한다.
-+-
-+-- backlog 항목은 자동 실행이 확정된 task가 아니다.
-+-- 현재 단계에서는 AI가 backlog에서 다음 목표를 자동 선택하지 않는다.
-+-- 실행할 항목은 사용자가 검토한 뒤 `goal.md`로 옮긴다.
-+-
-+-## AI가 추후 생성하거나 수정할 파일
-+-
-+-실제 자동 개발 루프를 구현하는 단계에서는 AI가 다음 파일을 생성하거나 갱신할 수 있다.
-+-
-+-- `plan.md`
-+-- `queue.json`
-+-- `state.json`
-+-- `test-result.md`
-+-- `review.md`
-+-- `loop-log.md`
-+-
-+-이 파일들은 목표 분석, task queue, 실행 상태, 검증 결과, 리뷰 결과, 실행 이력을 기록하기 위한 파일이다.
-+-
-+-현재 단계에서는 위 파일을 만들지 않는다.
-+-
-+-## 구조 예시 파일
-+-
-+-- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시다.
-+-- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시다.
-+-
-+-예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복사 원본으로 사용한다.
-+-
-+-## UTF-8 콘솔 설정
-+-
-+-AI Dev PowerShell 스크립트의 UTF-8 콘솔 설정은 `scripts/ai-dev-env.ps1`에서 자동 처리한다. 사용자가 매번 `chcp 65001`을 직접
- 
-+-실행할 필요는 없다.
-+-
-+-기존에 한글이 깨진 `.ai-dev/*.md` 상태 파일은 필요한 경우 다음 명령으로 재초기화할 수 있다.
-+-
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-init.ps1 -Force
-+-```
-+-
-+-## 실행 파일 초기화
-+-
-+-프로젝트 루트에서 다음 명령을 실행하면 실제 실행용 상태 파일과 기본 Markdown 템플릿을 생성한다.
-+-
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1
-+-```
-+-
-+-이미 생성된 파일을 덮어쓰려면 `-Force` 옵션을 사용한다.
-+-
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-init.ps1 -Force
-+-```
-+-
-+-초기화 스크립트는 Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
-+-
-+-## 현재 task 확인
-+-
-+-`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-+-
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1
-+-```
-+-
-+-현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-+-
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.ps1 -Json
-+-```
-+-
-+-현재 task 확인 스크립트는 상태 파일을 수정하지 않으며, Codex 실행, GPT 리뷰, build/test 실행, git commit을 수행하지 않는다.
-+-
-+-## 현재 task 프롬프트 생성
-+-
-+-`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai-dev/current-task-prompt.md` 파일
-을
-+- 생성한다.
-+-
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1
-+-```
-+-
-+-프롬프트 생성 스크립트는 기존 `current-task-prompt.md`를 현재 task 기준으로 덮어쓴다. Codex 실행, GPT 리뷰, build/test 실행, gi
-t
-+- commit은 수행하지 않는다.
-+-
-+-기본 추가 지시 파일인 `.ai-dev/extra-instructions.md`가 있으면 해당 내용을 프롬프트 끝의 `Extra Instructions` 섹션에 포함한다. 이
- 
-+-파일이 없으면 기존과 동일하게 goal, queue, state만 사용해 프롬프트를 생성한다.
-+-
-+-다른 추가 지시 파일을 사용하려면 `-ExtraInstructionsPath`를 지정한다.
-+-
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-prompt.ps1 -ExtraInstructionsPath .a
-i
-+--dev/extra-instructions.example.md
-+-```
-+-
-+-추가 지시는 goal, queue, state 내용을 덮어쓰지 않는다. `current-task-prompt.md`를 직접 수정하지 말고, 반복해서 필요한 보조 지시는 `.a
-i
-+--dev/extra-instructions.md` 또는 별도 파일에 작성한다.
-+-
-+-## task 완료 처리
-+-
-+-현재 task를 완료 처리하고 다음 pending task로 이동하려면 다음 명령을 실행한다.
-+-
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary "T001 분석
- 
-+-완료"
-+-```
-+-
-+-특정 task를 완료 처리하려면 `-TaskId`를 지정한다.
-++특정 task를 완료 처리하려면 `-TaskId`를 지정한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -TaskId T001 -ResultSum
-m
-+-ary "분석 완료"
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -TaskId T001 -ResultSum
-m
-+a
-++ry "분석 완료"
-+ ```
-+ 
-+ 다음 task로 자동 이동하지 않으려면 `-NoNext` 옵션을 사용한다.
-+@@ -726,8 +374,8 @@ ary "분석 완료"
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1 -NoNext
-+ ```
-+ 
-+-task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행
-,
-+- git commit은 수행하지 않는다.
-++task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행
-,
-+ 
-++git commit은 수행하지 않는다.
-+ 
-+ ## 프로젝트 검증 실행
-+ 
-+@@ -747,23 +395,23 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -BuildOnly
-+ 
-+ 검증 실행 스크립트는 Codex 실행, GPT 리뷰, git commit을 수행하지 않는다.
-+ 
-+-T006 같은 최종 검증 task에서 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인
- 
-+-결과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build/test/lint를 실행하지 않는다.
-++T006 같은 최종 검증 task에서 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인
- 
-+결
-++과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build/test/lint를 실행하지 않는다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -ManualSummaryOnly -ManualSumma
-r
-+-y "PowerShell 문법 검증 통과. auto-step DryRun 통과. auto-cycle DryRun 통과. save-review 실패/성공 흐름 확인."
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1 -ManualSummaryOnly -ManualSumma
-r
-+y
-++ "PowerShell 문법 검증 통과. auto-step DryRun 통과. auto-cycle DryRun 통과. save-review 실패/성공 흐름 확인."
-+ ```
-+ 
-+ 수동 요약 모드는 실제 명령 실행 결과를 조작하는 용도가 아니다. 실행하지 않은 검증은 통과로 적지 말고, 수행한 명령과 결과만 요약한다.
-+ 
-+-이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다. 실제 검증 통과/실패/미수행 판단은 `test-resu
-l
-+-t.md`의 요약 본문에 명확히 적는다.
-++이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다. 실제 검증 통과/실패/미수행 판단은 `test-resu
-l
-+t
-++.md`의 요약 본문에 명확히 적는다.
-+ 
-+ ## git diff 저장
-+ 
-+-현재 git 변경사항을 `diff.md`에 저장해 GPT 리뷰 또는 사람 검토에 사용할 수 있게 한다. 기본 동작은 untracked 파일명을 git status에 표시하지만
- 
-+-파일 내용은 포함하지 않는다.
-++현재 git 변경사항을 `diff.md`에 저장해 GPT 리뷰 또는 사람 검토에 사용할 수 있게 한다. 기본 동작은 untracked 파일명을 git status에 표시하지만
- 
-+파
-++일 내용은 포함하지 않는다.
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1
-+@@ -775,12 +423,12 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-diff.ps1 -IncludeUntrackedContent
-+ ```
-+ 
-+-git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행, git commit은 수행하
-지
-+- 않는다.
-++git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰, build/test 실행, git commit은 수행하
-지
-+ 
-++않는다.
-+ 
-+-다음 생성 산출물은 자기 중첩과 과도한 크기 증가를 줄이기 위해 Unstaged/Staged Diff와 Diff Stat 대상에서 기본 제외한다. tracked 변경 파일은 
-`
-+-Skipped Generated AI Dev Artifact Diffs` 섹션에 파일명만 기록한다. `-IncludeUntrackedContent`를 사용한 경우 untrac
-k
-+-ed 생성 산출물도 내용 포함에서 제외하고 `Skipped Generated AI Dev Artifacts` 섹션에 파일명만 기록한다.
-++다음 생성 산출물은 자기 중첩과 과도한 크기 증가를 줄이기 위해 Unstaged/Staged Diff와 Diff Stat 대상에서 기본 제외한다. tracked 변경 파일은 
-`
-+S
-++kipped Generated AI Dev Artifact Diffs` 섹션에 파일명만 기록한다. `-IncludeUntrackedContent`를 사용한 경우 untrack
-e
-+d
-++ 생성 산출물도 내용 포함에서 제외하고 `Skipped Generated AI Dev Artifacts` 섹션에 파일명만 기록한다.
-+ 
-+ - `.ai-dev/diff.md`
-+ - `.ai-dev/review-prompt.md`
-+@@ -792,8 +440,8 @@ ed 생성 산출물도 내용 포함에서 제외하고 `Skipped Generated AI De
-+ 
-+ git diff/stat 명령의 stderr 경고는 diff 본문과 분리한다. git 명령이 실패하면 기존처럼 `state.json`에 실패 결과를 기록하고 종료한다.
-+ 
-+-`Git Status` 섹션에는 생성 산출물을 포함한 전체 변경 파일을 그대로 표시한다. `.ai-dev/README.md`, `scripts/ai-dev-save-diff.
-p
-+-s1`처럼 실제 작업 대상 파일은 위 제외 목록에 없으므로 diff 본문에 포함된다.
-++`Git Status` 섹션에는 생성 산출물을 포함한 전체 변경 파일을 그대로 표시한다. `.ai-dev/README.md`, `scripts/ai-dev-save-diff.
-p
-+s
-++1`처럼 실제 작업 대상 파일은 위 제외 목록에 없으므로 diff 본문에 포함된다.
-+ 
-+ ## GPT 리뷰 프롬프트 생성
-+ 
-+@@ -813,8 +461,8 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-review-prompt.ps1
- 
-+-
-+ 
-+ ## 수동 GPT 리뷰 브리지
-+ 
-+-GPT API 키가 없는 환경에서는 ChatGPT 웹 화면을 사용해 리뷰를 수동으로 진행한다. AI Dev Loop는 prompt 생성과 저장 흐름만 돕고, ChatGPT 웹
- 
-+-화면에 자동 접속하거나 GPT API를 직접 호출하지 않는다.
-++GPT API 키가 없는 환경에서는 ChatGPT 웹 화면을 사용해 리뷰를 수동으로 진행한다. AI Dev Loop는 prompt 생성과 저장 흐름만 돕고, ChatGPT 웹
- 
-+화
-++면에 자동 접속하거나 GPT API를 직접 호출하지 않는다.
-+ 
-+ 기본 흐름:
-+ 
-+@@ -826,8 +474,8 @@ GPT API 키가 없는 환경에서는 ChatGPT 웹 화면을 사용해 리뷰를
-+ 6. `ai-dev-save-review.ps1 -FromClipboard`로 리뷰를 저장한다.
-+ 7. 저장된 decision에 따라 `pass`, `revise`, `blocked` 흐름을 계속 진행한다.
-+ 
-+-현재 task의 다음 단계에서는 `review-prompt.md` 클립보드 복사를 돕는 helper를 추가할 예정이다. 그 전까지는 파일 내용을 직접 복사하거나 운영 환경에 
-맞
-+-는 안전한 방법으로 클립보드에 옮긴다.
-++현재 task의 다음 단계에서는 `review-prompt.md` 클립보드 복사를 돕는 helper를 추가할 예정이다. 그 전까지는 파일 내용을 직접 복사하거나 운영 환경에 
-맞
-+는
-++ 안전한 방법으로 클립보드에 옮긴다.
-+ 
-+ `review-prompt.md`가 이미 있으면 다음 명령으로 내용을 클립보드에 복사한다.
-+ 
-+@@ -844,8 +492,8 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
- 
-+-
-+ 엄격한 리뷰 프롬프트를 생성한 뒤 복사하려면 `-Strict`를 함께 사용한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -GenerateIfMissing
- 
-+--Strict
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1 -GenerateIfMissing
- 
-+-
-++Strict
-+ ```
-+ 
-+ 자동화에서 결과만 읽고 싶으면 `-Json` 옵션을 사용한다.
-+@@ -872,21 +520,21 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -Fro
-m
-+Cli
-+ GPT Chat 또는 외부 리뷰어가 반환한 리뷰 JSON을 `review.md`에 저장하고, decision과 severity를 `state.json`에 반영한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewFile .ai-dev/revie
-w
-+--response.json
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewFile .ai-dev/revie
-w
-+-
-++response.json
-+ ```
-+ 
-+ JSON 문자열이나 클립보드 내용을 직접 사용할 수도 있다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewJson '{"decision":
-"
-+-pass","severity":"none","summary":"OK","required_changes":[],"optional_suggestions":[],"next_step
-"
-+-:"complete_task"}'
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -ReviewJson '{"decision":
-"
-+p
-++ass","severity":"none","summary":"OK","required_changes":[],"optional_suggestions":[],"next_step"
-:
-+"
-++complete_task"}'
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
-+ ```
-+ 
-+-ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboard`를 실행한다. 순수 JSON 객체, `json` 코
-드
-+-블록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-++ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboard`를 실행한다. 순수 JSON 객체, `json` 코
-드
-+블
-++록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-+ 
-+ 저장 실패 시 확인할 항목:
-+ 
-+@@ -895,20 +543,20 @@ ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를
-+ - Markdown 코드블록을 포함해도 되지만, 가능하면 JSON만 복사한다.
-+ - 오류 메시지의 입력 preview 앞부분을 보고 다른 설명 문장이나 잘린 JSON이 섞였는지 확인한다.
-+ 
-+-파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수정하지 않는다. review-prompt나 명령어를 실수
-로
-+- 클립보드에 둔 상태에서 `-FromClipboard`를 실행해도 기존 리뷰 상태를 blocked/critical로 덮어쓰지 않기 위한 안전 동작이다.
-++파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수정하지 않는다. review-prompt나 명령어를 실수
-로
-+ 
-++클립보드에 둔 상태에서 `-FromClipboard`를 실행해도 기존 리뷰 상태를 blocked/critical로 덮어쓰지 않기 위한 안전 동작이다.
-+ 
-+ 실패 자체를 리뷰 결과로 기록해야 하는 특별한 경우에만 `-WriteFailureReview`를 함께 사용한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard -WriteFail
-u
-+-reReview
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard -WriteFail
-u
-+r
-++eReview
-+ ```
-+ 
-+ 잘못 실행했다면 ChatGPT가 출력한 JSON 리뷰만 다시 클립보드에 복사한 뒤 `-FromClipboard`를 재실행한다.
-+ 
-+-리뷰 결과 저장 스크립트는 `pass + complete_task`, `revise + revise_with_codex`, `blocked` 또는 `stop_for_user`
- 
-+-결과에 맞는 다음 행동을 안내만 한다. GPT API 호출, Codex 실행, build/test 실행, git commit은 수행하지 않는다.
-++리뷰 결과 저장 스크립트는 `pass + complete_task`, `revise + revise_with_codex`, `blocked` 또는 `stop_for_user`
- 
-+결
-++과에 맞는 다음 행동을 안내만 한다. GPT API 호출, Codex 실행, build/test 실행, git commit은 수행하지 않는다.
-+ 
-+ ## 리뷰 반영 재수정 프롬프트 생성
-+ 
-+@@ -921,8 +569,8 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1
-+ optional suggestions도 현재 task 범위 안에서 반영할 수 있게 하려면 `-AllowOptionalSuggestions` 옵션을 사용한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1 -AllowOptionalSugg
-e
-+-stions
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1 -AllowOptionalSugg
-e
-+s
-++tions
-+ ```
-+ 
-+ 재수정 프롬프트 생성 스크립트는 Codex 실행, GPT API 호출, build/test 실행, git commit을 수행하지 않는다.
-+@@ -939,24 +587,24 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1
-+ 지정한 파일만 커밋 대상으로 확인하거나 실제로 커밋하려면 `-Files` 옵션을 사용한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.p
-s
-+-1,.ai-dev/README.md -DryRun
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.p
-s
-+-1,.ai-dev/README.md
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.p
-s
-+1
-++,.ai-dev/README.md -DryRun
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Files scripts/ai-dev-commit.p
-s
-+1
-++,.ai-dev/README.md
-+ ```
-+ 
-+ 커밋 메시지를 직접 지정하거나 검증·리뷰 게이트를 명시적으로 우회할 수도 있다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Message "chore(ai-dev): add a
-u
-+-tomation helpers"
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -AllowWithoutPassedCheck -Allo
-w
-+-WithoutPassedReview
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -Message "chore(ai-dev): add a
-u
-+t
-++omation helpers"
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1 -AllowWithoutPassedCheck -Allo
-w
-+W
-++ithoutPassedReview
-+ ```
-+ 
-+-`-Files`를 지정하지 않으면 자동 커밋 스크립트는 기존처럼 `git add -A`로 현재 작업 트리의 모든 변경사항을 stage한다. `-Files`를 지정하면 각 파일
-이
-+- 존재하고 변경사항이 있는지 확인한 뒤 `git add -- <files>`로 지정 파일만 stage한다. 선택 파일 외에 이미 staged 된 파일이 있으면 안전을 위해 중
-단
-+-한다.
-++`-Files`를 지정하지 않으면 자동 커밋 스크립트는 기존처럼 `git add -A`로 현재 작업 트리의 모든 변경사항을 stage한다. `-Files`를 지정하면 각 파일
-이
-+ 
-++존재하고 변경사항이 있는지 확인한 뒤 `git add -- <files>`로 지정 파일만 stage한다. 선택 파일 외에 이미 staged 된 파일이 있으면 안전을 위해 중단
-한
-+다
-++.
-+ 
-+ 실행 전 예상하지 못한 사용자 변경이 없는지 반드시 확인한다. Codex 실행, GPT API 호출, build/test 실행은 수행하지 않는다.
-+ 
-+@@ -965,18 +613,18 @@ WithoutPassedReview
-+ AI Dev Loop의 실제 기능 변경과 `.ai-dev` 실행 상태는 가능한 한 별도 커밋으로 관리한다.
-+ 
-+ - 기능 커밋에는 현재 task의 코드와 직접 관련 문서만 포함한다.
-+-- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `test-result.md`, `review.md` 등을 포함
-할
-+- 수 있다.
-++- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `test-result.md`, `review.md` 등을 포함
-할
-+ 
-++수 있다.
-+ - `diff.md`와 `review-prompt.md`는 커질 수 있으므로 항상 커밋하지 않는다.
-+ - `review-response.json`은 민감 정보나 장문 내용이 포함될 수 있으므로 내용을 확인한 뒤 커밋 여부를 결정한다.
-+ - untracked 텍스트 파일 내용은 리뷰에 필요할 수 있지만, 리뷰 포함 여부와 커밋 대상 여부는 별도로 판단한다.
-+ - 대형 또는 민감 산출물은 정리하거나 별도 정책에 따라 제외한다.
-+ 
-+-커밋 전에는 `git status --short`와 `ai-dev-commit.ps1 -DryRun`으로 대상을 먼저 확인한다. 선택 파일 커밋 기능이 준비되면 전체 stag
-e
-+-보다 선택 파일 방식을 우선한다.
-++커밋 전에는 `git status --short`와 `ai-dev-commit.ps1 -DryRun`으로 대상을 먼저 확인한다. 선택 파일 커밋 기능이 준비되면 전체 stag
-e
-+보
-++다 선택 파일 방식을 우선한다.
-+ 
-+-세부 기준은 `docs/ai-dev-loop-policy.md`의 `.ai-dev` 실행 산출물 커밋/무시 정책을 따른다. 실제 `.gitignore` 변경은 별도 task에
-서
-+- 검토한다.
-++세부 기준은 `docs/ai-dev-loop-policy.md`의 `.ai-dev` 실행 산출물 커밋/무시 정책을 따른다. 실제 `.gitignore` 변경은 별도 task에
-서
-+ 
-++검토한다.
-+ 
-+ ## 자동 개발 루프 상태 요약
-+ 
-+@@ -1024,11 +672,11 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1 -Json
-+ ```
-+ 
-+-수동 사이클 안내 스크립트는 추천 명령과 파일 경로만 보여주며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하
-지
-+- 않는다.
-++수동 사이클 안내 스크립트는 추천 명령과 파일 경로만 보여주며, Codex 실행, GPT API 호출, build/test 실행, git add, git commit을 수행하
-지
-+ 
-++않는다.
-+ 
-+-`manual-cycle`에서 다음 action이 `ask_gpt_review`로 표시되면 수동 리뷰 브리지를 사용한다. GPT API 키가 없어도 아래 흐름으로 리뷰를 진행
-할
-+- 수 있다.
-++`manual-cycle`에서 다음 action이 `ask_gpt_review`로 표시되면 수동 리뷰 브리지를 사용한다. GPT API 키가 없어도 아래 흐름으로 리뷰를 진행
-할
-+ 
-++수 있다.
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle.ps1
-+@@ -1055,20 +703,20 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -Fr
-o
-+mCli
-+ 
-+ ## 자동 실행 단계 정책
-+ 
-+-`ai-dev-auto-step.ps1`는 현재 상태를 보고 안전한 다음 한 단계만 실행하는 보조 스크립트로 도입할 예정이다. `ai-dev-next.ps1`처럼 안내만 하는
- 
-+-명령과 달리, 허용된 로컬 스크립트는 직접 실행할 수 있다.
-++`ai-dev-auto-step.ps1`는 현재 상태를 보고 안전한 다음 한 단계만 실행하는 보조 스크립트로 도입할 예정이다. `ai-dev-next.ps1`처럼 안내만 하는
- 
-+명
-++령과 달리, 허용된 로컬 스크립트는 직접 실행할 수 있다.
-+ 
-+ 명령 선택 기준:
-+ 
-+ | 상황 | 사용할 명령 | 이유 |
-+ | --- | --- | --- |
-+-| 현재 상태와 다음 행동을 한 화면에서 보고 싶을 때 | `scripts/ai-dev-manual-cycle.ps1` | 상태 요약, 다음 action, 관련 파일 존재 여
-부
-+-를 함께 보여주는 수동 운영 대시보드다. |
-++| 현재 상태와 다음 행동을 한 화면에서 보고 싶을 때 | `scripts/ai-dev-manual-cycle.ps1` | 상태 요약, 다음 action, 관련 파일 존재 여
-부
-+를
-++ 함께 보여주는 수동 운영 대시보드다. |
-+ | 다음 action과 추천 명령만 확인하고 싶을 때 | `scripts/ai-dev-next.ps1` | 읽기/안내용 스크립트이며 하위 명령을 자동 실행하지 않는다. |
-+-| 안전한 다음 한 단계만 자동으로 진행하고 싶을 때 | `scripts/ai-dev-auto-step.ps1` | `next` 판단 결과를 기반으로 허용된 로컬 스크립트만 
-한
-+- 번 실행한다. |
-+-| 여러 안전 단계를 제한 횟수 안에서 이어가고 싶을 때 | `scripts/ai-dev-auto-cycle.ps1` | auto-step 반복용이며 사용자 개입 필요 상태에
-서
-+- 중단한다. |
-++| 안전한 다음 한 단계만 자동으로 진행하고 싶을 때 | `scripts/ai-dev-auto-step.ps1` | `next` 판단 결과를 기반으로 허용된 로컬 스크립트만 
-한
-+ 
-++번 실행한다. |
-++| 여러 안전 단계를 제한 횟수 안에서 이어가고 싶을 때 | `scripts/ai-dev-auto-cycle.ps1` | auto-step 반복용이며 사용자 개입 필요 상태에
-서
-+ 
-++중단한다. |
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-step.ps1
-+@@ -1114,11 +762,11 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.
-p
-+s1
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
-+ ```
-+ 
-+-첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. ChatGPT가 JSON 리뷰만 출력하면 그 JSON을 다시 
-클
-+-립보드에 복사하고 두 번째 명령으로 저장한다.
-++첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. ChatGPT가 JSON 리뷰만 출력하면 그 JSON을 다시 
-클
-+립
-++보드에 복사하고 두 번째 명령으로 저장한다.
-+ 
-+-`ai-dev-auto-cycle.ps1`는 제한 횟수 안에서 auto-step을 반복하는 초기 자동화 스크립트다. 사용자 개입이 필요한 action, 실패, 반복 위험, c
-o
-+-mmit 필요 상태에서는 중단한다.
-++`ai-dev-auto-cycle.ps1`는 제한 횟수 안에서 auto-step을 반복하는 초기 자동화 스크립트다. 사용자 개입이 필요한 action, 실패, 반복 위험, c
-o
-+m
-++mit 필요 상태에서는 중단한다.
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1
-+@@ -1135,26 +783,26 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -All
-o
-+wSav
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -AllowReviewPrompt
-+ ```
-+ 
-+-auto-cycle은 Codex/Cline 작업, GPT 리뷰, revise, commit, blocked, 사용자 개입 필요 action에서 멈춘다. 무한 루프를 피하기 위
-해
-+- `-MaxSteps` 제한과 같은 action 반복 감지를 사용한다.
-++auto-cycle은 Codex/Cline 작업, GPT 리뷰, revise, commit, blocked, 사용자 개입 필요 action에서 멈춘다. 무한 루프를 피하기 위
-해
-+ 
-++`-MaxSteps` 제한과 같은 action 반복 감지를 사용한다.
-+ 
-+-auto-cycle이 `ask_gpt_review`에서 멈추면 `stoppedReason`은 `user_intervention_required:ask_gpt_review`로 
-표
-+-시된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-++auto-cycle이 `ask_gpt_review`에서 멈추면 `stoppedReason`은 `user_intervention_required:ask_gpt_review`로 
-표
-+시
-++된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-copy-review-prompt.ps1
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.ps1 -FromClipboard
-+ ```
-+ 
-+-첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 리뷰를 받은 뒤 두 번째 명령으로 클립보드의 리뷰 JSO
-N
-+-을 저장한다. auto-cycle은 이 단계에서 GPT API를 호출하지 않고 중단한다.
-++첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 리뷰를 받은 뒤 두 번째 명령으로 클립보드의 리뷰 JSO
-N
-+을
-++ 저장한다. auto-cycle은 이 단계에서 GPT API를 호출하지 않고 중단한다.
-+ 
-+ 명령 역할 구분:
-+ 
-+ - `ai-dev-next.ps1`: 현재 상태를 읽고 다음 추천 action과 명령만 출력한다. 자동 실행하지 않는다.
-+ - `ai-dev-manual-cycle.ps1`: `status`와 `next`를 함께 보여주는 수동 운영 대시보드다. 자동 실행하지 않는다.
-+-- `ai-dev-auto-step.ps1`: `next` 판단 결과를 기반으로 안전한 action 한 단계만 자동 실행한다. 위험 action은 실행하지 않고 안내 후 중단
-한
-+-다.
-++- `ai-dev-auto-step.ps1`: `next` 판단 결과를 기반으로 안전한 action 한 단계만 자동 실행한다. 위험 action은 실행하지 않고 안내 후 중단
-한
-+다
-++.
-+ - `ai-dev-auto-cycle.ps1`: 제한 횟수 안에서 auto-step을 반복한다. 사용자 개입 필요 action에서 중단한다.
-+ 
-+ auto-step과 auto-cycle은 Codex/GPT API 호출, git commit, git push를 자동 실행하지 않는다.
-+@@ -1174,24 +822,24 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle.ps1 -Dry
-R
-+un -
-+ 확인 기준:
-+ 
-+ - `goal_completed`: 더 이상 실행할 task가 없으므로 새 goal 초기화가 필요하다.
-+-- `ask_gpt_review`: `ai-dev-copy-review-prompt.ps1`와 `ai-dev-save-review.ps1 -FromClipboard`를 사용해
- 
-+-수동 리뷰 브리지로 진행한다.
-++- `ask_gpt_review`: `ai-dev-copy-review-prompt.ps1`와 `ai-dev-save-review.ps1 -FromClipboard`를 사용해
- 
-+수
-++동 리뷰 브리지로 진행한다.
-+ - `revise`: `ai-dev-make-revise-prompt.ps1`로 재수정 프롬프트를 만든다.
-+ - `blocked`: 사용자 판단이 필요하므로 자동 진행을 멈춘다.
-+ - `commit`: 자동 실행하지 않고 커밋 조건과 대상 파일을 사람이 확인한다.
-+ 
-+-`goal_completed` 상태에서 `auto-step`은 추가 작업을 실행하지 않고 현재 goal이 완료되었으며 더 실행할 task가 없다고 안내한다. 추천 명령에는 상
-태
-+- 확인용 `ai-dev-status.ps1`만 포함한다.
-++`goal_completed` 상태에서 `auto-step`은 추가 작업을 실행하지 않고 현재 goal이 완료되었으며 더 실행할 task가 없다고 안내한다. 추천 명령에는 상
-태
-+ 
-++확인용 `ai-dev-status.ps1`만 포함한다.
-+ 
-+-`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고, JSON 출력에서는 `completed: true`,
- 
-+-`stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.
-a
-+-i-dev/state.json`을 새 목표 기준으로 초기화한다.
-++`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고, JSON 출력에서는 `completed: true`,
- 
-+`
-++stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.a
-i
-+-
-++dev/state.json`을 새 목표 기준으로 초기화한다.
-+ 
-+-`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하고 JSON 리뷰만 다시 복사한다. 완료된 goal이나 
-기
-+-존 정상 상태를 불필요하게 오염시키지 않도록 실패 기록 방식은 별도 정책에 따라 개선한다.
-++`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하고 JSON 리뷰만 다시 복사한다. 완료된 goal이나 
-기
-+존
-++ 정상 상태를 불필요하게 오염시키지 않도록 실패 기록 방식은 별도 정책에 따라 개선한다.
-+ 
-+-최종 검증 task에서는 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 결과를 `t
-e
-+-st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-++최종 검증 task에서는 PowerShell 문법 검증, `auto-step`/`auto-cycle` DryRun, `save-review` 성공/실패 흐름 확인 결과를 `t
-e
-+s
-++t-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-+ 
-+ ## Codex CLI 완전 자동화 준비
-+ 
-+@@ -1209,14 +857,14 @@ st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검
-+ 
-+ 도입 예정 스크립트:
-+ 
-+-- `scripts/ai-dev-run-codex.ps1`: `.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 수행
-한
-+-다.
-+-- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고
- 
-+-저장 흐름에 연결한다.
-+-- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 저장, Codex 리뷰, save-revie
-w
-+-, pass 시 commit/complete-task를 연결한다.
-+-- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력하면 goal/queue/state 생성, 
-c
-+-urrent-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
-++- `scripts/ai-dev-run-codex.ps1`: `.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 수행
-한
-+다
-++.
-++- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고
- 
-+저
-++장 흐름에 연결한다.
-++- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 저장, Codex 리뷰, save-revie
-w
-+,
-++ pass 시 commit/complete-task를 연결한다.
-++- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력하면 goal/queue/state 생성, 
-c
-+u
-++rrent-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
-+ 
-+ 초기 안전 기준:
-+ 
-+@@ -1240,9 +888,8 @@ urrent-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
-+ 
-+ ### Codex 구현 실행
-+ 
-+-`.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 실행한다. 실행 전 `git status --short`를 확인하
-고
-+-, 작업 트리가 dirty이면 기본 중단한다. 단, dirty 파일이 `.ai-dev/current-task-prompt.md`뿐이면 프롬프트 생성 산출물로 보고 진행할 수 
-있
-+-다.
-++`.ai-dev/current-task-prompt.md`를 Codex CLI에 전달해 현재 task 구현을 실행한다. 실행 전 `git status --short`를 확인하
-고
-+,
-++ 작업 트리가 dirty이면 기본 중단한다. 단, dirty 파일이 `.ai-dev/current-task-prompt.md`뿐이면 프롬프트 생성 산출물로 보고 진행할 수 있
-다
-+.
-+ 
-+ 실행 예정 정보만 확인하려면 `-DryRun`을 사용한다.
-+ 
-+@@ -1268,21 +915,21 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -Gene
-r
-+ateP
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -DryRun -Json
-+ ```
-+ 
-+-dirty worktree에서도 실행해야 하는 특별한 경우에만 `-AllowDirty`를 명시한다. 이 옵션은 사용자 변경과 Codex 변경이 섞일 수 있으므로 full cy
-c
-+-le에서는 초기 기본값으로 사용하지 않는다.
-++dirty worktree에서도 실행해야 하는 특별한 경우에만 `-AllowDirty`를 명시한다. 이 옵션은 사용자 변경과 Codex 변경이 섞일 수 있으므로 full cy
-c
-+l
-++e에서는 초기 기본값으로 사용하지 않는다.
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps1 -AllowDirty
-+ ```
-+ 
-+-이 스크립트는 Codex에 전달하는 프롬프트 끝에 추가 안전 규칙을 붙인다. git commit, git reset, git clean, npm install, package
- 
-+-파일 수정, 현재 task 범위 밖 작업을 금지한다. 스크립트 자체도 build/test/lint, git add, git commit을 실행하지 않는다.
-++이 스크립트는 Codex에 전달하는 프롬프트 끝에 추가 안전 규칙을 붙인다. git commit, git reset, git clean, npm install, package
- 
-+파
-++일 수정, 현재 task 범위 밖 작업을 금지한다. 스크립트 자체도 build/test/lint, git add, git commit을 실행하지 않는다.
-+ 
-+ ### Codex 리뷰 실행
-+ 
-+-`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/review-response.json`에 저장한다. 실행
- 
-+-전 `git status --short`를 확인하고, 작업 트리가 dirty이면 기본 중단한다. 리뷰 대상 변경사항이 남아 있는 상태에서 실행해야 하는 경우에만 `-Allow
-D
-+-irty`를 명시한다.
-++`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/review-response.json`에 저장한다. 실행
- 
-+전
-++ `git status --short`를 확인하고, 작업 트리가 dirty이면 기본 중단한다. 리뷰 대상 변경사항이 남아 있는 상태에서 실행해야 하는 경우에만 `-AllowD
-i
-+r
-++ty`를 명시한다.
-+ 
-+ 실행 예정 정보만 확인하려면 `-DryRun`을 사용한다.
-+ 
-+@@ -1290,8 +937,8 @@ irty`를 명시한다.
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -DryRun
-+ ```
-+ 
-+-기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-result.md`에 저장한다. Codex 출력에서 첫 번째 
-유
-+-효 JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-++기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-result.md`에 저장한다. Codex 출력에서 첫 번째 
-유
-+효
-++ JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1
-+@@ -1300,16 +947,16 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps
-1
-+ `review-prompt.md`가 없을 때 자동 생성까지 허용하려면 `-GenerateReviewPromptIfMissing`를 사용한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -GenerateReviewPromp
-t
-+-IfMissing
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -GenerateReviewPromp
-t
-+I
-++fMissing
-+ ```
-+ 
-+-리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-response.json` 흐름까지 이어가려면 `-SaveR
-e
-+-view`를 사용한다.
-++리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-response.json` 흐름까지 이어가려면 `-SaveR
-e
-+v
-++iew`를 사용한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveRev
-i
-+-ew
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveRev
-i
-+e
-++w
-+ ```
-+ 
-+ 자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
-+@@ -1321,18 +968,18 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-review-codex.ps
-1
-+ -Dr
-+ 주의:
-+ 
-+ - Codex 리뷰 실행 프롬프트에는 파일 수정 금지, git 명령 금지, build/test/lint 실행 금지, JSON 객체만 출력하라는 안전 규칙을 추가한다.
-+-- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`, `optional_suggestions`, `next_
-s
-+-tep` 필드가 필요하다.
-++- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`, `optional_suggestions`, `next_
-s
-+t
-++ep` 필드가 필요하다.
-+ - JSON 추출에 실패하면 출력 preview를 보여주고 종료 코드 1로 종료한다.
-+ - `-AllowDirty`는 구현 변경사항을 리뷰해야 할 때만 사용한다. 사용자 변경과 자동화 산출물이 섞여 있으면 리뷰 결과와 후속 저장 상태가 혼동될 수 있다.
-+ - 이 스크립트는 git add, git commit, build/test/lint, GPT API 호출을 수행하지 않는다.
-+ 
-+ ### full auto-cycle 초안
-+ 
-+-`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
-e
-+-w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-++`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
-e
-+w
-++ prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-+ 
-+-T004 버전의 실행 단계:
-++T005 버전의 실행 단계:
-+ 
-+ 1. `ai-dev-make-prompt.ps1`
-+ 2. `ai-dev-run-codex.ps1`
-+@@ -1340,6 +987,12 @@ T004 버전의 실행 단계:
-+ 4. `ai-dev-save-diff.ps1`
-+ 5. `ai-dev-make-review-prompt.ps1 -Strict`
-+ 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
-++7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
-++8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
-++9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다. `-CommitFiles`가 있으면 선택 파일만 커밋 대상으로 전달한다.
-++10. 커밋 결과 게이트 확인: `state.lastCommand`가 `commit`이고 `lastCommitHash`가 있어야 한다.
-++11. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-++12. `MaxTasks`가 1보다 크면 complete-task 성공 후 다음 task를 다시 읽고 같은 흐름을 반복한다.
-+ 
-+ 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-+ 
-+@@ -1353,16 +1006,23 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+ -Dry
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-+ ```
-+ 
-+-기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
-리
-+-를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
-++기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이다. `MaxTasks`가 1보다 크면
- 
-+각
-++ task가 커밋과 complete-task까지 성공한 경우에만 다음 task로 반복한다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
-M
-+a
-++xSteps 20
-++```
-++
-++여러 task 반복 구조를 확인하려면 `MaxTasks`를 늘릴 수 있다. 실제 반복은 각 task의 commit/complete-task가 성공해야 다음 task로 넘어간다
-.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
-M
-+-axSteps 20
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 3 -
-M
-+a
-++xSteps 20
-+ ```
-+ 
-+-`-AllowCodex` 없이 실제 실행하면 prompt 생성 이후 Codex 구현 실행 직전에서 중단하고 추천 명령을 출력한다. Codex 구현까지 허용하려면 명시적으로 `
--
-+-AllowCodex`를 지정한다.
-++`-AllowCodex` 없이 실제 실행하면 prompt 생성 이후 Codex 구현 실행 직전에서 중단하고 추천 명령을 출력한다. Codex 구현까지 허용하려면 명시적으로 `
--
-+A
-++llowCodex`를 지정한다.
-+ 
-+ ```powershell
-+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex
-+@@ -1371,49 +1031,109 @@ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.p
-s
-+1 -All
-+ Codex 구현과 Codex 리뷰까지 이어가려면 `-AllowCodex`와 `-AllowReviewCodex`를 함께 지정한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+-ewCodex -MaxTasks 1
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+e
-++wCodex -MaxTasks 1
-++```
-++
-++리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 함께 지정한다. 커밋 메시지는 기본적으로 현재
- 
-+t
-++ask의 `commitMessage`를 사용한다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+e
-++wCodex -AllowCommit -MaxTasks 1
-++```
-++
-++선택 파일만 커밋하려면 `-CommitFiles`를 지정한다. 이 값은 그대로 `ai-dev-commit.ps1 -Files`에 전달된다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+e
-++wCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks 1
-++```
-++
-++최대 3개 task까지 연속 실행하려면 `MaxTasks 3`을 지정한다. complete-task가 queue/state/loop-log를 갱신하므로, 다음 task 구현 
-실
-+행
-++ 시 dirty worktree 정책을 별도로 판단해야 한다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+e
-++wCodex -AllowCommit -AllowDirty -MaxTasks 3
-+ ```
-+ 
-+-dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
-o
-+-dex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-++dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
-o
-+d
-++ex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+-ewCodex -AllowDirty -MaxTasks 1
-++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+e
-++wCodex -AllowDirty -MaxTasks 1
-+ ```
-+ 
-+-`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
-결
-+-은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
-++리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 comple
-t
-+e
-++-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `package-
-l
-+o
-++ck.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 `c
-o
-+m
-++mit_not_confirmed`로 중단하고 complete-task를 실행하지 않는다.
-+ 
-+ ### 목표 입력 기반 자동 실행
-+ 
-+ 최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 하는 것이다.
-+ 
-+-예상 최종 명령:
-++먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+ 
-++추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-++```
-++
-++자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-++DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 파일 작성 전에 생성될 goal/task 
-계
-+획
-++을 검토할 수 있다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+ 
-++추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-++```
-++
-++실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이 모드는 `ai-dev-auto-cycle
--
-+f
-++ull.ps1`을 호출하지 않는다.
-++
-++```powershell
-++powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+ 
-++추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-++```
-++
-++생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowReviewCodex`, `-AllowComm
-i
-+t
-++` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다. 실제
- 
-+구
-++현까지 허용하려면 `-AllowCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일
-하
-+게
-++ `-AllowCodex -AllowReviewCodex -AllowCommit`을 함께 지정한다.
-+ 
-+ ```powershell
-+-powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+- 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -Al
-l
-+-owCommit -MaxTasks 1
-++powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -GoalTitle "업무 검색 결과 하이라이
-트
-+ 
-++추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -All
-o
-+w
-++Commit -MaxTasks 1
-+ ```
-+ 
-+-예정 동작:
-++동작:
-+ 
-+ 1. `GoalTitle`과 `GoalDescription`을 검증한다.
-+ 2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
-+-3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
-+-4. `.ai-dev/current-task-prompt.md`를 생성한다.
-+-5. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-++3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-++4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한다.
-++5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
-++6. `.ai-dev/current-task-prompt.md`를 생성한다.
-++7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-++8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current-task-prompt.md`, 결과 파일 
-때
-+문
-++에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이트는 a
-u
-+t
-++o-goal 시작 전에 별도로 유지한다.
-+ 
-+ 안전 기준:
-+ 
-+ - `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
-+-- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시한다.
-++- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
-+ - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-+-- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-+-- full cycle 호출은 `-AllowRun` 또는 그에 준하는 명시적 실행 옵션이 있을 때만 수행한다.
-++- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 않는다.
-++- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowCommit` 중 하나가 있을 때만 수행한다
-.
-++- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCodex`, `-AllowReviewCodex
-`
-+,
-++ `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
-+ - package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커밋하지 않는다.
-++- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
-+ 
-+ ## 운영 원칙
-+ 
-+@@ -1431,757 +1151,232 @@ owCommit -MaxTasks 1
-+ 세부 기준은 `docs/ai-dev-loop-policy.md`를 따른다.
-+ 
-+ codex
-+-현재 full cycle은 T004 자리표시자에서 멈추고, `-AllowCommit`도 실제 사용하지 않는 상태입니다. 커밋 스크립트와 task 완료 스크립트의 파라미터/게이
-트
-+-를 확인한 뒤 full cycle에 최소 연결만 추가하겠습니다.
-+-exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path sc
-r
-+-ipts/ai-dev-complete-task.ps1 -Pattern "param|ResultSummary|TaskId|loop-log|queue|state" -Context
- 
-+-0,3' in D:\ai-apps\planpilot-local
-+-exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path sc
-r
-+-ipts/ai-dev-save-review.ps1 -Pattern "lastReview|decision|next_step|state" -Context 0,3' in D:\ai
--
-+-apps\planpilot-local
-+-exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path sc
-r
-+-ipts/ai-dev-commit.ps1 -Pattern "param|Files|DryRun|package|lastCommandStatus|lastReview|AllowWit
-h
-+-out" -Context 0,3' in D:\ai-apps\planpilot-local
-+- succeeded in 2602ms:
-+-
-+-> scripts\ai-dev-complete-task.ps1:1:param(
-+-> scripts\ai-dev-complete-task.ps1:2:    [string]$TaskId,
-+-> scripts\ai-dev-complete-task.ps1:3:    [string]$ResultSummary,
-+-  scripts\ai-dev-complete-task.ps1:4:    [switch]$NoNext
-+-  scripts\ai-dev-complete-task.ps1:5:)
-+-  scripts\ai-dev-complete-task.ps1:6:
-+-> scripts\ai-dev-complete-task.ps1:10:$queueRelativePath = ".ai-dev/queue.json"
-+-> scripts\ai-dev-complete-task.ps1:11:$stateRelativePath = ".ai-dev/state.json"
-+-> scripts\ai-dev-complete-task.ps1:12:$loopLogRelativePath = ".ai-dev/loop-log.md"
-+-  scripts\ai-dev-complete-task.ps1:13:
-+-> scripts\ai-dev-complete-task.ps1:14:$queuePath = Join-Path $projectRoot $queueRelativePath
-+-> scripts\ai-dev-complete-task.ps1:15:$statePath = Join-Path $projectRoot $stateRelativePath
-+-  scripts\ai-dev-complete-task.ps1:16:$loopLogPath = Join-Path $projectRoot $loopLogRelativePath
-+-  scripts\ai-dev-complete-task.ps1:17:$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-+-  scripts\ai-dev-complete-task.ps1:18:
-+-> scripts\ai-dev-complete-task.ps1:20:    param(
-+-  scripts\ai-dev-complete-task.ps1:21:        [string]$Message
-+-  scripts\ai-dev-complete-task.ps1:22:    )
-+-  scripts\ai-dev-complete-task.ps1:23:
-+-> scripts\ai-dev-complete-task.ps1:29:    param(
-+-  scripts\ai-dev-complete-task.ps1:30:        [string]$Path,
-+-  scripts\ai-dev-complete-task.ps1:31:        [string]$RelativePath
-+-  scripts\ai-dev-complete-task.ps1:32:    )
-+-> scripts\ai-dev-complete-task.ps1:42:    param(
-+-  scripts\ai-dev-complete-task.ps1:43:        [object]$Value
-+-  scripts\ai-dev-complete-task.ps1:44:    )
-+-  scripts\ai-dev-complete-task.ps1:45:
-+-> scripts\ai-dev-complete-task.ps1:58:    param(
-+-  scripts\ai-dev-complete-task.ps1:59:        [object]$InputObject,
-+-  scripts\ai-dev-complete-task.ps1:60:        [string]$Name,
-+-  scripts\ai-dev-complete-task.ps1:61:        [object]$Value
-+-> scripts\ai-dev-complete-task.ps1:72:    param(
-+-  scripts\ai-dev-complete-task.ps1:73:        [string]$Path,
-+-  scripts\ai-dev-complete-task.ps1:74:        [object]$Value
-+-  scripts\ai-dev-complete-task.ps1:75:    )
-+-> scripts\ai-dev-complete-task.ps1:81:foreach ($requiredPath in @($queueRelativePath, $stateRelat
-i
-+-vePath, $loopLogRelat
-+-ivePath)) {
-+-  scripts\ai-dev-complete-task.ps1:82:    $fullPath = Join-Path $projectRoot $requiredPath
-+-  scripts\ai-dev-complete-task.ps1:83:
-+-  scripts\ai-dev-complete-task.ps1:84:    if (-not (Test-Path -LiteralPath $fullPath -PathType Le
-a
-+-f)) {
-+-> scripts\ai-dev-complete-task.ps1:89:$queue = Read-JsonFile $queuePath $queueRelativePath
-+-> scripts\ai-dev-complete-task.ps1:90:$state = Read-JsonFile $statePath $stateRelativePath
-+-  scripts\ai-dev-complete-task.ps1:91:
-+-> scripts\ai-dev-complete-task.ps1:92:if (-not ($queue.PSObject.Properties.Name -contains "tasks"
-)
-+-) {
-+-> scripts\ai-dev-complete-task.ps1:93:    Stop-WithError "$queueRelativePath 필수 필드가 누락되었습니다: task
-s
-+-"
-+-  scripts\ai-dev-complete-task.ps1:94:}
-+-  scripts\ai-dev-complete-task.ps1:95:
-+-> scripts\ai-dev-complete-task.ps1:96:if ($null -eq $queue.tasks -or -not ($queue.tasks -is [Syst
-e
-+-m.Collections.IEnumer
-+-able])) {
-+-> scripts\ai-dev-complete-task.ps1:97:    Stop-WithError "$queueRelativePath tasks 필드는 배열이어야 합니다.
-"
-+-  scripts\ai-dev-complete-task.ps1:98:}
-+-  scripts\ai-dev-complete-task.ps1:99:
-+-> scripts\ai-dev-complete-task.ps1:100:$tasks = @($queue.tasks)
-+-> scripts\ai-dev-complete-task.ps1:101:$targetTaskId = $null
-+-  scripts\ai-dev-complete-task.ps1:102:
-+-> scripts\ai-dev-complete-task.ps1:103:if (Test-HasValue $TaskId) {
-+-> scripts\ai-dev-complete-task.ps1:104:    $targetTaskId = $TaskId
-+-> scripts\ai-dev-complete-task.ps1:105:} elseif (Test-HasValue $state.currentTaskId) {
-+-> scripts\ai-dev-complete-task.ps1:106:    $targetTaskId = [string]$state.currentTaskId
-+-> scripts\ai-dev-complete-task.ps1:107:} elseif (Test-HasValue $queue.currentTaskId) {
-+-> scripts\ai-dev-complete-task.ps1:108:    $targetTaskId = [string]$queue.currentTaskId
-+-  scripts\ai-dev-complete-task.ps1:109:} else {
-+-  scripts\ai-dev-complete-task.ps1:110:    $fallbackTask = $tasks |
-+-  scripts\ai-dev-complete-task.ps1:111:        Where-Object { $_.status -eq "in_progress" -or $_.
-s
-+-tatus -eq "pending" }
-+- |
-+-> scripts\ai-dev-complete-task.ps1:115:        $targetTaskId = [string]$fallbackTask.id
-+-  scripts\ai-dev-complete-task.ps1:116:    }
-+-  scripts\ai-dev-complete-task.ps1:117:}
-+-  scripts\ai-dev-complete-task.ps1:118:
-+-> scripts\ai-dev-complete-task.ps1:119:if (-not (Test-HasValue $targetTaskId)) {
-+-  scripts\ai-dev-complete-task.ps1:120:    Stop-WithError "완료 처리할 task가 없습니다."
-+-  scripts\ai-dev-complete-task.ps1:121:}
-+-  scripts\ai-dev-complete-task.ps1:122:
-+-> scripts\ai-dev-complete-task.ps1:123:$completedTask = $tasks | Where-Object { $_.id -eq $target
-T
-+-askId } | Select-Obje
-+-ct -First 1
-+-  scripts\ai-dev-complete-task.ps1:124:
-+-  scripts\ai-dev-complete-task.ps1:125:if ($null -eq $completedTask) {
-+-> scripts\ai-dev-complete-task.ps1:126:    Stop-WithError "완료 처리할 task를 찾을 수 없습니다: $targetTaskId"
-+-  scripts\ai-dev-complete-task.ps1:127:}
-+-  scripts\ai-dev-complete-task.ps1:128:
-+-  scripts\ai-dev-complete-task.ps1:129:$now = [DateTimeOffset]::UtcNow.ToString("o")
-+-> scripts\ai-dev-complete-task.ps1:130:$summary = if (Test-HasValue $ResultSummary) { $ResultSumm
-a
-+-ry } else { "완료 처리됨" 
-+-}
-+-  scripts\ai-dev-complete-task.ps1:131:
-+-  scripts\ai-dev-complete-task.ps1:132:Set-ObjectProperty $completedTask "status" "done"
-+-  scripts\ai-dev-complete-task.ps1:133:Set-ObjectProperty $completedTask "completedAt" $now
-+-> scripts\ai-dev-complete-task.ps1:135:if (Test-HasValue $ResultSummary) {
-+-> scripts\ai-dev-complete-task.ps1:136:    Set-ObjectProperty $completedTask "resultSummary" $Res
-u
-+-ltSummary
-+-  scripts\ai-dev-complete-task.ps1:137:}
-+-  scripts\ai-dev-complete-task.ps1:138:
-+-  scripts\ai-dev-complete-task.ps1:139:$nextTask = $null
-+-> scripts\ai-dev-complete-task.ps1:146:    Set-ObjectProperty $queue "currentTaskId" $null
-+-> scripts\ai-dev-complete-task.ps1:147:    Set-ObjectProperty $state "currentTaskId" $null
-+-> scripts\ai-dev-complete-task.ps1:148:    Set-ObjectProperty $state "goalStatus" "in_progress"
-+-> scripts\ai-dev-complete-task.ps1:149:    Set-ObjectProperty $state "stopReason" "manual_next_se
-l
-+-ection_required"
-+-  scripts\ai-dev-complete-task.ps1:150:} elseif ($null -ne $nextTask) {
-+-  scripts\ai-dev-complete-task.ps1:151:    Set-ObjectProperty $nextTask "status" "in_progress"
-+-> scripts\ai-dev-complete-task.ps1:152:    Set-ObjectProperty $queue "currentTaskId" $nextTask.id
-+-> scripts\ai-dev-complete-task.ps1:153:    Set-ObjectProperty $state "currentTaskId" $nextTask.id
-+-> scripts\ai-dev-complete-task.ps1:154:    Set-ObjectProperty $state "goalStatus" "in_progress"
-+-> scripts\ai-dev-complete-task.ps1:155:    Set-ObjectProperty $state "stopReason" $null
-+-  scripts\ai-dev-complete-task.ps1:156:} else {
-+-> scripts\ai-dev-complete-task.ps1:157:    Set-ObjectProperty $queue "currentTaskId" $null
-+-> scripts\ai-dev-complete-task.ps1:158:    Set-ObjectProperty $state "currentTaskId" $null
-+-> scripts\ai-dev-complete-task.ps1:159:    Set-ObjectProperty $state "goalStatus" "completed"
-+-> scripts\ai-dev-complete-task.ps1:160:    Set-ObjectProperty $state "stopReason" "all_tasks_comp
-l
-+-eted"
-+-  scripts\ai-dev-complete-task.ps1:161:}
-+-  scripts\ai-dev-complete-task.ps1:162:
-+-> scripts\ai-dev-complete-task.ps1:163:Set-ObjectProperty $queue "updatedAt" $now
-+-> scripts\ai-dev-complete-task.ps1:164:Set-ObjectProperty $state "updatedAt" $now
-+-> scripts\ai-dev-complete-task.ps1:165:Set-ObjectProperty $state "lastCommand" "complete-task"
-+-> scripts\ai-dev-complete-task.ps1:166:Set-ObjectProperty $state "lastCommandStatus" "passed"
-+-> scripts\ai-dev-complete-task.ps1:167:Set-ObjectProperty $state "lastErrorSummary" ""
-+-  scripts\ai-dev-complete-task.ps1:168:
-+-> scripts\ai-dev-complete-task.ps1:169:Write-JsonFile $queuePath $queue
-+-> scripts\ai-dev-complete-task.ps1:170:Write-JsonFile $statePath $state
-+-  scripts\ai-dev-complete-task.ps1:171:
-+-  scripts\ai-dev-complete-task.ps1:172:$nextTaskText = if ($null -ne $nextTask) {
-+-  scripts\ai-dev-complete-task.ps1:173:    "$($nextTask.id) $($nextTask.title)"
-+-> scripts\ai-dev-complete-task.ps1:192:Write-Host "queue/state 저장 완료"
-+-
-+-
-+-
-+- succeeded in 2771ms:
-+-
-+-> scripts\ai-dev-commit.ps1:1:param(
-+-  scripts\ai-dev-commit.ps1:2:    [string]$Message,
-+-> scripts\ai-dev-commit.ps1:3:    [string[]]$Files,
-+-> scripts\ai-dev-commit.ps1:4:    [switch]$AllowWithoutPassedCheck,
-+-> scripts\ai-dev-commit.ps1:5:    [switch]$AllowWithoutPassedReview,
-+-> scripts\ai-dev-commit.ps1:6:    [switch]$DryRun
-+-  scripts\ai-dev-commit.ps1:7:)
-+-  scripts\ai-dev-commit.ps1:8:
-+-  scripts\ai-dev-commit.ps1:9:. "$PSScriptRoot\ai-dev-env.ps1"
-+-> scripts\ai-dev-commit.ps1:22:    param(
-+-  scripts\ai-dev-commit.ps1:23:        [string]$Message
-+-  scripts\ai-dev-commit.ps1:24:    )
-+-  scripts\ai-dev-commit.ps1:25:
-+-> scripts\ai-dev-commit.ps1:31:    param(
-+-  scripts\ai-dev-commit.ps1:32:        [string]$Path,
-+-  scripts\ai-dev-commit.ps1:33:        [string]$RelativePath
-+-  scripts\ai-dev-commit.ps1:34:    )
-+-> scripts\ai-dev-commit.ps1:44:    param(
-+-  scripts\ai-dev-commit.ps1:45:        [object]$Value
-+-  scripts\ai-dev-commit.ps1:46:    )
-+-  scripts\ai-dev-commit.ps1:47:
-+-> scripts\ai-dev-commit.ps1:60:    param(
-+-  scripts\ai-dev-commit.ps1:61:        [object]$InputObject,
-+-  scripts\ai-dev-commit.ps1:62:        [string]$Name,
-+-  scripts\ai-dev-commit.ps1:63:        [object]$Value
-+-> scripts\ai-dev-commit.ps1:74:    param(
-+-  scripts\ai-dev-commit.ps1:75:        [object]$State,
-+-  scripts\ai-dev-commit.ps1:76:        [string]$Status,
-+-  scripts\ai-dev-commit.ps1:77:        [string]$ErrorSummary,
-+-> scripts\ai-dev-commit.ps1:82:    Set-ObjectProperty $State "lastCommandStatus" $Status
-+-  scripts\ai-dev-commit.ps1:83:    Set-ObjectProperty $State "lastErrorSummary" $ErrorSummary
-+-  scripts\ai-dev-commit.ps1:84:    Set-ObjectProperty $State "updatedAt" ([DateTimeOffset]::UtcNo
-w
-+-.ToString("o"))
-+-  scripts\ai-dev-commit.ps1:85:
-+-> scripts\ai-dev-commit.ps1:95:    param(
-+-  scripts\ai-dev-commit.ps1:96:        [string[]]$Arguments,
-+-  scripts\ai-dev-commit.ps1:97:        [string]$DisplayName
-+-  scripts\ai-dev-commit.ps1:98:    )
-+-> scripts\ai-dev-commit.ps1:166:if (-not $AllowWithoutPassedCheck -and $state.lastCommandStatus -
-n
-+-e "passed") {
-+-> scripts\ai-dev-commit.ps1:167:    Stop-WithError "state.lastCommandStatus가 passed가 아닙니다. 검증 결과를
- 
-+-확인하거나 -AllowWithoutPa
-+-ssedCheck를 사용하세요."
-+-  scripts\ai-dev-commit.ps1:168:}
-+-  scripts\ai-dev-commit.ps1:169:
-+-> scripts\ai-dev-commit.ps1:170:if (-not $AllowWithoutPassedReview -and $state.lastReviewDecision
- 
-+--ne "pass") {
-+-> scripts\ai-dev-commit.ps1:171:    Stop-WithError "state.lastReviewDecision이 pass가 아닙니다. 리뷰 결과를 
-확
-+-인하거나 -AllowWithoutPas
-+-sedReview를 사용하세요."
-+-  scripts\ai-dev-commit.ps1:172:}
-+-  scripts\ai-dev-commit.ps1:173:
-+-  scripts\ai-dev-commit.ps1:174:try {
-+-> scripts\ai-dev-commit.ps1:187:$selectedFiles = @()
-+-  scripts\ai-dev-commit.ps1:188:$targetChangeLines = $changeLines
-+-  scripts\ai-dev-commit.ps1:189:
-+-> scripts\ai-dev-commit.ps1:190:if ($null -ne $Files -and $Files.Count -gt 0) {
-+-> scripts\ai-dev-commit.ps1:191:    $requestedFiles = @($Files | ForEach-Object { $_ -split "," }
- 
-+-| Where-Object { -not
-+- [string]::IsNullOrWhiteSpace($_) })
-+-  scripts\ai-dev-commit.ps1:192:
-+-> scripts\ai-dev-commit.ps1:193:    if ($requestedFiles.Count -eq 0) {
-+-> scripts\ai-dev-commit.ps1:194:        Stop-WithError "-Files에는 비어 있지 않은 파일 경로만 지정할 수 있습니다."
-+-  scripts\ai-dev-commit.ps1:195:    }
-+-  scripts\ai-dev-commit.ps1:196:
-+-> scripts\ai-dev-commit.ps1:197:    foreach ($file in $requestedFiles) {
-+-  scripts\ai-dev-commit.ps1:198:        if ([string]::IsNullOrWhiteSpace($file)) {
-+-> scripts\ai-dev-commit.ps1:199:            Stop-WithError "-Files에는 비어 있지 않은 파일 경로만 지정할 수 있습니다."
-+-  scripts\ai-dev-commit.ps1:200:        }
-+-  scripts\ai-dev-commit.ps1:201:
-+-  scripts\ai-dev-commit.ps1:202:        $normalizedFile = $file.Replace('\', '/')
-+-> scripts\ai-dev-commit.ps1:215:            $fileStatus = Invoke-GitCapture -Arguments @("status"
-,
-+- "--porcelain", "--",
-+- $normalizedFile) -DisplayName "git status --porcelain -- $normalizedFile"
-+-  scripts\ai-dev-commit.ps1:216:        } catch {
-+-  scripts\ai-dev-commit.ps1:217:            Stop-WithError $_.Exception.Message
-+-  scripts\ai-dev-commit.ps1:218:        }
-+-> scripts\ai-dev-commit.ps1:220:        if ([string]::IsNullOrWhiteSpace($fileStatus)) {
-+-  scripts\ai-dev-commit.ps1:221:            Stop-WithError "선택한 파일에 커밋할 변경사항이 없습니다: $file"
-+-  scripts\ai-dev-commit.ps1:222:        }
-+-  scripts\ai-dev-commit.ps1:223:
-+-> scripts\ai-dev-commit.ps1:224:        $selectedFiles += $normalizedFile
-+-  scripts\ai-dev-commit.ps1:225:    }
-+-  scripts\ai-dev-commit.ps1:226:
-+-> scripts\ai-dev-commit.ps1:227:    $selectedFiles = @($selectedFiles | Select-Object -Unique)
-+-> scripts\ai-dev-commit.ps1:228:    $targetChangeLines = @($selectedFiles)
-+-  scripts\ai-dev-commit.ps1:229:
-+-  scripts\ai-dev-commit.ps1:230:    try {
-+-> scripts\ai-dev-commit.ps1:231:        $stagedFiles = Invoke-GitCapture -Arguments @("diff", "--
-c
-+-ached", "--name-only"
-+-) -DisplayName "git diff --cached --name-only"
-+-  scripts\ai-dev-commit.ps1:232:    } catch {
-+-  scripts\ai-dev-commit.ps1:233:        Stop-WithError $_.Exception.Message
-+-  scripts\ai-dev-commit.ps1:234:    }
-+-> scripts\ai-dev-commit.ps1:236:    $stagedFileList = @($stagedFiles -split "`r?`n" | Where-Objec
-t
-+- { -not [string]::IsN
-+-ullOrWhiteSpace($_) })
-+-> scripts\ai-dev-commit.ps1:237:    $unexpectedStagedFiles = @($stagedFileList | Where-Object { $
-s
-+-electedFiles -notcont
-+-ains $_ })
-+-  scripts\ai-dev-commit.ps1:238:
-+-> scripts\ai-dev-commit.ps1:239:    if ($unexpectedStagedFiles.Count -gt 0) {
-+-> scripts\ai-dev-commit.ps1:240:        Stop-WithError "선택 파일 외에 이미 staged 된 파일이 있습니다: $($unexpec
-t
-+-edStagedFiles -join '
-+-, ')"
-+-  scripts\ai-dev-commit.ps1:241:    }
-+-  scripts\ai-dev-commit.ps1:242:}
-+-  scripts\ai-dev-commit.ps1:243:
-+-> scripts\ai-dev-commit.ps1:262:if ($DryRun) {
-+-  scripts\ai-dev-commit.ps1:263:    Write-Host "Dry run: git add/commit을 실행하지 않습니다."
-+-  scripts\ai-dev-commit.ps1:264:    Write-Host "커밋 메시지: $commitMessage"
-+-  scripts\ai-dev-commit.ps1:265:    Write-Host "현재 task: $taskText"
-+-> scripts\ai-dev-commit.ps1:275:    if ($selectedFiles.Count -gt 0) {
-+-> scripts\ai-dev-commit.ps1:276:        $addOutput = & git add -- @selectedFiles 2>&1 | Out-Strin
-g
-+-> scripts\ai-dev-commit.ps1:277:        $addDisplayName = "git add -- <selected files>"
-+-  scripts\ai-dev-commit.ps1:278:    } else {
-+-  scripts\ai-dev-commit.ps1:279:        $addOutput = & git add -A 2>&1 | Out-String
-+-  scripts\ai-dev-commit.ps1:280:        $addDisplayName = "git add -A"
-+-> scripts\ai-dev-commit.ps1:287:    if ($selectedFiles.Count -gt 0) {
-+-> scripts\ai-dev-commit.ps1:288:        $stagedFilesAfterAdd = Invoke-GitCapture -Arguments @("di
-f
-+-f", "--cached", "--na
-+-me-only") -DisplayName "git diff --cached --name-only"
-+-> scripts\ai-dev-commit.ps1:289:        $stagedFileListAfterAdd = @($stagedFilesAfterAdd -split "
-`
-+-r?`n" | Where-Object 
-+-{ -not [string]::IsNullOrWhiteSpace($_) })
-+-> scripts\ai-dev-commit.ps1:290:        $unexpectedStagedFilesAfterAdd = @($stagedFileListAfterAd
-d
-+- | Where-Object { $se
-+-lectedFiles -notcontains $_ })
-+-  scripts\ai-dev-commit.ps1:291:
-+-> scripts\ai-dev-commit.ps1:292:        if ($unexpectedStagedFilesAfterAdd.Count -gt 0) {
-+-> scripts\ai-dev-commit.ps1:293:            throw "선택 파일 외의 staged 파일이 감지되었습니다: $($unexpectedStag
-e
-+-dFilesAfterAdd -join 
-+-', ')"
-+-  scripts\ai-dev-commit.ps1:294:        }
-+-  scripts\ai-dev-commit.ps1:295:    }
-+-  scripts\ai-dev-commit.ps1:296:
-+-
-+-
-+-
-+- succeeded in 2836ms:
-+-
-+-> scripts\ai-dev-save-review.ps1:11:$stateRelativePath = ".ai-dev/state.json"
-+-  scripts\ai-dev-save-review.ps1:12:$reviewRelativePath = ".ai-dev/review.md"
-+-> scripts\ai-dev-save-review.ps1:13:$statePath = Join-Path $projectRoot $stateRelativePath
-+-  scripts\ai-dev-save-review.ps1:14:$reviewPath = Join-Path $projectRoot $reviewRelativePath
-+-  scripts\ai-dev-save-review.ps1:15:$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-+-  scripts\ai-dev-save-review.ps1:16:$codeFence = '```'
-+-> scripts\ai-dev-save-review.ps1:70:function Save-StateReviewResult {
-+-  scripts\ai-dev-save-review.ps1:71:    param(
-+-> scripts\ai-dev-save-review.ps1:72:        [object]$State,
-+-> scripts\ai-dev-save-review.ps1:73:        [string]$Decision,
-+-  scripts\ai-dev-save-review.ps1:74:        [string]$Severity,
-+-  scripts\ai-dev-save-review.ps1:75:        [string]$CommandStatus,
-+-  scripts\ai-dev-save-review.ps1:76:        [string]$ErrorSummary
-+-> scripts\ai-dev-save-review.ps1:79:    Set-ObjectProperty $State "lastReviewDecision" $Decision
-+-> scripts\ai-dev-save-review.ps1:80:    Set-ObjectProperty $State "lastReviewSeverity" $Severity
-+-> scripts\ai-dev-save-review.ps1:81:    Set-ObjectProperty $State "lastCommand" "save-review"
-+-> scripts\ai-dev-save-review.ps1:82:    Set-ObjectProperty $State "lastCommandStatus" $CommandSta
-t
-+-us
-+-> scripts\ai-dev-save-review.ps1:83:    Set-ObjectProperty $State "lastErrorSummary" $ErrorSummar
-y
-+-> scripts\ai-dev-save-review.ps1:84:    Set-ObjectProperty $State "updatedAt" ([DateTimeOffset]::
-U
-+-tcNow.ToString("o"))
-+-  scripts\ai-dev-save-review.ps1:85:
-+-> scripts\ai-dev-save-review.ps1:86:    $stateJson = $State | ConvertTo-Json -Depth 20
-+-> scripts\ai-dev-save-review.ps1:87:    [System.IO.File]::WriteAllText($statePath, $stateJson, $u
-t
-+-f8WithBom)
-+-  scripts\ai-dev-save-review.ps1:88:}
-+-  scripts\ai-dev-save-review.ps1:89:
-+-  scripts\ai-dev-save-review.ps1:90:function Get-ReviewInput {
-+-> scripts\ai-dev-save-review.ps1:108:        Write-Host '  powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-save
-+--review.ps1 -ReviewJson ''{"decision":"pass","severity":"none","summary":"OK","required_changes":
-[
-+-],"optional_suggestio
-+-ns":[],"next_step":"complete_task"}'''
-+-  scripts\ai-dev-save-review.ps1:109:        Write-Host "  powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-save
-+--review.ps1 -FromClipboard"
-+-  scripts\ai-dev-save-review.ps1:110:        Stop-WithError "리뷰 입력을 지정해야 합니다."
-+-  scripts\ai-dev-save-review.ps1:111:    }
-+-> scripts\ai-dev-save-review.ps1:282:        "decision",
-+-  scripts\ai-dev-save-review.ps1:283:        "severity",
-+-  scripts\ai-dev-save-review.ps1:284:        "summary",
-+-  scripts\ai-dev-save-review.ps1:285:        "required_changes",
-+-> scripts\ai-dev-save-review.ps1:287:        "next_step"
-+-  scripts\ai-dev-save-review.ps1:288:    )
-+-  scripts\ai-dev-save-review.ps1:289:
-+-  scripts\ai-dev-save-review.ps1:290:    foreach ($field in $requiredFields) {
-+-> scripts\ai-dev-save-review.ps1:300:    if (@("pass", "revise", "blocked") -notcontains $Review.
-d
-+-ecision) {
-+-> scripts\ai-dev-save-review.ps1:301:        $errors += "허용되지 않은 decision: $($Review.decision)"
-+-  scripts\ai-dev-save-review.ps1:302:    }
-+-  scripts\ai-dev-save-review.ps1:303:
-+-  scripts\ai-dev-save-review.ps1:304:    if (@("none", "low", "medium", "high", "critical") -notc
-o
-+-ntains $Review.severi
-+-ty) {
-+-> scripts\ai-dev-save-review.ps1:308:    if (@("complete_task", "revise_with_codex", "stop_for_us
-e
-+-r") -notcontains $Rev
-+-iew.next_step) {
-+-> scripts\ai-dev-save-review.ps1:309:        $errors += "허용되지 않은 next_step: $($Review.next_step)"
-+-  scripts\ai-dev-save-review.ps1:310:    }
-+-  scripts\ai-dev-save-review.ps1:311:
-+-  scripts\ai-dev-save-review.ps1:312:    if ($Review.required_changes -isnot [System.Collections.
-I
-+-Enumerable] -or $Revi
-+-ew.required_changes -is [string]) {
-+-> scripts\ai-dev-save-review.ps1:362:        [object]$State,
-+-  scripts\ai-dev-save-review.ps1:363:        [string]$RawInput,
-+-  scripts\ai-dev-save-review.ps1:364:        [string]$ErrorSummary
-+-  scripts\ai-dev-save-review.ps1:365:    )
-+-> scripts\ai-dev-save-review.ps1:373:- Decision: blocked
-+-  scripts\ai-dev-save-review.ps1:374:- Severity: critical
-+-  scripts\ai-dev-save-review.ps1:375:- Next step: stop_for_user
-+-  scripts\ai-dev-save-review.ps1:376:- Summary: 리뷰 JSON을 파싱하거나 검증하지 못했습니다.
-+-> scripts\ai-dev-save-review.ps1:390:    Save-StateReviewResult $State "blocked" "critical" "fail
-e
-+-d" $ErrorSummary
-+-  scripts\ai-dev-save-review.ps1:391:}
-+-  scripts\ai-dev-save-review.ps1:392:
-+-  scripts\ai-dev-save-review.ps1:393:function Stop-WithInvalidReviewInput {
-+-> scripts\ai-dev-save-review.ps1:395:        [object]$State,
-+-  scripts\ai-dev-save-review.ps1:396:        [string]$RawInput,
-+-  scripts\ai-dev-save-review.ps1:397:        [string]$ErrorSummary
-+-  scripts\ai-dev-save-review.ps1:398:    )
-+-> scripts\ai-dev-save-review.ps1:401:        Save-InvalidReview $State $RawInput $ErrorSummary
-+-  scripts\ai-dev-save-review.ps1:402:        Stop-WithError "리뷰 JSON 저장에 실패했습니다: $ErrorSummary"
-+-  scripts\ai-dev-save-review.ps1:403:    }
-+-  scripts\ai-dev-save-review.ps1:404:
-+-> scripts\ai-dev-save-review.ps1:405:    Write-Host "리뷰 입력 오류로 review.md와 state.json을 수정하지 않았습니다.
-"
-+-  scripts\ai-dev-save-review.ps1:406:    Write-Host "실패 리뷰를 명시적으로 기록하려면 -WriteFailureReview 옵션을 사
-용
-+-하세요."
-+-  scripts\ai-dev-save-review.ps1:407:    Stop-WithError "리뷰 JSON 저장에 실패했습니다: $ErrorSummary"
-+-  scripts\ai-dev-save-review.ps1:408:}
-+-> scripts\ai-dev-save-review.ps1:410:if (-not (Test-Path -LiteralPath $statePath -PathType Leaf))
- 
-+-{
-+-> scripts\ai-dev-save-review.ps1:411:    Stop-WithError "필수 파일이 없습니다: $stateRelativePath"
-+-  scripts\ai-dev-save-review.ps1:412:}
-+-  scripts\ai-dev-save-review.ps1:413:
-+-> scripts\ai-dev-save-review.ps1:414:$state = Read-JsonFile $statePath $stateRelativePath
-+-  scripts\ai-dev-save-review.ps1:415:$rawReview = Get-ReviewInput
-+-  scripts\ai-dev-save-review.ps1:416:$inputPreview = Get-InputPreview $rawReview
-+-  scripts\ai-dev-save-review.ps1:417:
-+-> scripts\ai-dev-save-review.ps1:420:    Stop-WithInvalidReviewInput $state $rawReview $errorSumm
-a
-+-ry
-+-  scripts\ai-dev-save-review.ps1:421:}
-+-  scripts\ai-dev-save-review.ps1:422:
-+-  scripts\ai-dev-save-review.ps1:423:try {
-+-> scripts\ai-dev-save-review.ps1:433:    Stop-WithInvalidReviewInput $state $rawReview $errorSumm
-a
-+-ry
-+-  scripts\ai-dev-save-review.ps1:434:}
-+-  scripts\ai-dev-save-review.ps1:435:
-+-  scripts\ai-dev-save-review.ps1:436:$prettyJson = $review | ConvertTo-Json -Depth 20
-+-> scripts\ai-dev-save-review.ps1:446:- Decision: $($review.decision)
-+-  scripts\ai-dev-save-review.ps1:447:- Severity: $($review.severity)
-+-> scripts\ai-dev-save-review.ps1:448:- Next step: $($review.next_step)
-+-  scripts\ai-dev-save-review.ps1:449:- Summary: $($review.summary)
-+-  scripts\ai-dev-save-review.ps1:450:
-+-  scripts\ai-dev-save-review.ps1:451:### Required Changes
-+-> scripts\ai-dev-save-review.ps1:467:Save-StateReviewResult $state $review.decision $review.sever
-i
-+-ty "passed" ""
-+-  scripts\ai-dev-save-review.ps1:468:
-+-> scripts\ai-dev-save-review.ps1:469:$nextAction = if ($review.decision -eq "pass" -and $review.n
-e
-+-xt_step -eq "complete
-+-_task") {
-+-  scripts\ai-dev-save-review.ps1:470:    "pass + complete_task: 다음 명령을 실행해 현재 task 완료 처리를 검토하세요. 
-p
-+-owershell -ExecutionP
-+-olicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 통과`""
-+-> scripts\ai-dev-save-review.ps1:471:} elseif ($review.decision -eq "revise" -and $review.next_st
-e
-+-p -eq "revise_with_co
-+-dex") {
-+-  scripts\ai-dev-save-review.ps1:472:    "revise + make_revise_prompt: 다음 명령으로 재수정 프롬프트를 생성한 뒤 Co
-d
-+-ex/Cline에 전달하세요. powe
-+-rshell -ExecutionPolicy Bypass -File scripts/ai-dev-make-revise-prompt.ps1"
-+-> scripts\ai-dev-save-review.ps1:473:} elseif ($review.decision -eq "blocked" -or $review.next_st
-e
-+-p -eq "stop_for_user"
-+-) {
-+-  scripts\ai-dev-save-review.ps1:474:    "blocked/stop_for_user: 사용자 판단이 필요합니다. 자동 진행을 중단하고 revie
-w
-+-.md의 Required Changes
-+-와 Summary를 확인하세요."
-+-  scripts\ai-dev-save-review.ps1:475:} else {
-+-> scripts\ai-dev-save-review.ps1:476:    "decision과 next_step 조합을 확인한 뒤 다음 행동을 결정하세요."
-+-  scripts\ai-dev-save-review.ps1:477:}
-+-  scripts\ai-dev-save-review.ps1:478:
-+-  scripts\ai-dev-save-review.ps1:479:Write-Host "리뷰 저장 완료: $reviewRelativePath"
-+-> scripts\ai-dev-save-review.ps1:480:Write-Host "Decision: $($review.decision)"
-+-  scripts\ai-dev-save-review.ps1:481:Write-Host "Severity: $($review.severity)"
-+-> scripts\ai-dev-save-review.ps1:482:Write-Host "Next step: $($review.next_step)"
-+-  scripts\ai-dev-save-review.ps1:483:Write-Host "안내: $nextAction"
-+-
-+-
-+-
-++현재 `scripts/ai-dev-auto-goal.ps1` 파일이 이미 존재합니다. 새로 덮어쓰기 전에 기존 구현 상태를 읽고, 부족한 부분만 최소 수정하겠습니다.
-+ exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path .ai-
-d
-+-ev/queue.json' in D:\ai-apps\planpilot-local
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPa
-t
-+h
-++ 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-cycle-full.ps1'" in D:\ai-apps\planpilot-loc
-a
-+l
-+ exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path scri
-p
-+-ts/ai-dev-commit.ps1 | Select-Object -First 340' in D:\ai-apps\planpilot-local
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPa
-t
-+h
-++ 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-env.ps1'" in D:\ai-apps\planpilot-local
-+ exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path .ai-
-d
-+-ev/state.json' in D:\ai-apps\planpilot-local
-+- succeeded in 4378ms:
-+-{
-+-    "goalTitle":  "AI Dev Loop Codex CLI 완전 자동화 도입",
-+-    "goalSource":  ".ai-dev/goal.md",
-+-    "createdAt":  "2026-06-07T19:59:48.1282711+09:00",
-+-    "updatedAt":  "2026-06-07T13:57:13.9217915+00:00",
-+-    "currentTaskId":  "T005",
-+-    "tasks":  [
-+-                  {
-+-                      "id":  "T001",
-+-                      "title":  "Codex CLI 완전 자동화 정책 문서화",
-+-                      "description":  "Codex CLI를 구현자와 리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전 중단 조건을 문
-서
-+-화한다.",
-+-                      "type":  "documentation",
-+-                      "status":  "done",
-+-                      "priority":  "P0",
-+-                      "dependsOn":  [
-+-
-+-                                    ],
-+-                      "filesLikelyToChange":  [
-+-                                                  "docs/ai-dev-loop-policy.md",
-+-                                                  ".ai-dev/README.md",
-+-                                                  ".ai-dev/plan.md"
-+-                                              ],
-+-                      "verification":  [
-+-                                           "Codex CLI 사용 방식과 안전 기준이 문서화되었는지 확인",
-+-                                           "Cline/Copilot/GPT API 없이 진행하는 구조가 명확한지 확인"
-+-                                       ],
-+-                      "commitMessage":  "Document Codex CLI automation policy",
-+-                      "completedAt":  "2026-06-07T12:46:13.9691878+00:00",
-+-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안
-전
-+- 중단 조건 문서화"
-+-                  },
-+-                  {
-+-                      "id":  "T002",
-+-                      "title":  "Codex 구현 실행 스크립트 추가",
-+-                      "description":  "current-task-prompt.md를 Codex CLI에 전달해 현재 task 구현을 수행하는 ai
--
-+-dev-run-codex.ps1을 추가한다.",
-+-                      "type":  "implementation",
-+-                      "status":  "done",
-+-                      "priority":  "P0",
-+-                      "dependsOn":  [
-+-                                        "T001"
-+-                                    ],
-+-                      "filesLikelyToChange":  [
-+-                                                  "scripts/ai-dev-run-codex.ps1",
-+-                                                  ".ai-dev/README.md"
-+-                                              ],
-+-                      "verification":  [
-+-                                           "DryRun에서 실행 예정 명령만 출력되는지 확인",
-+-                                           "current-task-prompt.md가 없으면 명확히 안내하는지 확인",
-+-                                           "git status dirty 상태에서 기본 중단하는지 확인",
-+-                                           "PowerShell 문법 검증"
-+-                                       ],
-+-                      "commitMessage":  "Add Codex task runner",
-+-                      "completedAt":  "2026-06-07T12:48:36.8786703+00:00",
-+-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안
-전
-+- 중단 조건 문서화"
-+-                  },
-+-                  {
-+-                      "id":  "T003",
-+-                      "title":  "Codex 리뷰 실행 스크립트 추가",
-+-                      "description":  "review-prompt.md를 Codex CLI에 전달해 JSON 리뷰를 생성하고 review-resp
-o
-+-nse.json 및 save-review 흐름에 연결하는 ai-dev-run-review-codex.ps1을 추가한다.",
-+-                      "type":  "implementation",
-+-                      "status":  "done",
-+-                      "priority":  "P0",
-+-                      "dependsOn":  [
-+-                                        "T002"
-+-                                    ],
-+-                      "filesLikelyToChange":  [
-+-                                                  "scripts/ai-dev-run-review-codex.ps1",
-+-                                                  ".ai-dev/README.md"
-+-                                              ],
-+-                      "verification":  [
-+-                                           "DryRun에서 실행 예정 명령만 출력되는지 확인",
-+-                                           "Codex 출력에서 JSON 객체를 추출해 저장하는지 확인",
-+-                                           "save-review와 연결되는지 확인",
-+-                                           "PowerShell 문법 검증"
-+-                                       ],
-+-                      "commitMessage":  "Add Codex review runner",
-+-                      "completedAt":  "2026-06-07T13:23:42.7240601+00:00",
-+-                      "resultSummary":  "T003 완료: review-prompt.md를 codex exec에 전달해 JSON 리뷰를 생성하는
- 
-+-ai-dev-run-review-codex.ps1 추가, 문법 검증과 DryRun/Json 확인"
-+-                  },
-+-                  {
-+-                      "id":  "T004",
-+-                      "title":  "full auto-cycle 초안 추가",
-+-                      "description":  "make-prompt, run-codex, check, save-diff, make-review-prom
-p
-+-t, run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안을 추가한다.",
-+-                      "type":  "implementation",
-+-                      "status":  "done",
-+-                      "priority":  "P0",
-+-                      "dependsOn":  [
-+-                                        "T003"
-+-                                    ],
-+-                      "filesLikelyToChange":  [
-+-                                                  "scripts/ai-dev-auto-cycle-full.ps1",
-+-                                                  ".ai-dev/README.md"
-+-                                              ],
-+-                      "verification":  [
-+-                                           "DryRun에서 전체 단계가 표시되는지 확인",
-+-                                           "MaxTasks 제한이 있는지 확인",
-+-                                           "AllowCodex, AllowReviewCodex, AllowCommit 같은 명시적 옵션이 
-있
-+-는지 확인",
-+-                                           "PowerShell 문법 검증"
-+-                                       ],
-+-                      "commitMessage":  "Add full AI dev auto cycle draft",
-+-                      "completedAt":  "2026-06-07T13:57:13.9217915+00:00",
-+-                      "resultSummary":  "T004 완료: make-prompt/run-codex/check/save-diff/make-revi
-e
-+-w-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가, DryRun/Json/AllowCodex 중단 확인"
-+-                  },
-+-                  {
-+-                      "id":  "T005",
-+-                      "title":  "자동 커밋과 task 완료 연결",
-+-                      "description":  "리뷰 결과가 pass일 때만 선택 파일 커밋과 complete-task를 수행하도록 full auto-c
-y
-+-cle에 연결한다.",
-+-                      "type":  "implementation",
-+-                      "status":  "in_progress",
-+-                      "priority":  "P0",
-+-                      "dependsOn":  [
-+-                                        "T004"
-+-                                    ],
-+-                      "filesLikelyToChange":  [
-+-                                                  "scripts/ai-dev-auto-cycle-full.ps1",
-+-                                                  ".ai-dev/README.md"
-+-                                              ],
-+-                      "verification":  [
-+-                                           "pass가 아니면 commit하지 않는지 확인",
-+-                                           "build 실패 시 commit하지 않는지 확인",
-+-                                           "package 파일 변경 감지 시 중단하는지 확인",
-+-                                           "complete-task가 pass 이후에만 실행되는지 확인",
-+-                                           "PowerShell 문법 검증"
-+-                                       ],
-+-                      "commitMessage":  "Connect pass review to auto commit and task completion"
-+-                  },
-+-                  {
-+-                      "id":  "T006",
-+-                      "title":  "작은 앱 task로 end-to-end 검증",
-+-                      "description":  "작은 문서 또는 UI 변경 task를 MaxTasks 1로 실행해 Codex 구현부터 리뷰, 커밋, 완료
- 
-+-처리까지 검증한다.",
-+-                      "type":  "verification",
-+-                      "status":  "pending",
-+-                      "priority":  "P0",
-+-                      "dependsOn":  [
-+-                                        "T005"
-+-                                    ],
-+-                      "filesLikelyToChange":  [
-+-                                                  ".ai-dev/plan.md",
-+-                                                  ".ai-dev/loop-log.md",
-+-                                                  ".ai-dev/test-result.md"
-+-                                              ],
-+-                      "verification":  [
-+-                                           "full auto-cycle DryRun 확인",
-+-                                           "MaxTasks 1 실제 실행 확인",
-+-                                           "Codex 구현 로그 확인",
-+-                                           "Codex 리뷰 JSON 저장 확인",
-+-                                           "pass 시 커밋/complete-task 확인",
-+-                                           "git status clean 확인"
-+-                                       ],
-+-                      "commitMessage":  null
-+-                  },
-+-                  {
-+-                      "id":  "T007",
-+-                      "title":  "목표 입력 기반 자동 goal 실행 스크립트 추가",
-+-                      "description":  "사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/
-q
-+-ueue/state를 생성하고, current-task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한
-다
-+-.",
-+-                      "type":  "implementation",
-+-                      "status":  "pending",
-+-                      "priority":  "P0",
-+-                      "dependsOn":  [
-+-                                        "T005"
-+-                                    ],
-+-                      "filesLikelyToChange":  [
-+-                                                  "scripts/ai-dev-auto-goal.ps1",
-+-                                                  ".ai-dev/README.md"
-+-                                              ],
-+-                      "verification":  [
-+-                                           "DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인",
-+-                                           "Json 출력이 가능한지 확인",
-+-                                           "goal/queue/state JSON 파싱 검증을 수행하는지 확인",
-+-                                           "auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확
-인
-+-",
-+-                                           "GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인",
-+-                                           "PowerShell 문법 검증"
-+-                                       ],
-+-                      "commitMessage":  "Add automatic goal runner"
-+-                  }
-+-              ]
-+-}
-+-
-+- succeeded in 4404ms:
-+-{
-+-    "goalStatus":  "in_progress",
-+-    "currentTaskId":  "T005",
-+-    "currentLoop":  0,
-+-    "maxLoopsPerTask":  2,
-+-    "repeatedFailureCount":  0,
-+-    "lastCommand":  "complete-task",
-+-    "lastCommandStatus":  "passed",
-+-    "lastErrorSummary":  "",
-+-    "lastReviewDecision":  "not_started",
-+-    "lastReviewSeverity":  "",
-+-    "lastCommitHash":  null,
-+-    "startedAt":  "2026-06-07T19:59:48.1282711+09:00",
-+-    "updatedAt":  "2026-06-07T13:57:13.9217915+00:00",
-+-    "stopReason":  null
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPa
-t
-+h
-++ 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1'" in D:\ai-apps\planpilot-local
-++ succeeded in 2506ms:
-++try {
-++    chcp 65001 | Out-Null
-++    $OutputEncoding = [System.Text.Encoding]::UTF8
-++    [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
-++    [Console]::InputEncoding = [System.Text.Encoding]::UTF8
-++} catch {
-++    # UTF-8 콘솔 설정 실패는 AI Dev 스크립트 실행을 중단하지 않는다.
-+ }
-+ 
-+- succeeded in 4567ms:
-++ succeeded in 2657ms:
-+ param(
-+-    [string]$Message,
-+-    [string[]]$Files,
-+-    [switch]$AllowWithoutPassedCheck,
-+-    [switch]$AllowWithoutPassedReview,
-+-    [switch]$DryRun
-++    [int]$MaxTasks = 1,
-++    [int]$MaxSteps = 20,
-++    [switch]$DryRun,
-++    [switch]$Json,
-++    [switch]$AllowCodex,
-++    [switch]$AllowReviewCodex,
-++    [switch]$AllowCommit,
-++    [switch]$AllowDirty,
-++    [string[]]$CommitFiles
-+ )
-+ 
-+-. "$PSScriptRoot\ai-dev-env.ps1"
-++. $PSScriptRoot\ai-dev-env.ps1
-+ 
-+-$projectRoot = (Get-Location).Path
-+-$stateRelativePath = ".ai-dev/state.json"
-++$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+ $queueRelativePath = ".ai-dev/queue.json"
-+-$loopLogRelativePath = ".ai-dev/loop-log.md"
-+-
-+-$statePath = Join-Path $projectRoot $stateRelativePath
-+-$queuePath = Join-Path $projectRoot $queueRelativePath
-+-$loopLogPath = Join-Path $projectRoot $loopLogRelativePath
-+-$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-++$stateRelativePath = ".ai-dev/state.json"
-++$reviewResponseRelativePath = ".ai-dev/review-response.json"
-++$queuePath = Join-Path $repoRoot $queueRelativePath
-++$statePath = Join-Path $repoRoot $stateRelativePath
-++$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+ 
-+-function Stop-WithError {
-++function Test-HasValue {
-+     param(
-+-        [string]$Message
-++        [object]$Value
-+     )
-+ 
-+-    Write-Error $Message
-+-    exit 1
-+-}
-++    if ($null -eq $Value) {
-++        return $false
-++    }
-+ 
-+-function Read-JsonFile {
-+-    param(
-+-        [string]$Path,
-+-        [string]$RelativePath
-++    if ($Value -is [string]) {
-++        return -not [string]::IsNullOrWhiteSpace($Value)
-++    }
-++
-++    return $true
-++}
-++
-++function Read-JsonFile {
-++    param(
-++        [string]$Path,
-++        [string]$RelativePath
-+     )
-+ 
-+     try {
-+         return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | ConvertFrom-Json
-+     } catch {
-+-        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
-++        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)"
-+     }
-+ }
-+ 
-+-function Test-HasValue {
-++function Get-CurrentTask {
-+     param(
-+-        [object]$Value
-++        [object]$Queue,
-++        [object]$State
-+     )
-+ 
-+-    if ($null -eq $Value) {
-+-        return $false
-++    $tasks = @($Queue.tasks)
-++    $currentTaskId = $null
-++
-++    if (Test-HasValue $State.currentTaskId) {
-++        $currentTaskId = [string]$State.currentTaskId
-++    } elseif (Test-HasValue $Queue.currentTaskId) {
-++        $currentTaskId = [string]$Queue.currentTaskId
-++    } else {
-++        $inProgressTask = $tasks | Where-Object { $_.status -eq "in_progress" } | Select-Object -
-F
-+i
-++rst 1
-++
-++        if ($null -ne $inProgressTask) {
-++            $currentTaskId = [string]$inProgressTask.id
-++        } else {
-++            $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -Fir
-s
-+t
-++ 1
-++
-++            if ($null -ne $pendingTask) {
-++                $currentTaskId = [string]$pendingTask.id
-++            }
-++        }
-+     }
-+ 
-+-    if ($Value -is [string]) {
-+-        return -not [string]::IsNullOrWhiteSpace($Value)
-++    if (-not (Test-HasValue $currentTaskId)) {
-++        return $null
-+     }
-+ 
-+-    return $true
-++    return $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
-+ }
-+ 
-+-function Set-ObjectProperty {
-++function New-StepResult {
-+     param(
-+-        [object]$InputObject,
-++        [int]$Step,
-+         [string]$Name,
-+-        [object]$Value
-++        [string]$Command,
-++        [bool]$Executed,
-++        [bool]$Skipped,
-++        [int]$ExitCode,
-++        [string]$Message
-+     )
-+ 
-+-    if ($InputObject.PSObject.Properties.Name -contains $Name) {
-+-        $InputObject.$Name = $Value
-+-    } else {
-+-        $InputObject | Add-Member -NotePropertyName $Name -NotePropertyValue $Value
-++    return [PSCustomObject][ordered]@{
-++        step = $Step
-++        name = $Name
-++        command = $Command
-++        executed = $Executed
-++        skipped = $Skipped
-++        exitCode = $ExitCode
-++        message = $Message
-+     }
-+ }
-+ 
-+-function Save-StateResult {
-++function New-CycleResult {
-+     param(
-+-        [object]$State,
-+-        [string]$Status,
-+-        [string]$ErrorSummary,
-+-        [string]$CommitHash
-++        [object[]]$Steps,
-++        [string]$StoppedReason,
-++        [bool]$Completed,
-++        [int]$ExitCode
-++    )
-++
-++    return [PSCustomObject][ordered]@{
-++        steps = @($Steps)
-++        stoppedReason = $StoppedReason
-++        completed = $Completed
-++        exitCode = $ExitCode
-++    }
-++}
-++
-++function Write-CycleResult {
-++    param(
-++        [object]$Result
-++    )
-++
-++    if ($Json) {
-++        $Result | ConvertTo-Json -Depth 20
-++        return
-++    }
-++
-++    foreach ($step in @($Result.steps)) {
-++        Write-Host "Step $($step.step): $($step.name)"
-++        Write-Host "  Command: $($step.command)"
-++        Write-Host "  Executed: $($step.executed)"
-++        Write-Host "  Skipped: $($step.skipped)"
-++        Write-Host "  Exit code: $($step.exitCode)"
-++        Write-Host "  Message: $($step.message)"
-++    }
-++
-++    Write-Host "Stopped reason: $($Result.stoppedReason)"
-++    Write-Host "Completed: $($Result.completed)"
-++    Write-Host "Exit code: $($Result.exitCode)"
-++
-++}
-++
-++function Stop-Cycle {
-++    param(
-++        [object[]]$Steps,
-++        [string]$StoppedReason,
-++        [bool]$Completed,
-++        [int]$ExitCode
-++    )
-++
-++    $result = New-CycleResult $Steps $StoppedReason $Completed $ExitCode
-++    Write-CycleResult $result
-++    exit $ExitCode
-++}
-++
-++function Invoke-CycleCommand {
-++    param(
-++        [int]$StepNumber,
-++        [string]$Name,
-++        [string]$Command,
-++        [string]$ScriptPath,
-++        [string[]]$Arguments
-++    )
-++
-++    if ($DryRun) {
-++        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: 하위 스크립
-트
-+를
-++ 실행하지 않았습니다."
-++        return
-++    }
-++
-++    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
-++    $exitCode = $LASTEXITCODE
-++    $message = $output.Trim()
-++
-++    if ([string]::IsNullOrWhiteSpace($message)) {
-++        $message = "완료"
-++    }
-++
-++    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-++
-++    if ($exitCode -ne 0) {
-++        Stop-Cycle $script:steps "$Name`_failed" $false 1
-++    }
-++}
-++
-++function Get-ScriptPath {
-++    param(
-++        [string]$Name
-+     )
-+ 
-+-    Set-ObjectProperty $State "lastCommand" "commit"
-+-    Set-ObjectProperty $State "lastCommandStatus" $Status
-+-    Set-ObjectProperty $State "lastErrorSummary" $ErrorSummary
-+-    Set-ObjectProperty $State "updatedAt" ([DateTimeOffset]::UtcNow.ToString("o"))
-++    $scriptPath = Join-Path $PSScriptRoot $Name
-+ 
-+-    if (Test-HasValue $CommitHash) {
-+-        Set-ObjectProperty $State "lastCommitHash" $CommitHash
-++    if (-not (Test-Path -LiteralPath $scriptPath -PathType Leaf)) {
-++        throw "필수 스크립트를 찾을 수 없습니다: scripts/$Name"
-+     }
-+ 
-+-    $stateJson = $State | ConvertTo-Json -Depth 20
-+-    [System.IO.File]::WriteAllText($statePath, $stateJson, $utf8WithBom)
-++    return $scriptPath
-+ }
-+ 
-+ function Invoke-GitCapture {
-+@@ -2200,7967 +1395,4513 @@ function Invoke-GitCapture {
-+     return $output.TrimEnd()
-+ }
-+ 
-+-foreach ($requiredPath in @($stateRelativePath, $queueRelativePath, $loopLogRelativePath)) {
-+-    $fullPath = Join-Path $projectRoot $requiredPath
-+-
-+-    if (-not (Test-Path -LiteralPath $fullPath -PathType Leaf)) {
-+-        Stop-WithError "필수 파일이 없습니다: $requiredPath"
-+-    }
-++function Test-PackageFileChanged {
-++    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.js
-o
-+n
-++") "git status --porcelain -- package.json package-lock.json"
-++    return -not [string]::IsNullOrWhiteSpace($status)
-+ }
-+ 
-+-try {
-+-    $null = Invoke-GitCapture -Arguments @("rev-parse", "--show-toplevel") -DisplayName "git rev-
-p
-+-arse --show-toplevel"
-+-} catch {
-+-    Stop-WithError "현재 위치가 git repository가 아닙니다."
-+-}
-++function Get-ReviewGate {
-++    $state = Read-JsonFile $statePath $stateRelativePath
-++    $nextStep = $null
-+ 
-+-$queue = Read-JsonFile $queuePath $queueRelativePath
-+-$state = Read-JsonFile $statePath $stateRelativePath
-++    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-++        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+ 
-+-if (-not ($queue.PSObject.Properties.Name -contains "tasks")) {
-+-    Stop-WithError "$queueRelativePath 필수 필드가 누락되었습니다: tasks"
-+-}
-++        if (Test-HasValue $reviewResponse.next_step) {
-++            $nextStep = [string]$reviewResponse.next_step
-++        }
-++    }
-+ 
-+-if ($null -eq $queue.tasks -or -not ($queue.tasks -is [System.Collections.IEnumerable])) {
-+-    Stop-WithError "$queueRelativePath tasks 필드는 배열이어야 합니다."
-++    return [PSCustomObject][ordered]@{
-++        lastCommandStatus = [string]$state.lastCommandStatus
-++        decision = [string]$state.lastReviewDecision
-++        nextStep = $nextStep
-++    }
-+ }
-+ 
-+-$tasks = @($queue.tasks)
-+-$currentTaskId = $null
-+-
-+-if (Test-HasValue $state.currentTaskId) {
-+-    $currentTaskId = [string]$state.currentTaskId
-+-} elseif (Test-HasValue $queue.currentTaskId) {
-+-    $currentTaskId = [string]$queue.currentTaskId
-+-} else {
-+-    $inProgressTask = $tasks | Where-Object { $_.status -eq "in_progress" } | Select-Object -Firs
-t
-+- 1
-++function Get-CommitArguments {
-++    $arguments = @()
-+ 
-+-    if ($null -ne $inProgressTask) {
-+-        $currentTaskId = [string]$inProgressTask.id
-+-    } else {
-+-        $pendingTask = $tasks | Where-Object { $_.status -eq "pending" } | Select-Object -First 1
-++    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-++        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
-t
-+ 
-++[string]::IsNullOrWhiteSpace($_) })
-+ 
-+-        if ($null -ne $pendingTask) {
-+-            $currentTaskId = [string]$pendingTask.id
-++        if ($normalizedFiles.Count -gt 0) {
-++            $arguments += "-Files"
-++            $arguments += ($normalizedFiles -join ",")
-+         }
-+     }
-++
-++    return $arguments
-+ }
-+ 
-+-$currentTask = $null
-++function Get-CommitGate {
-++    $state = Read-JsonFile $statePath $stateRelativePath
-+ 
-+-if (Test-HasValue $currentTaskId) {
-+-    $currentTask = $tasks | Where-Object { $_.id -eq $currentTaskId } | Select-Object -First 1
-++    return [PSCustomObject][ordered]@{
-++        lastCommand = [string]$state.lastCommand
-++        lastCommandStatus = [string]$state.lastCommandStatus
-++        lastCommitHash = [string]$state.lastCommitHash
-++    }
-+ }
-+ 
-+-if ($null -eq $currentTask) {
-+-    Write-Warning "현재 task를 찾지 못했습니다. 기본 커밋 메시지를 사용할 수 있습니다."
-+-}
-++Set-Location $repoRoot
-++
-++$script:steps = @()
-+ 
-+-if (-not $AllowWithoutPassedCheck -and $state.lastCommandStatus -ne "passed") {
-+-    Stop-WithError "state.lastCommandStatus가 passed가 아닙니다. 검증 결과를 확인하거나 -AllowWithoutPassedCheck를
- 
-+-사용하세요."
-++if ($MaxTasks -lt 1) {
-++    Stop-Cycle $script:steps "max_tasks_must_be_at_least_1" $false 1
-+ }
-+ 
-+-if (-not $AllowWithoutPassedReview -and $state.lastReviewDecision -ne "pass") {
-+-    Stop-WithError "state.lastReviewDecision이 pass가 아닙니다. 리뷰 결과를 확인하거나 -AllowWithoutPassedReview를
- 
-+-사용하세요."
-++if ($MaxSteps -lt 1) {
-++    Stop-Cycle $script:steps "max_steps_must_be_at_least_1" $false 1
-+ }
-+ 
-+ try {
-+-    $statusPorcelain = Invoke-GitCapture -Arguments @("status", "--porcelain") -DisplayName "git 
-s
-+-tatus --porcelain"
-++    foreach ($requiredPath in @($queuePath, $statePath)) {
-++        if (-not (Test-Path -LiteralPath $requiredPath -PathType Leaf)) {
-++            throw "필수 상태 파일이 없습니다: $requiredPath"
-++        }
-++    }
-++
-++    $queue = Read-JsonFile $queuePath $queueRelativePath
-++    $state = Read-JsonFile $statePath $stateRelativePath
-++
-++    if ($state.goalStatus -eq "completed") {
-++        Stop-Cycle $script:steps "goal_completed" $true 0
-++    }
-++
-++    if (-not ($queue.PSObject.Properties.Name -contains "tasks") -or $null -eq $queue.tasks) {
-++        Stop-Cycle $script:steps "no_task" $false 0
-++    }
-++
-++    $currentTask = Get-CurrentTask $queue $state
-++
-++    if ($null -eq $currentTask) {
-++        Stop-Cycle $script:steps "no_task" $false 0
-++    }
-++
-++    if ($currentTask.status -eq "done") {
-++        Stop-Cycle $script:steps "current_task_done" $true 0
-++    }
-++
-++    $scriptPaths = @{
-++        makePrompt = Get-ScriptPath "ai-dev-make-prompt.ps1"
-++        runCodex = Get-ScriptPath "ai-dev-run-codex.ps1"
-++        check = Get-ScriptPath "ai-dev-check.ps1"
-++        saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-++        makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-++        runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-++        commit = Get-ScriptPath "ai-dev-commit.ps1"
-++        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-++    }
-+ } catch {
-+-    Stop-WithError $_.Exception.Message
-++    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Mes
-s
-+a
-++ge
-++    Stop-Cycle $script:steps "prepare_failed" $false 1
-+ }
-+ 
-+-$changeLines = @($statusPorcelain -split "`r?`n" | Where-Object { -not [string]::IsNullOrWhiteSpa
-c
-+-e($_) })
-++$plannedSteps = @(
-++    "make-prompt",
-++    "run-codex",
-++    "check",
-++    "save-diff",
-++    "make-review-prompt",
-++    "run-review-codex",
-++    "review-gate",
-++    "package-change-gate",
-++    "commit",
-++    "commit-result-gate",
-++    "complete-task"
-++)
-+ 
-+-if ($changeLines.Count -eq 0) {
-+-    Write-Host "커밋할 변경사항 없음"
-+-    exit 0
-++if ($plannedSteps.Count -gt $MaxSteps) {
-++    Stop-Cycle $script:steps "max_steps_too_small_for_full_cycle" $false 1
-+ }
-+ 
-+-$selectedFiles = @()
-+-$targetChangeLines = $changeLines
-++$stepNumber = 1
-++$completedTaskCount = 0
-++
-++while ($completedTaskCount -lt $MaxTasks) {
-++    try {
-++        $queue = Read-JsonFile $queuePath $queueRelativePath
-++        $state = Read-JsonFile $statePath $stateRelativePath
-++        $currentTask = Get-CurrentTask $queue $state
-++    } catch {
-++        $script:steps += New-StepResult $stepNumber "load-task" "state/queue 확인" $false $false 1 
-$
-+_
-++.Exception.Message
-++        Stop-Cycle $script:steps "load_task_failed" $false 1
-++    }
-++
-++    if ($state.goalStatus -eq "completed") {
-++        Stop-Cycle $script:steps "goal_completed" $true 0
-++    }
-+ 
-+-if ($null -ne $Files -and $Files.Count -gt 0) {
-+-    $requestedFiles = @($Files | ForEach-Object { $_ -split "," } | Where-Object { -not [string]:
-:
-+-IsNullOrWhiteSpace($_) })
-++    if ($null -eq $currentTask) {
-++        Stop-Cycle $script:steps "no_task" $false 0
-++    }
-+ 
-+-    if ($requestedFiles.Count -eq 0) {
-+-        Stop-WithError "-Files에는 비어 있지 않은 파일 경로만 지정할 수 있습니다."
-++    if ($currentTask.status -eq "done") {
-++        Stop-Cycle $script:steps "current_task_done" $true 0
-+     }
-+ 
-+-    foreach ($file in $requestedFiles) {
-+-        if ([string]::IsNullOrWhiteSpace($file)) {
-+-            Stop-WithError "-Files에는 비어 있지 않은 파일 경로만 지정할 수 있습니다."
-++    $taskLabel = "$($currentTask.id) $($currentTask.title)"
-++    $script:steps += New-StepResult $stepNumber "task-start" "MaxTasks=$MaxTasks" $false $false 0
- 
-+"
-++현재 task 실행 시작: $taskLabel"
-++    $stepNumber++
-++
-++    Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scrip
-t
-+s
-++/ai-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-++    $stepNumber++
-++
-++    if (-not $AllowCodex -and -not $DryRun) {
-++        $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -
-A
-+l
-++lowCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-++        if ($AllowDirty) {
-++            $command = "$command -AllowDirty"
-+         }
-+ 
-+-        $normalizedFile = $file.Replace('\', '/')
-+-        $fullPath = [System.IO.Path]::GetFullPath((Join-Path $projectRoot $normalizedFile))
-+-        $projectRootPrefix = [System.IO.Path]::GetFullPath($projectRoot).TrimEnd('\', '/') + [Sys
-t
-+-em.IO.Path]::DirectorySeparatorChar
-++        if ($AllowCommit) {
-++            $command = "$command -AllowCommit"
-++        }
-+ 
-+-        if (-not $fullPath.StartsWith($projectRootPrefix, [System.StringComparison]::OrdinalIgnor
-e
-+-Case)) {
-+-            Stop-WithError "저장소 밖 파일은 선택할 수 없습니다: $file"
-++        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-++            $command = "$command -CommitFiles $($CommitFiles -join ',')"
-+         }
-+ 
-+-        if (-not (Test-Path -LiteralPath $fullPath -PathType Leaf)) {
-+-            Stop-WithError "선택한 파일이 존재하지 않습니다: $file"
-++        $script:steps += New-StepResult $stepNumber "run-codex" "powershell -ExecutionPolicy Bypa
-s
-+s
-++ -File scripts/ai-dev-run-codex.ps1" $false $true 1 "Codex 구현 실행에는 -AllowCodex가 필요합니다. 추천 명령: $co
-m
-+m
-++and"
-++        Stop-Cycle $script:steps "allow_codex_required" $false 1
-++    }
-++
-++    $runCodexArguments = @()
-++    if ($AllowDirty) {
-++        $runCodexArguments += "-AllowDirty"
-++    }
-++
-++    Invoke-CycleCommand $stepNumber "run-codex" "powershell -ExecutionPolicy Bypass -File scripts
-/
-+a
-++i-dev-run-codex.ps1" $scriptPaths.runCodex $runCodexArguments
-++    $stepNumber++
-++
-++    Invoke-CycleCommand $stepNumber "check" "powershell -ExecutionPolicy Bypass -File scripts/ai-
-d
-+e
-++v-check.ps1 -BuildOnly" $scriptPaths.check @("-BuildOnly")
-++    $stepNumber++
-++
-++    Invoke-CycleCommand $stepNumber "save-diff" "powershell -ExecutionPolicy Bypass -File scripts
-/
-+a
-++i-dev-save-diff.ps1" $scriptPaths.saveDiff @()
-++    $stepNumber++
-++
-++    Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -Fil
-e
-+ 
-++scripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-++    $stepNumber++
-++
-++    if (-not $AllowReviewCodex -and -not $DryRun) {
-++        $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -
-A
-+l
-++lowCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-++        if ($AllowDirty) {
-++            $command = "$command -AllowDirty"
-+         }
-+ 
-+-        try {
-+-            $fileStatus = Invoke-GitCapture -Arguments @("status", "--porcelain", "--", $normaliz
-e
-+-dFile) -DisplayName "git status --porcelain -- $normalizedFile"
-+-        } catch {
-+-            Stop-WithError $_.Exception.Message
-++        if ($AllowCommit) {
-++            $command = "$command -AllowCommit"
-+         }
-+ 
-+-        if ([string]::IsNullOrWhiteSpace($fileStatus)) {
-+-            Stop-WithError "선택한 파일에 커밋할 변경사항이 없습니다: $file"
-++        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-++            $command = "$command -CommitFiles $($CommitFiles -join ',')"
-+         }
-+ 
-+-        $selectedFiles += $normalizedFile
-++        $script:steps += New-StepResult $stepNumber "run-review-codex" "powershell -ExecutionPoli
-c
-+y
-++ Bypass -File scripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $false $true 1 "Codex 
-리
-+뷰
-++ 실행에는 -AllowReviewCodex가 필요합니다. 추천 명령: $command"
-++        Stop-Cycle $script:steps "allow_review_codex_required" $false 1
-+     }
-+ 
-+-    $selectedFiles = @($selectedFiles | Select-Object -Unique)
-+-    $targetChangeLines = @($selectedFiles)
-++    Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File 
-s
-+c
-++ripts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowD
-i
-+r
-++ty", "-SaveReview")
-++    $stepNumber++
-++
-++    if ($DryRun) {
-++        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $
-f
-+a
-++lse $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-++        $stepNumber++
-++        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
- 
-+-
-++- package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-++        $stepNumber++
-++        $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass 
--
-+F
-++ile scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-++        $stepNumber++
-++        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastC
-o
-+m
-++mitHash 확인" $false $true 0 "DryRun: 실제 커밋 생성 여부를 확인하지 않았습니다."
-++        $stepNumber++
-++        $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy 
-B
-+y
-++pass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"자동 완료: Codex 구현, build/check, Codex 
-리
-+뷰
-++ pass, 자동 커밋 완료`"" $false $true 0 "DryRun: task 완료 처리를 실행하지 않았습니다."
-++        Stop-Cycle $script:steps "dry_run" $false 0
-++    }
-+ 
-+     try {
-+-        $stagedFiles = Invoke-GitCapture -Arguments @("diff", "--cached", "--name-only") -Display
-N
-+-ame "git diff --cached --name-only"
-++        $reviewGate = Get-ReviewGate
-+     } catch {
-+-        Stop-WithError $_.Exception.Message
-++        $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $fal
-s
-+e
-++ $false 1 $_.Exception.Message
-++        Stop-Cycle $script:steps "review_gate_failed" $false 1
-+     }
-+ 
-+-    $stagedFileList = @($stagedFiles -split "`r?`n" | Where-Object { -not [string]::IsNullOrWhite
-S
-+-pace($_) })
-+-    $unexpectedStagedFiles = @($stagedFileList | Where-Object { $selectedFiles -notcontains $_ })
-++    if ($reviewGate.lastCommandStatus -ne "passed") {
-++        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $f
-a
-+l
-++se $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-++        Stop-Cycle $script:steps "review_save_not_passed" $false 1
-++    }
-+ 
-+-    if ($unexpectedStagedFiles.Count -gt 0) {
-+-        Stop-WithError "선택 파일 외에 이미 staged 된 파일이 있습니다: $($unexpectedStagedFiles -join ', ')"
-++    if ($reviewGate.decision -ne "pass") {
-++        $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $
-f
-+a
-++lse $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-++        Stop-Cycle $script:steps "review_not_pass" $false 0
-+     }
-+-}
-+ 
-+-$commitMessage = if (Test-HasValue $Message) {
-+-    $Message
-+-} elseif ($null -ne $currentTask -and (Test-HasValue $currentTask.commitMessage)) {
-+-    [string]$currentTask.commitMessage
-+-} elseif ($null -ne $currentTask) {
-+-    "chore(ai-dev): complete $($currentTask.id) $($currentTask.title)"
-+-} else {
-+-    "chore(ai-dev): automated task commit"
-++    if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-++        $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath ne
-x
-+t
-++_step 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($r
-e
-+v
-++iewGate.nextStep)"
-++        Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-++    }
-++
-++    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fals
-e
-+ 
-++$false 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-++    $stepNumber++
-++
-++    try {
-++        if (Test-PackageFileChanged) {
-++            $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porce
-l
-+a
-++in -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 
-자
-+동
-++ 커밋하지 않습니다."
-++            Stop-Cycle $script:steps "package_files_changed" $false 1
-++        }
-++    } catch {
-++        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain
- 
-+-
-++- package.json package-lock.json" $false $false 1 $_.Exception.Message
-++        Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-++    }
-++
-++    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- 
-p
-+a
-++ckage.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-++    $stepNumber++
-++
-++    if (-not $AllowCommit) {
-++        $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full
-.
-+p
-++s1 -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-++        if ($AllowDirty) {
-++            $commitCommand = "$commitCommand -AllowDirty"
-++        }
-++
-++        if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-++            $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-++        }
-++
-++        $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass 
--
-+F
-++ile scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand
-"
-++        Stop-Cycle $script:steps "allow_commit_required" $false 1
-++    }
-++
-++    $commitArguments = Get-CommitArguments
-++    $commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-++    if ($commitArguments.Count -gt 0) {
-++        $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-++    }
-++
-++    Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArgume
-n
-+t
-++s
-++    $stepNumber++
-++
-++    try {
-++        $commitGate = Get-CommitGate
-++    } catch {
-++        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastC
-o
-+m
-++mitHash 확인" $false $false 1 $_.Exception.Message
-++        Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-++    }
-++
-++    if ($commitGate.lastCommand -ne "commit" -or $commitGate.lastCommandStatus -ne "passed" -or -
-n
-+o
-++t (Test-HasValue $commitGate.lastCommitHash)) {
-++        $message = "커밋 완료 상태를 확인하지 못해 complete-task를 실행하지 않습니다. lastCommand=$($commitGate.lastCom
-m
-+a
-++nd), lastCommandStatus=$($commitGate.lastCommandStatus), lastCommitHash=$($commitGate.lastCommitH
-a
-+s
-++h)"
-++        $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastC
-o
-+m
-++mitHash 확인" $false $true 1 $message
-++        Stop-Cycle $script:steps "commit_not_confirmed" $false 1
-++    }
-++
-++    $script:steps += New-StepResult $stepNumber "commit-result-gate" "state.lastCommand/lastCommi
-t
-+H
-++ash 확인" $false $false 0 "커밋 생성 확인: $($commitGate.lastCommitHash)"
-++    $stepNumber++
-++
-++    $resultSummary = "자동 완료: Codex 구현, build/check, Codex 리뷰 pass, 자동 커밋 완료"
-++    Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File scr
-i
-+p
-++ts/ai-dev-complete-task.ps1 -ResultSummary `"$resultSummary`"" $scriptPaths.completeTask @("-Resu
-l
-+t
-++Summary", $resultSummary)
-++    $stepNumber++
-++    $completedTaskCount++
-+ }
-+ 
-+-$taskText = if ($null -ne $currentTask) {
-+-    "$($currentTask.id) $($currentTask.title)"
-+-} elseif (Test-HasValue $currentTaskId) {
-+-    "$currentTaskId unknown"
-+-} else {
-+-    "unknown"
-++Stop-Cycle $script:steps "max_tasks_reached" $true 0
-++
-++ succeeded in 2661ms:
-++param(
-++    [AllowEmptyString()]
-++    [string]$GoalTitle,
-++    [AllowEmptyString()]
-++    [string]$GoalDescription,
-++    [int]$MaxTasks = 1,
-++    [int]$MaxSteps = 20,
-++    [switch]$DryRun,
-++    [switch]$Json,
-++    [switch]$AllowRun,
-++    [switch]$AllowCodex,
-++    [switch]$AllowReviewCodex,
-++    [switch]$AllowCommit,
-++    [switch]$AllowDirty,
-++    [string[]]$CommitFiles,
-++    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
-++)
-++
-++. "$PSScriptRoot\ai-dev-env.ps1"
-++
-++$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-++$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-++$codeFence = '```'
-++$goalRelativePath = ".ai-dev/goal.md"
-++$queueRelativePath = ".ai-dev/queue.json"
-++$stateRelativePath = ".ai-dev/state.json"
-++$promptRelativePath = ".ai-dev/current-task-prompt.md"
-++
-++function Resolve-RepoPath {
-++    param([string]$Path)
-++
-++    if ([System.IO.Path]::IsPathRooted($Path)) {
-++        return $Path
-++    }
-++
-++    return Join-Path $repoRoot $Path
-+ }
-+ 
-+-if ($DryRun) {
-+-    Write-Host "Dry run: git add/commit을 실행하지 않습니다."
-+-    Write-Host "커밋 메시지: $commitMessage"
-+-    Write-Host "현재 task: $taskText"
-+-    Write-Host "커밋 대상 변경 파일:"
-+-    foreach ($line in $targetChangeLines) {
-+-        Write-Host "  - $line"
-++function ConvertTo-RepoRelativePath {
-++    param([string]$Path)
-++
-++    $fullPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $Path))
-++    $rootWithSeparator = $repoRoot.TrimEnd("\") + "\"
-++
-++    if ($fullPath.StartsWith($rootWithSeparator, [System.StringComparison]::OrdinalIgnoreCase)) {
-++        return $fullPath.Substring($rootWithSeparator.Length).Replace("\", "/")
-+     }
-+-    Write-Host "변경 파일 수: $($targetChangeLines.Count)"
-+-    exit 0
-++
-++    return $fullPath.Replace("\", "/")
-+ }
-+ 
-+-try {
-+-    if ($selectedFiles.Count -gt 0) {
-+-        $addOutput = & git add -- @selectedFiles 2>&1 | Out-String
-+-        $addDisplayName = "git add -- <selected files>"
-+-    } else {
-+-        $addOutput = & git add -A 2>&1 | Out-String
-+-        $addDisplayName = "git add -A"
-++function Test-HasValue {
-++    param([object]$Value)
-++
-++    if ($null -eq $Value) {
-++        return $false
-+     }
-+ 
-+-    if ($LASTEXITCODE -ne 0) {
-+-        throw "$addDisplayName 실행에 실패했습니다. exit code: $LASTEXITCODE`n$addOutput"
-++    if ($Value -is [string]) {
-++        return -not [string]::IsNullOrWhiteSpace($Value)
-++    }
-++
-++    return $true
-++}
-++
-++function New-StepResult {
-++    param(
-++        [int]$Step,
-++        [string]$Name,
-++        [string]$Command,
-++        [bool]$Executed,
-++        [bool]$Skipped,
-++        [int]$ExitCode,
-++        [string]$Message
-++    )
-++
-++    return [PSCustomObject][ordered]@{
-++        step = $Step
-++        name = $Name
-++        command = $Command
-++        executed = $Executed
-++        skipped = $Skipped
-++        exitCode = $ExitCode
-++        message = $Message
-++    }
-++}
-++
-++function New-AutoGoalResult {
-++    param(
-++        [object[]]$Steps,
-++        [string]$StoppedReason,
-++        [bool]$Completed,
-++        [int]$ExitCode
-++    )
-++
-++    return [PSCustomObject][ordered]@{
-++        steps = @($Steps)
-++        stoppedReason = $StoppedReason
-++        completed = $Completed
-++        exitCode = $ExitCode
-++        plan = $script:autoGoalPlanPreview
-++        goalPath = $goalRelativePath
-++        queuePath = $queueRelativePath
-++        statePath = $stateRelativePath
-++        promptPath = $promptRelativePath
-++        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-++    }
-++}
-++
-++function Write-AutoGoalResult {
-++    param([object]$Result)
-++
-++    if ($Json) {
-++        $Result | ConvertTo-Json -Depth 30
-++        return
-+     }
-+ 
-+-    if ($selectedFiles.Count -gt 0) {
-+-        $stagedFilesAfterAdd = Invoke-GitCapture -Arguments @("diff", "--cached", "--name-only") 
--
-+-DisplayName "git diff --cached --name-only"
-+-        $stagedFileListAfterAdd = @($stagedFilesAfterAdd -split "`r?`n" | Where-Object { -not [st
-r
-+-ing]::IsNullOrWhiteSpace($_) })
-+-        $unexpectedStagedFilesAfterAdd = @($stagedFileListAfterAdd | Where-Object { $selectedFile
-s
-+- -notcontains $_ })
-++    foreach ($step in @($Result.steps)) {
-++        Write-Host "Step $($step.step): $($step.name)"
-++        Write-Host "  Command: $($step.command)"
-++        Write-Host "  Executed: $($step.executed)"
-++        Write-Host "  Skipped: $($step.skipped)"
-++        Write-Host "  Exit code: $($step.exitCode)"
-++        Write-Host "  Message: $($step.message)"
-++    }
-+ 
-+-        if ($unexpectedStagedFilesAfterAdd.Count -gt 0) {
-+-            throw "선택 파일 외의 staged 파일이 감지되었습니다: $($unexpectedStagedFilesAfterAdd -join ', ')"
-++    if ($null -ne $Result.plan) {
-++        Write-Host "Plan preview:"
-++        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)"
-++        Write-Host "  Current task id: $($Result.plan.queue.currentTaskId)"
-++
-++        foreach ($task in @($Result.plan.queue.tasks)) {
-++            Write-Host "  Task $($task.id): $($task.title)"
-++            Write-Host "    Type: $($task.type)"
-++            Write-Host "    Status: $($task.status)"
-++            Write-Host "    Priority: $($task.priority)"
-++            Write-Host "    Likely files: $((@($task.filesLikelyToChange) -join ', '))"
-++            Write-Host "    Verification: $((@($task.verification) -join ' / '))"
-+         }
-+     }
-+ 
-+-    $commitOutput = & git commit -m $commitMessage 2>&1 | Out-String
-++    Write-Host "Stopped reason: $($Result.stoppedReason)"
-++    Write-Host "Completed: $($Result.completed)"
-++    Write-Host "Exit code: $($Result.exitCode)"
-++}
-++
-++function Stop-AutoGoal {
-++    param(
-++        [object[]]$Steps,
-++        [string]$StoppedReason,
-++        [bool]$Completed,
-++        [int]$ExitCode
-++    )
-++
-++    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReason $Completed $ExitCode)
-++    exit $ExitCode
-++}
-++
-++function Get-InputPreview {
-++    param([string]$RawInput)
-++
-++    if ($null -eq $RawInput) {
-++        return "<null>"
-++    }
-++
-++    $normalized = $RawInput.Replace("`r", " ").Replace("`n", " ").Trim()
-++
-++    if ($normalized.Length -eq 0) {
-++        return "<empty>"
-++    }
-++
-++    if ($normalized.Length -le 500) {
-++        return $normalized
-++    }
-++
-++    return $normalized.Substring(0, 500)
-++}
-++
-++function Get-GitStatusLines {
-++    $statusOutput = & git status --short 2>&1
-+ 
-+     if ($LASTEXITCODE -ne 0) {
-+-        throw "git commit 실행에 실패했습니다. exit code: $LASTEXITCODE`n$commitOutput"
-++        $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $fals
-e
-+ 
-++1 "git status failed: $($statusOutput -join "`n")"
-++        Stop-AutoGoal $script:steps "git_status_failed" $false 1
-+     }
-+ 
-+-    $commitHash = Invoke-GitCapture -Arguments @("rev-parse", "HEAD") -DisplayName "git rev-parse
- 
-+-HEAD"
-+-} catch {
-+-    $errorSummary = $_.Exception.Message
-+-    Save-StateResult $state "failed" $errorSummary $null
-+-    Stop-WithError "자동 커밋에 실패했습니다: $errorSummary"
-++    return @($statusOutput | Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
-+ }
-+ 
-+-Save-StateResult $state "passed" "" $commitHash
-++function Get-JsonObjectCandidates {
-++    param([string]$RawInput)
-+ 
-+-$logTimestamp = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-+-$logEntry = @"
-++    $candidates = @()
-+ 
-+-## $logTimestamp - Commit created
-++    for ($start = 0; $start -lt $RawInput.Length; $start++) {
-++        if ($RawInput[$start] -ne "{") {
-++            continue
-++        }
-+ 
-+-- Task: $taskText
-+-- Commit: $commitHash
-+-- Message: $commitMessage
-+-"@
-++        $depth = 0
-++        $inString = $false
-++        $escaped = $false
-+ 
-+-[System.IO.File]::AppendAllText($loopLogPath, $logEntry, $utf8WithBom)
-++        for ($index = $start; $index -lt $RawInput.Length; $index++) {
-++            $char = $RawInput[$index]
-+ 
-+-Write-Host "커밋 메시지: $commitMessage"
-+-Write-Host "커밋 해시: $commitHash"
-+-Write-Host "변경 파일 수: $($targetChangeLines.Count)"
-+-Write-Host "현재 task: $taskText"
-++            if ($escaped) {
-++                $escaped = $false
-++                continue
-++            }
-+ 
-+-codex
-+-커밋 연결은 `ai-dev-commit.ps1`의 기존 검증 게이트를 재사용하되, full cycle 쪽에서 한 번 더 `review pass`, `next_step comp
-l
-+-ete_task`, package 파일 변경 여부를 확인하도록 넣겠습니다. `complete-task`는 커밋 단계 성공 뒤에만 호출되게 순서를 고정합니다.
-+-apply patch
-+-patch: completed
-+-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-cycle-full.ps1
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..7240437c74a8273e33925eeb7e651c6e85eac995
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,9 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,7 +328,7 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+-@@ -314,11 +373,68 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-+-
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..7240437c74a8273e33925eeb7e651c6e85eac995
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,9 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,7 +328,7 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+-@@ -314,11 +373,68 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-++            if ($char -eq "\") {
-++                $escaped = $true
-++                continue
-++            }
-+ 
-+-codex
-+-패치 후 한 가지 중요한 점은 DryRun과 실제 실행의 의미가 갈리지 않게 하는 것입니다. 이제 문법을 먼저 확인하고, 필요하면 DryRun에서 게이트 표시가 어색한 부분만
- 
-+-좁게 조정하겠습니다.
-+-exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$null = [System.Manage
-m
-+-ent.Automation.Language.Parser]::ParseFile('"'scripts/ai-dev-auto-cycle-full.ps1', [ref]"'$null, 
-[
-+-ref]$parseErrors); if ($parseErrors.Count -gt 0) { $parseErrors | ForEach-Object { $_.Message }; 
-e
-+-xit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-local
-+-exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path sc
-r
-+-ipts/ai-dev-auto-cycle-full.ps1 -Pattern "review-gate|package-change-gate|AllowCommit|complete-ta
-s
-+-k|CommitFiles|plannedSteps" -Context 2,2' in D:\ai-apps\planpilot-local
-+- succeeded in 2171ms:
-+-syntax ok
-+-존재하지 않는 변수에 [ref]을(를) 적용할 수 없습니다.
-+-위치 줄:2 문자:1
-+-+ $null = [System.Management.Automation.Language.Parser]::ParseFile('sc ...
-+-+ ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
-+-    + CategoryInfo          : InvalidOperation: (parseErrors:VariablePath) [], RuntimeException
-+-    + FullyQualifiedErrorId : NonExistingVariableReference
-+- 
-++            if ($char -eq '"') {
-++                $inString = -not $inString
-++                continue
-++            }
-+ 
-+- succeeded in 2288ms:
-+-
-+-  scripts\ai-dev-auto-cycle-full.ps1:6:    [switch]$AllowCodex,
-+-  scripts\ai-dev-auto-cycle-full.ps1:7:    [switch]$AllowReviewCodex,
-+-> scripts\ai-dev-auto-cycle-full.ps1:8:    [switch]$AllowCommit,
-+-  scripts\ai-dev-auto-cycle-full.ps1:9:    [switch]$AllowDirty,
-+-> scripts\ai-dev-auto-cycle-full.ps1:10:    [string[]]$CommitFiles
-+-  scripts\ai-dev-auto-cycle-full.ps1:11:)
-+-  scripts\ai-dev-auto-cycle-full.ps1:12:
-+-  scripts\ai-dev-auto-cycle-full.ps1:246:    $arguments = @()
-+-  scripts\ai-dev-auto-cycle-full.ps1:247:
-+-> scripts\ai-dev-auto-cycle-full.ps1:248:    if ($null -ne $CommitFiles -and $CommitFiles.Count -
-g
-+-t 0) {
-+-> scripts\ai-dev-auto-cycle-full.ps1:249:        $normalizedFiles = @($CommitFiles | ForEach-Obje
-c
-+-t { $_ -split "," } |
-+- Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
-+-  scripts\ai-dev-auto-cycle-full.ps1:250:
-+-  scripts\ai-dev-auto-cycle-full.ps1:251:        if ($normalizedFiles.Count -gt 0) {
-+-  scripts\ai-dev-auto-cycle-full.ps1:306:        runReviewCodex = Get-ScriptPath "ai-dev-run-revi
-e
-+-w-codex.ps1"
-+-  scripts\ai-dev-auto-cycle-full.ps1:307:        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-> scripts\ai-dev-auto-cycle-full.ps1:308:        completeTask = Get-ScriptPath "ai-dev-complete-t
-a
-+-sk.ps1"
-+-  scripts\ai-dev-auto-cycle-full.ps1:309:    }
-+-  scripts\ai-dev-auto-cycle-full.ps1:310:} catch {
-+-  scripts\ai-dev-auto-cycle-full.ps1:313:}
-+-  scripts\ai-dev-auto-cycle-full.ps1:314:
-+-> scripts\ai-dev-auto-cycle-full.ps1:315:$plannedSteps = @(
-+-  scripts\ai-dev-auto-cycle-full.ps1:316:    "make-prompt",
-+-  scripts\ai-dev-auto-cycle-full.ps1:317:    "run-codex",
-+-  scripts\ai-dev-auto-cycle-full.ps1:321:    "run-review-codex",
-+-  scripts\ai-dev-auto-cycle-full.ps1:322:    "commit",
-+-> scripts\ai-dev-auto-cycle-full.ps1:323:    "complete-task"
-+-  scripts\ai-dev-auto-cycle-full.ps1:324:)
-+-  scripts\ai-dev-auto-cycle-full.ps1:325:
-+-> scripts\ai-dev-auto-cycle-full.ps1:326:if ($plannedSteps.Count -gt $MaxSteps) {
-+-  scripts\ai-dev-auto-cycle-full.ps1:327:    Stop-Cycle $script:steps "max_steps_too_small_for_fu
-l
-+-l_cycle" $false 1
-+-  scripts\ai-dev-auto-cycle-full.ps1:328:}
-+-  scripts\ai-dev-auto-cycle-full.ps1:379:    $reviewGate = Get-ReviewGate
-+-  scripts\ai-dev-auto-cycle-full.ps1:380:} catch {
-+-> scripts\ai-dev-auto-cycle-full.ps1:381:    $script:steps += New-StepResult $stepNumber "review-
-g
-+-ate" "state/review-re
-+-sponse 확인" $false $false 1 $_.Exception.Message
-+-  scripts\ai-dev-auto-cycle-full.ps1:382:    Stop-Cycle $script:steps "review_gate_failed" $false
- 
-+-1
-+-  scripts\ai-dev-auto-cycle-full.ps1:383:}
-+-  scripts\ai-dev-auto-cycle-full.ps1:384:
-+-  scripts\ai-dev-auto-cycle-full.ps1:385:if ($reviewGate.lastCommandStatus -ne "passed") {
-+-> scripts\ai-dev-auto-cycle-full.ps1:386:    $script:steps += New-StepResult $stepNumber "review-
-g
-+-ate" "state.lastComma
-+-ndStatus 확인" $false $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommand
-S
-+-tatus)"
-+-  scripts\ai-dev-auto-cycle-full.ps1:387:    Stop-Cycle $script:steps "review_save_not_passed" $f
-a
-+-lse 1
-+-  scripts\ai-dev-auto-cycle-full.ps1:388:}
-+-  scripts\ai-dev-auto-cycle-full.ps1:389:
-+-  scripts\ai-dev-auto-cycle-full.ps1:390:if ($reviewGate.decision -ne "pass") {
-+-> scripts\ai-dev-auto-cycle-full.ps1:391:    $script:steps += New-StepResult $stepNumber "review-
-g
-+-ate" "state.lastRevie
-+-wDecision 확인" $false $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewG
-a
-+-te.decision)"
-+-  scripts\ai-dev-auto-cycle-full.ps1:392:    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-  scripts\ai-dev-auto-cycle-full.ps1:393:}
-+-  scripts\ai-dev-auto-cycle-full.ps1:394:
-+-  scripts\ai-dev-auto-cycle-full.ps1:395:if (Test-HasValue $reviewGate.nextStep -and $reviewGate.
-n
-+-extStep -ne "complete
-+-_task") {
-+-> scripts\ai-dev-auto-cycle-full.ps1:396:    $script:steps += New-StepResult $stepNumber "review-
-g
-+-ate" "$reviewResponse
-+-RelativePath next_step 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task
-를
-+- 실행하지 않습니다: $($review
-+-Gate.nextStep)"
-+-  scripts\ai-dev-auto-cycle-full.ps1:397:    Stop-Cycle $script:steps "review_next_step_not_compl
-e
-+-te_task" $false 0
-+-  scripts\ai-dev-auto-cycle-full.ps1:398:}
-+-  scripts\ai-dev-auto-cycle-full.ps1:399:
-+-> scripts\ai-dev-auto-cycle-full.ps1:400:$script:steps += New-StepResult $stepNumber "review-gate
-"
-+- "state.lastReviewDec
-+-ision 확인" $false $false 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-  scripts\ai-dev-auto-cycle-full.ps1:401:$stepNumber++
-+-  scripts\ai-dev-auto-cycle-full.ps1:402:
-+-  scripts\ai-dev-auto-cycle-full.ps1:403:try {
-+-  scripts\ai-dev-auto-cycle-full.ps1:404:    if (Test-PackageFileChanged) {
-+-> scripts\ai-dev-auto-cycle-full.ps1:405:        $script:steps += New-StepResult $stepNumber "pac
-k
-+-age-change-gate" "git
-+- status --porcelain -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lo
-c
-+-k.json 변경이 감지되어 자동 커밋
-+-하지 않습니다."
-+-  scripts\ai-dev-auto-cycle-full.ps1:406:        Stop-Cycle $script:steps "package_files_changed"
- 
-+-$false 1
-+-  scripts\ai-dev-auto-cycle-full.ps1:407:    }
-+-  scripts\ai-dev-auto-cycle-full.ps1:408:} catch {
-+-> scripts\ai-dev-auto-cycle-full.ps1:409:    $script:steps += New-StepResult $stepNumber "package
--
-+-change-gate" "git sta
-+-tus --porcelain -- package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-  scripts\ai-dev-auto-cycle-full.ps1:410:    Stop-Cycle $script:steps "package_change_gate_failed
-"
-+- $false 1
-+-  scripts\ai-dev-auto-cycle-full.ps1:411:}
-+-  scripts\ai-dev-auto-cycle-full.ps1:412:
-+-> scripts\ai-dev-auto-cycle-full.ps1:413:$script:steps += New-StepResult $stepNumber "package-cha
-n
-+-ge-gate" "git status 
-+---porcelain -- package.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다
-.
-+-"
-+-  scripts\ai-dev-auto-cycle-full.ps1:414:$stepNumber++
-+-  scripts\ai-dev-auto-cycle-full.ps1:415:
-+-> scripts\ai-dev-auto-cycle-full.ps1:416:if (-not $AllowCommit) {
-+-> scripts\ai-dev-auto-cycle-full.ps1:417:    $commitCommand = "powershell -ExecutionPolicy Bypass
- 
-+--File scripts/ai-dev-
-+-auto-cycle-full.ps1 -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-  scripts\ai-dev-auto-cycle-full.ps1:418:    if ($AllowDirty) {
-+-  scripts\ai-dev-auto-cycle-full.ps1:419:        $commitCommand = "$commitCommand -AllowDirty"
-+-  scripts\ai-dev-auto-cycle-full.ps1:420:    }
-+-  scripts\ai-dev-auto-cycle-full.ps1:421:
-+-> scripts\ai-dev-auto-cycle-full.ps1:422:    if ($null -ne $CommitFiles -and $CommitFiles.Count -
-g
-+-t 0) {
-+-> scripts\ai-dev-auto-cycle-full.ps1:423:        $commitCommand = "$commitCommand -CommitFiles $(
-$
-+-CommitFiles -join ','
-+-)"
-+-  scripts\ai-dev-auto-cycle-full.ps1:424:    }
-+-  scripts\ai-dev-auto-cycle-full.ps1:425:
-+-> scripts\ai-dev-auto-cycle-full.ps1:426:    $script:steps += New-StepResult $stepNumber "commit"
- 
-+-"powershell -Executio
-+-nPolicy Bypass -File scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명
-령
-+-: $commitCommand"
-+-  scripts\ai-dev-auto-cycle-full.ps1:427:    Stop-Cycle $script:steps "allow_commit_required" $fa
-l
-+-se 1
-+-  scripts\ai-dev-auto-cycle-full.ps1:428:}
-+-  scripts\ai-dev-auto-cycle-full.ps1:437:$stepNumber++
-+-  scripts\ai-dev-auto-cycle-full.ps1:438:
-+-> scripts\ai-dev-auto-cycle-full.ps1:439:Invoke-CycleCommand $stepNumber "complete-task" "powersh
-e
-+-ll -ExecutionPolicy B
-+-ypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.
-c
-+-ompleteTask @("-Resul
-+-tSummary", "리뷰 pass 후 자동 커밋 완료")
-+-  scripts\ai-dev-auto-cycle-full.ps1:440:Stop-Cycle $script:steps "completed" $true 0
-+-
-+-
-+-
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..7240437c74a8273e33925eeb7e651c6e85eac995
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,9 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,7 +328,7 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+-@@ -314,11 +373,68 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-++            if ($inString) {
-++                continue
-++            }
-+ 
-+-codex
-+-문법 검증 명령에서 `[ref]` 대상 변수를 초기화하지 않아 PowerShell 경고가 섞였습니다. 스크립트 자체 문법은 통과했지만, 검증 명령을 바로잡아 다시 돌리기 전에
- 
-+-DryRun이 실제 상태를 읽지 않고 전체 단계를 보여주도록 보완하겠습니다.
-+-apply patch
-+-patch: completed
-+-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-cycle-full.ps1
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,11 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "review-gate",
-+-+    "package-change-gate",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,14 +330,14 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/
-a
-+-i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-+- $stepNumber++
-+- 
-+--if (-not $AllowCodex) {
-+-+if (-not $AllowCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -303,7 +364,7 @@
-+- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File s
-c
-+-ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-+- $stepNumber++
-+- 
-+--if (-not $AllowReviewCodex) {
-+-+if (-not $AllowReviewCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -314,11 +375,79 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($DryRun) {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Byp
-a
-+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "
-D
-+-ryRun: task 완료 처리를 실행하지 않았습니다."
-+-+    Stop-Cycle $script:steps "dry_run" $false 0
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-+-
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,11 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "review-gate",
-+-+    "package-change-gate",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,14 +330,14 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/
-a
-+-i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-+- $stepNumber++
-+- 
-+--if (-not $AllowCodex) {
-+-+if (-not $AllowCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -303,7 +364,7 @@
-+- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File s
-c
-+-ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-+- $stepNumber++
-+- 
-+--if (-not $AllowReviewCodex) {
-+-+if (-not $AllowReviewCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -314,11 +375,79 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($DryRun) {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Byp
-a
-+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "
-D
-+-ryRun: task 완료 처리를 실행하지 않았습니다."
-+-+    Stop-Cycle $script:steps "dry_run" $false 0
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-++            if ($char -eq "{") {
-++                $depth++
-++                continue
-++            }
-+ 
-+-codex
-+-스크립트는 T005 흐름으로 바뀌었습니다. 이제 `.ai-dev/README.md`의 T004 자리표시자 설명을 T005 기준으로 갱신해 사용자가 Allow 옵션, pass 
-게
-+-이트, package 중단 조건을 확인할 수 있게 하겠습니다.
-+-exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Select-String -Path .a
-i
-+--dev/README.md -Pattern "full auto-cycle 초안|T004|AllowCommit|package|complete-task" -Context 2,4'
- 
-+-in D:\ai-apps\planpilot-local
-+- succeeded in 3078ms:
-+-
-+-  .ai-dev\README.md:113:
-+-  .ai-dev\README.md:114:```powershell
-+-> .ai-dev\README.md:115:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1
- 
-+--ResultSummary "T001 
-+-분석 완료"
-+-  .ai-dev\README.md:116:```
-+-  .ai-dev\README.md:117:
-+-  .ai-dev\README.md:118:특정 task를 완료 처리하려면 `-TaskId`를 지정한다.
-+-  .ai-dev\README.md:119:
-+-  .ai-dev\README.md:120:```powershell
-+-> .ai-dev\README.md:121:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1
- 
-+--TaskId T001 -ResultS
-+-ummary "분석 완료"
-+-  .ai-dev\README.md:122:```
-+-  .ai-dev\README.md:123:
-+-  .ai-dev\README.md:124:다음 task로 자동 이동하지 않으려면 `-NoNext` 옵션을 사용한다.
-+-  .ai-dev\README.md:125:
-+-  .ai-dev\README.md:126:```powershell
-+-> .ai-dev\README.md:127:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task.ps1
- 
-+--NoNext
-+-  .ai-dev\README.md:128:```
-+-  .ai-dev\README.md:129:
-+-  .ai-dev\README.md:130:task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Codex 실
-행
-+-, GPT 리뷰, build/test 
-+-실행, git commit은 수행하지 않는다.
-+-  .ai-dev\README.md:131:
-+-  .ai-dev\README.md:132:## 프로젝트 검증 실행
-+-  .ai-dev\README.md:133:
-+-> .ai-dev\README.md:134:`package.json`에 정의된 build, test, lint script를 확인하고 실행 결과를 `test-result.md
-`
-+-와 `state.json`에 기록한다.
-+-  .ai-dev\README.md:135:
-+-  .ai-dev\README.md:136:```powershell
-+-  .ai-dev\README.md:137:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.ps1
-+-  .ai-dev\README.md:138:```
-+-  .ai-dev\README.md:418:리뷰 저장 후 decision별 다음 단계:
-+-  .ai-dev\README.md:419:
-+-> .ai-dev\README.md:420:- `pass`: `ai-dev-complete-task.ps1` 실행 또는 커밋 조건을 검토한다.
-+-  .ai-dev\README.md:421:- `revise`: `ai-dev-make-revise-prompt.ps1`로 재수정 프롬프트를 만든 뒤 Codex/Cline에 
-수
-+-동 전달한다.
-+-  .ai-dev\README.md:422:- `blocked`: 사용자 판단이 필요하므로 루프를 중단하고 요구사항이나 범위를 조정한다.
-+-  .ai-dev\README.md:423:
-+-  .ai-dev\README.md:424:`review-prompt.md`에는 diff, 테스트 결과, 로컬 경로, 민감 정보가 포함될 수 있다. ChatGPT에 붙여넣기 
-전
-+-에 사용자가 공유 범위를 확인해야 한다
-+-.
-+-  .ai-dev\README.md:566:- `scripts/ai-dev-run-codex.ps1`: `.ai-dev/current-task-prompt.md`를 Codex
- 
-+-CLI에 전달해 현재 task 구현을 
-+-수행한다.
-+-  .ai-dev\README.md:567:- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Code
-x
-+- CLI에 전달해 JSON 리뷰를 생성
-+-하고 저장 흐름에 연결한다.
-+-> .ai-dev\README.md:568:- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 
-저
-+-장, Codex 리뷰, save-rev
-+-iew, pass 시 commit/complete-task를 연결한다.
-+-  .ai-dev\README.md:569:- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력
-하
-+-면 goal/queue/state 생성
-+-, current-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
-+-  .ai-dev\README.md:570:
-+-  .ai-dev\README.md:571:초기 안전 기준:
-+-  .ai-dev\README.md:572:
-+-  .ai-dev\README.md:574:- Codex 구현 실행은 `AllowCodex`가 있을 때만 수행한다.
-+-  .ai-dev\README.md:575:- Codex 리뷰 실행은 `AllowReviewCodex`가 있을 때만 수행한다.
-+-> .ai-dev\README.md:576:- git commit은 `AllowCommit`이 있을 때만 수행한다.
-+-  .ai-dev\README.md:577:- auto-goal에서 실제 full cycle 실행은 `AllowRun` 또는 명시적 실행 옵션이 있을 때만 수행한다.
-+-  .ai-dev\README.md:578:- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
-+-> .ai-dev\README.md:579:- build/check 실패, package 파일 변경, 리뷰 decision이 `pass`가 아닌 경우 자동 커밋하지 않는다.
-+-  .ai-dev\README.md:580:- `git reset`, `git clean`, `npm install`, `git push`는 자동 실행하지 않는다.
-+-  .ai-dev\README.md:581:
-+-  .ai-dev\README.md:582:자동화 로그와 결과 파일:
-+-  .ai-dev\README.md:583:
-+-  .ai-dev\README.md:623:```
-+-  .ai-dev\README.md:624:
-+-> .ai-dev\README.md:625:이 스크립트는 Codex에 전달하는 프롬프트 끝에 추가 안전 규칙을 붙인다. git commit, git reset, git cle
-a
-+-n, npm install, packa
-+-ge 파일 수정, 현재 task 범위 밖 작업을 금지한다. 스크립트 자체도 build/test/lint, git add, git commit을 실행하지 않는다.
-+-  .ai-dev\README.md:626:
-+-  .ai-dev\README.md:627:### Codex 리뷰 실행
-+-  .ai-dev\README.md:628:
-+-  .ai-dev\README.md:629:`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/review-
-r
-+-esponse.json`에 저장한다. 
-+-실행 전 `git status --short`를 확인하고, 작업 트리가 dirty이면 기본 중단한다. 리뷰 대상 변경사항이 남아 있는 상태에서 실행해야 하는 경우에만 `-Al
-l
-+-owDirty`를 명시한다.
-+-  .ai-dev\README.md:667:- 이 스크립트는 git add, git commit, build/test/lint, GPT API 호출을 수행하지 않는다.
-+-  .ai-dev\README.md:668:
-+-> .ai-dev\README.md:669:### full auto-cycle 초안
-+-  .ai-dev\README.md:670:
-+-  .ai-dev\README.md:671:`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build che
-c
-+-k, diff 저장, strict re
-+-view prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-+-  .ai-dev\README.md:672:
-+-> .ai-dev\README.md:673:T004 버전의 실행 단계:
-+-  .ai-dev\README.md:674:
-+-  .ai-dev\README.md:675:1. `ai-dev-make-prompt.ps1`
-+-  .ai-dev\README.md:676:2. `ai-dev-run-codex.ps1`
-+-  .ai-dev\README.md:677:3. `ai-dev-check.ps1 -BuildOnly`
-+-  .ai-dev\README.md:692:```
-+-  .ai-dev\README.md:693:
-+-> .ai-dev\README.md:694:기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 
-제
-+-한이며, T004 초안은 task 완료
-+- 처리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
-+-  .ai-dev\README.md:695:
-+-  .ai-dev\README.md:696:```powershell
-+-  .ai-dev\README.md:697:powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.p
-s
-+-1 -DryRun -MaxTasks 1
-+- -MaxSteps 20
-+-  .ai-dev\README.md:698:```
-+-  .ai-dev\README.md:716:```
-+-  .ai-dev\README.md:717:
-+-> .ai-dev\README.md:718:`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-
-d
-+-ev-complete-task.ps1`
-+- 연결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는
-다
-+-.
-+-  .ai-dev\README.md:719:
-+-  .ai-dev\README.md:720:### 목표 입력 기반 자동 실행
-+-  .ai-dev\README.md:721:
-+-  .ai-dev\README.md:722:최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있
-게
-+- 하는 것이다.
-+-  .ai-dev\README.md:725:
-+-  .ai-dev\README.md:726:```powershell
-+-> .ai-dev\README.md:727:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -
-G
-+-oalTitle "업무 검색 결과 하이
-+-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex 
--
-+-AllowCommit -MaxTasks
-+- 1
-+-  .ai-dev\README.md:728:```
-+-  .ai-dev\README.md:729:
-+-  .ai-dev\README.md:730:예정 동작:
-+-  .ai-dev\README.md:731:
-+-  .ai-dev\README.md:743:- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-+-  .ai-dev\README.md:744:- full cycle 호출은 `-AllowRun` 또는 그에 준하는 명시적 실행 옵션이 있을 때만 수행한다.
-+-> .ai-dev\README.md:745:- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 
-커
-+-밋하지 않는다.
-+-  .ai-dev\README.md:746:
-+-  .ai-dev\README.md:747:## 운영 원칙
-+-  .ai-dev\README.md:748:
-+-  .ai-dev\README.md:749:- 저장소의 `AGENTS.md`, 사용자 지시, 보안 정책이 자동 개발 루프보다 우선한다.
-+-
-+-
-+-
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,11 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "review-gate",
-+-+    "package-change-gate",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,14 +330,14 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/
-a
-+-i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-+- $stepNumber++
-+- 
-+--if (-not $AllowCodex) {
-+-+if (-not $AllowCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -303,7 +364,7 @@
-+- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File s
-c
-+-ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-+- $stepNumber++
-+- 
-+--if (-not $AllowReviewCodex) {
-+-+if (-not $AllowReviewCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -314,11 +375,79 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($DryRun) {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Byp
-a
-+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "
-D
-+-ryRun: task 완료 처리를 실행하지 않았습니다."
-+-+    Stop-Cycle $script:steps "dry_run" $false 0
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-++            if ($char -eq "}") {
-++                $depth--
-+ 
-+-exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'Get-Content -Path .ai-
-d
-+-ev/README.md | Select-Object -Skip 668 -First 55' in D:\ai-apps\planpilot-local
-+- succeeded in 1406ms:
-+-### full auto-cycle 초안
-++                if ($depth -eq 0) {
-++                    $candidates += $RawInput.Substring($start, $index - $start + 1).Trim()
-++                    break
-++                }
-++            }
-++        }
-++    }
-+ 
-+-`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict revi
-e
-+-w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-++    return @($candidates | Select-Object -Unique)
-++}
-+ 
-+-T004 버전의 실행 단계:
-++function Get-MissingFields {
-++    param(
-++        [object]$InputObject,
-++        [string[]]$RequiredFields
-++    )
-+ 
-+-1. `ai-dev-make-prompt.ps1`
-+-2. `ai-dev-run-codex.ps1`
-+-3. `ai-dev-check.ps1 -BuildOnly`
-+-4. `ai-dev-save-diff.ps1`
-+-5. `ai-dev-make-review-prompt.ps1 -Strict`
-+-6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
-++    $missingFields = @()
-+ 
-+-먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-++    foreach ($field in $RequiredFields) {
-++        if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
-++            $missingFields += $field
-++        }
-++    }
-+ 
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun
-+-```
-++    return $missingFields
-++}
-+ 
-+-자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다.
-++function Get-QueueValidationErrors {
-++    param([object]$Queue)
-+ 
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-+-```
-++    $errors = @()
-++    $requiredFields = @("goalTitle", "goalSource", "createdAt", "updatedAt", "currentTaskId", "ta
-s
-+k
-++s")
-+ 
-+-기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 처
-리
-+-를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
-++    foreach ($field in @(Get-MissingFields $Queue $requiredFields)) {
-++        $errors += "queue missing field: $field"
-++    }
-+ 
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -
-M
-+-axSteps 20
-+-```
-++    if ($errors.Count -gt 0) {
-++        return $errors
-++    }
-+ 
-+-`-AllowCodex` 없이 실제 실행하면 prompt 생성 이후 Codex 구현 실행 직전에서 중단하고 추천 명령을 출력한다. Codex 구현까지 허용하려면 명시적으로 `
--
-+-AllowCodex`를 지정한다.
-++    if (-not (Test-HasValue $Queue.goalTitle)) {
-++        $errors += "queue.goalTitle must not be empty."
-++    }
-+ 
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex
-+-```
-++    if ($Queue.goalSource -ne $goalRelativePath) {
-++        $errors += "queue.goalSource must be $goalRelativePath."
-++    }
-+ 
-+-Codex 구현과 Codex 리뷰까지 이어가려면 `-AllowCodex`와 `-AllowReviewCodex`를 함께 지정한다.
-++    if ($Queue.tasks -isnot [System.Collections.IEnumerable] -or $Queue.tasks -is [string]) {
-++        $errors += "queue.tasks must be an array."
-++        return $errors
-++    }
-+ 
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+-ewCodex -MaxTasks 1
-+-```
-++    $tasks = @($Queue.tasks)
-+ 
-+-dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-c
-o
-+-dex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-++    if ($tasks.Count -lt 1) {
-++        $errors += "queue.tasks must contain at least one task."
-++        return $errors
-++    }
-+ 
-+-```powershell
-+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRev
-i
-+-ewCodex -AllowDirty -MaxTasks 1
-+-```
-++    $taskIds = @()
-+ 
-+-`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 연
-결
-+-은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
-++    foreach ($task in $tasks) {
-++        $taskRequiredFields = @("id", "title", "description", "type", "status", "priority", "depe
-n
-+d
-++sOn", "filesLikelyToChange", "verification", "commitMessage")
-++        $taskMissingFields = @(Get-MissingFields $task $taskRequiredFields)
-+ 
-+-### 목표 입력 기반 자동 실행
-++        foreach ($field in $taskMissingFields) {
-++            $errors += "task missing field: $field"
-++        }
-+ 
-+-최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 하는 것이다.
-++        if ($taskMissingFields.Count -gt 0) {
-++            continue
-++        }
-+ 
-++        if (-not (Test-HasValue $task.id)) {
-++            $errors += "task.id must not be empty."
-++        } else {
-++            $taskIds += [string]$task.id
-++        }
-+ 
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,11 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "review-gate",
-+-+    "package-change-gate",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,14 +330,14 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/
-a
-+-i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-+- $stepNumber++
-+- 
-+--if (-not $AllowCodex) {
-+-+if (-not $AllowCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -303,7 +364,7 @@
-+- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File s
-c
-+-ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-+- $stepNumber++
-+- 
-+--if (-not $AllowReviewCodex) {
-+-+if (-not $AllowReviewCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -314,11 +375,79 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($DryRun) {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Byp
-a
-+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "
-D
-+-ryRun: task 완료 처리를 실행하지 않았습니다."
-+-+    Stop-Cycle $script:steps "dry_run" $false 0
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-++        if (-not (Test-HasValue $task.title)) {
-++            $errors += "task.title must not be empty."
-++        }
-+ 
-+-codex
-+-README의 full cycle 설명은 아직 T004 기준이라 실제 동작과 어긋납니다. 해당 섹션만 갱신하고, 다른 정책/운영 문구는 그대로 두겠습니다.
-+-apply patch
-+-patch: completed
-+-D:\ai-apps\planpilot-local\.ai-dev\README.md
-+-diff --git a/.ai-dev/README.md b/.ai-dev/README.md
-+-index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..b50e832b1d99857130daa9949b9f52658b299bb3
-+---- a/.ai-dev/README.md
-+-+++ b/.ai-dev/README.md
-+-@@ -668,9 +668,9 @@
-+- 
-+- ### full auto-cycle 초안
-+- 
-+--`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict rev
-i
-+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-+-+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict rev
-i
-+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-+- 
-+--T004 버전의 실행 단계:
-+-+T005 버전의 실행 단계:
-+- 
-+- 1. `ai-dev-make-prompt.ps1`
-+- 2. `ai-dev-run-codex.ps1`
-+-@@ -678,6 +678,10 @@
-+- 4. `ai-dev-save-diff.ps1`
-+- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-+- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
-+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
-+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
-+-+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
-+-+10. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-+- 
-+- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-+- 
-+-@@ -691,7 +695,7 @@
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-+- ```
-+- 
-+--기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 
-처
-+-리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
-+-+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전
-을
-+- 위해 현재 task 한 개만 다룬다.
-+- 
-+- ```powershell
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 
--
-+-MaxSteps 20
-+-@@ -709,13 +713,19 @@
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -MaxTasks 1
-+- ```
-+- 
-+-+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정
-하
-+-면 선택한 파일만 커밋 대상으로 전달한다.
-+-+
-+-+```powershell
-+-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks
- 
-+-1
-+-+```
-+-+
-+- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-
-c
-+-odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-+- 
-+- ```powershell
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -AllowDirty -MaxTasks 1
-+- ```
-+- 
-+--`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 
-연
-+-결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
-+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 compl
-e
-+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `packag
-e
-+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-+- 
-+- ### 목표 입력 기반 자동 실행
-+- 
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,11 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "review-gate",
-+-+    "package-change-gate",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,14 +330,14 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/
-a
-+-i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-+- $stepNumber++
-+- 
-+--if (-not $AllowCodex) {
-+-+if (-not $AllowCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -303,7 +364,7 @@
-+- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File s
-c
-+-ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-+- $stepNumber++
-+- 
-+--if (-not $AllowReviewCodex) {
-+-+if (-not $AllowReviewCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -314,11 +375,79 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($DryRun) {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Byp
-a
-+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "
-D
-+-ryRun: task 완료 처리를 실행하지 않았습니다."
-+-+    Stop-Cycle $script:steps "dry_run" $false 0
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-+-
-+-diff --git a/.ai-dev/README.md b/.ai-dev/README.md
-+-index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..b50e832b1d99857130daa9949b9f52658b299bb3
-+---- a/.ai-dev/README.md
-+-+++ b/.ai-dev/README.md
-+-@@ -668,9 +668,9 @@
-+- 
-+- ### full auto-cycle 초안
-+- 
-+--`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict rev
-i
-+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-+-+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict rev
-i
-+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-+- 
-+--T004 버전의 실행 단계:
-+-+T005 버전의 실행 단계:
-+- 
-+- 1. `ai-dev-make-prompt.ps1`
-+- 2. `ai-dev-run-codex.ps1`
-+-@@ -678,6 +678,10 @@
-+- 4. `ai-dev-save-diff.ps1`
-+- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-+- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
-+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
-+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
-+-+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
-+-+10. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-+- 
-+- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-+- 
-+-@@ -691,7 +695,7 @@
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-+- ```
-+- 
-+--기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 
-처
-+-리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
-+-+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전
-을
-+- 위해 현재 task 한 개만 다룬다.
-+- 
-+- ```powershell
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 
--
-+-MaxSteps 20
-+-@@ -709,13 +713,19 @@
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -MaxTasks 1
-+- ```
-+- 
-+-+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정
-하
-+-면 선택한 파일만 커밋 대상으로 전달한다.
-+-+
-+-+```powershell
-+-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks
- 
-+-1
-+-+```
-+-+
-+- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-
-c
-+-odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-+- 
-+- ```powershell
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -AllowDirty -MaxTasks 1
-+- ```
-+- 
-+--`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 
-연
-+-결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
-+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 compl
-e
-+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `packag
-e
-+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-+- 
-+- ### 목표 입력 기반 자동 실행
-+- 
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-+- 
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+-+    }
-+-+
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-+- 
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-+-     }
-+- } catch {
-+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $false $false 1 $_.Exception.Me
-s
-+-sage
-+-@@ -261,7 +318,11 @@
-+-     "check",
-+-     "save-diff",
-+-     "make-review-prompt",
-+--    "run-review-codex"
-+-+    "run-review-codex",
-+-+    "review-gate",
-+-+    "package-change-gate",
-+-+    "commit",
-+-+    "complete-task"
-+- )
-+- 
-+- if ($plannedSteps.Count -gt $MaxSteps) {
-+-@@ -269,14 +330,14 @@
-+- }
-+- 
-+- if ($MaxTasks -gt 1) {
-+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "T004 초안은 
-t
-+-ask 완료 처리를 하지 않으므로 한 번에 현재 task만 실행합니다."
-+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks" $false $true 0 "초기 full c
-y
-+-cle은 안전을 위해 현재 task 하나만 실행합니다."
-+- }
-+- 
-+- $stepNumber = 1
-+- Invoke-CycleCommand $stepNumber "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/
-a
-+-i-dev-make-prompt.ps1" $scriptPaths.makePrompt @()
-+- $stepNumber++
-+- 
-+--if (-not $AllowCodex) {
-+-+if (-not $AllowCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -303,7 +364,7 @@
-+- Invoke-CycleCommand $stepNumber "make-review-prompt" "powershell -ExecutionPolicy Bypass -File s
-c
-+-ripts/ai-dev-make-review-prompt.ps1 -Strict" $scriptPaths.makeReviewPrompt @("-Strict")
-+- $stepNumber++
-+- 
-+--if (-not $AllowReviewCodex) {
-+-+if (-not $AllowReviewCodex -and -not $DryRun) {
-+-     $command = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -All
-o
-+-wCodex -AllowReviewCodex -MaxTasks $MaxTasks"
-+-     if ($AllowDirty) {
-+-         $command = "$command -AllowDirty"
-+-@@ -314,11 +375,79 @@
-+- }
-+- 
-+- Invoke-CycleCommand $stepNumber "run-review-codex" "powershell -ExecutionPolicy Bypass -File scr
-i
-+-pts/ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview" $scriptPaths.runReviewCodex @("-AllowDir
-t
-+-y", "-SaveReview")
-+-+$stepNumber++
-+- 
-+--$finalMessage = "T004 초안은 여기서 중단합니다. 자동 커밋과 complete-task는 T005에서 연결합니다."
-+--if ($AllowCommit) {
-+--    $finalMessage = "$finalMessage -AllowCommit은 지정되었지만 이번 버전에서는 출력만 하고 사용하지 않습니다."
-+-+if ($DryRun) {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 0 "DryRun: git add/commit을 실행하지 않았습니다."
-+-+    $stepNumber++
-+-+    $script:steps += New-StepResult $stepNumber "complete-task" "powershell -ExecutionPolicy Byp
-a
-+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $false $true 0 "
-D
-+-ryRun: task 완료 처리를 실행하지 않았습니다."
-+-+    Stop-Cycle $script:steps "dry_run" $false 0
-+- }
-+- 
-+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-placeholder" "T005에서 ai-dev-c
-o
-+-mmit.ps1 및 ai-dev-complete-task.ps1 연결 예정" $false $true 0 $finalMessage
-+--Stop-Cycle $script:steps "t004_commit_complete_not_implemented" $false 0
-+-+try {
-+-+    $reviewGate = Get-ReviewGate
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state/review-response 확인" $false 
-$
-+-false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "review_gate_failed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.lastCommandStatus -ne "passed") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastCommandStatus 확인" $fals
-e
-+- $true 1 "리뷰 저장 또는 직전 명령이 passed가 아니므로 자동 커밋하지 않습니다: $($reviewGate.lastCommandStatus)"
-+-+    Stop-Cycle $script:steps "review_save_not_passed" $false 1
-+-+}
-+-+
-+-+if ($reviewGate.decision -ne "pass") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $fal
-s
-+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($reviewGate.decision)"
-+-+    Stop-Cycle $script:steps "review_not_pass" $false 0
-+-+}
-+-+
-+-+if (Test-HasValue $reviewGate.nextStep -and $reviewGate.nextStep -ne "complete_task") {
-+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$reviewResponseRelativePath next_
-s
-+-tep 확인" $false $true 0 "리뷰 next_step이 complete_task가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: $($rev
-i
-+-ewGate.nextStep)"
-+-+    Stop-Cycle $script:steps "review_next_step_not_complete_task" $false 0
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.lastReviewDecision 확인" $false $
-f
-+-alse 0 "리뷰 pass 확인. 커밋 게이트로 진행합니다."
-+-+$stepNumber++
-+-+
-+-+try {
-+-+    if (Test-PackageFileChanged) {
-+-+        $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelai
-n
-+- -- package.json package-lock.json" $false $true 1 "package.json 또는 package-lock.json 변경이 감지되어 자동
- 
-+-커밋하지 않습니다."
-+-+        Stop-Cycle $script:steps "package_files_changed" $false 1
-+-+    }
-+-+} catch {
-+-+    $script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain --
- 
-+-package.json package-lock.json" $false $false 1 $_.Exception.Message
-+-+    Stop-Cycle $script:steps "package_change_gate_failed" $false 1
-+-+}
-+-+
-+-+$script:steps += New-StepResult $stepNumber "package-change-gate" "git status --porcelain -- pac
-k
-+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용 여부를 확인합니다."
-+-+$stepNumber++
-+-+
-+-+if (-not $AllowCommit) {
-+-+    $commitCommand = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1
-+- -AllowCodex -AllowReviewCodex -AllowCommit -MaxTasks $MaxTasks"
-+-+    if ($AllowDirty) {
-+-+        $commitCommand = "$commitCommand -AllowDirty"
-+-+    }
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $commitCommand = "$commitCommand -CommitFiles $($CommitFiles -join ',')"
-+-+    }
-+-+
-+-+    $script:steps += New-StepResult $stepNumber "commit" "powershell -ExecutionPolicy Bypass -Fi
-l
-+-e scripts/ai-dev-commit.ps1" $false $true 1 "자동 커밋에는 -AllowCommit이 필요합니다. 추천 명령: $commitCommand"
-+-+    Stop-Cycle $script:steps "allow_commit_required" $false 1
-+-+}
-+-+
-+-+$commitArguments = Get-CommitArguments
-+-+$commitCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit.ps1"
-+-+if ($commitArguments.Count -gt 0) {
-+-+    $commitCommandText = "$commitCommandText $($commitArguments -join ' ')"
-+-+}
-+-+
-+-+Invoke-CycleCommand $stepNumber "commit" $commitCommandText $scriptPaths.commit $commitArguments
-+-+$stepNumber++
-+-+
-+-+Invoke-CycleCommand $stepNumber "complete-task" "powershell -ExecutionPolicy Bypass -File script
-s
-+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $scriptPaths.completeTask @("-Re
-s
-+-ultSummary", "리뷰 pass 후 자동 커밋 완료")
-+-+Stop-Cycle $script:steps "completed" $true 0
-++        if (-not (Test-HasValue $task.description)) {
-++            $errors += "task.description must not be empty."
-++        }
-++
-++        if (@("analysis", "implementation", "documentation", "verification") -notcontains $task.t
-y
-+p
-++e) {
-++            $errors += "invalid task.type: $($task.type)"
-++        }
-++
-++        if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
-++            $errors += "invalid task.status: $($task.status)"
-++        }
-++
-++        if (@("P0", "P1", "P2") -notcontains $task.priority) {
-++            $errors += "invalid task.priority: $($task.priority)"
-++        }
-++
-++        if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is [stri
-n
-+g
-++]) {
-++            $errors += "task.dependsOn must be an array: $($task.id)"
-++        }
-++
-++        if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.filesLike
-l
-+y
-++ToChange -is [string]) {
-++            $errors += "task.filesLikelyToChange must be an array: $($task.id)"
-++        }
-++
-++        if ($task.verification -isnot [System.Collections.IEnumerable] -or $task.verification -is
- 
-+[
-++string]) {
-++            $errors += "task.verification must be an array: $($task.id)"
-++        }
-++    }
-++
-++    if ((@($tasks | Where-Object { $_.status -eq "in_progress" })).Count -ne 1) {
-++        $errors += "queue.tasks must contain exactly one in_progress task."
-++    }
-++
-++    if (-not (Test-HasValue $Queue.currentTaskId)) {
-++        $errors += "queue.currentTaskId must not be empty."
-++    } elseif ($taskIds -notcontains [string]$Queue.currentTaskId) {
-++        $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-++    }
-++
-++    return $errors
-++}
-++
-++function Get-StateValidationErrors {
-++    param(
-++        [object]$State,
-++        [object]$Queue
-++    )
-++
-++    $errors = @()
-++    $requiredFields = @("goalStatus", "currentTaskId", "currentLoop", "maxLoopsPerTask", "repeate
-d
-+F
-++ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDecision", "last
-R
-+e
-++viewSeverity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
-++
-++    foreach ($field in @(Get-MissingFields $State $requiredFields)) {
-++        $errors += "state missing field: $field"
-++    }
-++
-++    if ($errors.Count -gt 0) {
-++        return $errors
-++    }
-++
-++    if (@("idle", "in_progress", "completed", "blocked") -notcontains $State.goalStatus) {
-++        $errors += "invalid state.goalStatus: $($State.goalStatus)"
-++    }
-++
-++    if ($State.currentTaskId -ne $Queue.currentTaskId) {
-++        $errors += "state.currentTaskId must match queue.currentTaskId."
-++    }
-++
-++    if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-++        $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-++    }
-++
-++    if (@("not_started", "pass", "revise", "blocked") -notcontains $State.lastReviewDecision) {
-++        $errors += "invalid state.lastReviewDecision: $($State.lastReviewDecision)"
-++    }
-++
-++    return $errors
-++}
-++
-++function Get-AutoGoalValidationErrors {
-++    param([object]$Plan)
-++
-++    $errors = @()
-++
-++    foreach ($field in @(Get-MissingFields $Plan @("goalMarkdown", "queue", "state"))) {
-++        $errors += "plan missing field: $field"
-++    }
-++
-++    if ($errors.Count -gt 0) {
-++        return $errors
-++    }
-++
-++    if (-not (Test-HasValue $Plan.goalMarkdown)) {
-++        $errors += "goalMarkdown must not be empty."
-++    }
-++
-++    $errors += @(Get-QueueValidationErrors $Plan.queue)
-++    $errors += @(Get-StateValidationErrors $Plan.state $Plan.queue)
-++
-++    return $errors
-++}
-++
-++function New-DryRunAutoGoalPlan {
-++    param(
-++        [string]$Title,
-++        [string]$Description
-++    )
-++
-++    $now = (Get-Date).ToUniversalTime().ToString("o")
-++    $safeCommitTitle = $Title.Trim()
-++
-++    return [PSCustomObject][ordered]@{
-++        goalMarkdown = @"
-++# 紐⑺몴
-++
-++$safeCommitTitle
-++
-++## 諛곌꼍
-++
-++$($Description.Trim())
-++
-++## ?깃났 湲곗?
-++
-++- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-++- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-++- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++
-++## ?쒖빟?ы빆
-++
-++- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-++- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++
-++## ?쒖쇅 踰붿쐞
-++
-++- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++
-++## ?섎룞 ?뺤씤
-++
-++- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-++- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-++- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++"@
-++        queue = [PSCustomObject][ordered]@{
-++            goalTitle = $safeCommitTitle
-++            goalSource = $goalRelativePath
-++            createdAt = $now
-++            updatedAt = $now
-++            currentTaskId = "T001"
-++            tasks = @(
-++                [PSCustomObject][ordered]@{
-++                    id = "T001"
-++                    title = $safeCommitTitle
-++                    description = $Description.Trim()
-++                    type = "implementation"
-++                    status = "in_progress"
-++                    priority = "P0"
-++                    dependsOn = @()
-++                    filesLikelyToChange = @()
-++                    verification = @(
-++                        "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-++                        "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-++                    )
-++                    commitMessage = $null
-++                }
-++            )
-++        }
-++        state = [PSCustomObject][ordered]@{
-++            goalStatus = "in_progress"
-++            currentTaskId = "T001"
-++            currentLoop = 0
-++            maxLoopsPerTask = 2
-++            repeatedFailureCount = 0
-++            lastCommand = $null
-++            lastCommandStatus = "not_started"
-++            lastErrorSummary = $null
-++            lastReviewDecision = "not_started"
-++            lastReviewSeverity = $null
-++            lastCommitHash = $null
-++            startedAt = $now
-++            updatedAt = $now
-++            stopReason = $null
-++        }
-++    }
-++}
-++
-++function ConvertFrom-CodexAutoGoalOutput {
-++    param([string]$RawInput)
-++
-++    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-++        try {
-++            $parsed = $candidate | ConvertFrom-Json
-++
-++            if ($null -eq $parsed -or $parsed -is [System.Array]) {
-++                continue
-++            }
-++
-++            if (@(Get-AutoGoalValidationErrors $parsed).Count -gt 0) {
-++                continue
-++            }
-++
-++            return [PSCustomObject]@{
-++                Parsed = $parsed
-++                JsonText = ($parsed | ConvertTo-Json -Depth 30)
-++            }
-++        } catch {
-++        }
-++    }
-++
-++    throw "No valid auto-goal JSON object was found in Codex output."
-++}
-++
-++function New-CodexAutoGoalPrompt {
-++    param(
-++        [string]$Title,
-++        [string]$Description
-++    )
-++
-++    return @"
-++You are preparing local AI Dev Loop state files for the repository at $repoRoot.
-++
-++Return exactly one JSON object and no markdown fences or commentary.
-++
-++The user supplied this new goal:
-++Title: $Title
-++Description: $Description
-++
-++Create a small, safe goal plan for this repository. The response object must have these fields:
-++- goalMarkdown: a markdown string for .ai-dev/goal.md. It must include "# Goal", Background, Succ
-e
-+s
-++s Criteria, Constraints, Out of Scope, and Manual Verification sections.
-++- queue: an object for .ai-dev/queue.json.
-++- state: an object for .ai-dev/state.json.
-++
-++queue rules:
-++- goalTitle must equal the supplied title.
-++- goalSource must be ".ai-dev/goal.md".
-++- currentTaskId must be "T001".
-++- tasks must contain one to three tasks.
-++- T001 must be status "in_progress"; later tasks, if any, must be "pending".
-++- each task must include id, title, description, type, status, priority, dependsOn, filesLikelyTo
-C
-+h
-++ange, verification, commitMessage.
-++- type must be one of analysis, implementation, documentation, verification.
-++- priority must be P0, P1, or P2.
-++- dependsOn, filesLikelyToChange, and verification must be arrays.
-++- commitMessage must be null or a short English commit message.
-++
-++state rules:
-++- goalStatus must be "in_progress".
-++- currentTaskId must be "T001".
-++- currentLoop must be 0.
-++- maxLoopsPerTask must be 2.
-++- repeatedFailureCount must be 0.
-++- lastCommand must be null.
-++- lastCommandStatus must be "not_started".
-++- lastErrorSummary must be null.
-++- lastReviewDecision must be "not_started".
-++- lastReviewSeverity must be null.
-++- lastCommitHash must be null.
-++- startedAt and updatedAt must be ISO 8601 strings.
-++- stopReason must be null.
-++
-++Safety rules:
-++- Do not mention server APIs, login, cloud sync, npm install, git reset, git clean, git push, DB 
-d
-+e
-++letion, or broad rewrites as implementation steps.
-++- Prefer one small implementation task when the goal is small.
-++- Use Korean for user-facing task titles, descriptions, verification, and goalMarkdown.
-++"@
-++}
-++
-++function Invoke-CycleCommand {
-++    param(
-++        [int]$StepNumber,
-++        [string]$Name,
-++        [string]$Command,
-++        [string]$ScriptPath,
-++        [string[]]$Arguments
-++    )
-++
-++    if ($DryRun) {
-++        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: child 
-s
-+c
-++ript was not executed."
-++        return
-++    }
-++
-++    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
-++    $exitCode = $LASTEXITCODE
-++    $message = $output.Trim()
-++
-++    if ([string]::IsNullOrWhiteSpace($message)) {
-++        $message = "completed"
-++    }
-++
-++    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-++
-++    if ($exitCode -ne 0) {
-++        Stop-AutoGoal $script:steps "$Name`_failed" $false 1
-++    }
-++}
-++
-++function Get-FullCycleArguments {
-++    $arguments = @("-MaxTasks", ([string]$MaxTasks), "-MaxSteps", ([string]$MaxSteps))
-++
-++    if ($AllowCodex) {
-++        $arguments += "-AllowCodex"
-++    }
-++
-++    if ($AllowReviewCodex) {
-++        $arguments += "-AllowReviewCodex"
-++    }
-++
-++    if ($AllowCommit) {
-++        $arguments += "-AllowCommit"
-++    }
-++
-++    # auto-goal writes goal/queue/state/current prompt/result files after the initial dirty gate.
-++    # The downstream full cycle must tolerate those intended artifacts.
-++    $arguments += "-AllowDirty"
-++
-++    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-++        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -no
-t
-+ 
-++[string]::IsNullOrWhiteSpace($_) })
-++
-++        if ($normalizedFiles.Count -gt 0) {
-++            $arguments += "-CommitFiles"
-++            $arguments += ($normalizedFiles -join ",")
-++        }
-++    }
-++
-++    return $arguments
-++}
-++
-++Set-Location $repoRoot
-++
-++$script:steps = @()
-++$script:autoGoalPlanPreview = $null
-++$resolvedGoalPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $goalRelativePath))
-++$resolvedQueuePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $queueRelativePath))
-++$resolvedStatePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $stateRelativePath))
-++$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $ResultPath))
-++
-++if (-not (Test-HasValue $GoalTitle)) {
-++    $script:steps += New-StepResult 0 "validate-input" "check GoalTitle" $false $false 1 "GoalTit
-l
-+e
-++ must not be empty."
-++    Stop-AutoGoal $script:steps "invalid_goal_title" $false 1
-++}
-++
-++if (-not (Test-HasValue $GoalDescription)) {
-++    $script:steps += New-StepResult 0 "validate-input" "check GoalDescription" $false $false 1 "G
-o
-+a
-++lDescription must not be empty."
-++    Stop-AutoGoal $script:steps "invalid_goal_description" $false 1
-++}
-++
-++if ($MaxTasks -lt 1) {
-++    $script:steps += New-StepResult 0 "validate-input" "check MaxTasks" $false $false 1 "MaxTasks
- 
-+m
-++ust be at least 1."
-++    Stop-AutoGoal $script:steps "max_tasks_must_be_at_least_1" $false 1
-++}
-++
-++if ($MaxSteps -lt 1) {
-++    $script:steps += New-StepResult 0 "validate-input" "check MaxSteps" $false $false 1 "MaxSteps
- 
-+m
-++ust be at least 1."
-++    Stop-AutoGoal $script:steps "max_steps_must_be_at_least_1" $false 1
-++}
-++
-++$makePromptPath = Join-Path $PSScriptRoot "ai-dev-make-prompt.ps1"
-++$autoCycleFullPath = Join-Path $PSScriptRoot "ai-dev-auto-cycle-full.ps1"
-++
-++foreach ($requiredScript in @($makePromptPath, $autoCycleFullPath)) {
-++    if (-not (Test-Path -LiteralPath $requiredScript -PathType Leaf)) {
-++        $script:steps += New-StepResult 0 "prepare" "check required scripts" $false $false 1 "Mis
-s
-+i
-++ng required script: $requiredScript"
-++        Stop-AutoGoal $script:steps "prepare_failed" $false 1
-++    }
-++}
-++
-++$shouldRunFullCycle = [bool]($AllowRun -or $AllowCodex -or $AllowReviewCodex -or $AllowCommit)
-++$fullCycleArguments = @(Get-FullCycleArguments)
-++$fullCycleCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.
-p
-+s
-++1 $($fullCycleArguments -join ' ')".Trim()
-++
-++$script:steps += New-StepResult 1 "validate-input" "check GoalTitle/GoalDescription" $false $fals
-e
-+ 
-++0 "Input validation completed: $GoalTitle"
-++
-++if ($DryRun) {
-++    $script:autoGoalPlanPreview = New-DryRunAutoGoalPlan $GoalTitle.Trim() $GoalDescription.Trim(
-)
-++    $previewValidationErrors = @(Get-AutoGoalValidationErrors $script:autoGoalPlanPreview)
-++
-++    if ($previewValidationErrors.Count -gt 0) {
-++        $script:steps += New-StepResult 2 "preview-plan" "local dry-run plan preview" $false $fal
-s
-+e
-++ 1 "DryRun preview plan validation failed: $($previewValidationErrors -join '; ')"
-++        Stop-AutoGoal $script:steps "dry_run_preview_invalid" $false 1
-++    }
-++
-++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 0 "
-D
-+r
-++yRun: dirty worktree gate was not executed."
-++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false
- 
-+$
-++true 0 "DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, task: $GoalTit
-l
-+e
-++"
-++    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation
-"
-+ 
-++$false $true 0 "DryRun: preview goal/queue/state plan passed local schema validation."
-++    $script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath,
- 
-+$
-++stateRelativePath" $false $true 0 "DryRun: state files were not written."
-++    $script:steps += New-StepResult 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scr
-i
-+p
-++ts/ai-dev-make-prompt.ps1" $false $true 0 "DryRun: current-task-prompt.md was not generated."
-++
-++    if ($shouldRunFullCycle) {
-++        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 
-"
-+D
-++ryRun: explicit run option is present, but full cycle was not executed. -AllowRun only invokes th
-e
-+ 
-++full-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-++    } else {
-++        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 
-"
-+D
-++ryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the fu
-l
-+l
-++-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-++    }
-++
-++    Stop-AutoGoal $script:steps "dry_run" $false 0
-++}
-++
-++$statusLines = @(Get-GitStatusLines)
-++
-++if ($statusLines.Count -gt 0 -and -not $AllowDirty) {
-++    $dirtyText = ($statusLines -join "`n")
-++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 1 "
-W
-+o
-++rktree is dirty. Use -AllowDirty only when this is intentional.`n$dirtyText"
-++    Stop-AutoGoal $script:steps "dirty_worktree" $false 1
-++}
-++
-++if ($statusLines.Count -gt 0) {
-++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 
-"
-+A
-++llowDirty is set. DirtyCount: $($statusLines.Count)"
-++} else {
-++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 
-"
-+W
-++orktree is clean."
-++}
-++
-++$codexCommand = Get-Command codex -ErrorAction SilentlyContinue
-++
-++if ($null -eq $codexCommand) {
-++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false
- 
-+$
-++false 1 "Codex CLI was not found."
-++    Stop-AutoGoal $script:steps "codex_not_found" $false 1
-++}
-++
-++$plannerPrompt = New-CodexAutoGoalPrompt $GoalTitle.Trim() $GoalDescription.Trim()
-++$startedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-++$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-++$codexExitCode = $LASTEXITCODE
-++$endedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-++
-++$resultContent = @"
-++# Codex Auto Goal Result
-++
-++## Run
-++
-++- Started at: $startedAt
-++- Ended at: $endedAt
-++- Exit code: $codexExitCode
-++- Goal title: $($GoalTitle.Trim())
-++- Command: codex exec <auto-goal planning prompt>
-++
-++## Output
-++
-++${codeFence}text
-++$codexOutput
-++$codeFence
-++"@
-++
-++[System.IO.File]::WriteAllText($resolvedResultPath, $resultContent, $utf8WithBom)
-++
-++if ($codexExitCode -ne 0) {
-++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true 
-$
-+f
-++alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
-++    Stop-AutoGoal $script:steps "codex_plan_failed" $false 1
-++}
-++
-++$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $fal
-s
-+e
-++ 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
-++
-++try {
-++    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-++} catch {
-++    $preview = Get-InputPreview $codexOutput
-++    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation
-"
-+ 
-++$false $false 1 "Codex plan JSON extraction failed: $($_.Exception.Message) Output preview: $prev
-i
-+e
-++w"
-++    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
-++}
-++
-++$script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" $f
-a
-+l
-++se $false 0 "goalMarkdown, queue, and state JSON validation completed. currentTaskId: $($autoGoal
-P
-+l
-++an.Parsed.queue.currentTaskId)"
-++$script:autoGoalPlanPreview = $autoGoalPlan.Parsed
-++
-++$goalMarkdown = [string]$autoGoalPlan.Parsed.goalMarkdown
-++$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 30
-++$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 30
-++
-++[System.IO.File]::WriteAllText($resolvedGoalPath, $goalMarkdown, $utf8WithBom)
-++[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $utf8WithBom)
-++[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $utf8WithBom)
-++
-++$script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, $st
-a
-+t
-++eRelativePath" $true $false 0 "New goal, queue, and state files were written."
-++
-++Invoke-CycleCommand 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make
--
-+p
-++rompt.ps1" $makePromptPath @()
-++
-++if (-not $shouldRunFullCycle) {
-++    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "Ful
-l
-+ 
-++cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the full-cycle wra
-p
-+p
-++er; implementation and review still require -AllowCodex and -AllowReviewCodex."
-++    Stop-AutoGoal $script:steps "prepared_without_full_cycle" $true 0
-++}
-++
-++Invoke-CycleCommand 7 "auto-cycle-full" $fullCycleCommandText $autoCycleFullPath $fullCycleArgume
-n
-+t
-++s
-++
-++Stop-AutoGoal $script:steps "completed" $true 0
-+ 
-+ codex
-+-문서까지 맞췄습니다. 이제 수정한 PowerShell 파일의 파서 문법 검증과 full cycle DryRun을 실행해 실제 하위 스크립트, build, git commit이
- 
-+-호출되지 않는 범위에서 흐름을 확인하겠습니다.
-++기존 auto-goal 스크립트는 큰 틀은 들어가 있지만 DryRun 미리보기의 한국어 템플릿이 깨져 있습니다. 먼저 스키마 예시와 현재 상태 파일 구조를 확인해서 검증 로직
-과
-+ 
-++기본값이 맞는지 대조하겠습니다.
-+ exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$tokens = $null; $pars
-e
-+-Errors = $null; $null = [System.Management.Automation.Language.Parser]::ParseFile('"'scripts/ai-d
-e
-+-v-auto-cycle-full.ps1', [ref]"'$tokens, [ref]$parseErrors); if ($parseErrors.Count -gt 0) { $pars
-e
-+-Errors | ForEach-Object { $_.Message }; exit 1 } else { '"'syntax ok' }" in D:\ai-apps\planpilot-
-l
-+-ocal
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPa
-t
-+h
-++ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\queue.example.json'" in D:\ai-apps\planpilot-local
-++exec
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPa
-t
-+h
-++ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\state.example.json'" in D:\ai-apps\planpilot-local
-++exec
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -Literal
-P
-+a
-++th 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md' -Pattern '목표 입력 기반 자동 실행' -Context 0,90" in
- 
-+D
-++:\ai-apps\planpilot-local
-++ succeeded in 2334ms:
-++{
-++  "goalTitle": "JSON 諛깆뾽 import 寃利?諛?誘몃━蹂닿린 湲곕뒫 異붽?",
-++  "goalSource": ".ai-dev/goal.md",
-++  "createdAt": "2026-06-03T00:00:00.000Z",
-++  "updatedAt": "2026-06-03T00:00:00.000Z",
-++  "currentTaskId": null,
-++  "tasks": [
-++    {
-++      "id": "T001",
-++      "title": "湲곗〈 諛깆뾽 export/import 愿??臾몄꽌? 肄붾뱶 ?꾩튂 ?뺤씤",
-++      "description": "JSON 諛깆뾽 援ъ“, import ?뺤콉, 湲곗〈 寃利??좏떥, SettingsView ?곌껐 吏?먯쓣 ?뺤씤?섍퀬 援ы쁽 踰붿
-쐞
-+瑜
-++??뺤젙?쒕떎.",
-++      "type": "analysis",
-++      "status": "pending",
-++      "priority": "P0",
-++      "dependsOn": [],
-++      "filesLikelyToChange": [],
-++      "verification": [
-++        "愿??臾몄꽌? 肄붾뱶 ?꾩튂 ?뺤씤",
-++        "?ㅼ젣 DB 諛섏쁺??踰붿쐞?먯꽌 ?쒖쇅?섏뿀?붿? ?뺤씤"
-++      ],
-++      "commitMessage": null
-++    },
-++    {
-++      "id": "T002",
-++      "title": "諛깆뾽 JSON 寃利??좏떥 異붽?",
-++      "description": "format, schemaVersion, ?꾩닔 ?꾨뱶, ?곗씠???붿빟???뺤씤?섎뒗 ?쒖닔 寃利??좏떥??援ы쁽?쒕떎.",
-++      "type": "implementation",
-++      "status": "pending",
-++      "priority": "P0",
-++      "dependsOn": [
-++        "T001"
-++      ],
-++      "filesLikelyToChange": [
-++        "src/utils/importValidation.ts"
-++      ],
-++      "verification": [
-++        "寃利??좏떥??IndexedDB ?곌린瑜??섑뻾?섏? ?딅뒗吏 肄붾뱶 由щ럭",
-++        "npm run build"
-++      ],
-++      "commitMessage": "Add JSON import validation utility"
-++    },
-++    {
-++      "id": "T003",
-++      "title": "import 誘몃━蹂닿린 UI 異붽?",
-++      "description": "?ъ슜?먭? JSON 諛깆뾽 ?뚯씪???좏깮?섍퀬 寃利?寃곌낵 ?붿빟???뺤씤?????덈뒗 誘몃━蹂닿린 UI瑜?異붽??쒕떎.",
-++      "type": "implementation",
-++      "status": "pending",
-++      "priority": "P0",
-++      "dependsOn": [
-++        "T002"
-++      ],
-++      "filesLikelyToChange": [
-++        "src/views/SettingsView.tsx",
-++        "src/App.tsx"
-++      ],
-++      "verification": [
-++        "?뚯씪 ?좏깮 ??DB ?곗씠?곌? 蹂寃쎈릺吏 ?딅뒗吏 ?뺤씤",
-++        "npm run build"
-++      ],
-++      "commitMessage": "Add JSON import preview"
-++    },
-++    {
-++      "id": "T004",
-++      "title": "寃利??ㅽ뙣 硫붿떆吏? ?깃났 ?붿빟 ?쒖떆",
-++      "description": "寃利??ㅽ뙣 ?댁쑀? ?깃났 ??tasks, projects 媛쒖닔 諛?appSettings ?ы븿 ?щ?瑜??ъ슜?먯뿉寃??쒖떆?
-쒕
-+떎
-++.",
-++      "type": "implementation",
-++      "status": "pending",
-++      "priority": "P0",
-++      "dependsOn": [
-++        "T003"
-++      ],
-++      "filesLikelyToChange": [
-++        "src/views/SettingsView.tsx"
-++      ],
-++      "verification": [
-++        "?ㅽ뙣 ?댁쑀媛 ?쒖떆?섎뒗吏 ?섎룞 ?뺤씤",
-++        "?깃났 ?붿빟???쒖떆?섎뒗吏 ?섎룞 ?뺤씤",
-++        "npm run build"
-++      ],
-++      "commitMessage": "Show JSON import validation feedback"
-++    },
-++    {
-++      "id": "T005",
-++      "title": "?섎룞寃利?泥댄겕由ъ뒪???낅뜲?댄듃",
-++      "description": "JSON import 寃利앷낵 誘몃━蹂닿린 湲곕뒫???섎룞 ?뺤씤 ??ぉ??臾몄꽌??諛섏쁺?쒕떎.",
-++      "type": "documentation",
-++      "status": "pending",
-++      "priority": "P1",
-++      "dependsOn": [
-++        "T004"
-++      ],
-++      "filesLikelyToChange": [
-++        "docs/manual-test-checklist.md",
-++        "ROADMAP.md",
-++        "memory-bank/progress.md"
-++      ],
-++      "verification": [
-++        "援ы쁽 踰붿쐞? 臾몄꽌 ?쒗쁽???쇱튂?섎뒗吏 ?뺤씤",
-++        "蹂듭썝 湲곕뒫??援ы쁽??寃껋쑝濡??쒗쁽?섏? ?딆븯?붿? ?뺤씤"
-++      ],
-++      "commitMessage": "Document JSON import preview checks"
-++    },
-++    {
-++      "id": "T006",
-++      "title": "鍮뚮뱶 寃利?諛?理쒖쥌 ?붿빟",
-++      "description": "?꾩껜 蹂寃쎌쓣 鍮뚮뱶?섍퀬 diff瑜?由щ럭????紐⑺몴 ?꾨즺 ?щ?瑜??뺣━?쒕떎.",
-++      "type": "verification",
-++      "status": "pending",
-++      "priority": "P0",
-++      "dependsOn": [
-++        "T005"
-++      ],
-++      "filesLikelyToChange": [],
-++      "verification": [
-++        "npm run build",
-++        "?꾩껜 git diff 由щ럭",
-++        "?ㅼ젣 DB 諛섏쁺, 蹂듭썝, ??뼱?곌린, 蹂묓빀 湲곕뒫??異붽??섏? ?딆븯?붿? ?뺤씤"
-++      ],
-++      "commitMessage": null
-++    }
-++  ]
-++}
-++
-++ succeeded in 2534ms:
-++{
-++  "goalStatus": "idle",
-++  "currentTaskId": null,
-++  "currentLoop": 0,
-++  "maxLoopsPerTask": 2,
-++  "repeatedFailureCount": 0,
-++  "lastCommand": null,
-++  "lastCommandStatus": "not_started",
-++  "lastErrorSummary": null,
-++  "lastReviewDecision": "not_started",
-++  "lastReviewSeverity": null,
-++  "lastCommitHash": null,
-++  "startedAt": null,
-++  "updatedAt": "2026-06-03T00:00:00.000Z",
-++  "stopReason": null
-++}
-++
-++ succeeded in 2545ms:
-++
-++> .ai-dev\README.md:750:### 목표 입력 기반 자동 실행
-++  .ai-dev\README.md:751:
-++  .ai-dev\README.md:752:최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있
-게
-+ 
-++하는 것이다.
-++  .ai-dev\README.md:753:
-++  .ai-dev\README.md:754:먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-++  .ai-dev\README.md:755:
-++  .ai-dev\README.md:756:```powershell
-++  .ai-dev\README.md:757:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -
-G
-+o
-++alTitle "업무 검색 결과 하이
-++라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-++  .ai-dev\README.md:758:```
-++  .ai-dev\README.md:759:
-++  .ai-dev\README.md:760:자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-++  .ai-dev\README.md:761:DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로
- 
-+파
-++일 작성 전에 생성될 goal/tas
-++k 계획을 검토할 수 있다.
-++  .ai-dev\README.md:762:
-++  .ai-dev\README.md:763:```powershell
-++  .ai-dev\README.md:764:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -
-G
-+o
-++alTitle "업무 검색 결과 하이
-++라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-++  .ai-dev\README.md:765:```
-++  .ai-dev\README.md:766:
-++  .ai-dev\README.md:767:실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 
-이
-+ 
-++모드는 `ai-dev-auto-cyc
-++le-full.ps1`을 호출하지 않는다.
-++  .ai-dev\README.md:768:
-++  .ai-dev\README.md:769:```powershell
-++  .ai-dev\README.md:770:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -
-G
-+o
-++alTitle "업무 검색 결과 하이
-++라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-++  .ai-dev\README.md:771:```
-++  .ai-dev\README.md:772:
-++  .ai-dev\README.md:773:생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowR
-e
-+v
-++iewCodex`, `-AllowCo
-++mmit` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다
-.
-+ 
-++실제 구현까지 허용하려면 `-Allo
-++wCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하게 `-AllowCodex 
--
-+A
-++llowReviewCodex -All
-++owCommit`을 함께 지정한다.
-++  .ai-dev\README.md:774:
-++  .ai-dev\README.md:775:```powershell
-++  .ai-dev\README.md:776:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -
-G
-+o
-++alTitle "업무 검색 결과 하이
-++라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex 
--
-+A
-++llowCommit -MaxTasks
-++ 1
-++  .ai-dev\README.md:777:```
-++  .ai-dev\README.md:778:
-++  .ai-dev\README.md:779:동작:
-++  .ai-dev\README.md:780:
-++  .ai-dev\README.md:781:1. `GoalTitle`과 `GoalDescription`을 검증한다.
-++  .ai-dev\README.md:782:2. Codex CLI를 이용해 goal/queue/state 초안을 생성한다.
-++  .ai-dev\README.md:783:3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-++  .ai-dev\README.md:784:4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을 검증한
-다
-+.
-++  .ai-dev\README.md:785:5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한다.
-++  .ai-dev\README.md:786:6. `.ai-dev/current-task-prompt.md`를 생성한다.
-++  .ai-dev\README.md:787:7. 명시적 실행 옵션이 있을 때만 `ai-dev-auto-cycle-full.ps1`로 이어간다.
-++  .ai-dev\README.md:788:8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `current
--
-+t
-++ask-prompt.md`, 결과 파
-++일 때문에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktree 게이
-트
-+는
-++ auto-goal 시작 전에 별도로
-++ 유지한다.
-++  .ai-dev\README.md:789:
-++  .ai-dev\README.md:790:안전 기준:
-++  .ai-dev\README.md:791:
-++  .ai-dev\README.md:792:- `GoalTitle` 또는 `GoalDescription`이 비어 있으면 종료한다.
-++  .ai-dev\README.md:793:- DryRun에서는 생성될 goal/task 계획과 실행 단계만 표시하고 파일을 수정하지 않는다.
-++  .ai-dev\README.md:794:- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-++  .ai-dev\README.md:795:- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지
- 
-+않
-++는다.
-++  .ai-dev\README.md:796:- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-Allow
-C
-+o
-++mmit` 중 하나가 있을 때만 수행
-++한다.
-++  .ai-dev\README.md:797:- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCo
-d
-+e
-++x`, `-AllowReviewCod
-++ex`, `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
-++  .ai-dev\README.md:798:- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 
-커
-+밋
-++하지 않는다.
-++  .ai-dev\README.md:799:- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
-++  .ai-dev\README.md:800:
-++  .ai-dev\README.md:801:## 운영 원칙
-++  .ai-dev\README.md:802:
-++  .ai-dev\README.md:803:- 저장소의 `AGENTS.md`, 사용자 지시, 보안 정책이 자동 개발 루프보다 우선한다.
-++  .ai-dev\README.md:804:- 한 번에 하나의 task만 수행한다.
-++  .ai-dev\README.md:805:- task 단위로 구현, 검증, 리뷰, 커밋한다.
-++  .ai-dev\README.md:806:- 현재 task 범위 밖 파일을 임의로 수정하지 않는다.
-++  .ai-dev\README.md:807:- 검증하지 않은 결과를 통과로 기록하지 않는다.
-++  .ai-dev\README.md:808:- 실제 DB 삭제, 초기화, 복원, 대규모 리팩터링은 초기 실험 범위에서 제외한다.
-++  .ai-dev\README.md:809:
-++  .ai-dev\README.md:810:## 다음 단계
-++  .ai-dev\README.md:811:
-++  .ai-dev\README.md:812:실제 자동 개발 루프 실행기와 상태 갱신 로직은 별도 정책 검토 후 추가한다.
-++  .ai-dev\README.md:813:
-++  .ai-dev\README.md:814:세부 기준은 `docs/ai-dev-loop-policy.md`를 따른다.
-++
-++
-++
-++codex
-++문서의 auto-goal 섹션은 이미 요구사항을 대부분 반영하고 있습니다. 수정은 스크립트의 깨진 DryRun 템플릿과 검증/출력 품질을 중심으로 작게 하겠습니다.
-+ exec
-+-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command 'powershell -ExecutionP
-o
-+-licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 -MaxSteps 20 -Json' in D
-:
-+-\ai-apps\planpilot-local
-+- succeeded in 1811ms:
-+-syntax ok
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$errors = $null; [Syst
-e
-+m
-++.Management.Automation.PSParser]::Tokenize((Get-Content -Raw -LiteralPath '"'D:\\ai-apps\\planpil
-o
-+t
-++-local\\scripts\\ai-dev-auto-goal.ps1'), [ref]"'$errors) | Out-Null; if ($errors.Count -eq 0) { '
-"
-+'
-++OK' } else { "'$errors | Format-List * }' in D:\ai-apps\planpilot-local
-++ succeeded in 1611ms:
-++OK
-+ 
-+- succeeded in 3087ms:
-++codex
-++문법 파서는 통과했습니다. 이제 DryRun JSON을 실행해 실제 출력 구조와 오류 여부를 확인하겠습니다. 이 모드는 파일을 쓰지 않고 하위 스크립트도 실행하지 않도록 설계
-되
-+어
-++ 있습니다.
-++exec
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionP
-o
-+l
-++icy Bypass -File 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -GoalTitle '업무 검색 
-결
-+과
-++ 하이라이트 추가' -GoalDescription '검색어와 일치하는 업무 제목과 메모를 화면에서 강조 표시한다.' -DryRun -Json" in D:\ai-apps\pla
-n
-+p
-++ilot-local
-++ succeeded in 3030ms:
-+ {
-+     "steps":  [
-+                   {
-+                       "step":  1,
-+-                      "name":  "make-prompt",
-+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-p
-r
-+-ompt.ps1",
-++                      "name":  "validate-input",
-++                      "command":  "check GoalTitle/GoalDescription",
-+                       "executed":  false,
-+-                      "skipped":  true,
-++                      "skipped":  false,
-+                       "exitCode":  0,
-+-                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-++                      "message":  "Input validation completed: 업무 검색 결과 하이라이트 추가"
-+                   },
-+                   {
-+                       "step":  2,
-+-                      "name":  "run-codex",
-+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-co
-d
-+-ex.ps1",
-++                      "name":  "dirty-worktree-gate",
-++                      "command":  "git status --short",
-+                       "executed":  false,
-+                       "skipped":  true,
-+                       "exitCode":  0,
-+-                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-++                      "message":  "DryRun: dirty worktree gate was not executed."
-+                   },
-+                   {
-+                       "step":  3,
-+-                      "name":  "check",
-+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-check.
-p
-+-s1 -BuildOnly",
-++                      "name":  "plan-goal",
-++                      "command":  "codex exec \u003cauto-goal planning prompt\u003e",
-+                       "executed":  false,
-+                       "skipped":  true,
-+                       "exitCode":  0,
-+-                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-++                      "message":  "DryRun: Codex goal planning was not executed. Preview currentT
-a
-+s
-++kId: T001, task: 업무 검색 결과 하이라이트 추가"
-+                   },
-+                   {
-+                       "step":  4,
-+-                      "name":  "save-diff",
-+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-d
-i
-+-ff.ps1",
-++                      "name":  "validate-generated-json",
-++                      "command":  "goal/queue/state JSON validation",
-+                       "executed":  false,
-+                       "skipped":  true,
-+                       "exitCode":  0,
-+-                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-++                      "message":  "DryRun: preview goal/queue/state plan passed local schema vali
-d
-+a
-++tion."
-+                   },
-+                   {
-+                       "step":  5,
-+-                      "name":  "make-review-prompt",
-+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-r
-e
-+-view-prompt.ps1 -Strict",
-++                      "name":  "write-state-files",
-++                      "command":  ".ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
-+                       "executed":  false,
-+                       "skipped":  true,
-+                       "exitCode":  0,
-+-                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-++                      "message":  "DryRun: state files were not written."
-+                   },
-+                   {
-+                       "step":  6,
-+-                      "name":  "run-review-codex",
-+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-re
-v
-+-iew-codex.ps1 -AllowDirty -SaveReview",
-++                      "name":  "make-prompt",
-++                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-p
-r
-+o
-++mpt.ps1",
-+                       "executed":  false,
-+                       "skipped":  true,
-+                       "exitCode":  0,
-+-                      "message":  "DryRun: 하위 스크립트를 실행하지 않았습니다."
-++                      "message":  "DryRun: current-task-prompt.md was not generated."
-+                   },
-+                   {
-+                       "step":  7,
-+-                      "name":  "review-gate",
-+-                      "command":  "state.lastReviewDecision 확인",
-+-                      "executed":  false,
-+-                      "skipped":  true,
-+-                      "exitCode":  0,
-+-                      "message":  "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았습니다."
-+-                  },
-+-                  {
-+-                      "step":  8,
-+-                      "name":  "package-change-gate",
-+-                      "command":  "git status --porcelain -- package.json package-lock.json",
-+-                      "executed":  false,
-+-                      "skipped":  true,
-+-                      "exitCode":  0,
-+-                      "message":  "DryRun: package 파일 변경 여부를 확인하지 않았습니다."
-+-                  },
-+-                  {
-+-                      "step":  9,
-+-                      "name":  "commit",
-+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-commit
-.
-+-ps1",
-+-                      "executed":  false,
-+-                      "skipped":  true,
-+-                      "exitCode":  0,
-+-                      "message":  "DryRun: git add/commit을 실행하지 않았습니다."
-+-                  },
-+-                  {
-+-                      "step":  10,
-+-                      "name":  "complete-task",
-+-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-comple
-t
-+-e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-++                      "name":  "auto-cycle-full",
-++                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-c
-y
-+c
-++le-full.ps1 -MaxTasks 1 -MaxSteps 20 -AllowDirty",
-+                       "executed":  false,
-+                       "skipped":  true,
-+                       "exitCode":  0,
-+-                      "message":  "DryRun: task 완료 처리를 실행하지 않았습니다."
-++                      "message":  "DryRun: full cycle requires -AllowRun or explicit execution op
-t
-+i
-++ons. -AllowRun only invokes the full-cycle wrapper; implementation and review still require -Allo
-w
-+C
-++odex and -AllowReviewCodex."
-+                   }
-+               ],
-+     "stoppedReason":  "dry_run",
-+     "completed":  false,
-+-    "exitCode":  0
-++    "exitCode":  0,
-++    "plan":  {
-++                 "goalMarkdown":  "# 紐⑺몴\n\n업무 검색 결과 하이라이트 추가\n\n## 諛곌꼍\n\n검색어와 일치하는 업무 제목과 메모를 화
-면
-+에
-++서 강조 표시한다.\n\n## ?깃났 湲곗?\n\n- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.\n- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?
-묒
-+뾽
-++ ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.\n- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.\n\n## ?쒖빟?ы빆\n\n- ?쒕쾭 API, 濡쒓
-렇
-+?
-++? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??\n- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??\n\n## 
-?
-+쒖
-++쇅 踰붿쐞\n\n- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅\n\n## ?섎룞 ?뺤씤\n\n- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃
-
-+?
-++좏븳??\n- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.\n- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.",
-++                 "queue":  {
-++                               "goalTitle":  "업무 검색 결과 하이라이트 추가",
-++                               "goalSource":  ".ai-dev/goal.md",
-++                               "createdAt":  "2026-06-09T13:46:22.3925596Z",
-++                               "updatedAt":  "2026-06-09T13:46:22.3925596Z",
-++                               "currentTaskId":  "T001",
-++                               "tasks":  [
-++                                             {
-++                                                 "id":  "T001",
-++                                                 "title":  "업무 검색 결과 하이라이트 추가",
-++                                                 "description":  "검색어와 일치하는 업무 제목과 메모를 화면에서 강조 표시
-한
-+다
-++.",
-++                                                 "type":  "implementation",
-++                                                 "status":  "in_progress",
-++                                                 "priority":  "P0",
-++                                                 "dependsOn":  [
-++
-++                                                               ],
-++                                                 "filesLikelyToChange":  [
-++
-++                                                                         ],
-++                                                 "verification":  [
-++                                                                      "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗
-吏
-+
-++ ?뺤씤?쒕떎.",
-++                                                                      "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨
-룄
-+濡
-++??ㅽ뻾?쒕떎."
-++                                                                  ],
-++                                                 "commitMessage":  null
-++                                             }
-++                                         ]
-++                           },
-++                 "state":  {
-++                               "goalStatus":  "in_progress",
-++                               "currentTaskId":  "T001",
-++                               "currentLoop":  0,
-++                               "maxLoopsPerTask":  2,
-++                               "repeatedFailureCount":  0,
-++                               "lastCommand":  null,
-++                               "lastCommandStatus":  "not_started",
-++                               "lastErrorSummary":  null,
-++                               "lastReviewDecision":  "not_started",
-++                               "lastReviewSeverity":  null,
-++                               "lastCommitHash":  null,
-++                               "startedAt":  "2026-06-09T13:46:22.3925596Z",
-++                               "updatedAt":  "2026-06-09T13:46:22.3925596Z",
-++                               "stopReason":  null
-++                           }
-++             },
-++    "goalPath":  ".ai-dev/goal.md",
-++    "queuePath":  ".ai-dev/queue.json",
-++    "statePath":  ".ai-dev/state.json",
-++    "promptPath":  ".ai-dev/current-task-prompt.md",
-++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-+ }
-+ 
-+-diff --git a/.ai-dev/README.md b/.ai-dev/README.md
-+-index 5bf9253347c33a852d2bba22cd59a4f1a4918d9c..b50e832b1d99857130daa9949b9f52658b299bb3
-+---- a/.ai-dev/README.md
-+-+++ b/.ai-dev/README.md
-+-@@ -668,9 +668,9 @@
-+- 
-+- ### full auto-cycle 초안
-+- 
-+--`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict rev
-i
-+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-+-+`ai-dev-auto-cycle-full.ps1`는 현재 task 기준으로 prompt 생성, Codex 구현, build check, diff 저장, strict rev
-i
-+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task 완료 처리를 순서대로 연결하는 초안이다.
-+- 
-+--T004 버전의 실행 단계:
-+-+T005 버전의 실행 단계:
-+- 
-+- 1. `ai-dev-make-prompt.ps1`
-+- 2. `ai-dev-run-codex.ps1`
-+-@@ -678,6 +678,10 @@
-+- 4. `ai-dev-save-diff.ps1`
-+- 5. `ai-dev-make-review-prompt.ps1 -Strict`
-+- 6. `ai-dev-run-review-codex.ps1 -AllowDirty -SaveReview`
-+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastReviewDecision`이 `pass`여야 한다.
-+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있으면 자동 커밋하지 않는다.
-+-+9. `-AllowCommit`이 있을 때만 `ai-dev-commit.ps1`을 실행한다.
-+-+10. 커밋 성공 후에만 `ai-dev-complete-task.ps1`을 실행한다.
-+- 
-+- 먼저 DryRun으로 전체 단계를 확인한다. DryRun은 하위 스크립트를 실제 실행하지 않는다.
-+- 
-+-@@ -691,7 +695,7 @@
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Json
-+- ```
-+- 
-+--기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, T004 초안은 task 완료 
-처
-+-리를 하지 않으므로 실제로는 현재 task 한 개만 다룬다.
-+-+기본값은 `MaxTasks 1`, `MaxSteps 20`이다. `MaxTasks`와 `MaxSteps`는 무한 반복을 막기 위한 제한이며, 초기 full cycle은 안전
-을
-+- 위해 현재 task 한 개만 다룬다.
-+- 
-+- ```powershell
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTasks 1 
--
-+-MaxSteps 20
-+-@@ -709,13 +713,19 @@
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -MaxTasks 1
-+- ```
-++codex
-++이제 `ai-dev-auto-goal.ps1`만 수정합니다. DryRun 미리보기의 깨진 문구를 정상 한국어/명확한 섹션으로 바꾸고, Codex가 4개 이상 task를 반환하
-는
-+ 
-++경우를 막는 검증을 추가하겠습니다.
-++2026-06-09T13:46:52.361668Z ERROR codex_core::tools::router: error=apply_patch verification faile
-d
-+:
-++ Failed to find expected lines in D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1:
-++        goalMarkdown = @"
-++# 紐⑺몴
-++
-++$safeCommitTitle
-++
-++## 諛곌꼍
-++
-++$($Description.Trim())
-++
-++## ?깃났 湲곗?
-++
-++- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-++- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-++- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++
-++## ?쒖빟?ы빆
-++
-++- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-++- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++
-++## ?쒖쇅 踰붿쐞
-++
-++- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++
-++## ?섎룞 ?뺤씤
-++
-++- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-++- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-++- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++"@
-++codex
-++패치가 깨진 문자 영역에서 정확히 매칭되지 않았습니다. 해당 함수 주변만 다시 읽고 더 작은 범위로 수정하겠습니다.
-++exec
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$lines = Get-Content -
-L
-+i
-++teralPath '"'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1'; for ("'$i = 230; $i -l
-e
-+ 
-++340; $i++) { if ($i -le $lines.Count) { '"'{0}: {1}' -f "'$i, $lines[$i - 1] } }' in D:\ai-apps\p
-l
-+a
-++npilot-local
-++ succeeded in 1849ms:
-++230: 
-++231:             if ($char -eq "}") {
-++232:                 $depth--
-++233: 
-++234:                 if ($depth -eq 0) {
-++235:                     $candidates += $RawInput.Substring($start, $index - $start + 1).Trim()
-++236:                     break
-++237:                 }
-++238:             }
-++239:         }
-++240:     }
-++241: 
-++242:     return @($candidates | Select-Object -Unique)
-++243: }
-++244: 
-++245: function Get-MissingFields {
-++246:     param(
-++247:         [object]$InputObject,
-++248:         [string[]]$RequiredFields
-++249:     )
-++250: 
-++251:     $missingFields = @()
-++252: 
-++253:     foreach ($field in $RequiredFields) {
-++254:         if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
-++255:             $missingFields += $field
-++256:         }
-++257:     }
-++258: 
-++259:     return $missingFields
-++260: }
-++261: 
-++262: function Get-QueueValidationErrors {
-++263:     param([object]$Queue)
-++264: 
-++265:     $errors = @()
-++266:     $requiredFields = @("goalTitle", "goalSource", "createdAt", "updatedAt", "currentTaskId"
-,
-+ 
-++"tasks")
-++267: 
-++268:     foreach ($field in @(Get-MissingFields $Queue $requiredFields)) {
-++269:         $errors += "queue missing field: $field"
-++270:     }
-++271: 
-++272:     if ($errors.Count -gt 0) {
-++273:         return $errors
-++274:     }
-++275: 
-++276:     if (-not (Test-HasValue $Queue.goalTitle)) {
-++277:         $errors += "queue.goalTitle must not be empty."
-++278:     }
-++279: 
-++280:     if ($Queue.goalSource -ne $goalRelativePath) {
-++281:         $errors += "queue.goalSource must be $goalRelativePath."
-++282:     }
-++283: 
-++284:     if ($Queue.tasks -isnot [System.Collections.IEnumerable] -or $Queue.tasks -is [string]) 
-{
-++285:         $errors += "queue.tasks must be an array."
-++286:         return $errors
-++287:     }
-++288: 
-++289:     $tasks = @($Queue.tasks)
-++290: 
-++291:     if ($tasks.Count -lt 1) {
-++292:         $errors += "queue.tasks must contain at least one task."
-++293:         return $errors
-++294:     }
-++295: 
-++296:     $taskIds = @()
-++297: 
-++298:     foreach ($task in $tasks) {
-++299:         $taskRequiredFields = @("id", "title", "description", "type", "status", "priority", 
-"
-+d
-++ependsOn", "filesLikelyToChange", "verification", "commitMessage")
-++300:         $taskMissingFields = @(Get-MissingFields $task $taskRequiredFields)
-++301: 
-++302:         foreach ($field in $taskMissingFields) {
-++303:             $errors += "task missing field: $field"
-++304:         }
-++305: 
-++306:         if ($taskMissingFields.Count -gt 0) {
-++307:             continue
-++308:         }
-++309: 
-++310:         if (-not (Test-HasValue $task.id)) {
-++311:             $errors += "task.id must not be empty."
-++312:         } else {
-++313:             $taskIds += [string]$task.id
-++314:         }
-++315: 
-++316:         if (-not (Test-HasValue $task.title)) {
-++317:             $errors += "task.title must not be empty."
-++318:         }
-++319: 
-++320:         if (-not (Test-HasValue $task.description)) {
-++321:             $errors += "task.description must not be empty."
-++322:         }
-++323: 
-++324:         if (@("analysis", "implementation", "documentation", "verification") -notcontains $t
-a
-+s
-++k.type) {
-++325:             $errors += "invalid task.type: $($task.type)"
-++326:         }
-++327: 
-++328:         if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
-++329:             $errors += "invalid task.status: $($task.status)"
-++330:         }
-++331: 
-++332:         if (@("P0", "P1", "P2") -notcontains $task.priority) {
-++333:             $errors += "invalid task.priority: $($task.priority)"
-++334:         }
-++335: 
-++336:         if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is 
-[
-+s
-++tring]) {
-++337:             $errors += "task.dependsOn must be an array: $($task.id)"
-++338:         }
-++339: 
-++340:         if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.file
-s
-+L
-++ikelyToChange -is [string]) {
-++
-++exec
-++"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$lines = Get-Content -
-L
-+i
-++teralPath '"'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1'; for ("'$i = 420; $i -l
-e
-+ 
-++510; $i++) { if ($i -le $lines.Count) { '"'{0}: {1}' -f "'$i, $lines[$i - 1] } }' in D:\ai-apps\p
-l
-+a
-++npilot-local
-++ succeeded in 1658ms:
-++420: 
-++421: function New-DryRunAutoGoalPlan {
-++422:     param(
-++423:         [string]$Title,
-++424:         [string]$Description
-++425:     )
-++426: 
-++427:     $now = (Get-Date).ToUniversalTime().ToString("o")
-++428:     $safeCommitTitle = $Title.Trim()
-++429: 
-++430:     return [PSCustomObject][ordered]@{
-++431:         goalMarkdown = @"
-++432: # 紐⑺몴
-++433: 
-++434: $safeCommitTitle
-++435: 
-++436: ## 諛곌꼍
-++437: 
-++438: $($Description.Trim())
-++439: 
-++440: ## ?깃났 湲곗?
-++441: 
-++442: - ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-++443: - ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-++444: - ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++445: 
-++446: ## ?쒖빟?ы빆
-++447: 
-++448: - ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-++449: - ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++450: 
-++451: ## ?쒖쇅 踰붿쐞
-++452: 
-++453: - ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++454: 
-++455: ## ?섎룞 ?뺤씤
-++456: 
-++457: - DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-++458: - ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-++459: - ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++460: "@
-++461:         queue = [PSCustomObject][ordered]@{
-++462:             goalTitle = $safeCommitTitle
-++463:             goalSource = $goalRelativePath
-++464:             createdAt = $now
-++465:             updatedAt = $now
-++466:             currentTaskId = "T001"
-++467:             tasks = @(
-++468:                 [PSCustomObject][ordered]@{
-++469:                     id = "T001"
-++470:                     title = $safeCommitTitle
-++471:                     description = $Description.Trim()
-++472:                     type = "implementation"
-++473:                     status = "in_progress"
-++474:                     priority = "P0"
-++475:                     dependsOn = @()
-++476:                     filesLikelyToChange = @()
-++477:                     verification = @(
-++478:                         "蹂寃?踰붿쐞媛 ?꾩옱 紐⑺몴? ?쇱튂?섎뒗吏 ?뺤씤?쒕떎.",
-++479:                         "?꾩슂??寃쎌슦 npm run build瑜?蹂꾨룄濡??ㅽ뻾?쒕떎."
-++480:                     )
-++481:                     commitMessage = $null
-++482:                 }
-++483:             )
-++484:         }
-++485:         state = [PSCustomObject][ordered]@{
-++486:             goalStatus = "in_progress"
-++487:             currentTaskId = "T001"
-++488:             currentLoop = 0
-++489:             maxLoopsPerTask = 2
-++490:             repeatedFailureCount = 0
-++491:             lastCommand = $null
-++492:             lastCommandStatus = "not_started"
-++493:             lastErrorSummary = $null
-++494:             lastReviewDecision = "not_started"
-++495:             lastReviewSeverity = $null
-++496:             lastCommitHash = $null
-++497:             startedAt = $now
-++498:             updatedAt = $now
-++499:             stopReason = $null
-++500:         }
-++501:     }
-++502: }
-++503: 
-++504: function ConvertFrom-CodexAutoGoalOutput {
-++505:     param([string]$RawInput)
-++506: 
-++507:     foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-++508:         try {
-++509:             $parsed = $candidate | ConvertFrom-Json
-++510: 
-++
-++codex
-++깨진 블록 위치를 확인했습니다. 해당 함수 내부의 here-string과 검증 배열만 교체하고, task 개수 상한 검증을 별도로 추가합니다.
-++apply patch
-++patch: completed
-++D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1
-++diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-++index c30c817ad8291f02334c2667276d837cfaa5c1fa..f7d6707e5216dde854b5f48ddcefe6d07dc87626
-++--- a/scripts/ai-dev-auto-goal.ps1
-+++++ b/scripts/ai-dev-auto-goal.ps1
-++@@ -293,6 +293,10 @@
-++         return $errors
-++     }
-+  
-+-+리뷰가 `pass`이고 package 파일 변경이 없을 때 자동 커밋과 task 완료까지 허용하려면 `-AllowCommit`을 추가한다. `-CommitFiles`를 지정
-하
-+-면 선택한 파일만 커밋 대상으로 전달한다.
-+-+
-+-+```powershell
-+-+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -AllowCommit -CommitFiles scripts/ai-dev-auto-cycle-full.ps1,.ai-dev/README.md -MaxTasks
- 
-+-1
-+-+```
-+++    if ($tasks.Count -gt 3) {
-+++        $errors += "queue.tasks must contain no more than three tasks."
-+++    }
-+ +
-+- dirty worktree에서 Codex 구현을 실행해야 하는 특별한 경우에만 `-AllowDirty`를 추가한다. dirty 상태 기본 중단 정책은 `ai-dev-run-
-c
-+-odex.ps1`와 `ai-dev-run-review-codex.ps1`의 안전 게이트가 담당한다.
-+- 
-+- ```powershell
-+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -AllowCodex -AllowRe
-v
-+-iewCodex -AllowDirty -MaxTasks 1
-+- ```
-+- 
-+--`-AllowCommit` 옵션은 T004에서 파라미터와 출력만 제공한다. 실제 `ai-dev-commit.ps1` 실행과 `ai-dev-complete-task.ps1` 
-연
-+-결은 T005에서 구현한다. 따라서 T004 full auto-cycle은 리뷰 저장 후에도 git add, git commit, complete-task를 수행하지 않는다.
-+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_task`가 아니면 자동 커밋과 compl
-e
-+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `packag
-e
-+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-+- 
-+- ### 목표 입력 기반 자동 실행
-++     $taskIds = @()
-+  
-+-diff --git a/scripts/ai-dev-auto-cycle-full.ps1 b/scripts/ai-dev-auto-cycle-full.ps1
-+-index 27f49eae70ad3c78fd202fe45ab5ff9afccd569a..ca7c819495ff1601486ebf2585a46b9cc1245292
-+---- a/scripts/ai-dev-auto-cycle-full.ps1
-+-+++ b/scripts/ai-dev-auto-cycle-full.ps1
-+-@@ -6,7 +6,8 @@
-+-     [switch]$AllowCodex,
-+-     [switch]$AllowReviewCodex,
-+-     [switch]$AllowCommit,
-+--    [switch]$AllowDirty
-+-+    [switch]$AllowDirty,
-+-+    [string[]]$CommitFiles
-+- )
-+- 
-+- . $PSScriptRoot\ai-dev-env.ps1
-+-@@ -14,8 +15,10 @@
-+- $repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-+- $queueRelativePath = ".ai-dev/queue.json"
-+- $stateRelativePath = ".ai-dev/state.json"
-+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-+- $queuePath = Join-Path $repoRoot $queueRelativePath
-+- $statePath = Join-Path $repoRoot $stateRelativePath
-+-+$reviewResponsePath = Join-Path $repoRoot $reviewResponseRelativePath
-+- 
-+- function Test-HasValue {
-+-     param(
-+-@@ -141,9 +144,6 @@
-+-     Write-Host "Completed: $($Result.completed)"
-+-     Write-Host "Exit code: $($Result.exitCode)"
-+- 
-+--    if ($AllowCommit) {
-+--        Write-Host "AllowCommit: specified, but T004 does not run commit or complete-task."
-+--    }
-+- }
-+- 
-+- function Stop-Cycle {
-+-@@ -202,6 +202,61 @@
-+-     return $scriptPath
-+- }
-++     foreach ($task in $tasks) {
-++
-++diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai-dev-auto-goal.ps1
-++index c30c817ad8291f02334c2667276d837cfaa5c1fa..f7d6707e5216dde854b5f48ddcefe6d07dc87626
-++--- a/scripts/ai-dev-auto-goal.ps1
-+++++ b/scripts/ai-dev-auto-goal.ps1
-++@@ -293,6 +293,10 @@
-++         return $errors
-++     }
-+  
-+-+function Invoke-GitCapture {
-+-+    param(
-+-+        [string[]]$Arguments,
-+-+        [string]$DisplayName
-+-+    )
-+-+
-+-+    $output = & git @Arguments 2>&1 | Out-String
-+-+    $exitCode = $LASTEXITCODE
-+-+
-+-+    if ($exitCode -ne 0) {
-+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$output"
-+-+    }
-+-+
-+-+    return $output.TrimEnd()
-+-+}
-+-+
-+-+function Test-PackageFileChanged {
-+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "package.json", "package-lock.j
-s
-+-on") "git status --porcelain -- package.json package-lock.json"
-+-+    return -not [string]::IsNullOrWhiteSpace($status)
-+-+}
-+-+
-+-+function Get-ReviewGate {
-+-+    $state = Read-JsonFile $statePath $stateRelativePath
-+-+    $nextStep = $null
-+-+
-+-+    if (Test-Path -LiteralPath $reviewResponsePath -PathType Leaf) {
-+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewResponseRelativePath
-+-+
-+-+        if (Test-HasValue $reviewResponse.next_step) {
-+-+            $nextStep = [string]$reviewResponse.next_step
-+-+        }
-+-+    }
-+-+
-+-+    return [PSCustomObject][ordered]@{
-+-+        lastCommandStatus = [string]$state.lastCommandStatus
-+-+        decision = [string]$state.lastReviewDecision
-+-+        nextStep = $nextStep
-+-+    }
-+-+}
-+-+
-+-+function Get-CommitArguments {
-+-+    $arguments = @()
-+-+
-+-+    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-+-+        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -n
-o
-+-t [string]::IsNullOrWhiteSpace($_) })
-+-+
-+-+        if ($normalizedFiles.Count -gt 0) {
-+-+            $arguments += "-Files"
-+-+            $arguments += ($normalizedFiles -join ",")
-+-+        }
-+++    if ($tasks.Count -gt 3) {
-+++        $errors += "queue.tasks must contain no more than three tasks."
-+ +    }
-+ +
-+-+    return $arguments
-+-+}
-+-+
-+- Set-Location $repoRoot
-++     $taskIds = @()
-+  
-+- $script:steps = @()
-+-@@ -249,6 +304,8 @@
-+-         saveDiff = Get-ScriptPath "ai-dev-save-diff.ps1"
-+-         makeReviewPrompt = Get-ScriptPath "ai-dev-make-review-prompt.ps1"
-+-         runReviewCodex = Get-ScriptPath "ai-dev-run-review-codex.ps1"
-+-+        commit = Get-ScriptPath "ai-dev-commit.ps1"
-+-+        completeTask = Get-ScriptPath "ai-dev-complete-task.ps1"
-++     foreach ($task in $tasks) {
-++
-++codex
-++task 개수 상한 검증은 반영됐습니다. 깨진 DryRun 문자열은 큰 블록 단위로 안전하게 교체하겠습니다.
-++2026-06-09T13:47:37.437229Z ERROR codex_core::tools::router: error=apply_patch verification faile
-d
-+:
-++ Failed to find expected lines in D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1:
-++        goalMarkdown = @"
-++# 紐⑺몴
-++
-++$safeCommitTitle
-++
-++## 諛곌꼍
-++
-++$($Description.Trim())
-++
-++## ?깃났 湲곗?
-++
-++- ?낅젰??紐⑺몴?먯꽌 ?묒? ?꾩옱 ?묒뾽??以鍮꾪븷 ???덈떎.
-++- ?꾩껜 ?먮룞 ?ъ씠???ㅽ뻾 ?꾩뿉 ?꾩옱 ?묒뾽 ?꾨＼?꾪듃媛 ?앹꽦?쒕떎.
-++- ?꾩껜 ?먮룞 ?ъ씠?댁? 紐낆떆?곸씤 ?덉슜 ?듭뀡???덉쓣 ?뚮쭔 ?ㅽ뻾?쒕떎.
-++
-++## ?쒖빟?ы빆
-++
-++- ?쒕쾭 API, 濡쒓렇?? ?대씪?곕뱶 ?숆린?? npm install, ?뚭눼?곸씤 git 紐낅졊? ?ъ슜?섏? ?딅뒗??
-++- ??μ냼 ?뺤콉怨??꾩옱 ?묒뾽 踰붿쐞瑜??곕Ⅸ??
-++
-++## ?쒖쇅 踰붿쐞
-++
-++- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅
-++
-++## ?섎룞 ?뺤씤
-++
-++- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-++- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤씤?쒕떎.
-++- ?꾩슂??寃쎌슦 鍮뚮뱶??寃?щ? 蹂꾨룄濡??ㅽ뻾?쒕떎.
-++"@
-++diff --git a/scripts/ai-dev-auto-goal.ps1 b/scripts/ai
-codex
-리뷰 프롬프트는 엄격 리뷰와 JSON 결과를 요구하는 흐름입니다. 제공된 diff가 매우 커서 핵심 산출물인 새 PowerShell 스크립트와 문서 변경 범위만 직접 확인하겠습니
-다.
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
- 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1'" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPa
-th 'D:\\ai-apps\\planpilot-local\\.ai-dev\\review-prompt.md' -Pattern 'Output','JSON','result','dec
-ision'" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Select-String -LiteralPa
-th 'D:\\ai-apps\\planpilot-local\\.ai-dev\\README.md' -Pattern '목표 입력 기반 자동 실행','ai-dev-auto-goal',
-'AllowRun','DryRun JSON' -Context 2,4" in D:\ai-apps\planpilot-local
- succeeded in 2761ms:
-param(
-    [AllowEmptyString()]
-    [string]$GoalTitle,
-    [AllowEmptyString()]
-    [string]$GoalDescription,
-    [int]$MaxTasks = 1,
-    [int]$MaxSteps = 20,
-    [switch]$DryRun,
-    [switch]$Json,
-    [switch]$AllowRun,
-    [switch]$AllowCodex,
-    [switch]$AllowReviewCodex,
-    [switch]$AllowCommit,
-    [switch]$AllowDirty,
-    [string[]]$CommitFiles,
-    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
-)
-
-. "$PSScriptRoot\ai-dev-env.ps1"
-
-$repoRoot = (Resolve-Path (Join-Path $PSScriptRoot "..")).Path
-$utf8WithBom = New-Object System.Text.UTF8Encoding($true)
-$codeFence = '```'
-$goalRelativePath = ".ai-dev/goal.md"
-$queueRelativePath = ".ai-dev/queue.json"
-$stateRelativePath = ".ai-dev/state.json"
-$promptRelativePath = ".ai-dev/current-task-prompt.md"
-
-function Resolve-RepoPath {
-    param([string]$Path)
-
-    if ([System.IO.Path]::IsPathRooted($Path)) {
-        return $Path
-    }
-
-    return Join-Path $repoRoot $Path
-}
-
-function ConvertTo-RepoRelativePath {
-    param([string]$Path)
-
-    $fullPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $Path))
-    $rootWithSeparator = $repoRoot.TrimEnd("\") + "\"
-
-    if ($fullPath.StartsWith($rootWithSeparator, [System.StringComparison]::OrdinalIgnoreCase)) {
-        return $fullPath.Substring($rootWithSeparator.Length).Replace("\", "/")
-    }
-
-    return $fullPath.Replace("\", "/")
-}
-
-function Test-HasValue {
-    param([object]$Value)
-
-    if ($null -eq $Value) {
-        return $false
-    }
-
-    if ($Value -is [string]) {
-        return -not [string]::IsNullOrWhiteSpace($Value)
-    }
-
-    return $true
-}
-
-function New-StepResult {
-    param(
-        [int]$Step,
-        [string]$Name,
-        [string]$Command,
-        [bool]$Executed,
-        [bool]$Skipped,
-        [int]$ExitCode,
-        [string]$Message
-    )
-
-    return [PSCustomObject][ordered]@{
-        step = $Step
-        name = $Name
-        command = $Command
-        executed = $Executed
-        skipped = $Skipped
-        exitCode = $ExitCode
-        message = $Message
-    }
-}
-
-function New-AutoGoalResult {
-    param(
-        [object[]]$Steps,
-        [string]$StoppedReason,
-        [bool]$Completed,
-        [int]$ExitCode
-    )
-
-    return [PSCustomObject][ordered]@{
-        steps = @($Steps)
-        stoppedReason = $StoppedReason
-        completed = $Completed
-        exitCode = $ExitCode
-        plan = $script:autoGoalPlanPreview
-        goalPath = $goalRelativePath
-        queuePath = $queueRelativePath
-        statePath = $stateRelativePath
-        promptPath = $promptRelativePath
-        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-    }
-}
-
-function Write-AutoGoalResult {
-    param([object]$Result)
-
-    if ($Json) {
-        $Result | ConvertTo-Json -Depth 30
-        return
-    }
-
-    foreach ($step in @($Result.steps)) {
-        Write-Host "Step $($step.step): $($step.name)"
-        Write-Host "  Command: $($step.command)"
-        Write-Host "  Executed: $($step.executed)"
-        Write-Host "  Skipped: $($step.skipped)"
-        Write-Host "  Exit code: $($step.exitCode)"
-        Write-Host "  Message: $($step.message)"
-    }
-
-    if ($null -ne $Result.plan) {
-        Write-Host "Plan preview:"
-        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)"
-        Write-Host "  Current task id: $($Result.plan.queue.currentTaskId)"
-
-        foreach ($task in @($Result.plan.queue.tasks)) {
-            Write-Host "  Task $($task.id): $($task.title)"
-            Write-Host "    Type: $($task.type)"
-            Write-Host "    Status: $($task.status)"
-            Write-Host "    Priority: $($task.priority)"
-            Write-Host "    Likely files: $((@($task.filesLikelyToChange) -join ', '))"
-            Write-Host "    Verification: $((@($task.verification) -join ' / '))"
-        }
-    }
-
-    Write-Host "Stopped reason: $($Result.stoppedReason)"
-    Write-Host "Completed: $($Result.completed)"
-    Write-Host "Exit code: $($Result.exitCode)"
-}
-
-function Stop-AutoGoal {
-    param(
-        [object[]]$Steps,
-        [string]$StoppedReason,
-        [bool]$Completed,
-        [int]$ExitCode
-    )
-
-    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReason $Completed $ExitCode)
-    exit $ExitCode
-}
-
-function Get-InputPreview {
-    param([string]$RawInput)
-
-    if ($null -eq $RawInput) {
-        return "<null>"
-    }
-
-    $normalized = $RawInput.Replace("`r", " ").Replace("`n", " ").Trim()
-
-    if ($normalized.Length -eq 0) {
-        return "<empty>"
-    }
-
-    if ($normalized.Length -le 500) {
-        return $normalized
-    }
-
-    return $normalized.Substring(0, 500)
-}
-
-function Get-GitStatusLines {
-    $statusOutput = & git status --short 2>&1
-
-    if ($LASTEXITCODE -ne 0) {
-        $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 
-1 "git status failed: $($statusOutput -join "`n")"
-        Stop-AutoGoal $script:steps "git_status_failed" $false 1
-    }
-
-    return @($statusOutput | Where-Object { -not [string]::IsNullOrWhiteSpace($_) })
-}
-
-function Get-JsonObjectCandidates {
-    param([string]$RawInput)
-
-    $candidates = @()
-
-    for ($start = 0; $start -lt $RawInput.Length; $start++) {
-        if ($RawInput[$start] -ne "{") {
-            continue
-        }
-
-        $depth = 0
-        $inString = $false
-        $escaped = $false
-
-        for ($index = $start; $index -lt $RawInput.Length; $index++) {
-            $char = $RawInput[$index]
-
-            if ($escaped) {
-                $escaped = $false
-                continue
-            }
-
-            if ($char -eq "\") {
-                $escaped = $true
-                continue
-            }
-
-            if ($char -eq '"') {
-                $inString = -not $inString
-                continue
-            }
-
-            if ($inString) {
-                continue
-            }
-
-            if ($char -eq "{") {
-                $depth++
-                continue
-            }
-
-            if ($char -eq "}") {
-                $depth--
-
-                if ($depth -eq 0) {
-                    $candidates += $RawInput.Substring($start, $index - $start + 1).Trim()
-                    break
-                }
-            }
-        }
-    }
-
-    return @($candidates | Select-Object -Unique)
-}
-
-function Get-MissingFields {
-    param(
-        [object]$InputObject,
-        [string[]]$RequiredFields
-    )
-
-    $missingFields = @()
-
-    foreach ($field in $RequiredFields) {
-        if (-not ($InputObject.PSObject.Properties.Name -contains $field)) {
-            $missingFields += $field
-        }
-    }
-
-    return $missingFields
-}
-
-function Get-QueueValidationErrors {
-    param([object]$Queue)
-
-    $errors = @()
-    $requiredFields = @("goalTitle", "goalSource", "createdAt", "updatedAt", "currentTaskId", "task
-s")
-
-    foreach ($field in @(Get-MissingFields $Queue $requiredFields)) {
-        $errors += "queue missing field: $field"
-    }
-
-    if ($errors.Count -gt 0) {
-        return $errors
-    }
-
-    if (-not (Test-HasValue $Queue.goalTitle)) {
-        $errors += "queue.goalTitle must not be empty."
-    }
-
-    if ($Queue.goalSource -ne $goalRelativePath) {
-        $errors += "queue.goalSource must be $goalRelativePath."
-    }
-
-    if ($Queue.tasks -isnot [System.Collections.IEnumerable] -or $Queue.tasks -is [string]) {
-        $errors += "queue.tasks must be an array."
-        return $errors
-    }
-
-    $tasks = @($Queue.tasks)
-
-    if ($tasks.Count -lt 1) {
-        $errors += "queue.tasks must contain at least one task."
-        return $errors
-    }
-
-    if ($tasks.Count -gt 3) {
-        $errors += "queue.tasks must contain no more than three tasks."
-    }
-
-    if ($Queue.currentTaskId -ne "T001") {
-        $errors += "queue.currentTaskId must be T001."
-    }
-
-    $taskIds = @()
-
-    foreach ($task in $tasks) {
-        $taskRequiredFields = @("id", "title", "description", "type", "status", "priority", "depend
-sOn", "filesLikelyToChange", "verification", "commitMessage")
-        $taskMissingFields = @(Get-MissingFields $task $taskRequiredFields)
-
-        foreach ($field in $taskMissingFields) {
-            $errors += "task missing field: $field"
-        }
-
-        if ($taskMissingFields.Count -gt 0) {
-            continue
-        }
-
-        if (-not (Test-HasValue $task.id)) {
-            $errors += "task.id must not be empty."
-        } else {
-            $taskIds += [string]$task.id
-        }
-
-        if (-not (Test-HasValue $task.title)) {
-            $errors += "task.title must not be empty."
-        }
-
-        if (-not (Test-HasValue $task.description)) {
-            $errors += "task.description must not be empty."
-        }
-
-        if (@("analysis", "implementation", "documentation", "verification") -notcontains $task.typ
-e) {
-            $errors += "invalid task.type: $($task.type)"
-        }
-
-        if (@("pending", "in_progress", "done", "blocked") -notcontains $task.status) {
-            $errors += "invalid task.status: $($task.status)"
-        }
-
-        if (@("P0", "P1", "P2") -notcontains $task.priority) {
-            $errors += "invalid task.priority: $($task.priority)"
-        }
-
-        if ($task.dependsOn -isnot [System.Collections.IEnumerable] -or $task.dependsOn -is [string
-]) {
-            $errors += "task.dependsOn must be an array: $($task.id)"
-        }
-
-        if ($task.filesLikelyToChange -isnot [System.Collections.IEnumerable] -or $task.filesLikely
-ToChange -is [string]) {
-            $errors += "task.filesLikelyToChange must be an array: $($task.id)"
-        }
-
-        if ($task.verification -isnot [System.Collections.IEnumerable] -or $task.verification -is [
-string]) {
-            $errors += "task.verification must be an array: $($task.id)"
-        }
-    }
-
-    if ((@($tasks | Where-Object { $_.status -eq "in_progress" })).Count -ne 1) {
-        $errors += "queue.tasks must contain exactly one in_progress task."
-    }
-
-    if (-not (Test-HasValue $Queue.currentTaskId)) {
-        $errors += "queue.currentTaskId must not be empty."
-    } elseif ($taskIds -notcontains [string]$Queue.currentTaskId) {
-        $errors += "queue.currentTaskId does not match a task: $($Queue.currentTaskId)"
-    }
-
-    $firstTask = $tasks | Where-Object { $_.id -eq "T001" } | Select-Object -First 1
-    if ($null -eq $firstTask) {
-        $errors += "queue.tasks must include T001."
-    } elseif ($firstTask.status -ne "in_progress") {
-        $errors += "T001 must be in_progress."
-    }
-
-    foreach ($laterTask in @($tasks | Where-Object { $_.id -ne "T001" })) {
-        if ($laterTask.status -ne "pending") {
-            $errors += "later tasks must be pending: $($laterTask.id)"
-        }
-    }
-
-    return $errors
-}
-
-function Get-StateValidationErrors {
-    param(
-        [object]$State,
-        [object]$Queue
-    )
-
-    $errors = @()
-    $requiredFields = @("goalStatus", "currentTaskId", "currentLoop", "maxLoopsPerTask", "repeatedF
-ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary", "lastReviewDecision", "lastRe
-viewSeverity", "lastCommitHash", "startedAt", "updatedAt", "stopReason")
+diff --git a/.ai-dev/goal.md b/.ai-dev/goal.md
+index ff42c93..4ad00a5 100644
+--- a/.ai-dev/goal.md
++++ b/.ai-dev/goal.md
+@@ -1,63 +1,25 @@
+-# Goal
+-
+-AI Dev Loop Codex CLI 완전 자동화 도입
+-
+-## Background
+-
+-Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완료 처리를 가능한 한 end-to-end로 자동화한다.
+-
+-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 
+생성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
+-
+-초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commit
+ 금지 정책을 적용한다.
+-
+-## Success Criteria
+-
+-- Codex CLI 실행 정책이 문서화된다.
+-- `current-task-prompt.md`를 Codex CLI에 전달하는 `ai-dev-run-codex.ps1`이 추가된다.
+-- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-review-codex.ps1`이 추가된다.
+-- Codex 구현, build/check, diff, Codex 리뷰, save-review 흐름이 연결된다.
+-- pass 리뷰에서만 자동 커밋과 complete-task를 수행하는 full cycle 초안이 추가된다.
+-- git status가 dirty이면 Codex 실행을 중단한다.
+-- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
+-- build 실패 시 자동 커밋하지 않는다.
+-- git reset, git clean, npm install은 자동 실행하지 않는다.
+-- PowerShell 문법 검증이 통과한다.
+-- 작은 앱 기능 task로 MaxTasks 1 end-to-end 검증이 가능하다.
+-
+-## Constraints
+-
+-- Codex CLI는 구현자와 리뷰어로 사용한다.
+-- GPT API 키 없이 진행한다.
+-- Cline은 삭제되어 사용하지 않는다.
+-- Copilot CLI와 gh는 현재 사용하지 않는다.
+-- 자동화는 명시적 Allow 옵션 없이는 위험한 단계를 실행하지 않는다.
+-- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
+-- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
+-- build/check 실패 시 자동 커밋하지 않는다.
+-- git reset, git clean, npm install은 자동 실행하지 않는다.
+-- src 코드는 각 구현 task에서만 수정한다.
+-
+-## Out of Scope
+-
+-- GPT API 직접 호출
+-- Cline 사용
+-- Copilot CLI 사용
+-- gh 또는 GitHub PR 자동 연동
+-- git push 자동화
+-- git reset/git clean 같은 destructive git 명령
+-- npm install 자동 실행
+-- package 대량 교체
+-- DB 삭제, 초기화, 복원, 마이그레이션 자동화
+-
+-## Manual Verification
+-
+-- Codex CLI 실행 정책이 문서화되어 있다.
+-- Codex 구현 실행 스크립트가 DryRun과 dirty worktree 중단을 지원한다.
+-- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
+-- full auto-cycle DryRun이 전체 단계를 보여준다.
+-- full auto-cycle은 MaxTasks 1 제한과 Allow 옵션을 가진다.
+-- pass 리뷰가 아니면 자동 커밋하지 않는다.
+-- build 실패 시 자동 커밋하지 않는다.
+-- package 파일 변경 감지 시 자동 커밋하지 않는다.
+-- PowerShell 문법 검증이 통과한다.
+-- 작은 task로 MaxTasks 1 end-to-end 검증이 가능하다.
++﻿# 목표
++업무 검색창 placeholder 문구를 더 명확한 한국어 문구로 개선한다.
++
++## 배경
++현재 업무 검색창의 안내 문구가 사용자가 무엇을 검색할 수 있는지 충분히 분명하지 않을 수 있다.
++
++## 성공 기준
++- 업무 검색창 placeholder가 자연스럽고 명확한 한국어 문구로 변경된다.
++- 변경 범위는 placeholder 문구 수정에 한정된다.
++- 기존 검색 동작과 상태 관리 방식은 유지된다.
++
++## 제약사항
++- 기존 코드 스타일과 컴포넌트 구조를 따른다.
++- 사용자-facing UI 문자열은 한국어를 사용한다.
++- 불필요한 리팩터링이나 구조 변경은 하지 않는다.
++- 한 번에 하나의 작은 변경만 수행한다.
++
++## 범위 제외
++- 검색 기능의 동작 변경은 제외한다.
++- 새 화면, 새 상태, 새 저장 구조 추가는 제외한다.
++- 디자인 전반 수정은 제외한다.
++
++## 수동 검증
++- 업무 화면의 검색창에 더 명확한 placeholder 문구가 표시되는지 확인한다.
++- 검색어 입력과 기존 필터링 동작이 그대로 유지되는지 확인한다.
+\ No newline at end of file
+diff --git a/.ai-dev/queue.json b/.ai-dev/queue.json
+index b95eb5f..9a38137 100644
+--- a/.ai-dev/queue.json
++++ b/.ai-dev/queue.json
+@@ -1,182 +1,28 @@
+ ﻿{
+-    "goalTitle":  "AI Dev Loop Codex CLI 완전 자동화 도입",
++    "goalTitle":  "업무 검색창 placeholder 문구 개선",
+     "goalSource":  ".ai-dev/goal.md",
+-    "createdAt":  "2026-06-07T19:59:48.1282711+09:00",
+-    "updatedAt":  "2026-06-09T14:05:15.9174264+00:00",
+-    "currentTaskId":  null,
++    "createdAt":  "2026-06-09T00:00:00+09:00",
++    "updatedAt":  "2026-06-09T00:00:00+09:00",
++    "currentTaskId":  "T001",
+     "tasks":  [
+                   {
+                       "id":  "T001",
+-                      "title":  "Codex CLI 완전 자동화 정책 문서화",
+-                      "description":  "Codex CLI를 구현자와 리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전 중단 조건을 문서
+화한다.",
+-                      "type":  "documentation",
+-                      "status":  "done",
+-                      "priority":  "P0",
+-                      "dependsOn":  [
+-
+-                                    ],
+-                      "filesLikelyToChange":  [
+-                                                  "docs/ai-dev-loop-policy.md",
+-                                                  ".ai-dev/README.md",
+-                                                  ".ai-dev/plan.md"
+-                                              ],
+-                      "verification":  [
+-                                           "Codex CLI 사용 방식과 안전 기준이 문서화되었는지 확인",
+-                                           "Cline/Copilot/GPT API 없이 진행하는 구조가 명확한지 확인"
+-                                       ],
+-                      "commitMessage":  "Document Codex CLI automation policy",
+-                      "completedAt":  "2026-06-07T12:46:13.9691878+00:00",
+-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전
+ 중단 조건 문서화"
+-                  },
+-                  {
+-                      "id":  "T002",
+-                      "title":  "Codex 구현 실행 스크립트 추가",
+-                      "description":  "current-task-prompt.md를 Codex CLI에 전달해 현재 task 구현을 수행하는 ai-
+dev-run-codex.ps1을 추가한다.",
+-                      "type":  "implementation",
+-                      "status":  "done",
+-                      "priority":  "P0",
+-                      "dependsOn":  [
+-                                        "T001"
+-                                    ],
+-                      "filesLikelyToChange":  [
+-                                                  "scripts/ai-dev-run-codex.ps1",
+-                                                  ".ai-dev/README.md"
+-                                              ],
+-                      "verification":  [
+-                                           "DryRun에서 실행 예정 명령만 출력되는지 확인",
+-                                           "current-task-prompt.md가 없으면 명확히 안내하는지 확인",
+-                                           "git status dirty 상태에서 기본 중단하는지 확인",
+-                                           "PowerShell 문법 검증"
+-                                       ],
+-                      "commitMessage":  "Add Codex task runner",
+-                      "completedAt":  "2026-06-07T12:48:36.8786703+00:00",
+-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전
+ 중단 조건 문서화"
+-                  },
+-                  {
+-                      "id":  "T003",
+-                      "title":  "Codex 리뷰 실행 스크립트 추가",
+-                      "description":  "review-prompt.md를 Codex CLI에 전달해 JSON 리뷰를 생성하고 review-respo
+nse.json 및 save-review 흐름에 연결하는 ai-dev-run-review-codex.ps1을 추가한다.",
+-                      "type":  "implementation",
+-                      "status":  "done",
+-                      "priority":  "P0",
+-                      "dependsOn":  [
+-                                        "T002"
+-                                    ],
+-                      "filesLikelyToChange":  [
+-                                                  "scripts/ai-dev-run-review-codex.ps1",
+-                                                  ".ai-dev/README.md"
+-                                              ],
+-                      "verification":  [
+-                                           "DryRun에서 실행 예정 명령만 출력되는지 확인",
+-                                           "Codex 출력에서 JSON 객체를 추출해 저장하는지 확인",
+-                                           "save-review와 연결되는지 확인",
+-                                           "PowerShell 문법 검증"
+-                                       ],
+-                      "commitMessage":  "Add Codex review runner",
+-                      "completedAt":  "2026-06-07T13:23:42.7240601+00:00",
+-                      "resultSummary":  "T003 완료: review-prompt.md를 codex exec에 전달해 JSON 리뷰를 생성하는 
+ai-dev-run-review-codex.ps1 추가, 문법 검증과 DryRun/Json 확인"
+-                  },
+-                  {
+-                      "id":  "T004",
+-                      "title":  "full auto-cycle 초안 추가",
+-                      "description":  "make-prompt, run-codex, check, save-diff, make-review-promp
+t, run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안을 추가한다.",
++                      "title":  "업무 검색창 placeholder 문구 수정",
++                      "description":  "업무 검색창 입력 요소를 찾아 placeholder 문구만 더 명확한 한국어 표현으로 변경한다.",
+                       "type":  "implementation",
+-                      "status":  "done",
+-                      "priority":  "P0",
++                      "status":  "in_progress",
++                      "priority":  "P1",
+                       "dependsOn":  [
+-                                        "T003"
+-                                    ],
+-                      "filesLikelyToChange":  [
+-                                                  "scripts/ai-dev-auto-cycle-full.ps1",
+-                                                  ".ai-dev/README.md"
+-                                              ],
+-                      "verification":  [
+-                                           "DryRun에서 전체 단계가 표시되는지 확인",
+-                                           "MaxTasks 제한이 있는지 확인",
+-                                           "AllowCodex, AllowReviewCodex, AllowCommit 같은 명시적 옵션이 있
+는지 확인",
+-                                           "PowerShell 문법 검증"
+-                                       ],
+-                      "commitMessage":  "Add full AI dev auto cycle draft",
+-                      "completedAt":  "2026-06-07T13:57:13.9217915+00:00",
+-                      "resultSummary":  "T004 완료: make-prompt/run-codex/check/save-diff/make-revie
+w-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가, DryRun/Json/AllowCodex 중단 확인"
+-                  },
+-                  {
+-                      "id":  "T005",
+-                      "title":  "자동 커밋과 task 완료 연결",
+-                      "description":  "리뷰 결과가 pass일 때만 선택 파일 커밋과 complete-task를 수행하도록 full auto-cy
+cle에 연결한다.",
+-                      "type":  "implementation",
+-                      "status":  "done",
+-                      "priority":  "P0",
+-                      "dependsOn":  [
+-                                        "T004"
+-                                    ],
+-                      "filesLikelyToChange":  [
+-                                                  "scripts/ai-dev-auto-cycle-full.ps1",
+-                                                  ".ai-dev/README.md"
+-                                              ],
+-                      "verification":  [
+-                                           "pass가 아니면 commit하지 않는지 확인",
+-                                           "build 실패 시 commit하지 않는지 확인",
+-                                           "package 파일 변경 감지 시 중단하는지 확인",
+-                                           "complete-task가 pass 이후에만 실행되는지 확인",
+-                                           "PowerShell 문법 검증"
+-                                       ],
+-                      "commitMessage":  "Connect pass review to auto commit and task completion",
+-                      "completedAt":  "2026-06-08T14:11:23.8613636+00:00",
+-                      "resultSummary":  "T004 완료: make-prompt/run-codex/check/save-diff/make-revie
+w-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가, DryRun/Json/AllowCodex 중단 확인"
+-                  },
+-                  {
+-                      "id":  "T006",
+-                      "title":  "작은 앱 task로 end-to-end 검증",
+-                      "description":  "작은 문서 또는 UI 변경 task를 MaxTasks 1로 실행해 Codex 구현부터 리뷰, 커밋, 완료 
+처리까지 검증한다.",
+-                      "type":  "verification",
+-                      "status":  "done",
+-                      "priority":  "P0",
+-                      "dependsOn":  [
+-                                        "T005"
+-                                    ],
+-                      "filesLikelyToChange":  [
+-                                                  ".ai-dev/plan.md",
+-                                                  ".ai-dev/loop-log.md",
+-                                                  ".ai-dev/test-result.md"
+-                                              ],
+-                      "verification":  [
+-                                           "full auto-cycle DryRun 확인",
+-                                           "MaxTasks 1 실제 실행 확인",
+-                                           "Codex 구현 로그 확인",
+-                                           "Codex 리뷰 JSON 저장 확인",
+-                                           "pass 시 커밋/complete-task 확인",
+-                                           "git status clean 확인"
+-                                       ],
+-                      "commitMessage":  null,
+-                      "completedAt":  "2026-06-08T15:17:52.2846812+00:00",
+-                      "resultSummary":  "T006 완료: full auto-cycle DryRun/Json 구조 검증 및 Codex runner
+ 인자 길이 버그 수정 확인, 실제 end-to-end 검증은 T007 auto-goal 이후 작은 앱 목표로 수행"
+-                  },
+-                  {
+-                      "id":  "T007",
+-                      "title":  "목표 입력 기반 자동 goal 실행 스크립트 추가",
+-                      "description":  "사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/q
+ueue/state를 생성하고, current-task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다.
+",
+-                      "type":  "implementation",
+-                      "status":  "done",
+-                      "priority":  "P0",
+-                      "dependsOn":  [
+-                                        "T005"
++
+                                     ],
+                       "filesLikelyToChange":  [
+-                                                  "scripts/ai-dev-auto-goal.ps1",
+-                                                  ".ai-dev/README.md"
++                                                  "src/App.tsx"
+                                               ],
+                       "verification":  [
+-                                           "DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인",
+-                                           "Json 출력이 가능한지 확인",
+-                                           "goal/queue/state JSON 파싱 검증을 수행하는지 확인",
+-                                           "auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인
+",
+-                                           "GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인",
+-                                           "PowerShell 문법 검증"
++                                           "업무 검색창 placeholder 문구가 변경되었는지 확인한다.",
++                                           "검색 입력 동작과 기존 필터링 동작이 유지되는지 확인한다."
+                                        ],
+-                      "commitMessage":  "Add automatic goal runner",
+-                      "completedAt":  "2026-06-09T14:05:15.9174264+00:00",
+-                      "resultSummary":  "T007 완료: 목표 제목/설명 입력 기반 auto-goal 실행 스크립트 추가, DryRun/Json
+ 출력, 입력 검증, dirty 처리, full auto-cycle 연결 및 Codex 리뷰 pass 확인"
++                      "commitMessage":  "Improve task search placeholder"
+                   }
+               ]
+ }
+\ No newline at end of file
+diff --git a/.ai-dev/state.json b/.ai-dev/state.json
+index b066f2f..db8787c 100644
+--- a/.ai-dev/state.json
++++ b/.ai-dev/state.json
+@@ -1,16 +1,16 @@
+ ﻿{
+-    "goalStatus":  "completed",
+-    "currentTaskId":  null,
++    "goalStatus":  "in_progress",
++    "currentTaskId":  "T001",
+     "currentLoop":  0,
+     "maxLoopsPerTask":  2,
+     "repeatedFailureCount":  0,
+-    "lastCommand":  "complete-task",
++    "lastCommand":  "npm run build",
+     "lastCommandStatus":  "passed",
+     "lastErrorSummary":  "",
+-    "lastReviewDecision":  "pass",
+-    "lastReviewSeverity":  "low",
++    "lastReviewDecision":  "not_started",
++    "lastReviewSeverity":  null,
+     "lastCommitHash":  null,
+-    "startedAt":  "2026-06-07T19:59:48.1282711+09:00",
+-    "updatedAt":  "2026-06-09T14:05:15.9174264+00:00",
+-    "stopReason":  "all_tasks_completed"
++    "startedAt":  "2026-06-09T00:00:00+09:00",
++    "updatedAt":  "2026-06-09T14:43:52.6444119+00:00",
++    "stopReason":  null
+ }
+\ No newline at end of file
+diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
+index f2f7fd8..7167cf7 100644
+--- a/src/views/TasksView.tsx
++++ b/src/views/TasksView.tsx
+@@ -112,7 +112,7 @@ export function TasksView({
+           type="text"
+           value={taskSearchQuery}
+           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+-          placeholder="업무 제목 검색"
++          placeholder="업무 제목이나 메모로 검색"
+         />
+       </label>
+```
 
-    foreach ($field in @(Get-MissingFields $State $requiredFields)) {
-        $errors += "state missing field: $field"
-    }
+## Staged Diff Stat
 
-    if ($errors.Count -gt 0) {
-        return $errors
-    }
+```text
+변경 없음
+```
 
-    if (@("idle", "in_progress", "completed", "blocked") -notcontains $State.goalStatus) {
-        $errors += "invalid state.goalStatus: $($State.goalStatus)"
-    }
+## Staged Diff
 
-    if ($State.currentTaskId -ne $Queue.currentTaskId) {
-        $errors += "state.currentTaskId must match queue.currentTaskId."
-    }
+```text
+변경 없음
+```
 
-    if ($State.currentTaskId -ne "T001") {
-        $errors += "state.currentTaskId must be T001."
-    }
+## Skipped Generated AI Dev Artifact Diffs
 
-    if (@("not_started", "passed", "failed", "blocked") -notcontains $State.lastCommandStatus) {
-        $errors += "invalid state.lastCommandStatus: $($State.lastCommandStatus)"
-    }
+- .ai-dev/current-task-prompt.md
+- .ai-dev/test-result.md
 
-    if (@("not_started", "pass", "revise", "blocked") -notcontains $State.lastReviewDecision) {
-        $errors += "invalid state.lastReviewDecision: $($State.lastReviewDecision)"
-    }
+## Review Criteria
 
-    return $errors
-}
+- 현재 task 요구사항을 충족했는가
+- 현재 task 범위를 벗어나지 않았는가
+- 다음 task를 미리 구현하지 않았는가
+- 기존 기능을 깨뜨릴 가능성이 있는가
+- 데이터 삭제, 초기화, 복원 같은 위험 작업이 포함되었는가
+- package.json 또는 package-lock.json을 불필요하게 수정했는가
+- 검증 결과가 충분한가
+- 문서나 수동검증 체크리스트 갱신이 필요한가
+- 더 단순한 구현이 가능한가
 
-function Get-AutoGoalValidationErrors {
-    param([object]$Plan)
+### Strict Criteria
 
-    $errors = @()
+- 작은 불확실성도 revise로 판정한다.
+- 테스트가 없거나 skipped이면 revise 후보로 본다.
+- task 범위를 벗어난 파일 수정은 high 이상으로 판정한다.
+- package 변경은 기본적으로 blocked 후보로 본다.
 
-    foreach ($field in @(Get-MissingFields $Plan @("goalMarkdown", "queue", "state"))) {
-        $errors += "plan missing field: $field"
-    }
+## Output Format
 
-    if ($errors.Count -gt 0) {
-        return $errors
-    }
+리뷰 결과는 아래 JSON 형식만 출력한다. JSON 앞뒤에 설명, Markdown 코드 펜스, 추가 문장을 출력하지 않는다.
 
-    if (-not (Test-HasValue $Plan.goalMarkdown)) {
-        $errors += "goalMarkdown must not be empty."
+{
+  "decision": "pass | revise | blocked",
+  "severity": "none | low | medium | high | critical",
+  "summary": "짧은 요약",
+  "required_changes": [
+    {
+      "file": "파일 경로 또는 unknown",
+      "reason": "수정이 필요한 이유",
+      "suggestion": "구체적 수정 방향"
     }
-
-    $errors += @(Get-QueueValidationErrors $Plan.queue)
-    $errors += @(Get-StateValidationErrors $Plan.state $Plan.queue)
-
-    return $errors
-}
-
-function New-DryRunAutoGoalPlan {
-    param(
-        [string]$Title,
-        [string]$Description
-    )
-
-    $now = (Get-Date).ToUniversalTime().ToString("o")
-    $safeTitle = $Title.Trim()
-    $safeDescription = $Description.Trim()
-
-    $plan = [PSCustomObject][ordered]@{
-        goalMarkdown = @"
-# 목표
-
-$safeTitle
-
-## 배경
-
-$safeDescription
-
-## 성공 기준
-
-- 입력한 목표에서 현재 작업을 준비한다.
-- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.
-- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.
-
-## 제약사항
-
-- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.
-- 저장소 정책과 현재 작업 범위를 따른다.
-
-## 범위 제외
-
-- 패키지 변경
-- DB 삭제 또는 초기화
-- 광범위한 리팩터링
-
-## 수동 검증
-
-- DryRun 계획과 JSON 출력을 검토한다.
-- 현재 작업 프롬프트 생성 여부를 확인한다.
-- 필요한 경우 빌드와 검증은 별도로 실행한다.
-"@
-        queue = [PSCustomObject][ordered]@{
-            goalTitle = $safeTitle
-            goalSource = $goalRelativePath
-            createdAt = $now
-            updatedAt = $now
-            currentTaskId = "T001"
-            tasks = @(
-                [PSCustomObject][ordered]@{
-                    id = "T001"
-                    title = $safeTitle
-                    description = $safeDescription
-                    type = "implementation"
-                    status = "in_progress"
-                    priority = "P0"
-                    dependsOn = @()
-                    filesLikelyToChange = @()
-                    verification = @(
-                        "변경 범위가 현재 목표와 일치하는지 확인한다.",
-                        "필요한 경우 npm run build를 별도로 실행한다."
-                    )
-                    commitMessage = $null
-                }
-            )
-        }
-        state = [PSCustomObject][ordered]@{
-            goalStatus = "in_progress"
-            currentTaskId = "T001"
-            currentLoop = 0
-            maxLoopsPerTask = 2
-            repeatedFailureCount = 0
-            lastCommand = $null
-            lastCommandStatus = "not_started"
-            lastErrorSummary = $null
-            lastReviewDecision = "not_started"
-            lastReviewSeverity = $null
-            lastCommitHash = $null
-            startedAt = $now
-            updatedAt = $now
-            stopReason = $null
-        }
+  ],
+  "optional_suggestions": [
+    {
+      "file": "파일 경로 또는 unknown",
+      "suggestion": "선택 개선 의견"
     }
-
-    return $plan
+  ],
+  "scope_check": {
+    "within_current_task": true,
+    "scope_issues": []
+  },
+  "test_check": {
+    "build_passed": true,
+    "test_passed": true,
+    "lint_passed": true,
+    "issues": []
+  },
+  "next_step": "complete_task | revise_with_codex | stop_for_user"
 }
 
-function ConvertFrom-CodexAutoGoalOutput {
-    param([string]$RawInput)
-
-    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-        try {
-            $parsed = $candidate | ConvertFrom-Json
+## Decision Rules
 
-            if ($null -eq $parsed -or $parsed -is [System.Array]) {
-                continue
-            }
-
-            if (@(Get-AutoGoalValidationErrors $parsed).Count -gt 0) {
-                continue
-            }
+- pass: 현재 task 요구사항 충족, 치명적 문제 없음, 다음 task로 넘어가도 됨
+- revise: 수정이 필요하지만 자동 수정 가능
+- blocked: 요구사항 충돌, 데이터 위험, 패키지 추가, 대규모 리팩터링 등 사용자 판단 필요
 
-            return [PSCustomObject]@{
-                Parsed = $parsed
-                JsonText = ($parsed | ConvertTo-Json -Depth 30)
+codex
+리뷰 프롬프트의 diff 기준을 확인했습니다. 실제 구현 파일의 주변 코드만 추가로 읽어 placeholder 변경이 검색 상태나 동작에 영향을 주지 않았는지 확인하겠습니다.
+exec
+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
+ 'D:\\ai-apps\\planpilot-local\\src\\views\\TasksView.tsx'" in D:\ai-apps\planpilot-local
+ succeeded in 1133ms:
+import type { FormEvent } from "react";
+import { TaskCard } from "../components/TaskCard";
+import { TaskForm } from "../components/TaskForm";
+import type { Project, Task } from "../types";
+import type { TaskSortOrder } from "../utils/taskFilters";
+
+type TasksViewProps = {
+  projects: Project[];
+  filteredTasks: Task[];
+  sortedTasks: Task[];
+  selectedProjectFilter: string;
+  showCompletedTasks: boolean;
+  taskSortOrder: TaskSortOrder;
+  taskSearchQuery: string;
+  isTaskFormOpen: boolean;
+  newTaskTitle: string;
+  newTaskMemo: string;
+  newTaskDueDate: string;
+  newTaskPriority: Task["priority"];
+  newTaskProjectId: string;
+  editingTaskId: string | null;
+  editTaskTitle: string;
+  editTaskMemo: string;
+  editTaskDueDate: string;
+  editTaskPriority: Task["priority"];
+  editTaskProjectId: string;
+  getProjectName: (projectId: string) => string;
+  onTaskSearchQueryChange: (value: string) => void;
+  onShowCompletedTasksChange: (value: boolean) => void;
+  onTaskSortOrderChange: (value: TaskSortOrder) => void;
+  onSelectedProjectFilterChange: (value: string) => void;
+  onTaskFormOpenChange: (updater: (current: boolean) => boolean) => void;
+  onNewTaskTitleChange: (value: string) => void;
+  onNewTaskMemoChange: (value: string) => void;
+  onNewTaskDueDateChange: (value: string) => void;
+  onNewTaskPriorityChange: (value: Task["priority"]) => void;
+  onNewTaskProjectIdChange: (value: string) => void;
+  onEditTaskTitleChange: (value: string) => void;
+  onEditTaskMemoChange: (value: string) => void;
+  onEditTaskDueDateChange: (value: string) => void;
+  onEditTaskPriorityChange: (value: Task["priority"]) => void;
+  onEditTaskProjectIdChange: (value: string) => void;
+  onAddTask: (event: FormEvent<HTMLFormElement>) => void;
+  onSaveEditTask: (task: Task) => void;
+  onCancelEditTask: () => void;
+  onToggleTaskDone: (task: Task) => void;
+  onDeleteTask: (task: Task) => void;
+  onStartEditTask: (task: Task) => void;
+};
+
+export function TasksView({
+  projects,
+  filteredTasks,
+  sortedTasks,
+  selectedProjectFilter,
+  showCompletedTasks,
+  taskSortOrder,
+  taskSearchQuery,
+  isTaskFormOpen,
+  newTaskTitle,
+  newTaskMemo,
+  newTaskDueDate,
+  newTaskPriority,
+  newTaskProjectId,
+  editingTaskId,
+  editTaskTitle,
+  editTaskMemo,
+  editTaskDueDate,
+  editTaskPriority,
+  editTaskProjectId,
+  getProjectName,
+  onTaskSearchQueryChange,
+  onShowCompletedTasksChange,
+  onTaskSortOrderChange,
+  onSelectedProjectFilterChange,
+  onTaskFormOpenChange,
+  onNewTaskTitleChange,
+  onNewTaskMemoChange,
+  onNewTaskDueDateChange,
+  onNewTaskPriorityChange,
+  onNewTaskProjectIdChange,
+  onEditTaskTitleChange,
+  onEditTaskMemoChange,
+  onEditTaskDueDateChange,
+  onEditTaskPriorityChange,
+  onEditTaskProjectIdChange,
+  onAddTask,
+  onSaveEditTask,
+  onCancelEditTask,
+  onToggleTaskDone,
+  onDeleteTask,
+  onStartEditTask,
+}: TasksViewProps) {
+  const hasSearchQuery = taskSearchQuery.trim().length > 0;
+  const hasProjectFilter = selectedProjectFilter !== "all";
+  const hasVisibilityFilter = !showCompletedTasks;
+  const emptyMessage =
+    hasSearchQuery
+      ? "寃??寃곌낵媛 ?놁뒿?덈떎."
+      : hasProjectFilter || hasVisibilityFilter
+        ? "?꾩옱 ?꾪꽣 議곌굔??留욌뒗 ?낅Т媛 ?놁뒿?덈떎."
+        : "?깅줉???낅Т媛 ?놁뒿?덈떎.";
+
+  return (
+    <section className="screen-card">
+      <h2>?꾩껜 ?낅Т</h2>
+      <p className="summary">珥?{filteredTasks.length}媛?/p>
+
+      <label>
+        ?낅Т 寃??        <input
+          type="text"
+          value={taskSearchQuery}
+          onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+          placeholder="?낅Т ?쒕ぉ?대굹 硫붾え濡?寃??
+        />
+      </label>
+
+      <label>
+        <input
+          type="checkbox"
+          checked={showCompletedTasks}
+          onChange={(event) => onShowCompletedTasksChange(event.target.checked)}
+        />
+        ?꾨즺 ?낅Т ?쒖떆
+      </label>
+
+      <label>
+        ?뺣젹
+        <select
+          value={taskSortOrder}
+          onChange={(event) =>
+            onTaskSortOrderChange(event.target.value as TaskSortOrder)
+          }
+        >
+          <option value="none">湲곕낯 ?쒖꽌</option>
+          <option value="dueDateAsc">留덇컧??鍮좊Ⅸ ??/option>
+        </select>
+      </label>
+
+      <label>
+        ?꾨줈?앺듃 ?꾪꽣
+        <select
+          value={selectedProjectFilter}
+          onChange={(event) => onSelectedProjectFilterChange(event.target.value)}
+        >
+          <option value="all">?꾩껜 ?꾨줈?앺듃</option>
+          {projects.map((project) => (
+            <option key={project.id} value={project.id}>
+              {project.name}
+            </option>
+          ))}
+        </select>
+      </label>
+
+      <button
+        type="button"
+        onClick={() => onTaskFormOpenChange((current) => !current)}
+      >
+        {isTaskFormOpen ? "???낅Т 異붽? ?リ린" : "???낅Т 異붽?"}
+      </button>
+
+      {isTaskFormOpen && (
+        <TaskForm
+          title="???낅Т 異붽?"
+          ariaLabel="?낅Т 異붽?"
+          taskTitle={newTaskTitle}
+          memo={newTaskMemo}
+          dueDate={newTaskDueDate}
+          priority={newTaskPriority}
+          projectId={newTaskProjectId}
+          projects={projects}
+          submitLabel="?낅Т 異붽?"
+          onTitleChange={onNewTaskTitleChange}
+          onMemoChange={onNewTaskMemoChange}
+          onDueDateChange={onNewTaskDueDateChange}
+          onPriorityChange={onNewTaskPriorityChange}
+          onProjectIdChange={onNewTaskProjectIdChange}
+          onSubmit={onAddTask}
+        />
+      )}
+
+      {filteredTasks.length === 0 ? (
+        <div className="empty">
+          <p>{emptyMessage}</p>
+          {hasSearchQuery && <p>?ㅻⅨ 寃?됱뼱瑜??낅젰?대낫?몄슂.</p>}
+        </div>
+      ) : (
+        <ul className="task-list">
+          {sortedTasks.map((task) => {
+            if (editingTaskId === task.id) {
+              return (
+                <li key={task.id} className="task-card">
+                  <TaskForm
+                    title="?낅Т ?섏젙"
+                    ariaLabel="?낅Т ?섏젙"
+                    taskTitle={editTaskTitle}
+                    memo={editTaskMemo}
+                    dueDate={editTaskDueDate}
+                    priority={editTaskPriority}
+                    projectId={editTaskProjectId}
+                    projects={projects}
+                    submitLabel="???
+                    onTitleChange={onEditTaskTitleChange}
+                    onMemoChange={onEditTaskMemoChange}
+                    onDueDateChange={onEditTaskDueDateChange}
+                    onPriorityChange={onEditTaskPriorityChange}
+                    onProjectIdChange={onEditTaskProjectIdChange}
+                    onSubmit={(event) => {
+                      event.preventDefault();
+                      onSaveEditTask(task);
+                    }}
+                    onCancel={onCancelEditTask}
+                    className=""
+                  />
+                </li>
+              );
             }
-        } catch {
-        }
-    }
-
-    throw "No valid auto-goal JSON object was found in Codex output."
-}
-
-function New-CodexAutoGoalPrompt {
-    param(
-        [string]$Title,
-        [string]$Description
-    )
-
-    return @"
-You are preparing local AI Dev Loop state files for the repository at $repoRoot.
-
-Return exactly one JSON object and no markdown fences or commentary.
-
-The user supplied this new goal:
-Title: $Title
-Description: $Description
-
-Create a small, safe goal plan for this repository. The response object must have these fields:
-- goalMarkdown: .ai-dev/goal.md에 작성할 마크다운 문자열입니다. "# 목표", "배경", "성공 기준", "제약사항", "범위 제외", "수동 검증" 섹
-션을 한국어로 포함해야 합니다.
-- queue: an object for .ai-dev/queue.json.
-- state: an object for .ai-dev/state.json.
-
-queue rules:
-- goalTitle must equal the supplied title.
-- goalSource must be ".ai-dev/goal.md".
-- currentTaskId must be "T001".
-- tasks must contain one to three tasks.
-- T001 must be status "in_progress"; later tasks, if any, must be "pending".
-- each task must include id, title, description, type, status, priority, dependsOn, filesLikelyToCh
-ange, verification, commitMessage.
-- type must be one of analysis, implementation, documentation, verification.
-- priority must be P0, P1, or P2.
-- dependsOn, filesLikelyToChange, and verification must be arrays.
-- commitMessage must be null or a short English commit message.
-
-state rules:
-- goalStatus must be "in_progress".
-- currentTaskId must be "T001".
-- currentLoop must be 0.
-- maxLoopsPerTask must be 2.
-- repeatedFailureCount must be 0.
-- lastCommand must be null.
-- lastCommandStatus must be "not_started".
-- lastErrorSummary must be null.
-- lastReviewDecision must be "not_started".
-- lastReviewSeverity must be null.
-- lastCommitHash must be null.
-- startedAt and updatedAt must be ISO 8601 strings.
-- stopReason must be null.
-
-Safety rules:
-- Do not mention server APIs, login, cloud sync, npm install, git reset, git clean, git push, DB de
-letion, or broad rewrites as implementation steps.
-- Prefer one small implementation task when the goal is small.
-- Use Korean for user-facing task titles, descriptions, verification, and goalMarkdown.
-"@
-}
-
-function Invoke-CycleCommand {
-    param(
-        [int]$StepNumber,
-        [string]$Name,
-        [string]$Command,
-        [string]$ScriptPath,
-        [string[]]$Arguments
-    )
-
-    if ($DryRun) {
-        $script:steps += New-StepResult $StepNumber $Name $Command $false $true 0 "DryRun: child sc
-ript was not executed."
-        return
-    }
-
-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath @Arguments 2>&1 | Out-String
-    $exitCode = $LASTEXITCODE
-    $message = $output.Trim()
-
-    if ([string]::IsNullOrWhiteSpace($message)) {
-        $message = "completed"
-    }
-
-    $script:steps += New-StepResult $StepNumber $Name $Command $true $false $exitCode $message
-
-    if ($exitCode -ne 0) {
-        Stop-AutoGoal $script:steps "$Name`_failed" $false 1
-    }
-}
-
-function Get-FullCycleArguments {
-    $arguments = @("-MaxTasks", ([string]$MaxTasks), "-MaxSteps", ([string]$MaxSteps))
-
-    if ($AllowCodex) {
-        $arguments += "-AllowCodex"
-    }
-
-    if ($AllowReviewCodex) {
-        $arguments += "-AllowReviewCodex"
-    }
-
-    if ($AllowCommit) {
-        $arguments += "-AllowCommit"
-    }
-
-    # auto-goal writes goal/queue/state/current prompt/result files after the initial dirty gate.
-    # The downstream full cycle must tolerate those intended artifacts.
-    $arguments += "-AllowDirty"
-
-    if ($null -ne $CommitFiles -and $CommitFiles.Count -gt 0) {
-        $normalizedFiles = @($CommitFiles | ForEach-Object { $_ -split "," } | Where-Object { -not 
-[string]::IsNullOrWhiteSpace($_) })
-
-        if ($normalizedFiles.Count -gt 0) {
-            $arguments += "-CommitFiles"
-            $arguments += ($normalizedFiles -join ",")
-        }
-    }
-
-    return $arguments
-}
-
-Set-Location $repoRoot
-
-$script:steps = @()
-$script:autoGoalPlanPreview = $null
-$resolvedGoalPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $goalRelativePath))
-$resolvedQueuePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $queueRelativePath))
-$resolvedStatePath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $stateRelativePath))
-$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-RepoPath $ResultPath))
-
-if (-not (Test-HasValue $GoalTitle)) {
-    $script:steps += New-StepResult 0 "validate-input" "check GoalTitle" $false $false 1 "GoalTitle
- must not be empty."
-    Stop-AutoGoal $script:steps "invalid_goal_title" $false 1
-}
-
-if (-not (Test-HasValue $GoalDescription)) {
-    $script:steps += New-StepResult 0 "validate-input" "check GoalDescription" $false $false 1 "Goa
-lDescription must not be empty."
-    Stop-AutoGoal $script:steps "invalid_goal_description" $false 1
-}
-
-if ($MaxTasks -lt 1) {
-    $script:steps += New-StepResult 0 "validate-input" "check MaxTasks" $false $false 1 "MaxTasks m
-ust be at least 1."
-    Stop-AutoGoal $script:steps "max_tasks_must_be_at_least_1" $false 1
-}
-
-if ($MaxSteps -lt 1) {
-    $script:steps += New-StepResult 0 "validate-input" "check MaxSteps" $false $false 1 "MaxSteps m
-ust be at least 1."
-    Stop-AutoGoal $script:steps "max_steps_must_be_at_least_1" $false 1
-}
-
-$makePromptPath = Join-Path $PSScriptRoot "ai-dev-make-prompt.ps1"
-$autoCycleFullPath = Join-Path $PSScriptRoot "ai-dev-auto-cycle-full.ps1"
-
-foreach ($requiredScript in @($makePromptPath, $autoCycleFullPath)) {
-    if (-not (Test-Path -LiteralPath $requiredScript -PathType Leaf)) {
-        $script:steps += New-StepResult 0 "prepare" "check required scripts" $false $false 1 "Missi
-ng required script: $requiredScript"
-        Stop-AutoGoal $script:steps "prepare_failed" $false 1
-    }
-}
-
-$shouldRunFullCycle = [bool]($AllowRun -or $AllowCodex -or $AllowReviewCodex -or $AllowCommit)
-$fullCycleArguments = @(Get-FullCycleArguments)
-$fullCycleCommandText = "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-full.ps
-1 $($fullCycleArguments -join ' ')".Trim()
-
-$script:steps += New-StepResult 1 "validate-input" "check GoalTitle/GoalDescription" $false $false 
-0 "Input validation completed: $GoalTitle"
-
-if ($DryRun) {
-    $script:autoGoalPlanPreview = New-DryRunAutoGoalPlan $GoalTitle.Trim() $GoalDescription.Trim()
-    $previewValidationErrors = @(Get-AutoGoalValidationErrors $script:autoGoalPlanPreview)
-
-    if ($previewValidationErrors.Count -gt 0) {
-        $script:steps += New-StepResult 2 "preview-plan" "local dry-run plan preview" $false $false
- 1 "DryRun preview plan validation failed: $($previewValidationErrors -join '; ')"
-        Stop-AutoGoal $script:steps "dry_run_preview_invalid" $false 1
-    }
-
-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 0 "Dr
-yRun: dirty worktree gate was not executed."
-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false $
-true 0 "DryRun: Codex goal planning was not executed. Preview currentTaskId: T001, task: $GoalTitle
-"
-    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" 
-$false $true 0 "DryRun: preview goal/queue/state plan passed local schema validation."
-    $script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, $
-stateRelativePath" $false $true 0 "DryRun: state files were not written."
-    $script:steps += New-StepResult 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scrip
-ts/ai-dev-make-prompt.ps1" $false $true 0 "DryRun: current-task-prompt.md was not generated."
-
-    if ($shouldRunFullCycle) {
-        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "D
-ryRun: explicit run option is present, but full cycle was not executed. -AllowRun only invokes the 
-full-cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-    } else {
-        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "D
-ryRun: full cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the full
--cycle wrapper; implementation and review still require -AllowCodex and -AllowReviewCodex."
-    }
-
-    Stop-AutoGoal $script:steps "dry_run" $false 0
-}
-
-$statusLines = @(Get-GitStatusLines)
-
-if ($statusLines.Count -gt 0 -and -not $AllowDirty) {
-    $dirtyText = ($statusLines -join "`n")
-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $true 1 "Wo
-rktree is dirty. Use -AllowDirty only when this is intentional.`n$dirtyText"
-    Stop-AutoGoal $script:steps "dirty_worktree" $false 1
-}
-
-if ($statusLines.Count -gt 0) {
-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 "A
-llowDirty is set. DirtyCount: $($statusLines.Count)"
-} else {
-    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git status --short" $false $false 0 "W
-orktree is clean."
-}
-
-$codexCommand = Get-Command codex -ErrorAction SilentlyContinue
-
-if ($null -eq $codexCommand) {
-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $false $
-false 1 "Codex CLI was not found."
-    Stop-AutoGoal $script:steps "codex_not_found" $false 1
-}
-
-$plannerPrompt = New-CodexAutoGoalPrompt $GoalTitle.Trim() $GoalDescription.Trim()
-$startedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-$codexExitCode = $LASTEXITCODE
-$endedAt = Get-Date -Format "yyyy-MM-dd HH:mm:ss"
-
-$resultContent = @"
-# Codex Auto Goal Result
-
-## Run
-
-- Started at: $startedAt
-- Ended at: $endedAt
-- Exit code: $codexExitCode
-- Goal title: $($GoalTitle.Trim())
-- Command: codex exec <auto-goal planning prompt>
 
-## Output
-
-${codeFence}text
-$codexOutput
-$codeFence
-"@
-
-[System.IO.File]::WriteAllText($resolvedResultPath, $resultContent, $utf8WithBom)
-
-if ($codexExitCode -ne 0) {
-    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $f
-alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
-    Stop-AutoGoal $script:steps "codex_plan_failed" $false 1
-}
-
-$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal planning prompt>" $true $false
- 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativePath $ResultPath)"
-
-try {
-    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-} catch {
-    $preview = Get-InputPreview $codexOutput
-    $script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" 
-$false $false 1 "Codex plan JSON extraction failed: $($_.Exception.Message) Output preview: $previe
-w"
-    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
-}
-
-$script:steps += New-StepResult 4 "validate-generated-json" "goal/queue/state JSON validation" $fal
-se $false 0 "goalMarkdown, queue, and state JSON validation completed. currentTaskId: $($autoGoalPl
-an.Parsed.queue.currentTaskId)"
-$script:autoGoalPlanPreview = $autoGoalPlan.Parsed
-
-$goalMarkdown = [string]$autoGoalPlan.Parsed.goalMarkdown
-$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 30
-$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 30
-
-[System.IO.File]::WriteAllText($resolvedGoalPath, $goalMarkdown, $utf8WithBom)
-[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $utf8WithBom)
-[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $utf8WithBom)
-
-$script:steps += New-StepResult 5 "write-state-files" "$goalRelativePath, $queueRelativePath, $stat
-eRelativePath" $true $false 0 "New goal, queue, and state files were written."
-
-Invoke-CycleCommand 6 "make-prompt" "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-p
-rompt.ps1" $makePromptPath @()
-
-if (-not $shouldRunFullCycle) {
-    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCommandText $false $true 0 "Full 
-cycle requires -AllowRun or explicit execution options. -AllowRun only invokes the full-cycle wrapp
-er; implementation and review still require -AllowCodex and -AllowReviewCodex."
-    Stop-AutoGoal $script:steps "prepared_without_full_cycle" $true 0
+            return (
+              <TaskCard
+                key={task.id}
+                task={task}
+                projectName={getProjectName(task.projectId)}
+                onToggleDone={onToggleTaskDone}
+                onDelete={onDeleteTask}
+                onStartEdit={onStartEditTask}
+              />
+            );
+          })}
+        </ul>
+      )}
+    </section>
+  );
 }
 
-Invoke-CycleCommand 7 "auto-cycle-full" $fullCycleCommandText $autoCycleFullPath $fullCycleArgument
-s
-
-Stop-AutoGoal $script:steps "completed" $true 0
-
-
- succeeded in 2903ms:
-
-  .ai-dev\README.md:567:- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를 Codex 
-CLI에 전달해 JSON 리뷰를 생성
-하고 저장 흐름에 연결한다.
-  .ai-dev\README.md:568:- `scripts/ai-dev-auto-cycle-full.ps1`: prompt 생성, Codex 구현, check, diff 저장
-, Codex 리뷰, save-rev
-iew, pass 시 commit/complete-task를 연결한다.
-> .ai-dev\README.md:569:- `scripts/ai-dev-auto-goal.ps1`: 사용자가 `GoalTitle`과 `GoalDescription`만 입력하면
- goal/queue/state 생성
-, current-task-prompt 생성, full auto-cycle 실행까지 이어지게 한다.
-  .ai-dev\README.md:570:
-  .ai-dev\README.md:571:초기 안전 기준:
-  .ai-dev\README.md:572:
-  .ai-dev\README.md:573:- full cycle은 처음에 `MaxTasks 1` 중심으로 제한한다.
-  .ai-dev\README.md:575:- Codex 리뷰 실행은 `AllowReviewCodex`가 있을 때만 수행한다.
-  .ai-dev\README.md:576:- git commit은 `AllowCommit`이 있을 때만 수행한다.
-> .ai-dev\README.md:577:- auto-goal에서 실제 full cycle 실행은 `AllowRun` 또는 명시적 실행 옵션이 있을 때만 수행한다.
-  .ai-dev\README.md:578:- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
-  .ai-dev\README.md:579:- build/check 실패, package 파일 변경, 리뷰 decision이 `pass`가 아닌 경우 자동 커밋하지 않는다.
-  .ai-dev\README.md:580:- `git reset`, `git clean`, `npm install`, `git push`는 자동 실행하지 않는다.
-  .ai-dev\README.md:581:
-  .ai-dev\README.md:748:리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `complete_ta
-sk`가 아니면 자동 커밋과 comp
-lete-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다. `package.json` 또는 `packag
-e-lock.json` 변경이 감지되
-면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으로 실제 commit hash를 기록하지 못하면 `commit_not_confirme
-d`로 중단하고 complete-ta
-sk를 실행하지 않는다.
-  .ai-dev\README.md:749:
-> .ai-dev\README.md:750:### 목표 입력 기반 자동 실행
-  .ai-dev\README.md:751:
-  .ai-dev\README.md:752:최종 목표는 사용자가 goal 파일과 queue 파일을 직접 편집하지 않고도 한 줄 명령으로 작은 앱 기능 task를 시작할 수 있게 
-하는 것이다.
-  .ai-dev\README.md:753:
-  .ai-dev\README.md:754:먼저 DryRun으로 생성될 goal/task 계획과 실행 단계를 확인한다.
-  .ai-dev\README.md:755:
-  .ai-dev\README.md:756:```powershell
-> .ai-dev\README.md:757:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -Go
-alTitle "업무 검색 결과 하이
-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun
-  .ai-dev\README.md:758:```
-  .ai-dev\README.md:759:
-  .ai-dev\README.md:760:자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-> .ai-dev\README.md:761:DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포함되므로 파
-일 작성 전에 생성될 goal/tas
-k 계획을 검토할 수 있다.
-  .ai-dev\README.md:762:
-  .ai-dev\README.md:763:```powershell
-> .ai-dev\README.md:764:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -Go
-alTitle "업무 검색 결과 하이
-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-  .ai-dev\README.md:765:```
-  .ai-dev\README.md:766:
-  .ai-dev\README.md:767:실제 goal/queue/state 생성과 current-task-prompt 생성까지만 수행하려면 `-DryRun`을 제거한다. 이 
-모드는 `ai-dev-auto-cyc
-le-full.ps1`을 호출하지 않는다.
-  .ai-dev\README.md:768:
-  .ai-dev\README.md:769:```powershell
-> .ai-dev\README.md:770:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -Go
-alTitle "업무 검색 결과 하이
-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
-  .ai-dev\README.md:771:```
-  .ai-dev\README.md:772:
-> .ai-dev\README.md:773:생성 후 full auto-cycle 래퍼 호출까지 이어가려면 `-AllowRun` 또는 `-AllowCodex`, `-AllowRev
-iewCodex`, `-AllowCo
-mmit` 같은 명시적 실행 옵션을 지정한다. `-AllowRun` 단독은 full-cycle 래퍼 호출만 허용하며 Codex 구현이나 Codex 리뷰 권한을 대신하지 않는다. 
-실제 구현까지 허용하려면 `-Allo
-wCodex`, 리뷰까지 허용하려면 `-AllowReviewCodex`, 자동 커밋과 task 완료까지 허용하려면 기존 full cycle과 동일하게 `-AllowCodex -A
-llowReviewCodex -All
-owCommit`을 함께 지정한다.
-  .ai-dev\README.md:774:
-  .ai-dev\README.md:775:```powershell
-> .ai-dev\README.md:776:powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.ps1 -Go
-alTitle "업무 검색 결과 하이
-라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -AllowCodex -AllowReviewCodex -A
-llowCommit -MaxTasks
- 1
-  .ai-dev\README.md:777:```
-  .ai-dev\README.md:778:
-  .ai-dev\README.md:779:동작:
-  .ai-dev\README.md:780:
-  .ai-dev\README.md:794:- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-  .ai-dev\README.md:795:- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 넘어가지 않
-는다.
-> .ai-dev\README.md:796:- full cycle 호출은 `-AllowRun`, `-AllowCodex`, `-AllowReviewCodex`, `-AllowCo
-mmit` 중 하나가 있을 때만 수행
-한다.
-> .ai-dev\README.md:797:- `-AllowRun` 단독은 구현, 리뷰, 커밋을 수행한다는 의미가 아니다. 하위 full cycle은 여전히 `-AllowCode
-x`, `-AllowReviewCod
-ex`, `-AllowCommit` 권한에 따라 각 위험 단계를 허용하거나 중단한다.
-  .ai-dev\README.md:798:- package 파일 변경, build/check 실패, 리뷰 `pass` 아님, dirty worktree 위험이 있으면 자동 커밋
-하지 않는다.
-  .ai-dev\README.md:799:- `git reset`, `git clean`, `npm install`, `git push`는 실행하지 않는다.
-  .ai-dev\README.md:800:
-  .ai-dev\README.md:801:## 운영 원칙
-
-
-
- succeeded in 7060ms:
-
-.ai-dev\review-prompt.md:23:`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md
-`를 Codex CLI에 전달해 JS
-ON 리뷰를 생성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
-.ai-dev\review-prompt.md:31:- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-review-
-codex.ps1`이 추가된다.
-.ai-dev\review-prompt.md:35:- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:49:- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:70:- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
-.ai-dev\review-prompt.md:92:- Json 출력이 가능한지 확인
-.ai-dev\review-prompt.md:93:- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-.ai-dev\review-prompt.md:98:## Test Result
-.ai-dev\review-prompt.md:100:# AI Dev Test Result
-.ai-dev\review-prompt.md:104:- Overall result: passed
-.ai-dev\review-prompt.md:161: M .ai-dev/codex-result.md
-.ai-dev\review-prompt.md:162: M .ai-dev/codex-review-result.md
-.ai-dev\review-prompt.md:166: M .ai-dev/review-response.json
-.ai-dev\review-prompt.md:168: M .ai-dev/state.json
-.ai-dev\review-prompt.md:169: M .ai-dev/test-result.md
-.ai-dev\review-prompt.md:177: .ai-dev/codex-result.md        | 13475 +++++++-------------
-.ai-dev\review-prompt.md:178: .ai-dev/codex-review-result.md | 25749 ++++++++++++++++++++++++++++++
-++++++++-
-.ai-dev\review-prompt.md:179: .ai-dev/state.json             |     8 +-
-.ai-dev\review-prompt.md:201:+자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:202:+DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 포
-함되므로 파일 작성 전에 생성될 go
-al/task 계획을 검토할 수 있다.
-.ai-dev\review-prompt.md:205:+powershell -ExecutionPolicy Bypass -File .\scripts\ai-dev-auto-goal.p
-s1 -GoalTitle "업무 검색
- 결과 하이라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryRun -Json
-.ai-dev\review-prompt.md:225:-3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
-.ai-dev\review-prompt.md:228:+3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-.ai-dev\review-prompt.md:229:+4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값을
- 검증한다.
-.ai-dev\review-prompt.md:230:+5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성한
-다.
-.ai-dev\review-prompt.md:233:+8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `cur
-rent-task-prompt.md`
-, 결과 파일 때문에 하위 full cycle의 dirty worktree 게이트가 중단되지 않도록 내부적으로 `-AllowDirty`를 전달한다. 초기 dirty worktre
-e 게이트는 auto-goal 시작 
-전에 별도로 유지한다.
-.ai-dev\review-prompt.md:240: - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:241:-- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:243:+- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로 
-넘어가지 않는다.
-.ai-dev\review-prompt.md:251:diff --git a/.ai-dev/codex-result.md b/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:253:--- a/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:254:+++ b/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:268: ## Output
-.ai-dev\review-prompt.md:293:-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.
-md`를 Codex CLI에 전달해 
-JSON 리뷰를 
-.ai-dev\review-prompt.md:295:+`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.
-md`를 Codex CLI에 전달해 
-JSON 리뷰를 생
-.ai-dev\review-prompt.md:340:+- Json 출력이 가능한지 확인
-.ai-dev\review-prompt.md:341:+- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-.ai-dev\review-prompt.md:356:-- package.json 또는 package-lock.json을 수정하지 않는다. 꼭 필요하면 작업을 중단하고 이유만 기록
-한다.
-.ai-dev\review-prompt.md:362:+작업 범위는 `scripts/ai-dev-auto-goal.ps1` 추가와 `.ai-dev/README.md` 갱신입니다. 
-기존 자동화 스크립트의 옵션 이름과 
-JSON 파일 형식
-.ai-dev\review-prompt.md:383:-    [switch]$Json,
-.ai-dev\review-prompt.md:423:-$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:424:-$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:428:+환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-.ai-dev\review-prompt.md:453:-function Read-JsonFile {
-.ai-dev\review-prompt.md:461:-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | C
-onvertFrom-Json
-.ai-dev\review-prompt.md:463:-        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message)
-"
-.ai-dev\review-prompt.md:496:+- `queue.json`
-.ai-dev\review-prompt.md:497:+- `state.json`
-.ai-dev\review-prompt.md:498:+- `test-result.md`
-.ai-dev\review-prompt.md:518:-function New-StepResult {
-.ai-dev\review-prompt.md:528:+- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시다.
-.ai-dev\review-prompt.md:529:+- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시다.
-.ai-dev\review-prompt.md:541:+예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복사 원
-본으로 사용한다.
-.ai-dev\review-prompt.md:543:-function New-CycleResult {
-.ai-dev\review-prompt.md:562:-function Write-CycleResult {
-.ai-dev\review-prompt.md:564:-        [object]$Result
-.ai-dev\review-prompt.md:568:-    if ($Json) {
-.ai-dev\review-prompt.md:569:-        $Result | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:576:-    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:586:-    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:587:-    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:588:-    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:608:-    $result = New-CycleResult $Steps $StoppedReason $Completed $ExitC
-ode
-.ai-dev\review-prompt.md:609:-    Write-CycleResult $result
-.ai-dev\review-prompt.md:627:-        $script:steps += New-StepResult $StepNumber $Name $Command $f
-alse $true 0 "DryRun
-: 하위 스크립트
-.ai-dev\review-prompt.md:633:-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath 
-@Arguments 2>&1 | Ou
-t-String
-.ai-dev\review-prompt.md:635:-    $message = $output.Trim()
-.ai-dev\review-prompt.md:636:+`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-.ai-dev\review-prompt.md:645:-    $script:steps += New-StepResult $StepNumber $Name $Command $true 
-$false $exitCode $me
-ssage
-.ai-dev\review-prompt.md:646:+현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:653:+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.
-ps1 -Json
-.ai-dev\review-prompt.md:668:+`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai-de
-v/current-task-promp
-t.md` 파일을 
-.ai-dev\review-prompt.md:707:-    $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:708:-    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:720:+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-task
-.ps1 -ResultSummary 
-"T001 분석 완
-.ai-dev\review-prompt.md:743:-    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $fal
-se $false 1 $_.Excep
-tion.Mess
-.ai-dev\review-prompt.md:762:-    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTasks
-" $false $true 0 "T0
-04 초안은 ta
-.ai-dev\review-prompt.md:778:-    $script:steps += New-StepResult $stepNumber "run-codex" "powershe
-ll -ExecutionPolicy 
-Bypass -F
-.ai-dev\review-prompt.md:812:-    $script:steps += New-StepResult $stepNumber "run-review-codex" "p
-owershell -Execution
-Policy By
-.ai-dev\review-prompt.md:827:-$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-pl
-aceholder" "T005에서 a
-i-dev-com
-.ai-dev\review-prompt.md:892:-전환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-.ai-dev\review-prompt.md:918:-- `queue.json`
-.ai-dev\review-prompt.md:919:-- `state.json`
-.ai-dev\review-prompt.md:920:-- `test-result.md`
-.ai-dev\review-prompt.md:930:-- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시다.
-.ai-dev\review-prompt.md:931:-- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시다.
-.ai-dev\review-prompt.md:933:-예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복사 원
-본으로 사용한다.
-.ai-dev\review-prompt.md:964:-`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-.ai-dev\review-prompt.md:970:-현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:973:-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-task.
-ps1 -Json
-.ai-dev\review-prompt.md:980:-`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai-de
-v/current-task-promp
-t.md` 파일을
-.ai-dev\review-prompt.md:1008:-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-tas
-k.ps1 -ResultSummary
- "T001 분석 
-.ai-dev\review-prompt.md:1016:-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-tas
-k.ps1 -TaskId T001 -
-ResultSumm
-.ai-dev\review-prompt.md:1018:+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-tas
-k.ps1 -TaskId T001 -
-ResultSumma
-.ai-dev\review-prompt.md:1027:-task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Co
-dex 실행, GPT 리뷰, buil
-d/test 실행,
-.ai-dev\review-prompt.md:1029:+task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. Co
-dex 실행, GPT 리뷰, buil
-d/test 실행, 
-.ai-dev\review-prompt.md:1039:-결과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build/
-test/lint를 실행하지 않는다.
-.ai-dev\review-prompt.md:1041:+과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build/t
-est/lint를 실행하지 않는다.
-.ai-dev\review-prompt.md:1052:-이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다. 실
-제 검증 통과/실패/미수행 판단은 `
-test-resul
-.ai-dev\review-prompt.md:1054:+이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다. 실
-제 검증 통과/실패/미수행 판단은 `
-test-result
-.ai-dev\review-prompt.md:1070:-git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰, b
-uild/test 실행, git co
-mmit은 수행하지
-.ai-dev\review-prompt.md:1072:+git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰, b
-uild/test 실행, git co
-mmit은 수행하지 
-.ai-dev\review-prompt.md:1086: git diff/stat 명령의 stderr 경고는 diff 본문과 분리한다. git 명령이 실패하면 기존처럼 `state
-.json`에 실패 결과를 기록하고 
-종료한다.
-.ai-dev\review-prompt.md:1108: 7. 저장된 decision에 따라 `pass`, `revise`, `blocked` 흐름을 계속 진행한다.
-.ai-dev\review-prompt.md:1127: 자동화에서 결과만 읽고 싶으면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:1129: GPT Chat 또는 외부 리뷰어가 반환한 리뷰 JSON을 `review.md`에 저장하고, decision과 severi
-ty를 `state.json`에 반영
-한다.
-.ai-dev\review-prompt.md:1133:--response.json
-.ai-dev\review-prompt.md:1135:+response.json
-.ai-dev\review-prompt.md:1138: JSON 문자열이나 클립보드 내용을 직접 사용할 수도 있다.
-.ai-dev\review-prompt.md:1141:-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.
-ps1 -ReviewJson '{"d
-ecision":"
-.ai-dev\review-prompt.md:1144:+powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-review.
-ps1 -ReviewJson '{"d
-ecision":"p
-.ai-dev\review-prompt.md:1150:-ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboard
-`를 실행한다. 순수 JSON 객체,
- `json` 코드
-.ai-dev\review-prompt.md:1151:-블록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-.ai-dev\review-prompt.md:1152:+ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboard
-`를 실행한다. 순수 JSON 객체,
- `json` 코드블
-.ai-dev\review-prompt.md:1153:+록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-.ai-dev\review-prompt.md:1157:@@ -895,20 +523,20 @@ ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를
-.ai-dev\review-prompt.md:1158: - Markdown 코드블록을 포함해도 되지만, 가능하면 JSON만 복사한다.
-.ai-dev\review-prompt.md:1159: - 오류 메시지의 입력 preview 앞부분을 보고 다른 설명 문장이나 잘린 JSON이 섞였는지 확인한다.
-.ai-dev\review-prompt.md:1161:-파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수정하
-지 않는다. review-prompt
-나 명령어를 실수로
-.ai-dev\review-prompt.md:1163:+파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수정하
-지 않는다. review-prompt
-나 명령어를 실수로 
-.ai-dev\review-prompt.md:1175: 잘못 실행했다면 ChatGPT가 출력한 JSON 리뷰만 다시 클립보드에 복사한 뒤 `-FromClipboard`를 재실행한
-다.
-.ai-dev\review-prompt.md:1235:-- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `test-
-result.md`, `review.
-md` 등을 포함할
-.ai-dev\review-prompt.md:1237:+- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `test-
-result.md`, `review.
-md` 등을 포함할 
-.ai-dev\review-prompt.md:1240: - `review-response.json`은 민감 정보나 장문 내용이 포함될 수 있으므로 내용을 확인한 뒤 커밋 여부를 
-결정한다.
-.ai-dev\review-prompt.md:1257: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cycle
-.ps1 -Json
-.ai-dev\review-prompt.md:1305:-첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. ChatG
-PT가 JSON 리뷰만 출력하면 그 
-JSON을 다시 클
-.ai-dev\review-prompt.md:1307:+첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. ChatG
-PT가 JSON 리뷰만 출력하면 그 
-JSON을 다시 클립
-.ai-dev\review-prompt.md:1327:-시된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-.ai-dev\review-prompt.md:1329:+된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-.ai-dev\review-prompt.md:1336:-첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 리뷰
-를 받은 뒤 두 번째 명령으로 클립보
-드의 리뷰 JSON
-.ai-dev\review-prompt.md:1338:+첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 리뷰
-를 받은 뒤 두 번째 명령으로 클립보
-드의 리뷰 JSON을
-.ai-dev\review-prompt.md:1369:-`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고, 
-JSON 출력에서는 `complete
-d: true`, 
-.ai-dev\review-prompt.md:1370:-`stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal.
-md`, `.ai-dev/queue.
-json`, `.a
-.ai-dev\review-prompt.md:1371:-i-dev/state.json`을 새 목표 기준으로 초기화한다.
-.ai-dev\review-prompt.md:1372:+`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고, 
-JSON 출력에서는 `complete
-d: true`, `
-.ai-dev\review-prompt.md:1373:+stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal.m
-d`, `.ai-dev/queue.j
-son`, `.ai-
-.ai-dev\review-prompt.md:1374:+dev/state.json`을 새 목표 기준으로 초기화한다.
-.ai-dev\review-prompt.md:1376:-`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하고 
-JSON 리뷰만 다시 복사한다. 완료
-된 goal이나 기
-.ai-dev\review-prompt.md:1378:+`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하고 
-JSON 리뷰만 다시 복사한다. 완료
-된 goal이나 기존
-.ai-dev\review-prompt.md:1382:-st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-.ai-dev\review-prompt.md:1384:+t-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-.ai-dev\review-prompt.md:1388:@@ -1209,14 +837,14 @@ st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검
-.ai-dev\review-prompt.md:1394:-- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를
- Codex CLI에 전달해 JSON
- 리뷰를 생성하고 
-.ai-dev\review-prompt.md:1402:+- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md`를
- Codex CLI에 전달해 JSON
- 리뷰를 생성하고 저
-.ai-dev\review-prompt.md:1424: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.ps
-1 -DryRun -Json
-.ai-dev\review-prompt.md:1443:-`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/re
-view-response.json`에
- 저장한다. 실행 
-.ai-dev\review-prompt.md:1446:+`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/re
-view-response.json`에
- 저장한다. 실행 전
-.ai-dev\review-prompt.md:1456:-기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-resul
-t.md`에 저장한다. Codex 출
-력에서 첫 번째 유
-.ai-dev\review-prompt.md:1457:-효 JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-.ai-dev\review-prompt.md:1458:+기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-resul
-t.md`에 저장한다. Codex 출
-력에서 첫 번째 유효
-.ai-dev\review-prompt.md:1459:+ JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-.ai-dev\review-prompt.md:1473:-리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-resp
-onse.json` 흐름까지 이어가려
-면 `-SaveRe
-.ai-dev\review-prompt.md:1475:+리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-resp
-onse.json` 흐름까지 이어가려
-면 `-SaveRev
-.ai-dev\review-prompt.md:1485: 자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
-.ai-dev\review-prompt.md:1489: - Codex 리뷰 실행 프롬프트에는 파일 수정 금지, git 명령 금지, build/test/lint 실행 금지, JSO
-N 객체만 출력하라는 안전 규칙을 추
-가한다.
-.ai-dev\review-prompt.md:1490:-- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`, `
-optional_suggestions
-`, `next_s
-.ai-dev\review-prompt.md:1492:+- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`, `
-optional_suggestions
-`, `next_st
-.ai-dev\review-prompt.md:1494: - JSON 추출에 실패하면 출력 preview를 보여주고 종료 코드 1로 종료한다.
-.ai-dev\review-prompt.md:1501:-w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:1503:+ prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 커밋과 task 완료 처리를 
-순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:1514:+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevie
-wDecision`이 `pass`여야
- 한다.
-.ai-dev\review-prompt.md:1515:+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 있
-으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:1524: powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-f
-ull.ps1 -DryRun -Jso
-n
-.ai-dev\review-prompt.md:1600:+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `comp
-lete_task`가 아니면 자동 커
-밋과 complete
-.ai-dev\review-prompt.md:1601:+-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않는다
-. `package.json` 또는 
-`package-lo
-.ai-dev\review-prompt.md:1602:+ck.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 등으
-로 실제 commit hash를 기록
-하지 못하면 `com
-.ai-dev\review-prompt.md:1617:+자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:1618:+DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기가 
-포함되므로 파일 작성 전에 생성될 g
-oal/task 계획
-.ai-dev\review-prompt.md:1623:+추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -DryR
-un -Json
-.ai-dev\review-prompt.md:1653:-3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
-.ai-dev\review-prompt.md:1656:+3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-.ai-dev\review-prompt.md:1657:+4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum 값
-을 검증한다.
-.ai-dev\review-prompt.md:1658:+5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 작성
-한다.
-.ai-dev\review-prompt.md:1661:+8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `cu
-rrent-task-prompt.md
-`, 결과 파일 때문
-.ai-dev\review-prompt.md:1670: - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:1671:-- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:1673:+- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행으로
- 넘어가지 않는다.
-.ai-dev\review-prompt.md:1691:-ipts/ai-dev-complete-task.ps1 -Pattern "param|ResultSummary|TaskId|l
-oop-log|queue|state"
- -Context 
-.ai-dev\review-prompt.md:1695:-ipts/ai-dev-save-review.ps1 -Pattern "lastReview|decision|next_step|
-state" -Context 0,3'
- in D:\ai-
-.ai-dev\review-prompt.md:1705:-> scripts\ai-dev-complete-task.ps1:3:    [string]$ResultSummary,
-.ai-dev\review-prompt.md:1709:-> scripts\ai-dev-complete-task.ps1:10:$queueRelativePath = ".ai-dev/
-queue.json"
-.ai-dev\review-prompt.md:1710:-> scripts\ai-dev-complete-task.ps1:11:$stateRelativePath = ".ai-dev/
-state.json"
-.ai-dev\review-prompt.md:1745:-> scripts\ai-dev-complete-task.ps1:89:$queue = Read-JsonFile $queueP
-ath $queueRelativePa
-th
-.ai-dev\review-prompt.md:1746:-> scripts\ai-dev-complete-task.ps1:90:$state = Read-JsonFile $stateP
-ath $stateRelativePa
-th
-.ai-dev\review-prompt.md:1791:-> scripts\ai-dev-complete-task.ps1:130:$summary = if (Test-HasValue 
-$ResultSummary) { $R
-esultSumma
-.ai-dev\review-prompt.md:1797:-> scripts\ai-dev-complete-task.ps1:135:if (Test-HasValue $ResultSumm
-ary) {
-.ai-dev\review-prompt.md:1798:-> scripts\ai-dev-complete-task.ps1:136:    Set-ObjectProperty $compl
-etedTask "resultSumm
-ary" $Resu
-.ai-dev\review-prompt.md:1828:-> scripts\ai-dev-complete-task.ps1:169:Write-JsonFile $queuePath $qu
-eue
-.ai-dev\review-prompt.md:1829:-> scripts\ai-dev-complete-task.ps1:170:Write-JsonFile $statePath $st
-ate
-.ai-dev\review-prompt.md:1884:-> scripts\ai-dev-commit.ps1:170:if (-not $AllowWithoutPassedReview -
-and $state.lastRevie
-wDecision 
-.ai-dev\review-prompt.md:1886:-> scripts\ai-dev-commit.ps1:171:    Stop-WithError "state.lastReview
-Decision이 pass가 아닙니다
-. 리뷰 결과를 확
-.ai-dev\review-prompt.md:1952:-> scripts\ai-dev-commit.ps1:276:        $addOutput = & git add -- @s
-electedFiles 2>&1 | 
-Out-String
-.ai-dev\review-prompt.md:1955:-  scripts\ai-dev-commit.ps1:279:        $addOutput = & git add -A 2>
-&1 | Out-String
-.ai-dev\review-prompt.md:1980:-> scripts\ai-dev-save-review.ps1:11:$stateRelativePath = ".ai-dev/st
-ate.json"
-.ai-dev\review-prompt.md:1986:-> scripts\ai-dev-save-review.ps1:70:function Save-StateReviewResult 
-{
-.ai-dev\review-prompt.md:1989:-> scripts\ai-dev-save-review.ps1:73:        [string]$Decision,
-.ai-dev\review-prompt.md:1993:-> scripts\ai-dev-save-review.ps1:79:    Set-ObjectProperty $State "l
-astReviewDecision" $
-Decision
-.ai-dev\review-prompt.md:2002:-> scripts\ai-dev-save-review.ps1:86:    $stateJson = $State | Conver
-tTo-Json -Depth 20
-.ai-dev\review-prompt.md:2003:-> scripts\ai-dev-save-review.ps1:87:    [System.IO.File]::WriteAllTe
-xt($statePath, $stat
-eJson, $ut
-.ai-dev\review-prompt.md:2010:--review.ps1 -ReviewJson ''{"decision":"pass","severity":"none","summ
-ary":"OK","required_
-changes":[
-.ai-dev\review-prompt.md:2018:-> scripts\ai-dev-save-review.ps1:282:        "decision",
-.ai-dev\review-prompt.md:2028:-> scripts\ai-dev-save-review.ps1:301:        $errors += "허용되지 않은 dec
-ision: $($Review.dec
-ision)"
-.ai-dev\review-prompt.md:2047:-> scripts\ai-dev-save-review.ps1:373:- Decision: blocked
-.ai-dev\review-prompt.md:2050:-  scripts\ai-dev-save-review.ps1:376:- Summary: 리뷰 JSON을 파싱하거나 검증하지 
-못했습니다.
-.ai-dev\review-prompt.md:2051:-> scripts\ai-dev-save-review.ps1:390:    Save-StateReviewResult $Sta
-te "blocked" "critic
-al" "faile
-.ai-dev\review-prompt.md:2061:-  scripts\ai-dev-save-review.ps1:402:        Stop-WithError "리뷰 JSON
- 저장에 실패했습니다: $ErrorS
-ummary"
-.ai-dev\review-prompt.md:2064:-> scripts\ai-dev-save-review.ps1:405:    Write-Host "리뷰 입력 오류로 revie
-w.md와 state.json을 수정
-하지 않았습니다."
-.ai-dev\review-prompt.md:2067:-  scripts\ai-dev-save-review.ps1:407:    Stop-WithError "리뷰 JSON 저장에
- 실패했습니다: $ErrorSumma
-ry"
-.ai-dev\review-prompt.md:2074:-> scripts\ai-dev-save-review.ps1:414:$state = Read-JsonFile $statePa
-th $stateRelativePat
-h
-.ai-dev\review-prompt.md:2087:-  scripts\ai-dev-save-review.ps1:436:$prettyJson = $review | Convert
-To-Json -Depth 20
-.ai-dev\review-prompt.md:2088:-> scripts\ai-dev-save-review.ps1:446:- Decision: $($review.decision)
-.ai-dev\review-prompt.md:2094:-> scripts\ai-dev-save-review.ps1:467:Save-StateReviewResult $state $
-review.decision $rev
-iew.severi
-.ai-dev\review-prompt.md:2097:-> scripts\ai-dev-save-review.ps1:469:$nextAction = if ($review.decis
-ion -eq "pass" -and 
-$review.ne
-.ai-dev\review-prompt.md:2102:-olicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `
-"리뷰 통과`""
-.ai-dev\review-prompt.md:2103:-> scripts\ai-dev-save-review.ps1:471:} elseif ($review.decision -eq 
-"revise" -and $revie
-w.next_ste
-.ai-dev\review-prompt.md:2109:-> scripts\ai-dev-save-review.ps1:473:} elseif ($review.decision -eq 
-"blocked" -or $revie
-w.next_ste
-.ai-dev\review-prompt.md:2116:-> scripts\ai-dev-save-review.ps1:476:    "decision과 next_step 조합을 확인
-한 뒤 다음 행동을 결정하세요."
-.ai-dev\review-prompt.md:2120:-> scripts\ai-dev-save-review.ps1:480:Write-Host "Decision: $($review
-.decision)"
-.ai-dev\review-prompt.md:2128:+codex-result.md
-.ai-dev\review-prompt.md:2129:+codex-review-result.md
-.ai-dev\review-prompt.md:2136:+queue.example.json
-.ai-dev\review-prompt.md:2137:+queue.json
-.ai-dev\review-prompt.md:2140:+review-response.json
-.ai-dev\review-prompt.md:2142:+state.example.json
-.ai-dev\review-prompt.md:2143:+state.json
-.ai-dev\review-prompt.md:2144:+test-result.md
-.ai-dev\review-prompt.md:2150:-ev/queue.json' in D:\ai-apps\planpilot-local
-.ai-dev\review-prompt.md:2160:-ev/state.json' in D:\ai-apps\planpilot-local
-.ai-dev\review-prompt.md:2191:-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어
-로 사용하는 완전 자동화 정책, 허용
-/금지 명령, 안전
-.ai-dev\review-prompt.md:2217:-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어
-로 사용하는 완전 자동화 정책, 허용
-/금지 명령, 안전
-.ai-dev\review-prompt.md:2223:-                      "description":  "review-prompt.md를 Codex CLI에 
-전달해 JSON 리뷰를 생성하고 re
-view-respo
-.ai-dev\review-prompt.md:2224:-nse.json 및 save-review 흐름에 연결하는 ai-dev-run-review-codex.ps1을 추가한다.",
-.ai-dev\review-prompt.md:2237:-                                           "Codex 출력에서 JSON 객체를 추출해 
-저장하는지 확인",
-.ai-dev\review-prompt.md:2243:-                      "resultSummary":  "T003 완료: review-prompt.md를 
-codex exec에 전달해 JSON
- 리뷰를 생성하는 
-.ai-dev\review-prompt.md:2244:-ai-dev-run-review-codex.ps1 추가, 문법 검증과 DryRun/Json 확인"
-.ai-dev\review-prompt.md:2270:-                      "resultSummary":  "T004 완료: make-prompt/run-co
-dex/check/save-diff/
-make-revie
-.ai-dev\review-prompt.md:2271:-w-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가, Dr
-yRun/Json/AllowCodex
- 중단 확인"
-.ai-dev\review-prompt.md:2311:-                                                  ".ai-dev/test-resu
-lt.md"
-.ai-dev\review-prompt.md:2317:-                                           "Codex 리뷰 JSON 저장 확인",
-.ai-dev\review-prompt.md:2341:-                                           "Json 출력이 가능한지 확인",
-.ai-dev\review-prompt.md:2342:-                                           "goal/queue/state JSON 파싱
- 검증을 수행하는지 확인",
-.ai-dev\review-prompt.md:2363:-    "lastReviewDecision":  "not_started",
-.ai-dev\review-prompt.md:2384:+    [switch]$Json,
-.ai-dev\review-prompt.md:2396:-$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:2398: $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:2405:+$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:2406:+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:2432: function Read-JsonFile {
-.ai-dev\review-prompt.md:2434:@@ -2129,8038 +1209,4349 @@ function Read-JsonFile {
-.ai-dev\review-prompt.md:2436:         return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | 
-ConvertFrom-Json
-.ai-dev\review-prompt.md:2438:-        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Exceptio
-n.Message)"
-.ai-dev\review-prompt.md:2439:+        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Message
-)"
-.ai-dev\review-prompt.md:2487:+function New-StepResult {
-.ai-dev\review-prompt.md:2515:-function Save-StateResult {
-.ai-dev\review-prompt.md:2516:+function New-CycleResult {
-.ai-dev\review-prompt.md:2542:-    $stateJson = $State | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:2543:-    [System.IO.File]::WriteAllText($statePath, $stateJson, $utf8With
-Bom)
-.ai-dev\review-prompt.md:2547:+function Write-CycleResult {
-.ai-dev\review-prompt.md:2551:+        [object]$Result
-.ai-dev\review-prompt.md:2554:-    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:2558:-        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outpu
-t"
-.ai-dev\review-prompt.md:2559:+    if ($Json) {
-.ai-dev\review-prompt.md:2560:+        $Result | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:2564:-    return $output.TrimEnd()
-.ai-dev\review-prompt.md:2572:+    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:2587:+    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:2588:+    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:2589:+    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:2593:-$queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:2594:-$state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:2605:+    $result = New-CycleResult $Steps $StoppedReason $Completed $Exit
-Code
-.ai-dev\review-prompt.md:2606:+    Write-CycleResult $result
-.ai-dev\review-prompt.md:2625:+        $script:steps += New-StepResult $StepNumber $Name $Command $
-false $true 0 "DryRu
-n: 하위 스크립트를
-.ai-dev\review-prompt.md:2637:+    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath
- @Arguments 2>&1 | O
-ut-String
-.ai-dev\review-prompt.md:2639:+    $message = $output.Trim()
-.ai-dev\review-prompt.md:2652:+    $script:steps += New-StepResult $StepNumber $Name $Command $true
- $false $exitCode $m
-essage
-.ai-dev\review-prompt.md:2683:-if (-not $AllowWithoutPassedReview -and $state.lastReviewDecision -n
-e "pass") {
-.ai-dev\review-prompt.md:2684:-    Stop-WithError "state.lastReviewDecision이 pass가 아닙니다. 리뷰 결과를 확인하
-거나 -AllowWithoutPass
-edReview를 
-.ai-dev\review-prompt.md:2699:+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:2705:+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outpu
-t"
-.ai-dev\review-prompt.md:2711:+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:2717:+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "pa
-ckage.json", "packag
-e-lock.json
-.ai-dev\review-prompt.md:2718:+") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:2726:+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:2733:+        $reviewResponse = Read-JsonFile $reviewResponsePath $reviewR
-esponseRelativePath
-.ai-dev\review-prompt.md:2754:+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:2801:+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:2851:-        $addOutput = & git add -- @selectedFiles 2>&1 | Out-String
-.ai-dev\review-prompt.md:2854:-        $addOutput = & git add -A 2>&1 | Out-String
-.ai-dev\review-prompt.md:2863:-        throw "$addDisplayName 실행에 실패했습니다. exit code: $LASTEXITCODE`
-n$addOutput"
-.ai-dev\review-prompt.md:2865:+    $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:2866:+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:2886:-    $commitOutput = & git commit -m $commitMessage 2>&1 | Out-String
-.ai-dev\review-prompt.md:2890:-        throw "git commit 실행에 실패했습니다. exit code: $LASTEXITCODE`n$com
-mitOutput"
-.ai-dev\review-prompt.md:2913:-    Save-StateResult $state "failed" $errorSummary $null
-.ai-dev\review-prompt.md:2915:+    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $fa
-lse $false 1 $_.Exce
-ption.Messa
-.ai-dev\review-prompt.md:2920:-Save-StateResult $state "passed" "" $commitHash
-.ai-dev\review-prompt.md:2931:+    "commit-result-gate",
-.ai-dev\review-prompt.md:2951:+        $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:2952:+        $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:2955:+        $script:steps += New-StepResult $stepNumber "load-task" "sta
-te/queue 확인" $false 
-$false 1 $_
-.ai-dev\review-prompt.md:2995:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:2996:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:2997:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:3005:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:3006:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:3024:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:3028:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:3031:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:3035:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:3036:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:3041:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:3045:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:3054:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:3086:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:3103:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:3105:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:3121:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:3130:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:3135:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:3138:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:3139:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:3140:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:3145:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:3151:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:3157:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:3158:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:3163:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:3164:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:3168:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:3169:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:3183:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:3198:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:3218:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:3219:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:3220:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:3228:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:3229:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:3247:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:3251:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:3254:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:3258:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:3259:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:3264:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:3268:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:3277:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:3309:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:3326:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:3328:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:3344:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:3353:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:3358:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:3361:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:3362:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:3363:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:3368:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:3374:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:3380:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:3381:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:3386:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:3387:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:3391:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:3392:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:3406:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:3421:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:3450:+    $script:steps += New-StepResult $stepNumber "task-start" "MaxTas
-ks=$MaxTasks" $false
- $false 0 "
-.ai-dev\review-prompt.md:3495:-> scripts\ai-dev-auto-cycle-full.ps1:381:    $script:steps += New-St
-epResult $stepNumber
- "review-g
-.ai-dev\review-prompt.md:3503:-> scripts\ai-dev-auto-cycle-full.ps1:386:    $script:steps += New-St
-epResult $stepNumber
- "review-g
-.ai-dev\review-prompt.md:3511:-  scripts\ai-dev-auto-cycle-full.ps1:390:if ($reviewGate.decision -n
-e "pass") {
-.ai-dev\review-prompt.md:3512:-> scripts\ai-dev-auto-cycle-full.ps1:391:    $script:steps += New-St
-epResult $stepNumber
- "review-g
-.ai-dev\review-prompt.md:3514:-wDecision 확인" $false $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complet
-e-task를 실행하지 않습니다: $
-($reviewGa
-.ai-dev\review-prompt.md:3515:-te.decision)"
-.ai-dev\review-prompt.md:3522:-> scripts\ai-dev-auto-cycle-full.ps1:396:    $script:steps += New-St
-epResult $stepNumber
- "review-g
-.ai-dev\review-prompt.md:3531:-> scripts\ai-dev-auto-cycle-full.ps1:400:$script:steps += New-StepRe
-sult $stepNumber "re
-view-gate"
-.ai-dev\review-prompt.md:3538:-> scripts\ai-dev-auto-cycle-full.ps1:405:        $script:steps += Ne
-w-StepResult $stepNu
-mber "pack
-.ai-dev\review-prompt.md:3540:- status --porcelain -- package.json package-lock.json" $false $true 
-1 "package.json 또는 p
-ackage-loc
-.ai-dev\review-prompt.md:3541:-k.json 변경이 감지되어 자동 커밋
-.ai-dev\review-prompt.md:3547:-> scripts\ai-dev-auto-cycle-full.ps1:409:    $script:steps += New-St
-epResult $stepNumber
- "package-
-.ai-dev\review-prompt.md:3549:-tus --porcelain -- package.json package-lock.json" $false $false 1 $
-_.Exception.Message
-.ai-dev\review-prompt.md:3554:-> scripts\ai-dev-auto-cycle-full.ps1:413:$script:steps += New-StepRe
-sult $stepNumber "pa
-ckage-chan
-.ai-dev\review-prompt.md:3556:---porcelain -- package.json package-lock.json" $false $false 0 "pack
-age 파일 변경 없음. 커밋 허용 
-여부를 확인합니다.
-.ai-dev\review-prompt.md:3575:-> scripts\ai-dev-auto-cycle-full.ps1:426:    $script:steps += New-St
-epResult $stepNumber
- "commit" 
-.ai-dev\review-prompt.md:3586:-ypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $scr
-iptPaths.c
-.ai-dev\review-prompt.md:3609:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:3610:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:3611:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:3619:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:3620:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:3638:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:3642:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:3645:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:3649:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:3650:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:3655:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:3659:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:3668:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:3700:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:3717:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:3719:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:3735:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:3744:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:3749:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:3752:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:3753:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:3754:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:3759:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:3765:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:3771:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:3772:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:3777:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:3778:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:3782:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:3783:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:3797:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:3812:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:3841:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:3842:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:3843:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:3851:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:3852:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:3870:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:3874:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:3877:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:3881:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:3882:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:3887:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:3891:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:3900:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:3932:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:3951:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:3953:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:3991:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:3994:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:3995:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:3997:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:4000:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:4001:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:4006:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:4012:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:4018:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:4023:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:4024:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:4025:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:4030:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:4036:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:4042:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:4043:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:4048:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:4049:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:4053:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:4054:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:4068:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:4083:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:4103:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:4104:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:4105:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:4113:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:4114:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:4132:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:4136:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:4139:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:4143:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:4144:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:4149:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:4153:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:4162:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:4194:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:4213:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:4215:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:4253:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:4256:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:4257:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:4259:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:4262:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:4263:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:4268:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:4274:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:4280:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:4285:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:4286:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:4287:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:4292:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:4298:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:4304:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:4305:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:4310:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:4311:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:4315:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:4316:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:4330:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:4345:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:4367:--ResultSummary "T001 
-.ai-dev\review-prompt.md:4375:--TaskId T001 -ResultS
-.ai-dev\review-prompt.md:4386:-  .ai-dev\README.md:130:task 완료 처리 스크립트는 `queue.json`, `state.json`,
- `loop-log.md`만 갱신한다
-. Codex 실행
-.ai-dev\review-prompt.md:4392:-> .ai-dev\README.md:134:`package.json`에 정의된 build, test, lint script
-를 확인하고 실행 결과를 `test-
-result.md`
-.ai-dev\review-prompt.md:4393:-와 `state.json`에 기록한다.
-.ai-dev\review-prompt.md:4398:-  .ai-dev\README.md:418:리뷰 저장 후 decision별 다음 단계:
-.ai-dev\review-prompt.md:4412:- CLI에 전달해 JSON 리뷰를 생성
-.ai-dev\review-prompt.md:4428:-> .ai-dev\README.md:579:- build/check 실패, package 파일 변경, 리뷰 decision
-이 `pass`가 아닌 경우 자동 커
-밋하지 않는다.
-.ai-dev\review-prompt.md:4441:-  .ai-dev\README.md:629:`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 J
-SON 리뷰를 생성하고 `.ai-de
-v/review-r
-.ai-dev\review-prompt.md:4442:-esponse.json`에 저장한다. 
-.ai-dev\review-prompt.md:4451:-view prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:4491:-  .ai-dev\README.md:743:- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로
- 넘어가지 않는다.
-.ai-dev\review-prompt.md:4518:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:4519:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:4520:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:4528:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:4529:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:4547:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:4551:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:4554:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:4558:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:4559:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:4564:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:4568:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:4577:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:4609:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:4628:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:4630:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:4668:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:4671:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:4672:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:4674:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:4677:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:4678:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:4683:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:4689:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:4695:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:4700:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:4701:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:4702:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:4707:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:4713:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:4719:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:4720:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:4725:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:4726:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:4730:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:4731:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:4745:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:4760:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:4777:-w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:4778:+        $script:steps += New-StepResult $stepNumber "run-codex" "pow
-ershell -ExecutionPo
-licy Bypass
-.ai-dev\review-prompt.md:4812:-자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다.
-.ai-dev\review-prompt.md:4818:-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-f
-ull.ps1 -DryRun -Jso
-n
-.ai-dev\review-prompt.md:4843:+        $script:steps += New-StepResult $stepNumber "run-review-code
-x" "powershell -Exec
-utionPolicy
-.ai-dev\review-prompt.md:4859:+        $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fa
-.ai-dev\review-prompt.md:4862:+        $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain -
-.ai-dev\review-prompt.md:4863:+- package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:4865:+        $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -F
-.ai-dev\review-prompt.md:4868:+        $script:steps += New-StepResult $stepNumber "commit-result-g
-ate" "state.lastComm
-and/lastCom
-.ai-dev\review-prompt.md:4871:+        $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy By
-.ai-dev\review-prompt.md:4872:+pass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"자동 완료: 
-Codex 구현, build/chec
-k, Codex 리뷰
-.ai-dev\review-prompt.md:4884:+        $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false
-.ai-dev\review-prompt.md:4892:+        $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fal
-.ai-dev\review-prompt.md:4901:+    if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:4902:+        $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fa
-.ai-dev\review-prompt.md:4903:+lse $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:4910:+        $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next
-.ai-dev\review-prompt.md:4917:+    $script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false 
-.ai-dev\review-prompt.md:4924:+            $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcela
-.ai-dev\review-prompt.md:4925:+in -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동
-.ai-dev\review-prompt.md:4930:+        $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain -
-.ai-dev\review-prompt.md:4931:+- package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:4935:+    $script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pa
-.ai-dev\review-prompt.md:4936:+ckage.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:4955:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:4956:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:4957:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:4965:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:4966:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:4984:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:4988:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:4991:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:4995:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:4996:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:5001:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:5005:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:5014:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:5046:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:5065:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:5067:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:5105:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:5108:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:5109:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:5111:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:5114:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:5115:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:5120:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:5126:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:5132:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:5137:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:5138:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:5139:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:5144:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:5150:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:5156:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:5157:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:5162:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:5163:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:5167:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:5168:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:5182:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:5197:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:5221:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:5223:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task
- 완료 처리를 순서대로 연결하는 초안
-이다.
-.ai-dev\review-prompt.md:5234:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevi
-ewDecision`이 `pass`여
-야 한다.
-.ai-dev\review-prompt.md:5235:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 
-있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:5242:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-
-full.ps1 -DryRun -Js
-on
-.ai-dev\review-prompt.md:5277:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `com
-plete_task`가 아니면 자동 
-커밋과 comple
-.ai-dev\review-prompt.md:5278:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package
-.ai-dev\review-prompt.md:5279:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:5299:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:5300:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:5301:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:5309:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:5310:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:5328:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:5332:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:5335:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:5339:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:5340:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:5345:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:5349:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:5358:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:5390:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:5409:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:5411:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:5449:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:5452:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:5453:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:5455:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:5458:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:5459:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:5464:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:5470:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:5476:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:5481:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:5482:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:5483:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:5488:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:5494:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:5500:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:5501:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:5506:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:5507:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:5511:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:5512:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:5526:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:5541:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:5554:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:5556:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task
- 완료 처리를 순서대로 연결하는 초안
-이다.
-.ai-dev\review-prompt.md:5567:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevi
-ewDecision`이 `pass`여
-야 한다.
-.ai-dev\review-prompt.md:5568:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 
-있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:5575:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-
-full.ps1 -DryRun -Js
-on
-.ai-dev\review-prompt.md:5610:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `com
-plete_task`가 아니면 자동 
-커밋과 comple
-.ai-dev\review-prompt.md:5611:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package
-.ai-dev\review-prompt.md:5612:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:5632:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:5633:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:5634:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:5642:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:5643:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:5661:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:5665:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:5668:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:5672:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:5673:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:5678:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:5682:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:5691:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:5723:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:5742:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:5744:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:5782:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:5785:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:5786:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:5788:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:5791:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:5792:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:5797:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:5803:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:5809:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:5814:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:5815:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:5816:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:5821:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:5827:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:5833:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:5834:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:5839:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:5840:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:5844:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:5845:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:5859:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:5874:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:5892:-licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTas
-ks 1 -MaxSteps 20 -J
-son' in D:
-.ai-dev\review-prompt.md:5896:+        $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -F
-.ai-dev\review-prompt.md:5967:-                      "command":  "state.lastReviewDecision 확인",
-.ai-dev\review-prompt.md:5976:-                      "command":  "git status --porcelain -- package
-.json package-lock.j
-son",
-.ai-dev\review-prompt.md:5996:-e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-.ai-dev\review-prompt.md:6019:+        $script:steps += New-StepResult $stepNumber "commit-result-g
-ate" "state.lastComm
-and/lastCom
-.ai-dev\review-prompt.md:6021:+        Stop-Cycle $script:steps "commit_result_gate_failed" $false 
-1
-.ai-dev\review-prompt.md:6029:+        $script:steps += New-StepResult $stepNumber "commit-result-g
-ate" "state.lastComm
-and/lastCom
-.ai-dev\review-prompt.md:6034:+    $script:steps += New-StepResult $stepNumber "commit-result-gate"
- "state.lastCommand/
-lastCommitH
-.ai-dev\review-prompt.md:6038:+    $resultSummary = "자동 완료: Codex 구현, build/check, Codex 리뷰 pass, 자
-동 커밋 완료"
-.ai-dev\review-prompt.md:6040:+ts/ai-dev-complete-task.ps1 -ResultSummary `"$resultSummary`"" $scri
-ptPaths.completeTask
- @("-Result
-.ai-dev\review-prompt.md:6041:+Summary", $resultSummary)
-.ai-dev\review-prompt.md:6055:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:6057:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task
- 완료 처리를 순서대로 연결하는 초안
-이다.
-.ai-dev\review-prompt.md:6068:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevi
-ewDecision`이 `pass`여
-야 한다.
-.ai-dev\review-prompt.md:6069:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 
-있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:6076:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-
-full.ps1 -DryRun -Js
-on
-.ai-dev\review-prompt.md:6111:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `com
-plete_task`가 아니면 자동 
-커밋과 comple
-.ai-dev\review-prompt.md:6112:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package
-.ai-dev\review-prompt.md:6113:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:6133:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:6134:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:6135:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:6143:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:6144:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:6162:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:6166:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:6169:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:6173:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:6174:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:6179:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:6183:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:6192:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:6224:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:6243:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:6245:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:6283:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:6286:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:6287:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:6289:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:6292:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:6293:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:6298:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:6304:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:6310:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:6315:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:6316:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:6317:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:6322:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:6328:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:6334:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:6335:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:6340:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:6341:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:6345:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:6346:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:6360:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:6375:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:6396:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:6398:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task
- 완료 처리를 순서대로 연결하는 초안
-이다.
-.ai-dev\review-prompt.md:6409:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevi
-ewDecision`이 `pass`여
-야 한다.
-.ai-dev\review-prompt.md:6410:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 
-있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:6417:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-
-full.ps1 -DryRun -Js
-on
-.ai-dev\review-prompt.md:6452:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `com
-plete_task`가 아니면 자동 
-커밋과 comple
-.ai-dev\review-prompt.md:6453:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package
-.ai-dev\review-prompt.md:6454:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:6474:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:6475:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:6476:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:6484:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:6485:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:6503:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:6507:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:6510:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:6514:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:6515:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:6520:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:6524:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:6533:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:6555:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:6575:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:6586:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:6595:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:6597:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:6632:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:6635:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:6636:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:6638:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:6641:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:6644:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:6645:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:6653:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:6659:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:6664:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:6665:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:6666:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:6671:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:6677:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:6683:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:6684:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:6689:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:6690:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:6694:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:6695:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:6709:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:6729:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:6731:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:6734:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:6742:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:6747:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "s
-tate.lastCommand/las
-tCommitHas
-.ai-dev\review-prompt.md:6752:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:6765:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:6767:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task
- 완료 처리를 순서대로 연결하는 초안
-이다.
-.ai-dev\review-prompt.md:6778:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevi
-ewDecision`이 `pass`여
-야 한다.
-.ai-dev\review-prompt.md:6779:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 
-있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:6786:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-
-full.ps1 -DryRun -Js
-on
-.ai-dev\review-prompt.md:6821:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `com
-plete_task`가 아니면 자동 
-커밋과 comple
-.ai-dev\review-prompt.md:6822:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package
-.ai-dev\review-prompt.md:6823:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:6843:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:6844:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:6845:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:6853:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:6854:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:6872:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:6876:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:6879:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:6883:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:6884:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:6889:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:6893:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:6902:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:6924:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:6944:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:6955:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:6964:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:6966:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:7001:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:7004:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:7005:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:7007:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:7010:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:7013:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:7014:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:7022:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:7028:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:7033:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:7034:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:7035:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:7040:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:7046:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:7052:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:7053:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:7058:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:7059:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:7063:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:7064:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:7078:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:7098:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:7100:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:7103:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:7111:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:7116:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "s
-tate.lastCommand/las
-tCommitHas
-.ai-dev\review-prompt.md:7121:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:7133:+    [switch]$Json,
-.ai-dev\review-prompt.md:7140:+    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:7149:+$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:7150:+$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:7190:+function New-StepResult {
-.ai-dev\review-prompt.md:7212:+function New-AutoGoalResult {
-.ai-dev\review-prompt.md:7230:+        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-.ai-dev\review-prompt.md:7234:+function Write-AutoGoalResult {
-.ai-dev\review-prompt.md:7235:+    param([object]$Result)
-.ai-dev\review-prompt.md:7237:+    if ($Json) {
-.ai-dev\review-prompt.md:7238:+        $Result | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:7242:+    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:7251:+    if ($null -ne $Result.plan) {
-.ai-dev\review-prompt.md:7253:+        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)"
-.ai-dev\review-prompt.md:7254:+        Write-Host "  Current task id: $($Result.plan.queue.currentT
-askId)"
-.ai-dev\review-prompt.md:7256:+        foreach ($task in @($Result.plan.queue.tasks)) {
-.ai-dev\review-prompt.md:7266:+    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:7267:+    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:7268:+    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:7279:+    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReason $
-Completed $ExitCode)
-.ai-dev\review-prompt.md:7304:+    $statusOutput = & git status --short 2>&1
-.ai-dev\review-prompt.md:7307:+        $script:steps += New-StepResult 2 "dirty-worktree-gate" "git
- status --short" $fa
-lse $false 
-.ai-dev\review-prompt.md:7308:+1 "git status failed: $($statusOutput -join "`n")"
-.ai-dev\review-prompt.md:7312:+    return @($statusOutput | Where-Object { -not [string]::IsNullOrW
-hiteSpace($_) })
-.ai-dev\review-prompt.md:7315:+function Get-JsonObjectCandidates {
-.ai-dev\review-prompt.md:7505:+ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary"
-, "lastReviewDecisio
-n", "lastRe
-.ai-dev\review-prompt.md:7528:+    if (@("not_started", "pass", "revise", "blocked") -notcontains $
-State.lastReviewDeci
-sion) {
-.ai-dev\review-prompt.md:7529:+        $errors += "invalid state.lastReviewDecision: $($State.lastR
-eviewDecision)"
-.ai-dev\review-prompt.md:7594:+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:7631:+            lastReviewDecision = "not_started"
-.ai-dev\review-prompt.md:7666:+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:7680:+function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:7683:+    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-.ai-dev\review-prompt.md:7685:+            $parsed = $candidate | ConvertFrom-Json
-.ai-dev\review-prompt.md:7697:+                JsonText = ($parsed | ConvertTo-Json -Depth 30)
-.ai-dev\review-prompt.md:7703:+    throw "No valid auto-goal JSON object was found in Codex output.
-"
-.ai-dev\review-prompt.md:7715:+Return exactly one JSON object and no markdown fences or commentary.
-.ai-dev\review-prompt.md:7724:+- queue: an object for .ai-dev/queue.json.
-.ai-dev\review-prompt.md:7725:+- state: an object for .ai-dev/state.json.
-.ai-dev\review-prompt.md:7749:+- lastReviewDecision must be "not_started".
-.ai-dev\review-prompt.md:7773:+        $script:steps += New-StepResult $StepNumber $Name $Command $
-false $true 0 "DryRu
-n: child sc
-.ai-dev\review-prompt.md:7778:+    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPath
- @Arguments 2>&1 | O
-ut-String
-.ai-dev\review-prompt.md:7780:+    $message = $output.Trim()
-.ai-dev\review-prompt.md:7786:+    $script:steps += New-StepResult $StepNumber $Name $Command $true
- $false $exitCode $m
-essage
-.ai-dev\review-prompt.md:7808:+    # auto-goal writes goal/queue/state/current prompt/result files 
-after the initial di
-rty gate.
-.ai-dev\review-prompt.md:7832:+$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-RepoPat
-h $ResultPath))
-.ai-dev\review-prompt.md:7835:+    $script:steps += New-StepResult 0 "validate-input" "check GoalTi
-tle" $false $false 1
- "GoalTitle
-.ai-dev\review-prompt.md:7841:+    $script:steps += New-StepResult 0 "validate-input" "check GoalDe
-scription" $false $f
-alse 1 "Goa
-.ai-dev\review-prompt.md:7847:+    $script:steps += New-StepResult 0 "validate-input" "check MaxTas
-ks" $false $false 1 
-"MaxTasks m
-.ai-dev\review-prompt.md:7853:+    $script:steps += New-StepResult 0 "validate-input" "check MaxSte
-ps" $false $false 1 
-"MaxSteps m
-.ai-dev\review-prompt.md:7863:+        $script:steps += New-StepResult 0 "prepare" "check required 
-scripts" $false $fal
-se 1 "Missi
-.ai-dev\review-prompt.md:7874:+$script:steps += New-StepResult 1 "validate-input" "check GoalTitle/
-GoalDescription" $fa
-lse $false 
-.ai-dev\review-prompt.md:7882:+        $script:steps += New-StepResult 2 "preview-plan" "local dry-
-run plan preview" $f
-alse $false
-.ai-dev\review-prompt.md:7887:+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git sta
-tus --short" $false 
-$true 0 "Dr
-.ai-dev\review-prompt.md:7889:+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-
-goal planning prompt
->" $false $
-.ai-dev\review-prompt.md:7892:+    $script:steps += New-StepResult 4 "validate-generated-json" "goa
-l/queue/state JSON v
-alidation" 
-.ai-dev\review-prompt.md:7894:+    $script:steps += New-StepResult 5 "write-state-files" "$goalRela
-tivePath, $queueRela
-tivePath, $
-.ai-dev\review-prompt.md:7896:+    $script:steps += New-StepResult 6 "make-prompt" "powershell -Exe
-cutionPolicy Bypass 
--File scrip
-.ai-dev\review-prompt.md:7900:+        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCyc
-leCommandText $false
- $true 0 "D
-.ai-dev\review-prompt.md:7904:+        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCyc
-leCommandText $false
- $true 0 "D
-.ai-dev\review-prompt.md:7916:+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git sta
-tus --short" $false 
-$true 1 "Wo
-.ai-dev\review-prompt.md:7922:+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git sta
-tus --short" $false 
-$false 0 "A
-.ai-dev\review-prompt.md:7925:+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git sta
-tus --short" $false 
-$false 0 "W
-.ai-dev\review-prompt.md:7932:+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-
-goal planning prompt
->" $false $
-.ai-dev\review-prompt.md:7939:+$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-.ai-dev\review-prompt.md:7943:+$resultContent = @"
-.ai-dev\review-prompt.md:7944:+# Codex Auto Goal Result
-.ai-dev\review-prompt.md:7954:+## Output
-.ai-dev\review-prompt.md:7957:+$codexOutput
-.ai-dev\review-prompt.md:7961:+[System.IO.File]::WriteAllText($resolvedResultPath, $resultContent, 
-$utf8WithBom)
-.ai-dev\review-prompt.md:7964:+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-
-goal planning prompt
->" $true $f
-.ai-dev\review-prompt.md:7965:+alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelative
-Path $ResultPath)"
-.ai-dev\review-prompt.md:7969:+$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goal
- planning prompt>" $
-true $false
-.ai-dev\review-prompt.md:7970:+ 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativeP
-ath $ResultPath)"
-.ai-dev\review-prompt.md:7973:+    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-.ai-dev\review-prompt.md:7975:+    $preview = Get-InputPreview $codexOutput
-.ai-dev\review-prompt.md:7976:+    $script:steps += New-StepResult 4 "validate-generated-json" "goa
-l/queue/state JSON v
-alidation" 
-.ai-dev\review-prompt.md:7977:+$false $false 1 "Codex plan JSON extraction failed: $($_.Exception.M
-essage) Output previ
-ew: $previe
-.ai-dev\review-prompt.md:7979:+    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
-.ai-dev\review-prompt.md:7982:+$script:steps += New-StepResult 4 "validate-generated-json" "goal/qu
-eue/state JSON valid
-ation" $fal
-.ai-dev\review-prompt.md:7983:+se $false 0 "goalMarkdown, queue, and state JSON validation complete
-d. currentTaskId: $(
-$autoGoalPl
-.ai-dev\review-prompt.md:7988:+$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:7989:+$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:7992:+[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $utf8
-WithBom)
-.ai-dev\review-prompt.md:7993:+[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $utf8
-WithBom)
-.ai-dev\review-prompt.md:7995:+$script:steps += New-StepResult 5 "write-state-files" "$goalRelative
-Path, $queueRelative
-Path, $stat
-.ai-dev\review-prompt.md:8002:+    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleCo
-mmandText $false $tr
-ue 0 "Full 
-.ai-dev\review-prompt.md:8023:+$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:8024:+$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:8041:+function Read-JsonFile {
-.ai-dev\review-prompt.md:8048:+        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path | 
-ConvertFrom-Json
-.ai-dev\review-prompt.md:8050:+        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Exceptio
-n.Message)"
-.ai-dev\review-prompt.md:8126:+$queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:8127:+$state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:8211:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:8213:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task
- 완료 처리를 순서대로 연결하는 초안
-이다.
-.ai-dev\review-prompt.md:8224:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevi
-ewDecision`이 `pass`여
-야 한다.
-.ai-dev\review-prompt.md:8225:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 
-있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:8233:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-
-full.ps1 -DryRun -Js
-on
-.ai-dev\review-prompt.md:8268:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `com
-plete_task`가 아니면 자동 
-커밋과 comple
-.ai-dev\review-prompt.md:8269:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package
-.ai-dev\review-prompt.md:8270:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음
- 등으로 실제 commit hash를
- 기록하지 못하면 
-.ai-dev\review-prompt.md:8291:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:8292:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:8293:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:8301:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:8302:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:8320:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:8324:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:8327:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:8331:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:8332:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:8337:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:8341:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:8350:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:8372:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:8392:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:8403:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:8412:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:8414:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:8449:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:8452:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:8453:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:8455:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:8458:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:8461:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:8462:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:8470:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:8476:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:8481:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:8482:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:8483:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:8488:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:8494:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:8500:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:8501:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:8506:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:8507:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:8511:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:8512:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:8526:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:8546:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:8548:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:8551:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:8559:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:8564:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "s
-tate.lastCommand/las
-tCommitHas
-.ai-dev\review-prompt.md:8569:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:8582:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:8584:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task
- 완료 처리를 순서대로 연결하는 초안
-이다.
-.ai-dev\review-prompt.md:8595:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevi
-ewDecision`이 `pass`여
-야 한다.
-.ai-dev\review-prompt.md:8596:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 
-있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:8604:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-
-full.ps1 -DryRun -Js
-on
-.ai-dev\review-prompt.md:8639:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `com
-plete_task`가 아니면 자동 
-커밋과 comple
-.ai-dev\review-prompt.md:8640:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package
-.ai-dev\review-prompt.md:8641:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음
- 등으로 실제 commit hash를
- 기록하지 못하면 
-.ai-dev\review-prompt.md:8662:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:8663:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:8664:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:8672:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:8673:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:8691:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:8695:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:8698:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:8702:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:8703:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:8708:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:8712:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:8721:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:8743:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:8763:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:8774:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:8783:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:8785:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:8820:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:8823:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:8824:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:8826:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:8829:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:8832:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:8833:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:8841:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:8847:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:8852:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:8853:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:8854:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:8859:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:8865:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:8871:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:8872:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:8877:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:8878:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:8882:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:8883:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:8897:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:8917:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:8919:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:8922:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:8930:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:8935:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "s
-tate.lastCommand/las
-tCommitHas
-.ai-dev\review-prompt.md:8940:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:8959:-licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTas
-ks 1 -MaxSteps 20 -J
-son' in D:
-.ai-dev\review-prompt.md:9032:-                      "command":  "state.lastReviewDecision 확인",
-.ai-dev\review-prompt.md:9041:-                      "command":  "git status --porcelain -- package
-.json package-lock.j
-son",
-.ai-dev\review-prompt.md:9059:-                      "name":  "commit-result-gate",
-.ai-dev\review-prompt.md:9070:-e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-.ai-dev\review-prompt.md:9117:+## Required Output
-.ai-dev\review-prompt.md:9129:+- ``package.json``과 ``package-lock.json``은 수정하지 않는다. 꼭 필요하면 중단하고 이유만
- 기록한다.
-.ai-dev\review-prompt.md:9166:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:9168:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 task
- 완료 처리를 순서대로 연결하는 초안
-이다.
-.ai-dev\review-prompt.md:9179:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRevi
-ewDecision`이 `pass`여
-야 한다.
-.ai-dev\review-prompt.md:9180:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이 
-있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:9188:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle-
-full.ps1 -DryRun -Js
-on
-.ai-dev\review-prompt.md:9223:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `com
-plete_task`가 아니면 자동 
-커밋과 comple
-.ai-dev\review-prompt.md:9224:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package
-.ai-dev\review-prompt.md:9225:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음
- 등으로 실제 commit hash를
- 기록하지 못하면 
-.ai-dev\review-prompt.md:9246:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:9247:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:9248:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:9256:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:9257:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:9275:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:9279:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$outp
-ut"
-.ai-dev\review-prompt.md:9282:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:9286:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "p
-ackage.json", "packa
-ge-lock.js
-.ai-dev\review-prompt.md:9287:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:9292:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:9296:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $review
-ResponseRelativePath
-.ai-dev\review-prompt.md:9305:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:9327:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:9347:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $f
-alse $false 1 $_.Exc
-eption.Mes
-.ai-dev\review-prompt.md:9358:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:9367:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-T004 초안은 t
-.ai-dev\review-prompt.md:9369:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTas
-ks" $false $true 0 "
-초기 full cy
-.ai-dev\review-prompt.md:9404:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:9407:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:9408:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 변
-경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:9410:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:9413:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:9416:-+    $script:steps += New-StepResult $stepNumber "complete-task" "po
-wershell -ExecutionP
-olicy Bypa
-.ai-dev\review-prompt.md:9417:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후
- 자동 커밋 완료`"" $false 
-$true 0 "D
-.ai-dev\review-prompt.md:9425:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e/review-response 확인
-" $false $
-.ai-dev\review-prompt.md:9431:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastCommandStatus 
-확인" $false
-.ai-dev\review-prompt.md:9436:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:9437:-+    $script:steps += New-StepResult $stepNumber "review-gate" "stat
-e.lastReviewDecision
- 확인" $fals
-.ai-dev\review-prompt.md:9438:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다: 
-$($reviewGate.decisi
-on)"
-.ai-dev\review-prompt.md:9443:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$rev
-iewResponseRelativeP
-ath next_s
-.ai-dev\review-prompt.md:9449:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.la
-stReviewDecision 확인"
- $false $f
-.ai-dev\review-prompt.md:9455:-+        $script:steps += New-StepResult $stepNumber "package-change
--gate" "git status -
--porcelain
-.ai-dev\review-prompt.md:9456:- -- package.json package-lock.json" $false $true 1 "package.json 또는 
-package-lock.json 변경
-이 감지되어 자동 
-.ai-dev\review-prompt.md:9461:-+    $script:steps += New-StepResult $stepNumber "package-change-gat
-e" "git status --por
-celain -- 
-.ai-dev\review-prompt.md:9462:-package.json package-lock.json" $false $false 1 $_.Exception.Message
-.ai-dev\review-prompt.md:9466:-+$script:steps += New-StepResult $stepNumber "package-change-gate" "
-git status --porcela
-in -- pack
-.ai-dev\review-prompt.md:9467:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허용
- 여부를 확인합니다."
-.ai-dev\review-prompt.md:9481:-+    $script:steps += New-StepResult $stepNumber "commit" "powershel
-l -ExecutionPolicy B
-ypass -Fil
-.ai-dev\review-prompt.md:9501:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:9503:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:9506:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete-
-placeholder" "T005에서
- ai-dev-co
-.ai-dev\review-prompt.md:9521:+현재 스크립트는 이미 주요 흐름을 갖고 있지만, DryRun 미리보기의 한국어 문자열이 깨져 있고 생성 JSON 검증이 프
-롬프트 규칙 일부를 충분히 강제하지 
-않습니다. 파일 작성
-.ai-dev\review-prompt.md:9675:+  scripts\ai-dev-auto-goal.ps1:461:- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:9712:+  scripts\ai-dev-auto-goal.ps1:498:            lastReviewDecision = 
-"not_started"
-.ai-dev\review-prompt.md:9743:+  scripts\ai-dev-auto-goal.ps1:614:- lastReviewDecision must be "not
-_started".
-.ai-dev\review-prompt.md:9770:+  scripts\ai-dev-auto-goal.ps1:637:        $script:steps += New-Step
-Result $StepNumber $
-Name $Comma
-.ai-dev\review-prompt.md:9776:+  scripts\ai-dev-auto-goal.ps1:641:    $output = & powershell -Execu
-tionPolicy Bypass -F
-ile $Script
-.ai-dev\review-prompt.md:9780:+  scripts\ai-dev-auto-goal.ps1:643:    $message = $output.Trim()
-.ai-dev\review-prompt.md:9786:+  scripts\ai-dev-auto-goal.ps1:649:    $script:steps += New-StepResu
-lt $StepNumber $Name
- $Command $
-.ai-dev\review-prompt.md:9812:+  scripts\ai-dev-auto-goal.ps1:671:    # auto-goal writes goal/queue
-/state/current promp
-t/result fi
-.ai-dev\review-prompt.md:9842:+  scripts\ai-dev-auto-goal.ps1:737:        $script:steps += New-Step
-Result 2 "preview-pl
-an" "local 
-.ai-dev\review-prompt.md:9849:+  scripts\ai-dev-auto-goal.ps1:741:    $script:steps += New-StepResu
-lt 2 "dirty-worktree
--gate" "git
-.ai-dev\review-prompt.md:9852:+  scripts\ai-dev-auto-goal.ps1:742:    $script:steps += New-StepResu
-lt 3 "plan-goal" "co
-dex exec <a
-.ai-dev\review-prompt.md:9856:+  scripts\ai-dev-auto-goal.ps1:743:    $script:steps += New-StepResu
-lt 4 "validate-gener
-ated-json" 
-.ai-dev\review-prompt.md:9860:+  scripts\ai-dev-auto-goal.ps1:744:    $script:steps += New-StepResu
-lt 5 "write-state-fi
-les" "$goal
-.ai-dev\review-prompt.md:9863:+  scripts\ai-dev-auto-goal.ps1:745:    $script:steps += New-StepResu
-lt 6 "make-prompt" "
-powershell 
-.ai-dev\review-prompt.md:9869:+  scripts\ai-dev-auto-goal.ps1:748:        $script:steps += New-Step
-Result 7 "auto-cycle
--full" $ful
-.ai-dev\review-prompt.md:9875:+  scripts\ai-dev-auto-goal.ps1:750:        $script:steps += New-Step
-Result 7 "auto-cycle
--full" $ful
-.ai-dev\review-prompt.md:9889:+  scripts\ai-dev-auto-goal.ps1:760:    $script:steps += New-StepResu
-lt 2 "dirty-worktree
--gate" "git
-.ai-dev\review-prompt.md:9896:+  scripts\ai-dev-auto-goal.ps1:765:    $script:steps += New-StepResu
-lt 2 "dirty-worktree
--gate" "git
-.ai-dev\review-prompt.md:9900:+  scripts\ai-dev-auto-goal.ps1:767:    $script:steps += New-StepResu
-lt 2 "dirty-worktree
--gate" "git
-.ai-dev\review-prompt.md:9908:+  scripts\ai-dev-auto-goal.ps1:773:    $script:steps += New-StepResu
-lt 3 "plan-goal" "co
-dex exec <a
-.ai-dev\review-prompt.md:9917:+  scripts\ai-dev-auto-goal.ps1:779:$codexOutput = & codex exec $plan
-nerPrompt 2>&1 | Out
--String
-.ai-dev\review-prompt.md:9921:+  scripts\ai-dev-auto-goal.ps1:783:$resultContent = @"
-.ai-dev\review-prompt.md:9922:+  scripts\ai-dev-auto-goal.ps1:784:# Codex Auto Goal Result
-.ai-dev\review-prompt.md:9932:+  scripts\ai-dev-auto-goal.ps1:794:## Output
-.ai-dev\review-prompt.md:9935:+  scripts\ai-dev-auto-goal.ps1:797:$codexOutput
-.ai-dev\review-prompt.md:9939:+  scripts\ai-dev-auto-goal.ps1:801:[System.IO.File]::WriteAllText($r
-esolvedResultPath, $
-resultConte
-.ai-dev\review-prompt.md:9943:+  scripts\ai-dev-auto-goal.ps1:804:    $script:steps += New-StepResu
-lt 3 "plan-goal" "co
-dex exec <a
-.ai-dev\review-prompt.md:9945:+ompt>" $true $false 1 "Codex goal planning failed. Result: $(Convert
-To-RepoRelativePath 
-$ResultPath
-.ai-dev\review-prompt.md:9947:-+    $script:steps += New-StepResult $stepNumber "commit-result-gate
-" "state.lastCommand
-/lastCommi
-.ai-dev\review-prompt.md:9952:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "s
-tate.lastCommand/las
-tCommitHas
-.ai-dev\review-prompt.md:9957:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $sc
-riptPaths.completeTa
-sk @("-Res
-.ai-dev\review-prompt.md:9963:+  scripts\ai-dev-auto-goal.ps1:808:$script:steps += New-StepResult 3
- "plan-goal" "codex 
-exec <auto-
-.ai-dev\review-prompt.md:9965:+>" $true $false 0 "Codex goal planning completed. Result: $(ConvertT
-o-RepoRelativePath $
-ResultPath)
-.ai-dev\review-prompt.md:9969:+  scripts\ai-dev-auto-goal.ps1:811:    $autoGoalPlan = ConvertFrom-C
-odexAutoGoalOutput $
-codexOutput
-.ai-dev\review-prompt.md:9971:+  scripts\ai-dev-auto-goal.ps1:813:    $preview = Get-InputPreview $
-codexOutput
-.ai-dev\review-prompt.md:9990:+  .ai-dev\README.md:760:자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 
-출력은 사람이 읽는 텍스트와 섞지 않
-는다.
-.ai-dev\review-prompt.md:9991:+  .ai-dev\README.md:761:DryRun JSON에는 `plan.goalMarkdown`, `plan.que
-ue`, `plan.state` 미리
-보기가 포함되므로 파
-.ai-dev\review-prompt.md:9998:+라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -
-DryRun -Json
-.ai-dev\review-prompt.md:10031:+  .ai-dev\README.md:783:3. Codex 출력에서 `goalMarkdown`, `queue`, `sta
-te` JSON을 추출한다.
-.ai-dev\review-prompt.md:10032:+  .ai-dev\README.md:784:4. `queue.json`과 `state.json`의 필수 필드, curre
-ntTaskId, task 배열, e
-num 값을 검증한다.
-.ai-dev\review-prompt.md:10033:+  .ai-dev\README.md:785:5. `.ai-dev/goal.md`, `.ai-dev/queue.json`,
- `.ai-dev/state.json
-`을 작성한다.
-.ai-dev\review-prompt.md:10036:+  .ai-dev\README.md:788:8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue
-.json`, `state.json`
-, `current-t
-.ai-dev\review-prompt.md:10046:+  .ai-dev\README.md:794:- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:10047:+  .ai-dev\README.md:795:- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패
-하면 파일 작성과 full cycle
- 실행으로 넘어가지 않
-.ai-dev\review-prompt.md:10091:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:10093:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 tas
-k 완료 처리를 순서대로 연결하는 초
-안이다.
-.ai-dev\review-prompt.md:10104:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRev
-iewDecision`이 `pass`
-여야 한다.
-.ai-dev\review-prompt.md:10105:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이
- 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:10113:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle
--full.ps1 -DryRun -J
-son
-.ai-dev\review-prompt.md:10148:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `co
-mplete_task`가 아니면 자동
- 커밋과 comple
-.ai-dev\review-prompt.md:10149:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 
-않는다. `package.json` 
-또는 `package
-.ai-dev\review-prompt.md:10150:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없
-음 등으로 실제 commit hash
-를 기록하지 못하면 
-.ai-dev\review-prompt.md:10171:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:10172:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:10173:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:10181:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:10182:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:10200:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:10204:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$out
-put"
-.ai-dev\review-prompt.md:10207:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:10211:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "
-package.json", "pack
-age-lock.js
-.ai-dev\review-prompt.md:10212:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:10217:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:10221:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revie
-wResponseRelativePat
-h
-.ai-dev\review-prompt.md:10230:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:10252:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:10323:+ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary
-", "lastReviewDecisi
-on", "lastRe
-.ai-dev\review-prompt.md:10346:+    if (@("not_started", "pass", "revise", "blocked") -notcontains 
-$State.lastReviewDec
-ision) {
-.ai-dev\review-prompt.md:10347:+        $errors += "invalid state.lastReviewDecision: $($State.last
-ReviewDecision)"
-.ai-dev\review-prompt.md:10412:+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:10449:+            lastReviewDecision = "not_started"
-.ai-dev\review-prompt.md:10484:+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:10498:+function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:10501:+    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-.ai-dev\review-prompt.md:10503:+            $parsed = $candidate | ConvertFrom-Json
-.ai-dev\review-prompt.md:10527:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $
-false $false 1 $_.Ex
-ception.Mes
-.ai-dev\review-prompt.md:10538:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:10547:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"T004 초안은 t
-.ai-dev\review-prompt.md:10549:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"초기 full cy
-.ai-dev\review-prompt.md:10584:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:10587:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:10588:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 
-변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:10590:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:10593:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:10596:-+    $script:steps += New-StepResult $stepNumber "complete-task" "p
-owershell -Execution
-Policy Bypa
-.ai-dev\review-prompt.md:10597:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 
-후 자동 커밋 완료`"" $false
- $true 0 "D
-.ai-dev\review-prompt.md:10605:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te/review-response 확
-인" $false $
-.ai-dev\review-prompt.md:10611:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastCommandStatus
- 확인" $false
-.ai-dev\review-prompt.md:10619:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:10620:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:10621:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다:
- $($reviewGate.decis
-ion)"
-.ai-dev\review-prompt.md:10630:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$re
-viewResponseRelative
-Path next_s
-.ai-dev\review-prompt.md:10636:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.l
-astReviewDecision 확인
-" $false $f
-.ai-dev\review-prompt.md:10642:-+        $script:steps += New-StepResult $stepNumber "package-chang
-e-gate" "git status 
---porcelain
-.ai-dev\review-prompt.md:10643:- -- package.json package-lock.json" $false $true 1 "package.json 또는
- package-lock.json 변
-경이 감지되어 자동 
-.ai-dev\review-prompt.md:10648:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:10649:-package.json package-lock.json" $false $false 1 $_.Exception.Messag
-e
-.ai-dev\review-prompt.md:10657:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete
--placeholder" "T005에
-서 ai-dev-co
-.ai-dev\review-prompt.md:10660:-+$script:steps += New-StepResult $stepNumber "package-change-gate" 
-"git status --porcel
-ain -- pack
-.ai-dev\review-prompt.md:10661:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허
-용 여부를 확인합니다."
-.ai-dev\review-prompt.md:10684:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:10692:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:10709:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:10711:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:10719:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:10724:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "
-state.lastCommand/la
-stCommitHas
-.ai-dev\review-prompt.md:10729:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $s
-criptPaths.completeT
-ask @("-Res
-.ai-dev\review-prompt.md:10742:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:10744:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 tas
-k 완료 처리를 순서대로 연결하는 초
-안이다.
-.ai-dev\review-prompt.md:10755:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRev
-iewDecision`이 `pass`
-여야 한다.
-.ai-dev\review-prompt.md:10756:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이
- 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:10764:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle
--full.ps1 -DryRun -J
-son
-.ai-dev\review-prompt.md:10799:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `co
-mplete_task`가 아니면 자동
- 커밋과 comple
-.ai-dev\review-prompt.md:10800:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 
-않는다. `package.json` 
-또는 `package
-.ai-dev\review-prompt.md:10801:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없
-음 등으로 실제 commit hash
-를 기록하지 못하면 
-.ai-dev\review-prompt.md:10822:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:10823:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:10824:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:10832:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:10833:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:10855:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:10859:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$out
-put"
-.ai-dev\review-prompt.md:10864:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:10868:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "
-package.json", "pack
-age-lock.js
-.ai-dev\review-prompt.md:10869:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:10874:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:10878:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revie
-wResponseRelativePat
-h
-.ai-dev\review-prompt.md:10901:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:10938:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:10958:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $
-false $false 1 $_.Ex
-ception.Mes
-.ai-dev\review-prompt.md:10969:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:10978:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"T004 초안은 t
-.ai-dev\review-prompt.md:10980:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"초기 full cy
-.ai-dev\review-prompt.md:11016:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:11019:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:11020:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 
-변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:11022:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:11025:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:11028:-+    $script:steps += New-StepResult $stepNumber "complete-task" "p
-owershell -Execution
-Policy Bypa
-.ai-dev\review-prompt.md:11029:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 
-후 자동 커밋 완료`"" $false
- $true 0 "D
-.ai-dev\review-prompt.md:11037:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te/review-response 확
-인" $false $
-.ai-dev\review-prompt.md:11043:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastCommandStatus
- 확인" $false
-.ai-dev\review-prompt.md:11048:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:11049:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:11050:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다:
- $($reviewGate.decis
-ion)"
-.ai-dev\review-prompt.md:11058:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$re
-viewResponseRelative
-Path next_s
-.ai-dev\review-prompt.md:11064:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.l
-astReviewDecision 확인
-" $false $f
-.ai-dev\review-prompt.md:11070:-+        $script:steps += New-StepResult $stepNumber "package-chang
-e-gate" "git status 
---porcelain
-.ai-dev\review-prompt.md:11071:- -- package.json package-lock.json" $false $true 1 "package.json 또는
- package-lock.json 변
-경이 감지되어 자동 
-.ai-dev\review-prompt.md:11076:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:11077:-package.json package-lock.json" $false $false 1 $_.Exception.Messag
-e
-.ai-dev\review-prompt.md:11084:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete
--placeholder" "T005에
-서 ai-dev-co
-.ai-dev\review-prompt.md:11087:-+$script:steps += New-StepResult $stepNumber "package-change-gate" 
-"git status --porcel
-ain -- pack
-.ai-dev\review-prompt.md:11088:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허
-용 여부를 확인합니다."
-.ai-dev\review-prompt.md:11104:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:11112:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:11129:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:11131:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:11139:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:11144:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "
-state.lastCommand/la
-stCommitHas
-.ai-dev\review-prompt.md:11149:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $s
-criptPaths.completeT
-ask @("-Res
-.ai-dev\review-prompt.md:11171:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:11173:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 tas
-k 완료 처리를 순서대로 연결하는 초
-안이다.
-.ai-dev\review-prompt.md:11184:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRev
-iewDecision`이 `pass`
-여야 한다.
-.ai-dev\review-prompt.md:11185:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이
- 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:11193:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle
--full.ps1 -DryRun -J
-son
-.ai-dev\review-prompt.md:11239:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `co
-mplete_task`가 아니면 자동
- 커밋과 comple
-.ai-dev\review-prompt.md:11240:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 
-않는다. `package.json` 
-또는 `package
-.ai-dev\review-prompt.md:11241:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없
-음 등으로 실제 commit hash
-를 기록하지 못하면 
-.ai-dev\review-prompt.md:11262:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:11263:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:11264:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:11272:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:11273:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:11296:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:11300:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$out
-put"
-.ai-dev\review-prompt.md:11303:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:11307:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "
-package.json", "pack
-age-lock.js
-.ai-dev\review-prompt.md:11308:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:11313:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:11317:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revie
-wResponseRelativePat
-h
-.ai-dev\review-prompt.md:11331:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:11356:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:11376:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $
-false $false 1 $_.Ex
-ception.Mes
-.ai-dev\review-prompt.md:11387:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:11396:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"T004 초안은 t
-.ai-dev\review-prompt.md:11398:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"초기 full cy
-.ai-dev\review-prompt.md:11434:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:11437:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:11438:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 
-변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:11440:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:11443:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:11446:-+    $script:steps += New-StepResult $stepNumber "complete-task" "p
-owershell -Execution
-Policy Bypa
-.ai-dev\review-prompt.md:11447:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 
-후 자동 커밋 완료`"" $false
- $true 0 "D
-.ai-dev\review-prompt.md:11455:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te/review-response 확
-인" $false $
-.ai-dev\review-prompt.md:11461:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastCommandStatus
- 확인" $false
-.ai-dev\review-prompt.md:11466:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:11467:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:11468:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다:
- $($reviewGate.decis
-ion)"
-.ai-dev\review-prompt.md:11479:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$re
-viewResponseRelative
-Path next_s
-.ai-dev\review-prompt.md:11485:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.l
-astReviewDecision 확인
-" $false $f
-.ai-dev\review-prompt.md:11491:-+        $script:steps += New-StepResult $stepNumber "package-chang
-e-gate" "git status 
---porcelain
-.ai-dev\review-prompt.md:11492:- -- package.json package-lock.json" $false $true 1 "package.json 또는
- package-lock.json 변
-경이 감지되어 자동 
-.ai-dev\review-prompt.md:11499:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:11500:-package.json package-lock.json" $false $false 1 $_.Exception.Messag
-e
-.ai-dev\review-prompt.md:11557:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:11560:++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:11622:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:11644:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete
--placeholder" "T005에
-서 ai-dev-co
-.ai-dev\review-prompt.md:11647:-+$script:steps += New-StepResult $stepNumber "package-change-gate" 
-"git status --porcel
-ain -- pack
-.ai-dev\review-prompt.md:11648:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허
-용 여부를 확인합니다."
-.ai-dev\review-prompt.md:11664:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:11672:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:11689:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:11691:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:11699:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:11704:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "
-state.lastCommand/la
-stCommitHas
-.ai-dev\review-prompt.md:11709:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $s
-criptPaths.completeT
-ask @("-Res
-.ai-dev\review-prompt.md:11722:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:11724:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 tas
-k 완료 처리를 순서대로 연결하는 초
-안이다.
-.ai-dev\review-prompt.md:11735:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRev
-iewDecision`이 `pass`
-여야 한다.
-.ai-dev\review-prompt.md:11736:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이
- 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:11744:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle
--full.ps1 -DryRun -J
-son
-.ai-dev\review-prompt.md:11780:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `co
-mplete_task`가 아니면 자동
- 커밋과 comple
-.ai-dev\review-prompt.md:11781:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 
-않는다. `package.json` 
-또는 `package
-.ai-dev\review-prompt.md:11782:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없
-음 등으로 실제 commit hash
-를 기록하지 못하면 
-.ai-dev\review-prompt.md:11803:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:11804:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:11805:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:11813:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:11814:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:11836:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:11840:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$out
-put"
-.ai-dev\review-prompt.md:11843:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:11847:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "
-package.json", "pack
-age-lock.js
-.ai-dev\review-prompt.md:11848:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:11853:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:11857:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revie
-wResponseRelativePat
-h
-.ai-dev\review-prompt.md:11866:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:11896:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:11916:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $
-false $false 1 $_.Ex
-ception.Mes
-.ai-dev\review-prompt.md:11927:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:11936:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"T004 초안은 t
-.ai-dev\review-prompt.md:11938:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"초기 full cy
-.ai-dev\review-prompt.md:11974:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:11977:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:11978:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 
-변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:11980:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:11983:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:11986:-+    $script:steps += New-StepResult $stepNumber "complete-task" "p
-owershell -Execution
-Policy Bypa
-.ai-dev\review-prompt.md:11987:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 
-후 자동 커밋 완료`"" $false
- $true 0 "D
-.ai-dev\review-prompt.md:11995:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te/review-response 확
-인" $false $
-.ai-dev\review-prompt.md:12001:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastCommandStatus
- 확인" $false
-.ai-dev\review-prompt.md:12006:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:12007:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:12008:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다:
- $($reviewGate.decis
-ion)"
-.ai-dev\review-prompt.md:12016:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$re
-viewResponseRelative
-Path next_s
-.ai-dev\review-prompt.md:12022:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.l
-astReviewDecision 확인
-" $false $f
-.ai-dev\review-prompt.md:12028:-+        $script:steps += New-StepResult $stepNumber "package-chang
-e-gate" "git status 
---porcelain
-.ai-dev\review-prompt.md:12029:- -- package.json package-lock.json" $false $true 1 "package.json 또는
- package-lock.json 변
-경이 감지되어 자동 
-.ai-dev\review-prompt.md:12034:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:12035:-package.json package-lock.json" $false $false 1 $_.Exception.Messag
-e
-.ai-dev\review-prompt.md:12042:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete
--placeholder" "T005에
-서 ai-dev-co
-.ai-dev\review-prompt.md:12045:-+$script:steps += New-StepResult $stepNumber "package-change-gate" 
-"git status --porcel
-ain -- pack
-.ai-dev\review-prompt.md:12046:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허
-용 여부를 확인합니다."
-.ai-dev\review-prompt.md:12062:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:12070:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:12087:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:12089:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:12097:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:12102:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "
-state.lastCommand/la
-stCommitHas
-.ai-dev\review-prompt.md:12107:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $s
-criptPaths.completeT
-ask @("-Res
-.ai-dev\review-prompt.md:12164:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:12167:++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:12229:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:12245:+수정은 완료되었습니다. 이제 PowerShell 파서로 문법을 확인하고, DryRun/JSON/빈 입력 오류 경로가 요구
-사항대로 동작하는지 실행 수준에서 검
-증하겠습니다.
-.ai-dev\review-prompt.md:12258:+검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun -Json" in D:\ai-ap
-ps\planpilot-local
-.ai-dev\review-prompt.md:12261:-licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxTa
-sks 1 -MaxSteps 20 -
-Json' in D:
-.ai-dev\review-prompt.md:12265:+icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalTitle '' -Go
-alDescription '설명' -
-DryRun -Json
-.ai-dev\review-prompt.md:12287:+Output:
-.ai-dev\review-prompt.md:12345:+                      "name":  "validate-generated-json",
-.ai-dev\review-prompt.md:12346:+                      "command":  "goal/queue/state JSON validation
-",
-.ai-dev\review-prompt.md:12360:+                      "command":  ".ai-dev/goal.md, .ai-dev/queue.j
-son, .ai-dev/state.j
-son",
-.ai-dev\review-prompt.md:12384:-                      "command":  "state.lastReviewDecision 확인",
-.ai-dev\review-prompt.md:12404:+# 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재 작업 프롬프트 생성 여부를 확인한다.\n
-- 필요한 경우 빌드와 검증은 별도로
- 실행한다.",
-.ai-dev\review-prompt.md:12444:+                               "lastReviewDecision":  "not_started"
-,
-.ai-dev\review-prompt.md:12453:+    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:12454:+    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:12456:+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:12479:+Step 4: validate-generated-json
-.ai-dev\review-prompt.md:12480:+  Command: goal/queue/state JSON validation
-.ai-dev\review-prompt.md:12486:+  Command: .ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json
-.ai-dev\review-prompt.md:12616:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:12619:++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:12681:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:12696:+DryRun과 JSON 출력은 정상입니다. 빈 문자열 테스트는 PowerShell 인수 파서가 `''`를 값 없는 파라미
-터로 처리해 스크립트 내부 검증까지 
-들어가지 않았고, 공백
-.ai-dev\review-prompt.md:12705:+검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다.' -DryRun -AllowRun -Json" i
-n D:\ai-apps\planpil
-ot-local
-.ai-dev\review-prompt.md:12708:+icy Bypass -File .\\scripts\\ai-dev-auto-goal.ps1 -GoalDescription 
-'설명' -DryRun -Json" 
-in D:\ai-app
-.ai-dev\review-prompt.md:12728:+    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:12729:+    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:12731:+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:12736:+Output:
-.ai-dev\review-prompt.md:12754:+    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:12755:+    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:12757:+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:12778:+    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:12779:+    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:12781:+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:12819:-                      "command":  "git status --porcelain -- packag
-e.json package-lock.
-json",
-.ai-dev\review-prompt.md:12821:+                      "name":  "validate-generated-json",
-.ai-dev\review-prompt.md:12822:+                      "command":  "goal/queue/state JSON validation
-",
-.ai-dev\review-prompt.md:12837:+                      "command":  ".ai-dev/goal.md, .ai-dev/queue.j
-son, .ai-dev/state.j
-son",
-.ai-dev\review-prompt.md:12846:-                      "name":  "commit-result-gate",
-.ai-dev\review-prompt.md:12862:-e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-.ai-dev\review-prompt.md:12885:+# 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재 작업 프롬프트 생성 여부를 확인한다.\n
-- 필요한 경우 빌드와 검증은 별도로
- 실행한다.",
-.ai-dev\review-prompt.md:12925:+                               "lastReviewDecision":  "not_started"
-,
-.ai-dev\review-prompt.md:12934:+    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:12935:+    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:12937:+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:12949:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:12951:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 tas
-k 완료 처리를 순서대로 연결하는 초
-안이다.
-.ai-dev\review-prompt.md:12962:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRev
-iewDecision`이 `pass`
-여야 한다.
-.ai-dev\review-prompt.md:12963:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이
- 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:12971:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle
--full.ps1 -DryRun -J
-son
-.ai-dev\review-prompt.md:12988:+Output:
-.ai-dev\review-prompt.md:13006:+    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:13007:+    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:13009:+    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:13042:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `co
-mplete_task`가 아니면 자동
- 커밋과 comple
-.ai-dev\review-prompt.md:13043:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 
-않는다. `package.json` 
-또는 `package
-.ai-dev\review-prompt.md:13044:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없
-음 등으로 실제 commit hash
-를 기록하지 못하면 
-.ai-dev\review-prompt.md:13066:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:13067:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:13068:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:13076:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:13077:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:13099:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:13103:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$out
-put"
-.ai-dev\review-prompt.md:13106:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:13110:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "
-package.json", "pack
-age-lock.js
-.ai-dev\review-prompt.md:13111:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:13116:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:13120:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revie
-wResponseRelativePat
-h
-.ai-dev\review-prompt.md:13129:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:13159:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:13179:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $
-false $false 1 $_.Ex
-ception.Mes
-.ai-dev\review-prompt.md:13190:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:13199:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"T004 초안은 t
-.ai-dev\review-prompt.md:13201:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"초기 full cy
-.ai-dev\review-prompt.md:13290:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:13293:++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:13355:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:13370:+검증 결과 DryRun은 파일을 쓰지 않고 계획과 단계만 출력했고, JSON 모드는 텍스트와 섞이지 않았습니다. 마지막으
-로 수정된 함수 주변을 다시 읽어 의
-도치 않은 중복이나 깨
-.ai-dev\review-prompt.md:13527:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:13530:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:13531:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 
-변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:13533:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:13536:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:13539:-+    $script:steps += New-StepResult $stepNumber "complete-task" "p
-owershell -Execution
-Policy Bypa
-.ai-dev\review-prompt.md:13540:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 
-후 자동 커밋 완료`"" $false
- $true 0 "D
-.ai-dev\review-prompt.md:13548:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te/review-response 확
-인" $false $
-.ai-dev\review-prompt.md:13554:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastCommandStatus
- 확인" $false
-.ai-dev\review-prompt.md:13562:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:13563:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:13564:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다:
- $($reviewGate.decis
-ion)"
-.ai-dev\review-prompt.md:13573:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$re
-viewResponseRelative
-Path next_s
-.ai-dev\review-prompt.md:13579:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.l
-astReviewDecision 확인
-" $false $f
-.ai-dev\review-prompt.md:13585:-+        $script:steps += New-StepResult $stepNumber "package-chang
-e-gate" "git status 
---porcelain
-.ai-dev\review-prompt.md:13586:- -- package.json package-lock.json" $false $true 1 "package.json 또는
- package-lock.json 변
-경이 감지되어 자동 
-.ai-dev\review-prompt.md:13591:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:13592:-package.json package-lock.json" $false $false 1 $_.Exception.Messag
-e
-.ai-dev\review-prompt.md:13600:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete
--placeholder" "T005에
-서 ai-dev-co
-.ai-dev\review-prompt.md:13603:-+$script:steps += New-StepResult $stepNumber "package-change-gate" 
-"git status --porcel
-ain -- pack
-.ai-dev\review-prompt.md:13604:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허
-용 여부를 확인합니다."
-.ai-dev\review-prompt.md:13627:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:13635:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:13652:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:13654:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:13662:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:13667:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "
-state.lastCommand/la
-stCommitHas
-.ai-dev\review-prompt.md:13682:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $s
-criptPaths.completeT
-ask @("-Res
-.ai-dev\review-prompt.md:13739:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:13742:++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:13804:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:13829:-- full auto-cycle에 `review-gate`, `package-change-gate`, `commit-re
-sult-gate`를 추가했습니다.
-.ai-dev\review-prompt.md:13830:-- 리뷰 저장 결과가 `passed`이고 `lastReviewDecision`이 `pass`일 때만 커밋 단계로 진행합니
-다.
-.ai-dev\review-prompt.md:13831:-- `review-response.json`의 `next_step`이 있으면 `complete_task`일 때만 진행합니
-다.
-.ai-dev\review-prompt.md:13832:-- `package.json` 또는 `package-lock.json` 변경 감지 시 `package_files_chan
-ged`로 중단합니다.
-.ai-dev\review-prompt.md:13851:- 1 -MaxSteps 20 -Json`
-.ai-dev\review-prompt.md:13856:+- `-DryRun -Json` 순수 JSON 출력 확인
-.ai-dev\review-prompt.md:13858:+- `-DryRun -AllowRun -Json`에서 full-cycle 호출이 실제 실행되지 않고 단계만 표시되는 것 
-확인
-.ai-dev\review-prompt.md:13890:-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:13892:-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 tas
-k 완료 처리를 순서대로 연결하는 초
-안이다.
-.ai-dev\review-prompt.md:13903:-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRev
-iewDecision`이 `pass`
-여야 한다.
-.ai-dev\review-prompt.md:13904:-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이
- 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:13912:- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle
--full.ps1 -DryRun -J
-son
-.ai-dev\review-prompt.md:13928:+rShell 문법, DryRun, Json 출력, 빈 GoalTitle 오류 경로를 확인했다.`
-.ai-dev\review-prompt.md:13959:-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `co
-mplete_task`가 아니면 자동
- 커밋과 comple
-.ai-dev\review-prompt.md:13960:-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 
-않는다. `package.json` 
-또는 `package
-.ai-dev\review-prompt.md:13961:--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없
-음 등으로 실제 commit hash
-를 기록하지 못하면 
-.ai-dev\review-prompt.md:13982:- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:13983:- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:13984:-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:13992:-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:13993:-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:14016:-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:14020:-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$out
-put"
-.ai-dev\review-prompt.md:14023:-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:14027:-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", "
-package.json", "pack
-age-lock.js
-.ai-dev\review-prompt.md:14028:-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:14033:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:14037:-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revie
-wResponseRelativePat
-h
-.ai-dev\review-prompt.md:14051:-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:14076:-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:14096:-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $
-false $false 1 $_.Ex
-ception.Mes
-.ai-dev\review-prompt.md:14107:-+    "commit-result-gate",
-.ai-dev\review-prompt.md:14116:--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"T004 초안은 t
-.ai-dev\review-prompt.md:14118:-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"초기 full cy
-.ai-dev\review-prompt.md:14154:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:14157:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:14158:-package.json package-lock.json" $false $true 0 "DryRun: package 파일 
-변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:14160:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:14163:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:14166:-+    $script:steps += New-StepResult $stepNumber "complete-task" "p
-owershell -Execution
-Policy Bypa
-.ai-dev\review-prompt.md:14167:-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 
-후 자동 커밋 완료`"" $false
- $true 0 "D
-.ai-dev\review-prompt.md:14175:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te/review-response 확
-인" $false $
-.ai-dev\review-prompt.md:14181:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastCommandStatus
- 확인" $false
-.ai-dev\review-prompt.md:14186:-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:14187:-+    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $fals
-.ai-dev\review-prompt.md:14188:-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다:
- $($reviewGate.decis
-ion)"
-.ai-dev\review-prompt.md:14196:-+    $script:steps += New-StepResult $stepNumber "review-gate" "$re
-viewResponseRelative
-Path next_s
-.ai-dev\review-prompt.md:14202:-+$script:steps += New-StepResult $stepNumber "review-gate" "state.l
-astReviewDecision 확인
-" $false $f
-.ai-dev\review-prompt.md:14208:-+        $script:steps += New-StepResult $stepNumber "package-chang
-e-gate" "git status 
---porcelain
-.ai-dev\review-prompt.md:14209:- -- package.json package-lock.json" $false $true 1 "package.json 또는
- package-lock.json 변
-경이 감지되어 자동 
-.ai-dev\review-prompt.md:14214:-+    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- 
-.ai-dev\review-prompt.md:14215:-package.json package-lock.json" $false $false 1 $_.Exception.Messag
-e
-.ai-dev\review-prompt.md:14222:--$script:steps += New-StepResult ($stepNumber + 1) "commit-complete
--placeholder" "T005에
-서 ai-dev-co
-.ai-dev\review-prompt.md:14225:-+$script:steps += New-StepResult $stepNumber "package-change-gate" 
-"git status --porcel
-ain -- pack
-.ai-dev\review-prompt.md:14226:-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 허
-용 여부를 확인합니다."
-.ai-dev\review-prompt.md:14242:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:14250:-+    $script:steps += New-StepResult $stepNumber "commit" "powershe
-ll -ExecutionPolicy 
-Bypass -Fil
-.ai-dev\review-prompt.md:14267:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:14269:-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:14277:-+    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommi
-.ai-dev\review-prompt.md:14282:-+$script:steps += New-StepResult $stepNumber "commit-result-gate" "
-state.lastCommand/la
-stCommitHas
-.ai-dev\review-prompt.md:14287:-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $s
-criptPaths.completeT
-ask @("-Res
-.ai-dev\review-prompt.md:14344:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:14347:++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:14409:+-- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:14434:-- full auto-cycle에 `review-gate`, `package-change-gate`, `commit-re
-sult-gate`를 추가했습니다.
-.ai-dev\review-prompt.md:14435:-- 리뷰 저장 결과가 `passed`이고 `lastReviewDecision`이 `pass`일 때만 커밋 단계로 진행합니
-다.
-.ai-dev\review-prompt.md:14436:-- `review-response.json`의 `next_step`이 있으면 `complete_task`일 때만 진행합니
-다.
-.ai-dev\review-prompt.md:14437:-- `package.json` 또는 `package-lock.json` 변경 감지 시 `package_files_chan
-ged`로 중단합니다.
-.ai-dev\review-prompt.md:14453:-  - `powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-c
-ycle-full.ps1 -DryRu
-n -MaxTasks 1 -MaxSteps 20 -Json`
-.ai-dev\review-prompt.md:14458:+- `-DryRun -Json` 순수 JSON 출력 확인
-.ai-dev\review-prompt.md:14460:+- `-DryRun -AllowRun -Json`에서 full-cycle 호출이 실제 실행되지 않고 단계만 표시되는 것 
-확인
-.ai-dev\review-prompt.md:14483:+`T007: ai-dev-auto-goal.ps1 DryRun 미리보기 문자열을 복구하고 goal/queue/state 
-생성 검증을 T001 시작 규칙에 맞
-게 강화했다. PowerShell 문법, DryRun, Json 출력, 빈 GoalTitle 오류 경로를 확인했다.`
-.ai-dev\review-prompt.md:14488:diff --git a/.ai-dev/codex-review-result.md b/.ai-dev/codex-review-r
-esult.md
-.ai-dev\review-prompt.md:14490:--- a/.ai-dev/codex-review-result.md
-.ai-dev\review-prompt.md:14491:+++ b/.ai-dev/codex-review-result.md
-.ai-dev\review-prompt.md:14503: - Review response: .ai-dev/review-response.json
-.ai-dev\review-prompt.md:14507: ## Output
-.ai-dev\review-prompt.md:14564:+`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-promp
-t.md`를 Codex CLI에 전달
-해 JSON 리뷰를 생
-.ai-dev\review-prompt.md:14574:+- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-rev
-iew-codex.ps1`이 추가된다
-.
-.ai-dev\review-prompt.md:14578:+- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:14592:+- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:14613:+- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
-.ai-dev\review-prompt.md:14636:+- Json 출력이 가능한지 확인
-.ai-dev\review-prompt.md:14637:+- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-.ai-dev\review-prompt.md:14642:+## Test Result
-.ai-dev\review-prompt.md:14644:+# AI Dev Test Result
-.ai-dev\review-prompt.md:14648:+- Overall result: passed
-.ai-dev\review-prompt.md:14705:+ M .ai-dev/codex-result.md
-.ai-dev\review-prompt.md:14706:+ M .ai-dev/codex-review-result.md
-.ai-dev\review-prompt.md:14710:+ M .ai-dev/review-response.json
-.ai-dev\review-prompt.md:14712:+ M .ai-dev/state.json
-.ai-dev\review-prompt.md:14713:+ M .ai-dev/test-result.md
-.ai-dev\review-prompt.md:14721:+ .ai-dev/codex-result.md        | 13217 +++++++-------------
-.ai-dev\review-prompt.md:14722:+ .ai-dev/codex-review-result.md | 25589 +++++++++++++++++++++++++++
-+++++++++++-
-.ai-dev\review-prompt.md:14723:+ .ai-dev/state.json             |     8 +-
-.ai-dev\review-prompt.md:14747:++자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다
-.
-.ai-dev\review-prompt.md:14748:++DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기
-가 포함되므로 파일 작성 전에 생성될
- goal/task 계
-.ai-dev\review-prompt.md:14753:+ 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -Dr
-yRun -Json
-.ai-dev\review-prompt.md:14780:+-3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
-.ai-dev\review-prompt.md:14783:++3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-.ai-dev\review-prompt.md:14784:++4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum
- 값을 검증한다.
-.ai-dev\review-prompt.md:14785:++5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 
-작성한다.
-.ai-dev\review-prompt.md:14788:++8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `
-current-task-prompt.
-md`, 결과 파일 때
-.ai-dev\review-prompt.md:14797:+ - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:14798:+-- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:14800:++- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행
-으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:14809:+diff --git a/.ai-dev/codex-result.md b/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:14811:+--- a/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:14812:++++ b/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:14826:+ ## Output
-.ai-dev\review-prompt.md:14853:+-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prom
-pt.md`를 Codex CLI에 전
-달해 JSON 리뷰를 
-.ai-dev\review-prompt.md:14855:++`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prom
-pt.md`를 Codex CLI에 전
-달해 JSON 리뷰를 
-.ai-dev\review-prompt.md:14903:++- Json 출력이 가능한지 확인
-.ai-dev\review-prompt.md:14904:++- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-.ai-dev\review-prompt.md:14919:+-- package.json 또는 package-lock.json을 수정하지 않는다. 꼭 필요하면 작업을 중단하고 이유만
- 기록한다.
-.ai-dev\review-prompt.md:14950:+-    [switch]$Json,
-.ai-dev\review-prompt.md:14987:++codex-result.md
-.ai-dev\review-prompt.md:14988:++codex-review-result.md
-.ai-dev\review-prompt.md:14995:++queue.example.json
-.ai-dev\review-prompt.md:14996:++queue.json
-.ai-dev\review-prompt.md:14999:++review-response.json
-.ai-dev\review-prompt.md:15001:++state.example.json
-.ai-dev\review-prompt.md:15002:++state.json
-.ai-dev\review-prompt.md:15003:++test-result.md
-.ai-dev\review-prompt.md:15012:+-$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:15013:+-$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:15018:++환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-.ai-dev\review-prompt.md:15043:+-function Read-JsonFile {
-.ai-dev\review-prompt.md:15051:+-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path 
-| ConvertFrom-Json
-.ai-dev\review-prompt.md:15053:+-        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Messa
-ge)"
-.ai-dev\review-prompt.md:15086:++- `queue.json`
-.ai-dev\review-prompt.md:15087:++- `state.json`
-.ai-dev\review-prompt.md:15088:++- `test-result.md`
-.ai-dev\review-prompt.md:15108:+-function New-StepResult {
-.ai-dev\review-prompt.md:15118:++- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시
-다.
-.ai-dev\review-prompt.md:15119:++- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시
-다.
-.ai-dev\review-prompt.md:15131:++예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복
-사 원본으로 사용한다.
-.ai-dev\review-prompt.md:15133:+-function New-CycleResult {
-.ai-dev\review-prompt.md:15153:+-function Write-CycleResult {
-.ai-dev\review-prompt.md:15155:+-        [object]$Result
-.ai-dev\review-prompt.md:15159:+-    if ($Json) {
-.ai-dev\review-prompt.md:15160:+-        $Result | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:15167:+-    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:15177:+-    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:15178:+-    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:15179:+-    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:15199:+-    $result = New-CycleResult $Steps $StoppedReason $Completed $Ex
-itCode
-.ai-dev\review-prompt.md:15200:+-    Write-CycleResult $result
-.ai-dev\review-prompt.md:15218:+-        $script:steps += New-StepResult $StepNumber $Name $Command
- $false $true 0 "Dry
-Run: 하위 스크립트
-.ai-dev\review-prompt.md:15224:+-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPa
-th @Arguments 2>&1 |
- Out-String
-.ai-dev\review-prompt.md:15226:+-    $message = $output.Trim()
-.ai-dev\review-prompt.md:15227:++`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-.ai-dev\review-prompt.md:15236:+-    $script:steps += New-StepResult $StepNumber $Name $Command $tr
-ue $false $exitCode 
-$message
-.ai-dev\review-prompt.md:15237:++현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:15244:++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-ta
-sk.ps1 -Json
-.ai-dev\review-prompt.md:15259:++`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai
--dev/current-task-pr
-ompt.md` 파일을
-.ai-dev\review-prompt.md:15303:+-    $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:15304:+-    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:15316:++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-t
-ask.ps1 -ResultSumma
-ry "T001 분석 
-.ai-dev\review-prompt.md:15340:+-    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $
-false $false 1 $_.Ex
-ception.Mess
-.ai-dev\review-prompt.md:15359:+-    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxTa
-sks" $false $true 0 
-"T004 초안은 ta
-.ai-dev\review-prompt.md:15375:+-    $script:steps += New-StepResult $stepNumber "run-codex" "power
-shell -ExecutionPoli
-cy Bypass -F
-.ai-dev\review-prompt.md:15409:+-    $script:steps += New-StepResult $stepNumber "run-review-codex"
- "powershell -Execut
-ionPolicy By
-.ai-dev\review-prompt.md:15424:+-$script:steps += New-StepResult ($stepNumber + 1) "commit-complete
--placeholder" "T005에
-서 ai-dev-com
-.ai-dev\review-prompt.md:15489:+-전환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-.ai-dev\review-prompt.md:15515:+-- `queue.json`
-.ai-dev\review-prompt.md:15516:+-- `state.json`
-.ai-dev\review-prompt.md:15517:+-- `test-result.md`
-.ai-dev\review-prompt.md:15527:+-- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예시
-다.
-.ai-dev\review-prompt.md:15528:+-- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예시
-다.
-.ai-dev\review-prompt.md:15530:+-예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 복
-사 원본으로 사용한다.
-.ai-dev\review-prompt.md:15561:+-`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-.ai-dev\review-prompt.md:15567:+-현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:15570:+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-ta
-sk.ps1 -Json
-.ai-dev\review-prompt.md:15577:+-`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.ai
--dev/current-task-pr
-ompt.md` 파일을
-.ai-dev\review-prompt.md:15605:+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-t
-ask.ps1 -ResultSumma
-ry "T001 분석 
-.ai-dev\review-prompt.md:15613:+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-t
-ask.ps1 -TaskId T001
- -ResultSumm
-.ai-dev\review-prompt.md:15615:++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-t
-ask.ps1 -TaskId T001
- -ResultSumm
-.ai-dev\review-prompt.md:15625:+-task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. 
-Codex 실행, GPT 리뷰, bu
-ild/test 실행,
-.ai-dev\review-prompt.md:15627:++task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다. 
-Codex 실행, GPT 리뷰, bu
-ild/test 실행,
-.ai-dev\review-prompt.md:15638:+-결과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm buil
-d/test/lint를 실행하지 않는
-다.
-.ai-dev\review-prompt.md:15641:++과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm build
-/test/lint를 실행하지 않는다
-.
-.ai-dev\review-prompt.md:15653:+-이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다.
- 실제 검증 통과/실패/미수행 판단은
- `test-resul
-.ai-dev\review-prompt.md:15655:++이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다.
- 실제 검증 통과/실패/미수행 판단은
- `test-resul
-.ai-dev\review-prompt.md:15673:+-git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰,
- build/test 실행, git 
-commit은 수행하지
-.ai-dev\review-prompt.md:15675:++git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰,
- build/test 실행, git 
-commit은 수행하지
-.ai-dev\review-prompt.md:15692:+ git diff/stat 명령의 stderr 경고는 diff 본문과 분리한다. git 명령이 실패하면 기존처럼 `sta
-te.json`에 실패 결과를 기록하
-고 종료한다.
-.ai-dev\review-prompt.md:15717:+ 7. 저장된 decision에 따라 `pass`, `revise`, `blocked` 흐름을 계속 진행한다.
-.ai-dev\review-prompt.md:15739:+ 자동화에서 결과만 읽고 싶으면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:15742:+ GPT Chat 또는 외부 리뷰어가 반환한 리뷰 JSON을 `review.md`에 저장하고, decision과 seve
-rity를 `state.json`에 
-반영한다.
-.ai-dev\review-prompt.md:15746:+--response.json
-.ai-dev\review-prompt.md:15749:++response.json
-.ai-dev\review-prompt.md:15752:+ JSON 문자열이나 클립보드 내용을 직접 사용할 수도 있다.
-.ai-dev\review-prompt.md:15755:+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-revie
-w.ps1 -ReviewJson '{
-"decision":"
-.ai-dev\review-prompt.md:15758:++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-revie
-w.ps1 -ReviewJson '{
-"decision":"
-.ai-dev\review-prompt.md:15766:+-ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboa
-rd`를 실행한다. 순수 JSON 객
-체, `json` 코드
-.ai-dev\review-prompt.md:15767:+-블록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-.ai-dev\review-prompt.md:15768:++ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipboa
-rd`를 실행한다. 순수 JSON 객
-체, `json` 코드
-.ai-dev\review-prompt.md:15770:++록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-.ai-dev\review-prompt.md:15774:+@@ -895,20 +543,20 @@ ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를
-.ai-dev\review-prompt.md:15775:+ - Markdown 코드블록을 포함해도 되지만, 가능하면 JSON만 복사한다.
-.ai-dev\review-prompt.md:15776:+ - 오류 메시지의 입력 preview 앞부분을 보고 다른 설명 문장이나 잘린 JSON이 섞였는지 확인한다.
-.ai-dev\review-prompt.md:15778:+-파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수
-정하지 않는다. review-prom
-pt나 명령어를 실수로
-.ai-dev\review-prompt.md:15780:++파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 수
-정하지 않는다. review-prom
-pt나 명령어를 실수로
-.ai-dev\review-prompt.md:15794:+ 잘못 실행했다면 ChatGPT가 출력한 JSON 리뷰만 다시 클립보드에 복사한 뒤 `-FromClipboard`를 재실
-행한다.
-.ai-dev\review-prompt.md:15862:+-- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `tes
-t-result.md`, `revie
-w.md` 등을 포함할
-.ai-dev\review-prompt.md:15864:++- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `tes
-t-result.md`, `revie
-w.md` 등을 포함할
-.ai-dev\review-prompt.md:15868:+ - `review-response.json`은 민감 정보나 장문 내용이 포함될 수 있으므로 내용을 확인한 뒤 커밋 여부
-를 결정한다.
-.ai-dev\review-prompt.md:15887:+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cyc
-le.ps1 -Json
-.ai-dev\review-prompt.md:15943:+-첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. Cha
-tGPT가 JSON 리뷰만 출력하면 
-그 JSON을 다시 클
-.ai-dev\review-prompt.md:15945:++첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. Cha
-tGPT가 JSON 리뷰만 출력하면 
-그 JSON을 다시 클
-.ai-dev\review-prompt.md:15969:+-시된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-.ai-dev\review-prompt.md:15972:++된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-.ai-dev\review-prompt.md:15979:+-첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 
-리뷰를 받은 뒤 두 번째 명령으로 클
-립보드의 리뷰 JSON
-.ai-dev\review-prompt.md:15981:++첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON 
-리뷰를 받은 뒤 두 번째 명령으로 클
-립보드의 리뷰 JSON
-.ai-dev\review-prompt.md:16017:+-`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고
-, JSON 출력에서는 `comple
-ted: true`, 
-.ai-dev\review-prompt.md:16018:+-`stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goa
-l.md`, `.ai-dev/queu
-e.json`, `.a
-.ai-dev\review-prompt.md:16019:+-i-dev/state.json`을 새 목표 기준으로 초기화한다.
-.ai-dev\review-prompt.md:16020:++`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하고
-, JSON 출력에서는 `comple
-ted: true`, 
-.ai-dev\review-prompt.md:16022:++stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goal
-.md`, `.ai-dev/queue
-.json`, `.ai
-.ai-dev\review-prompt.md:16024:++dev/state.json`을 새 목표 기준으로 초기화한다.
-.ai-dev\review-prompt.md:16026:+-`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하
-고 JSON 리뷰만 다시 복사한다. 
-완료된 goal이나 기
-.ai-dev\review-prompt.md:16028:++`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인하
-고 JSON 리뷰만 다시 복사한다. 
-완료된 goal이나 기
-.ai-dev\review-prompt.md:16033:+-st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-.ai-dev\review-prompt.md:16036:++t-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-.ai-dev\review-prompt.md:16040:+@@ -1209,14 +857,14 @@ st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검
-.ai-dev\review-prompt.md:16046:+-- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md
-`를 Codex CLI에 전달해 JS
-ON 리뷰를 생성하고 
-.ai-dev\review-prompt.md:16055:++- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.md
-`를 Codex CLI에 전달해 JS
-ON 리뷰를 생성하고 
-.ai-dev\review-prompt.md:16083:+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex.
-ps1 -DryRun -Json
-.ai-dev\review-prompt.md:16104:+-`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/
-review-response.json
-`에 저장한다. 실행 
-.ai-dev\review-prompt.md:16107:++`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev/
-review-response.json
-`에 저장한다. 실행 
-.ai-dev\review-prompt.md:16119:+-기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-res
-ult.md`에 저장한다. Codex
- 출력에서 첫 번째 유
-.ai-dev\review-prompt.md:16120:+-효 JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-.ai-dev\review-prompt.md:16121:++기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-res
-ult.md`에 저장한다. Codex
- 출력에서 첫 번째 유
-.ai-dev\review-prompt.md:16123:++ JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-.ai-dev\review-prompt.md:16138:+-리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-re
-sponse.json` 흐름까지 이어
-가려면 `-SaveRe
-.ai-dev\review-prompt.md:16140:++리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-re
-sponse.json` 흐름까지 이어
-가려면 `-SaveRe
-.ai-dev\review-prompt.md:16152:+ 자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
-.ai-dev\review-prompt.md:16157:+ - Codex 리뷰 실행 프롬프트에는 파일 수정 금지, git 명령 금지, build/test/lint 실행 금지, J
-SON 객체만 출력하라는 안전 규칙을
- 추가한다.
-.ai-dev\review-prompt.md:16158:+-- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`,
- `optional_suggestio
-ns`, `next_s
-.ai-dev\review-prompt.md:16160:++- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`,
- `optional_suggestio
-ns`, `next_s
-.ai-dev\review-prompt.md:16163:+ - JSON 추출에 실패하면 출력 preview를 보여주고 종료 코드 1로 종료한다.
-.ai-dev\review-prompt.md:16170:+-w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:16173:++ prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 커밋과 task 완료 처리
-를 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:16184:++7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRev
-iewDecision`이 `pass`
-여야 한다.
-.ai-dev\review-prompt.md:16185:++8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경이
- 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:16195:+ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle
--full.ps1 -DryRun -J
-son
-.ai-dev\review-prompt.md:16284:++리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `co
-mplete_task`가 아니면 자동
- 커밋과 complet
-.ai-dev\review-prompt.md:16286:++-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 않
-는다. `package.json` 또
-는 `package-l
-.ai-dev\review-prompt.md:16288:++ck.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음 
-등으로 실제 commit hash를 
-기록하지 못하면 `co
-.ai-dev\review-prompt.md:16305:++자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는다
-.
-.ai-dev\review-prompt.md:16306:++DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보기
-가 포함되므로 파일 작성 전에 생성될
- goal/task 계
-.ai-dev\review-prompt.md:16313:++추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -Dr
-yRun -Json
-.ai-dev\review-prompt.md:16350:+-3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
-.ai-dev\review-prompt.md:16353:++3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-.ai-dev\review-prompt.md:16354:++4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enum
- 값을 검증한다.
-.ai-dev\review-prompt.md:16355:++5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을 
-작성한다.
-.ai-dev\review-prompt.md:16358:++8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, `
-current-task-prompt.
-md`, 결과 파일 때
-.ai-dev\review-prompt.md:16369:+ - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:16370:+-- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:16372:++- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실행
-으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:16390:+-ipts/ai-dev-complete-task.ps1 -Pattern "param|ResultSummary|TaskId
-|loop-log|queue|stat
-e" -Context 
-.ai-dev\review-prompt.md:16394:+-ipts/ai-dev-save-review.ps1 -Pattern "lastReview|decision|next_ste
-p|state" -Context 0,
-3' in D:\ai-
-.ai-dev\review-prompt.md:16404:+-> scripts\ai-dev-complete-task.ps1:3:    [string]$ResultSummary,
-.ai-dev\review-prompt.md:16408:+-> scripts\ai-dev-complete-task.ps1:10:$queueRelativePath = ".ai-de
-v/queue.json"
-.ai-dev\review-prompt.md:16409:+-> scripts\ai-dev-complete-task.ps1:11:$stateRelativePath = ".ai-de
-v/state.json"
-.ai-dev\review-prompt.md:16444:+-> scripts\ai-dev-complete-task.ps1:89:$queue = Read-JsonFile $queu
-ePath $queueRelative
-Path
-.ai-dev\review-prompt.md:16445:+-> scripts\ai-dev-complete-task.ps1:90:$state = Read-JsonFile $stat
-ePath $stateRelative
-Path
-.ai-dev\review-prompt.md:16490:+-> scripts\ai-dev-complete-task.ps1:130:$summary = if (Test-HasValu
-e $ResultSummary) { 
-$ResultSumma
-.ai-dev\review-prompt.md:16496:+-> scripts\ai-dev-complete-task.ps1:135:if (Test-HasValue $ResultSu
-mmary) {
-.ai-dev\review-prompt.md:16497:+-> scripts\ai-dev-complete-task.ps1:136:    Set-ObjectProperty $com
-pletedTask "resultSu
-mmary" $Resu
-.ai-dev\review-prompt.md:16527:+-> scripts\ai-dev-complete-task.ps1:169:Write-JsonFile $queuePath $
-queue
-.ai-dev\review-prompt.md:16528:+-> scripts\ai-dev-complete-task.ps1:170:Write-JsonFile $statePath $
-state
-.ai-dev\review-prompt.md:16583:+-> scripts\ai-dev-commit.ps1:170:if (-not $AllowWithoutPassedReview
- -and $state.lastRev
-iewDecision 
-.ai-dev\review-prompt.md:16585:+-> scripts\ai-dev-commit.ps1:171:    Stop-WithError "state.lastRevi
-ewDecision이 pass가 아닙
-니다. 리뷰 결과를 확
-.ai-dev\review-prompt.md:16651:+-> scripts\ai-dev-commit.ps1:276:        $addOutput = & git add -- 
-@selectedFiles 2>&1 
-| Out-String
-.ai-dev\review-prompt.md:16654:+-  scripts\ai-dev-commit.ps1:279:        $addOutput = & git add -A 
-2>&1 | Out-String
-.ai-dev\review-prompt.md:16679:+-> scripts\ai-dev-save-review.ps1:11:$stateRelativePath = ".ai-dev/
-state.json"
-.ai-dev\review-prompt.md:16685:+-> scripts\ai-dev-save-review.ps1:70:function Save-StateReviewResul
-t {
-.ai-dev\review-prompt.md:16688:+-> scripts\ai-dev-save-review.ps1:73:        [string]$Decision,
-.ai-dev\review-prompt.md:16692:+-> scripts\ai-dev-save-review.ps1:79:    Set-ObjectProperty $State 
-"lastReviewDecision"
- $Decision
-.ai-dev\review-prompt.md:16701:+-> scripts\ai-dev-save-review.ps1:86:    $stateJson = $State | Conv
-ertTo-Json -Depth 20
-.ai-dev\review-prompt.md:16702:+-> scripts\ai-dev-save-review.ps1:87:    [System.IO.File]::WriteAll
-Text($statePath, $st
-ateJson, $ut
-.ai-dev\review-prompt.md:16709:+--review.ps1 -ReviewJson ''{"decision":"pass","severity":"none","su
-mmary":"OK","require
-d_changes":[
-.ai-dev\review-prompt.md:16717:+-> scripts\ai-dev-save-review.ps1:282:        "decision",
-.ai-dev\review-prompt.md:16727:+-> scripts\ai-dev-save-review.ps1:301:        $errors += "허용되지 않은 d
-ecision: $($Review.d
-ecision)"
-.ai-dev\review-prompt.md:16746:+-> scripts\ai-dev-save-review.ps1:373:- Decision: blocked
-.ai-dev\review-prompt.md:16749:+-  scripts\ai-dev-save-review.ps1:376:- Summary: 리뷰 JSON을 파싱하거나 검증하
-지 못했습니다.
-.ai-dev\review-prompt.md:16750:+-> scripts\ai-dev-save-review.ps1:390:    Save-StateReviewResult $S
-tate "blocked" "crit
-ical" "faile
-.ai-dev\review-prompt.md:16760:+-  scripts\ai-dev-save-review.ps1:402:        Stop-WithError "리뷰 JS
-ON 저장에 실패했습니다: $Erro
-rSummary"
-.ai-dev\review-prompt.md:16763:+-> scripts\ai-dev-save-review.ps1:405:    Write-Host "리뷰 입력 오류로 rev
-iew.md와 state.json을 
-수정하지 않았습니다."
-.ai-dev\review-prompt.md:16766:+-  scripts\ai-dev-save-review.ps1:407:    Stop-WithError "리뷰 JSON 저
-장에 실패했습니다: $ErrorSum
-mary"
-.ai-dev\review-prompt.md:16773:+-> scripts\ai-dev-save-review.ps1:414:$state = Read-JsonFile $state
-Path $stateRelativeP
-ath
-.ai-dev\review-prompt.md:16786:+-  scripts\ai-dev-save-review.ps1:436:$prettyJson = $review | Conve
-rtTo-Json -Depth 20
-.ai-dev\review-prompt.md:16787:+-> scripts\ai-dev-save-review.ps1:446:- Decision: $($review.decisio
-n)
-.ai-dev\review-prompt.md:16793:+-> scripts\ai-dev-save-review.ps1:467:Save-StateReviewResult $state
- $review.decision $r
-eview.severi
-.ai-dev\review-prompt.md:16796:+-> scripts\ai-dev-save-review.ps1:469:$nextAction = if ($review.dec
-ision -eq "pass" -an
-d $review.ne
-.ai-dev\review-prompt.md:16801:+-olicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary
- `"리뷰 통과`""
-.ai-dev\review-prompt.md:16802:+-> scripts\ai-dev-save-review.ps1:471:} elseif ($review.decision -e
-q "revise" -and $rev
-iew.next_ste
-.ai-dev\review-prompt.md:16808:+-> scripts\ai-dev-save-review.ps1:473:} elseif ($review.decision -e
-q "blocked" -or $rev
-iew.next_ste
-.ai-dev\review-prompt.md:16815:+-> scripts\ai-dev-save-review.ps1:476:    "decision과 next_step 조합을 
-확인한 뒤 다음 행동을 결정하세요."
-.ai-dev\review-prompt.md:16819:+-> scripts\ai-dev-save-review.ps1:480:Write-Host "Decision: $($revi
-ew.decision)"
-.ai-dev\review-prompt.md:16829:+-ev/queue.json' in D:\ai-apps\planpilot-local
-.ai-dev\review-prompt.md:16842:+-ev/state.json' in D:\ai-apps\planpilot-local
-.ai-dev\review-prompt.md:16873:+-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리
-뷰어로 사용하는 완전 자동화 정책, 
-허용/금지 명령, 안전
-.ai-dev\review-prompt.md:16899:+-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리
-뷰어로 사용하는 완전 자동화 정책, 
-허용/금지 명령, 안전
-.ai-dev\review-prompt.md:16905:+-                      "description":  "review-prompt.md를 Codex CLI
-에 전달해 JSON 리뷰를 생성하고 
-review-respo
-.ai-dev\review-prompt.md:16906:+-nse.json 및 save-review 흐름에 연결하는 ai-dev-run-review-codex.ps1을 추가한다.
-",
-.ai-dev\review-prompt.md:16919:+-                                           "Codex 출력에서 JSON 객체를 추출
-해 저장하는지 확인",
-.ai-dev\review-prompt.md:16925:+-                      "resultSummary":  "T003 완료: review-prompt.md
-를 codex exec에 전달해 JS
-ON 리뷰를 생성하는 
-.ai-dev\review-prompt.md:16926:+-ai-dev-run-review-codex.ps1 추가, 문법 검증과 DryRun/Json 확인"
-.ai-dev\review-prompt.md:16952:+-                      "resultSummary":  "T004 완료: make-prompt/run-
-codex/check/save-dif
-f/make-revie
-.ai-dev\review-prompt.md:16953:+-w-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가, 
-DryRun/Json/AllowCod
-ex 중단 확인"
-.ai-dev\review-prompt.md:16993:+-                                                  ".ai-dev/test-re
-sult.md"
-.ai-dev\review-prompt.md:16999:+-                                           "Codex 리뷰 JSON 저장 확인",
-.ai-dev\review-prompt.md:17023:+-                                           "Json 출력이 가능한지 확인",
-.ai-dev\review-prompt.md:17024:+-                                           "goal/queue/state JSON 
-파싱 검증을 수행하는지 확인",
-.ai-dev\review-prompt.md:17045:+-    "lastReviewDecision":  "not_started",
-.ai-dev\review-prompt.md:17057:++    $OutputEncoding = [System.Text.Encoding]::UTF8
-.ai-dev\review-prompt.md:17058:++    [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
-.ai-dev\review-prompt.md:17075:++    [switch]$Json,
-.ai-dev\review-prompt.md:17087:+-$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:17089:+ $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:17096:++$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:17097:++$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:17116:+-function Read-JsonFile {
-.ai-dev\review-prompt.md:17127:++function Read-JsonFile {
-.ai-dev\review-prompt.md:17134:+         return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path 
-| ConvertFrom-Json
-.ai-dev\review-prompt.md:17136:+-        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Except
-ion.Message)"
-.ai-dev\review-prompt.md:17137:++        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Messa
-ge)"
-.ai-dev\review-prompt.md:17187:++function New-StepResult {
-.ai-dev\review-prompt.md:17215:+-function Save-StateResult {
-.ai-dev\review-prompt.md:17216:++function New-CycleResult {
-.ai-dev\review-prompt.md:17236:++function Write-CycleResult {
-.ai-dev\review-prompt.md:17238:++        [object]$Result
-.ai-dev\review-prompt.md:17241:++    if ($Json) {
-.ai-dev\review-prompt.md:17242:++        $Result | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:17246:++    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:17255:++    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:17256:++    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:17257:++    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:17269:++    $result = New-CycleResult $Steps $StoppedReason $Completed $Ex
-itCode
-.ai-dev\review-prompt.md:17270:++    Write-CycleResult $result
-.ai-dev\review-prompt.md:17284:++        $script:steps += New-StepResult $StepNumber $Name $Command
- $false $true 0 "Dry
-Run: 하위 스크립트
-.ai-dev\review-prompt.md:17290:++    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPa
-th @Arguments 2>&1 |
- Out-String
-.ai-dev\review-prompt.md:17292:++    $message = $output.Trim()
-.ai-dev\review-prompt.md:17298:++    $script:steps += New-StepResult $StepNumber $Name $Command $tr
-ue $false $exitCode 
-$message
-.ai-dev\review-prompt.md:17322:+-    $stateJson = $State | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:17323:+-    [System.IO.File]::WriteAllText($statePath, $stateJson, $utf8Wi
-thBom)
-.ai-dev\review-prompt.md:17329:+     return $output.TrimEnd()
-.ai-dev\review-prompt.md:17339:++    $status = Invoke-GitCapture @("status", "--porcelain", "--", "
-package.json", "pack
-age-lock.jso
-.ai-dev\review-prompt.md:17341:++") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:17352:++    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:17355:+-$queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:17356:+-$state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:17358:++        $reviewResponse = Read-JsonFile $reviewResponsePath $revie
-wResponseRelativePat
-h
-.ai-dev\review-prompt.md:17372:++        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:17412:++    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:17437:+-if (-not $AllowWithoutPassedReview -and $state.lastReviewDecision 
--ne "pass") {
-.ai-dev\review-prompt.md:17438:+-    Stop-WithError "state.lastReviewDecision이 pass가 아닙니다. 리뷰 결과를 확
-인하거나 -AllowWithoutPa
-ssedReview를 
-.ai-dev\review-prompt.md:17453:++    $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:17454:++    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:17486:++    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" $
-false $false 1 $_.Ex
-ception.Mess
-.ai-dev\review-prompt.md:17504:++    "commit-result-gate",
-.ai-dev\review-prompt.md:17522:++        $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:17523:++        $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:17526:++        $script:steps += New-StepResult $stepNumber "load-task" "s
-tate/queue 확인" $fals
-e $false 1 $
-.ai-dev\review-prompt.md:17553:++    $script:steps += New-StepResult $stepNumber "task-start" "MaxT
-asks=$MaxTasks" $fal
-se $false 0 
-.ai-dev\review-prompt.md:17588:++        $script:steps += New-StepResult $stepNumber "run-codex" "p
-owershell -Execution
-Policy Bypas
-.ai-dev\review-prompt.md:17645:++        $script:steps += New-StepResult $stepNumber "run-review-co
-dex" "powershell -Ex
-ecutionPolic
-.ai-dev\review-prompt.md:17663:++        $script:steps += New-StepResult $stepNumber "review-gate" 
-"state.lastReviewDec
-ision 확인" $f
-.ai-dev\review-prompt.md:17667:++        $script:steps += New-StepResult $stepNumber "package-chang
-e-gate" "git status 
---porcelain 
-.ai-dev\review-prompt.md:17669:++- package.json package-lock.json" $false $true 0 "DryRun: package 
-파일 변경 여부를 확인하지 않았습니다
-."
-.ai-dev\review-prompt.md:17671:++        $script:steps += New-StepResult $stepNumber "commit" "powe
-rshell -ExecutionPol
-icy Bypass -
-.ai-dev\review-prompt.md:17675:++        $script:steps += New-StepResult $stepNumber "commit-result
--gate" "state.lastCo
-mmand/lastCo
-.ai-dev\review-prompt.md:17679:++        $script:steps += New-StepResult $stepNumber "complete-task
-" "powershell -Execu
-tionPolicy B
-.ai-dev\review-prompt.md:17681:++pass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"자동 완료
-: Codex 구현, build/ch
-eck, Codex 리
-.ai-dev\review-prompt.md:17693:++        $script:steps += New-StepResult $stepNumber "review-gate" 
-"state/review-respon
-se 확인" $fals
-.ai-dev\review-prompt.md:17703:++        $script:steps += New-StepResult $stepNumber "review-gate" 
-"state.lastCommandSt
-atus 확인" $fa
-.ai-dev\review-prompt.md:17711:++    if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:17712:++        $script:steps += New-StepResult $stepNumber "review-gate" 
-"state.lastReviewDec
-ision 확인" $f
-.ai-dev\review-prompt.md:17714:++lse $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습
-니다: $($reviewGate.de
-cision)"
-.ai-dev\review-prompt.md:17728:++        $script:steps += New-StepResult $stepNumber "review-gate" 
-"$reviewResponseRela
-tivePath nex
-.ai-dev\review-prompt.md:17736:++    $script:steps += New-StepResult $stepNumber "review-gate" "sta
-te.lastReviewDecisio
-n 확인" $false
-.ai-dev\review-prompt.md:17743:++            $script:steps += New-StepResult $stepNumber "package-c
-hange-gate" "git sta
-tus --porcel
-.ai-dev\review-prompt.md:17745:++in -- package.json package-lock.json" $false $true 1 "package.json
- 또는 package-lock.jso
-n 변경이 감지되어 자
-.ai-dev\review-prompt.md:17751:++        $script:steps += New-StepResult $stepNumber "package-chang
-e-gate" "git status 
---porcelain 
-.ai-dev\review-prompt.md:17753:++- package.json package-lock.json" $false $false 1 $_.Exception.Mes
-sage
-.ai-dev\review-prompt.md:17757:++    $script:steps += New-StepResult $stepNumber "package-change-ga
-te" "git status --po
-rcelain -- p
-.ai-dev\review-prompt.md:17759:++ckage.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커
-밋 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:17774:++        $script:steps += New-StepResult $stepNumber "commit" "powe
-rshell -ExecutionPol
-icy Bypass -
-.ai-dev\review-prompt.md:17794:++        $script:steps += New-StepResult $stepNumber "commit-result
--gate" "state.lastCo
-mmand/lastCo
-.ai-dev\review-prompt.md:17797:++        Stop-Cycle $script:steps "commit_result_gate_failed" $fals
-e 1
-.ai-dev\review-prompt.md:17808:++        $script:steps += New-StepResult $stepNumber "commit-result
--gate" "state.lastCo
-mmand/lastCo
-.ai-dev\review-prompt.md:17814:++    $script:steps += New-StepResult $stepNumber "commit-result-gat
-e" "state.lastComman
-d/lastCommit
-.ai-dev\review-prompt.md:17819:++    $resultSummary = "자동 완료: Codex 구현, build/check, Codex 리뷰 pass,
- 자동 커밋 완료"
-.ai-dev\review-prompt.md:17822:++ts/ai-dev-complete-task.ps1 -ResultSummary `"$resultSummary`"" $sc
-riptPaths.completeTa
-sk @("-Resul
-.ai-dev\review-prompt.md:17824:++Summary", $resultSummary)
-.ai-dev\review-prompt.md:17846:++    [switch]$Json,
-.ai-dev\review-prompt.md:17853:++    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:17862:++$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:17863:++$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:17900:+-        $addOutput = & git add -- @selectedFiles 2>&1 | Out-String
-.ai-dev\review-prompt.md:17903:+-        $addOutput = & git add -A 2>&1 | Out-String
-.ai-dev\review-prompt.md:17913:+-        throw "$addDisplayName 실행에 실패했습니다. exit code: $LASTEXITCOD
-E`n$addOutput"
-.ai-dev\review-prompt.md:17921:++function New-StepResult {
-.ai-dev\review-prompt.md:17943:++function New-AutoGoalResult {
-.ai-dev\review-prompt.md:17961:++        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-.ai-dev\review-prompt.md:17965:++function Write-AutoGoalResult {
-.ai-dev\review-prompt.md:17966:++    param([object]$Result)
-.ai-dev\review-prompt.md:17968:++    if ($Json) {
-.ai-dev\review-prompt.md:17969:++        $Result | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:17980:++    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:17991:++    if ($null -ne $Result.plan) {
-.ai-dev\review-prompt.md:17993:++        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)"
-.ai-dev\review-prompt.md:17994:++        Write-Host "  Current task id: $($Result.plan.queue.curren
-tTaskId)"
-.ai-dev\review-prompt.md:17996:++        foreach ($task in @($Result.plan.queue.tasks)) {
-.ai-dev\review-prompt.md:18006:+-    $commitOutput = & git commit -m $commitMessage 2>&1 | Out-Stri
-ng
-.ai-dev\review-prompt.md:18007:++    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:18008:++    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:18009:++    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:18020:++    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReason
- $Completed $ExitCod
-e)
-.ai-dev\review-prompt.md:18045:++    $statusOutput = & git status --short 2>&1
-.ai-dev\review-prompt.md:18048:+-        throw "git commit 실행에 실패했습니다. exit code: $LASTEXITCODE`n$c
-ommitOutput"
-.ai-dev\review-prompt.md:18049:++        $script:steps += New-StepResult 2 "dirty-worktree-gate" "g
-it status --short" $
-false $false
-.ai-dev\review-prompt.md:18051:++1 "git status failed: $($statusOutput -join "`n")"
-.ai-dev\review-prompt.md:18059:+-    Save-StateResult $state "failed" $errorSummary $null
-.ai-dev\review-prompt.md:18061:++    return @($statusOutput | Where-Object { -not [string]::IsNullO
-rWhiteSpace($_) })
-.ai-dev\review-prompt.md:18064:+-Save-StateResult $state "passed" "" $commitHash
-.ai-dev\review-prompt.md:18065:++function Get-JsonObjectCandidates {
-.ai-dev\review-prompt.md:18121:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:18122:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:18123:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:18131:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:18132:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:18150:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:18154:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:18157:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:18161:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:18162:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:18167:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:18171:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:18180:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:18212:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:18229:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:18231:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:18247:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:18256:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:18261:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:18264:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:18265:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:18266:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:18271:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:18277:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:18283:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:18284:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:18289:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:18290:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:18294:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:18295:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:18309:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:18324:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:18344:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:18345:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:18346:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:18354:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:18355:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:18373:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:18377:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:18380:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:18384:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:18385:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:18390:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:18394:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:18403:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:18435:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:18452:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:18454:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:18470:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:18479:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:18484:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:18487:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:18488:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:18489:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:18494:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:18500:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:18506:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:18507:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:18512:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:18513:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:18517:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:18518:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:18532:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:18547:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:18622:+-> scripts\ai-dev-auto-cycle-full.ps1:381:    $script:steps += New-
-StepResult $stepNumb
-er "review-g
-.ai-dev\review-prompt.md:18630:+-> scripts\ai-dev-auto-cycle-full.ps1:386:    $script:steps += New-
-StepResult $stepNumb
-er "review-g
-.ai-dev\review-prompt.md:18638:+-  scripts\ai-dev-auto-cycle-full.ps1:390:if ($reviewGate.decision 
--ne "pass") {
-.ai-dev\review-prompt.md:18639:+-> scripts\ai-dev-auto-cycle-full.ps1:391:    $script:steps += New-
-StepResult $stepNumb
-er "review-g
-.ai-dev\review-prompt.md:18641:+-wDecision 확인" $false $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 compl
-ete-task를 실행하지 않습니다:
- $($reviewGa
-.ai-dev\review-prompt.md:18642:+-te.decision)"
-.ai-dev\review-prompt.md:18649:+-> scripts\ai-dev-auto-cycle-full.ps1:396:    $script:steps += New-
-StepResult $stepNumb
-er "review-g
-.ai-dev\review-prompt.md:18658:+-> scripts\ai-dev-auto-cycle-full.ps1:400:$script:steps += New-Step
-Result $stepNumber "
-review-gate"
-.ai-dev\review-prompt.md:18665:+-> scripts\ai-dev-auto-cycle-full.ps1:405:        $script:steps += 
-New-StepResult $step
-Number "pack
-.ai-dev\review-prompt.md:18667:+- status --porcelain -- package.json package-lock.json" $false $tru
-e 1 "package.json 또는
- package-loc
-.ai-dev\review-prompt.md:18668:+-k.json 변경이 감지되어 자동 커밋
-.ai-dev\review-prompt.md:18674:+-> scripts\ai-dev-auto-cycle-full.ps1:409:    $script:steps += New-
-StepResult $stepNumb
-er "package-
-.ai-dev\review-prompt.md:18676:+-tus --porcelain -- package.json package-lock.json" $false $false 1
- $_.Exception.Messag
-e
-.ai-dev\review-prompt.md:18681:+-> scripts\ai-dev-auto-cycle-full.ps1:413:$script:steps += New-Step
-Result $stepNumber "
-package-chan
-.ai-dev\review-prompt.md:18683:+---porcelain -- package.json package-lock.json" $false $false 0 "pa
-ckage 파일 변경 없음. 커밋 허
-용 여부를 확인합니다.
-.ai-dev\review-prompt.md:18702:+-> scripts\ai-dev-auto-cycle-full.ps1:426:    $script:steps += New-
-StepResult $stepNumb
-er "commit" 
-.ai-dev\review-prompt.md:18713:+-ypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 p
-ass 후 자동 커밋 완료`"" $s
-criptPaths.c
-.ai-dev\review-prompt.md:18736:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:18737:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:18738:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:18746:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:18747:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:18765:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:18769:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:18772:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:18776:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:18777:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:18782:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:18786:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:18795:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:18827:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:18844:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:18846:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:18862:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:18871:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:18876:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:18879:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:18880:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:18881:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:18886:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:18892:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:18898:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:18899:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:18904:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:18905:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:18909:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:18910:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:18924:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:18939:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:18968:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:18969:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:18970:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:18978:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:18979:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:18997:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:19001:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:19004:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:19008:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:19009:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:19014:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:19018:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:19027:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:19059:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:19078:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:19080:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:19118:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:19121:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:19122:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:19124:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:19127:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:19128:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:19133:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:19139:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:19145:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:19150:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:19151:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:19152:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:19157:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:19163:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:19169:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:19170:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:19175:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:19176:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:19180:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:19181:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:19195:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:19210:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:19230:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:19231:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:19232:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:19240:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:19241:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:19259:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:19263:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:19266:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:19270:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:19271:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:19276:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:19280:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:19289:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:19321:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:19340:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:19342:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:19380:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:19383:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:19384:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:19386:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:19389:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:19390:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:19395:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:19401:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:19407:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:19412:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:19413:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:19414:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:19419:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:19425:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:19431:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:19432:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:19437:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:19438:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:19442:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:19443:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:19457:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:19472:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:19492:+--ResultSummary "T001 
-.ai-dev\review-prompt.md:19500:+--TaskId T001 -ResultS
-.ai-dev\review-prompt.md:19511:+-  .ai-dev\README.md:130:task 완료 처리 스크립트는 `queue.json`, `state.json
-`, `loop-log.md`만 갱신
-한다. Codex 실행
-.ai-dev\review-prompt.md:19517:+-> .ai-dev\README.md:134:`package.json`에 정의된 build, test, lint scri
-pt를 확인하고 실행 결과를 `tes
-t-result.md`
-.ai-dev\review-prompt.md:19518:+-와 `state.json`에 기록한다.
-.ai-dev\review-prompt.md:19523:+-  .ai-dev\README.md:418:리뷰 저장 후 decision별 다음 단계:
-.ai-dev\review-prompt.md:19537:+- CLI에 전달해 JSON 리뷰를 생성
-.ai-dev\review-prompt.md:19553:+-> .ai-dev\README.md:579:- build/check 실패, package 파일 변경, 리뷰 decisi
-on이 `pass`가 아닌 경우 자동
- 커밋하지 않는다.
-.ai-dev\review-prompt.md:19566:+-  .ai-dev\README.md:629:`.ai-dev/review-prompt.md`를 Codex CLI에 전달해
- JSON 리뷰를 생성하고 `.ai-
-dev/review-r
-.ai-dev\review-prompt.md:19567:+-esponse.json`에 저장한다. 
-.ai-dev\review-prompt.md:19576:+-view prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:19616:+-  .ai-dev\README.md:743:- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행
-으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:19643:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:19644:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:19645:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:19653:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:19654:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:19672:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:19676:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:19679:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:19683:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:19684:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:19689:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:19693:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:19702:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:19734:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:19753:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:19755:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:19793:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:19796:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:19797:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:19799:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:19802:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:19803:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:19808:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:19814:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:19820:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:19825:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:19826:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:19827:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:19832:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:19838:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:19844:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:19845:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:19850:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:19851:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:19855:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:19856:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:19870:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:19885:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:19905:+-w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:19937:+-자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다.
-.ai-dev\review-prompt.md:19942:+-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycle
--full.ps1 -DryRun -J
-son
-.ai-dev\review-prompt.md:20041:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:20042:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:20043:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:20051:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:20052:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:20070:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:20074:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:20077:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:20081:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:20082:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:20087:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:20091:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:20100:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:20132:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:20151:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:20153:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:20191:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:20194:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:20195:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:20197:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:20200:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:20201:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:20206:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:20212:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:20218:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:20223:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:20224:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:20225:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:20230:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:20236:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:20242:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:20243:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:20248:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:20249:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:20253:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:20254:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:20268:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:20283:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:20304:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:20306:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:20317:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:20318:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:20325:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:20360:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:20361:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:20362:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:20382:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:20383:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:20384:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:20392:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:20393:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:20411:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:20415:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:20418:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:20422:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:20423:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:20428:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:20432:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:20441:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:20473:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:20492:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:20494:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:20532:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:20535:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:20536:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:20538:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:20541:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:20542:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:20547:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:20553:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:20559:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:20564:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:20565:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:20566:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:20571:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:20577:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:20583:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:20584:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:20589:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:20590:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:20594:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:20595:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:20609:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:20624:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:20637:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:20639:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:20650:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:20651:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:20658:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:20693:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:20694:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:20695:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:20715:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:20716:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:20717:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:20725:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:20726:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:20744:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:20748:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:20751:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:20755:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:20756:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:20761:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:20765:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:20774:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:20806:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:20825:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:20827:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:20865:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:20868:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:20869:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:20871:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:20874:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:20875:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:20880:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:20886:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:20892:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:20897:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:20898:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:20899:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:20904:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:20910:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:20916:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:20917:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:20922:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:20923:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:20927:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:20928:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:20942:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:20957:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:21019:++ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummar
-y", "lastReviewDecis
-ion", "lastR
-.ai-dev\review-prompt.md:21043:++    if (@("not_started", "pass", "revise", "blocked") -notcontains
- $State.lastReviewDe
-cision) {
-.ai-dev\review-prompt.md:21044:++        $errors += "invalid state.lastReviewDecision: $($State.las
-tReviewDecision)"
-.ai-dev\review-prompt.md:21109:++- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:21146:++            lastReviewDecision = "not_started"
-.ai-dev\review-prompt.md:21156:++function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:21159:++    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-.ai-dev\review-prompt.md:21161:++            $parsed = $candidate | ConvertFrom-Json
-.ai-dev\review-prompt.md:21173:++                JsonText = ($parsed | ConvertTo-Json -Depth 30)
-.ai-dev\review-prompt.md:21179:++    throw "No valid auto-goal JSON object was found in Codex outpu
-t."
-.ai-dev\review-prompt.md:21191:++Return exactly one JSON object and no markdown fences or commentar
-y.
-.ai-dev\review-prompt.md:21201:++- queue: an object for .ai-dev/queue.json.
-.ai-dev\review-prompt.md:21202:++- state: an object for .ai-dev/state.json.
-.ai-dev\review-prompt.md:21227:++- lastReviewDecision must be "not_started".
-.ai-dev\review-prompt.md:21252:++        $script:steps += New-StepResult $StepNumber $Name $Command
- $false $true 0 "Dry
-Run: child s
-.ai-dev\review-prompt.md:21258:++    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPa
-th @Arguments 2>&1 |
- Out-String
-.ai-dev\review-prompt.md:21260:++    $message = $output.Trim()
-.ai-dev\review-prompt.md:21266:++    $script:steps += New-StepResult $StepNumber $Name $Command $tr
-ue $false $exitCode 
-$message
-.ai-dev\review-prompt.md:21288:++    # auto-goal writes goal/queue/state/current prompt/result file
-s after the initial 
-dirty gate.
-.ai-dev\review-prompt.md:21313:++$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-RepoP
-ath $ResultPath))
-.ai-dev\review-prompt.md:21316:++    $script:steps += New-StepResult 0 "validate-input" "check Goal
-Title" $false $false
- 1 "GoalTitl
-.ai-dev\review-prompt.md:21323:++    $script:steps += New-StepResult 0 "validate-input" "check Goal
-Description" $false 
-$false 1 "Go
-.ai-dev\review-prompt.md:21330:++    $script:steps += New-StepResult 0 "validate-input" "check MaxT
-asks" $false $false 
-1 "MaxTasks 
-.ai-dev\review-prompt.md:21337:++    $script:steps += New-StepResult 0 "validate-input" "check MaxS
-teps" $false $false 
-1 "MaxSteps 
-.ai-dev\review-prompt.md:21348:++        $script:steps += New-StepResult 0 "prepare" "check require
-d scripts" $false $f
-alse 1 "Miss
-.ai-dev\review-prompt.md:21361:++$script:steps += New-StepResult 1 "validate-input" "check GoalTitl
-e/GoalDescription" $
-false $false
-.ai-dev\review-prompt.md:21370:++        $script:steps += New-StepResult 2 "preview-plan" "local dr
-y-run plan preview" 
-$false $fals
-.ai-dev\review-prompt.md:21376:++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git s
-tatus --short" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:21379:++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <aut
-o-goal planning prom
-pt>" $false 
-.ai-dev\review-prompt.md:21384:++    $script:steps += New-StepResult 4 "validate-generated-json" "g
-oal/queue/state JSON
- validation"
-.ai-dev\review-prompt.md:21387:++    $script:steps += New-StepResult 5 "write-state-files" "$goalRe
-lativePath, $queueRe
-lativePath, 
-.ai-dev\review-prompt.md:21390:++    $script:steps += New-StepResult 6 "make-prompt" "powershell -E
-xecutionPolicy Bypas
-s -File scri
-.ai-dev\review-prompt.md:21395:++        $script:steps += New-StepResult 7 "auto-cycle-full" $fullC
-ycleCommandText $fal
-se $true 0 "
-.ai-dev\review-prompt.md:21401:++        $script:steps += New-StepResult 7 "auto-cycle-full" $fullC
-ycleCommandText $fal
-se $true 0 "
-.ai-dev\review-prompt.md:21415:++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git s
-tatus --short" $fals
-e $true 1 "W
-.ai-dev\review-prompt.md:21422:++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git s
-tatus --short" $fals
-e $false 0 "
-.ai-dev\review-prompt.md:21426:++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git s
-tatus --short" $fals
-e $false 0 "
-.ai-dev\review-prompt.md:21434:++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <aut
-o-goal planning prom
-pt>" $false 
-.ai-dev\review-prompt.md:21442:++$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-.ai-dev\review-prompt.md:21446:++$resultContent = @"
-.ai-dev\review-prompt.md:21447:++# Codex Auto Goal Result
-.ai-dev\review-prompt.md:21457:++## Output
-.ai-dev\review-prompt.md:21460:++$codexOutput
-.ai-dev\review-prompt.md:21464:++[System.IO.File]::WriteAllText($resolvedResultPath, $resultContent
-, $utf8WithBom)
-.ai-dev\review-prompt.md:21467:++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <aut
-o-goal planning prom
-pt>" $true $
-.ai-dev\review-prompt.md:21469:++alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelati
-vePath $ResultPath)"
-.ai-dev\review-prompt.md:21473:++$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-go
-al planning prompt>"
- $true $fals
-.ai-dev\review-prompt.md:21475:++ 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelativ
-ePath $ResultPath)"
-.ai-dev\review-prompt.md:21478:++    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-.ai-dev\review-prompt.md:21480:++    $preview = Get-InputPreview $codexOutput
-.ai-dev\review-prompt.md:21481:++    $script:steps += New-StepResult 4 "validate-generated-json" "g
-oal/queue/state JSON
- validation"
-.ai-dev\review-prompt.md:21483:++$false $false 1 "Codex plan JSON extraction failed: $($_.Exception
-.Message) Output pre
-view: $previ
-.ai-dev\review-prompt.md:21486:++    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
-.ai-dev\review-prompt.md:21489:++$script:steps += New-StepResult 4 "validate-generated-json" "goal/
-queue/state JSON val
-idation" $fa
-.ai-dev\review-prompt.md:21491:++se $false 0 "goalMarkdown, queue, and state JSON validation comple
-ted. currentTaskId: 
-$($autoGoalP
-.ai-dev\review-prompt.md:21497:++$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:21498:++$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:21501:++[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $ut
-f8WithBom)
-.ai-dev\review-prompt.md:21502:++[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $ut
-f8WithBom)
-.ai-dev\review-prompt.md:21504:++$script:steps += New-StepResult 5 "write-state-files" "$goalRelati
-vePath, $queueRelati
-vePath, $sta
-.ai-dev\review-prompt.md:21513:++    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycle
-CommandText $false $
-true 0 "Full
-.ai-dev\review-prompt.md:21541:++ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\queue.example.json'" in D
-:\ai-apps\planpilot-
-local
-.ai-dev\review-prompt.md:21545:++ 'D:\\ai-apps\\planpilot-local\\.ai-dev\\state.example.json'" in D
-:\ai-apps\planpilot-
-local
-.ai-dev\review-prompt.md:21554:++  "goalTitle": "JSON 諛깆뾽 import 寃利?諛?誘몃━蹂닿린 湲곕뒫 異붽?",
-.ai-dev\review-prompt.md:21563:++      "description": "JSON 諛깆뾽 援ъ“, import ?뺤콉, 湲곗〈 寃利??좏떥, Setti
-ngsView ?곌껐 吏?먯쓣 ?뺤
-씤?섍퀬 援ы쁽 踰붿쐞
-.ai-dev\review-prompt.md:21579:++      "title": "諛깆뾽 JSON 寃利??좏떥 異붽?",
-.ai-dev\review-prompt.md:21594:++      "commitMessage": "Add JSON import validation utility"
-.ai-dev\review-prompt.md:21599:++      "description": "?ъ슜?먭? JSON 諛깆뾽 ?뚯씪???좏깮?섍퀬 寃利?寃곌낵 ?붿빟???뺤씤
-?????덈뒗 誘몃━蹂닿린 UI瑜?異
-붽??쒕떎.",
-.ai-dev\review-prompt.md:21614:++      "commitMessage": "Add JSON import preview"
-.ai-dev\review-prompt.md:21636:++      "commitMessage": "Show JSON import validation feedback"
-.ai-dev\review-prompt.md:21641:++      "description": "JSON import 寃利앷낵 誘몃━蹂닿린 湲곕뒫???섎룞 ?뺤씤 ??ぉ??臾
-몄꽌??諛섏쁺?쒕떎.",
-.ai-dev\review-prompt.md:21657:++      "commitMessage": "Document JSON import preview checks"
-.ai-dev\review-prompt.md:21690:++  "lastReviewDecision": "not_started",
-.ai-dev\review-prompt.md:21715:++  .ai-dev\README.md:760:자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSO
-N 출력은 사람이 읽는 텍스트와 섞지
- 않는다.
-.ai-dev\review-prompt.md:21716:++  .ai-dev\README.md:761:DryRun JSON에는 `plan.goalMarkdown`, `plan.q
-ueue`, `plan.state` 
-미리보기가 포함되므로 
-.ai-dev\review-prompt.md:21725:++라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다."
- -DryRun -Json
-.ai-dev\review-prompt.md:21765:++  .ai-dev\README.md:783:3. Codex 출력에서 `goalMarkdown`, `queue`, `st
-ate` JSON을 추출한다.
-.ai-dev\review-prompt.md:21766:++  .ai-dev\README.md:784:4. `queue.json`과 `state.json`의 필수 필드, curr
-entTaskId, task 배열, 
-enum 값을 검증한다
-.ai-dev\review-prompt.md:21768:++  .ai-dev\README.md:785:5. `.ai-dev/goal.md`, `.ai-dev/queue.json`
-, `.ai-dev/state.jso
-n`을 작성한다.
-.ai-dev\review-prompt.md:21771:++  .ai-dev\README.md:788:8. auto-goal이 생성한 `.ai-dev/goal.md`, `queu
-e.json`, `state.json
-`, `current-
-.ai-dev\review-prompt.md:21783:++  .ai-dev\README.md:794:- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:21784:++  .ai-dev\README.md:795:- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실
-패하면 파일 작성과 full cycl
-e 실행으로 넘어가지 
-.ai-dev\review-prompt.md:21821:+-licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxT
-asks 1 -MaxSteps 20 
--Json' in D:
-.ai-dev\review-prompt.md:21837:++문법 파서는 통과했습니다. 이제 DryRun JSON을 실행해 실제 출력 구조와 오류 여부를 확인하겠습니다. 이 모드는
- 파일을 쓰지 않고 하위 스크립트도 
-실행하지 않도록 설계되
-.ai-dev\review-prompt.md:21845:++ 하이라이트 추가' -GoalDescription '검색어와 일치하는 업무 제목과 메모를 화면에서 강조 표시한다.' -
-DryRun -Json" in D:\
-ai-apps\plan
-.ai-dev\review-prompt.md:21898:++                      "name":  "validate-generated-json",
-.ai-dev\review-prompt.md:21899:++                      "command":  "goal/queue/state JSON validatio
-n",
-.ai-dev\review-prompt.md:21914:++                      "command":  ".ai-dev/goal.md, .ai-dev/queue.
-json, .ai-dev/state.
-json",
-.ai-dev\review-prompt.md:21939:+-                      "command":  "state.lastReviewDecision 확인",
-.ai-dev\review-prompt.md:21948:+-                      "command":  "git status --porcelain -- packa
-ge.json package-lock
-.json",
-.ai-dev\review-prompt.md:21968:+-e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-.ai-dev\review-prompt.md:21997:++쇅 踰붿쐞\n\n- ?⑦궎吏 蹂寃?- DB ??젣 ?먮뒗 珥덇린??- 愿묐쾾?꾪븳 由ы뙥?곕쭅\n\n## ?섎룞 ?
-뺤씤\n\n- DryRun 怨꾪쉷怨?
-JSON 異쒕젰??寃
-.ai-dev\review-prompt.md:22043:++                               "lastReviewDecision":  "not_started
-",
-.ai-dev\review-prompt.md:22052:++    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:22053:++    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:22055:++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:22067:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:22069:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:22080:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:22081:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:22088:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:22136:++- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:22319:++457: - DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:22356:++494:             lastReviewDecision = "not_started"
-.ai-dev\review-prompt.md:22366:++504: function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:22369:++507:     foreach ($candidate in Get-JsonObjectCandidates $RawInput
-) {
-.ai-dev\review-prompt.md:22371:++509:             $parsed = $candidate | ConvertFrom-Json
-.ai-dev\review-prompt.md:22409:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:22410:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:22411:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:22432:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:22433:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:22434:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:22442:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:22443:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:22470:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:22474:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:22477:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:22481:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:22482:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:22487:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:22491:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:22500:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:22566:++- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:22578:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:22597:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:22599:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:22637:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:22640:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:22641:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:22643:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:22646:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:22647:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:22652:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:22658:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:22664:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:22669:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:22670:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:22671:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:22676:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:22682:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:22688:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:22689:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:22694:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:22695:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:22699:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:22700:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:22716:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:22731:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:22742:++깨진 문자열 자체가 패치 매칭을 계속 방해합니다. 함수 반환 직전에 DryRun 미리보기 값을 정상 문자열로 덮어쓰는 
-방식으로 좁게 수정해서, 실제 출력과
- JSON에는 깨진 문
-.ai-dev\review-prompt.md:22757:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:22759:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:22770:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:22771:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:22778:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:22813:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:22814:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:22815:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:22835:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:22836:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:22837:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:22845:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:22846:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:22864:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:22868:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:22871:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:22875:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:22876:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:22881:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:22885:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:22912:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:22940:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:22964:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:22975:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:22984:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:22986:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:23021:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:23024:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:23025:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:23027:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:23030:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:23033:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:23034:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:23042:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:23048:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:23053:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:23054:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:23055:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:23060:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:23066:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:23072:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:23073:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:23078:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:23079:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:23083:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:23084:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:23101:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:23125:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:23127:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:23130:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:23138:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:23143:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:23148:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:23161:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:23163:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:23174:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:23175:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:23182:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:23200:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:23226:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:23227:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:23228:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:23248:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:23249:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:23250:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:23258:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:23259:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:23271:++ function New-AutoGoalResult {
-.ai-dev\review-prompt.md:23285:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:23310:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:23315:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:23320:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:23321:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:23327:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:23332:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:23345:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:23375:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:23376:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:23404:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:23415:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:23424:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:23426:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:23455:++ function New-AutoGoalResult {
-.ai-dev\review-prompt.md:23466:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:23469:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:23470:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:23472:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:23475:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:23478:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:23479:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:23487:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:23493:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:23498:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:23499:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:23500:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:23505:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:23511:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:23517:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:23518:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:23523:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:23524:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:23528:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:23529:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:23580:++  scripts\ai-dev-auto-goal.ps1:129:function New-AutoGoalResult {
-.ai-dev\review-prompt.md:23618:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:23641:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:23643:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:23667:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:23681:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:23689:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:23694:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:23699:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:23702:++ function New-AutoGoalResult {
-.ai-dev\review-prompt.md:23717:++수정 위치가 `New-StepResult`에 잘못 들어갔습니다. 바로 되돌려서 `New-StepResult`를 원래 결
-과 객체 반환으로 복구한 뒤, Dry
-Run 함수에는 더 구
-.ai-dev\review-prompt.md:23745:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:23747:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:23772:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:23773:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:23800:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:23859:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:23860:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:23861:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:23886:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:23887:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:23888:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:23909:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:23910:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:23941:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:23949:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:23954:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:23959:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:23960:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:23966:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:23971:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:23984:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:24014:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:24015:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:24043:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:24054:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:24063:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:24065:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:24094:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:24110:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:24113:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:24114:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:24116:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:24119:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:24122:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:24123:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:24131:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:24137:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:24142:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:24143:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:24144:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:24149:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:24155:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:24161:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:24162:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:24167:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:24168:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:24172:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:24173:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:24189:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:24209:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:24211:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:24214:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:24222:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:24227:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:24232:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:24245:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:24247:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:24258:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:24259:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:24267:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:24302:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:24303:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:24304:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:24326:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:24327:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:24328:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:24336:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:24337:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:24363:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:24371:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:24376:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:24381:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:24382:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:24388:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:24393:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:24406:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:24436:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:24437:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:24465:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:24476:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:24486:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:24488:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:24491:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:24591:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:24594:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:24595:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:24597:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:24600:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:24603:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:24604:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:24612:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:24618:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:24623:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:24624:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:24625:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:24630:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:24636:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:24642:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:24643:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:24648:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:24649:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:24653:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:24654:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:24674:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:24696:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:24698:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:24720:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:24734:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:24742:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:24747:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:24752:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:24755:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:24767:+-licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxT
-asks 1 -MaxSteps 20 
--Json' in D:
-.ai-dev\review-prompt.md:24777:++ 하이라이트 추가' -GoalDescription '검색어와 일치하는 업무 제목과 메모를 화면에서 강조 표시한다.' -
-DryRun -Json" in D:\
-ai-apps\plan
-.ai-dev\review-prompt.md:24830:++                      "name":  "validate-generated-json",
-.ai-dev\review-prompt.md:24831:++                      "command":  "goal/queue/state JSON validatio
-n",
-.ai-dev\review-prompt.md:24846:++                      "command":  ".ai-dev/goal.md, .ai-dev/queue.
-json, .ai-dev/state.
-json",
-.ai-dev\review-prompt.md:24871:+-                      "command":  "state.lastReviewDecision 확인",
-.ai-dev\review-prompt.md:24880:+-                      "command":  "git status --porcelain -- packa
-ge.json package-lock
-.json",
-.ai-dev\review-prompt.md:24898:+-                      "name":  "commit-result-gate",
-.ai-dev\review-prompt.md:24909:+-e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-.ai-dev\review-prompt.md:24940:++n\n\n- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??\n- ?꾩옱 ?묒뾽 ?꾨＼?꾪듃 ?앹꽦 ?щ?瑜??뺤
-씤?쒕떎.\n- ?꾩슂??寃쎌슦 鍮뚮
-뱶? 寃利앹? 蹂꾨
-.ai-dev\review-prompt.md:24986:++                               "lastReviewDecision":  "not_started
-",
-.ai-dev\review-prompt.md:24995:++    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:24996:++    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:24998:++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:25010:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:25012:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:25023:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:25024:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:25032:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:25077:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:25078:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:25079:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:25105:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:25106:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:25107:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:25119:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:25120:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:25138:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:25142:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:25145:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:25149:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:25150:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:25155:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:25163:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:25175:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:25204:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:25227:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:25238:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:25247:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:25249:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:25284:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:25287:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:25288:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:25290:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:25293:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:25296:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:25297:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:25305:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:25311:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:25316:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:25317:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:25318:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:25323:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:25329:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:25335:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:25336:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:25341:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:25342:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:25346:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:25347:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:25365:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:25369:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:25394:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:25396:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:25400:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:25408:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:25413:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:25418:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:25421:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:25441:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:25443:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:25454:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:25455:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:25471:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:25525:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:25526:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:25527:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:25548:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:25549:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:25550:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:25558:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:25559:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:25582:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:25587:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:25593:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:25598:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:25599:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:25606:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:25611:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:25624:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:25628:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:25659:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:25681:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:25692:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:25701:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:25703:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:25747:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:25767:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:25770:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:25771:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:25773:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:25776:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:25779:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:25780:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:25788:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:25794:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:25799:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:25800:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:25801:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:25809:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:25815:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:25821:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:25822:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:25827:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:25828:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:25831:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:25841:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:25844:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:25845:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:25861:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:25869:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:25886:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:25888:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:25896:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:25901:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:25906:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:25920:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:25922:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:25941:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:25942:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:25950:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:25991:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:25992:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:25993:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:26014:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:26015:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:26016:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:26024:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:26025:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:26044:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:26049:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:26053:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:26058:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:26059:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:26067:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:26072:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:26084:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:26099:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:26121:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:26143:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:26154:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:26163:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:26165:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:26200:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:26203:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:26204:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:26206:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:26209:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:26212:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:26213:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:26221:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:26227:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:26232:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:26233:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:26234:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:26242:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:26248:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:26254:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:26255:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:26260:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:26261:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:26265:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:26268:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:26269:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:26302:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:26307:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:26315:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:26332:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:26334:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:26342:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:26347:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:26352:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:26364:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:26380:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:26382:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:26393:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:26394:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:26400:++이제 DryRun JSON과 빈 입력 오류, 문법 검증을 다시 확인하겠습니다. 모두 파일 쓰기나 build 없이 끝나는
- 검증입니다.
-.ai-dev\review-prompt.md:26406:++ 하이라이트 추가' -GoalDescription '검색어와 일치하는 업무 제목과 메모를 화면에서 강조 표시한다.' -
-DryRun -Json" in D:\
-ai-apps\plan
-.ai-dev\review-prompt.md:26414:++escription '설명' -DryRun -Json" in D:\ai-apps\planpilot-local
-.ai-dev\review-prompt.md:26439:++Output:
-.ai-dev\review-prompt.md:26484:++                      "name":  "validate-generated-json",
-.ai-dev\review-prompt.md:26485:++                      "command":  "goal/queue/state JSON validatio
-n",
-.ai-dev\review-prompt.md:26496:++                      "command":  ".ai-dev/goal.md, .ai-dev/queue.
-json, .ai-dev/state.
-json",
-.ai-dev\review-prompt.md:26545:++ring\n\n## Manual Verification\n\n- Review the DryRun plan and JSO
-N output.\n- Confirm
- current-tas
-.ai-dev\review-prompt.md:26591:++                               "lastReviewDecision":  "not_started
-",
-.ai-dev\review-prompt.md:26600:++    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:26601:++    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:26603:++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:26615:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:26669:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:26670:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:26671:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:26692:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:26693:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:26694:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:26702:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:26703:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:26722:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:26727:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:26731:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:26736:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:26737:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:26745:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:26750:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:26762:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:26777:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:26799:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:26821:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:26832:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:26841:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:26843:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:26878:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:26881:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:26882:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:26884:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:26887:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:26890:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:26891:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:26899:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:26905:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:26910:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:26911:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:26912:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:26920:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:26926:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:26932:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:26933:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:26938:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:26939:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:26943:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:26946:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:26947:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:26963:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:26971:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:26988:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:26990:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:26998:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:27003:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:27008:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:27021:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:27023:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:27034:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:27035:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:27043:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:27082:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:27083:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:27084:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:27112:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:27129:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:27130:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:27131:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:27135:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:27138:++DryRun JSON은 정상 구조로 나왔습니다. 빈 문자열 검증은 PowerShell 인수 파서가 `''`를 “인수 없
-음”으로 처리해 스크립트 내부까지 도
-달하지 않았으므로, 공
-.ai-dev\review-prompt.md:27146:++alDescription '설명' -DryRun -Json" in D:\ai-apps\planpilot-local
-.ai-dev\review-prompt.md:27165:++    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:27166:++    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:27168:++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:27173:++Output:
-.ai-dev\review-prompt.md:27191:++    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:27192:++    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:27194:++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:27208:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:27209:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:27240:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:27244:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:27251:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:27257:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:27258:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:27264:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:27269:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:27280:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:27312:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:27321:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:27337:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:27348:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:27357:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:27359:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:27394:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:27397:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:27398:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:27400:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:27403:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:27406:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:27407:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:27415:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:27421:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:27426:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:27427:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:27428:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:27436:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:27442:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:27448:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:27449:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:27454:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:27455:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:27459:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:27462:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:27463:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:27501:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:27509:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:27526:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:27528:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:27536:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:27541:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:27546:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:27557:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:27571:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:27584:+-licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -MaxT
-asks 1 -MaxSteps 20 
--Json' in D:
-.ai-dev\review-prompt.md:27594:++ 하이라이트 추가' -GoalDescription '검색어와 일치하는 업무 제목과 메모를 화면에서 강조 표시한다.' -
-DryRun -Json -AllowR
-un" in D:\ai
-.ai-dev\review-prompt.md:27647:++                      "name":  "validate-generated-json",
-.ai-dev\review-prompt.md:27648:++                      "command":  "goal/queue/state JSON validatio
-n",
-.ai-dev\review-prompt.md:27663:++                      "command":  ".ai-dev/goal.md, .ai-dev/queue.
-json, .ai-dev/state.
-json",
-.ai-dev\review-prompt.md:27688:+-                      "command":  "state.lastReviewDecision 확인",
-.ai-dev\review-prompt.md:27697:+-                      "command":  "git status --porcelain -- packa
-ge.json package-lock
-.json",
-.ai-dev\review-prompt.md:27715:+-                      "name":  "commit-result-gate",
-.ai-dev\review-prompt.md:27726:+-e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-.ai-dev\review-prompt.md:27759:++ring\n\n## Manual Verification\n\n- Review the DryRun plan and JSO
-N output.\n- Confirm
- current-tas
-.ai-dev\review-prompt.md:27805:++                               "lastReviewDecision":  "not_started
-",
-.ai-dev\review-prompt.md:27814:++    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:27815:++    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:27817:++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:27829:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:27831:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:27842:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:27843:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:27858:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:27912:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:27913:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:27914:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:27956:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:27995:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:28012:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:28013:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:28014:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:28018:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:28064:++461: - DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:28101:++498:             lastReviewDecision = "not_started"
-.ai-dev\review-prompt.md:28136:++533: - DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:28171:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:28172:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:28207:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:28213:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:28217:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:28222:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:28223:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:28229:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:28234:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:28247:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:28272:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:28286:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:28321:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:28343:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:28371:++572: - Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:28385:++586: function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:28388:++589:     foreach ($candidate in Get-JsonObjectCandidates $RawInput
-) {
-.ai-dev\review-prompt.md:28390:++591:             $parsed = $candidate | ConvertFrom-Json
-.ai-dev\review-prompt.md:28404:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:28415:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:28424:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:28426:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:28499:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:28538:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:28558:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:28561:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:28562:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:28564:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:28567:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:28570:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:28571:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:28579:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:28585:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:28590:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:28591:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:28592:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:28595:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:28630:++Step 4: validate-generated-json
-.ai-dev\review-prompt.md:28631:++  Command: goal/queue/state JSON validation
-.ai-dev\review-prompt.md:28637:++  Command: .ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json
-.ai-dev\review-prompt.md:28687:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:28693:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:28699:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:28700:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:28707:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:28708:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:28714:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:28717:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:28718:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:28773:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:28801:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:28809:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:28826:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:28828:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:28836:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:28841:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:28846:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:28860:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:28874:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:28888:+-- full auto-cycle에 `review-gate`, `package-change-gate`, `commit-r
-esult-gate`를 추가했습니다.
-.ai-dev\review-prompt.md:28889:+-- 리뷰 저장 결과가 `passed`이고 `lastReviewDecision`이 `pass`일 때만 커밋 단계로 진행합
-니다.
-.ai-dev\review-prompt.md:28890:+-- `review-response.json`의 `next_step`이 있으면 `complete_task`일 때만 진행합
-니다.
-.ai-dev\review-prompt.md:28891:+-- `package.json` 또는 `package-lock.json` 변경 감지 시 `package_files_cha
-nged`로 중단합니다.
-.ai-dev\review-prompt.md:28897:++- `queue.tasks`가 3개를 초과하면 생성 JSON 검증에서 실패하도록 상한 검증을 추가했습니다.
-.ai-dev\review-prompt.md:28908:+- 1 -MaxSteps 20 -Json`
-.ai-dev\review-prompt.md:28912:++- `ai-dev-auto-goal.ps1 -DryRun -Json`: 통과, `plan.goalMarkdown`, `
-queue`, `state`, 실행 
-단계 출력 확인
-.ai-dev\review-prompt.md:28913:++- `-AllowRun -DryRun -Json`: full-cycle 호출 계획 표시 확인
-.ai-dev\review-prompt.md:28944:+-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:28946:+-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 ta
-sk 완료 처리를 순서대로 연결하는 
-초안이다.
-.ai-dev\review-prompt.md:28957:+-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:28958:+-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:28964:++`T007: ai-dev-auto-goal.ps1의 DryRun/JSON 출력과 생성 계획 검증을 보강했다. queue
- task 최대 3개 검증을 추가했고
-, PowerShell
-.ai-dev\review-prompt.md:28966:++5.1 인코딩 문제를 피하도록 DryRun 기본 미리보기 문구를 ASCII 출력으로 보정했다. 문법 검증, DryRun
- JSON, AllowRun DryR
-un, 빈 GoalTi
-.ai-dev\review-prompt.md:28978:+- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:29032:+-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:29033:+-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지
- 않는다. `package.json`
- 또는 `package
-.ai-dev\review-prompt.md:29034:+--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 
-없음 등으로 실제 commit has
-h를 기록하지 못하면 
-.ai-dev\review-prompt.md:29055:+- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:29056:+- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:29057:+-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:29065:+-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:29066:+-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:29085:+-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:29090:+-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:29094:+-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:29099:+-+    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:29100:+-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:29108:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:29113:+-+        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:29125:+-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:29140:+++- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:29162:+-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:29184:+-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:29195:+-+    "commit-result-gate",
-.ai-dev\review-prompt.md:29204:+--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:29206:+-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "초기 full cy
-.ai-dev\review-prompt.md:29241:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:29244:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:29245:+-package.json package-lock.json" $false $true 0 "DryRun: package 파일
- 변경 여부를 확인하지 않았습니다."
-.ai-dev\review-prompt.md:29247:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:29250:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:29253:+-+    $script:steps += New-StepResult $stepNumber "complete-task" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:29254:+-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass
- 후 자동 커밋 완료`"" $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:29262:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate/review-response 
-확인" $false $
-.ai-dev\review-prompt.md:29268:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastCommandStatu
-s 확인" $false
-.ai-dev\review-prompt.md:29273:+-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:29274:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "st
-ate.lastReviewDecisi
-on 확인" $fals
-.ai-dev\review-prompt.md:29275:+-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니다
-: $($reviewGate.deci
-sion)"
-.ai-dev\review-prompt.md:29283:+-+    $script:steps += New-StepResult $stepNumber "review-gate" "$r
-eviewResponseRelativ
-ePath next_s
-.ai-dev\review-prompt.md:29289:+-+$script:steps += New-StepResult $stepNumber "review-gate" "state.
-lastReviewDecision 확
-인" $false $f
-.ai-dev\review-prompt.md:29295:+-+        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:29296:+- -- package.json package-lock.json" $false $true 1 "package.json 또
-는 package-lock.json 
-변경이 감지되어 자동 
-.ai-dev\review-prompt.md:29301:+-+    $script:steps += New-StepResult $stepNumber "package-change-g
-ate" "git status --p
-orcelain -- 
-.ai-dev\review-prompt.md:29302:+-package.json package-lock.json" $false $false 1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:29306:+--$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:29309:+-+$script:steps += New-StepResult $stepNumber "package-change-gate"
- "git status --porce
-lain -- pack
-.ai-dev\review-prompt.md:29310:+-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋 
-허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:29326:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:29334:+-+    $script:steps += New-StepResult $stepNumber "commit" "powersh
-ell -ExecutionPolicy
- Bypass -Fil
-.ai-dev\review-prompt.md:29351:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:29353:+-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 1
-.ai-dev\review-prompt.md:29361:+-+    $script:steps += New-StepResult $stepNumber "commit-result-ga
-te" "state.lastComma
-nd/lastCommi
-.ai-dev\review-prompt.md:29366:+-+$script:steps += New-StepResult $stepNumber "commit-result-gate" 
-"state.lastCommand/l
-astCommitHas
-.ai-dev\review-prompt.md:29371:+-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" $
-scriptPaths.complete
-Task @("-Res
-.ai-dev\review-prompt.md:29395:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:29409:++ function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:29418:+-- full auto-cycle에 `review-gate`, `package-change-gate`, `commit-r
-esult-gate`를 추가했습니다.
-.ai-dev\review-prompt.md:29419:+-- 리뷰 저장 결과가 `passed`이고 `lastReviewDecision`이 `pass`일 때만 커밋 단계로 진행합
-니다.
-.ai-dev\review-prompt.md:29420:+-- `review-response.json`의 `next_step`이 있으면 `complete_task`일 때만 진행합
-니다.
-.ai-dev\review-prompt.md:29421:+-- `package.json` 또는 `package-lock.json` 변경 감지 시 `package_files_cha
-nged`로 중단합니다.
-.ai-dev\review-prompt.md:29427:++- `queue.tasks`가 3개를 초과하면 생성 JSON 검증에서 실패하도록 상한 검증을 추가했습니다.
-.ai-dev\review-prompt.md:29437:+ 1 -MaxSteps 20 -Json`
-.ai-dev\review-prompt.md:29441:++- `ai-dev-auto-goal.ps1 -DryRun -Json`: 통과, `plan.goalMarkdown`, `
-queue`, `state`, 실행 
-단계 출력 확인
-.ai-dev\review-prompt.md:29442:++- `-AllowRun -DryRun -Json`: full-cycle 호출 계획 표시 확인
-.ai-dev\review-prompt.md:29464:++`T007: ai-dev-auto-goal.ps1의 DryRun/JSON 출력과 생성 계획 검증을 보강했다. queue
- task 최대 3개 검증을 추가했고
-, PowerShell
-.ai-dev\review-prompt.md:29465:+ 5.1 인코딩 문제를 피하도록 DryRun 기본 미리보기 문구를 ASCII 출력으로 보정했다. 문법 검증, DryRun
- JSON, AllowRun DryR
-un, 빈 GoalTi
-.ai-dev\review-prompt.md:29472:+diff --git a/.ai-dev/codex-review-result.md b/.ai-dev/codex-review-
-result.md
-.ai-dev\review-prompt.md:29474:+--- a/.ai-dev/codex-review-result.md
-.ai-dev\review-prompt.md:29475:++++ b/.ai-dev/codex-review-result.md
-.ai-dev\review-prompt.md:29487:+ - Review response: .ai-dev/review-response.json
-.ai-dev\review-prompt.md:29491:+ ## Output
-.ai-dev\review-prompt.md:29550:++`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prom
-pt.md`를 Codex CLI에 전
-달해 JSON 리뷰를 
-.ai-dev\review-prompt.md:29562:++- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-re
-view-codex.ps1`이 추가된
-다.
-.ai-dev\review-prompt.md:29566:++- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:29580:++- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:29601:++- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
-.ai-dev\review-prompt.md:29625:++- Json 출력이 가능한지 확인
-.ai-dev\review-prompt.md:29626:++- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-.ai-dev\review-prompt.md:29631:++## Test Result
-.ai-dev\review-prompt.md:29633:++# AI Dev Test Result
-.ai-dev\review-prompt.md:29637:++- Overall result: passed
-.ai-dev\review-prompt.md:29694:++ M .ai-dev/codex-result.md
-.ai-dev\review-prompt.md:29695:++ M .ai-dev/codex-review-result.md
-.ai-dev\review-prompt.md:29699:++ M .ai-dev/review-response.json
-.ai-dev\review-prompt.md:29701:++ M .ai-dev/state.json
-.ai-dev\review-prompt.md:29702:++ M .ai-dev/test-result.md
-.ai-dev\review-prompt.md:29710:++ .ai-dev/codex-result.md        | 11795 ++++--------------
-.ai-dev\review-prompt.md:29711:++ .ai-dev/codex-review-result.md | 26294 ++++++++++++++++++++++++++
-++++++++++++-
-.ai-dev\review-prompt.md:29712:++ .ai-dev/state.json             |     8 +-
-.ai-dev\review-prompt.md:29738:+++자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는
-다.
-.ai-dev\review-prompt.md:29739:+++DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보
-기가 포함되므로 파일 작성 전에 생성
-될 goal/task 
-.ai-dev\review-prompt.md:29746:++ 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -D
-ryRun -Json
-.ai-dev\review-prompt.md:29780:++-3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
-.ai-dev\review-prompt.md:29783:+++3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-.ai-dev\review-prompt.md:29784:+++4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enu
-m 값을 검증한다.
-.ai-dev\review-prompt.md:29785:+++5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을
- 작성한다.
-.ai-dev\review-prompt.md:29788:+++8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, 
-`current-task-prompt
-.md`, 결과 파일 
-.ai-dev\review-prompt.md:29799:++ - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:29800:++-- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:29802:+++- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실
-행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:29813:++diff --git a/.ai-dev/codex-result.md b/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:29815:++--- a/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:29816:+++++ b/.ai-dev/codex-result.md
-.ai-dev\review-prompt.md:29830:++ ## Output
-.ai-dev\review-prompt.md:29859:++-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-pro
-mpt.md`를 Codex CLI에 
-전달해 JSON 리뷰를
-.ai-dev\review-prompt.md:29862:+++`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-pro
-mpt.md`를 Codex CLI에 
-전달해 JSON 리뷰를
-.ai-dev\review-prompt.md:29914:+++- Json 출력이 가능한지 확인
-.ai-dev\review-prompt.md:29915:+++- goal/queue/state JSON 파싱 검증을 수행하는지 확인
-.ai-dev\review-prompt.md:29930:++-- package.json 또는 package-lock.json을 수정하지 않는다. 꼭 필요하면 작업을 중단하고 이유
-만 기록한다.
-.ai-dev\review-prompt.md:29962:++-    [switch]$Json,
-.ai-dev\review-prompt.md:29979:++-$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:29980:++-$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:29986:+++환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-.ai-dev\review-prompt.md:30011:++-function Read-JsonFile {
-.ai-dev\review-prompt.md:30019:++-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path
- | ConvertFrom-Json
-.ai-dev\review-prompt.md:30021:++-        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Mess
-age)"
-.ai-dev\review-prompt.md:30056:+++- `queue.json`
-.ai-dev\review-prompt.md:30057:+++- `state.json`
-.ai-dev\review-prompt.md:30058:+++- `test-result.md`
-.ai-dev\review-prompt.md:30078:++-function New-StepResult {
-.ai-dev\review-prompt.md:30088:+++- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예
-시다.
-.ai-dev\review-prompt.md:30089:+++- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예
-시다.
-.ai-dev\review-prompt.md:30101:+++예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 
-복사 원본으로 사용한다.
-.ai-dev\review-prompt.md:30103:++-function New-CycleResult {
-.ai-dev\review-prompt.md:30124:++-function Write-CycleResult {
-.ai-dev\review-prompt.md:30126:++-        [object]$Result
-.ai-dev\review-prompt.md:30130:++-    if ($Json) {
-.ai-dev\review-prompt.md:30131:++-        $Result | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:30138:++-    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:30148:++-    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:30149:++-    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:30150:++-    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:30170:++-    $result = New-CycleResult $Steps $StoppedReason $Completed $E
-xitCode
-.ai-dev\review-prompt.md:30171:++-    Write-CycleResult $result
-.ai-dev\review-prompt.md:30189:++-        $script:steps += New-StepResult $StepNumber $Name $Comman
-d $false $true 0 "Dr
-yRun: 하위 스크립
-.ai-dev\review-prompt.md:30196:++-    $output = & powershell -ExecutionPolicy Bypass -File $ScriptP
-ath @Arguments 2>&1 
-| Out-String
-.ai-dev\review-prompt.md:30198:++-    $message = $output.Trim()
-.ai-dev\review-prompt.md:30199:+++`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-.ai-dev\review-prompt.md:30208:++-    $script:steps += New-StepResult $StepNumber $Name $Command $t
-rue $false $exitCode
- $message
-.ai-dev\review-prompt.md:30209:+++현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:30216:+++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-t
-ask.ps1 -Json
-.ai-dev\review-prompt.md:30231:+++`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.a
-i-dev/current-task-p
-rompt.md` 파일
-.ai-dev\review-prompt.md:30280:++-    $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:30281:++-    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:30312:++-    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:30332:++-    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$MaxT
-asks" $false $true 0
- "T004 초안은 t
-.ai-dev\review-prompt.md:30351:++-    $script:steps += New-StepResult $stepNumber "run-codex" "powe
-rshell -ExecutionPol
-icy Bypass -
-.ai-dev\review-prompt.md:30392:++-    $script:steps += New-StepResult $stepNumber "run-review-codex
-" "powershell -Execu
-tionPolicy B
-.ai-dev\review-prompt.md:30411:++-$script:steps += New-StepResult ($stepNumber + 1) "commit-complet
-e-placeholder" "T005
-에서 ai-dev-co
-.ai-dev\review-prompt.md:30501:++-전환 전에는 `goal.md`, `queue.json`, `state.json`을 확인한다.
-.ai-dev\review-prompt.md:30527:++-- `queue.json`
-.ai-dev\review-prompt.md:30528:++-- `state.json`
-.ai-dev\review-prompt.md:30529:++-- `test-result.md`
-.ai-dev\review-prompt.md:30539:++-- `queue.example.json`: `goal.md`를 task 단위로 분해한 `queue.json` 구조 예
-시다.
-.ai-dev\review-prompt.md:30540:++-- `state.example.json`: 자동 개발 루프의 현재 진행 상태를 기록할 `state.json` 구조 예
-시다.
-.ai-dev\review-prompt.md:30542:++-예시 파일은 실제 실행 상태가 아니며, 초기화 스크립트가 `queue.json`과 `state.json`을 만들 때 
-복사 원본으로 사용한다.
-.ai-dev\review-prompt.md:30574:++-`queue.json`과 `state.json`에서 현재 수행할 task를 사람이 읽기 쉬운 형태로 확인한다.
-.ai-dev\review-prompt.md:30580:++-현재 task 정보를 JSON으로 출력하려면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:30583:++-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-current-t
-ask.ps1 -Json
-.ai-dev\review-prompt.md:30590:++-`goal.md`, `queue.json`, `state.json`을 읽어 현재 task만 수행하도록 지시하는 `.a
-i-dev/current-task-p
-rompt.md` 파일
-.ai-dev\review-prompt.md:30623:++-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-
-task.ps1 -ResultSumm
-ary "T001 분석
-.ai-dev\review-prompt.md:30628:+++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-
-task.ps1 -ResultSumm
-ary "T001 분석
-.ai-dev\review-prompt.md:30637:++-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-
-task.ps1 -TaskId T00
-1 -ResultSum
-.ai-dev\review-prompt.md:30640:+++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-complete-
-task.ps1 -TaskId T00
-1 -ResultSum
-.ai-dev\review-prompt.md:30651:++-task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다.
- Codex 실행, GPT 리뷰, b
-uild/test 실행
-.ai-dev\review-prompt.md:30654:+++task 완료 처리 스크립트는 `queue.json`, `state.json`, `loop-log.md`만 갱신한다.
- Codex 실행, GPT 리뷰, b
-uild/test 실행
-.ai-dev\review-prompt.md:30667:++-결과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm bui
-ld/test/lint를 실행하지 않
-는다.
-.ai-dev\review-prompt.md:30671:+++과를 사람이 직접 수행했다면 수동 요약만 `test-result.md`에 기록할 수 있다. 이 모드는 npm buil
-d/test/lint를 실행하지 않는
-다.
-.ai-dev\review-prompt.md:30685:++-이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다
-. 실제 검증 통과/실패/미수행 판단
-은 `test-resu
-.ai-dev\review-prompt.md:30688:+++이 모드에서 `state.json`의 `lastCommandStatus`는 수동 요약 기록 작업의 성공 여부를 뜻한다
-. 실제 검증 통과/실패/미수행 판단
-은 `test-resu
-.ai-dev\review-prompt.md:30709:++-git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰
-, build/test 실행, git
- commit은 수행하
-.ai-dev\review-prompt.md:30712:+++git diff 저장 스크립트는 `diff.md`와 `state.json`만 갱신한다. Codex 실행, GPT 리뷰
-, build/test 실행, git
- commit은 수행하
-.ai-dev\review-prompt.md:30734:++ git diff/stat 명령의 stderr 경고는 diff 본문과 분리한다. git 명령이 실패하면 기존처럼 `st
-ate.json`에 실패 결과를 기록
-하고 종료한다.
-.ai-dev\review-prompt.md:30764:++ 7. 저장된 decision에 따라 `pass`, `revise`, `blocked` 흐름을 계속 진행한다.
-.ai-dev\review-prompt.md:30791:++ 자동화에서 결과만 읽고 싶으면 `-Json` 옵션을 사용한다.
-.ai-dev\review-prompt.md:30795:++ GPT Chat 또는 외부 리뷰어가 반환한 리뷰 JSON을 `review.md`에 저장하고, decision과 sev
-erity를 `state.json`에
- 반영한다.
-.ai-dev\review-prompt.md:30800:++--response.json
-.ai-dev\review-prompt.md:30804:+++response.json
-.ai-dev\review-prompt.md:30807:++ JSON 문자열이나 클립보드 내용을 직접 사용할 수도 있다.
-.ai-dev\review-prompt.md:30810:++-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-revi
-ew.ps1 -ReviewJson '
-{"decision":
-.ai-dev\review-prompt.md:30815:+++powershell -ExecutionPolicy Bypass -File scripts/ai-dev-save-revi
-ew.ps1 -ReviewJson '
-{"decision":
-.ai-dev\review-prompt.md:30825:++-ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipbo
-ard`를 실행한다. 순수 JSON 
-객체, `json` 코
-.ai-dev\review-prompt.md:30827:++-블록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-.ai-dev\review-prompt.md:30828:+++ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를 클립보드에 복사한 뒤 `-FromClipbo
-ard`를 실행한다. 순수 JSON 
-객체, `json` 코
-.ai-dev\review-prompt.md:30831:+++록, 일반 텍스트 안에 포함된 첫 번째 JSON 객체를 최대한 파싱한다.
-.ai-dev\review-prompt.md:30835:++@@ -895,20 +494,20 @@ ChatGPT 웹 화면에서 받은 리뷰를 저장할 때는 JSON 리뷰 전체를
-.ai-dev\review-prompt.md:30836:++ - Markdown 코드블록을 포함해도 되지만, 가능하면 JSON만 복사한다.
-.ai-dev\review-prompt.md:30837:++ - 오류 메시지의 입력 preview 앞부분을 보고 다른 설명 문장이나 잘린 JSON이 섞였는지 확인한다.
-.ai-dev\review-prompt.md:30839:++-파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 
-수정하지 않는다. review-pro
-mpt나 명령어를 실수
-.ai-dev\review-prompt.md:30842:+++파싱 실패, 빈 입력, 필수 필드 누락 같은 입력 오류는 기본적으로 `review.md`와 `state.json`을 
-수정하지 않는다. review-pro
-mpt나 명령어를 실수
-.ai-dev\review-prompt.md:30859:++ 잘못 실행했다면 ChatGPT가 출력한 JSON 리뷰만 다시 클립보드에 복사한 뒤 `-FromClipboard`를 재
-실행한다.
-.ai-dev\review-prompt.md:30943:++-- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `te
-st-result.md`, `revi
-ew.md` 등을 포함
-.ai-dev\review-prompt.md:30946:+++- 루프 상태 커밋에는 필요할 때 `queue.json`, `state.json`, `loop-log.md`, `te
-st-result.md`, `revi
-ew.md` 등을 포함
-.ai-dev\review-prompt.md:30951:++ - `review-response.json`은 민감 정보나 장문 내용이 포함될 수 있으므로 내용을 확인한 뒤 커밋 여
-부를 결정한다.
-.ai-dev\review-prompt.md:30974:++ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-manual-cy
-cle.ps1 -Json
-.ai-dev\review-prompt.md:31044:++-첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. Ch
-atGPT가 JSON 리뷰만 출력하면
- 그 JSON을 다시 
-.ai-dev\review-prompt.md:31047:+++첫 번째 명령으로 `review-prompt.md`를 클립보드에 복사한 뒤 ChatGPT 웹 화면에 붙여넣는다. Ch
-atGPT가 JSON 리뷰만 출력하면
- 그 JSON을 다시 
-.ai-dev\review-prompt.md:31078:++-시된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-.ai-dev\review-prompt.md:31082:+++된다. 이때 텍스트 출력과 JSON 출력의 step 항목에는 수동 리뷰 브리지 명령이 함께 표시된다.
-.ai-dev\review-prompt.md:31089:++-첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON
- 리뷰를 받은 뒤 두 번째 명령으로 
-클립보드의 리뷰 JSO
-.ai-dev\review-prompt.md:31092:+++첫 번째 명령으로 `review-prompt.md`를 ChatGPT 웹 화면에 붙여넣을 수 있도록 복사하고, JSON
- 리뷰를 받은 뒤 두 번째 명령으로 
-클립보드의 리뷰 JSO
-.ai-dev\review-prompt.md:31136:++-`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하
-고, JSON 출력에서는 `compl
-eted: true`,
-.ai-dev\review-prompt.md:31138:++-`stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/go
-al.md`, `.ai-dev/que
-ue.json`, `.
-.ai-dev\review-prompt.md:31140:++-i-dev/state.json`을 새 목표 기준으로 초기화한다.
-.ai-dev\review-prompt.md:31141:+++`auto-cycle`이 `goal_completed`로 멈추면 텍스트 출력에서는 “목표 완료로 종료” 의미를 표시하
-고, JSON 출력에서는 `compl
-eted: true`,
-.ai-dev\review-prompt.md:31144:+++stoppedReason: "goal_completed"`를 유지한다. 다음 목표를 시작하려면 `.ai-dev/goa
-l.md`, `.ai-dev/queu
-e.json`, `.a
-.ai-dev\review-prompt.md:31147:+++dev/state.json`을 새 목표 기준으로 초기화한다.
-.ai-dev\review-prompt.md:31149:++-`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인
-하고 JSON 리뷰만 다시 복사한다.
- 완료된 goal이나 
-.ai-dev\review-prompt.md:31152:+++`save-review` 실패는 잘못된 클립보드 입력 때문에 발생할 수 있다. 실패 시에는 오류 preview를 확인
-하고 JSON 리뷰만 다시 복사한다.
- 완료된 goal이나 
-.ai-dev\review-prompt.md:31159:++-st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-.ai-dev\review-prompt.md:31163:+++t-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검증은 통과로 기록하지 않는다.
-.ai-dev\review-prompt.md:31167:++@@ -1209,14 +808,14 @@ st-result.md`에 남기는 것을 원칙으로 한다. 실행하지 않은 검
-.ai-dev\review-prompt.md:31174:++-- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.m
-d`를 Codex CLI에 전달해 J
-SON 리뷰를 생성하고
-.ai-dev\review-prompt.md:31187:+++- `scripts/ai-dev-run-review-codex.ps1`: `.ai-dev/review-prompt.m
-d`를 Codex CLI에 전달해 J
-SON 리뷰를 생성하고
-.ai-dev\review-prompt.md:31223:++ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-run-codex
-.ps1 -DryRun -Json
-.ai-dev\review-prompt.md:31248:++-`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev
-/review-response.jso
-n`에 저장한다. 실행
-.ai-dev\review-prompt.md:31253:+++`.ai-dev/review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성하고 `.ai-dev
-/review-response.jso
-n`에 저장한다. 실행
-.ai-dev\review-prompt.md:31267:++-기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-re
-sult.md`에 저장한다. Code
-x 출력에서 첫 번째 
-.ai-dev\review-prompt.md:31269:++-효 JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-.ai-dev\review-prompt.md:31270:+++기본 실행은 `codex exec`를 호출하고 stdout/stderr를 `.ai-dev/codex-review-re
-sult.md`에 저장한다. Code
-x 출력에서 첫 번째 
-.ai-dev\review-prompt.md:31273:+++ JSON 리뷰 객체를 추출해 `.ai-dev/review-response.json`에 저장한다.
-.ai-dev\review-prompt.md:31291:++-리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-r
-esponse.json` 흐름까지 이
-어가려면 `-SaveR
-.ai-dev\review-prompt.md:31294:+++리뷰 JSON 저장 후 `ai-dev-save-review.ps1 -ReviewFile .ai-dev/review-r
-esponse.json` 흐름까지 이
-어가려면 `-SaveR
-.ai-dev\review-prompt.md:31309:++ 자동화에서 결과만 읽고 싶으면 `-Json`을 함께 사용할 수 있다.
-.ai-dev\review-prompt.md:31315:++ - Codex 리뷰 실행 프롬프트에는 파일 수정 금지, git 명령 금지, build/test/lint 실행 금지, 
-JSON 객체만 출력하라는 안전 규칙
-을 추가한다.
-.ai-dev\review-prompt.md:31316:++-- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`
-, `optional_suggesti
-ons`, `next_
-.ai-dev\review-prompt.md:31319:+++- JSON 리뷰에는 `decision`, `severity`, `summary`, `required_changes`
-, `optional_suggesti
-ons`, `next_
-.ai-dev\review-prompt.md:31323:++ - JSON 추출에 실패하면 출력 preview를 보여주고 종료 코드 1로 종료한다.
-.ai-dev\review-prompt.md:31331:++-w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:31335:+++ prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 커밋과 task 완료 처
-리를 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:31346:+++7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastRe
-viewDecision`이 `pass
-`여야 한다.
-.ai-dev\review-prompt.md:31347:+++8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변경
-이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:31358:++ powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:31468:+++리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `c
-omplete_task`가 아니면 자
-동 커밋과 comple
-.ai-dev\review-prompt.md:31471:+++-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되지 
-않는다. `package.json` 
-또는 `package-
-.ai-dev\review-prompt.md:31474:+++ck.json` 변경이 감지되면 `package_files_changed`로 중단한다. 커밋 스크립트가 변경사항 없음
- 등으로 실제 commit hash를
- 기록하지 못하면 `c
-.ai-dev\review-prompt.md:31493:+++자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON 출력은 사람이 읽는 텍스트와 섞지 않는
-다.
-.ai-dev\review-prompt.md:31494:+++DryRun JSON에는 `plan.goalMarkdown`, `plan.queue`, `plan.state` 미리보
-기가 포함되므로 파일 작성 전에 생성
-될 goal/task 
-.ai-dev\review-prompt.md:31503:+++추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." -D
-ryRun -Json
-.ai-dev\review-prompt.md:31549:++-3. 생성된 JSON을 파싱해 필수 필드를 확인한다.
-.ai-dev\review-prompt.md:31552:+++3. Codex 출력에서 `goalMarkdown`, `queue`, `state` JSON을 추출한다.
-.ai-dev\review-prompt.md:31553:+++4. `queue.json`과 `state.json`의 필수 필드, currentTaskId, task 배열, enu
-m 값을 검증한다.
-.ai-dev\review-prompt.md:31554:+++5. `.ai-dev/goal.md`, `.ai-dev/queue.json`, `.ai-dev/state.json`을
- 작성한다.
-.ai-dev\review-prompt.md:31557:+++8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue.json`, `state.json`, 
-`current-task-prompt
-.md`, 결과 파일 
-.ai-dev\review-prompt.md:31570:++ - Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:31571:++-- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:31573:+++- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패하면 파일 작성과 full cycle 실
-행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:31596:++-ipts/ai-dev-complete-task.ps1 -Pattern "param|ResultSummary|TaskI
-d|loop-log|queue|sta
-te" -Context
-.ai-dev\review-prompt.md:31602:++-ipts/ai-dev-save-review.ps1 -Pattern "lastReview|decision|next_st
-ep|state" -Context 0
-,3' in D:\ai
-.ai-dev\review-prompt.md:31615:++-> scripts\ai-dev-complete-task.ps1:3:    [string]$ResultSummary,
-.ai-dev\review-prompt.md:31619:++-> scripts\ai-dev-complete-task.ps1:10:$queueRelativePath = ".ai-d
-ev/queue.json"
-.ai-dev\review-prompt.md:31620:++-> scripts\ai-dev-complete-task.ps1:11:$stateRelativePath = ".ai-d
-ev/state.json"
-.ai-dev\review-prompt.md:31657:++-> scripts\ai-dev-complete-task.ps1:89:$queue = Read-JsonFile $que
-uePath $queueRelativ
-ePath
-.ai-dev\review-prompt.md:31658:++-> scripts\ai-dev-complete-task.ps1:90:$state = Read-JsonFile $sta
-tePath $stateRelativ
-ePath
-.ai-dev\review-prompt.md:31709:++-> scripts\ai-dev-complete-task.ps1:130:$summary = if (Test-HasVal
-ue $ResultSummary) {
- $ResultSumm
-.ai-dev\review-prompt.md:31716:++-> scripts\ai-dev-complete-task.ps1:135:if (Test-HasValue $ResultS
-ummary) {
-.ai-dev\review-prompt.md:31717:++-> scripts\ai-dev-complete-task.ps1:136:    Set-ObjectProperty $co
-mpletedTask "resultS
-ummary" $Res
-.ai-dev\review-prompt.md:31750:++-> scripts\ai-dev-complete-task.ps1:169:Write-JsonFile $queuePath 
-$queue
-.ai-dev\review-prompt.md:31751:++-> scripts\ai-dev-complete-task.ps1:170:Write-JsonFile $statePath 
-$state
-.ai-dev\review-prompt.md:31809:++-> scripts\ai-dev-commit.ps1:170:if (-not $AllowWithoutPassedRevie
-w -and $state.lastRe
-viewDecision
-.ai-dev\review-prompt.md:31812:++-> scripts\ai-dev-commit.ps1:171:    Stop-WithError "state.lastRev
-iewDecision이 pass가 아
-닙니다. 리뷰 결과를 
-.ai-dev\review-prompt.md:31885:++-> scripts\ai-dev-commit.ps1:276:        $addOutput = & git add --
- @selectedFiles 2>&1
- | Out-Strin
-.ai-dev\review-prompt.md:31889:++-  scripts\ai-dev-commit.ps1:279:        $addOutput = & git add -A
- 2>&1 | Out-String
-.ai-dev\review-prompt.md:31918:++-> scripts\ai-dev-save-review.ps1:11:$stateRelativePath = ".ai-dev
-/state.json"
-.ai-dev\review-prompt.md:31924:++-> scripts\ai-dev-save-review.ps1:70:function Save-StateReviewResu
-lt {
-.ai-dev\review-prompt.md:31927:++-> scripts\ai-dev-save-review.ps1:73:        [string]$Decision,
-.ai-dev\review-prompt.md:31931:++-> scripts\ai-dev-save-review.ps1:79:    Set-ObjectProperty $State
- "lastReviewDecision
-" $Decision
-.ai-dev\review-prompt.md:31943:++-> scripts\ai-dev-save-review.ps1:86:    $stateJson = $State | Con
-vertTo-Json -Depth 2
-0
-.ai-dev\review-prompt.md:31944:++-> scripts\ai-dev-save-review.ps1:87:    [System.IO.File]::WriteAl
-lText($statePath, $s
-tateJson, $u
-.ai-dev\review-prompt.md:31953:++--review.ps1 -ReviewJson ''{"decision":"pass","severity":"none","s
-ummary":"OK","requir
-ed_changes":
-.ai-dev\review-prompt.md:31963:++-> scripts\ai-dev-save-review.ps1:282:        "decision",
-.ai-dev\review-prompt.md:31974:++-> scripts\ai-dev-save-review.ps1:301:        $errors += "허용되지 않은 
-decision: $($Review.
-decision)"
-.ai-dev\review-prompt.md:31996:++-> scripts\ai-dev-save-review.ps1:373:- Decision: blocked
-.ai-dev\review-prompt.md:31999:++-  scripts\ai-dev-save-review.ps1:376:- Summary: 리뷰 JSON을 파싱하거나 검증
-하지 못했습니다.
-.ai-dev\review-prompt.md:32000:++-> scripts\ai-dev-save-review.ps1:390:    Save-StateReviewResult $
-State "blocked" "cri
-tical" "fail
-.ai-dev\review-prompt.md:32011:++-  scripts\ai-dev-save-review.ps1:402:        Stop-WithError "리뷰 J
-SON 저장에 실패했습니다: $Err
-orSummary"
-.ai-dev\review-prompt.md:32014:++-> scripts\ai-dev-save-review.ps1:405:    Write-Host "리뷰 입력 오류로 re
-view.md와 state.json을
- 수정하지 않았습니다.
-.ai-dev\review-prompt.md:32019:++-  scripts\ai-dev-save-review.ps1:407:    Stop-WithError "리뷰 JSON 
-저장에 실패했습니다: $ErrorSu
-mmary"
-.ai-dev\review-prompt.md:32027:++-> scripts\ai-dev-save-review.ps1:414:$state = Read-JsonFile $stat
-ePath $stateRelative
-Path
-.ai-dev\review-prompt.md:32042:++-  scripts\ai-dev-save-review.ps1:436:$prettyJson = $review | Conv
-ertTo-Json -Depth 20
-.ai-dev\review-prompt.md:32043:++-> scripts\ai-dev-save-review.ps1:446:- Decision: $($review.decisi
-on)
-.ai-dev\review-prompt.md:32049:++-> scripts\ai-dev-save-review.ps1:467:Save-StateReviewResult $stat
-e $review.decision $
-review.sever
-.ai-dev\review-prompt.md:32053:++-> scripts\ai-dev-save-review.ps1:469:$nextAction = if ($review.de
-cision -eq "pass" -a
-nd $review.n
-.ai-dev\review-prompt.md:32060:++-olicy Bypass -File scripts/ai-dev-complete-task.ps1 -ResultSummar
-y `"리뷰 통과`""
-.ai-dev\review-prompt.md:32061:++-> scripts\ai-dev-save-review.ps1:471:} elseif ($review.decision -
-eq "revise" -and $re
-view.next_st
-.ai-dev\review-prompt.md:32069:++-> scripts\ai-dev-save-review.ps1:473:} elseif ($review.decision -
-eq "blocked" -or $re
-view.next_st
-.ai-dev\review-prompt.md:32078:++-> scripts\ai-dev-save-review.ps1:476:    "decision과 next_step 조합을
- 확인한 뒤 다음 행동을 결정하세요.
-"
-.ai-dev\review-prompt.md:32082:++-> scripts\ai-dev-save-review.ps1:480:Write-Host "Decision: $($rev
-iew.decision)"
-.ai-dev\review-prompt.md:32089:+++.ai-dev\test-result.md
-.ai-dev\review-prompt.md:32090:+++.ai-dev\state.json
-.ai-dev\review-prompt.md:32091:+++.ai-dev\state.example.json
-.ai-dev\review-prompt.md:32093:+++.ai-dev\review-response.json
-.ai-dev\review-prompt.md:32096:+++.ai-dev\queue.json
-.ai-dev\review-prompt.md:32097:+++.ai-dev\queue.example.json
-.ai-dev\review-prompt.md:32104:+++.ai-dev\codex-review-result.md
-.ai-dev\review-prompt.md:32105:+++.ai-dev\codex-result.md
-.ai-dev\review-prompt.md:32135:++-ev/queue.json' in D:\ai-apps\planpilot-local
-.ai-dev\review-prompt.md:32153:++-ev/state.json' in D:\ai-apps\planpilot-local
-.ai-dev\review-prompt.md:32185:++-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/
-리뷰어로 사용하는 완전 자동화 정책,
- 허용/금지 명령, 안
-.ai-dev\review-prompt.md:32213:++-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/
-리뷰어로 사용하는 완전 자동화 정책,
- 허용/금지 명령, 안
-.ai-dev\review-prompt.md:32220:++-                      "description":  "review-prompt.md를 Codex CL
-I에 전달해 JSON 리뷰를 생성하고
- review-resp
-.ai-dev\review-prompt.md:32222:++-nse.json 및 save-review 흐름에 연결하는 ai-dev-run-review-codex.ps1을 추가한다
-.",
-.ai-dev\review-prompt.md:32235:++-                                           "Codex 출력에서 JSON 객체를 추
-출해 저장하는지 확인",
-.ai-dev\review-prompt.md:32241:++-                      "resultSummary":  "T003 완료: review-prompt.m
-d를 codex exec에 전달해 J
-SON 리뷰를 생성하는
-.ai-dev\review-prompt.md:32243:++-ai-dev-run-review-codex.ps1 추가, 문법 검증과 DryRun/Json 확인"
-.ai-dev\review-prompt.md:32271:++-                      "resultSummary":  "T004 완료: make-prompt/run
--codex/check/save-di
-ff/make-revi
-.ai-dev\review-prompt.md:32273:++-w-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가,
- DryRun/Json/AllowCo
-dex 중단 확인"
-.ai-dev\review-prompt.md:32315:++-                                                  ".ai-dev/test-r
-esult.md"
-.ai-dev\review-prompt.md:32321:++-                                           "Codex 리뷰 JSON 저장 확인",
-.ai-dev\review-prompt.md:32347:++-                                           "Json 출력이 가능한지 확인",
-.ai-dev\review-prompt.md:32348:++-                                           "goal/queue/state JSON
- 파싱 검증을 수행하는지 확인",
-.ai-dev\review-prompt.md:32370:++-    "lastReviewDecision":  "not_started",
-.ai-dev\review-prompt.md:32391:+++    [switch]$Json,
-.ai-dev\review-prompt.md:32395:+++    [string]$ResultPath = ".ai-dev/codex-result.md"
-.ai-dev\review-prompt.md:32401:++-$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:32402:++-$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:32428:++-function Read-JsonFile {
-.ai-dev\review-prompt.md:32437:++-        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path
- | ConvertFrom-Json
-.ai-dev\review-prompt.md:32439:++-        Stop-WithError "$RelativePath JSON 파싱에 실패했습니다: $($_.Excep
-tion.Message)"
-.ai-dev\review-prompt.md:32451:+++function Write-RunResult {
-.ai-dev\review-prompt.md:32462:+++    if ($Json) {
-.ai-dev\review-prompt.md:32467:+++            resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-.ai-dev\review-prompt.md:32469:+++        } | ConvertTo-Json -Depth 10
-.ai-dev\review-prompt.md:32480:+++    $statusOutput = & git status --short 2>&1
-.ai-dev\review-prompt.md:32483:+++        $message = "git status 확인에 실패했습니다: $($statusOutput -join 
-"`n")"
-.ai-dev\review-prompt.md:32484:+++        Write-RunResult "run_codex" $false 1 $message
-.ai-dev\review-prompt.md:32488:+++    return @($statusOutput | Where-Object { -not [string]::IsNull
-OrWhiteSpace($_) })
-.ai-dev\review-prompt.md:32517:++-function Save-StateResult {
-.ai-dev\review-prompt.md:32546:++-    $stateJson = $State | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:32547:++-    [System.IO.File]::WriteAllText($statePath, $stateJson, $utf8W
-ithBom)
-.ai-dev\review-prompt.md:32559:++-    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:32565:++-        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:32569:+++$resultRelativePath = ConvertTo-RepoRelativePath $ResultPath
-.ai-dev\review-prompt.md:32571:+++$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-Repo
-Path $ResultPath))
-.ai-dev\review-prompt.md:32581:+++        Write-RunResult "run_codex" $false 1 $message
-.ai-dev\review-prompt.md:32585:++-    return $output.TrimEnd()
-.ai-dev\review-prompt.md:32592:+++        Write-RunResult "run_codex" $false 1 $message
-.ai-dev\review-prompt.md:32598:+++        Write-RunResult "run_codex" $false 1 "프롬프트 생성 스크립트를 찾을 수 
-없습니다: scripts/ai-dev
--make-prompt
-.ai-dev\review-prompt.md:32604:+++    $makePromptOutput = & powershell -ExecutionPolicy Bypass -Fil
-e $makePromptPath 2>
-&1 | Out-Str
-.ai-dev\review-prompt.md:32610:+++        Write-RunResult "run_codex" $false 1 "프롬프트 생성에 실패했습니다: $(
-$makePromptOutput.Tr
-im())"
-.ai-dev\review-prompt.md:32615:+++    Write-RunResult "run_codex" $false 1 "프롬프트 파일을 찾을 수 없습니다: $pr
-omptRelativePath"
-.ai-dev\review-prompt.md:32626:+++- Result: $resultRelativePath
-.ai-dev\review-prompt.md:32632:+++    Write-RunResult "run_codex" $false 0 $message
-.ai-dev\review-prompt.md:32638:+++    Write-RunResult "run_codex" $false 1 "Codex CLI를 찾을 수 없습니다. c
-odex 명령을 사용할 수 있는지 확
-인하세요."
-.ai-dev\review-prompt.md:32642:+++$codexOutput = & codex exec $codexPrompt 2>&1 | Out-String
-.ai-dev\review-prompt.md:32646:+++$resultContent = @"
-.ai-dev\review-prompt.md:32647:+++# Codex Implementation Result
-.ai-dev\review-prompt.md:32657:+++## Output
-.ai-dev\review-prompt.md:32660:+++$codexOutput
-.ai-dev\review-prompt.md:32664:+++[System.IO.File]::WriteAllText($resolvedResultPath, $resultConten
-t, $utf8WithBom)
-.ai-dev\review-prompt.md:32667:+++    Write-RunResult "run_codex" $true 1 "Codex 실행에 실패했습니다. 결과 파일을
- 확인하세요: $resultRelat
-ivePath"
-.ai-dev\review-prompt.md:32672:+++Write-RunResult "run_codex" $true 0 "Codex 실행이 완료되었습니다. 결과 파일: $r
-esultRelativePath"
-.ai-dev\review-prompt.md:32681:+++    [switch]$Json,
-.ai-dev\review-prompt.md:32688:+++    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:32697:+++$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:32698:+++$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:32732:++-$queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:32733:++-$state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:32752:+++function New-StepResult {
-.ai-dev\review-prompt.md:32776:+++function New-AutoGoalResult {
-.ai-dev\review-prompt.md:32802:+++        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-.ai-dev\review-prompt.md:32810:+++function Write-AutoGoalResult {
-.ai-dev\review-prompt.md:32811:+++    param([object]$Result)
-.ai-dev\review-prompt.md:32813:+++    if ($Json) {
-.ai-dev\review-prompt.md:32814:+++        $Result | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:32818:+++    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:32829:+++    if ($null -ne $Result.plan) {
-.ai-dev\review-prompt.md:32831:+++        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)
-"
-.ai-dev\review-prompt.md:32832:+++        Write-Host "  Current task id: $($Result.plan.queue.curre
-ntTaskId)"
-.ai-dev\review-prompt.md:32834:+++        foreach ($task in @($Result.plan.queue.tasks)) {
-.ai-dev\review-prompt.md:32844:+++    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:32845:+++    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:32846:+++    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:32860:+++    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReaso
-n $Completed $ExitCo
-de)
-.ai-dev\review-prompt.md:32891:++-if (-not $AllowWithoutPassedReview -and $state.lastReviewDecision
- -ne "pass") {
-.ai-dev\review-prompt.md:32892:++-    Stop-WithError "state.lastReviewDecision이 pass가 아닙니다. 리뷰 결과를 
-확인하거나 -AllowWithoutP
-assedReview를
-.ai-dev\review-prompt.md:32896:+++    $statusOutput = & git status --short 2>&1
-.ai-dev\review-prompt.md:32899:+++        $script:steps += New-StepResult 2 "dirty-worktree-gate" "
-git status --short" 
-$false $fals
-.ai-dev\review-prompt.md:32902:+++1 "git status failed: $($statusOutput -join "`n")"
-.ai-dev\review-prompt.md:32906:+++    return @($statusOutput | Where-Object { -not [string]::IsNull
-OrWhiteSpace($_) })
-.ai-dev\review-prompt.md:32915:+++function Get-JsonObjectCandidates {
-.ai-dev\review-prompt.md:33194:+++ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSumma
-ry", "lastReviewDeci
-sion", "last
-.ai-dev\review-prompt.md:33201:++-        $addOutput = & git add -- @selectedFiles 2>&1 | Out-Strin
-g
-.ai-dev\review-prompt.md:33204:++-        $addOutput = & git add -A 2>&1 | Out-String
-.ai-dev\review-prompt.md:33211:++-        throw "$addDisplayName 실행에 실패했습니다. exit code: $LASTEXITCO
-DE`n$addOutput"
-.ai-dev\review-prompt.md:33237:++-    $commitOutput = & git commit -m $commitMessage 2>&1 | Out-Str
-ing
-.ai-dev\review-prompt.md:33243:++-        throw "git commit 실행에 실패했습니다. exit code: $LASTEXITCODE`n$
-commitOutput"
-.ai-dev\review-prompt.md:33244:+++    if (@("not_started", "pass", "revise", "blocked") -notcontain
-s $State.lastReviewD
-ecision) {
-.ai-dev\review-prompt.md:33245:+++        $errors += "invalid state.lastReviewDecision: $($State.la
-stReviewDecision)"
-.ai-dev\review-prompt.md:33253:++-    Save-StateResult $state "failed" $errorSummary $null
-.ai-dev\review-prompt.md:33258:++-Save-StateResult $state "passed" "" $commitHash
-.ai-dev\review-prompt.md:33314:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:33315:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:33316:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:33324:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:33325:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:33343:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:33347:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:33350:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:33354:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:33356:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:33361:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:33365:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:33374:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:33407:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:33425:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:33428:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:33447:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:33457:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:33463:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:33467:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:33468:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:33470:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:33475:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:33483:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:33490:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:33492:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:33498:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:33500:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:33504:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:33506:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:33521:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:33538:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:33559:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:33560:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:33561:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:33569:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:33570:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:33588:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:33592:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:33595:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:33599:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:33601:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:33606:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:33610:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:33619:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:33652:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:33670:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:33673:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:33692:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:33702:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:33708:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:33712:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:33713:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:33715:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:33720:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:33728:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:33735:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:33737:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:33743:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:33745:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:33749:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:33751:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:33766:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:33783:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:33869:++-> scripts\ai-dev-auto-cycle-full.ps1:381:    $script:steps += New
--StepResult $stepNum
-ber "review-
-.ai-dev\review-prompt.md:33879:++-> scripts\ai-dev-auto-cycle-full.ps1:386:    $script:steps += New
--StepResult $stepNum
-ber "review-
-.ai-dev\review-prompt.md:33890:++-  scripts\ai-dev-auto-cycle-full.ps1:390:if ($reviewGate.decision
- -ne "pass") {
-.ai-dev\review-prompt.md:33891:++-> scripts\ai-dev-auto-cycle-full.ps1:391:    $script:steps += New
--StepResult $stepNum
-ber "review-
-.ai-dev\review-prompt.md:33894:++-wDecision 확인" $false $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 comp
-lete-task를 실행하지 않습니다
-: $($reviewG
-.ai-dev\review-prompt.md:33896:++-te.decision)"
-.ai-dev\review-prompt.md:33904:++-> scripts\ai-dev-auto-cycle-full.ps1:396:    $script:steps += New
--StepResult $stepNum
-ber "review-
-.ai-dev\review-prompt.md:33916:++-> scripts\ai-dev-auto-cycle-full.ps1:400:$script:steps += New-Ste
-pResult $stepNumber 
-"review-gate
-.ai-dev\review-prompt.md:33924:++-> scripts\ai-dev-auto-cycle-full.ps1:405:        $script:steps +=
- New-StepResult $ste
-pNumber "pac
-.ai-dev\review-prompt.md:33927:++- status --porcelain -- package.json package-lock.json" $false $tr
-ue 1 "package.json 또
-는 package-lo
-.ai-dev\review-prompt.md:33929:++-k.json 변경이 감지되어 자동 커밋
-.ai-dev\review-prompt.md:33936:++-> scripts\ai-dev-auto-cycle-full.ps1:409:    $script:steps += New
--StepResult $stepNum
-ber "package
-.ai-dev\review-prompt.md:33939:++-tus --porcelain -- package.json package-lock.json" $false $false 
-1 $_.Exception.Messa
-ge
-.ai-dev\review-prompt.md:33945:++-> scripts\ai-dev-auto-cycle-full.ps1:413:$script:steps += New-Ste
-pResult $stepNumber 
-"package-cha
-.ai-dev\review-prompt.md:33948:++---porcelain -- package.json package-lock.json" $false $false 0 "p
-ackage 파일 변경 없음. 커밋 
-허용 여부를 확인합니다
-.ai-dev\review-prompt.md:33971:++-> scripts\ai-dev-auto-cycle-full.ps1:426:    $script:steps += New
--StepResult $stepNum
-ber "commit"
-.ai-dev\review-prompt.md:33986:++-ypass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 
-pass 후 자동 커밋 완료`"" $
-scriptPaths.
-.ai-dev\review-prompt.md:34010:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:34011:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:34012:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:34020:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:34021:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:34039:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:34043:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:34046:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:34050:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:34052:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:34057:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:34061:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:34070:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:34103:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:34121:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:34124:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:34143:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:34153:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:34159:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:34163:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:34164:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:34166:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:34171:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:34179:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:34186:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:34188:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:34194:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:34196:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:34200:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:34202:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:34217:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:34234:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:34264:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:34265:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:34266:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:34274:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:34275:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:34293:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:34297:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:34300:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:34304:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:34306:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:34311:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:34315:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:34324:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:34357:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:34377:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:34380:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:34425:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:34429:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:34431:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:34433:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:34437:++-+    $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy Byp
-.ai-dev\review-prompt.md:34439:++-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:34445:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:34452:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:34459:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:34465:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:34466:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:34468:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:34473:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:34481:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:34488:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:34490:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:34496:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:34498:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:34502:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:34504:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:34519:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:34536:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:34557:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:34558:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:34559:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:34567:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:34568:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:34586:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:34590:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:34593:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:34597:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:34599:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:34604:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:34608:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:34617:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:34650:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:34670:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:34673:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:34718:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:34722:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:34724:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:34726:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:34730:++-+    $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy Byp
-.ai-dev\review-prompt.md:34732:++-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:34738:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:34745:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:34752:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:34758:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:34759:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:34761:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:34766:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:34774:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:34781:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:34783:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:34789:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:34791:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:34795:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:34797:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:34812:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:34829:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:34853:++--ResultSummary "T001 
-.ai-dev\review-prompt.md:34862:++--TaskId T001 -ResultS
-.ai-dev\review-prompt.md:34874:++-  .ai-dev\README.md:130:task 완료 처리 스크립트는 `queue.json`, `state.jso
-n`, `loop-log.md`만 갱
-신한다. Codex 실
-.ai-dev\review-prompt.md:34881:++-> .ai-dev\README.md:134:`package.json`에 정의된 build, test, lint scr
-ipt를 확인하고 실행 결과를 `te
-st-result.md
-.ai-dev\review-prompt.md:34883:++-와 `state.json`에 기록한다.
-.ai-dev\review-prompt.md:34888:++-  .ai-dev\README.md:418:리뷰 저장 후 decision별 다음 단계:
-.ai-dev\review-prompt.md:34906:++- CLI에 전달해 JSON 리뷰를 생성
-.ai-dev\review-prompt.md:34924:++-> .ai-dev\README.md:579:- build/check 실패, package 파일 변경, 리뷰 decis
-ion이 `pass`가 아닌 경우 자
-동 커밋하지 않는다.
-.ai-dev\review-prompt.md:34938:++-  .ai-dev\README.md:629:`.ai-dev/review-prompt.md`를 Codex CLI에 전달
-해 JSON 리뷰를 생성하고 `.ai
--dev/review-
-.ai-dev\review-prompt.md:34940:++-esponse.json`에 저장한다. 
-.ai-dev\review-prompt.md:34951:++-view prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:34998:++-  .ai-dev\README.md:743:- 생성된 goal/queue/state JSON 파싱에 실패하면 실제 실
-행으로 넘어가지 않는다.
-.ai-dev\review-prompt.md:35026:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:35027:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:35028:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:35036:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:35037:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:35055:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:35059:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:35062:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:35066:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:35068:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:35073:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:35077:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:35086:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:35119:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:35139:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:35142:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:35187:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:35191:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:35193:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:35195:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:35199:++-+    $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy Byp
-.ai-dev\review-prompt.md:35201:++-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:35207:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:35214:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:35221:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:35227:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:35228:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:35230:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:35235:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:35243:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:35250:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:35252:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:35258:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:35260:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:35264:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:35266:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:35281:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:35298:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:35314:++-w prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:35339:++-자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다.
-.ai-dev\review-prompt.md:35343:++-powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cycl
-e-full.ps1 -DryRun -
-Json
-.ai-dev\review-prompt.md:35359:+++- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:35396:+++            lastReviewDecision = "not_started"
-.ai-dev\review-prompt.md:35409:+++function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:35415:+++    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-.ai-dev\review-prompt.md:35417:+++            $parsed = $candidate | ConvertFrom-Json
-.ai-dev\review-prompt.md:35438:+++                JsonText = ($parsed | ConvertTo-Json -Depth 30)
-.ai-dev\review-prompt.md:35449:+++    throw "No valid auto-goal JSON object was found in Codex outp
-ut."
-.ai-dev\review-prompt.md:35465:+++Return exactly one JSON object and no markdown fences or commenta
-ry.
-.ai-dev\review-prompt.md:35476:+++- queue: an object for .ai-dev/queue.json.
-.ai-dev\review-prompt.md:35477:+++- state: an object for .ai-dev/state.json.
-.ai-dev\review-prompt.md:35503:+++- lastReviewDecision must be "not_started".
-.ai-dev\review-prompt.md:35530:+++        $script:steps += New-StepResult $StepNumber $Name $Comman
-d $false $true 0 "Dr
-yRun: child 
-.ai-dev\review-prompt.md:35553:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:35554:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:35555:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:35563:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:35564:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:35582:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:35586:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:35589:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:35593:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:35595:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:35600:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:35604:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:35613:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:35646:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:35666:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:35669:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:35714:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:35718:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:35720:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:35722:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:35726:++-+    $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy Byp
-.ai-dev\review-prompt.md:35728:++-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:35734:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:35741:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:35748:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:35754:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:35755:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:35757:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:35762:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:35770:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:35777:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:35779:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:35785:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:35787:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:35791:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:35793:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:35808:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:35825:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:35829:+++    $output = & powershell -ExecutionPolicy Bypass -File $ScriptP
-ath @Arguments 2>&1 
-| Out-String
-.ai-dev\review-prompt.md:35831:+++    $message = $output.Trim()
-.ai-dev\review-prompt.md:35848:++-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:35851:++-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 t
-ask 완료 처리를 순서대로 연결하는
- 초안이다.
-.ai-dev\review-prompt.md:35862:++-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastR
-eviewDecision`이 `pas
-s`여야 한다.
-.ai-dev\review-prompt.md:35863:++-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변
-경이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:35870:++- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cyc
-le-full.ps1 -DryRun 
--Json
-.ai-dev\review-prompt.md:35915:++-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `
-complete_task`가 아니면 
-자동 커밋과 compl
-.ai-dev\review-prompt.md:35917:++-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되
-지 않는다. `package.json
-` 또는 `packag
-.ai-dev\review-prompt.md:35919:++--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:35939:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:35940:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:35941:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:35949:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:35950:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:35968:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:35972:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:35975:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:35979:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:35981:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:35986:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:35990:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:35999:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:36032:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:36052:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:36055:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:36100:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:36104:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:36106:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:36108:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:36112:++-+    $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy Byp
-.ai-dev\review-prompt.md:36114:++-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:36120:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:36127:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:36134:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:36140:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:36141:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:36143:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:36148:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:36156:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:36163:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:36165:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:36171:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:36173:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:36177:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:36179:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:36194:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:36211:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:36226:++-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:36229:++-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 t
-ask 완료 처리를 순서대로 연결하는
- 초안이다.
-.ai-dev\review-prompt.md:36240:++-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastR
-eviewDecision`이 `pas
-s`여야 한다.
-.ai-dev\review-prompt.md:36241:++-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변
-경이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:36248:++- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cyc
-le-full.ps1 -DryRun 
--Json
-.ai-dev\review-prompt.md:36293:++-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `
-complete_task`가 아니면 
-자동 커밋과 compl
-.ai-dev\review-prompt.md:36295:++-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되
-지 않는다. `package.json
-` 또는 `packag
-.ai-dev\review-prompt.md:36297:++--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:36317:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:36318:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:36319:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:36327:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:36328:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:36346:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:36350:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:36353:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:36357:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:36359:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:36364:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:36368:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:36377:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:36410:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:36430:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:36433:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:36478:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:36482:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:36484:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:36486:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:36490:++-+    $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy Byp
-.ai-dev\review-prompt.md:36492:++-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:36498:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:36505:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:36512:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:36518:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:36519:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:36521:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:36526:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:36534:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:36541:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:36543:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:36549:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:36551:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:36555:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:36557:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:36572:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:36589:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:36614:++-licy Bypass -File scripts/ai-dev-auto-cycle-full.ps1 -DryRun -Max
-Tasks 1 -MaxSteps 20
- -Json' in D
-.ai-dev\review-prompt.md:36619:+++    $script:steps += New-StepResult $StepNumber $Name $Command $t
-rue $false $exitCode
- $message
-.ai-dev\review-prompt.md:36693:++-                      "command":  "state.lastReviewDecision 확인",
-.ai-dev\review-prompt.md:36702:++-                      "command":  "git status --porcelain -- pack
-age.json package-loc
-k.json",
-.ai-dev\review-prompt.md:36724:++-e-task.ps1 -ResultSummary \"리뷰 pass 후 자동 커밋 완료\"",
-.ai-dev\review-prompt.md:36754:+++    # auto-goal writes goal/queue/state/current prompt/result fil
-es after the initial
- dirty gate.
-.ai-dev\review-prompt.md:36780:+++$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-Repo
-Path $ResultPath))
-.ai-dev\review-prompt.md:36783:+++    $script:steps += New-StepResult 0 "validate-input" "check Goa
-lTitle" $false $fals
-e 1 "GoalTit
-.ai-dev\review-prompt.md:36791:+++    $script:steps += New-StepResult 0 "validate-input" "check Goa
-lDescription" $false
- $false 1 "G
-.ai-dev\review-prompt.md:36799:+++    $script:steps += New-StepResult 0 "validate-input" "check Max
-Tasks" $false $false
- 1 "MaxTasks
-.ai-dev\review-prompt.md:36807:+++    $script:steps += New-StepResult 0 "validate-input" "check Max
-Steps" $false $false
- 1 "MaxSteps
-.ai-dev\review-prompt.md:36819:+++        $script:steps += New-StepResult 0 "prepare" "check requir
-ed scripts" $false $
-false 1 "Mis
-.ai-dev\review-prompt.md:36834:+++$script:steps += New-StepResult 1 "validate-input" "check GoalTit
-le/GoalDescription" 
-$false $fals
-.ai-dev\review-prompt.md:36845:+++        $script:steps += New-StepResult 2 "preview-plan" "local d
-ry-run plan preview"
- $false $fal
-.ai-dev\review-prompt.md:36852:+++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git 
-status --short" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:36856:+++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <au
-to-goal planning pro
-mpt>" $false
-.ai-dev\review-prompt.md:36863:+++    $script:steps += New-StepResult 4 "validate-generated-json" "
-goal/queue/state JSO
-N validation
-.ai-dev\review-prompt.md:36867:+++    $script:steps += New-StepResult 5 "write-state-files" "$goalR
-elativePath, $queueR
-elativePath,
-.ai-dev\review-prompt.md:36871:+++    $script:steps += New-StepResult 6 "make-prompt" "powershell -
-ExecutionPolicy Bypa
-ss -File scr
-.ai-dev\review-prompt.md:36877:+++        $script:steps += New-StepResult 7 "auto-cycle-full" $full
-CycleCommandText $fa
-lse $true 0 
-.ai-dev\review-prompt.md:36885:+++        $script:steps += New-StepResult 7 "auto-cycle-full" $full
-CycleCommandText $fa
-lse $true 0 
-.ai-dev\review-prompt.md:36901:+++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git 
-status --short" $fal
-se $true 1 "
-.ai-dev\review-prompt.md:36909:+++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git 
-status --short" $fal
-se $false 0 
-.ai-dev\review-prompt.md:36914:+++    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git 
-status --short" $fal
-se $false 0 
-.ai-dev\review-prompt.md:36923:+++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <au
-to-goal planning pro
-mpt>" $false
-.ai-dev\review-prompt.md:36932:+++$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-.ai-dev\review-prompt.md:36936:+++$resultContent = @"
-.ai-dev\review-prompt.md:36937:+++# Codex Auto Goal Result
-.ai-dev\review-prompt.md:36947:+++## Output
-.ai-dev\review-prompt.md:36950:+++$codexOutput
-.ai-dev\review-prompt.md:36954:+++[System.IO.File]::WriteAllText($resolvedResultPath, $resultConten
-t, $utf8WithBom)
-.ai-dev\review-prompt.md:36957:+++    $script:steps += New-StepResult 3 "plan-goal" "codex exec <au
-to-goal planning pro
-mpt>" $true 
-.ai-dev\review-prompt.md:36960:+++alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelat
-ivePath $ResultPath)
-"
-.ai-dev\review-prompt.md:36964:+++$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-g
-oal planning prompt>
-" $true $fal
-.ai-dev\review-prompt.md:36967:+++ 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelati
-vePath $ResultPath)"
-.ai-dev\review-prompt.md:36970:+++    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-.ai-dev\review-prompt.md:36972:+++    $preview = Get-InputPreview $codexOutput
-.ai-dev\review-prompt.md:36973:+++    $script:steps += New-StepResult 4 "validate-generated-json" "
-goal/queue/state JSO
-N validation
-.ai-dev\review-prompt.md:36976:+++$false $false 1 "Codex plan JSON extraction failed: $($_.Exceptio
-n.Message) Output pr
-eview: $prev
-.ai-dev\review-prompt.md:36980:+++    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
-.ai-dev\review-prompt.md:36983:+++$script:steps += New-StepResult 4 "validate-generated-json" "goal
-/queue/state JSON va
-lidation" $f
-.ai-dev\review-prompt.md:36986:+++se $false 0 "goalMarkdown, queue, and state JSON validation compl
-eted. currentTaskId:
- $($autoGoal
-.ai-dev\review-prompt.md:36993:+++$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 3
-0
-.ai-dev\review-prompt.md:36994:+++$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 3
-0
-.ai-dev\review-prompt.md:36997:+++[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $u
-tf8WithBom)
-.ai-dev\review-prompt.md:36998:+++[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $u
-tf8WithBom)
-.ai-dev\review-prompt.md:37000:+++$script:steps += New-StepResult 5 "write-state-files" "$goalRelat
-ivePath, $queueRelat
-ivePath, $st
-.ai-dev\review-prompt.md:37011:+++    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycl
-eCommandText $false 
-$true 0 "Ful
-.ai-dev\review-prompt.md:37033:+++    [switch]$Json,
-.ai-dev\review-prompt.md:37044:+++$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:37045:+++$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:37046:+++$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:37067:+++function Read-JsonFile {
-.ai-dev\review-prompt.md:37074:+++        return Get-Content -Raw -Encoding UTF8 -LiteralPath $Path
- | ConvertFrom-Json
-.ai-dev\review-prompt.md:37076:+++        throw "$RelativePath JSON 파싱에 실패했습니다: $($_.Exception.Mess
-age)"
-.ai-dev\review-prompt.md:37120:+++function New-StepResult {
-.ai-dev\review-prompt.md:37142:+++function New-CycleResult {
-.ai-dev\review-prompt.md:37158:+++function Write-CycleResult {
-.ai-dev\review-prompt.md:37160:+++        [object]$Result
-.ai-dev\review-prompt.md:37163:+++    if ($Json) {
-.ai-dev\review-prompt.md:37164:+++        $Result | ConvertTo-Json -Depth 20
-.ai-dev\review-prompt.md:37168:+++    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:37177:+++    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:37178:+++    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:37179:+++    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:37191:+++    $result = New-CycleResult $Steps $StoppedReason $Completed $E
-xitCode
-.ai-dev\review-prompt.md:37192:+++    Write-CycleResult $result
-.ai-dev\review-prompt.md:37206:+++        $script:steps += New-StepResult $StepNumber $Name $Comman
-d $false $true 0 "Dr
-yRun: 하위 스크립
-.ai-dev\review-prompt.md:37213:+++    $output = & powershell -ExecutionPolicy Bypass -File $ScriptP
-ath @Arguments 2>&1 
-| Out-String
-.ai-dev\review-prompt.md:37215:+++    $message = $output.Trim()
-.ai-dev\review-prompt.md:37221:+++    $script:steps += New-StepResult $StepNumber $Name $Command $t
-rue $false $exitCode
- $message
-.ai-dev\review-prompt.md:37248:+++    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:37252:+++        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$ou
-tput"
-.ai-dev\review-prompt.md:37255:+++    return $output.TrimEnd()
-.ai-dev\review-prompt.md:37259:+++    $status = Invoke-GitCapture @("status", "--porcelain", "--", 
-"package.json", "pac
-kage-lock.js
-.ai-dev\review-prompt.md:37262:+++") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:37267:+++    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:37271:+++        $reviewResponse = Read-JsonFile $reviewResponsePath $revi
-ewResponseRelativePa
-th
-.ai-dev\review-prompt.md:37280:+++        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:37295:++-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:37298:++-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 t
-ask 완료 처리를 순서대로 연결하는
- 초안이다.
-.ai-dev\review-prompt.md:37309:++-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastR
-eviewDecision`이 `pas
-s`여야 한다.
-.ai-dev\review-prompt.md:37310:++-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변
-경이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:37317:++- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cyc
-le-full.ps1 -DryRun 
--Json
-.ai-dev\review-prompt.md:37362:++-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `
-complete_task`가 아니면 
-자동 커밋과 compl
-.ai-dev\review-prompt.md:37364:++-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되
-지 않는다. `package.json
-` 또는 `packag
-.ai-dev\review-prompt.md:37366:++--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:37386:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:37387:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:37388:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:37396:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:37397:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:37415:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:37419:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:37422:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:37426:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:37428:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:37433:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:37437:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:37446:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:37479:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:37499:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:37502:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:37547:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:37551:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:37553:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:37555:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:37559:++-+    $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy Byp
-.ai-dev\review-prompt.md:37561:++-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:37567:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:37574:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:37581:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:37587:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:37588:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:37590:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:37595:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:37603:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:37610:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:37612:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:37618:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:37620:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:37624:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:37626:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:37641:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:37658:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:37681:+++    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:37709:+++    $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:37710:+++    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:37741:+++    $script:steps += New-StepResult 0 "prepare" "state/queue 확인" 
-$false $false 1 $_.E
-xception.Mes
-.ai-dev\review-prompt.md:37758:+++    "commit-result-gate",
-.ai-dev\review-prompt.md:37771:+++        $queue = Read-JsonFile $queuePath $queueRelativePath
-.ai-dev\review-prompt.md:37772:+++        $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:37775:+++        $script:steps += New-StepResult $stepNumber "load-task" "
-state/queue 확인" $fal
-se $false 1 
-.ai-dev\review-prompt.md:37795:+++    $script:steps += New-StepResult $stepNumber "task-start" "Max
-Tasks=$MaxTasks" $fa
-lse $false 0
-.ai-dev\review-prompt.md:37824:+++        $script:steps += New-StepResult $stepNumber "run-codex" "
-powershell -Executio
-nPolicy Bypa
-.ai-dev\review-prompt.md:37880:+++        $script:steps += New-StepResult $stepNumber "run-review-c
-odex" "powershell -E
-xecutionPoli
-.ai-dev\review-prompt.md:37900:+++        $script:steps += New-StepResult $stepNumber "review-gate"
- "state.lastReviewDe
-cision 확인" $
-.ai-dev\review-prompt.md:37905:+++        $script:steps += New-StepResult $stepNumber "package-chan
-ge-gate" "git status
- --porcelain
-.ai-dev\review-prompt.md:37908:+++- package.json package-lock.json" $false $true 0 "DryRun: package
- 파일 변경 여부를 확인하지 않았습니
-다."
-.ai-dev\review-prompt.md:37910:+++        $script:steps += New-StepResult $stepNumber "commit" "pow
-ershell -ExecutionPo
-licy Bypass 
-.ai-dev\review-prompt.md:37915:+++        $script:steps += New-StepResult $stepNumber "commit-resul
-t-gate" "state.lastC
-ommand/lastC
-.ai-dev\review-prompt.md:37920:+++        $script:steps += New-StepResult $stepNumber "complete-tas
-k" "powershell -Exec
-utionPolicy 
-.ai-dev\review-prompt.md:37923:+++pass -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"자동 완
-료: Codex 구현, build/c
-heck, Codex 
-.ai-dev\review-prompt.md:37933:+++        $script:steps += New-StepResult $stepNumber "review-gate"
- "state/review-respo
-nse 확인" $fal
-.ai-dev\review-prompt.md:37941:+++        $script:steps += New-StepResult $stepNumber "review-gate"
- "state.lastCommandS
-tatus 확인" $f
-.ai-dev\review-prompt.md:37948:+++    if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:37949:+++        $script:steps += New-StepResult $stepNumber "review-gate"
- "state.lastReviewDe
-cision 확인" $
-.ai-dev\review-prompt.md:37952:+++lse $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않
-습니다: $($reviewGate.d
-ecision)"
-.ai-dev\review-prompt.md:37975:++-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:37978:++-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 t
-ask 완료 처리를 순서대로 연결하는
- 초안이다.
-.ai-dev\review-prompt.md:37989:++-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastR
-eviewDecision`이 `pas
-s`여야 한다.
-.ai-dev\review-prompt.md:37990:++-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변
-경이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:37997:++- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cyc
-le-full.ps1 -DryRun 
--Json
-.ai-dev\review-prompt.md:38042:++-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `
-complete_task`가 아니면 
-자동 커밋과 compl
-.ai-dev\review-prompt.md:38044:++-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되
-지 않는다. `package.json
-` 또는 `packag
-.ai-dev\review-prompt.md:38046:++--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:38066:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:38067:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:38068:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:38076:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:38077:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:38095:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:38099:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:38102:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:38106:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:38108:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:38113:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:38117:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:38126:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:38149:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:38169:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:38181:++-+    "commit-result-gate",
-.ai-dev\review-prompt.md:38190:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:38193:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:38235:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:38239:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:38241:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:38243:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:38247:++-+    $script:steps += New-StepResult $stepNumber "commit-result-g
-ate" "state.lastComm
-and/lastComm
-.ai-dev\review-prompt.md:38251:++-+    $script:steps += New-StepResult $stepNumber "complete-task" 
-"powershell -Executi
-onPolicy Byp
-.ai-dev\review-prompt.md:38253:++-ss -File scripts/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pas
-s 후 자동 커밋 완료`"" $fal
-se $true 0 "
-.ai-dev\review-prompt.md:38262:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate/review-response
- 확인" $false 
-.ai-dev\review-prompt.md:38269:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastCommandStat
-us 확인" $fals
-.ai-dev\review-prompt.md:38275:++-+if ($reviewGate.decision -ne "pass") {
-.ai-dev\review-prompt.md:38276:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:38278:++-e $true 0 "리뷰 decision이 pass가 아니므로 자동 커밋과 complete-task를 실행하지 않습니
-다: $($reviewGate.dec
-ision)"
-.ai-dev\review-prompt.md:38283:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "$
-reviewResponseRelati
-vePath next_
-.ai-dev\review-prompt.md:38291:++-+$script:steps += New-StepResult $stepNumber "review-gate" "state
-.lastReviewDecision 
-확인" $false $
-.ai-dev\review-prompt.md:38298:++-+        $script:steps += New-StepResult $stepNumber "package-cha
-nge-gate" "git statu
-s --porcelai
-.ai-dev\review-prompt.md:38300:++- -- package.json package-lock.json" $false $true 1 "package.json 
-또는 package-lock.json
- 변경이 감지되어 자동
-.ai-dev\review-prompt.md:38306:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:38308:++-package.json package-lock.json" $false $false 1 $_.Exception.Mess
-age
-.ai-dev\review-prompt.md:38312:++-+$script:steps += New-StepResult $stepNumber "package-change-gate
-" "git status --porc
-elain -- pac
-.ai-dev\review-prompt.md:38314:++-age.json package-lock.json" $false $false 0 "package 파일 변경 없음. 커밋
- 허용 여부를 확인합니다."
-.ai-dev\review-prompt.md:38329:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:38350:++-+    $script:steps += New-StepResult $stepNumber "commit-result-g
-ate" "state.lastComm
-and/lastComm
-.ai-dev\review-prompt.md:38353:++-+    Stop-Cycle $script:steps "commit_result_gate_failed" $false 
-1
-.ai-dev\review-prompt.md:38356:++--$script:steps += New-StepResult ($stepNumber + 1) "commit-comple
-te-placeholder" "T00
-5에서 ai-dev-c
-.ai-dev\review-prompt.md:38368:++-+    $script:steps += New-StepResult $stepNumber "commit-result-g
-ate" "state.lastComm
-and/lastComm
-.ai-dev\review-prompt.md:38374:++-+$script:steps += New-StepResult $stepNumber "commit-result-gate"
- "state.lastCommand/
-lastCommitHa
-.ai-dev\review-prompt.md:38381:++-/ai-dev-complete-task.ps1 -ResultSummary `"리뷰 pass 후 자동 커밋 완료`"" 
-$scriptPaths.complet
-eTask @("-Re
-.ai-dev\review-prompt.md:38396:++-ew prompt 생성, Codex JSON 리뷰와 save-review 흐름을 순서대로 연결하는 초안이다.
-.ai-dev\review-prompt.md:38399:++-ew prompt 생성, Codex JSON 리뷰와 save-review, pass 리뷰 이후의 선택 파일 커밋과 t
-ask 완료 처리를 순서대로 연결하는
- 초안이다.
-.ai-dev\review-prompt.md:38410:++-+7. 리뷰 게이트 확인: `state.lastCommandStatus`가 `passed`이고 `state.lastR
-eviewDecision`이 `pas
-s`여야 한다.
-.ai-dev\review-prompt.md:38411:++-+8. package 파일 변경 게이트 확인: `package.json` 또는 `package-lock.json` 변
-경이 있으면 자동 커밋하지 않는다.
-.ai-dev\review-prompt.md:38418:++- powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cyc
-le-full.ps1 -DryRun 
--Json
-.ai-dev\review-prompt.md:38463:++-+리뷰 decision이 `pass`가 아니거나 `review-response.json`의 `next_step`이 `
-complete_task`가 아니면 
-자동 커밋과 compl
-.ai-dev\review-prompt.md:38465:++-te-task를 실행하지 않는다. build/check 실패는 하위 check 단계 실패로 중단되므로 커밋까지 진행되
-지 않는다. `package.json
-` 또는 `packag
-.ai-dev\review-prompt.md:38467:++--lock.json` 변경이 감지되면 `package_files_changed`로 중단한다.
-.ai-dev\review-prompt.md:38487:++- $queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:38488:++- $stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:38489:++-+$reviewResponseRelativePath = ".ai-dev/review-response.json"
-.ai-dev\review-prompt.md:38497:++-     Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:38498:++-     Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:38516:++-+    $output = & git @Arguments 2>&1 | Out-String
-.ai-dev\review-prompt.md:38520:++-+        throw "$DisplayName 실행에 실패했습니다. exit code: $exitCode`n$o
-utput"
-.ai-dev\review-prompt.md:38523:++-+    return $output.TrimEnd()
-.ai-dev\review-prompt.md:38527:++-+    $status = Invoke-GitCapture @("status", "--porcelain", "--",
- "package.json", "pa
-ckage-lock.j
-.ai-dev\review-prompt.md:38529:++-on") "git status --porcelain -- package.json package-lock.json"
-.ai-dev\review-prompt.md:38534:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:38538:++-+        $reviewResponse = Read-JsonFile $reviewResponsePath $rev
-iewResponseRelativeP
-ath
-.ai-dev\review-prompt.md:38547:++-+        decision = [string]$state.lastReviewDecision
-.ai-dev\review-prompt.md:38570:++-+    $state = Read-JsonFile $statePath $stateRelativePath
-.ai-dev\review-prompt.md:38590:++-     $script:steps += New-StepResult 0 "prepare" "state/queue 확인"
- $false $false 1 $_.
-Exception.Me
-.ai-dev\review-prompt.md:38602:++-+    "commit-result-gate",
-.ai-dev\review-prompt.md:38611:++--    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "T004 초안은 
-.ai-dev\review-prompt.md:38614:++-+    $script:steps += New-StepResult 0 "max-tasks" "MaxTasks=$Max
-Tasks" $false $true 
-0 "초기 full c
-.ai-dev\review-prompt.md:38656:++-+    $script:steps += New-StepResult $stepNumber "review-gate" "s
-tate.lastReviewDecis
-ion 확인" $fal
-.ai-dev\review-prompt.md:38660:++-+    $script:steps += New-StepResult $stepNumber "package-change-
-gate" "git status --
-porcelain --
-.ai-dev\review-prompt.md:38662:++-package.json package-lock.json" $false $true 0 "DryRun: package 파
-일 변경 여부를 확인하지 않았습니다.
-"
-.ai-dev\review-prompt.md:38664:++-+    $script:steps += New-StepResult $stepNumber "commit" "powers
-hell -ExecutionPolic
-y Bypass -Fi
-.ai-dev\review-prompt.md:38668:++-+    $script:steps += New-StepResult $stepNumber "commit-result-g
-ate" "state.lastComm
-and/lastComm
-.ai-dev\review-prompt.md:38689:++    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:38690:++    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:38692:++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:38697:++Output:
-.ai-dev\review-prompt.md:38715:++    "queuePath":  ".ai-dev/queue.json",
-.ai-dev\review-prompt.md:38716:++    "statePath":  ".ai-dev/state.json",
-.ai-dev\review-prompt.md:38718:++    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:38723:++  "decision": "revise",
-.ai-dev\review-prompt.md:38733:++cope, Manual Verification과 queue.tasks.verification 문자열이 영어입니다. 이 
-출력은 DryRun 계획 및 JSON
-으로 사용자에게 노출되
-.ai-dev\review-prompt.md:38744:++      "suggestion": "ResultPath가 absolute path로 저장소 밖을 가리킬 수 있으므로,
- 자동화 결과 파일 경로를 저장소 내
-부로 제한하는 검증을 
-.ai-dev\review-prompt.md:38762:++      "DryRun -Json 출력과 빈 GoalTitle 직접 호출 오류 경로는 확인했습니다."
-.ai-dev\review-prompt.md:38770:++  "decision": "revise",
-.ai-dev\review-prompt.md:38778:+Scope, Manual Verification과 queue.tasks.verification 문자열이 영어입니다. 이 
-출력은 DryRun 계획 및 JSON
-으로 사용자에게 노출되
-.ai-dev\review-prompt.md:38787:++      "suggestion": "ResultPath가 absolute path로 저장소 밖을 가리킬 수 있으므로,
- 자동화 결과 파일 경로를 저장소 내
-부로 제한하는 검증을 
-.ai-dev\review-prompt.md:38804:++      "DryRun -Json 출력과 빈 GoalTitle 직접 호출 오류 경로는 확인했습니다."
-.ai-dev\review-prompt.md:38812:+diff --git a/.ai-dev/state.json b/.ai-dev/state.json
-.ai-dev\review-prompt.md:38814:+--- a/.ai-dev/state.json
-.ai-dev\review-prompt.md:38815:++++ b/.ai-dev/state.json
-.ai-dev\review-prompt.md:38824:+-    "lastReviewDecision":  "not_started",
-.ai-dev\review-prompt.md:38826:++    "lastReviewDecision":  "revise",
-.ai-dev\review-prompt.md:38854:+- .ai-dev/review-response.json
-.ai-dev\review-prompt.md:38856:+- .ai-dev/test-result.md
-.ai-dev\review-prompt.md:38865:+- package.json 또는 package-lock.json을 불필요하게 수정했는가
-.ai-dev\review-prompt.md:38877:+## Output Format
-.ai-dev\review-prompt.md:38879:+리뷰 결과는 아래 JSON 형식만 출력한다. JSON 앞뒤에 설명, Markdown 코드 펜스, 추가 문장을 출력하지 않
-는다.
-.ai-dev\review-prompt.md:38882:+  "decision": "pass | revise | blocked",
-.ai-dev\review-prompt.md:38911:+## Decision Rules
-.ai-dev\review-prompt.md:38932:+  .ai-dev\README.md:760:자동화 도구에서 결과만 파싱해야 하면 `-Json`을 함께 사용한다. JSON
- 출력은 사람이 읽는 텍스트와 섞지 
-않는다.
-.ai-dev\review-prompt.md:38933:+  .ai-dev\README.md:761:DryRun JSON에는 `plan.goalMarkdown`, `plan.qu
-eue`, `plan.state` 미
-리보기가 포함되므로 파
-.ai-dev\review-prompt.md:38940:+라이트 추가" -GoalDescription "검색어와 일치하는 업무 제목/메모/프로젝트명을 화면에서 강조 표시한다." 
--DryRun -Json
-.ai-dev\review-prompt.md:38973:+  .ai-dev\README.md:783:3. Codex 출력에서 `goalMarkdown`, `queue`, `sta
-te` JSON을 추출한다.
-.ai-dev\review-prompt.md:38974:+  .ai-dev\README.md:784:4. `queue.json`과 `state.json`의 필수 필드, curre
-ntTaskId, task 배열, e
-num 값을 검증한다.
-.ai-dev\review-prompt.md:38975:+  .ai-dev\README.md:785:5. `.ai-dev/goal.md`, `.ai-dev/queue.json`,
- `.ai-dev/state.json
-`을 작성한다.
-.ai-dev\review-prompt.md:38978:+  .ai-dev\README.md:788:8. auto-goal이 생성한 `.ai-dev/goal.md`, `queue
-.json`, `state.json`
-, `current-t
-.ai-dev\review-prompt.md:38988:+  .ai-dev\README.md:794:- Json 출력은 사람이 읽는 텍스트와 섞지 않는다.
-.ai-dev\review-prompt.md:38989:+  .ai-dev\README.md:795:- 생성된 goal/queue/state JSON 파싱 또는 필드 검증에 실패
-하면 파일 작성과 full cycle
- 실행으로 넘어가지 않
-.ai-dev\review-prompt.md:39027:+    [switch]$Json,
-.ai-dev\review-prompt.md:39034:+    [string]$ResultPath = ".ai-dev/auto-goal-codex-result.md"
-.ai-dev\review-prompt.md:39043:+$queueRelativePath = ".ai-dev/queue.json"
-.ai-dev\review-prompt.md:39044:+$stateRelativePath = ".ai-dev/state.json"
-.ai-dev\review-prompt.md:39084:+function New-StepResult {
-.ai-dev\review-prompt.md:39106:+function New-AutoGoalResult {
-.ai-dev\review-prompt.md:39124:+        resultPath = (ConvertTo-RepoRelativePath $ResultPath)
-.ai-dev\review-prompt.md:39128:+function Write-AutoGoalResult {
-.ai-dev\review-prompt.md:39129:+    param([object]$Result)
-.ai-dev\review-prompt.md:39131:+    if ($Json) {
-.ai-dev\review-prompt.md:39132:+        $Result | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:39136:+    foreach ($step in @($Result.steps)) {
-.ai-dev\review-prompt.md:39145:+    if ($null -ne $Result.plan) {
-.ai-dev\review-prompt.md:39147:+        Write-Host "  Goal title: $($Result.plan.queue.goalTitle)"
-.ai-dev\review-prompt.md:39148:+        Write-Host "  Current task id: $($Result.plan.queue.current
-TaskId)"
-.ai-dev\review-prompt.md:39150:+        foreach ($task in @($Result.plan.queue.tasks)) {
-.ai-dev\review-prompt.md:39160:+    Write-Host "Stopped reason: $($Result.stoppedReason)"
-.ai-dev\review-prompt.md:39161:+    Write-Host "Completed: $($Result.completed)"
-.ai-dev\review-prompt.md:39162:+    Write-Host "Exit code: $($Result.exitCode)"
-.ai-dev\review-prompt.md:39173:+    Write-AutoGoalResult (New-AutoGoalResult $Steps $StoppedReason 
-$Completed $ExitCode
-)
-.ai-dev\review-prompt.md:39198:+    $statusOutput = & git status --short 2>&1
-.ai-dev\review-prompt.md:39201:+        $script:steps += New-StepResult 2 "dirty-worktree-gate" "gi
-t status --short" $f
-alse $false 
-.ai-dev\review-prompt.md:39202:+1 "git status failed: $($statusOutput -join "`n")"
-.ai-dev\review-prompt.md:39206:+    return @($statusOutput | Where-Object { -not [string]::IsNullOr
-WhiteSpace($_) })
-.ai-dev\review-prompt.md:39209:+function Get-JsonObjectCandidates {
-.ai-dev\review-prompt.md:39399:+ailureCount", "lastCommand", "lastCommandStatus", "lastErrorSummary
-", "lastReviewDecisi
-on", "lastRe
-.ai-dev\review-prompt.md:39422:+    if (@("not_started", "pass", "revise", "blocked") -notcontains 
-$State.lastReviewDec
-ision) {
-.ai-dev\review-prompt.md:39423:+        $errors += "invalid state.lastReviewDecision: $($State.last
-ReviewDecision)"
-.ai-dev\review-prompt.md:39488:+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:39525:+            lastReviewDecision = "not_started"
-.ai-dev\review-prompt.md:39560:+- DryRun 怨꾪쉷怨?JSON 異쒕젰??寃?좏븳??
-.ai-dev\review-prompt.md:39599:+- Review the DryRun plan and JSON output.
-.ai-dev\review-prompt.md:39613:+function ConvertFrom-CodexAutoGoalOutput {
-.ai-dev\review-prompt.md:39616:+    foreach ($candidate in Get-JsonObjectCandidates $RawInput) {
-.ai-dev\review-prompt.md:39618:+            $parsed = $candidate | ConvertFrom-Json
-.ai-dev\review-prompt.md:39630:+                JsonText = ($parsed | ConvertTo-Json -Depth 30)
-.ai-dev\review-prompt.md:39636:+    throw "No valid auto-goal JSON object was found in Codex output
-."
-.ai-dev\review-prompt.md:39648:+Return exactly one JSON object and no markdown fences or commentary
-.
-.ai-dev\review-prompt.md:39657:+- queue: an object for .ai-dev/queue.json.
-.ai-dev\review-prompt.md:39658:+- state: an object for .ai-dev/state.json.
-.ai-dev\review-prompt.md:39682:+- lastReviewDecision must be "not_started".
-.ai-dev\review-prompt.md:39706:+        $script:steps += New-StepResult $StepNumber $Name $Command 
-$false $true 0 "DryR
-un: child sc
-.ai-dev\review-prompt.md:39711:+    $output = & powershell -ExecutionPolicy Bypass -File $ScriptPat
-h @Arguments 2>&1 | 
-Out-String
-.ai-dev\review-prompt.md:39713:+    $message = $output.Trim()
-.ai-dev\review-prompt.md:39719:+    $script:steps += New-StepResult $StepNumber $Name $Command $tru
-e $false $exitCode $
-message
-.ai-dev\review-prompt.md:39741:+    # auto-goal writes goal/queue/state/current prompt/result files
- after the initial d
-irty gate.
-.ai-dev\review-prompt.md:39765:+$resolvedResultPath = [System.IO.Path]::GetFullPath((Resolve-RepoPa
-th $ResultPath))
-.ai-dev\review-prompt.md:39768:+    $script:steps += New-StepResult 0 "validate-input" "check GoalT
-itle" $false $false 
-1 "GoalTitle
-.ai-dev\review-prompt.md:39774:+    $script:steps += New-StepResult 0 "validate-input" "check GoalD
-escription" $false $
-false 1 "Goa
-.ai-dev\review-prompt.md:39780:+    $script:steps += New-StepResult 0 "validate-input" "check MaxTa
-sks" $false $false 1
- "MaxTasks m
-.ai-dev\review-prompt.md:39786:+    $script:steps += New-StepResult 0 "validate-input" "check MaxSt
-eps" $false $false 1
- "MaxSteps m
-.ai-dev\review-prompt.md:39796:+        $script:steps += New-StepResult 0 "prepare" "check required
- scripts" $false $fa
-lse 1 "Missi
-.ai-dev\review-prompt.md:39807:+$script:steps += New-StepResult 1 "validate-input" "check GoalTitle
-/GoalDescription" $f
-alse $false 
-.ai-dev\review-prompt.md:39815:+        $script:steps += New-StepResult 2 "preview-plan" "local dry
--run plan preview" $
-false $false
-.ai-dev\review-prompt.md:39820:+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git st
-atus --short" $false
- $true 0 "Dr
-.ai-dev\review-prompt.md:39822:+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto
--goal planning promp
-t>" $false $
-.ai-dev\review-prompt.md:39825:+    $script:steps += New-StepResult 4 "validate-generated-json" "go
-al/queue/state JSON 
-validation" 
-.ai-dev\review-prompt.md:39827:+    $script:steps += New-StepResult 5 "write-state-files" "$goalRel
-ativePath, $queueRel
-ativePath, $
-.ai-dev\review-prompt.md:39829:+    $script:steps += New-StepResult 6 "make-prompt" "powershell -Ex
-ecutionPolicy Bypass
- -File scrip
-.ai-dev\review-prompt.md:39833:+        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCy
-cleCommandText $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:39837:+        $script:steps += New-StepResult 7 "auto-cycle-full" $fullCy
-cleCommandText $fals
-e $true 0 "D
-.ai-dev\review-prompt.md:39849:+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git st
-atus --short" $false
- $true 1 "Wo
-.ai-dev\review-prompt.md:39855:+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git st
-atus --short" $false
- $false 0 "A
-.ai-dev\review-prompt.md:39858:+    $script:steps += New-StepResult 2 "dirty-worktree-gate" "git st
-atus --short" $false
- $false 0 "W
-.ai-dev\review-prompt.md:39865:+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto
--goal planning promp
-t>" $false $
-.ai-dev\review-prompt.md:39872:+$codexOutput = & codex exec $plannerPrompt 2>&1 | Out-String
-.ai-dev\review-prompt.md:39876:+$resultContent = @"
-.ai-dev\review-prompt.md:39877:+# Codex Auto Goal Result
-.ai-dev\review-prompt.md:39887:+## Output
-.ai-dev\review-prompt.md:39890:+$codexOutput
-.ai-dev\review-prompt.md:39894:+[System.IO.File]::WriteAllText($resolvedResultPath, $resultContent,
- $utf8WithBom)
-.ai-dev\review-prompt.md:39897:+    $script:steps += New-StepResult 3 "plan-goal" "codex exec <auto
--goal planning promp
-t>" $true $f
-.ai-dev\review-prompt.md:39898:+alse 1 "Codex goal planning failed. Result: $(ConvertTo-RepoRelativ
-ePath $ResultPath)"
-.ai-dev\review-prompt.md:39902:+$script:steps += New-StepResult 3 "plan-goal" "codex exec <auto-goa
-l planning prompt>" 
-$true $false
-.ai-dev\review-prompt.md:39903:+ 0 "Codex goal planning completed. Result: $(ConvertTo-RepoRelative
-Path $ResultPath)"
-.ai-dev\review-prompt.md:39906:+    $autoGoalPlan = ConvertFrom-CodexAutoGoalOutput $codexOutput
-.ai-dev\review-prompt.md:39908:+    $preview = Get-InputPreview $codexOutput
-.ai-dev\review-prompt.md:39909:+    $script:steps += New-StepResult 4 "validate-generated-json" "go
-al/queue/state JSON 
-validation" 
-.ai-dev\review-prompt.md:39910:+$false $false 1 "Codex plan JSON extraction failed: $($_.Exception.
-Message) Output prev
-iew: $previe
-.ai-dev\review-prompt.md:39912:+    Stop-AutoGoal $script:steps "generated_json_invalid" $false 1
-.ai-dev\review-prompt.md:39915:+$script:steps += New-StepResult 4 "validate-generated-json" "goal/q
-ueue/state JSON vali
-dation" $fal
-.ai-dev\review-prompt.md:39916:+se $false 0 "goalMarkdown, queue, and state JSON validation complet
-ed. currentTaskId: $
-($autoGoalPl
-.ai-dev\review-prompt.md:39921:+$queueJson = $autoGoalPlan.Parsed.queue | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:39922:+$stateJson = $autoGoalPlan.Parsed.state | ConvertTo-Json -Depth 30
-.ai-dev\review-prompt.md:39925:+[System.IO.File]::WriteAllText($resolvedQueuePath, $queueJson, $utf
-8WithBom)
-.ai-dev\review-prompt.md:39926:+[System.IO.File]::WriteAllText($resolvedStatePath, $stateJson, $utf
-8WithBom)
-.ai-dev\review-prompt.md:39928:+$script:steps += New-StepResult 5 "write-state-files" "$goalRelativ
-ePath, $queueRelativ
-ePath, $stat
-.ai-dev\review-prompt.md:39935:+    $script:steps += New-StepResult 7 "auto-cycle-full" $fullCycleC
-ommandText $false $t
-rue 0 "Full 
-.ai-dev\review-prompt.md:39947:+스크립트 본문에서 DryRun 미리보기 생성 경로가 실제 Codex 생성 경로와 별도로 구현되어 있습니다. 이 경로가 사
-용자에게 직접 노출되는 JSON/계획
-이어서 언어와 인코딩 
-.ai-dev\review-prompt.md:39976:+  scripts\ai-dev-auto-goal.ps1:537:- DryRun 계획과 JSON 출력을 검토한다.
-.ai-dev\review-prompt.md:39998:+  scripts\ai-dev-auto-goal.ps1:576:- Review the DryRun plan and JSO
-N output.
-.ai-dev\review-prompt.md:40013:+  scripts\ai-dev-auto-goal.ps1:633:- queue: an object for .ai-dev/q
-ueue.json.
-.ai-dev\review-prompt.md:40014:+  scripts\ai-dev-auto-goal.ps1:634:- state: an object for .ai-dev/s
-tate.json.
-.ai-dev\review-prompt.md:40035:+  scripts\ai-dev-auto-cycle-full.ps1:5:    [switch]$Json,
-.ai-dev\review-prompt.md:40068:+  scripts\ai-dev-auto-cycle-full.ps1:126:        [object]$Result
-.ai-dev\review-prompt.md:40071:+  scripts\ai-dev-auto-cycle-full.ps1:129:    if ($Json) {
-.ai-dev\review-prompt.md:40113:+  scripts\ai-dev-auto-cycle-full.ps1:389:        $script:steps += N
-ew-StepResult $stepN
-umber "run-c
-.ai-dev\review-prompt.md:40140:+> scripts\ai-dev-auto-cycle-full.ps1:424:        $script:steps += N
-ew-StepResult $stepN
-umber "run-r
-.ai-dev\review-prompt.md:40157:+  scripts\ai-dev-auto-cycle-full.ps1:432:        $script:steps += N
-ew-StepResult $stepN
-umber "revie
-.ai-dev\review-prompt.md:40159:+eviewDecision 확인" $false $true 0 "DryRun: 리뷰 pass 여부를 실제 상태에서 읽지 않았
-습니다."
-.ai-dev\review-prompt.md:40171:+  scripts\ai-dev-auto-cycle-full.ps1:492:        $script:steps += N
-ew-StepResult $stepN
-umber "commi
-.ai-dev\review-prompt.md:40182:+  "decision": "revise",
-.ai-dev\review-prompt.md:40190:+ification을 영어 문자열로 다시 덮어씁니다. DryRun 및 -Json 출력에 포함되는 사용자-facing 계획이
-므로 AGENTS.md의 한국어 기본
- 규칙과 New-Cod
-.ai-dev\review-prompt.md:40199:+      "suggestion": "ResultPath가 절대 경로로 저장소 밖을 가리킬 수 있으므로, 결과 파일 쓰기
- 경로를 저장소 내부로 제한하면 자동
-화 스크립트 안전성이 
-.ai-dev\review-prompt.md:40216:+      "DryRun -Json과 빈 GoalTitle 오류 경로는 확인된 것으로 기록되어 있으나, DryRun 출력
- 언어 품질 문제가 남아 있습니다."
-.ai-dev\review-prompt.md:40224:+  "decision": "revise",
-.ai-dev\review-prompt.md:40230:+      "reason": "New-DryRunAutoGoalPlan 내부에서 한국어 goalMarkdown을 만든 뒤
-, 뒤쪽에서 같은 plan.goalM
-arkdown과 verification을 영어 문자열로 다시 덮어씁니다. DryRun 및 -Json 출력에 포함되는 사용자-facing 계획이므로 AGENTS.md의 한국어 기본
- 규칙과 New-CodexAutoGo
-alPrompt의 'Use Korean' 규칙과 일치해야 합니다.",
-.ai-dev\review-prompt.md:40237:+      "suggestion": "ResultPath가 절대 경로로 저장소 밖을 가리킬 수 있으므로, 결과 파일 쓰기
- 경로를 저장소 내부로 제한하면 자동
-화 스크립트 안전성이 더 좋아집니다."
-.ai-dev\review-prompt.md:40253:+      "DryRun -Json과 빈 GoalTitle 오류 경로는 확인된 것으로 기록되어 있으나, DryRun 출력
- 언어 품질 문제가 남아 있습니다."
-.ai-dev\review-prompt.md:40261:diff --git a/.ai-dev/state.json b/.ai-dev/state.json
-.ai-dev\review-prompt.md:40263:--- a/.ai-dev/state.json
-.ai-dev\review-prompt.md:40264:+++ b/.ai-dev/state.json
-.ai-dev\review-prompt.md:40273:-    "lastReviewDecision":  "not_started",
-.ai-dev\review-prompt.md:40275:+    "lastReviewDecision":  "revise",
-.ai-dev\review-prompt.md:40303:- .ai-dev/review-response.json
-.ai-dev\review-prompt.md:40305:- .ai-dev/test-result.md
-.ai-dev\review-prompt.md:40314:- package.json 또는 package-lock.json을 불필요하게 수정했는가
-.ai-dev\review-prompt.md:40326:## Output Format
-.ai-dev\review-prompt.md:40328:리뷰 결과는 아래 JSON 형식만 출력한다. JSON 앞뒤에 설명, Markdown 코드 펜스, 추가 문장을 출력하지 않는
-다.
-.ai-dev\review-prompt.md:40331:  "decision": "pass | revise | blocked",
-.ai-dev\review-prompt.md:40360:## Decision Rules
-
-
-
-codex
-스크립트 본문을 확인했습니다. 이제 출력 형식과 판정 규칙을 정확히 맞추기 위해 리뷰 프롬프트의 마지막 지시 구간만 다시 좁혀서 읽겠습니다.
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$lines = Get-Content -Li
-teralPath '"'D:\\ai-apps\\planpilot-local\\.ai-dev\\review-prompt.md'; for ("'$i = 40320; $i -le 40
-375; $i++) { if ($i -le $lines.Count) { '"'{0}: {1}' -f "'$i, $lines[$i - 1] } }' in D:\ai-apps\pla
-npilot-local
- succeeded in 1854ms:
-40320: 
-40321: - 작은 불확실성도 revise로 판정한다.
-40322: - 테스트가 없거나 skipped이면 revise 후보로 본다.
-40323: - task 범위를 벗어난 파일 수정은 high 이상으로 판정한다.
-40324: - package 변경은 기본적으로 blocked 후보로 본다.
-40325: 
-40326: ## Output Format
-40327: 
-40328: 리뷰 결과는 아래 JSON 형식만 출력한다. JSON 앞뒤에 설명, Markdown 코드 펜스, 추가 문장을 출력하지 않는다.
-40329: 
-40330: {
-40331:   "decision": "pass | revise | blocked",
-40332:   "severity": "none | low | medium | high | critical",
-40333:   "summary": "짧은 요약",
-40334:   "required_changes": [
-40335:     {
-40336:       "file": "파일 경로 또는 unknown",
-40337:       "reason": "수정이 필요한 이유",
-40338:       "suggestion": "구체적 수정 방향"
-40339:     }
-40340:   ],
-40341:   "optional_suggestions": [
-40342:     {
-40343:       "file": "파일 경로 또는 unknown",
-40344:       "suggestion": "선택 개선 의견"
-40345:     }
-40346:   ],
-40347:   "scope_check": {
-40348:     "within_current_task": true,
-40349:     "scope_issues": []
-40350:   },
-40351:   "test_check": {
-40352:     "build_passed": true,
-40353:     "test_passed": true,
-40354:     "lint_passed": true,
-40355:     "issues": []
-40356:   },
-40357:   "next_step": "complete_task | revise_with_codex | stop_for_user"
-40358: }
-40359: 
-40360: ## Decision Rules
-40361: 
-40362: - pass: 현재 task 요구사항 충족, 치명적 문제 없음, 다음 task로 넘어가도 됨
-40363: - revise: 수정이 필요하지만 자동 수정 가능
-40364: - blocked: 요구사항 충돌, 데이터 위험, 패키지 추가, 대규모 리팩터링 등 사용자 판단 필요
-
 codex
-핵심 요구사항은 대체로 구현되어 보입니다. 남은 리스크가 실제 실행 경로에서 재현되는지 확인하기 위해 PowerShell 구문 파싱과 DryRun JSON만 실행하겠습니다. 빌드
-/린트/git 명령은 실행하지 않습니다.
+PowerShell 기본 출력에서 한글이 깨져 보였기 때문에, 파일 자체가 깨진 것인지 콘솔 인코딩 표시 문제인지 UTF-8로 한 번 더 확인하겠습니다.
 exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -GoalTitle '테스트 목표' 
--GoalDescription '테스트 설명' -DryRun -Json" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -GoalTitle '' -GoalD
-escription '테스트 설명' -DryRun -Json" in D:\ai-apps\planpilot-local
-exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command '$tokens = $null; $errors
- = $null; [System.Management.Automation.Language.Parser]::ParseFile('"'D:\\ai-apps\\planpilot-local
-\\scripts\\ai-dev-auto-goal.ps1', [ref]"'$tokens, [ref]$errors) > $null; if ($errors.Count -gt 0) {
- $errors | ForEach-Object { $_.Message } } else { '"'OK' }" in D:\ai-apps\planpilot-local
- succeeded in 2603ms:
-OK
-
- exited 1 in 3438ms:
-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1 : Missing an argument for parameter 'GoalTi
-tle'. Specify a para
-meter of type 'System.String' and try again.
-    + CategoryInfo          : InvalidArgument: (:) [ai-dev-auto-goal.ps1], ParentContainsErrorRecor
-dException
-    + FullyQualifiedErrorId : MissingArgument,ai-dev-auto-goal.ps1
- 
-
-2026-06-09T14:02:02.900752Z ERROR codex_core::tools::router: error=Exit code: 1
-Wall time: 3.4 seconds
-Output:
-D:\ai-apps\planpilot-local\scripts\ai-dev-auto-goal.ps1 : Missing an argument for parameter 'GoalTi
-tle'. Specify a para
-meter of type 'System.String' and try again.
-    + CategoryInfo          : InvalidArgument: (:) [ai-dev-auto-goal.ps1], ParentContainsErrorRecor
-dException
-    + FullyQualifiedErrorId : MissingArgument,ai-dev-auto-goal.ps1
- 
+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
+ 'D:\\ai-apps\\planpilot-local\\src\\views\\TasksView.tsx' -Encoding UTF8 | Select-String -Pattern 
+'placeholder|업무 검색|업무 제목' -Context 2,2" in D:\ai-apps\planpilot-local
+ succeeded in 1168ms:
 
- succeeded in 4191ms:
-{
-    "steps":  [
-                  {
-                      "step":  1,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle/GoalDescription",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  0,
-                      "message":  "Input validation completed: 테스트 목표"
-                  },
-                  {
-                      "step":  2,
-                      "name":  "dirty-worktree-gate",
-                      "command":  "git status --short",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: dirty worktree gate was not executed."
-                  },
-                  {
-                      "step":  3,
-                      "name":  "plan-goal",
-                      "command":  "codex exec \u003cauto-goal planning prompt\u003e",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: Codex goal planning was not executed. Preview currentTas
-kId: T001, task: 테스트 목표"
-                  },
-                  {
-                      "step":  4,
-                      "name":  "validate-generated-json",
-                      "command":  "goal/queue/state JSON validation",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: preview goal/queue/state plan passed local schema valida
-tion."
-                  },
-                  {
-                      "step":  5,
-                      "name":  "write-state-files",
-                      "command":  ".ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: state files were not written."
-                  },
-                  {
-                      "step":  6,
-                      "name":  "make-prompt",
-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-make-pro
-mpt.ps1",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: current-task-prompt.md was not generated."
-                  },
-                  {
-                      "step":  7,
-                      "name":  "auto-cycle-full",
-                      "command":  "powershell -ExecutionPolicy Bypass -File scripts/ai-dev-auto-cyc
-le-full.ps1 -MaxTasks 1 -MaxSteps 20 -AllowDirty",
-                      "executed":  false,
-                      "skipped":  true,
-                      "exitCode":  0,
-                      "message":  "DryRun: full cycle requires -AllowRun or explicit execution opti
-ons. -AllowRun only invokes the full-cycle wrapper; implementation and review still require -AllowC
-odex and -AllowReviewCodex."
-                  }
-              ],
-    "stoppedReason":  "dry_run",
-    "completed":  false,
-    "exitCode":  0,
-    "plan":  {
-                 "goalMarkdown":  "# 목표\n\n테스트 목표\n\n## 배경\n\n테스트 설명\n\n## 성공 기준\n\n- 입력한 목표에서 현재 작
-업을 준비한다.\n- full auto-cycle 실행 전에 현재 작업 프롬프트가 생성된다.\n- full auto-cycle은 명시적인 허용 옵션이 있을 때만 실행된다.\n\n
-## 제약사항\n\n- 서버 API, 로그인, 클라우드 동기화, npm install, 위험한 git 명령은 사용하지 않는다.\n- 저장소 정책과 현재 작업 범위를 따른다.\n\
-n## 범위 제외\n\n- 패키지 변경\n- DB 삭제 또는 초기화\n- 광범위한 리팩터링\n\n## 수동 검증\n\n- DryRun 계획과 JSON 출력을 검토한다.\n- 현재
- 작업 프롬프트 생성 여부를 확인한다.\n- 필요한 경우 빌드와 검증은 별도로 실행한다.",
-                 "queue":  {
-                               "goalTitle":  "테스트 목표",
-                               "goalSource":  ".ai-dev/goal.md",
-                               "createdAt":  "2026-06-09T14:02:02.2292958Z",
-                               "updatedAt":  "2026-06-09T14:02:02.2292958Z",
-                               "currentTaskId":  "T001",
-                               "tasks":  [
-                                             {
-                                                 "id":  "T001",
-                                                 "title":  "테스트 목표",
-                                                 "description":  "테스트 설명",
-                                                 "type":  "implementation",
-                                                 "status":  "in_progress",
-                                                 "priority":  "P0",
-                                                 "dependsOn":  [
+  
+        <label>
+>         업무 검색
+          <input
+            type="text"
+            value={taskSearchQuery}
+            onChange={(event) => onTaskSearchQueryChange(event.target.value)}
+>           placeholder="업무 제목이나 메모로 검색"
+          />
+        </label>
 
-                                                               ],
-                                                 "filesLikelyToChange":  [
 
-                                                                         ],
-                                                 "verification":  [
-                                                                      "변경 범위가 현재 목표와 일치하는지 확인한다.",
-                                                                      "필요한 경우 npm run build를 별도로 실행
-한다."
-                                                                  ],
-                                                 "commitMessage":  null
-                                             }
-                                         ]
-                           },
-                 "state":  {
-                               "goalStatus":  "in_progress",
-                               "currentTaskId":  "T001",
-                               "currentLoop":  0,
-                               "maxLoopsPerTask":  2,
-                               "repeatedFailureCount":  0,
-                               "lastCommand":  null,
-                               "lastCommandStatus":  "not_started",
-                               "lastErrorSummary":  null,
-                               "lastReviewDecision":  "not_started",
-                               "lastReviewSeverity":  null,
-                               "lastCommitHash":  null,
-                               "startedAt":  "2026-06-09T14:02:02.2292958Z",
-                               "updatedAt":  "2026-06-09T14:02:02.2292958Z",
-                               "stopReason":  null
-                           }
-             },
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
-}
 
 codex
-DryRun JSON은 정상 JSON만 출력했고 구문 파싱도 통과했습니다. 빈 제목 경로는 명령행 인용 방식 때문에 PowerShell 파라미터 바인딩 단계에서 먼저 막혔습니다.
- 스크립트 내부 검증은 공백 문자열로 추가 확인하겠습니다.
+placeholder 자체는 한국어로 변경되어 있고 입력 핸들러도 그대로입니다. 문구가 실제 검색 대상과 일치하는지 확인하기 위해 필터 함수만 추가로 확인하겠습니다.
 exec
-"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "powershell -ExecutionPol
-icy Bypass -File 'D:\\ai-apps\\planpilot-local\\scripts\\ai-dev-auto-goal.ps1' -GoalTitle '   ' -Go
-alDescription '테스트 설명' -DryRun -Json" in D:\ai-apps\planpilot-local
- exited 1 in 2876ms:
-{
-    "steps":  [
-                  {
-                      "step":  0,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  1,
-                      "message":  "GoalTitle must not be empty."
-                  }
-              ],
-    "stoppedReason":  "invalid_goal_title",
-    "completed":  false,
-    "exitCode":  1,
-    "plan":  null,
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+"C:\\WINDOWS\\System32\\WindowsPowerShell\\v1.0\\powershell.exe" -Command "Get-Content -LiteralPath
+ 'D:\\ai-apps\\planpilot-local\\src\\utils\\taskFilters.ts' -Encoding UTF8" in D:\ai-apps\planpilot
+-local
+ succeeded in 1158ms:
+import type { Project, Task } from "../types";
+
+export type TaskSortOrder = "none" | "dueDateAsc";
+
+type FilterTasksOptions = {
+  selectedProjectFilter: string;
+  showCompletedTasks: boolean;
+  taskSearchQuery: string;
+  projects?: Project[];
+};
+
+export function filterTasks(
+  tasks: Task[],
+  {
+    selectedProjectFilter,
+    showCompletedTasks,
+    taskSearchQuery,
+    projects = [],
+  }: FilterTasksOptions,
+) {
+  const projectNameById = new Map(
+    projects.map((project) => [project.id, project.name.toLowerCase()]),
+  );
+
+  return tasks.filter((task) => {
+    const matchesProject =
+      selectedProjectFilter === "all" || task.projectId === selectedProjectFilter;
+
+    const searchText = taskSearchQuery.trim().toLowerCase();
+    const searchableText = [
+      task.title,
+      task.memo ?? "",
+      projectNameById.get(task.projectId) ?? "",
+    ].join(" ");
+    const matchesSearch =
+      !searchText || searchableText.toLowerCase().includes(searchText);
+
+    const matchesStatus = showCompletedTasks || task.status !== "done";
+
+    return matchesProject && matchesSearch && matchesStatus;
+  });
 }
 
-2026-06-09T14:02:14.523218Z ERROR codex_core::tools::router: error=Exit code: 1
-Wall time: 2.9 seconds
-Output:
-{
-    "steps":  [
-                  {
-                      "step":  0,
-                      "name":  "validate-input",
-                      "command":  "check GoalTitle",
-                      "executed":  false,
-                      "skipped":  false,
-                      "exitCode":  1,
-                      "message":  "GoalTitle must not be empty."
-                  }
-              ],
-    "stoppedReason":  "invalid_goal_title",
-    "completed":  false,
-    "exitCode":  1,
-    "plan":  null,
-    "goalPath":  ".ai-dev/goal.md",
-    "queuePath":  ".ai-dev/queue.json",
-    "statePath":  ".ai-dev/state.json",
-    "promptPath":  ".ai-dev/current-task-prompt.md",
-    "resultPath":  ".ai-dev/auto-goal-codex-result.md"
+export function sortTasks(tasks: Task[], taskSortOrder: TaskSortOrder) {
+  return [...tasks].sort((a, b) => {
+    if (taskSortOrder === "none") return 0;
+
+    if (taskSortOrder === "dueDateAsc") {
+      if (!a.dueDate && !b.dueDate) return 0;
+      if (!a.dueDate) return 1;
+      if (!b.dueDate) return -1;
+      return new Date(a.dueDate).getTime() - new Date(b.dueDate).getTime();
+    }
+
+    return 0;
+  });
 }
 
-codex
-검증 결과까지 확인했습니다. 리뷰 출력은 프롬프트의 지시대로 JSON만 반환하겠습니다.
 codex
 {
-  "decision": "pass",
-  "severity": "low",
-  "summary": "T007 요구사항인 auto-goal 스크립트 추가, DryRun/Json 출력, 입력 검증, JSON 검증, 명시적 실행 옵션 게이트가 구현되어 있습니
-다.",
-  "required_changes": [],
-  "optional_suggestions": [
+  "decision": "revise",
+  "severity": "high",
+  "summary": "placeholder 변경 자체는 검색 상태/핸들러를 건드리지 않았지만, 실제 검색 대상에 프로젝트명이 포함되는데 문구가 이를 빠뜨리고 있으며 .ai-d
+ev 메타 파일 변경이 task 범위를 크게 벗어납니다.",
+  "required_changes": [
+    {
+      "file": "src/views/TasksView.tsx",
+      "reason": "기존 필터 로직은 업무 제목, 메모, 프로젝트명을 검색 대상으로 사용하므로 현재 placeholder가 실제 동작을 완전히 설명하지 못합니다.",
+      "suggestion": "placeholder를 예: \"업무 제목, 메모, 프로젝트로 검색\"처럼 실제 검색 대상과 일치하게 수정합니다."
+    },
     {
-      "file": "scripts/ai-dev-auto-goal.ps1",
-      "suggestion": "ResultPath가 절대 경로로 저장소 밖을 가리킬 수 있으므로, 결과 파일 쓰기 경로를 저장소 내부로 제한하면 안전성이 더 좋아집니다."
+      "file": ".ai-dev/codex-result.md, .ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
+      "reason": "현재 task는 업무 검색창 placeholder 문구 수정에 한정되어 있는데, 리뷰 대상 diff에 대규모 AI Dev 상태/로그 변경이 포함되어
+ 있습니다.",
+      "suggestion": "이번 task의 최종 변경 범위에서 unrelated .ai-dev 메타 변경을 제외하거나, 워크플로우상 필수 생성물이라면 리뷰/커밋 대상에
+서 명확히 분리합니다."
     }
   ],
+  "optional_suggestions": [],
   "scope_check": {
-    "within_current_task": true,
-    "scope_issues": []
+    "within_current_task": false,
+    "scope_issues": [
+      ".ai-dev 메타 파일의 대규모 변경이 placeholder 문구 수정 범위를 벗어납니다.",
+      "placeholder 문구가 기존 검색 동작의 전체 대상과 완전히 일치하지 않습니다."
+    ]
   },
   "test_check": {
     "build_passed": true,
     "test_passed": false,
     "lint_passed": false,
     "issues": [
-      "제공된 test-result 기준 npm run build는 통과했습니다.",
-      "npm run test와 npm run lint는 BuildOnly 옵션으로 건너뛰었습니다.",
-      "PowerShell 구문 파싱은 직접 확인했고 통과했습니다.",
-      "DryRun -Json 출력과 공백 GoalTitle 오류 경로를 직접 확인했습니다."
+      "npm run build는 통과했습니다.",
+      "npm run test는 BuildOnly 옵션으로 skipped입니다.",
+      "npm run lint는 BuildOnly 옵션으로 skipped입니다."
     ]
   },
-  "next_step": "complete_task"
+  "next_step": "revise_with_codex"
 }
 tokens used
-50,275
+26,436
 {
-  "decision": "pass",
-  "severity": "low",
-  "summary": "T007 요구사항인 auto-goal 스크립트 추가, DryRun/Json 출력, 입력 검증, JSON 검증, 명시적 실행 옵션 게이트가 구현되어 있습니다.",
-  "required_changes": [],
-  "optional_suggestions": [
+  "decision": "revise",
+  "severity": "high",
+  "summary": "placeholder 변경 자체는 검색 상태/핸들러를 건드리지 않았지만, 실제 검색 대상에 프로젝트명이 포함되는데 문구가 이를 빠뜨리고 있으며 .ai-dev 메타 파일 변경이 task 범위를 크게 벗어납니다.",
+  "required_changes": [
     {
-      "file": "scripts/ai-dev-auto-goal.ps1",
-      "suggestion": "ResultPath가 절대 경로로 저장소 밖을 가리킬 수 있으므로, 결과 파일 쓰기 경로를 저장소 내부로 제한하면 안전성이 더 좋아집니다."
+      "file": "src/views/TasksView.tsx",
+      "reason": "기존 필터 로직은 업무 제목, 메모, 프로젝트명을 검색 대상으로 사용하므로 현재 placeholder가 실제 동작을 완전히 설명하지 못합니다.",
+      "suggestion": "placeholder를 예: \"업무 제목, 메모, 프로젝트로 검색\"처럼 실제 검색 대상과 일치하게 수정합니다."
+    },
+    {
+      "file": ".ai-dev/codex-result.md, .ai-dev/goal.md, .ai-dev/queue.json, .ai-dev/state.json",
+      "reason": "현재 task는 업무 검색창 placeholder 문구 수정에 한정되어 있는데, 리뷰 대상 diff에 대규모 AI Dev 상태/로그 변경이 포함되어 있습니다.",
+      "suggestion": "이번 task의 최종 변경 범위에서 unrelated .ai-dev 메타 변경을 제외하거나, 워크플로우상 필수 생성물이라면 리뷰/커밋 대상에서 명확히 분리합니다."
     }
   ],
+  "optional_suggestions": [],
   "scope_check": {
-    "within_current_task": true,
-    "scope_issues": []
+    "within_current_task": false,
+    "scope_issues": [
+      ".ai-dev 메타 파일의 대규모 변경이 placeholder 문구 수정 범위를 벗어납니다.",
+      "placeholder 문구가 기존 검색 동작의 전체 대상과 완전히 일치하지 않습니다."
+    ]
   },
   "test_check": {
     "build_passed": true,
     "test_passed": false,
     "lint_passed": false,
     "issues": [
-      "제공된 test-result 기준 npm run build는 통과했습니다.",
-      "npm run test와 npm run lint는 BuildOnly 옵션으로 건너뛰었습니다.",
-      "PowerShell 구문 파싱은 직접 확인했고 통과했습니다.",
-      "DryRun -Json 출력과 공백 GoalTitle 오류 경로를 직접 확인했습니다."
+      "npm run build는 통과했습니다.",
+      "npm run test는 BuildOnly 옵션으로 skipped입니다.",
+      "npm run lint는 BuildOnly 옵션으로 skipped입니다."
     ]
   },
-  "next_step": "complete_task"
+  "next_step": "revise_with_codex"
 }
 
 ```
\ No newline at end of file
diff --git a/.ai-dev/goal.md b/.ai-dev/goal.md
index ff42c93..4ad00a5 100644
--- a/.ai-dev/goal.md
+++ b/.ai-dev/goal.md
@@ -1,63 +1,25 @@
-# Goal
-
-AI Dev Loop Codex CLI 완전 자동화 도입
-
-## Background
-
-Codex CLI를 사용해 AI Dev Loop의 구현, 검증, 리뷰, 커밋, task 완료 처리를 가능한 한 end-to-end로 자동화한다.
-
-`current-task-prompt.md`를 Codex CLI에 전달해 코드 수정을 수행하고, `review-prompt.md`를 Codex CLI에 전달해 JSON 리뷰를 생성한 뒤, `pass`, `revise`, `blocked` 결과에 따라 자동 분기한다.
-
-초기 버전은 안전을 위해 `MaxTasks 1`, 명시적 Allow 옵션, git status clean 확인, package 변경 감지 중단, build 실패 시 commit 금지 정책을 적용한다.
-
-## Success Criteria
-
-- Codex CLI 실행 정책이 문서화된다.
-- `current-task-prompt.md`를 Codex CLI에 전달하는 `ai-dev-run-codex.ps1`이 추가된다.
-- `review-prompt.md`를 Codex CLI에 전달하고 JSON 리뷰를 저장하는 `ai-dev-run-review-codex.ps1`이 추가된다.
-- Codex 구현, build/check, diff, Codex 리뷰, save-review 흐름이 연결된다.
-- pass 리뷰에서만 자동 커밋과 complete-task를 수행하는 full cycle 초안이 추가된다.
-- git status가 dirty이면 Codex 실행을 중단한다.
-- package.json 또는 package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-- build 실패 시 자동 커밋하지 않는다.
-- git reset, git clean, npm install은 자동 실행하지 않는다.
-- PowerShell 문법 검증이 통과한다.
-- 작은 앱 기능 task로 MaxTasks 1 end-to-end 검증이 가능하다.
-
-## Constraints
-
-- Codex CLI는 구현자와 리뷰어로 사용한다.
-- GPT API 키 없이 진행한다.
-- Cline은 삭제되어 사용하지 않는다.
-- Copilot CLI와 gh는 현재 사용하지 않는다.
-- 자동화는 명시적 Allow 옵션 없이는 위험한 단계를 실행하지 않는다.
-- git status가 dirty이면 Codex 구현 실행을 기본 중단한다.
-- package.json/package-lock.json 변경이 감지되면 자동 커밋하지 않는다.
-- build/check 실패 시 자동 커밋하지 않는다.
-- git reset, git clean, npm install은 자동 실행하지 않는다.
-- src 코드는 각 구현 task에서만 수정한다.
-
-## Out of Scope
-
-- GPT API 직접 호출
-- Cline 사용
-- Copilot CLI 사용
-- gh 또는 GitHub PR 자동 연동
-- git push 자동화
-- git reset/git clean 같은 destructive git 명령
-- npm install 자동 실행
-- package 대량 교체
-- DB 삭제, 초기화, 복원, 마이그레이션 자동화
-
-## Manual Verification
-
-- Codex CLI 실행 정책이 문서화되어 있다.
-- Codex 구현 실행 스크립트가 DryRun과 dirty worktree 중단을 지원한다.
-- Codex 리뷰 실행 스크립트가 JSON 리뷰를 저장하고 save-review 흐름과 연결된다.
-- full auto-cycle DryRun이 전체 단계를 보여준다.
-- full auto-cycle은 MaxTasks 1 제한과 Allow 옵션을 가진다.
-- pass 리뷰가 아니면 자동 커밋하지 않는다.
-- build 실패 시 자동 커밋하지 않는다.
-- package 파일 변경 감지 시 자동 커밋하지 않는다.
-- PowerShell 문법 검증이 통과한다.
-- 작은 task로 MaxTasks 1 end-to-end 검증이 가능하다.
+﻿# 목표
+업무 검색창 placeholder 문구를 더 명확한 한국어 문구로 개선한다.
+
+## 배경
+현재 업무 검색창의 안내 문구가 사용자가 무엇을 검색할 수 있는지 충분히 분명하지 않을 수 있다.
+
+## 성공 기준
+- 업무 검색창 placeholder가 자연스럽고 명확한 한국어 문구로 변경된다.
+- 변경 범위는 placeholder 문구 수정에 한정된다.
+- 기존 검색 동작과 상태 관리 방식은 유지된다.
+
+## 제약사항
+- 기존 코드 스타일과 컴포넌트 구조를 따른다.
+- 사용자-facing UI 문자열은 한국어를 사용한다.
+- 불필요한 리팩터링이나 구조 변경은 하지 않는다.
+- 한 번에 하나의 작은 변경만 수행한다.
+
+## 범위 제외
+- 검색 기능의 동작 변경은 제외한다.
+- 새 화면, 새 상태, 새 저장 구조 추가는 제외한다.
+- 디자인 전반 수정은 제외한다.
+
+## 수동 검증
+- 업무 화면의 검색창에 더 명확한 placeholder 문구가 표시되는지 확인한다.
+- 검색어 입력과 기존 필터링 동작이 그대로 유지되는지 확인한다.
\ No newline at end of file
diff --git a/.ai-dev/queue.json b/.ai-dev/queue.json
index b95eb5f..9a38137 100644
--- a/.ai-dev/queue.json
+++ b/.ai-dev/queue.json
@@ -1,182 +1,28 @@
 ﻿{
-    "goalTitle":  "AI Dev Loop Codex CLI 완전 자동화 도입",
+    "goalTitle":  "업무 검색창 placeholder 문구 개선",
     "goalSource":  ".ai-dev/goal.md",
-    "createdAt":  "2026-06-07T19:59:48.1282711+09:00",
-    "updatedAt":  "2026-06-09T14:05:15.9174264+00:00",
-    "currentTaskId":  null,
+    "createdAt":  "2026-06-09T00:00:00+09:00",
+    "updatedAt":  "2026-06-09T00:00:00+09:00",
+    "currentTaskId":  "T001",
     "tasks":  [
                   {
                       "id":  "T001",
-                      "title":  "Codex CLI 완전 자동화 정책 문서화",
-                      "description":  "Codex CLI를 구현자와 리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전 중단 조건을 문서화한다.",
-                      "type":  "documentation",
-                      "status":  "done",
-                      "priority":  "P0",
-                      "dependsOn":  [
-
-                                    ],
-                      "filesLikelyToChange":  [
-                                                  "docs/ai-dev-loop-policy.md",
-                                                  ".ai-dev/README.md",
-                                                  ".ai-dev/plan.md"
-                                              ],
-                      "verification":  [
-                                           "Codex CLI 사용 방식과 안전 기준이 문서화되었는지 확인",
-                                           "Cline/Copilot/GPT API 없이 진행하는 구조가 명확한지 확인"
-                                       ],
-                      "commitMessage":  "Document Codex CLI automation policy",
-                      "completedAt":  "2026-06-07T12:46:13.9691878+00:00",
-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전 중단 조건 문서화"
-                  },
-                  {
-                      "id":  "T002",
-                      "title":  "Codex 구현 실행 스크립트 추가",
-                      "description":  "current-task-prompt.md를 Codex CLI에 전달해 현재 task 구현을 수행하는 ai-dev-run-codex.ps1을 추가한다.",
-                      "type":  "implementation",
-                      "status":  "done",
-                      "priority":  "P0",
-                      "dependsOn":  [
-                                        "T001"
-                                    ],
-                      "filesLikelyToChange":  [
-                                                  "scripts/ai-dev-run-codex.ps1",
-                                                  ".ai-dev/README.md"
-                                              ],
-                      "verification":  [
-                                           "DryRun에서 실행 예정 명령만 출력되는지 확인",
-                                           "current-task-prompt.md가 없으면 명확히 안내하는지 확인",
-                                           "git status dirty 상태에서 기본 중단하는지 확인",
-                                           "PowerShell 문법 검증"
-                                       ],
-                      "commitMessage":  "Add Codex task runner",
-                      "completedAt":  "2026-06-07T12:48:36.8786703+00:00",
-                      "resultSummary":  "T001 완료: Codex CLI를 구현자/리뷰어로 사용하는 완전 자동화 정책, 허용/금지 명령, 안전 중단 조건 문서화"
-                  },
-                  {
-                      "id":  "T003",
-                      "title":  "Codex 리뷰 실행 스크립트 추가",
-                      "description":  "review-prompt.md를 Codex CLI에 전달해 JSON 리뷰를 생성하고 review-response.json 및 save-review 흐름에 연결하는 ai-dev-run-review-codex.ps1을 추가한다.",
-                      "type":  "implementation",
-                      "status":  "done",
-                      "priority":  "P0",
-                      "dependsOn":  [
-                                        "T002"
-                                    ],
-                      "filesLikelyToChange":  [
-                                                  "scripts/ai-dev-run-review-codex.ps1",
-                                                  ".ai-dev/README.md"
-                                              ],
-                      "verification":  [
-                                           "DryRun에서 실행 예정 명령만 출력되는지 확인",
-                                           "Codex 출력에서 JSON 객체를 추출해 저장하는지 확인",
-                                           "save-review와 연결되는지 확인",
-                                           "PowerShell 문법 검증"
-                                       ],
-                      "commitMessage":  "Add Codex review runner",
-                      "completedAt":  "2026-06-07T13:23:42.7240601+00:00",
-                      "resultSummary":  "T003 완료: review-prompt.md를 codex exec에 전달해 JSON 리뷰를 생성하는 ai-dev-run-review-codex.ps1 추가, 문법 검증과 DryRun/Json 확인"
-                  },
-                  {
-                      "id":  "T004",
-                      "title":  "full auto-cycle 초안 추가",
-                      "description":  "make-prompt, run-codex, check, save-diff, make-review-prompt, run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안을 추가한다.",
+                      "title":  "업무 검색창 placeholder 문구 수정",
+                      "description":  "업무 검색창 입력 요소를 찾아 placeholder 문구만 더 명확한 한국어 표현으로 변경한다.",
                       "type":  "implementation",
-                      "status":  "done",
-                      "priority":  "P0",
+                      "status":  "in_progress",
+                      "priority":  "P1",
                       "dependsOn":  [
-                                        "T003"
-                                    ],
-                      "filesLikelyToChange":  [
-                                                  "scripts/ai-dev-auto-cycle-full.ps1",
-                                                  ".ai-dev/README.md"
-                                              ],
-                      "verification":  [
-                                           "DryRun에서 전체 단계가 표시되는지 확인",
-                                           "MaxTasks 제한이 있는지 확인",
-                                           "AllowCodex, AllowReviewCodex, AllowCommit 같은 명시적 옵션이 있는지 확인",
-                                           "PowerShell 문법 검증"
-                                       ],
-                      "commitMessage":  "Add full AI dev auto cycle draft",
-                      "completedAt":  "2026-06-07T13:57:13.9217915+00:00",
-                      "resultSummary":  "T004 완료: make-prompt/run-codex/check/save-diff/make-review-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가, DryRun/Json/AllowCodex 중단 확인"
-                  },
-                  {
-                      "id":  "T005",
-                      "title":  "자동 커밋과 task 완료 연결",
-                      "description":  "리뷰 결과가 pass일 때만 선택 파일 커밋과 complete-task를 수행하도록 full auto-cycle에 연결한다.",
-                      "type":  "implementation",
-                      "status":  "done",
-                      "priority":  "P0",
-                      "dependsOn":  [
-                                        "T004"
-                                    ],
-                      "filesLikelyToChange":  [
-                                                  "scripts/ai-dev-auto-cycle-full.ps1",
-                                                  ".ai-dev/README.md"
-                                              ],
-                      "verification":  [
-                                           "pass가 아니면 commit하지 않는지 확인",
-                                           "build 실패 시 commit하지 않는지 확인",
-                                           "package 파일 변경 감지 시 중단하는지 확인",
-                                           "complete-task가 pass 이후에만 실행되는지 확인",
-                                           "PowerShell 문법 검증"
-                                       ],
-                      "commitMessage":  "Connect pass review to auto commit and task completion",
-                      "completedAt":  "2026-06-08T14:11:23.8613636+00:00",
-                      "resultSummary":  "T004 완료: make-prompt/run-codex/check/save-diff/make-review-prompt/run-review-codex를 연결하는 ai-dev-auto-cycle-full.ps1 초안 추가, DryRun/Json/AllowCodex 중단 확인"
-                  },
-                  {
-                      "id":  "T006",
-                      "title":  "작은 앱 task로 end-to-end 검증",
-                      "description":  "작은 문서 또는 UI 변경 task를 MaxTasks 1로 실행해 Codex 구현부터 리뷰, 커밋, 완료 처리까지 검증한다.",
-                      "type":  "verification",
-                      "status":  "done",
-                      "priority":  "P0",
-                      "dependsOn":  [
-                                        "T005"
-                                    ],
-                      "filesLikelyToChange":  [
-                                                  ".ai-dev/plan.md",
-                                                  ".ai-dev/loop-log.md",
-                                                  ".ai-dev/test-result.md"
-                                              ],
-                      "verification":  [
-                                           "full auto-cycle DryRun 확인",
-                                           "MaxTasks 1 실제 실행 확인",
-                                           "Codex 구현 로그 확인",
-                                           "Codex 리뷰 JSON 저장 확인",
-                                           "pass 시 커밋/complete-task 확인",
-                                           "git status clean 확인"
-                                       ],
-                      "commitMessage":  null,
-                      "completedAt":  "2026-06-08T15:17:52.2846812+00:00",
-                      "resultSummary":  "T006 완료: full auto-cycle DryRun/Json 구조 검증 및 Codex runner 인자 길이 버그 수정 확인, 실제 end-to-end 검증은 T007 auto-goal 이후 작은 앱 목표로 수행"
-                  },
-                  {
-                      "id":  "T007",
-                      "title":  "목표 입력 기반 자동 goal 실행 스크립트 추가",
-                      "description":  "사용자가 GoalTitle과 GoalDescription만 입력하면 Codex CLI를 이용해 goal/queue/state를 생성하고, current-task-prompt 생성 후 auto-cycle-full을 실행할 수 있게 하는 ai-dev-auto-goal.ps1을 추가한다.",
-                      "type":  "implementation",
-                      "status":  "done",
-                      "priority":  "P0",
-                      "dependsOn":  [
-                                        "T005"
+
                                     ],
                       "filesLikelyToChange":  [
-                                                  "scripts/ai-dev-auto-goal.ps1",
-                                                  ".ai-dev/README.md"
+                                                  "src/App.tsx"
                                               ],
                       "verification":  [
-                                           "DryRun에서 생성될 goal/task 계획과 실행 단계가 표시되는지 확인",
-                                           "Json 출력이 가능한지 확인",
-                                           "goal/queue/state JSON 파싱 검증을 수행하는지 확인",
-                                           "auto-cycle-full 호출은 -AllowRun 또는 명시적 실행 옵션에서만 수행되는지 확인",
-                                           "GoalTitle/GoalDescription이 비어 있으면 오류로 종료하는지 확인",
-                                           "PowerShell 문법 검증"
+                                           "업무 검색창 placeholder 문구가 변경되었는지 확인한다.",
+                                           "검색 입력 동작과 기존 필터링 동작이 유지되는지 확인한다."
                                        ],
-                      "commitMessage":  "Add automatic goal runner",
-                      "completedAt":  "2026-06-09T14:05:15.9174264+00:00",
-                      "resultSummary":  "T007 완료: 목표 제목/설명 입력 기반 auto-goal 실행 스크립트 추가, DryRun/Json 출력, 입력 검증, dirty 처리, full auto-cycle 연결 및 Codex 리뷰 pass 확인"
+                      "commitMessage":  "Improve task search placeholder"
                   }
               ]
 }
\ No newline at end of file
diff --git a/.ai-dev/state.json b/.ai-dev/state.json
index b066f2f..abb0def 100644
--- a/.ai-dev/state.json
+++ b/.ai-dev/state.json
@@ -1,16 +1,16 @@
 ﻿{
-    "goalStatus":  "completed",
-    "currentTaskId":  null,
+    "goalStatus":  "in_progress",
+    "currentTaskId":  "T001",
     "currentLoop":  0,
     "maxLoopsPerTask":  2,
     "repeatedFailureCount":  0,
-    "lastCommand":  "complete-task",
+    "lastCommand":  "npm run build",
     "lastCommandStatus":  "passed",
     "lastErrorSummary":  "",
-    "lastReviewDecision":  "pass",
-    "lastReviewSeverity":  "low",
+    "lastReviewDecision":  "revise",
+    "lastReviewSeverity":  "high",
     "lastCommitHash":  null,
-    "startedAt":  "2026-06-07T19:59:48.1282711+09:00",
-    "updatedAt":  "2026-06-09T14:05:15.9174264+00:00",
-    "stopReason":  "all_tasks_completed"
+    "startedAt":  "2026-06-09T00:00:00+09:00",
+    "updatedAt":  "2026-06-09T14:47:32.1707435+00:00",
+    "stopReason":  null
 }
\ No newline at end of file
diff --git a/src/views/TasksView.tsx b/src/views/TasksView.tsx
index f2f7fd8..d887bf9 100644
--- a/src/views/TasksView.tsx
+++ b/src/views/TasksView.tsx
@@ -112,7 +112,7 @@ export function TasksView({
           type="text"
           value={taskSearchQuery}
           onChange={(event) => onTaskSearchQueryChange(event.target.value)}
-          placeholder="업무 제목 검색"
+          placeholder="업무 제목 또는 메모 내용을 검색하세요"
         />
       </label>
```

## Staged Diff Stat

```text
변경 없음
```

## Staged Diff

```text
변경 없음
```

## Skipped Generated AI Dev Artifact Diffs

- .ai-dev/current-task-prompt.md
- .ai-dev/diff.md
- .ai-dev/review-prompt.md
- .ai-dev/review-response.json
- .ai-dev/review.md
- .ai-dev/test-result.md